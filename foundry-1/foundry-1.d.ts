
/**
 * Type Definitions for Gjs (https://gjs.guide/)
 *
 * These type definitions are automatically generated, do not edit them by hand.
 * If you found a bug fix it in `ts-for-gir` or create a bug report on https://github.com/gjsify/ts-for-gir
 *
 * The based EJS template file is used for the generated .d.ts file of each GIR module like Gtk-4.0, GObject-2.0, ...
 */

import '@girs/gjs';

// Module dependencies
import type Peas from '@girs/peas-2';
import type Gio from '@girs/gio-2.0';
import type GObject from '@girs/gobject-2.0';
import type GLib from '@girs/glib-2.0';
import type GModule from '@girs/gmodule-2.0';
import type Json from '@girs/json-1.0';
import type Dex from '@girs/dex-1';

export namespace Foundry {

    /**
     * Foundry-1
     */


    /**
     * @gir-type Struct
     */
    class BuildError extends GLib.Error {
        static $gtype: GObject.GType<GLib.Error>;

        // Static fields
        static UNKNOWN: number;

        static INVALID_CONFIG: number;

        static INVALID_DEVICE: number;

        static INVALID_SDK: number;

        // Constructors
        constructor(options: { message: string, code: number });
    }


    /**
     * {@link Foundry.CommandLineError.COMMAND_LINE_ERROR_RUN_LOCAL}: indicate that the command should be run
     *   on the client side rather than in the parent process.
     * @gir-type Enum
     */
    enum CommandLineError {
        COMMAND_LINE_ERROR_RUN_LOCAL,
    }


    /**
     * @gir-type Enum
     */
    export namespace CommandLocality {
        export const $gtype: GObject.GType<CommandLocality>;
    }

    /**
     * {@link Foundry.CommandLocality.SUBPROCESS}: run as a subprocess of builder
     * {@link Foundry.CommandLocality.HOST}: run on the host system, possibly bypassing container
     * {@link Foundry.CommandLocality.PIPELINE}: run from build pipeline
     * {@link Foundry.CommandLocality.APPLICATION}: run like a target application
     * @gir-type Enum
     */
    enum CommandLocality {
        SUBPROCESS,
        HOST,
        PIPELINE,
        APPLICATION,
    }


    /**
     * @gir-type Enum
     */
    enum CompletionActivation {
        NONE,
        INTERACTIVE,
        USER_REQUESTED,
    }


    /**
     * @gir-type Enum
     */
    enum ContextError {
        CONTEXT_ERROR_IN_SHUTDOWN,
    }


    /**
     * @gir-type Enum
     */
    enum ContextualError {
        CONTEXTUAL_ERROR_IN_SHUTDOWN,
    }


    /**
     * @gir-type Enum
     */
    export namespace DebuggerMovement {
        export const $gtype: GObject.GType<DebuggerMovement>;
    }

    /**
     * @gir-type Enum
     */
    enum DebuggerMovement {
        START,
        CONTINUE,
        STEP_IN,
        STEP_OUT,
        STEP_OVER,
    }


    /**
     * @gir-type Enum
     */
    export namespace DebuggerStopReason {
        export const $gtype: GObject.GType<DebuggerStopReason>;
    }

    /**
     * @gir-type Enum
     */
    enum DebuggerStopReason {
        BREAKPOINT_HIT,
        EXITED,
        EXITED_NORMALLY,
        SIGNALED,
        FUNCTION_FINISHED,
        LOCATION_REACHED,
        SIGNAL_RECEIVED,
        CATCH,
        UNKNOWN,
    }


    /**
     * @gir-type Enum
     */
    export namespace DebuggerTrapDisposition {
        export const $gtype: GObject.GType<DebuggerTrapDisposition>;
    }

    /**
     * @gir-type Enum
     */
    enum DebuggerTrapDisposition {
        KEEP,
        DISABLE,
        NEXT_HIT,
        NEXT_STOP,
    }


    /**
     * @gir-type Enum
     */
    export namespace DebuggerTrapKind {
        export const $gtype: GObject.GType<DebuggerTrapKind>;
    }

    /**
     * @gir-type Enum
     */
    enum DebuggerTrapKind {
        BREAKPOINT,
        COUNTPOINT,
        WATCHPOINT,
    }


    /**
     * @gir-type Enum
     */
    export namespace DeviceChassis {
        export const $gtype: GObject.GType<DeviceChassis>;
    }

    /**
     * @gir-type Enum
     */
    enum DeviceChassis {
        WORKSTATION,
        HANDSET,
        TABLET,
        OTHER,
    }


    /**
     * @gir-type Enum
     */
    export namespace DiagnosticSeverity {
        export const $gtype: GObject.GType<DiagnosticSeverity>;
    }

    /**
     * @gir-type Enum
     */
    enum DiagnosticSeverity {
        IGNORED,
        NOTE,
        UNUSED,
        DEPRECATED,
        WARNING,
        ERROR,
        FATAL,
    }


    /**
     * @gir-type Enum
     */
    enum DiagnosticToolError {
        DIAGNOSTIC_TOOL_ERROR_NO_COMMAND,
    }


    /**
     * @gir-type Enum
     */
    enum DoapFileError {
        DOAP_FILE_ERROR_INVALID_FORMAT,
    }


    /**
     * @gir-type Enum
     */
    enum ForgeError {
        FAILED,
        NOT_CONFIGURED,
        NOT_FOUND,
    }


    /**
     * @gir-type Enum
     */
    enum GirError {
        FAILED,
        PARSE,
    }


    /**
     * @gir-type Enum
     */
    enum GirNodeType {
        UNKNOWN,
        REPOSITORY,
        INCLUDE,
        C_INCLUDE,
        PACKAGE,
        NAMESPACE,
        ALIAS,
        ARRAY,
        BITFIELD,
        CALLBACK,
        CLASS,
        CLASS_METHOD,
        CLASS_VIRTUAL_METHOD,
        CLASS_PROPERTY,
        CONSTRUCTOR,
        CONSTANT,
        DOC,
        DOC_PARA,
        DOC_TEXT,
        ENUM,
        ENUM_MEMBER,
        FIELD,
        FUNCTION,
        FUNCTION_MACRO,
        GLIB_BOXED,
        GLIB_ERROR_DOMAIN,
        GLIB_SIGNAL,
        IMPLEMENTS,
        INSTANCE_PARAMETER,
        INTERFACE,
        METHOD,
        NAMESPACE_FUNCTION,
        PARAMETER,
        PARAMETERS,
        PREREQUISITE,
        PROPERTY,
        RECORD,
        RETURN_VALUE,
        SOURCE_POSITION,
        TYPE,
        UNION,
        VARARGS,
        VIRTUAL_METHOD,
    }


    /**
     * @gir-type Struct
     */
    class GitCloneError extends GLib.Error {
        static $gtype: GObject.GType<GLib.Error>;

        // Static fields
        static URI: number;

        static DIRECTORY: number;

        static REMOTE_BRANCH_NAME: number;

        // Constructors
        constructor(options: { message: string, code: number });
    }


    /**
     * @gir-type Enum
     */
    export namespace Locality {
        export const $gtype: GObject.GType<Locality>;
    }

    /**
     * @gir-type Enum
     */
    enum Locality {
        RUN,
        BUILD,
        TOOL,
    }


    /**
     * @gir-type Enum
     */
    export namespace MarkupKind {
        export const $gtype: GObject.GType<MarkupKind>;
    }

    /**
     * @gir-type Enum
     */
    enum MarkupKind {
        PLAINTEXT,
        MARKDOWN,
        HTML,
        PANGO,
    }


    /**
     * @gir-type Enum
     */
    export namespace ObjectSerializerFormat {
        export const $gtype: GObject.GType<ObjectSerializerFormat>;
    }

    /**
     * @gir-type Enum
     */
    enum ObjectSerializerFormat {
        TEXT,
        JSON,
    }


    /**
     * Describes the type of shell to be used within the context.
     * @gir-type Enum
     */
    enum ProcessLauncherShell {
        /**
         * A basic shell with no user scripts
         */
        DEFAULT,
        /**
         * A user login shell similar to `bash -l`
         */
        LOGIN,
        /**
         * A user interactive shell similar to `bash -i`
         */
        INTERACTIVE,
    }


    /**
     * @gir-type Enum
     */
    enum SdkConfigOption {
        PREFIX,
        LIBDIR,
    }


    /**
     * @gir-type Enum
     */
    enum ServiceError {
        STARTED,
        STOPPED,
    }


    /**
     * @gir-type Enum
     */
    export namespace SettingsLayer {
        export const $gtype: GObject.GType<SettingsLayer>;
    }

    /**
     * @gir-type Enum
     */
    enum SettingsLayer {
        /**
         * Application-wide settings global to
         *   any new project opened or created with Foundry.
         */
        APPLICATION,
        /**
         * Project-level overrides which take
         *   priority over {@link Foundry.SettingsLayer.APPLICATION}.
         */
        PROJECT,
        /**
         * User-level overrides which take priority
         *   over {@link Foundry.SettingsLayer.APPLICATION} and
         *   {@link Foundry.SettingsLayer.PROJECT}.
         */
        USER,
    }


    /**
     * @gir-type Enum
     */
    export namespace TextBufferNotifyFlags {
        export const $gtype: GObject.GType<TextBufferNotifyFlags>;
    }

    /**
     * @gir-type Enum
     * @since 1.1
     */
    enum TextBufferNotifyFlags {
        /**
         * Be notified before text
         *   is inserted into the underlying buffer.
         */
        BEFORE_INSERT,
        /**
         * Be notified after text
         *   has been inserted into the underlying buffer.
         */
        AFTER_INSERT,
        /**
         * Be notified before text
         *   is deleted from the underlying buffer.
         */
        BEFORE_DELETE,
        /**
         * Be notified after text
         *   has been deleted from the underlying buffer.
         */
        AFTER_DELETE,
    }


    /**
     * @gir-type Enum
     */
    export namespace TextSetting {
        export const $gtype: GObject.GType<TextSetting>;
    }

    /**
     * @gir-type Enum
     */
    enum TextSetting {
        NONE,
        AUTO_INDENT,
        COMPLETION_AUTO_SELECT,
        COMPLETION_PAGE_SIZE,
        CUSTOM_FONT,
        ENABLE_COMPLETION,
        ENABLE_SNIPPETS,
        ENABLE_SPELL_CHECK,
        HIGHLIGHT_CURRENT_LINE,
        HIGHLIGHT_MATCHING_BRACKETS,
        IMPLICIT_TRAILING_NEWLINE,
        INDENT_ON_TAB,
        INDENT_WIDTH,
        INSERT_MATCHING_BRACE,
        INSERT_SPACES_INSTEAD_OF_TABS,
        LINE_HEIGHT,
        OVERRIDE_INDENT_WIDTH,
        OVERWRITE_MATCHING_BRACE,
        RIGHT_MARGIN_POSITION,
        SHOW_DIAGNOSTICS,
        SHOW_LINE_CHANGES,
        SHOW_LINE_CHANGES_OVERVIEW,
        SHOW_LINE_NUMBERS,
        SHOW_RIGHT_MARGIN,
        SMART_BACKSPACE,
        SMART_HOME_END,
        TAB_WIDTH,
        USE_CUSTOM_FONT,
        WRAP,
    }


    /**
     * @gir-type Enum
     */
    export namespace TextWrap {
        export const $gtype: GObject.GType<TextWrap>;
    }

    /**
     * @gir-type Enum
     */
    enum TextWrap {
        NONE,
        CHAR,
        WORD,
        WORD_CHAR,
    }


    /**
     * @gir-type Enum
     */
    export namespace VcsDeltaStatus {
        export const $gtype: GObject.GType<VcsDeltaStatus>;
    }

    /**
     * @gir-type Enum
     */
    enum VcsDeltaStatus {
        UNMODIFIED,
        ADDED,
        DELETED,
        MODIFIED,
        RENAMED,
        COPIED,
        IGNORED,
        UNTRACKED,
        TYPECHANGE,
        UNREADABLE,
        CONFLICTED,
    }


    /**
     * @gir-type Enum
     */
    export namespace VcsDiffLineOrigin {
        export const $gtype: GObject.GType<VcsDiffLineOrigin>;
    }

    /**
     * @gir-type Enum
     */
    enum VcsDiffLineOrigin {
        ADDED,
        DELETED,
        CONTEXT,
        CONTEXT_EOFNL,
        ADD_EOFNL,
        DEL_EOFNL,
    }


    const DOCUMENTATION_ATTRIBUTE_DEPRECATED: string;

    const DOCUMENTATION_ATTRIBUTE_SINCE: string;

    const DOCUMENTATION_ATTRIBUTE_STABILITY: string;

    const GIR_NODE_LAST: number;

    const SEARCH_CATEGORY_ACTIONS: string;

    const SEARCH_CATEGORY_DOCUMENTATION: string;

    const SEARCH_CATEGORY_FILES: string;

    const SEARCH_CATEGORY_SYMBOLS: string;

    const TEXT_SETTING_LAST: number;

    /**
     * @param instance 
     * @param action 
     */
    function action_mixin_get_enabled(instance: null, action: string): boolean;

    /**
     * @param instance 
     * @param action 
     * @param enabled 
     */
    function action_mixin_set_enabled(instance: null, action: string, enabled: boolean): void;

    function build_error_quark(): GLib.Quark;

    /**
     * @param node 
     */
    function dap_protocol_extract_error(node: Json.Node): GLib.Error;

    /**
     * @param node 
     */
    function dap_protocol_has_error(node: Json.Node): boolean;

    /**
     * @param completed 
     * @param user_data 
     */
    function dap_protocol_unwrap_error(completed: Dex.Future, user_data: null): Dex.Future;

    function dup_projects_directory(): string;

    function dup_projects_directory_file(): Gio.File;

    /**
     * @param file a {@link Gio.File}
     */
    function file_canonicalize(file: Gio.File): Gio.File;

    /**
     * Locates `name` within any of the ancestors of `file` up to the root of
     * the filesystem.
     * @param file a {@link Gio.File}
     * @param name the name of the file to find in the ancestors such as ".gitignore"
     * @returns a {@link Dex.Future} that resolves to   a {@link Gio.File} or rejects with error,
     */
    function file_find_in_ancestors(file: Gio.File, name: string): Dex.Future;

    /**
     * Locates files starting from `file` matching `regex`.
     * 
     * The regex will be passed the name within the parent directory, not the enter
     * path from `file`.
     * @param file an {@link Gio.File}
     * @param regex A regex to match filenames within descendants
     * @param max_depth the max depth to recurse
     * @returns a {@link Dex.Future}
     */
    function file_find_regex_with_depth(file: Gio.File, regex: GLib.Regex, max_depth: number): Dex.Future;

    /**
     * Locates files starting from `file` matching `pattern`.
     * @param file an {@link Gio.File}
     * @param pattern the pattern to find
     * @param max_depth the max depth to recurse
     * @returns a {@link Dex.Future} that resolves   to a {@link GLib.PtrArray} of {@link Gio.File}.
     */
    function file_find_with_depth(file: Gio.File, pattern: string, max_depth: number): Dex.Future;

    /**
     * @param file 
     * @param toplevel 
     */
    function file_is_in(file: Gio.File, toplevel: Gio.File): boolean;

    /**
     * @param file a {@link Gio.File}
     * @param file_type 
     * @param attributes 
     * @returns a {@link Dex.Future} that resolves to a   {@link GLib.PtrArray} of {@link Gio.File} or rejects with error.
     */
    function file_list_children_typed(file: Gio.File, file_type: Gio.FileType, attributes: string): Dex.Future;

    /**
     * Resolves to true if `file` exists.
     * 
     * Does not follow symlink.
     * @param file a {@link Gio.File}
     * @returns a future that resolves to a boolean
     */
    function file_query_exists_nofollow(file: Gio.File): Dex.Future;

    /**
     * Asynchronously resolves `file` if it is a symbolic link.
     * 
     * If `file` is not a symbolic link (as determined by
     * {@link Gio.File.query_info} with
     * `G_FILE_ATTRIBUTE_STANDARD_IS_SYMLINK`), the future resolves to `file`
     * itself. If it is a symbolic link, the link target is read with
     * {@link GLib.file_read_link} and a new {@link Gio.File} for the
     * resolved path is returned. On failure, the future rejects with an error.
     * 
     * The work is performed in a fiber on the thread pool scheduler.
     * @param file a {@link Gio.File}
     * @returns a {@link Dex.Future} that resolves to a   {@link Gio.File}, or rejects with {@link GLib.Error}
     * @since 1.1
     */
    function file_read_link(file: Gio.File): Dex.Future;

    /**
     * Asynchronously resolves `file` by repeatedly following symbolic links
     * until the result is not a symbolic link, or `max_depth` steps have been
     * taken.
     * 
     * Uses a "seen" set to detect cycles: if the same {@link Gio.File} is
     * encountered again while resolving, the future rejects with
     * {@link Gio.IOErrorEnum.FAILED} and message "Symbolic link loop detected".
     * 
     * The work is performed in a fiber on the thread pool scheduler.
     * @param file a {@link Gio.File}
     * @param max_depth maximum number of symbolic links to resolve (must be > 0)
     * @returns a {@link Dex.Future} that resolves to a   {@link Gio.File}, or rejects with {@link GLib.Error}
     * @since 1.1
     */
    function file_read_link_recurse(file: Gio.File, max_depth: number): Dex.Future;

    /**
     * Similar to `g_file_test()` but performed on the thread pool and yields a future.
     * @param path the path to check
     * @param test the {@link GLib.FileTest} to check for
     * @returns a {@link Dex.Future} that resolves to a boolean   of `true` if the test was met, otherwise resolves to `false`.
     */
    function file_test(path: string, test: GLib.FileTest): Dex.Future;

    /**
     * @param model 
     */
    function flatten_list_model_new(model: (Gio.ListModel | null)): Gio.ListModel;

    /**
     * This helper function can do a fuzzy match for you giving a haystack and
     * casefolded needle.
     * 
     * Casefold your needle using {@link GLib.utf8_casefold} before
     * running the query.
     * 
     * Score will be set with the score of the match upon success. Otherwise,
     * it will be set to zero.
     * @param haystack the string to be searched.
     * @param casefold_needle A `g_utf8_casefold()` version of the needle.
     * @returns `true` if `haystack` matched `casefold_needle`, otherwise `false`.
     * @since 1.1
     */
    function fuzzy_match(haystack: (string | null), casefold_needle: string): [boolean, number];

    function get_default_arch(): string;

    /**
     * @param key_name 
     */
    function get_os_info(key_name: string): string;

    function get_version_string(): string;

    function git_clone_error_quark(): GLib.Quark;

    /**
     * Initializes a new git repository.
     * @param directory 
     * @param bare 
     * @returns a {@link Dex.Future} that resolves to   any value if successful or rejects with error.
     */
    function git_initialize(directory: Gio.File, bare: boolean): Dex.Future;

    /**
     * @param uri_string 
     */
    function git_uri_is_valid(uri_string: string): boolean;

    /**
     * @param path a path to a file on the host
     * @returns a {@link Dex.Future} that resolves to a   {@link GLib.Bytes} or rejects with error.
     */
    function host_file_get_contents_bytes(path: string): Dex.Future;

    /**
     * Initializes libfoundry which is completed once the resulting
     * {@link Dex.Future} completes.
     * 
     * It is generally fine to use the library after calling this function
     * but some data may not be fully loaded until it resolves. For example,
     * if you need to sniff the user shell or other environment data, await
     * this future before accessing user shell APIs.
     * @returns a {@link Dex.Promise} that resolves when the   library and dynamically loaded data has completed.
     */
    function init(): Dex.Future;

    /**
     * Bytes to be deocded into a json node
     * @param bytes a {@link GLib.Bytes}
     * @returns a {@link Dex.Future} that resolves to   a {@link Json.Node} or rejects with error.
     */
    function json_node_from_bytes(bytes: (GLib.Bytes | Uint8Array)): Dex.Future;

    /**
     * @param strv 
     */
    function json_node_new_strv(strv: string): Json.Node;

    /**
     * `node` must not be modified after calling this function
     * until the future as resolved or rejected.
     * @param node 
     * @returns a future that resolves to a   {@link GLib.Bytes} or rejects with error.
     */
    function json_node_to_bytes(node: Json.Node): Dex.Future;

    /**
     * Converts `node` to a {@link GLib.Bytes} asynchronously.
     * 
     * If `pretty_print` is `true`, the output will be formatted with
     * 4-space indentation for readability.
     * 
     * `node` must not be modified after calling this function
     * until the future has resolved or rejected.
     * @param node a {@link Json.Node}
     * @param pretty_print whether to pretty print the JSON output
     * @returns a future that resolves to a   {@link GLib.Bytes} or rejects with error.
     * @since 1.1
     */
    function json_node_to_bytes_full(node: Json.Node, pretty_print: boolean): Dex.Future;

    /**
     * Converts `node` to a string synchronously using a {@link Json.Generator}.
     * 
     * If `pretty_print` is `true`, the output will be formatted with
     * 4-space indentation for readability.
     * @param node a {@link Json.Node}
     * @param pretty_print whether to pretty print the JSON output
     * @returns a newly allocated string containing   the JSON representation of `node`, or `null` on error.
     * @since 1.1
     */
    function json_node_to_string(node: Json.Node, pretty_print: boolean): string;

    /**
     * Loads `file` into `parser` and returns a future that resolves
     * when that has completed.
     * @param parser a {@link Json.Parser}
     * @param file a {@link Gio.File}
     * @returns a {@link Dex.Future} that resolves   to a boolean
     */
    function json_parser_load_from_file(parser: Json.Parser, file: Gio.File): Dex.Future;

    /**
     * Like `json_parser_load_from_stream()` but asynchronous and returns
     * a {@link Dex.Future} which can be awaited upon.
     * @param parser a {@link Json.Parser}
     * @param stream a {@link Gio.InputStream}
     * @returns a {@link Dex.Future}.
     */
    function json_parser_load_from_stream(parser: Json.Parser, stream: Gio.InputStream): Dex.Future;

    /**
     * Similar to calling `g_key_file_new()` followed by a load function. This
     * handles both construction and loading as well as doing parsing off
     * of the main thread.
     * @param file a {@link Gio.File}
     * @param flags flags that may affect loading
     * @returns a {@link Dex.Future} that resolves to a   {@link GLib.KeyFile} or rejects with error
     */
    function key_file_new_from_file(file: Gio.File, flags: GLib.KeyFileFlags): Dex.Future;

    /**
     * @param search_dirs 
     * @param file 
     * @param flags 
     * @returns a {@link Dex.Future} that resolves to a   {@link GLib.KeyFile} or rejects with error.
     */
    function key_file_new_merged(search_dirs: string, file: string, flags: GLib.KeyFileFlags): Dex.Future;

    /**
     * Returns a future that resolves when the list has completed
     * being populated or rejects with error.
     * 
     * Use `foundry_list_model_set_future()` to affect the future that is
     * used here.
     * 
     * If no future has been set, this function returns a future that
     * has already resolved (e.g. True).
     * @param model 
     */
    function list_model_await(model: Gio.ListModel): Dex.Future;

    /**
     * Sets the future that can be awaited for completion of populating
     * the list model.
     * @param model 
     * @param future a {@link Dex.Future} or `null`
     */
    function list_model_set_future(model: Gio.ListModel, future: (Dex.Future | null)): void;

    /**
     * @param model a {@link Gio.ListModel}
     * @param map_func 
     */
    function map_list_model_new(model: (Gio.ListModel | null), map_func: ListModelMapFunc): Gio.ListModel;

    /**
     * Similar to `g_mkdir_with_parents()` but runs on a dedicated thread.
     * @param path a path to a directory to create
     * @param mode the mode for the directory such as `0750`
     * @returns a {@link Dex.Future} that resolves to 0   if successful, otherwise rejects with error.
     * @deprecated since 1.1
     */
    function mkdir_with_parents(path: string, mode: number): Dex.Future;

    /**
     * @param string 
     */
    function object_serializer_format_parse(string: string): ObjectSerializerFormat;

    /**
     * This function will collapse a path that starts with the users home
     * directory into a shorthand notation using ~/ for the home directory.
     * 
     * If the path does not have the home directory as a prefix, it will
     * simply return a copy of `path`.
     * @param path 
     * @returns A new path, possibly collapsed.
     */
    function path_collapse(path: string): string;

    /**
     * This function will expand various "shell-like" features of the provided
     * path using the POSIX wordexp(3) function. Command substitution will
     * not be enabled, but path features such as ~user will be expanded.
     * @param path 
     * @returns A newly allocated string containing the   expansion. A copy of the input string upon failure to expand.
     */
    function path_expand(path: string): string;

    /**
     * @param read_fd 
     * @param write_fd 
     * @param flags 
     */
    function pipe(read_fd: number, write_fd: number, flags: number): boolean;

    /**
     * @param pty_consumer_fd 
     * @param blocking 
     */
    function pty_create_producer(pty_consumer_fd: number, blocking: boolean): number;

    /**
     * @param path 
     * @param append 
     */
    function search_path_append(path: string, append: string): string;

    /**
     * @param path 
     * @param prepend 
     */
    function search_path_prepend(path: string, prepend: string): string;

    /**
     * Gets the user preferred shell on the host.
     * 
     * If the background shell discovery has not yet finished due to
     * slow or misconfigured getent on the host, this will provide a
     * sensible fallback.
     * @returns a shell such as "/bin/sh"
     */
    function shell_get_default(): string;

    /**
     * Gets the default `$PATH` on the system for the user on the host.
     * 
     * This value is sniffed during startup and will default to `SAFE_PATH`
     * configured when building Builder until that value has been discovered.
     * @returns a string such as "/bin:/usr/bin"
     */
    function shell_get_default_path(): string;

    /**
     * @param shell 
     */
    function shell_supports_dash_c(shell: string): boolean;

    /**
     * Checks if the shell is known to support login semantics. Originally,
     * this meant `--login`, but now is meant to mean `-l` as more shells
     * support `-l` than `--login` (notably dash).
     * @param shell the name of the shell, such as `sh` or `/bin/sh`
     * @returns `true` if `shell` likely supports `-l`.
     */
    function shell_supports_dash_login(shell: string): boolean;

    /**
     * Like `g_subprocess_communicate()` but only supports stdout and is
     * returned as a future to {@link GLib.Bytes}.
     * @param subprocess a `FoundrySubprocess`
     * @param stdin_bytes the standard input buffer
     * @returns a {@link Dex.Future} that resolves to a string
     */
    function subprocess_communicate(subprocess: Gio.Subprocess, stdin_bytes: (GLib.Bytes | null)): Dex.Future;

    /**
     * Like `g_subprocess_communicate_utf8()` but only supports stdout and is
     * returned as a future to a string.
     * @param subprocess a `FoundrySubprocess`
     * @param stdin_buf the standard input buffer
     * @returns a {@link Dex.Future} that resolves to a string
     */
    function subprocess_communicate_utf8(subprocess: Gio.Subprocess, stdin_buf: string): Dex.Future;

    /**
     * If `cancellable` is cancelled, then `subprocess` will be force exited.
     * @param subprocess a {@link Gio.Subprocess}
     * @param cancellable a {@link Dex.Cancellable}
     * @returns a {@link Dex.Future} that resolves to a boolean
     */
    function subprocess_wait_check(subprocess: Gio.Subprocess, cancellable: Dex.Cancellable): Dex.Future;

    /**
     * @gir-type Callback
     */
    interface ActionActivateFunc {
        (instance: null, action_name: string, param: GLib.Variant): void;
    }

    /**
     * @gir-type Callback
     */
    interface ExtensionSetForeachFunc<A = GObject.Object> {
        (set: ExtensionSet, plugin_info: Peas.PluginInfo, extension: A): void;
    }

    /**
     * @gir-type Callback
     */
    interface ListModelMapFunc<A = GObject.Object> {
        (item: A): null;
    }

    /**
     * @gir-type Callback
     */
    interface OnTypeDiagnosticsForeachFunc {
        (diagnostic: Diagnostic): void;
    }

    /**
     * @gir-type Callback
     */
    interface ProcessLauncherHandler {
        (process_launcher: ProcessLauncher, argv: string, env: string, cwd: string, unix_fd_map: UnixFDMap): boolean;
    }

    /**
     * @gir-type Callback
     */
    interface ServiceAction {
        (self: Service, action_name: string, param: GLib.Variant): void;
    }

    /**
     * @gir-type Callback
     */
    interface TextBufferCommitNotify {
        (buffer: TextBuffer, flags: TextBufferNotifyFlags, position: number, length: number): void;
    }

    /**
     * @gir-type Callback
     */
    interface VcsLineChangesForeach {
        (line: number, change: VcsLineChange): void;
    }

    /**
     * @gir-type Flags
     */
    export namespace BuildPipelinePhase {
        export const $gtype: GObject.GType<BuildPipelinePhase>;
    }

    /**
     * @gir-type Flags
     */
    enum BuildPipelinePhase {
        NONE,
        PREPARE,
        DOWNLOADS,
        DEPENDENCIES,
        AUTOGEN,
        CONFIGURE,
        BUILD,
        INSTALL,
        COMMIT,
        EXPORT,
        FINAL,
        BEFORE,
        AFTER,
        FINISHED,
        FAILED,
    }


    /**
     * @gir-type Flags
     */
    export namespace ContextFlags {
        export const $gtype: GObject.GType<ContextFlags>;
    }

    /**
     * @gir-type Flags
     */
    enum ContextFlags {
        NONE,
        CREATE,
    }


    /**
     * @gir-type Flags
     */
    export namespace DapDebuggerQuirk {
        export const $gtype: GObject.GType<DapDebuggerQuirk>;
    }

    /**
     * @gir-type Flags
     */
    enum DapDebuggerQuirk {
        NONE,
        QUERY_THREADS,
    }


    /**
     * @gir-type Flags
     */
    export namespace DebuggerWatchAccess {
        export const $gtype: GObject.GType<DebuggerWatchAccess>;
    }

    /**
     * @gir-type Flags
     */
    enum DebuggerWatchAccess {
        NONE,
        READ,
        WRITE,
        READWRITE,
    }


    /**
     * @gir-type Flags
     */
    export namespace ModifierType {
        export const $gtype: GObject.GType<ModifierType>;
    }

    /**
     * @gir-type Flags
     */
    enum ModifierType {
        CONTROL,
        SHIFT,
        ALT,
        SUPER,
        COMMAND,
    }


    /**
     * @gir-type Flags
     */
    export namespace VcsFileStatus {
        export const $gtype: GObject.GType<VcsFileStatus>;
    }

    /**
     * @gir-type Flags
     */
    enum VcsFileStatus {
        CURRENT,
        MODIFIED_IN_STAGE,
        MODIFIED_IN_TREE,
        NEW_IN_STAGE,
        NEW_IN_TREE,
        DELETED_IN_STAGE,
        DELETED_IN_TREE,
    }


    /**
     * @gir-type Flags
     */
    export namespace VcsLineChange {
        export const $gtype: GObject.GType<VcsLineChange>;
    }

    /**
     * @gir-type Flags
     */
    enum VcsLineChange {
        ADDED,
        REMOVED,
        CHANGED,
        PREVIOUS_REMOVED,
    }


    namespace ActionIntent {
        // Signal signatures
        interface SignalSignatures extends Intent.SignalSignatures {
            "notify::action-name": (pspec: GObject.ParamSpec) => void;
            "notify::action-target": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Intent.ConstructorProps {
            action_name: string;
            actionName: string;
            action_target: GLib.Variant;
            actionTarget: GLib.Variant;
        }
    }

    /**
     * Represents an intent to perform an action with optional parameters.
     * 
     * FoundryActionIntent provides functionality for representing actions
     * that can be performed within the development environment. It stores
     * an action name and optional target parameters as a GVariant.
     * @gir-type Class
     * @since 1.1
     */
    class ActionIntent extends Intent {
        static $gtype: GObject.GType<ActionIntent>;

        // Properties
        /**
         * @construct-only
         * @default null
         */
        get action_name(): string;

        /**
         * @construct-only
         * @default null
         */
        get actionName(): string;

        /**
         * @construct-only
         */
        get action_target(): GLib.Variant;

        /**
         * @construct-only
         */
        get actionTarget(): GLib.Variant;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ActionIntent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ActionIntent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](action_name: string, action_target: (GLib.Variant | null)): ActionIntent;

        // Signals
        /** @signal */
        connect<K extends keyof ActionIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ActionIntent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ActionIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ActionIntent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ActionIntent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ActionIntent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets a copy of the action name.
         * @returns a newly allocated copy of the action name
         */
        dup_action_name(): string;

        /**
         * Gets a copy of the action target parameters.
         * @returns a {@link GLib.Variant} with the action target,   or `null` if no target is set
         */
        dup_action_target(): (GLib.Variant | null);
    }


    namespace ActionMuxer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Gio.ActionGroup.ConstructorProps {

        }
    }

    /**
     * Custom GActionGroup integration.
     * 
     * FoundryActionMuxer coordinates multiple action groups and provides a unified
     * interface for action handling. It supports action prefixing, disabling,
     * and provides efficient action lookup and management for the development
     * environment's action system.
     * @gir-type Class
     */
    class ActionMuxer extends GObject.Object implements Gio.ActionGroup {
        static $gtype: GObject.GType<ActionMuxer>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ActionMuxer.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ActionMuxer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): ActionMuxer;

        // Signals
        /** @signal */
        connect<K extends keyof ActionMuxer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ActionMuxer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ActionMuxer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ActionMuxer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ActionMuxer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ActionMuxer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param instance 
         * @param actions 
         */
        connect_actions(instance: null, actions: Action): void;

        /**
         * Locates the {@link Gio.ActionGroup} inserted as `prefix`.
         * 
         * If no group was found matching `group`, `null` is returned.
         * @param prefix the name of the inserted action group
         * @returns a {@link Gio.ActionGroup} matching `prefix` if   found, otherwise `null`.
         */
        get_action_group(prefix: string): (Gio.ActionGroup | null);

        /**
         * @param action 
         */
        get_enabled(action: Action): boolean;

        /**
         * @param prefix 
         * @param action_group 
         */
        insert_action_group(prefix: string, action_group: Gio.ActionGroup): void;

        /**
         * Gets a list of group names in the muxer.
         * @returns an array containing the names of groups within the muxer
         */
        list_groups(): string[];

        /**
         * @param prefix 
         */
        remove_action_group(prefix: string): void;

        remove_all(): void;

        /**
         * @param action 
         * @param enabled 
         */
        set_enabled(action: Action, enabled: boolean): void;

        /**
         * Emits the `Gio.ActionGroup::action-added` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         */
        action_added(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-enabled-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param enabled whether the action is now enabled
         */
        action_enabled_changed(action_name: string, enabled: boolean): void;

        /**
         * Emits the `Gio.ActionGroup::action-removed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         */
        action_removed(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-state-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param state the new state of the named action
         */
        action_state_changed(action_name: string, state: GLib.Variant): void;

        /**
         * Activate the named action within `action_group`.
         * 
         * If the action is expecting a parameter, then the correct type of
         * parameter must be given as `parameter`.  If the action is expecting no
         * parameters then `parameter` must be `NULL`.  See
         * {@link Gio.ActionGroup.get_action_parameter_type}.
         * 
         * If the {@link Gio.ActionGroup} implementation supports asynchronous remote
         * activation over D-Bus, this call may return before the relevant
         * D-Bus traffic has been sent, or any replies have been received. In
         * order to block on such asynchronous activation calls,
         * {@link Gio.DBusConnection.flush} should be called prior to the code, which
         * depends on the result of the action activation. Without flushing
         * the D-Bus connection, there is no guarantee that the action would
         * have been activated.
         * 
         * The following code which runs in a remote app instance, shows an
         * example of a ‘quit’ action being activated on the primary app
         * instance over D-Bus. Here {@link Gio.DBusConnection.flush} is called
         * before `exit()`. Without `g_dbus_connection_flush()`, the ‘quit’ action
         * may fail to be activated on the primary instance.
         * 
         * ```c
         * // call ‘quit’ action on primary instance
         * g_action_group_activate_action (G_ACTION_GROUP (app), "quit", NULL);
         * 
         * // make sure the action is activated now
         * g_dbus_connection_flush (…);
         * 
         * g_debug ("Application has been terminated. Exiting.");
         * 
         * exit (0);
         * ```
         * @param action_name the name of the action to activate
         * @param parameter parameters to the activation
         */
        activate_action(action_name: string, parameter: (GLib.Variant | null)): void;

        /**
         * Request for the state of the named action within `action_group` to be
         * changed to `value`.
         * 
         * The action must be stateful and `value` must be of the correct type.
         * See {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * This call merely requests a change.  The action may refuse to change
         * its state or may change its state to something other than `value`.
         * See {@link Gio.ActionGroup.get_action_state_hint}.
         * 
         * If the `value` GVariant is floating, it is consumed.
         * @param action_name the name of the action to request the change on
         * @param value the new state
         */
        change_action_state(action_name: string, value: GLib.Variant): void;

        /**
         * Checks if the named action within `action_group` is currently enabled.
         * 
         * An action must be enabled in order to be activated or in order to
         * have its state changed from outside callers.
         * @param action_name the name of the action to query
         * @returns whether the action is currently enabled
         */
        get_action_enabled(action_name: string): boolean;

        /**
         * Queries the type of the parameter that must be given when activating
         * the named action within `action_group`.
         * 
         * When activating the action using {@link Gio.ActionGroup.activate_action},
         * the {@link GLib.Variant} given to that function must be of the type returned
         * by this function.
         * 
         * In the case that this function returns `NULL`, you must not give any
         * {@link GLib.Variant}, but `NULL` instead.
         * 
         * The parameter type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different parameter type.
         * @param action_name the name of the action to query
         * @returns the parameter type
         */
        get_action_parameter_type(action_name: string): (GLib.VariantType | null);

        /**
         * Queries the current state of the named action within `action_group`.
         * 
         * If the action is not stateful then `NULL` will be returned.  If the
         * action is stateful then the type of the return value is the type
         * given by {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @returns the current state of the action
         */
        get_action_state(action_name: string): (GLib.Variant | null);

        /**
         * Requests a hint about the valid range of values for the state of the
         * named action within `action_group`.
         * 
         * If `NULL` is returned it either means that the action is not stateful
         * or that there is no hint about the valid range of values for the
         * state of the action.
         * 
         * If a {@link GLib.Variant} array is returned then each item in the array is a
         * possible value for the state.  If a {@link GLib.Variant} pair (ie: two-tuple) is
         * returned then the tuple specifies the inclusive lower and upper bound
         * of valid values for the state.
         * 
         * In any case, the information is merely a hint.  It may be possible to
         * have a state value outside of the hinted range and setting a value
         * within the range may fail.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @returns the state range hint
         */
        get_action_state_hint(action_name: string): (GLib.Variant | null);

        /**
         * Queries the type of the state of the named action within
         * `action_group`.
         * 
         * If the action is stateful then this function returns the
         * {@link GLib.VariantType} of the state.  All calls to
         * {@link Gio.ActionGroup.change_action_state} must give a {@link GLib.Variant} of this
         * type and {@link Gio.ActionGroup.get_action_state} will return a {@link GLib.Variant}
         * of the same type.
         * 
         * If the action is not stateful then this function will return `NULL`.
         * In that case, {@link Gio.ActionGroup.get_action_state} will return `NULL`
         * and you must not call {@link Gio.ActionGroup.change_action_state}.
         * 
         * The state type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different state type.
         * @param action_name the name of the action to query
         * @returns the state type, if the action is stateful
         */
        get_action_state_type(action_name: string): (GLib.VariantType | null);

        /**
         * Checks if the named action exists within `action_group`.
         * @param action_name the name of the action to check for
         * @returns whether the named action exists
         */
        has_action(action_name: string): boolean;

        /**
         * Lists the actions contained within `action_group`.
         * 
         * The caller is responsible for freeing the list with {@link GLib.strfreev} when
         * it is no longer required.
         * @returns a `NULL`-terminated array   of the names of the actions in the group
         */
        list_actions(): string[];

        /**
         * Queries all aspects of the named action within an `action_group`.
         * 
         * This function acquires the information available from
         * {@link Gio.ActionGroup.has_action}, {@link Gio.ActionGroup.get_action_enabled},
         * {@link Gio.ActionGroup.get_action_parameter_type},
         * {@link Gio.ActionGroup.get_action_state_type},
         * {@link Gio.ActionGroup.get_action_state_hint} and
         * {@link Gio.ActionGroup.get_action_state} with a single function call.
         * 
         * This provides two main benefits.
         * 
         * The first is the improvement in efficiency that comes with not having
         * to perform repeated lookups of the action in order to discover
         * different things about it.  The second is that implementing
         * {@link Gio.ActionGroup} can now be done by only overriding this one virtual
         * function.
         * 
         * The interface provides a default implementation of this function that
         * calls the individual functions, as required, to fetch the
         * information.  The interface also provides default implementations of
         * those functions that call this function.  All implementations,
         * therefore, must override either this function or all of the others.
         * 
         * If the action exists, `TRUE` is returned and any of the requested
         * fields (as indicated by having a non-`NULL` reference passed in) are
         * filled.  If the action doesn’t exist, `FALSE` is returned and the
         * fields may or may not have been modified.
         * @param action_name the name of an action in the group
         * @returns `TRUE` if the action exists, else `FALSE`
         */
        query_action(action_name: string): [boolean, boolean, GLib.VariantType | null, GLib.VariantType | null, GLib.Variant | null, GLib.Variant | null];

        /**
         * Emits the `Gio.ActionGroup::action-added` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_action_added(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-enabled-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param enabled whether the action is now enabled
         * @virtual
         */
        vfunc_action_enabled_changed(action_name: string, enabled: boolean): void;

        /**
         * Emits the `Gio.ActionGroup::action-removed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_action_removed(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-state-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param state the new state of the named action
         * @virtual
         */
        vfunc_action_state_changed(action_name: string, state: GLib.Variant): void;

        /**
         * Activate the named action within `action_group`.
         * 
         * If the action is expecting a parameter, then the correct type of
         * parameter must be given as `parameter`.  If the action is expecting no
         * parameters then `parameter` must be `NULL`.  See
         * {@link Gio.ActionGroup.get_action_parameter_type}.
         * 
         * If the {@link Gio.ActionGroup} implementation supports asynchronous remote
         * activation over D-Bus, this call may return before the relevant
         * D-Bus traffic has been sent, or any replies have been received. In
         * order to block on such asynchronous activation calls,
         * {@link Gio.DBusConnection.flush} should be called prior to the code, which
         * depends on the result of the action activation. Without flushing
         * the D-Bus connection, there is no guarantee that the action would
         * have been activated.
         * 
         * The following code which runs in a remote app instance, shows an
         * example of a ‘quit’ action being activated on the primary app
         * instance over D-Bus. Here {@link Gio.DBusConnection.flush} is called
         * before `exit()`. Without `g_dbus_connection_flush()`, the ‘quit’ action
         * may fail to be activated on the primary instance.
         * 
         * ```c
         * // call ‘quit’ action on primary instance
         * g_action_group_activate_action (G_ACTION_GROUP (app), "quit", NULL);
         * 
         * // make sure the action is activated now
         * g_dbus_connection_flush (…);
         * 
         * g_debug ("Application has been terminated. Exiting.");
         * 
         * exit (0);
         * ```
         * @param action_name the name of the action to activate
         * @param parameter parameters to the activation
         * @virtual
         */
        vfunc_activate_action(action_name: string, parameter: (GLib.Variant | null)): void;

        /**
         * Request for the state of the named action within `action_group` to be
         * changed to `value`.
         * 
         * The action must be stateful and `value` must be of the correct type.
         * See {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * This call merely requests a change.  The action may refuse to change
         * its state or may change its state to something other than `value`.
         * See {@link Gio.ActionGroup.get_action_state_hint}.
         * 
         * If the `value` GVariant is floating, it is consumed.
         * @param action_name the name of the action to request the change on
         * @param value the new state
         * @virtual
         */
        vfunc_change_action_state(action_name: string, value: GLib.Variant): void;

        /**
         * Checks if the named action within `action_group` is currently enabled.
         * 
         * An action must be enabled in order to be activated or in order to
         * have its state changed from outside callers.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_enabled(action_name: string): boolean;

        /**
         * Queries the type of the parameter that must be given when activating
         * the named action within `action_group`.
         * 
         * When activating the action using {@link Gio.ActionGroup.activate_action},
         * the {@link GLib.Variant} given to that function must be of the type returned
         * by this function.
         * 
         * In the case that this function returns `NULL`, you must not give any
         * {@link GLib.Variant}, but `NULL` instead.
         * 
         * The parameter type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different parameter type.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_parameter_type(action_name: string): (GLib.VariantType | null);

        /**
         * Queries the current state of the named action within `action_group`.
         * 
         * If the action is not stateful then `NULL` will be returned.  If the
         * action is stateful then the type of the return value is the type
         * given by {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state(action_name: string): (GLib.Variant | null);

        /**
         * Requests a hint about the valid range of values for the state of the
         * named action within `action_group`.
         * 
         * If `NULL` is returned it either means that the action is not stateful
         * or that there is no hint about the valid range of values for the
         * state of the action.
         * 
         * If a {@link GLib.Variant} array is returned then each item in the array is a
         * possible value for the state.  If a {@link GLib.Variant} pair (ie: two-tuple) is
         * returned then the tuple specifies the inclusive lower and upper bound
         * of valid values for the state.
         * 
         * In any case, the information is merely a hint.  It may be possible to
         * have a state value outside of the hinted range and setting a value
         * within the range may fail.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state_hint(action_name: string): (GLib.Variant | null);

        /**
         * Queries the type of the state of the named action within
         * `action_group`.
         * 
         * If the action is stateful then this function returns the
         * {@link GLib.VariantType} of the state.  All calls to
         * {@link Gio.ActionGroup.change_action_state} must give a {@link GLib.Variant} of this
         * type and {@link Gio.ActionGroup.get_action_state} will return a {@link GLib.Variant}
         * of the same type.
         * 
         * If the action is not stateful then this function will return `NULL`.
         * In that case, {@link Gio.ActionGroup.get_action_state} will return `NULL`
         * and you must not call {@link Gio.ActionGroup.change_action_state}.
         * 
         * The state type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different state type.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state_type(action_name: string): (GLib.VariantType | null);

        /**
         * Checks if the named action exists within `action_group`.
         * @param action_name the name of the action to check for
         * @virtual
         */
        vfunc_has_action(action_name: string): boolean;

        /**
         * Lists the actions contained within `action_group`.
         * 
         * The caller is responsible for freeing the list with {@link GLib.strfreev} when
         * it is no longer required.
         * @virtual
         */
        vfunc_list_actions(): string[];

        /**
         * Queries all aspects of the named action within an `action_group`.
         * 
         * This function acquires the information available from
         * {@link Gio.ActionGroup.has_action}, {@link Gio.ActionGroup.get_action_enabled},
         * {@link Gio.ActionGroup.get_action_parameter_type},
         * {@link Gio.ActionGroup.get_action_state_type},
         * {@link Gio.ActionGroup.get_action_state_hint} and
         * {@link Gio.ActionGroup.get_action_state} with a single function call.
         * 
         * This provides two main benefits.
         * 
         * The first is the improvement in efficiency that comes with not having
         * to perform repeated lookups of the action in order to discover
         * different things about it.  The second is that implementing
         * {@link Gio.ActionGroup} can now be done by only overriding this one virtual
         * function.
         * 
         * The interface provides a default implementation of this function that
         * calls the individual functions, as required, to fetch the
         * information.  The interface also provides default implementations of
         * those functions that call this function.  All implementations,
         * therefore, must override either this function or all of the others.
         * 
         * If the action exists, `TRUE` is returned and any of the requested
         * fields (as indicated by having a non-`NULL` reference passed in) are
         * filled.  If the action doesn’t exist, `FALSE` is returned and the
         * fields may or may not have been modified.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_query_action(action_name: string): [boolean, boolean, GLib.VariantType | null, GLib.VariantType | null, GLib.Variant | null, GLib.Variant | null];
    }


    namespace AuthProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract class for requesting credentials from a user.
     * 
     * This abstract class can be implemented to provide an interface for the
     * user to provide credentials. Applications are encouraged to provide this
     * so that password input and similar is possible.
     * @gir-type Class
     */
    abstract class AuthProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<AuthProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AuthProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<AuthProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static new_for_context(context: Context): AuthProvider;

        // Signals
        /** @signal */
        connect<K extends keyof AuthProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AuthProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof AuthProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AuthProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AuthProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AuthProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param input a {@link Foundry.Input}
         * @virtual
         */
        vfunc_prompt(input: Input): Dex.Future;

        // Methods
        /**
         * @param input a {@link Foundry.Input}
         * @returns a {@link Dex.Future} that resolves to any value   when the prompt has been completed by the user.
         */
        prompt(input: Input): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace BuildAddin {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::pipeline": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            pipeline: BuildPipeline;
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Customize build pipelines from plugins.
     * 
     * This class provides plugins the ability to modify how a build pipeline
     * works by registering stages in the pipeline.
     * @gir-type Class
     */
    abstract class BuildAddin extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<BuildAddin>;

        // Properties
        /**
         * @construct-only
         */
        get pipeline(): BuildPipeline;

        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: BuildAddin.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<BuildAddin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof BuildAddin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildAddin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof BuildAddin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildAddin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof BuildAddin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<BuildAddin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Discover the build system used by the project.
         * 
         * This may be called _before_ {@link Foundry.BuildAddin.load} is called so
         * that addins may discover what build system should be used. If the user
         * has already specified a build system then this will not be called during
         * pipeline initialization.
         * @virtual
         */
        vfunc_discover_build_system(): Dex.Future;

        // Methods
        /**
         * Discover the build system used by the project.
         * 
         * This may be called _before_ {@link Foundry.BuildAddin.load} is called so
         * that addins may discover what build system should be used. If the user
         * has already specified a build system then this will not be called during
         * pipeline initialization.
         * @returns a {@link Dex.Future} that resolves to a   string name of the build system or rejects with error.
         */
        discover_build_system(): Dex.Future;

        /**
         * Gets the pipeline for which the addin belongs.
         * @returns a [class@Foundry.BuildPipeline   or `null` if the pipeline has been destroyed.
         */
        dup_pipeline(): (BuildPipeline | null);

        /**
         * @returns a {@link Peas.PluginInfo}
         */
        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace BuildFlags {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::directory": (pspec: GObject.ParamSpec) => void;
            "notify::flags": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            directory: string;
            flags: string[];
        }
    }

    /**
     * Represents build flags to a compiler or other command.
     * @gir-type Class
     */
    class BuildFlags extends GObject.Object {
        static $gtype: GObject.GType<BuildFlags>;

        // Properties
        /**
         * @construct-only
         * @default null
         */
        get directory(): string;

        /**
         * @construct-only
         */
        get flags(): string[];

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: BuildFlags.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<BuildFlags.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](flags: string, directory: string): BuildFlags;

        // Signals
        /** @signal */
        connect<K extends keyof BuildFlags.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildFlags.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof BuildFlags.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildFlags.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof BuildFlags.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<BuildFlags.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @returns the directory for the build or `null`
         */
        dup_directory(): (string | null);

        /**
         * @returns an array of build flags or `null`
         */
        dup_flags(): (string[] | null);
    }


    namespace BuildManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            /**
             * This signal is emitted when a loaded pipeline has become invalidated.
             * Observers of pipelines may want to call
             * {@link Foundry.BuildManager.load_pipeline} to request a new pipeline.
             * @signal
             * @run-last
             */
            "pipeline-invalidated": () => void;
            "notify::busy": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {
            busy: boolean;
        }
    }

    /**
     * Manages build operations and build system integration.
     * 
     * FoundryBuildManager coordinates build pipelines, manages build progress, and
     * provides a unified interface for different build systems. It handles build
     * configuration, execution, and integrates with device management for deployment
     * and testing operations.
     * @gir-type Class
     */
    class BuildManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<BuildManager>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get busy(): boolean;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: BuildManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<BuildManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof BuildManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof BuildManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof BuildManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<BuildManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error.
         */
        build(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error.
         */
        clean(): Dex.Future;

        /**
         * Runs the build pipeline to the export phase.
         * @returns a {@link Dex.Future} that resolves   to a {@link Gio.ListModel} of {@link Gio.File} or rejects   with error.
         */
        ["export"](): Dex.Future;

        /**
         * If the build manager is currently busy running an operation on the
         * active pipeline.
         */
        get_busy(): boolean;

        get_default_pty(): number;

        /**
         * Runs the build pipeline to the install phase.
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error.
         */
        install(): Dex.Future;

        invalidate(): void;

        /**
         * Loads the pipeline as a future.
         * 
         * If the pipeline is already being loaded, the future will be completed
         * as part of that request.
         * 
         * If the pipeline is already loaded, the future returned will already
         * be resolved.
         * 
         * Otherwise, a new request to load the pipeline is created and the future
         * will resolve upon completion.
         * @returns a {@link Dex.Future} that resolves to a {@link Foundry.Pipeline}
         */
        load_pipeline(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error.
         */
        purge(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error.
         */
        rebuild(): Dex.Future;

        /**
         * @param pty_fd 
         */
        set_default_pty(pty_fd: number): void;

        /**
         * Stop any active builds controlled by the build manager.
         */
        stop(): void;
    }


    namespace BuildPipeline {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::arch": (pspec: GObject.ParamSpec) => void;
            "notify::build-system": (pspec: GObject.ParamSpec) => void;
            "notify::config": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::enable-addins": (pspec: GObject.ParamSpec) => void;
            "notify::phase": (pspec: GObject.ParamSpec) => void;
            "notify::sdk": (pspec: GObject.ParamSpec) => void;
            "notify::triplet": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            arch: string;
            build_system: string;
            buildSystem: string;
            config: Config;
            device: Device;
            enable_addins: boolean;
            enableAddins: boolean;
            phase: BuildPipelinePhase;
            sdk: Sdk;
            triplet: Triplet;
        }
    }

    /**
     * Manages build pipeline execution and coordination.
     * 
     * FoundryBuildPipeline provides functionality for executing build pipelines
     * including configuration management, device targeting, and addin integration.
     * It coordinates build stages, manages build artifacts, and provides
     * a unified interface for build operations across different build systems.
     * @gir-type Class
     */
    class BuildPipeline<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<BuildPipeline>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get arch(): string;

        /**
         * @read-only
         * @default null
         */
        get build_system(): string;

        /**
         * @read-only
         * @default null
         */
        get buildSystem(): string;

        /**
         * @construct-only
         */
        get config(): Config;

        /**
         * @construct-only
         */
        get device(): Device;

        /**
         * @construct-only
         * @default true
         */
        get enable_addins(): boolean;

        /**
         * @construct-only
         * @default true
         */
        get enableAddins(): boolean;

        /**
         * @read-only
         * @default Foundry.BuildPipelinePhase.NONE
         */
        get phase(): BuildPipelinePhase;

        /**
         * @construct-only
         */
        get sdk(): Sdk;

        /**
         * @construct-only
         */
        get triplet(): Triplet;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: BuildPipeline.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<BuildPipeline.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, config: Config, device: Device, sdk: Sdk, enable_adddins: boolean): BuildPipeline;

        // Signals
        /** @signal */
        connect<K extends keyof BuildPipeline.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildPipeline.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof BuildPipeline.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildPipeline.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof BuildPipeline.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<BuildPipeline.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param stage 
         */
        add_stage(stage: BuildStage): void;

        /**
         * Adds a path to the list of paths that will be appended to PATH.
         * @param path the path to append
         */
        append_path(path: string): void;

        /**
         * Build the build pipeline.
         * @param phase 
         * @param pty_fd 
         * @param cancellable a {@link Dex.Cancellable}
         * @returns a {@link Foundry.BuildProgress}
         */
        build(phase: BuildPipelinePhase, pty_fd: number, cancellable: (Dex.Cancellable | null)): BuildProgress;

        /**
         * Clean the build pipeline. (e.g. `make clean`)
         * @param phase 
         * @param pty_fd 
         * @param cancellable a {@link Dex.Cancellable}
         * @returns a {@link Foundry.BuildProgress}
         */
        clean(phase: BuildPipelinePhase, pty_fd: number, cancellable: (Dex.Cancellable | null)): BuildProgress;

        /**
         * @param program the name of a program such as "meson"
         * @returns a {@link Dex.Future} that resolves to a   string containing the path or rejects with error.
         */
        contains_program(program: string): Dex.Future;

        /**
         * Gets the joined form of all append paths (e.g. "a:b:c").
         * @returns the joined append paths, or `null`
         */
        dup_append_path(): (string | null);

        dup_arch(): string;

        /**
         * Get the expected build system for the pipeline.
         */
        dup_build_system(): (string | null);

        /**
         * Gets the directory where the project should be built.
         * @returns a string containing a file path
         */
        dup_builddir(): string;

        /**
         * Gets the CONFIG to use for the platform.
         * @returns a {@link Foundry.Config}
         */
        dup_config(): Config;

        /**
         * Gets the device used for the pipeline.
         * @returns a {@link Foundry.Device}
         */
        dup_device(): Device;

        /**
         * Gets the joined form of all prepend paths (e.g. "a:b:c").
         * @returns the joined prepend paths, or `null`
         */
        dup_prepend_path(): (string | null);

        /**
         * Gets the SDK to use for the platform.
         * @returns a {@link Foundry.Sdk}
         */
        dup_sdk(): Sdk;

        /**
         * Gets the project directory path from the context as a string.
         * @returns a string containing the project directory path
         */
        dup_srcdir(): string;

        /**
         * Gets the triplet for a build pipeline.
         * @returns a {@link Foundry.Triplet}
         */
        dup_triplet(): Triplet;

        /**
         * Queries build stages for a build command for `file`.
         * @param file 
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.Command} or rejects with error.
         */
        find_build_flags(file: Gio.File): Dex.Future;

        /**
         * Gets the maximum phase reached by all completed stages.
         * 
         * The phase is calculated as the maximum phase of all stages that have
         * the completed property set to true, up to the first stage that is not
         * completed. If two stages in the same phase have different values for
         * completed, that phase has not been reached.
         * @returns the maximum phase reached
         */
        get_phase(): BuildPipelinePhase;

        /**
         * Gets a list of build targets.
         * 
         * This list is dynamically populated. If you want to wait for all
         * providers to complete use {@link Foundry.list_model_await} on
         * the resulting {@link Gio.ListModel}.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.BuildTarget} or rejects   with error.
         */
        list_build_targets(): Dex.Future;

        /**
         * Prepares the launcher for running within the build pipeline.
         * @param launcher a {@link Foundry.ProcessLauncher}
         * @param phase 
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        prepare(launcher: ProcessLauncher, phase: BuildPipelinePhase): Dex.Future;

        /**
         * Prepares the launcher for running within the build pipeline.
         * @param launcher a {@link Foundry.ProcessLauncher}
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        prepare_for_run(launcher: ProcessLauncher): Dex.Future;

        /**
         * Adds a path to the list of paths that will be prepended to PATH.
         * @param path the path to prepend
         */
        prepend_path(path: string): void;

        /**
         * Purge the build pipeline. (e.g. `make distclean`)
         * @param phase 
         * @param pty_fd 
         * @param cancellable a {@link Dex.Cancellable}
         * @returns a {@link Foundry.BuildProgress}
         */
        purge(phase: BuildPipelinePhase, pty_fd: number, cancellable: (Dex.Cancellable | null)): BuildProgress;

        /**
         * Query information about the state of all stages in the pipeline
         * and update as necessary. This method calls {@link Foundry.BuildStage.query}
         * for each stage in parallel and resolves when all queries are complete.
         * @returns a {@link Dex.Future} that resolves when all stage   queries have completed or rejects with an error.
         */
        query(): Dex.Future;

        /**
         * @param stage 
         */
        remove_stage(stage: BuildStage): void;

        /**
         * @param variable 
         * @param value 
         */
        setenv(variable: string, value: string): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace BuildProgress {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::builddir": (pspec: GObject.ParamSpec) => void;
            "notify::phase": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            builddir: string;
            phase: BuildPipelinePhase;
        }
    }

    /**
     * Manages build progress tracking and stage execution.
     * 
     * FoundryBuildProgress provides functionality for tracking build progress
     * including stage execution, artifact collection, and progress reporting.
     * It integrates with the build pipeline and provides real-time feedback
     * on build operations.
     * @gir-type Class
     */
    class BuildProgress extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<BuildProgress>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get builddir(): string;

        /**
         * @read-only
         * @default Foundry.BuildPipelinePhase.NONE
         */
        get phase(): BuildPipelinePhase;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: BuildProgress.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<BuildProgress.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof BuildProgress.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildProgress.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof BuildProgress.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildProgress.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof BuildProgress.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<BuildProgress.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param file 
         */
        add_artifact(file: Gio.File): void;

        /**
         * Gets a {@link Dex.Future} that will resolve when the progress
         * has completed running.
         * @returns a {@link Dex.Future}.
         */
        ["await"](): Dex.Future;

        /**
         * Gets a cancellable that will reject when the build has been cancelled.
         * @returns a {@link Dex.Future} that resolves when the   build operation has been cancelled.
         */
        dup_cancellable(): Dex.Cancellable;

        get_phase(): BuildPipelinePhase;

        /**
         * Gets a {@link Gio.ListModel} of {@link Gio.File} representing build arifacts.
         * 
         * This may include, for example, a path to a ".flatpak" bundle.
         * @returns a {@link Gio.ListModel} of {@link Gio.File}
         */
        list_artifacts(): Gio.ListModel;

        /**
         * @param launcher 
         */
        setup_pty(launcher: ProcessLauncher): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace BuildStage {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::completed": (pspec: GObject.ParamSpec) => void;
            "notify::kind": (pspec: GObject.ParamSpec) => void;
            "notify::phase": (pspec: GObject.ParamSpec) => void;
            "notify::pipeline": (pspec: GObject.ParamSpec) => void;
            "notify::priority": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            completed: boolean;
            kind: string;
            phase: BuildPipelinePhase;
            pipeline: BuildPipeline;
            priority: number;
            title: string;
        }
    }

    /**
     * A single state in a build pipeline.
     * 
     * Abstrct base class for implementing custom stages in the build pipeline.
     * @gir-type Class
     */
    abstract class BuildStage extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<BuildStage>;

        // Properties
        /**
         * @default false
         */
        get completed(): boolean;
        set completed(val: boolean);

        /**
         * @default null
         */
        get kind(): string;
        set kind(val: string);

        /**
         * @read-only
         * @default Foundry.BuildPipelinePhase.NONE
         */
        get phase(): BuildPipelinePhase;

        /**
         * @read-only
         */
        get pipeline(): BuildPipeline;

        /**
         * @read-only
         * @default 0
         */
        get priority(): number;

        /**
         * @default null
         */
        get title(): string;
        set title(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: BuildStage.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<BuildStage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof BuildStage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildStage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof BuildStage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildStage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof BuildStage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<BuildStage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Run the build for the stage.
         * @param progress 
         * @virtual
         */
        vfunc_build(progress: BuildProgress): Dex.Future;

        /**
         * Clean operation for the stage.
         * 
         * This is used to perform an equivalent of a `make clean` or
         * `ninja clean` for the build. It is not necessary on all stages
         * but any stage may implement it.
         * @param progress 
         * @virtual
         */
        vfunc_clean(progress: BuildProgress): Dex.Future;

        /**
         * Locates the compiler flags used to when compiling `file`.
         * 
         * Most build stages will not know about any build flags and therefore
         * do not need to implement this function.
         * @param file 
         * @virtual
         */
        vfunc_find_build_flags(file: Gio.File): Dex.Future;

        /**
         * @virtual
         */
        vfunc_get_phase(): BuildPipelinePhase;

        /**
         * @virtual
         */
        vfunc_get_priority(): number;

        /**
         * Lists the available {@link Foundry.BuildTarget} known to the stage.
         * 
         * Most build stages will not know about any build targets and therefore
         * do not need to implement this function.
         * @virtual
         */
        vfunc_list_build_targets(): Dex.Future;

        /**
         * Purge operation for the stage.
         * 
         * This is used to perform a purge of an existing pipeline.
         * 
         * The purge command is run when doing a rebuild.
         * @param progress 
         * @virtual
         */
        vfunc_purge(progress: BuildProgress): Dex.Future;

        /**
         * Query information about the state of the stage and update as
         * necessary.
         * 
         * Some implementations may choose to look at the state of contents
         * on disk and mark the stage as completed without running. Alternatively,
         * they may use it to mark a stage as needing to be run.
         * @virtual
         */
        vfunc_query(): Dex.Future;

        // Methods
        /**
         * Run the build for the stage.
         * @param progress 
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with an error.
         */
        build(progress: BuildProgress): Dex.Future;

        /**
         * Clean operation for the stage.
         * 
         * This is used to perform an equivalent of a `make clean` or
         * `ninja clean` for the build. It is not necessary on all stages
         * but any stage may implement it.
         * @param progress 
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with an error.
         */
        clean(progress: BuildProgress): Dex.Future;

        /**
         * @returns the kind of the stage such as "flatpak"
         */
        dup_kind(): (string | null);

        /**
         * @returns a {@link Foundry.BuildPipeline} or `null`
         */
        dup_pipeline(): (BuildPipeline | null);

        /**
         * @returns the title of the stage
         */
        dup_title(): (string | null);

        /**
         * Locates the compiler flags used to when compiling `file`.
         * 
         * Most build stages will not know about any build flags and therefore
         * do not need to implement this function.
         * @param file 
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.BuildFlags}.
         */
        find_build_flags(file: Gio.File): Dex.Future;

        get_completed(): boolean;

        get_phase(): BuildPipelinePhase;

        get_priority(): number;

        invalidate(): void;

        /**
         * Lists the available {@link Foundry.BuildTarget} known to the stage.
         * 
         * Most build stages will not know about any build targets and therefore
         * do not need to implement this function.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.BuildTarget} or   rejects with error.
         */
        list_build_targets(): Dex.Future;

        /**
         * Purge operation for the stage.
         * 
         * This is used to perform a purge of an existing pipeline.
         * 
         * The purge command is run when doing a rebuild.
         * @param progress 
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with an error.
         */
        purge(progress: BuildProgress): Dex.Future;

        /**
         * Query information about the state of the stage and update as
         * necessary.
         * 
         * Some implementations may choose to look at the state of contents
         * on disk and mark the stage as completed without running. Alternatively,
         * they may use it to mark a stage as needing to be run.
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with an error.
         */
        query(): Dex.Future;

        /**
         * @param completed 
         */
        set_completed(completed: boolean): void;

        /**
         * @param kind 
         */
        set_kind(kind: string): void;

        /**
         * @param title 
         */
        set_title(title: string): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace BuildTarget {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
            title: string;
        }
    }

    /**
     * Abstract base class representing a singular build target.
     * 
     * Build systems may export information about possible build targets and
     * to allow reaching a certain target.
     * @gir-type Class
     */
    abstract class BuildTarget extends GObject.Object {
        static $gtype: GObject.GType<BuildTarget>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: BuildTarget.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<BuildTarget.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof BuildTarget.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildTarget.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof BuildTarget.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BuildTarget.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof BuildTarget.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<BuildTarget.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        // Methods
        dup_id(): string;

        dup_title(): string;
    }


    namespace CliCommandTree {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Manages a hierarchical tree of CLI commands.
     * 
     * FoundryCliCommandTree provides functionality for organizing and managing
     * command line interface commands in a hierarchical structure. It supports
     * command registration, lookup, and provides efficient access to command
     * functionality for the CLI system.
     * @gir-type Class
     */
    class CliCommandTree extends GObject.Object {
        static $gtype: GObject.GType<CliCommandTree>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CliCommandTree.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CliCommandTree.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): CliCommandTree;

        // Signals
        /** @signal */
        connect<K extends keyof CliCommandTree.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CliCommandTree.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CliCommandTree.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CliCommandTree.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CliCommandTree.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CliCommandTree.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Gets the default instance for use by the foundry CLI tool.
         */
        static get_default(): CliCommandTree;

        // Methods
        /**
         * @param command_line 
         * @param line 
         * @param point 
         * @param current 
         */
        complete(command_line: CommandLine, line: string, point: number, current: string): string[];

        /**
         * @param args 
         * @param options 
         */
        lookup(args: string, options: CliOptions): CliCommand;

        /**
         * @param path 
         * @param command 
         */
        register(path: string, command: CliCommand): void;
    }


    namespace CliCommandTreeAddin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Addin interface for extending the CLI command tree.
     * @gir-type Class
     * @since 1.1
     */
    abstract class CliCommandTreeAddin extends GObject.Object {
        static $gtype: GObject.GType<CliCommandTreeAddin>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CliCommandTreeAddin.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CliCommandTreeAddin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof CliCommandTreeAddin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CliCommandTreeAddin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CliCommandTreeAddin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CliCommandTreeAddin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CliCommandTreeAddin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CliCommandTreeAddin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace CodeAction {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::kind": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            kind: string;
            name: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class CodeAction extends GObject.Object {
        static $gtype: GObject.GType<CodeAction>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get kind(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CodeAction.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CodeAction.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof CodeAction.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CodeAction.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CodeAction.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CodeAction.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CodeAction.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CodeAction.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_kind(): string;

        /**
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * Runs the code action and returns a {@link Dex.Future} that resolves
         * when the action has completed.
         * @virtual
         */
        vfunc_run(): Dex.Future;

        // Methods
        dup_kind(): string;

        dup_name(): string;

        /**
         * Runs the code action and returns a {@link Dex.Future} that resolves
         * when the action has completed.
         * @returns a {@link Dex.Future}.
         */
        run(): Dex.Future;
    }


    namespace CodeTemplate {
        // Signal signatures
        interface SignalSignatures extends Template.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::input": (pspec: GObject.ParamSpec) => void;
            "notify::tags": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Template.ConstructorProps {
            context: Context;
        }
    }

    /**
     * A template for generating new code in a project.
     * @gir-type Class
     */
    abstract class CodeTemplate extends Template {
        static $gtype: GObject.GType<CodeTemplate>;

        // Properties
        /**
         * @construct-only
         */
        get context(): Context;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CodeTemplate.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CodeTemplate.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof CodeTemplate.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CodeTemplate.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CodeTemplate.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CodeTemplate.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CodeTemplate.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CodeTemplate.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_context(): (Context | null);
    }


    namespace Command {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::argv": (pspec: GObject.ParamSpec) => void;
            "notify::cwd": (pspec: GObject.ParamSpec) => void;
            "notify::environ": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::locality": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            argv: string[];
            cwd: string;
            environ: string[];
            id: string;
            locality: CommandLocality;
            name: string;
            provider: CommandProvider;
        }
    }

    /**
     * A command that may be run in a shell environment.
     * @gir-type Class
     */
    class Command extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Command>;

        // Properties
        get argv(): string[];
        set argv(val: string[]);

        /**
         * @default null
         */
        get cwd(): string;
        set cwd(val: string);

        get environ(): string[];
        set environ(val: string[]);

        /**
         * @default null
         */
        get id(): string;
        set id(val: string);

        /**
         * @default Foundry.CommandLocality.PIPELINE
         */
        get locality(): CommandLocality;
        set locality(val: CommandLocality);

        /**
         * @default null
         */
        get name(): string;
        set name(val: string);

        /**
         * @read-only
         */
        get provider(): CommandProvider;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Command.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Command.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context): Command;

        // Signals
        /** @signal */
        connect<K extends keyof Command.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Command.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Command.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Command.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Command.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Command.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Checks to see if `self` is suitable to be run as the default command when
         * running a project. The priority indicates if it should take priority over
         * other commands which can be default. The highest priority value wins.
         * @virtual
         */
        vfunc_can_default(): [boolean, number];

        /**
         * Prepares `launcher` to run `self`.
         * 
         * If `pipeline` is set, the command may use that to run the command within
         * a particular environment based on the locality settings.
         * @param pipeline an optional {@link Foundry.BuildPipeline}
         * @param launcher a {@link Foundry.ProcessLauncher}
         * @param phase the phase of the pipeline.
         * @virtual
         */
        vfunc_prepare(pipeline: (BuildPipeline | null), launcher: ProcessLauncher, phase: BuildPipelinePhase): Dex.Future;

        // Methods
        /**
         * Checks to see if `self` is suitable to be run as the default command when
         * running a project. The priority indicates if it should take priority over
         * other commands which can be default. The highest priority value wins.
         * @returns `true` if `self` can be the default command
         */
        can_default(): [boolean, number];

        /**
         * @returns a string array of arguments for   the command to run.
         */
        dup_argv(): (string[] | null);

        dup_cwd(): string;

        /**
         * @returns a string array containing the   environment of `null`.
         */
        dup_environ(): (string[] | null);

        dup_id(): string;

        dup_name(): string;

        get_locality(): CommandLocality;

        /**
         * Prepares `launcher` to run `self`.
         * 
         * If `pipeline` is set, the command may use that to run the command within
         * a particular environment based on the locality settings.
         * @param pipeline an optional {@link Foundry.BuildPipeline}
         * @param launcher a {@link Foundry.ProcessLauncher}
         * @param phase the phase of the pipeline.
         * @returns a {@link Dex.Future} that resolves to any value   when the preparation has completed. Otherwise rejects with error.
         */
        prepare(pipeline: (BuildPipeline | null), launcher: ProcessLauncher, phase: BuildPipelinePhase): Dex.Future;

        /**
         * @param argv 
         */
        set_argv(argv: string): void;

        /**
         * @param cwd 
         */
        set_cwd(cwd: string): void;

        /**
         * @param environ 
         */
        set_environ(environ: string): void;

        /**
         * @param id 
         */
        set_id(id: string): void;

        /**
         * @param locality 
         */
        set_locality(locality: CommandLocality): void;

        /**
         * @param id 
         */
        set_name(id: string): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace CommandLine {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Abstract base class for command line interface implementations.
     * 
     * FoundryCommandLine provides the core interface for command line operations including
     * command parsing, execution, and communication with remote services. Concrete
     * implementations handle different command line modes and provide unified access
     * to Foundry functionality from the terminal.
     * @gir-type Class
     */
    abstract class CommandLine extends GObject.Object {
        static $gtype: GObject.GType<CommandLine>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CommandLine.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CommandLine.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): CommandLine;

        // Signals
        /** @signal */
        connect<K extends keyof CommandLine.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CommandLine.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CommandLine.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CommandLine.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CommandLine.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CommandLine.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * @param arg 
         */
        build_file_for_arg(arg: string): Gio.File;

        clear_progress(): void;

        /**
         * Gets an auth provider for this command line client if possible.
         * 
         * Currrently, this only returns an auth provider if the command line
         * client provides a TTY.
         */
        dup_auth_provider(): (AuthProvider | null);

        get_directory(): string;

        /**
         * @returns the environment of the command line
         */
        get_environ(): string[];

        get_stderr(): number;

        get_stdin(): number;

        get_stdout(): number;

        /**
         * @param name 
         */
        getenv(name: string): string;

        is_remote(): boolean;

        isatty(): boolean;

        /**
         * @param model 
         * @param entries 
         * @param format 
         * @param expected_type 
         */
        print_list(model: Gio.ListModel, entries: ObjectSerializerEntry, format: ObjectSerializerFormat, expected_type: GObject.GType): void;

        /**
         * @param object 
         * @param entries 
         * @param format 
         */
        print_object(object: GObject.Object, entries: ObjectSerializerEntry, format: ObjectSerializerFormat): void;

        /**
         * Queries the user for the information requested in `input`.
         * @param input a {@link Foundry.Input}
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        request_input(input: Input): Dex.Future;

        /**
         * Runs the command line.
         * @param argv 
         * @returns a {@link Dex.Future} that resolves to an int
         */
        run(argv: string): Dex.Future;

        /**
         * @param progress 
         */
        set_progress(progress: number): void;

        /**
         * @param title 
         */
        set_title(title: string): void;
    }


    namespace CommandManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Service managing command providers and their commands.
     * @gir-type Class
     */
    class CommandManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<CommandManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CommandManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CommandManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof CommandManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CommandManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CommandManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CommandManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CommandManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CommandManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Looks through available commands to find one matching `command_id`.
         * @param command_id an identifier matching a {@link Foundry.Command.id}
         * @returns a {@link Foundry.Command} or `null`
         */
        find_command(command_id: string): (Command | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace CommandProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class allowing plugins to provide commands.
     * @gir-type Class
     */
    abstract class CommandProvider<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<CommandProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CommandProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CommandProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof CommandProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CommandProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CommandProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CommandProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CommandProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CommandProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @virtual
         */
        vfunc_dup_name(): string;

        // Methods
        /**
         * @param command 
         */
        command_added(command: Command): void;

        /**
         * @param command 
         */
        command_removed(command: Command): void;

        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @returns the name of the provider
         */
        dup_name(): string;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace CommandStage {
        // Signal signatures
        interface SignalSignatures extends BuildStage.SignalSignatures {
            "notify::build-command": (pspec: GObject.ParamSpec) => void;
            "notify::clean-command": (pspec: GObject.ParamSpec) => void;
            "notify::phony": (pspec: GObject.ParamSpec) => void;
            "notify::purge-command": (pspec: GObject.ParamSpec) => void;
            "notify::queyr-file": (pspec: GObject.ParamSpec) => void;
            "notify::completed": (pspec: GObject.ParamSpec) => void;
            "notify::kind": (pspec: GObject.ParamSpec) => void;
            "notify::phase": (pspec: GObject.ParamSpec) => void;
            "notify::pipeline": (pspec: GObject.ParamSpec) => void;
            "notify::priority": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends BuildStage.ConstructorProps, Json.Serializable.ConstructorProps {
            build_command: Command;
            buildCommand: Command;
            clean_command: Command;
            cleanCommand: Command;
            phony: boolean;
            purge_command: Command;
            purgeCommand: Command;
            queyr_file: Gio.File;
            queyrFile: Gio.File;
        }
    }

    /**
     * An {@link Foundry.BuildStage} that runs a command in the build pipeline.
     * @gir-type Class
     */
    class CommandStage extends BuildStage implements Json.Serializable {
        static $gtype: GObject.GType<CommandStage>;

        // Properties
        /**
         * @read-only
         */
        get build_command(): Command;

        /**
         * @read-only
         */
        get buildCommand(): Command;

        /**
         * @read-only
         */
        get clean_command(): Command;

        /**
         * @read-only
         */
        get cleanCommand(): Command;

        /**
         * @read-only
         * @default false
         */
        get phony(): boolean;

        /**
         * @read-only
         */
        get purge_command(): Command;

        /**
         * @read-only
         */
        get purgeCommand(): Command;

        /**
         * @read-only
         */
        get queyr_file(): Gio.File;

        /**
         * @read-only
         */
        get queyrFile(): Gio.File;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CommandStage.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CommandStage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, phase: BuildPipelinePhase, build_command: (Command | null), clean_command: (Command | null), purge_command: (Command | null), query_file: (Gio.File | null), phony: boolean): CommandStage;

        // Signals
        /** @signal */
        connect<K extends keyof CommandStage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CommandStage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CommandStage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CommandStage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CommandStage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CommandStage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @returns a {@link Foundry.Command}
         */
        dup_build_command(): (Command | null);

        /**
         * @returns a {@link Foundry.Command}
         */
        dup_clean_command(): (Command | null);

        /**
         * @returns a {@link Foundry.Command}
         */
        dup_purge_command(): (Command | null);

        /**
         * @returns the file to check for completion    or `null` if unset.
         */
        dup_query_file(): (Gio.File | null);
    }


    namespace CompileCommands {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class CompileCommands extends GObject.Object {
        static $gtype: GObject.GType<CompileCommands>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CompileCommands.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CompileCommands.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](file: Gio.File): CompileCommands;

        // Signals
        /** @signal */
        connect<K extends keyof CompileCommands.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CompileCommands.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CompileCommands.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CompileCommands.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CompileCommands.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CompileCommands.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Locates the commands to compile the `file` requested.
         * 
         * If `directory` is non-NULL, then the directory to run the command from
         * is placed in `directory`.
         * @param file a {@link Gio.File} representing the file to lookup
         * @param system_includes system include dirs if any
         * @returns A string array or `null` if   there was a failure to locate or parse the command.
         */
        lookup(file: Gio.File, system_includes: string): [(string[] | null), Gio.File | null];
    }


    namespace CompletionProposal {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::after": (pspec: GObject.ParamSpec) => void;
            "notify::comment": (pspec: GObject.ParamSpec) => void;
            "notify::details": (pspec: GObject.ParamSpec) => void;
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::typed-text": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            after: string;
            comment: string;
            details: string;
            icon: Gio.Icon;
            typed_text: string;
            typedText: string;
        }
    }

    /**
     * An abstract base class for completion providers.
     * @gir-type Class
     */
    abstract class CompletionProposal extends GObject.Object {
        static $gtype: GObject.GType<CompletionProposal>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get after(): string;

        /**
         * @read-only
         * @default null
         */
        get comment(): string;

        /**
         * @read-only
         * @default null
         */
        get details(): string;

        /**
         * @read-only
         */
        get icon(): Gio.Icon;

        /**
         * @read-only
         * @default null
         */
        get typed_text(): string;

        /**
         * @read-only
         * @default null
         */
        get typedText(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CompletionProposal.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CompletionProposal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof CompletionProposal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CompletionProposal.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CompletionProposal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CompletionProposal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CompletionProposal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CompletionProposal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_after(): string;

        /**
         * @virtual
         */
        vfunc_dup_comment(): string;

        /**
         * @virtual
         */
        vfunc_dup_details(): string;

        /**
         * @virtual
         */
        vfunc_dup_icon(): (Gio.Icon | null);

        /**
         * If the proposal supports inserting a snippet, then this contains
         * the text of that snippet.
         * 
         * The format should be in `GtkSourceSnippet` format.
         * @virtual
         */
        vfunc_dup_snippet_text(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_typed_text(): (string | null);

        // Methods
        dup_after(): string;

        dup_comment(): string;

        dup_details(): string;

        dup_icon(): (Gio.Icon | null);

        /**
         * If the proposal supports inserting a snippet, then this contains
         * the text of that snippet.
         * 
         * The format should be in `GtkSourceSnippet` format.
         */
        dup_snippet_text(): (string | null);

        dup_typed_text(): (string | null);
    }


    namespace CompletionProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::document": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-ifo": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            document: TextDocument;
            plugin_ifo: Peas.PluginInfo;
            pluginIfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for providing code completion suggestions.
     * 
     * FoundryCompletionProvider provides the core interface for code completion
     * functionality including suggestion generation and context-aware completions.
     * Concrete implementations handle specific languages and provide specialized
     * completion logic for different programming contexts and editor states.
     * @gir-type Class
     */
    abstract class CompletionProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<CompletionProvider>;

        // Properties
        /**
         * @construct-only
         */
        get document(): TextDocument;

        /**
         * @construct-only
         */
        get plugin_ifo(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginIfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CompletionProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CompletionProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof CompletionProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CompletionProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CompletionProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CompletionProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CompletionProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CompletionProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Asynchronously generate a list of completion items.
         * @param request a {@link Foundry.CompletionRequest}
         * @virtual
         */
        vfunc_complete(request: CompletionRequest): Dex.Future;

        /**
         * @param iter 
         * @param ch 
         * @virtual
         */
        vfunc_is_trigger(iter: TextIter, ch: string): boolean;

        /**
         * Asynchronously refilter `model` or provides an alternate result set.
         * @param request a {@link Foundry.CompletionRequest}
         * @param model a {@link Gio.ListModel}
         * @virtual
         */
        vfunc_refilter(request: CompletionRequest, model: Gio.ListModel): Dex.Future;

        // Methods
        /**
         * Asynchronously generate a list of completion items.
         * @param request a {@link Foundry.CompletionRequest}
         * @returns {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.CompletionProposal} or   rejects with error.
         */
        complete(request: CompletionRequest): Dex.Future;

        dup_document(): TextDocument;

        /**
         * @returns a {@link Peas.PluginInfo}
         */
        get_plugin_info(): Peas.PluginInfo;

        /**
         * @param iter 
         * @param ch 
         */
        is_trigger(iter: TextIter, ch: string): boolean;

        /**
         * Asynchronously refilter `model` or provides an alternate result set.
         * @param request a {@link Foundry.CompletionRequest}
         * @param model a {@link Gio.ListModel}
         * @returns {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.CompletionProposal} or   rejects with error.
         */
        refilter(request: CompletionRequest, model: Gio.ListModel): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace CompletionRequest {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::word": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            word: string;
        }
    }

    /**
     * Represents a single completion request.
     * @gir-type Class
     */
    abstract class CompletionRequest extends GObject.Object {
        static $gtype: GObject.GType<CompletionRequest>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get word(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CompletionRequest.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<CompletionRequest.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof CompletionRequest.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CompletionRequest.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CompletionRequest.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CompletionRequest.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CompletionRequest.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CompletionRequest.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets the file that is to be completed.
         * @virtual
         */
        vfunc_dup_file(): (Gio.File | null);

        /**
         * Gets the language identifier for the completion request, such as "c" or "js".
         * 
         * The language identifiers are expected to match GtkSourceView language identifiers.
         * @virtual
         */
        vfunc_dup_language_id(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_word(): (string | null);

        /**
         * @virtual
         */
        vfunc_get_activation(): CompletionActivation;

        /**
         * This gets the bounds for the completion request.
         * 
         * Generally, `begin` will be right after a break character such as "." and
         * `end` will be where cursor is currently.
         * @virtual
         */
        vfunc_get_bounds(): [TextIter | null, TextIter | null];

        // Methods
        /**
         * Gets the file that is to be completed.
         */
        dup_file(): (Gio.File | null);

        /**
         * Gets the language identifier for the completion request, such as "c" or "js".
         * 
         * The language identifiers are expected to match GtkSourceView language identifiers.
         * @returns the language identifier or `null`
         */
        dup_language_id(): (string | null);

        /**
         * @returns the word to complete
         */
        dup_word(): (string | null);

        get_activation(): CompletionActivation;

        /**
         * This gets the bounds for the completion request.
         * 
         * Generally, `begin` will be right after a break character such as "." and
         * `end` will be where cursor is currently.
         */
        get_bounds(): [TextIter | null, TextIter | null];
    }


    namespace Config {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::active": (pspec: GObject.ParamSpec) => void;
            "notify::build-system": (pspec: GObject.ParamSpec) => void;
            "notify::can-default": (pspec: GObject.ParamSpec) => void;
            "notify::config-opts": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::priority": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            active: boolean;
            build_system: string;
            buildSystem: string;
            can_default: boolean;
            canDefault: boolean;
            config_opts: string[];
            configOpts: string[];
            id: string;
            name: string;
            priority: number;
            provider: ConfigProvider;
        }
    }

    /**
     * An abstract base class for build configurations.
     * @gir-type Class
     */
    abstract class Config extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Config>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get active(): boolean;

        /**
         * @read-only
         * @default null
         */
        get build_system(): string;

        /**
         * @read-only
         * @default null
         */
        get buildSystem(): string;

        /**
         * @read-only
         * @default false
         */
        get can_default(): boolean;

        /**
         * @read-only
         * @default false
         */
        get canDefault(): boolean;

        /**
         * @read-only
         */
        get config_opts(): string[];

        /**
         * @read-only
         */
        get configOpts(): string[];

        /**
         * @default null
         */
        get id(): string;
        set id(val: string);

        /**
         * @default null
         */
        get name(): string;
        set name(val: string);

        /**
         * @read-only
         * @default 0
         */
        get priority(): number;

        /**
         * @read-only
         */
        get provider(): ConfigProvider;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Config.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Config.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Config.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Config.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Config.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Config.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Config.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Config.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_can_default(): [boolean, number];

        /**
         * Change the SDK for the config and write the config to disk.
         * @param sdk a {@link Foundry.Sdk}
         * @virtual
         */
        vfunc_change_sdk(sdk: Sdk): Dex.Future;

        /**
         * The build system the configuration specifies to be used.
         * @virtual
         */
        vfunc_dup_build_system(): (string | null);

        /**
         * Determines where the project build should occur.
         * @param pipeline the {@link Foundry.BuildPipeline} which will perform the build
         * @virtual
         */
        vfunc_dup_builddir(pipeline: BuildPipeline): string;

        /**
         * The config options.
         * 
         * This is generally passed to something like `meson setup` or `cmake` when
         * configuring the project for a build.
         * @virtual
         */
        vfunc_dup_config_opts(): (string[] | null);

        /**
         * Gets the default command for the config, if any.
         * @virtual
         */
        vfunc_dup_default_command(): (Command | null);

        /**
         * Gets the environment variables to use for a particular locality.
         * @param locality 
         * @virtual
         */
        vfunc_dup_environ(locality: Locality): (string[] | null);

        /**
         * Tries to locate the preferred SDK for a configuration and device.
         * 
         * This might be used to locate an SDK which is not yet installed but would
         * need to be installed to properly setup a build pipeline.
         * @param device a {@link Foundry.Device}
         * @virtual
         */
        vfunc_resolve_sdk(device: Device): Dex.Future;

        /**
         * Requests the config save back to disk.
         * @virtual
         */
        vfunc_save(): Dex.Future;

        /**
         * Checks if the configuration supports the given SDK.
         * 
         * This allows configurations to restrict which SDKs they can use. By default,
         * all configurations support all SDKs, but subclasses can override this
         * behavior.
         * @param sdk a {@link Foundry.Sdk}
         * @virtual
         */
        vfunc_supports_sdk(sdk: Sdk): boolean;

        // Methods
        /**
         * Change the SDK for the config and write the config to disk.
         * @param sdk a {@link Foundry.Sdk}
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error.
         */
        change_sdk(sdk: Sdk): Dex.Future;

        /**
         * The build system the configuration specifies to be used.
         * @returns a build system or `null`
         */
        dup_build_system(): (string | null);

        /**
         * Determines where the project build should occur.
         * @param pipeline the {@link Foundry.BuildPipeline} which will perform the build
         * @returns the directory where the build should occur
         */
        dup_builddir(pipeline: BuildPipeline): string;

        /**
         * The config options.
         * 
         * This is generally passed to something like `meson setup` or `cmake` when
         * configuring the project for a build.
         * @returns the config options for   configuring the project.
         */
        dup_config_opts(): (string[] | null);

        /**
         * Gets the default command for the config, if any.
         * @returns a {@link Foundry.Config} or `null`.
         */
        dup_default_command(): (Command | null);

        /**
         * Gets the environment variables to use for a particular locality.
         * @param locality 
         * @returns an array of UTF-8 encoded strings   or `null` to use the default environment.
         */
        dup_environ(locality: Locality): (string[] | null);

        /**
         * @returns the identifier of the config
         */
        dup_id(): (string | null);

        /**
         * Gets the user-visible name for the configuration.
         * @returns a newly allocated string
         */
        dup_name(): string;

        /**
         * Gets the provider which provides this config.
         */
        dup_provider(): (ConfigProvider | null);

        get_active(): boolean;

        /**
         * Tries to locate the preferred SDK for a configuration and device.
         * 
         * This might be used to locate an SDK which is not yet installed but would
         * need to be installed to properly setup a build pipeline.
         * @param device a {@link Foundry.Device}
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.Sdk} or rejects with error.
         */
        resolve_sdk(device: Device): Dex.Future;

        /**
         * Requests the config save back to disk.
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        save(): Dex.Future;

        /**
         * Sets the identifier of the config.
         * 
         * This should only be called by {@link Foundry.ConfigProvider} on their
         * {@link Foundry.Config} before they have been registered.
         * @param id the unique identifier for the config
         */
        set_id(id: string): void;

        /**
         * Set the user-visible name of the config.
         * 
         * This should only be called by implementations of {@link Foundry.ConfigProvider}.
         * @param name 
         */
        set_name(name: string): void;

        /**
         * Checks if the configuration supports the given SDK.
         * 
         * This allows configurations to restrict which SDKs they can use. By default,
         * all configurations support all SDKs, but subclasses can override this
         * behavior.
         * @param sdk a {@link Foundry.Sdk}
         * @returns `true` if `self` supports `sdk`, `false` otherwise
         */
        supports_sdk(sdk: Sdk): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace ConfigManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::config": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            config: Config;
        }
    }

    /**
     * Manage configuration providers and their configs.
     * @gir-type Class
     */
    class ConfigManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<ConfigManager>;

        // Properties
        get config(): Config;
        set config(val: Config);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ConfigManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ConfigManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ConfigManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ConfigManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ConfigManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ConfigManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ConfigManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ConfigManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the active configuration
         * @returns a {@link Foundry.Config} or `null`
         */
        dup_config(): (Config | null);

        /**
         * Looks through available configs to find one matching `config_id`.
         * @param config_id an identifier matching a {@link Foundry.Config.id}
         * @returns a {@link Foundry.Config} or `null`
         */
        find_config(config_id: string): (Config | null);

        /**
         * Sets the active configuration for the config manager.
         * 
         * Other services such as the build manager or sdk manager may respond
         * to changes of this property and update accordingly.
         * @param config 
         */
        set_config(config: Config): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace ConfigProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class for plugins to implement access to custom configs.
     * @gir-type Class
     */
    abstract class ConfigProvider<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<ConfigProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ConfigProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ConfigProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ConfigProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ConfigProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ConfigProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ConfigProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ConfigProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ConfigProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @virtual
         */
        vfunc_dup_name(): string;

        // Methods
        /**
         * @param config 
         */
        config_added(config: Config): void;

        /**
         * @param config 
         */
        config_removed(config: Config): void;

        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @returns the name of the provider
         */
        dup_name(): string;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Context {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::build-manager": (pspec: GObject.ParamSpec) => void;
            "notify::build-system": (pspec: GObject.ParamSpec) => void;
            "notify::command-manager": (pspec: GObject.ParamSpec) => void;
            "notify::config-manager": (pspec: GObject.ParamSpec) => void;
            "notify::debugger-manager": (pspec: GObject.ParamSpec) => void;
            "notify::dependency-manager": (pspec: GObject.ParamSpec) => void;
            "notify::device-manager": (pspec: GObject.ParamSpec) => void;
            "notify::diagnostic-manager": (pspec: GObject.ParamSpec) => void;
            "notify::doap-service": (pspec: GObject.ParamSpec) => void;
            "notify::documentation-manager": (pspec: GObject.ParamSpec) => void;
            "notify::file-manager": (pspec: GObject.ParamSpec) => void;
            "notify::forge-manager": (pspec: GObject.ParamSpec) => void;
            "notify::intent-manager": (pspec: GObject.ParamSpec) => void;
            "notify::llm-manager": (pspec: GObject.ParamSpec) => void;
            "notify::log-manager": (pspec: GObject.ParamSpec) => void;
            "notify::lsp-manager": (pspec: GObject.ParamSpec) => void;
            "notify::operation-manager": (pspec: GObject.ParamSpec) => void;
            "notify::project-directory": (pspec: GObject.ParamSpec) => void;
            "notify::run-manager": (pspec: GObject.ParamSpec) => void;
            "notify::sdk-manager": (pspec: GObject.ParamSpec) => void;
            "notify::search-manager": (pspec: GObject.ParamSpec) => void;
            "notify::secret-service": (pspec: GObject.ParamSpec) => void;
            "notify::state-directory": (pspec: GObject.ParamSpec) => void;
            "notify::terminal-service": (pspec: GObject.ParamSpec) => void;
            "notify::test-manager": (pspec: GObject.ParamSpec) => void;
            "notify::text-manager": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::tweak-manager": (pspec: GObject.ParamSpec) => void;
            "notify::vcs-manager": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            build_manager: BuildManager;
            buildManager: BuildManager;
            build_system: string;
            buildSystem: string;
            command_manager: CommandManager;
            commandManager: CommandManager;
            config_manager: ConfigManager;
            configManager: ConfigManager;
            debugger_manager: DebuggerManager;
            debuggerManager: DebuggerManager;
            dependency_manager: DependencyManager;
            dependencyManager: DependencyManager;
            device_manager: DeviceManager;
            deviceManager: DeviceManager;
            diagnostic_manager: DiagnosticManager;
            diagnosticManager: DiagnosticManager;
            doap_service: DoapService;
            doapService: DoapService;
            documentation_manager: DocumentationManager;
            documentationManager: DocumentationManager;
            file_manager: FileManager;
            fileManager: FileManager;
            forge_manager: ForgeManager;
            forgeManager: ForgeManager;
            intent_manager: IntentManager;
            intentManager: IntentManager;
            llm_manager: LlmManager;
            llmManager: LlmManager;
            log_manager: LogManager;
            logManager: LogManager;
            lsp_manager: LspManager;
            lspManager: LspManager;
            operation_manager: OperationManager;
            operationManager: OperationManager;
            project_directory: Gio.File;
            projectDirectory: Gio.File;
            run_manager: RunManager;
            runManager: RunManager;
            sdk_manager: SdkManager;
            sdkManager: SdkManager;
            search_manager: SearchManager;
            searchManager: SearchManager;
            secret_service: SecretService;
            secretService: SecretService;
            state_directory: Gio.File;
            stateDirectory: Gio.File;
            terminal_service: TerminalService;
            terminalService: TerminalService;
            test_manager: TestManager;
            testManager: TestManager;
            text_manager: TextManager;
            textManager: TextManager;
            title: string;
            tweak_manager: TweakManager;
            tweakManager: TweakManager;
            vcs_manager: VcsManager;
            vcsManager: VcsManager;
        }
    }

    /**
     * The central object representing an open project.
     * 
     * FoundryContext serves as the main coordinator for all Foundry services and
     * provides access to managers for files, builds, debugging, and other development
     * tools. It maintains the application state and facilitates communication between
     * different components of the development environment.
     * @gir-type Class
     */
    class Context extends GObject.Object {
        static $gtype: GObject.GType<Context>;

        // Properties
        /**
         * @read-only
         */
        get build_manager(): BuildManager;

        /**
         * @read-only
         */
        get buildManager(): BuildManager;

        /**
         * @read-only
         * @default null
         */
        get build_system(): string;

        /**
         * @read-only
         * @default null
         */
        get buildSystem(): string;

        /**
         * @read-only
         */
        get command_manager(): CommandManager;

        /**
         * @read-only
         */
        get commandManager(): CommandManager;

        /**
         * @read-only
         */
        get config_manager(): ConfigManager;

        /**
         * @read-only
         */
        get configManager(): ConfigManager;

        /**
         * @read-only
         */
        get debugger_manager(): DebuggerManager;

        /**
         * @read-only
         */
        get debuggerManager(): DebuggerManager;

        /**
         * @read-only
         */
        get dependency_manager(): DependencyManager;

        /**
         * @read-only
         */
        get dependencyManager(): DependencyManager;

        /**
         * @read-only
         */
        get device_manager(): DeviceManager;

        /**
         * @read-only
         */
        get deviceManager(): DeviceManager;

        /**
         * @read-only
         */
        get diagnostic_manager(): DiagnosticManager;

        /**
         * @read-only
         */
        get diagnosticManager(): DiagnosticManager;

        /**
         * @since 1.1
         * @read-only
         */
        get doap_service(): DoapService;

        /**
         * @since 1.1
         * @read-only
         */
        get doapService(): DoapService;

        /**
         * @read-only
         */
        get documentation_manager(): DocumentationManager;

        /**
         * @read-only
         */
        get documentationManager(): DocumentationManager;

        /**
         * @read-only
         */
        get file_manager(): FileManager;

        /**
         * @read-only
         */
        get fileManager(): FileManager;

        /**
         * @since 1.1
         * @read-only
         */
        get forge_manager(): ForgeManager;

        /**
         * @since 1.1
         * @read-only
         */
        get forgeManager(): ForgeManager;

        /**
         * @read-only
         */
        get intent_manager(): IntentManager;

        /**
         * @read-only
         */
        get intentManager(): IntentManager;

        /**
         * @read-only
         */
        get llm_manager(): LlmManager;

        /**
         * @read-only
         */
        get llmManager(): LlmManager;

        /**
         * @read-only
         */
        get log_manager(): LogManager;

        /**
         * @read-only
         */
        get logManager(): LogManager;

        /**
         * @read-only
         */
        get lsp_manager(): LspManager;

        /**
         * @read-only
         */
        get lspManager(): LspManager;

        /**
         * @read-only
         */
        get operation_manager(): OperationManager;

        /**
         * @read-only
         */
        get operationManager(): OperationManager;

        /**
         * The directory containing the project.
         * 
         * This is generally the directory which contains ".git" and ".foundry".
         * @read-only
         */
        get project_directory(): Gio.File;

        /**
         * The directory containing the project.
         * 
         * This is generally the directory which contains ".git" and ".foundry".
         * @read-only
         */
        get projectDirectory(): Gio.File;

        /**
         * @read-only
         */
        get run_manager(): RunManager;

        /**
         * @read-only
         */
        get runManager(): RunManager;

        /**
         * @read-only
         */
        get sdk_manager(): SdkManager;

        /**
         * @read-only
         */
        get sdkManager(): SdkManager;

        /**
         * @read-only
         */
        get search_manager(): SearchManager;

        /**
         * @read-only
         */
        get searchManager(): SearchManager;

        /**
         * @read-only
         */
        get secret_service(): SecretService;

        /**
         * @read-only
         */
        get secretService(): SecretService;

        /**
         * The directory of the context, which is typically ".foundry" within
         * the {@link Foundry.Context.project_directory}.
         * @read-only
         */
        get state_directory(): Gio.File;

        /**
         * The directory of the context, which is typically ".foundry" within
         * the {@link Foundry.Context.project_directory}.
         * @read-only
         */
        get stateDirectory(): Gio.File;

        /**
         * @read-only
         */
        get terminal_service(): TerminalService;

        /**
         * @read-only
         */
        get terminalService(): TerminalService;

        /**
         * @read-only
         */
        get test_manager(): TestManager;

        /**
         * @read-only
         */
        get testManager(): TestManager;

        /**
         * @read-only
         */
        get text_manager(): TextManager;

        /**
         * @read-only
         */
        get textManager(): TextManager;

        /**
         * @default null
         */
        get title(): string;
        set title(val: string);

        /**
         * @read-only
         */
        get tweak_manager(): TweakManager;

        /**
         * @read-only
         */
        get tweakManager(): TweakManager;

        /**
         * @read-only
         */
        get vcs_manager(): VcsManager;

        /**
         * @read-only
         */
        get vcsManager(): VcsManager;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Context.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Context.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](foundry_dir: string, project_dir: (string | null), flags: ContextFlags, cancellable: (Dex.Cancellable | null)): Context;

        static new_for_user(cancellable: (Dex.Cancellable | null)): Context;

        // Signals
        /** @signal */
        connect<K extends keyof Context.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Context.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Context.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Context.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Context.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Context.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Attempts to locate the nearest .foundry directory starting from `path`.
         * @param path the starting path
         * @param cancellable an optional cancellable
         */
        static discover(path: string, cancellable: (Dex.Cancellable | null)): Dex.Future;

        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Gets a {@link Gio.ActionGroup} that contains various actions for the context.
         * 
         * Actions may be provided by subclassing FoundryService and implementing the
         * {@link Gio.ActionGroup} interface.
         * @returns a {@link Gio.ActionGroup}
         */
        dup_action_group(): Gio.ActionGroup;

        /**
         * Gets the {@link Foundry.BuildManager} instance.
         * @returns a {@link Foundry.BuildManager}
         */
        dup_build_manager(): BuildManager;

        /**
         * Gets the name of the build system to use.
         * 
         * First the settings are checked. If set, that is preferred. After that,
         * the configuration is checked to see if it specifies a build system.
         * 
         * Otherwise, `null` is returned.
         * 
         * Build addins can also affect which build system is used and you may
         * check what the build pipeline thinks should be the build system (which
         * checks this value) by using {@link Foundry.BuildPipeline.build_system}.
         * @returns a build-system name or `null`
         */
        dup_build_system(): (string | null);

        /**
         * Gets the {@link Foundry.CommandManager} instance.
         * @returns a {@link Foundry.CommandManager}
         */
        dup_command_manager(): CommandManager;

        /**
         * Gets the {@link Foundry.ConfigManager} instance.
         * @returns a {@link Foundry.ConfigManager}
         */
        dup_config_manager(): ConfigManager;

        /**
         * Gets the {@link Foundry.DBusService} instance.
         * @returns a {@link Foundry.DBusService}
         */
        dup_dbus_service(): DBusService;

        /**
         * Gets the {@link Foundry.DebuggerManager} instance.
         * @returns a {@link Foundry.DebuggerManager}
         */
        dup_debugger_manager(): DebuggerManager;

        dup_default_license(): (License | null);

        /**
         * Gets the {@link Foundry.DependencyManager} instance.
         * @returns a {@link Foundry.DependencyManager}
         */
        dup_dependency_manager(): DependencyManager;

        /**
         * Gets the {@link Foundry.DeviceManager} instance.
         * @returns a {@link Foundry.DeviceManager}
         */
        dup_device_manager(): DeviceManager;

        /**
         * Gets the {@link Foundry.DiagnosticManager} instance.
         * @returns a {@link Foundry.DiagnosticManager}
         */
        dup_diagnostic_manager(): DiagnosticManager;

        /**
         * Gets the {@link Foundry.DoapService} instance.
         * @returns a {@link Foundry.DoapService}
         */
        dup_doap_service(): DoapService;

        /**
         * Gets the {@link Foundry.DocumentationManager} instance.
         * @returns a {@link Foundry.DocumentationManager}
         */
        dup_documentation_manager(): DocumentationManager;

        /**
         * Gets the {@link Foundry.FileManager} instance.
         * @returns a {@link Foundry.FileManager}
         */
        dup_file_manager(): FileManager;

        /**
         * Gets the {@link Foundry.ForgeManager} instance.
         * @returns a {@link Foundry.ForgeManager}
         */
        dup_forge_manager(): ForgeManager;

        /**
         * Gets the {@link Foundry.IntentManager} instance.
         * @returns a {@link Foundry.IntentManager}
         */
        dup_intent_manager(): IntentManager;

        /**
         * Gets the {@link Foundry.LlmManager} instance.
         * @returns a {@link Foundry.LlmManager}
         */
        dup_llm_manager(): LlmManager;

        /**
         * Gets the {@link Foundry.LogManager} instance.
         * @returns a {@link Foundry.LogManager}
         */
        dup_log_manager(): LogManager;

        /**
         * Gets the {@link Foundry.LspManager} instance.
         * @returns a {@link Foundry.LspManager}
         */
        dup_lsp_manager(): LspManager;

        /**
         * Gets the {@link Foundry.OperationManager} instance.
         * @returns a {@link Foundry.OperationManager}
         */
        dup_operation_manager(): OperationManager;

        /**
         * Gets the {@link Foundry.Context.project_directory}.
         * @returns a {@link Gio.File}
         */
        dup_project_directory(): Gio.File;

        /**
         * Gets the {@link Foundry.RunManager} instance.
         * @returns a {@link Foundry.RunManager}
         */
        dup_run_manager(): RunManager;

        /**
         * Gets the {@link Foundry.SdkManager} instance.
         * @returns a {@link Foundry.SdkManager}
         */
        dup_sdk_manager(): SdkManager;

        /**
         * Gets the {@link Foundry.SearchManager} instance.
         * @returns a {@link Foundry.SearchManager}
         */
        dup_search_manager(): SearchManager;

        /**
         * Gets the {@link Foundry.SecretService} instance.
         * @returns a {@link Foundry.SecretService}
         */
        dup_secret_service(): SecretService;

        /**
         * @param service_type 
         */
        dup_service_typed(service_type: GObject.GType): Service;

        /**
         * Gets the {@link Foundry.Context.state_directory}.
         * @returns a {@link Gio.File}
         */
        dup_state_directory(): Gio.File;

        /**
         * Gets the {@link Foundry.TerminalService} instance.
         * @returns a {@link Foundry.TerminalService}
         */
        dup_terminal_service(): TerminalService;

        /**
         * Gets the {@link Foundry.TestManager} instance.
         * @returns a {@link Foundry.TestManager}
         */
        dup_test_manager(): TestManager;

        /**
         * Gets the {@link Foundry.TextManager} instance.
         * @returns a {@link Foundry.TextManager}
         */
        dup_text_manager(): TextManager;

        dup_title(): (string | null);

        /**
         * Gets the {@link Foundry.TweakManager} instance.
         * @returns a {@link Foundry.TweakManager}
         */
        dup_tweak_manager(): TweakManager;

        /**
         * Gets the {@link Foundry.VcsManager} instance.
         * @returns a {@link Foundry.VcsManager}
         */
        dup_vcs_manager(): VcsManager;

        is_shared(): boolean;

        /**
         * @returns a {@link Gio.ListModel} of {@link Foundry.Service}
         */
        list_services(): Gio.ListModel;

        /**
         * This function is functionally equivalent to calling
         * {@link Foundry.Context.load_settings} with the "org.gnome.foundry.project"
         * gsettings schema id.
         * @returns a {@link Foundry.Settings}
         */
        load_project_settings(): Settings;

        /**
         * Loads layered {@link Gio.Settings} as a {@link Foundry.Settings}.
         * 
         * The {@link Foundry.Settings} allows for settings to come from multiple
         * layers such as user-defaults, project-defaults, and user-overrides.
         * @param schema_id the gsettings schema identifier
         * @param schema_path an optional schema path
         * @returns a {@link Foundry.Settings}
         */
        load_settings(schema_id: string, schema_path: (string | null)): Settings;

        /**
         * Checks if network is currently allowed.
         * 
         * This checks if data is allowed on metered connections and if the current
         * network connection is metered.
         * @returns `true` if network is allowed
         */
        network_allowed(): boolean;

        /**
         * Save the foundry state to the {@link Foundry.Context.directory}.
         * @param cancellable 
         * @returns A {@link Dex.Future} that will resolve to   a boolean.
         */
        save(cancellable: Dex.Cancellable): Dex.Future;

        /**
         * @param title 
         */
        set_title(title: string): void;

        /**
         * Requests that the context shutdown and cleanup state.
         * @returns a {@link Dex.Future} that resolves when the  context has shutdown.
         */
        shutdown(): Dex.Future;
    }


    namespace Contextual {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Json.Serializable.ConstructorProps {
            context: Context;
        }
    }

    /**
     * Abstract base class for objects that are associated with a Foundry context.
     * 
     * FoundryContextual provides the core interface for objects that need
     * access to the Foundry context and its services. It includes context
     * management, serialization support, and provides a unified interface
     * for context-aware objects throughout the development environment.
     * @gir-type Class
     */
    abstract class Contextual extends GObject.Object implements Json.Serializable {
        static $gtype: GObject.GType<Contextual>;

        // Properties
        /**
         * @construct-only
         */
        get context(): Context;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Contextual.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Contextual.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Contextual.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Contextual.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Contextual.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Contextual.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Contextual.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Contextual.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * This method provides a checked way to get a `context` for the contextual
         * 
         * If the resulting context is `null`, then the error is set to an
         * appropriate error.
         * @returns a {@link Foundry.Context} or `error` is set.
         */
        acquire(): Context;

        /**
         * Gets the {@link Foundry.Context} that `self` is a part of while safely increasing
         * the reference count of the resulting {@link Foundry.Context} by 1.
         * @returns a {@link Foundry.Context} or `null`
         */
        dup_context(): (Context | null);

        /**
         * Creates a new {@link Foundry.Inhibitor} that will keep the
         * {@link Foundry.Context} alive and prevent shutdown until
         * {@link Foundry.Inhibitor.uninhibit} is called or the
         * {@link Foundry.Inhibitor} is finalized, whichever comes first.
         * 
         * If the context is already in shutdown, then `null` is returned and
         * `error` is set.
         * @returns a {@link Foundry.Inhibitor} or `null` and   `error` is set.
         */
        inhibit(): Inhibitor;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DBusService {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages D-Bus service for remote command execution.
     * 
     * FoundryDBusService provides a D-Bus socket for clients to connect to
     * and execute commands within the primary application instance. This is
     * also can be used to execute a subshell and have persistent background
     * work running in between shell commands.
     * 
     * Development environments that wants to allow interacting with the UI
     * instance from a shell are encouraged to give access to terminal emulators
     * with this sockets address in the form of `FOUNDRY_ADDRESS=` environment
     * variable.
     * @gir-type Class
     */
    class DBusService extends Service implements Json.Serializable {
        static $gtype: GObject.GType<DBusService>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DBusService.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DBusService.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DBusService.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DBusService.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DBusService.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DBusService.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DBusService.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DBusService.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the D-Bus address of the embedded D-Bus server.
         * @returns A D-Bus server address if started   successfully otherwise `null`.
         */
        dup_address(): (string | null);

        /**
         * Ensures the GDBusServer is running and provides the address.
         * @returns a {@link Dex.Future} that resolves to   a string containing the address, or rejects with error.
         */
        query_address(): Dex.Future;
    }


    namespace DapDebugger {
        // Signal signatures
        interface SignalSignatures extends Debugger.SignalSignatures {
            "notify::quirks": (pspec: GObject.ParamSpec) => void;
            "notify::stream": (pspec: GObject.ParamSpec) => void;
            "notify::subprocess": (pspec: GObject.ParamSpec) => void;
            "notify::address-space": (pspec: GObject.ParamSpec) => void;
            "notify::log-messages": (pspec: GObject.ParamSpec) => void;
            "notify::modules": (pspec: GObject.ParamSpec) => void;
            "notify::primary-thread": (pspec: GObject.ParamSpec) => void;
            "notify::terminated": (pspec: GObject.ParamSpec) => void;
            "notify::threads": (pspec: GObject.ParamSpec) => void;
            "notify::traps": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Debugger.ConstructorProps, Json.Serializable.ConstructorProps {
            quirks: DapDebuggerQuirk;
            stream: Gio.IOStream;
            subprocess: Gio.Subprocess;
        }
    }

    /**
     * Debug Adapter Protocol (DAP) implementation of the debugger interface.
     * 
     * FoundryDapDebugger provides a concrete implementation of the debugger
     * interface using the Debug Adapter Protocol. It handles communication
     * with language-specific debug adapters and provides a unified interface
     * for debugging operations across different programming languages and
     * development environments.
     * @gir-type Class
     */
    abstract class DapDebugger extends Debugger implements Json.Serializable {
        static $gtype: GObject.GType<DapDebugger>;

        // Properties
        /**
         * @construct-only
         * @default Foundry.DapDebuggerQuirk.NONE
         */
        get quirks(): DapDebuggerQuirk;

        /**
         * @construct-only
         */
        get stream(): Gio.IOStream;

        /**
         * @construct-only
         */
        get subprocess(): Gio.Subprocess;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DapDebugger.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DapDebugger.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DapDebugger.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DapDebugger.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DapDebugger.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DapDebugger.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DapDebugger.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DapDebugger.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Makes a request to the DAP server. The reply will be provided
         * via the resulting future, even if the reply contains an error.
         * @param node 
         * @returns a {@link Dex.Future} that resolves to   a {@link Json.Node} or rejects with error.
         */
        call(node: Json.Node): Dex.Future;

        dup_stream(): (Gio.IOStream | null);

        dup_subprocess(): (Gio.Subprocess | null);

        get_quirks(): DapDebuggerQuirk;

        /**
         * Send a message to the peer without expecting a reply.
         * @param node 
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        send(node: Json.Node): Dex.Future;
    }


    namespace Debugger {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            /**
             * @signal
             * @run-last
             */
            event: (arg0: DebuggerEvent) => void;
            "notify::address-space": (pspec: GObject.ParamSpec) => void;
            "notify::log-messages": (pspec: GObject.ParamSpec) => void;
            "notify::modules": (pspec: GObject.ParamSpec) => void;
            "notify::primary-thread": (pspec: GObject.ParamSpec) => void;
            "notify::terminated": (pspec: GObject.ParamSpec) => void;
            "notify::threads": (pspec: GObject.ParamSpec) => void;
            "notify::traps": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            address_space: Gio.ListModel;
            addressSpace: Gio.ListModel;
            log_messages: Gio.ListModel;
            logMessages: Gio.ListModel;
            modules: Gio.ListModel;
            primary_thread: DebuggerThread;
            primaryThread: DebuggerThread;
            terminated: boolean;
            threads: Gio.ListModel;
            traps: Gio.ListModel;
        }
    }

    /**
     * Abstract base class for debugger implementations.
     * 
     * FoundryDebugger provides the core interface for debugging operations including
     * breakpoint management, variable inspection, and execution control. Concrete
     * implementations handle specific debugging protocols and backends while maintaining
     * a consistent API.
     * @gir-type Class
     */
    abstract class Debugger extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Debugger>;

        // Properties
        /**
         * @read-only
         */
        get address_space(): Gio.ListModel;

        /**
         * @read-only
         */
        get addressSpace(): Gio.ListModel;

        /**
         * @read-only
         */
        get log_messages(): Gio.ListModel;

        /**
         * @read-only
         */
        get logMessages(): Gio.ListModel;

        /**
         * @read-only
         */
        get modules(): Gio.ListModel;

        /**
         * The first thread that was created by the debugger.
         * @since 1.1
         * @read-only
         */
        get primary_thread(): DebuggerThread;

        /**
         * The first thread that was created by the debugger.
         * @since 1.1
         * @read-only
         */
        get primaryThread(): DebuggerThread;

        /**
         * If the debuggee has terminated.
         * @since 1.1
         * @read-only
         * @default false
         */
        get terminated(): boolean;

        /**
         * @since 1.1
         * @read-only
         */
        get threads(): Gio.ListModel;

        /**
         * @read-only
         */
        get traps(): Gio.ListModel;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Debugger.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Debugger.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Debugger.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Debugger.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Debugger.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Debugger.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Debugger.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Debugger.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Determines of the debugger can currently make `movement`.
         * 
         * Use {@link Foundry.DebuggerThread.can_move} in new code.
         * @param movement 
         * @virtual
         */
        vfunc_can_move(movement: DebuggerMovement): boolean;

        /**
         * Connects to `target`.
         * 
         * Not all debuggers may not support all debugger target types.
         * @param target a {@link Foundry.DebuggerTarget}
         * @virtual
         */
        vfunc_connect_to_target(target: DebuggerTarget): Dex.Future;

        /**
         * Disassemble the instructions found in the address range.
         * @param begin_address 
         * @param end_address 
         * @virtual
         */
        vfunc_disassemble(begin_address: number, end_address: number): Dex.Future;

        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "GNU Debugger".
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * Gets a copy of the primary thread (the first thread created by the debugger).
         * @virtual
         */
        vfunc_dup_primary_thread(): (DebuggerThread | null);

        /**
         * @param event 
         * @virtual
         */
        vfunc_event(event: DebuggerEvent): void;

        /**
         * @virtual
         */
        vfunc_has_terminated(): boolean;

        /**
         * This must be called before using the debugger instance and may only
         * be called once.
         * 
         * Subclasses are expected to perform capability negotiation as part
         * of this request.
         * @virtual
         */
        vfunc_initialize(): Dex.Future;

        /**
         * Requests that the debugger interpret a command. This is typically the
         * REPL of a debugger and can be used to bridge the normal interpreter of
         * a debugger to the the user.
         * @param command the command to interpret
         * @virtual
         */
        vfunc_interpret(command: string): Dex.Future;

        /**
         * This should cause the child process to pause.
         * 
         * Use {@link Foundry.DebuggerThread.interrupt} in new code.
         * @virtual
         */
        vfunc_interrupt(): Dex.Future;

        /**
         * Gets a {@link Gio.ListModel} of {@link Foundry.DebuggerMappedRegion}
         * that is updated based on the address mapping of the debuggee.
         * @virtual
         */
        vfunc_list_address_space(): Gio.ListModel;

        /**
         * Lists available log messages from the debugger instance.
         * @virtual
         */
        vfunc_list_log_messages(): Gio.ListModel;

        /**
         * Lists the known modules loaded into the address space.
         * @virtual
         */
        vfunc_list_modules(): Gio.ListModel;

        /**
         * List thread groups known to the debugger.
         * @virtual
         */
        vfunc_list_thread_groups(): Gio.ListModel;

        /**
         * List threads known to the debugger.
         * @virtual
         */
        vfunc_list_threads(): Gio.ListModel;

        /**
         * List known traps (breakpoints, countpoints, watchpoints) that have been
         * registered with the debugger.
         * @virtual
         */
        vfunc_list_traps(): Gio.ListModel;

        /**
         * Use {@link Foundry.DebuggerThread.move} in new code.
         * @param movement how to move within the debugger
         * @virtual
         */
        vfunc_move(movement: DebuggerMovement): Dex.Future;

        /**
         * Send signal `signum` to debuggee.
         * @param signum 
         * @virtual
         */
        vfunc_send_signal(signum: number): Dex.Future;

        /**
         * Stop the debugger fully. This should at least cause the inferior to be
         * sent a terminating signal.
         * @virtual
         */
        vfunc_stop(): Dex.Future;

        /**
         * Register a new breakpoint based on `params`.
         * @param params the params for creating the new trap
         * @virtual
         */
        vfunc_trap(params: DebuggerTrapParams): Dex.Future;

        // Methods
        /**
         * Determines of the debugger can currently make `movement`.
         * 
         * Use {@link Foundry.DebuggerThread.can_move} in new code.
         * @param movement 
         * @returns `true` if `movement` can be performed
         */
        can_move(movement: DebuggerMovement): boolean;

        /**
         * Connects to `target`.
         * 
         * Not all debuggers may not support all debugger target types.
         * @param target a {@link Foundry.DebuggerTarget}
         * @returns {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        connect_to_target(target: DebuggerTarget): Dex.Future;

        /**
         * Disassemble the instructions found in the address range.
         * @param begin_address 
         * @param end_address 
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.DebuggerInstruction}.
         */
        disassemble(begin_address: (bigint | number), end_address: (bigint | number)): Dex.Future;

        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "GNU Debugger".
         * @returns the name of the provider
         */
        dup_name(): string;

        /**
         * Gets a copy of the primary thread (the first thread created by the debugger).
         * @returns a {@link Foundry.DebuggerThread} or `null`
         */
        dup_primary_thread(): (DebuggerThread | null);

        /**
         * This should only be used by debugger implementations.
         * @param event 
         */
        emit_event(event: DebuggerEvent): void;

        has_terminated(): boolean;

        /**
         * This must be called before using the debugger instance and may only
         * be called once.
         * 
         * Subclasses are expected to perform capability negotiation as part
         * of this request.
         * @returns {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        initialize(): Dex.Future;

        /**
         * Requests that the debugger interpret a command. This is typically the
         * REPL of a debugger and can be used to bridge the normal interpreter of
         * a debugger to the the user.
         * @param command the command to interpret
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        interpret(command: string): Dex.Future;

        /**
         * This should cause the child process to pause.
         * 
         * Use {@link Foundry.DebuggerThread.interrupt} in new code.
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        interrupt(): Dex.Future;

        /**
         * Gets a {@link Gio.ListModel} of {@link Foundry.DebuggerMappedRegion}
         * that is updated based on the address mapping of the debuggee.
         */
        list_address_space(): Gio.ListModel;

        /**
         * Lists available log messages from the debugger instance.
         * @returns a {@link Gio.ListModel} of   {@link Foundry.DebuggerLogMessage}.
         */
        list_log_messages(): Gio.ListModel;

        /**
         * Lists the known modules loaded into the address space.
         * @returns a {@link Gio.ListModel} of {@link Foundry.DebuggerTrap}
         */
        list_modules(): Gio.ListModel;

        /**
         * List thread groups known to the debugger.
         * @returns a {@link Gio.ListModel} of {@link Foundry.DebuggerThreadGroup}
         */
        list_thread_groups(): Gio.ListModel;

        /**
         * List threads known to the debugger.
         * @returns a {@link Gio.ListModel} of {@link Foundry.DebuggerThread}
         */
        list_threads(): Gio.ListModel;

        /**
         * List known traps (breakpoints, countpoints, watchpoints) that have been
         * registered with the debugger.
         * @returns a {@link Gio.ListModel} of {@link Foundry.DebuggerTrap}
         */
        list_traps(): Gio.ListModel;

        /**
         * Use {@link Foundry.DebuggerThread.move} in new code.
         * @param movement how to move within the debugger
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        move(movement: DebuggerMovement): Dex.Future;

        /**
         * Send signal `signum` to debuggee.
         * @param signum 
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        send_signal(signum: number): Dex.Future;

        /**
         * Stop the debugger fully. This should at least cause the inferior to be
         * sent a terminating signal.
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        stop(): Dex.Future;

        /**
         * Register a new breakpoint based on `params`.
         * @param params the params for creating the new trap
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        trap(params: DebuggerTrapParams): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DebuggerActions {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::debugger": (pspec: GObject.ParamSpec) => void;
            "notify::thread": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Gio.ActionGroup.ConstructorProps {
            "debugger": Debugger;
            thread: DebuggerThread;
        }
    }

    /**
     * Simplified interface for performing debugger actions.
     * 
     * This {@link Gio.ActionGroup} provides actions and mechanics for writing
     * a debugger application. It does a light amount of state management to
     * ensure actions are enabled/disabled properly.
     * @gir-type Class
     */
    class DebuggerActions extends GObject.Object implements Gio.ActionGroup {
        static $gtype: GObject.GType<DebuggerActions>;

        // Properties
        /**
         * The debugger instance.
         * @since 1.1
         */
        get "debugger"(): Debugger;
        set "debugger"(val: Debugger);

        /**
         * The debugger thread.
         * @since 1.1
         */
        get thread(): DebuggerThread;
        set thread(val: DebuggerThread);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerActions.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerActions.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](_debugger: (Debugger | null), thread: (DebuggerThread | null)): DebuggerActions;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerActions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerActions.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerActions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerActions.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerActions.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerActions.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the debugger instance.
         * @returns the debugger instance
         */
        dup_debugger(): (Debugger | null);

        /**
         * Gets the debugger thread.
         * @returns the debugger thread
         */
        dup_thread(): (DebuggerThread | null);

        /**
         * Sets the debugger instance.
         * @param _debugger the debugger instance
         */
        set_debugger(_debugger: (Debugger | null)): void;

        /**
         * Sets the debugger thread and connects to its "changed" signal
         * to update action states.
         * @param thread the debugger thread
         */
        set_thread(thread: (DebuggerThread | null)): void;

        /**
         * Emits the `Gio.ActionGroup::action-added` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         */
        action_added(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-enabled-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param enabled whether the action is now enabled
         */
        action_enabled_changed(action_name: string, enabled: boolean): void;

        /**
         * Emits the `Gio.ActionGroup::action-removed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         */
        action_removed(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-state-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param state the new state of the named action
         */
        action_state_changed(action_name: string, state: GLib.Variant): void;

        /**
         * Activate the named action within `action_group`.
         * 
         * If the action is expecting a parameter, then the correct type of
         * parameter must be given as `parameter`.  If the action is expecting no
         * parameters then `parameter` must be `NULL`.  See
         * {@link Gio.ActionGroup.get_action_parameter_type}.
         * 
         * If the {@link Gio.ActionGroup} implementation supports asynchronous remote
         * activation over D-Bus, this call may return before the relevant
         * D-Bus traffic has been sent, or any replies have been received. In
         * order to block on such asynchronous activation calls,
         * {@link Gio.DBusConnection.flush} should be called prior to the code, which
         * depends on the result of the action activation. Without flushing
         * the D-Bus connection, there is no guarantee that the action would
         * have been activated.
         * 
         * The following code which runs in a remote app instance, shows an
         * example of a ‘quit’ action being activated on the primary app
         * instance over D-Bus. Here {@link Gio.DBusConnection.flush} is called
         * before `exit()`. Without `g_dbus_connection_flush()`, the ‘quit’ action
         * may fail to be activated on the primary instance.
         * 
         * ```c
         * // call ‘quit’ action on primary instance
         * g_action_group_activate_action (G_ACTION_GROUP (app), "quit", NULL);
         * 
         * // make sure the action is activated now
         * g_dbus_connection_flush (…);
         * 
         * g_debug ("Application has been terminated. Exiting.");
         * 
         * exit (0);
         * ```
         * @param action_name the name of the action to activate
         * @param parameter parameters to the activation
         */
        activate_action(action_name: string, parameter: (GLib.Variant | null)): void;

        /**
         * Request for the state of the named action within `action_group` to be
         * changed to `value`.
         * 
         * The action must be stateful and `value` must be of the correct type.
         * See {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * This call merely requests a change.  The action may refuse to change
         * its state or may change its state to something other than `value`.
         * See {@link Gio.ActionGroup.get_action_state_hint}.
         * 
         * If the `value` GVariant is floating, it is consumed.
         * @param action_name the name of the action to request the change on
         * @param value the new state
         */
        change_action_state(action_name: string, value: GLib.Variant): void;

        /**
         * Checks if the named action within `action_group` is currently enabled.
         * 
         * An action must be enabled in order to be activated or in order to
         * have its state changed from outside callers.
         * @param action_name the name of the action to query
         * @returns whether the action is currently enabled
         */
        get_action_enabled(action_name: string): boolean;

        /**
         * Queries the type of the parameter that must be given when activating
         * the named action within `action_group`.
         * 
         * When activating the action using {@link Gio.ActionGroup.activate_action},
         * the {@link GLib.Variant} given to that function must be of the type returned
         * by this function.
         * 
         * In the case that this function returns `NULL`, you must not give any
         * {@link GLib.Variant}, but `NULL` instead.
         * 
         * The parameter type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different parameter type.
         * @param action_name the name of the action to query
         * @returns the parameter type
         */
        get_action_parameter_type(action_name: string): (GLib.VariantType | null);

        /**
         * Queries the current state of the named action within `action_group`.
         * 
         * If the action is not stateful then `NULL` will be returned.  If the
         * action is stateful then the type of the return value is the type
         * given by {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @returns the current state of the action
         */
        get_action_state(action_name: string): (GLib.Variant | null);

        /**
         * Requests a hint about the valid range of values for the state of the
         * named action within `action_group`.
         * 
         * If `NULL` is returned it either means that the action is not stateful
         * or that there is no hint about the valid range of values for the
         * state of the action.
         * 
         * If a {@link GLib.Variant} array is returned then each item in the array is a
         * possible value for the state.  If a {@link GLib.Variant} pair (ie: two-tuple) is
         * returned then the tuple specifies the inclusive lower and upper bound
         * of valid values for the state.
         * 
         * In any case, the information is merely a hint.  It may be possible to
         * have a state value outside of the hinted range and setting a value
         * within the range may fail.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @returns the state range hint
         */
        get_action_state_hint(action_name: string): (GLib.Variant | null);

        /**
         * Queries the type of the state of the named action within
         * `action_group`.
         * 
         * If the action is stateful then this function returns the
         * {@link GLib.VariantType} of the state.  All calls to
         * {@link Gio.ActionGroup.change_action_state} must give a {@link GLib.Variant} of this
         * type and {@link Gio.ActionGroup.get_action_state} will return a {@link GLib.Variant}
         * of the same type.
         * 
         * If the action is not stateful then this function will return `NULL`.
         * In that case, {@link Gio.ActionGroup.get_action_state} will return `NULL`
         * and you must not call {@link Gio.ActionGroup.change_action_state}.
         * 
         * The state type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different state type.
         * @param action_name the name of the action to query
         * @returns the state type, if the action is stateful
         */
        get_action_state_type(action_name: string): (GLib.VariantType | null);

        /**
         * Checks if the named action exists within `action_group`.
         * @param action_name the name of the action to check for
         * @returns whether the named action exists
         */
        has_action(action_name: string): boolean;

        /**
         * Lists the actions contained within `action_group`.
         * 
         * The caller is responsible for freeing the list with {@link GLib.strfreev} when
         * it is no longer required.
         * @returns a `NULL`-terminated array   of the names of the actions in the group
         */
        list_actions(): string[];

        /**
         * Queries all aspects of the named action within an `action_group`.
         * 
         * This function acquires the information available from
         * {@link Gio.ActionGroup.has_action}, {@link Gio.ActionGroup.get_action_enabled},
         * {@link Gio.ActionGroup.get_action_parameter_type},
         * {@link Gio.ActionGroup.get_action_state_type},
         * {@link Gio.ActionGroup.get_action_state_hint} and
         * {@link Gio.ActionGroup.get_action_state} with a single function call.
         * 
         * This provides two main benefits.
         * 
         * The first is the improvement in efficiency that comes with not having
         * to perform repeated lookups of the action in order to discover
         * different things about it.  The second is that implementing
         * {@link Gio.ActionGroup} can now be done by only overriding this one virtual
         * function.
         * 
         * The interface provides a default implementation of this function that
         * calls the individual functions, as required, to fetch the
         * information.  The interface also provides default implementations of
         * those functions that call this function.  All implementations,
         * therefore, must override either this function or all of the others.
         * 
         * If the action exists, `TRUE` is returned and any of the requested
         * fields (as indicated by having a non-`NULL` reference passed in) are
         * filled.  If the action doesn’t exist, `FALSE` is returned and the
         * fields may or may not have been modified.
         * @param action_name the name of an action in the group
         * @returns `TRUE` if the action exists, else `FALSE`
         */
        query_action(action_name: string): [boolean, boolean, GLib.VariantType | null, GLib.VariantType | null, GLib.Variant | null, GLib.Variant | null];

        /**
         * Emits the `Gio.ActionGroup::action-added` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_action_added(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-enabled-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param enabled whether the action is now enabled
         * @virtual
         */
        vfunc_action_enabled_changed(action_name: string, enabled: boolean): void;

        /**
         * Emits the `Gio.ActionGroup::action-removed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_action_removed(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-state-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param state the new state of the named action
         * @virtual
         */
        vfunc_action_state_changed(action_name: string, state: GLib.Variant): void;

        /**
         * Activate the named action within `action_group`.
         * 
         * If the action is expecting a parameter, then the correct type of
         * parameter must be given as `parameter`.  If the action is expecting no
         * parameters then `parameter` must be `NULL`.  See
         * {@link Gio.ActionGroup.get_action_parameter_type}.
         * 
         * If the {@link Gio.ActionGroup} implementation supports asynchronous remote
         * activation over D-Bus, this call may return before the relevant
         * D-Bus traffic has been sent, or any replies have been received. In
         * order to block on such asynchronous activation calls,
         * {@link Gio.DBusConnection.flush} should be called prior to the code, which
         * depends on the result of the action activation. Without flushing
         * the D-Bus connection, there is no guarantee that the action would
         * have been activated.
         * 
         * The following code which runs in a remote app instance, shows an
         * example of a ‘quit’ action being activated on the primary app
         * instance over D-Bus. Here {@link Gio.DBusConnection.flush} is called
         * before `exit()`. Without `g_dbus_connection_flush()`, the ‘quit’ action
         * may fail to be activated on the primary instance.
         * 
         * ```c
         * // call ‘quit’ action on primary instance
         * g_action_group_activate_action (G_ACTION_GROUP (app), "quit", NULL);
         * 
         * // make sure the action is activated now
         * g_dbus_connection_flush (…);
         * 
         * g_debug ("Application has been terminated. Exiting.");
         * 
         * exit (0);
         * ```
         * @param action_name the name of the action to activate
         * @param parameter parameters to the activation
         * @virtual
         */
        vfunc_activate_action(action_name: string, parameter: (GLib.Variant | null)): void;

        /**
         * Request for the state of the named action within `action_group` to be
         * changed to `value`.
         * 
         * The action must be stateful and `value` must be of the correct type.
         * See {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * This call merely requests a change.  The action may refuse to change
         * its state or may change its state to something other than `value`.
         * See {@link Gio.ActionGroup.get_action_state_hint}.
         * 
         * If the `value` GVariant is floating, it is consumed.
         * @param action_name the name of the action to request the change on
         * @param value the new state
         * @virtual
         */
        vfunc_change_action_state(action_name: string, value: GLib.Variant): void;

        /**
         * Checks if the named action within `action_group` is currently enabled.
         * 
         * An action must be enabled in order to be activated or in order to
         * have its state changed from outside callers.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_enabled(action_name: string): boolean;

        /**
         * Queries the type of the parameter that must be given when activating
         * the named action within `action_group`.
         * 
         * When activating the action using {@link Gio.ActionGroup.activate_action},
         * the {@link GLib.Variant} given to that function must be of the type returned
         * by this function.
         * 
         * In the case that this function returns `NULL`, you must not give any
         * {@link GLib.Variant}, but `NULL` instead.
         * 
         * The parameter type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different parameter type.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_parameter_type(action_name: string): (GLib.VariantType | null);

        /**
         * Queries the current state of the named action within `action_group`.
         * 
         * If the action is not stateful then `NULL` will be returned.  If the
         * action is stateful then the type of the return value is the type
         * given by {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state(action_name: string): (GLib.Variant | null);

        /**
         * Requests a hint about the valid range of values for the state of the
         * named action within `action_group`.
         * 
         * If `NULL` is returned it either means that the action is not stateful
         * or that there is no hint about the valid range of values for the
         * state of the action.
         * 
         * If a {@link GLib.Variant} array is returned then each item in the array is a
         * possible value for the state.  If a {@link GLib.Variant} pair (ie: two-tuple) is
         * returned then the tuple specifies the inclusive lower and upper bound
         * of valid values for the state.
         * 
         * In any case, the information is merely a hint.  It may be possible to
         * have a state value outside of the hinted range and setting a value
         * within the range may fail.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state_hint(action_name: string): (GLib.Variant | null);

        /**
         * Queries the type of the state of the named action within
         * `action_group`.
         * 
         * If the action is stateful then this function returns the
         * {@link GLib.VariantType} of the state.  All calls to
         * {@link Gio.ActionGroup.change_action_state} must give a {@link GLib.Variant} of this
         * type and {@link Gio.ActionGroup.get_action_state} will return a {@link GLib.Variant}
         * of the same type.
         * 
         * If the action is not stateful then this function will return `NULL`.
         * In that case, {@link Gio.ActionGroup.get_action_state} will return `NULL`
         * and you must not call {@link Gio.ActionGroup.change_action_state}.
         * 
         * The state type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different state type.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state_type(action_name: string): (GLib.VariantType | null);

        /**
         * Checks if the named action exists within `action_group`.
         * @param action_name the name of the action to check for
         * @virtual
         */
        vfunc_has_action(action_name: string): boolean;

        /**
         * Lists the actions contained within `action_group`.
         * 
         * The caller is responsible for freeing the list with {@link GLib.strfreev} when
         * it is no longer required.
         * @virtual
         */
        vfunc_list_actions(): string[];

        /**
         * Queries all aspects of the named action within an `action_group`.
         * 
         * This function acquires the information available from
         * {@link Gio.ActionGroup.has_action}, {@link Gio.ActionGroup.get_action_enabled},
         * {@link Gio.ActionGroup.get_action_parameter_type},
         * {@link Gio.ActionGroup.get_action_state_type},
         * {@link Gio.ActionGroup.get_action_state_hint} and
         * {@link Gio.ActionGroup.get_action_state} with a single function call.
         * 
         * This provides two main benefits.
         * 
         * The first is the improvement in efficiency that comes with not having
         * to perform repeated lookups of the action in order to discover
         * different things about it.  The second is that implementing
         * {@link Gio.ActionGroup} can now be done by only overriding this one virtual
         * function.
         * 
         * The interface provides a default implementation of this function that
         * calls the individual functions, as required, to fetch the
         * information.  The interface also provides default implementations of
         * those functions that call this function.  All implementations,
         * therefore, must override either this function or all of the others.
         * 
         * If the action exists, `TRUE` is returned and any of the requested
         * fields (as indicated by having a non-`NULL` reference passed in) are
         * filled.  If the action doesn’t exist, `FALSE` is returned and the
         * fields may or may not have been modified.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_query_action(action_name: string): [boolean, boolean, GLib.VariantType | null, GLib.VariantType | null, GLib.Variant | null, GLib.Variant | null];
    }


    namespace DebuggerBreakpoint {
        // Signal signatures
        interface SignalSignatures extends DebuggerTrap.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends DebuggerTrap.ConstructorProps {

        }
    }

    /**
     * Abstract base class for debuggers to represent breakpoints.
     * @gir-type Class
     */
    abstract class DebuggerBreakpoint extends DebuggerTrap {
        static $gtype: GObject.GType<DebuggerBreakpoint>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerBreakpoint.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerBreakpoint.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerBreakpoint.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerBreakpoint.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerBreakpoint.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerBreakpoint.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerBreakpoint.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerBreakpoint.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace DebuggerCountpoint {
        // Signal signatures
        interface SignalSignatures extends DebuggerTrap.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends DebuggerTrap.ConstructorProps {

        }
    }

    /**
     * Abstract base class for debuggers to implement countpoints.
     * @gir-type Class
     */
    abstract class DebuggerCountpoint extends DebuggerTrap {
        static $gtype: GObject.GType<DebuggerCountpoint>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerCountpoint.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerCountpoint.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerCountpoint.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerCountpoint.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerCountpoint.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerCountpoint.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerCountpoint.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerCountpoint.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace DebuggerEvent {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    abstract class DebuggerEvent extends GObject.Object {
        static $gtype: GObject.GType<DebuggerEvent>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerEvent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerEvent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerEvent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerEvent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerEvent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerEvent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerEvent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerEvent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace DebuggerInstruction {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::display-text": (pspec: GObject.ParamSpec) => void;
            "notify::function": (pspec: GObject.ParamSpec) => void;
            "notify::instruction-pointer": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            display_text: string;
            displayText: string;
            "function": string;
            instruction_pointer: (bigint | number);
            instructionPointer: (bigint | number);
        }
    }

    /**
     * Represents a single assembly instruction.
     * @gir-type Class
     */
    abstract class DebuggerInstruction extends GObject.Object {
        static $gtype: GObject.GType<DebuggerInstruction>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get display_text(): string;

        /**
         * @read-only
         * @default null
         */
        get displayText(): string;

        /**
         * @read-only
         * @default null
         */
        get "function"(): string;

        /**
         * @read-only
         * @default 0
         */
        get instruction_pointer(): number;

        /**
         * @read-only
         * @default 0
         */
        get instructionPointer(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerInstruction.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerInstruction.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerInstruction.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerInstruction.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerInstruction.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerInstruction.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerInstruction.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerInstruction.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_display_text(): string;

        /**
         * @virtual
         */
        vfunc_dup_function(): string;

        /**
         * @virtual
         */
        vfunc_get_instruction_pointer(): (bigint | number);

        // Methods
        dup_display_text(): string;

        dup_function(): string;

        get_instruction_pointer(): number;
    }


    namespace DebuggerLogMessage {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::message": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            message: string;
        }
    }

    /**
     * Abstract base class for representing log messages in the debugger.
     * @gir-type Class
     */
    abstract class DebuggerLogMessage extends GObject.Object {
        static $gtype: GObject.GType<DebuggerLogMessage>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get message(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerLogMessage.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerLogMessage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerLogMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerLogMessage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerLogMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerLogMessage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerLogMessage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerLogMessage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * The message string from the debugger instance.
         * @virtual
         */
        vfunc_dup_message(): (string | null);

        // Methods
        /**
         * The message string from the debugger instance.
         */
        dup_message(): (string | null);
    }


    namespace DebuggerManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages debugger providers and coordinates debugging operations.
     * 
     * FoundryDebuggerManager coordinates debugger providers and provides a unified
     * interface for debugging operations. It handles debugger registration, lifecycle
     * management, and integrates with the build system for seamless debugging
     * workflow across different programming languages and platforms.
     * @gir-type Class
     */
    class DebuggerManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<DebuggerManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Discovers a {@link Foundry.DebuggerProvider} that is likely to be
         * usable with the pipeline and command.
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param command a {@link Foundry.Command}
         * @returns a {@link Dex.Future} that resolves to an   {@link Foundry.DebuggerProvider}.
         */
        discover(pipeline: (BuildPipeline | null), command: Command): Dex.Future;

        /**
         * @param module_name module name of the plugin
         * @returns a {@link Foundry.DebuggerProvider}
         */
        find(module_name: string): (DebuggerProvider | null);
    }


    namespace DebuggerMappedRegion {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::begin-address": (pspec: GObject.ParamSpec) => void;
            "notify::end-address": (pspec: GObject.ParamSpec) => void;
            "notify::mode": (pspec: GObject.ParamSpec) => void;
            "notify::offset": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            begin_address: (bigint | number);
            beginAddress: (bigint | number);
            end_address: (bigint | number);
            endAddress: (bigint | number);
            mode: number;
            offset: (bigint | number);
            path: string;
        }
    }

    /**
     * Abstract base class for representing mapped memory regions in the debugger.
     * 
     * FoundryDebuggerMappedRegion provides the core interface for representing
     * mapped memory regions during debugging sessions. It includes address
     * information, access modes, and provides a unified interface for
     * memory region inspection and management across different debugging backends.
     * @gir-type Class
     */
    abstract class DebuggerMappedRegion extends GObject.Object {
        static $gtype: GObject.GType<DebuggerMappedRegion>;

        // Properties
        /**
         * @read-only
         * @default 0
         */
        get begin_address(): number;

        /**
         * @read-only
         * @default 0
         */
        get beginAddress(): number;

        /**
         * @read-only
         * @default 0
         */
        get end_address(): number;

        /**
         * @read-only
         * @default 0
         */
        get endAddress(): number;

        /**
         * @read-only
         * @default 0
         */
        get mode(): number;

        /**
         * The offset within `path` where the mapping originates.
         * @since 1.1
         * @read-only
         * @default 0
         */
        get offset(): number;

        /**
         * @read-only
         * @default null
         */
        get path(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerMappedRegion.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerMappedRegion.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerMappedRegion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerMappedRegion.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerMappedRegion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerMappedRegion.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerMappedRegion.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerMappedRegion.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_path(): string;

        /**
         * @virtual
         */
        vfunc_get_mode(): number;

        /**
         * @virtual
         */
        vfunc_get_offset(): (bigint | number);

        /**
         * @param begin_address 
         * @param end_address 
         * @virtual
         */
        vfunc_get_range(begin_address: number, end_address: number): void;

        // Methods
        dup_path(): string;

        get_mode(): number;

        get_offset(): number;

        /**
         * @param beign_address 
         * @param end_address 
         */
        get_range(beign_address: (bigint | number), end_address: (bigint | number)): void;
    }


    namespace DebuggerModule {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::address-space": (pspec: GObject.ParamSpec) => void;
            "notify::host-path": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            address_space: Gio.ListModel;
            addressSpace: Gio.ListModel;
            host_path: string;
            hostPath: string;
            id: string;
            name: string;
            path: string;
        }
    }

    /**
     * Abstract base class for representing modules in the debugger.
     * 
     * FoundryDebuggerModule provides the core interface for representing
     * loaded modules during debugging sessions. It includes module identification,
     * path information, and provides a unified interface for module
     * inspection and management across different debugging backends.
     * @gir-type Class
     */
    abstract class DebuggerModule extends GObject.Object {
        static $gtype: GObject.GType<DebuggerModule>;

        // Properties
        /**
         * @read-only
         */
        get address_space(): Gio.ListModel;

        /**
         * @read-only
         */
        get addressSpace(): Gio.ListModel;

        /**
         * @read-only
         * @default null
         */
        get host_path(): string;

        /**
         * @read-only
         * @default null
         */
        get hostPath(): string;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @since 1.1
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         * @default null
         */
        get path(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerModule.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerModule.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerModule.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerModule.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerModule.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerModule.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerModule.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerModule.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_host_path(): string;

        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @virtual
         */
        vfunc_dup_path(): string;

        /**
         * @virtual
         */
        vfunc_list_address_space(): Gio.ListModel;

        // Methods
        dup_host_path(): string;

        dup_id(): string;

        dup_name(): string;

        dup_path(): string;

        /**
         * @returns a {@link Gio.ListModel} of   {@link Foundry.DebuggerMappedRegion}
         */
        list_address_space(): Gio.ListModel;
    }


    namespace DebuggerProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for providing debugger functionality.
     * 
     * FoundryDebuggerProvider provides the core interface for debugger providers
     * that can be loaded and unloaded dynamically. It includes lifecycle management
     * and provides a unified interface for debugger functionality across different
     * programming languages and debugging backends.
     * @gir-type Class
     */
    abstract class DebuggerProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<DebuggerProvider>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @virtual
         */
        vfunc_load_debugger(pipeline: BuildPipeline): Dex.Future;

        /**
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param command a {@link Foundry.Command}
         * @virtual
         */
        vfunc_supports(pipeline: (BuildPipeline | null), command: Command): Dex.Future;

        // Methods
        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * @param pipeline a {@link Foundry.BuildPipeline}
         */
        load_debugger(pipeline: BuildPipeline): Dex.Future;

        /**
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param command a {@link Foundry.Command}
         * @returns a {@link Dex.Future} that resolves to an integer   `G_TYPE_INT` of the priority of the debugger (larger value is higher   priority) or rejects with error.
         */
        supports(pipeline: (BuildPipeline | null), command: Command): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DebuggerSource {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
            name: string;
            path: string;
        }
    }

    /**
     * Represents a source code location.
     * 
     * Debugger plugins should implement this so that applications with
     * a debugger interface can navigate to source code.
     * @gir-type Class
     */
    abstract class DebuggerSource extends GObject.Object {
        static $gtype: GObject.GType<DebuggerSource>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         * @default null
         */
        get path(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerSource.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerSource.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerSource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerSource.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerSource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerSource.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerSource.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerSource.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @virtual
         */
        vfunc_dup_path(): string;

        // Methods
        dup_id(): string;

        dup_name(): string;

        dup_path(): string;
    }


    namespace DebuggerStackFrame {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::begin-line": (pspec: GObject.ParamSpec) => void;
            "notify::begin-line-offset": (pspec: GObject.ParamSpec) => void;
            "notify::can-restart": (pspec: GObject.ParamSpec) => void;
            "notify::end-line": (pspec: GObject.ParamSpec) => void;
            "notify::end-line-offset": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::instruction-pointer": (pspec: GObject.ParamSpec) => void;
            "notify::module-id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            begin_line: number;
            beginLine: number;
            begin_line_offset: number;
            beginLineOffset: number;
            can_restart: boolean;
            canRestart: boolean;
            end_line: number;
            endLine: number;
            end_line_offset: number;
            endLineOffset: number;
            id: string;
            instruction_pointer: (bigint | number);
            instructionPointer: (bigint | number);
            module_id: string;
            moduleId: string;
            name: string;
            source: DebuggerSource;
        }
    }

    /**
     * Abstract base class for representing stack frames in the debugger.
     * 
     * FoundryDebuggerStackFrame provides the core interface for representing
     * stack frames during debugging sessions. It includes frame identification,
     * source location information, and provides a unified interface for
     * stack frame inspection and navigation across different debugging backends.
     * @gir-type Class
     */
    abstract class DebuggerStackFrame extends GObject.Object {
        static $gtype: GObject.GType<DebuggerStackFrame>;

        // Properties
        /**
         * @read-only
         * @default 0
         */
        get begin_line(): number;

        /**
         * @read-only
         * @default 0
         */
        get beginLine(): number;

        /**
         * @read-only
         * @default 0
         */
        get begin_line_offset(): number;

        /**
         * @read-only
         * @default 0
         */
        get beginLineOffset(): number;

        /**
         * @read-only
         * @default false
         */
        get can_restart(): boolean;

        /**
         * @read-only
         * @default false
         */
        get canRestart(): boolean;

        /**
         * @read-only
         * @default 0
         */
        get end_line(): number;

        /**
         * @read-only
         * @default 0
         */
        get endLine(): number;

        /**
         * @read-only
         * @default 0
         */
        get end_line_offset(): number;

        /**
         * @read-only
         * @default 0
         */
        get endLineOffset(): number;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default 0
         */
        get instruction_pointer(): number;

        /**
         * @read-only
         * @default 0
         */
        get instructionPointer(): number;

        /**
         * @read-only
         * @default null
         */
        get module_id(): string;

        /**
         * @read-only
         * @default null
         */
        get moduleId(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @since 1.1
         * @read-only
         */
        get source(): DebuggerSource;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerStackFrame.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerStackFrame.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerStackFrame.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerStackFrame.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerStackFrame.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerStackFrame.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerStackFrame.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerStackFrame.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_can_restart(): boolean;

        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_module_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @virtual
         */
        vfunc_dup_source(): (DebuggerSource | null);

        /**
         * @virtual
         */
        vfunc_get_instruction_pointer(): (bigint | number);

        /**
         * @param begin_line 
         * @param begin_line_offset 
         * @param end_line 
         * @param end_line_offset 
         * @virtual
         */
        vfunc_get_source_range(begin_line: number, begin_line_offset: number, end_line: number, end_line_offset: number): void;

        /**
         * @virtual
         */
        vfunc_list_locals(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_list_params(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_list_registers(): Dex.Future;

        // Methods
        dup_id(): string;

        dup_module_id(): string;

        dup_name(): string;

        dup_source(): (DebuggerSource | null);

        get_instruction_pointer(): number;

        /**
         * @param begin_line 
         * @param begin_line_offset 
         * @param end_line 
         * @param end_line_offset 
         */
        get_source_range(begin_line: number, begin_line_offset: number, end_line: number, end_line_offset: number): void;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.DebuggerVariable}
         */
        list_locals(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.DebuggerVariable}
         */
        list_params(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.DebuggerVariable}
         */
        list_registers(): Dex.Future;
    }


    namespace DebuggerStopEvent {
        // Signal signatures
        interface SignalSignatures extends DebuggerEvent.SignalSignatures {
            "notify::reason": (pspec: GObject.ParamSpec) => void;
            "notify::trap": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends DebuggerEvent.ConstructorProps {
            reason: DebuggerStopReason;
            trap: DebuggerTrap;
        }
    }

    /**
     * Represents why a debugger has stopped.
     * @gir-type Class
     */
    abstract class DebuggerStopEvent extends DebuggerEvent {
        static $gtype: GObject.GType<DebuggerStopEvent>;

        // Properties
        /**
         * @read-only
         * @default Foundry.DebuggerStopReason.UNKNOWN
         */
        get reason(): DebuggerStopReason;

        /**
         * @read-only
         */
        get trap(): DebuggerTrap;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerStopEvent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerStopEvent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerStopEvent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerStopEvent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerStopEvent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerStopEvent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerStopEvent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerStopEvent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_trap(): (DebuggerTrap | null);

        /**
         * @virtual
         */
        vfunc_get_exit_code(): number;

        /**
         * @virtual
         */
        vfunc_get_reason(): DebuggerStopReason;

        /**
         * @virtual
         */
        vfunc_get_signal(): number;

        // Methods
        dup_trap(): (DebuggerTrap | null);

        get_exit_code(): number;

        get_reason(): DebuggerStopReason;

        get_signal(): number;
    }


    namespace DebuggerTarget {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Abstract base class for representing the types of targets a debugger can connect to.
     * @gir-type Class
     */
    abstract class DebuggerTarget extends GObject.Object {
        static $gtype: GObject.GType<DebuggerTarget>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerTarget.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerTarget.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerTarget.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTarget.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerTarget.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTarget.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerTarget.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerTarget.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace DebuggerTargetCommand {
        // Signal signatures
        interface SignalSignatures extends DebuggerTarget.SignalSignatures {
            "notify::command": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends DebuggerTarget.ConstructorProps {
            command: Command;
        }
    }

    /**
     * Command-based implementation of debugger target.
     * 
     * FoundryDebuggerTargetCommand provides functionality for debugging
     * applications by executing commands and managing their execution.
     * It integrates with the command system and provides a unified
     * interface for command-based debugging operations.
     * @gir-type Class
     */
    class DebuggerTargetCommand extends DebuggerTarget {
        static $gtype: GObject.GType<DebuggerTargetCommand>;

        // Properties
        /**
         * @construct-only
         */
        get command(): Command;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerTargetCommand.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerTargetCommand.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](command: Command): DebuggerTargetCommand;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerTargetCommand.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTargetCommand.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerTargetCommand.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTargetCommand.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerTargetCommand.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerTargetCommand.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_command(): Command;
    }


    namespace DebuggerTargetProcess {
        // Signal signatures
        interface SignalSignatures extends DebuggerTarget.SignalSignatures {
            "notify::pid": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends DebuggerTarget.ConstructorProps {
            pid: number;
        }
    }

    /**
     * Represents a request to connect to an existing process.
     * @gir-type Class
     */
    class DebuggerTargetProcess extends DebuggerTarget {
        static $gtype: GObject.GType<DebuggerTargetProcess>;

        // Properties
        /**
         * @construct-only
         * @default 0
         */
        get pid(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerTargetProcess.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerTargetProcess.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](pid: GLib.Pid): DebuggerTargetProcess;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerTargetProcess.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTargetProcess.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerTargetProcess.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTargetProcess.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerTargetProcess.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerTargetProcess.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        get_pid(): GLib.Pid;
    }


    namespace DebuggerTargetRemote {
        // Signal signatures
        interface SignalSignatures extends DebuggerTarget.SignalSignatures {
            "notify::address": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends DebuggerTarget.ConstructorProps {
            address: string;
        }
    }

    /**
     * Repreesnts connecting to a remote debugger instance.
     * @gir-type Class
     */
    class DebuggerTargetRemote extends DebuggerTarget {
        static $gtype: GObject.GType<DebuggerTargetRemote>;

        // Properties
        /**
         * @construct-only
         * @default null
         */
        get address(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerTargetRemote.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerTargetRemote.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](address: string): DebuggerTargetRemote;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerTargetRemote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTargetRemote.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerTargetRemote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTargetRemote.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerTargetRemote.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerTargetRemote.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_address(): string;
    }


    namespace DebuggerThread {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::group-id": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::stopped": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            group_id: string;
            groupId: string;
            id: string;
            stopped: boolean;
        }
    }

    /**
     * Abstract base class for representing threads in the debugger.
     * 
     * FoundryDebuggerThread provides the core interface for representing
     * threads during debugging sessions. It includes thread identification,
     * state management, and provides a unified interface for thread
     * control and inspection across different debugging backends.
     * @gir-type Class
     */
    abstract class DebuggerThread extends GObject.Object {
        static $gtype: GObject.GType<DebuggerThread>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get group_id(): string;

        /**
         * @read-only
         * @default null
         */
        get groupId(): string;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * If the thread is known to be stopped.
         * @since 1.1
         * @read-only
         * @default false
         */
        get stopped(): boolean;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerThread.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerThread.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerThread.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerThread.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerThread.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerThread.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * If `self` can attempt `movement` at this point.
         * @param movement 
         * @virtual
         */
        vfunc_can_move(movement: DebuggerMovement): boolean;

        /**
         * @virtual
         */
        vfunc_dup_group_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * Stops the thread.
         * @virtual
         */
        vfunc_interrupt(): Dex.Future;

        /**
         * Gets if the thread is currently stopped.
         * 
         * Subclasses should {@link GObject.Object.notify} the "stopped" property
         * when this value changes.
         * @virtual
         */
        vfunc_is_stopped(): boolean;

        /**
         * @virtual
         */
        vfunc_list_frames(): Dex.Future;

        /**
         * @param movement 
         * @virtual
         */
        vfunc_move(movement: DebuggerMovement): Dex.Future;

        // Methods
        /**
         * If `self` can attempt `movement` at this point.
         * @param movement 
         * @returns `true` if `movement` may be a valid movement now.
         */
        can_move(movement: DebuggerMovement): boolean;

        dup_group_id(): string;

        dup_id(): string;

        /**
         * Stops the thread.
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        interrupt(): Dex.Future;

        /**
         * Gets if the thread is currently stopped.
         * 
         * Subclasses should {@link GObject.Object.notify} the "stopped" property
         * when this value changes.
         * @returns `true` if thread is stopped
         */
        is_stopped(): boolean;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.DebuggerStackFrame}.
         */
        list_frames(): Dex.Future;

        /**
         * @param movement 
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        move(movement: DebuggerMovement): Dex.Future;
    }


    namespace DebuggerThreadGroup {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
        }
    }

    /**
     * Represents a group of threads.
     * @gir-type Class
     */
    abstract class DebuggerThreadGroup extends GObject.Object {
        static $gtype: GObject.GType<DebuggerThreadGroup>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerThreadGroup.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerThreadGroup.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerThreadGroup.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerThreadGroup.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerThreadGroup.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerThreadGroup.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerThreadGroup.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerThreadGroup.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        // Methods
        dup_id(): string;
    }


    namespace DebuggerTrap {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
        }
    }

    /**
     * Abstract base class for different types of ways a debugger may stop.
     * @gir-type Class
     */
    abstract class DebuggerTrap extends GObject.Object {
        static $gtype: GObject.GType<DebuggerTrap>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerTrap.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerTrap.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerTrap.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTrap.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerTrap.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTrap.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerTrap.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerTrap.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_arm(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_disarm(): Dex.Future;

        /**
         * Gets the identifier for this trap (such as breakpoint number).
         * @virtual
         */
        vfunc_dup_id(): (string | null);

        /**
         * @virtual
         */
        vfunc_is_armed(): boolean;

        /**
         * @virtual
         */
        vfunc_remove(): Dex.Future;

        // Methods
        /**
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error
         */
        arm(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error
         */
        disarm(): Dex.Future;

        /**
         * Gets the identifier for this trap (such as breakpoint number).
         */
        dup_id(): (string | null);

        is_armed(): boolean;

        /**
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error
         */
        remove(): Dex.Future;
    }


    namespace DebuggerTrapParams {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::access": (pspec: GObject.ParamSpec) => void;
            "notify::disposition": (pspec: GObject.ParamSpec) => void;
            "notify::function": (pspec: GObject.ParamSpec) => void;
            "notify::instruction-pointer": (pspec: GObject.ParamSpec) => void;
            "notify::kind": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::line-offset": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::stack-frame-id": (pspec: GObject.ParamSpec) => void;
            "notify::thread-id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            access: DebuggerWatchAccess;
            disposition: DebuggerTrapDisposition;
            "function": string;
            instruction_pointer: (bigint | number);
            instructionPointer: (bigint | number);
            kind: DebuggerTrapKind;
            line: number;
            line_offset: number;
            lineOffset: number;
            path: string;
            stack_frame_id: string;
            stackFrameId: string;
            thread_id: string;
            threadId: string;
        }
    }

    /**
     * Represents parameters for debugger traps and breakpoints.
     * 
     * FoundryDebuggerTrapParams provides functionality for configuring
     * debugger traps including breakpoints, watchpoints, and other
     * debugging stops. It includes location information, access patterns,
     * and disposition settings for precise control over debugging behavior.
     * @gir-type Class
     */
    class DebuggerTrapParams extends GObject.Object {
        static $gtype: GObject.GType<DebuggerTrapParams>;

        // Properties
        /**
         * @default Foundry.DebuggerWatchAccess.NONE
         */
        get access(): DebuggerWatchAccess;
        set access(val: DebuggerWatchAccess);

        /**
         * @default Foundry.DebuggerTrapDisposition.KEEP
         */
        get disposition(): DebuggerTrapDisposition;
        set disposition(val: DebuggerTrapDisposition);

        /**
         * @default null
         */
        get "function"(): string;
        set "function"(val: string);

        /**
         * @default 0
         */
        get instruction_pointer(): number;
        set instruction_pointer(val: (bigint | number));

        /**
         * @default 0
         */
        get instructionPointer(): number;
        set instructionPointer(val: (bigint | number));

        /**
         * @default Foundry.DebuggerTrapKind.BREAKPOINT
         */
        get kind(): DebuggerTrapKind;
        set kind(val: DebuggerTrapKind);

        /**
         * @default 0
         */
        get line(): number;
        set line(val: number);

        /**
         * @default 0
         */
        get line_offset(): number;
        set line_offset(val: number);

        /**
         * @default 0
         */
        get lineOffset(): number;
        set lineOffset(val: number);

        /**
         * @default null
         */
        get path(): string;
        set path(val: string);

        /**
         * @default null
         */
        get stack_frame_id(): string;
        set stack_frame_id(val: string);

        /**
         * @default null
         */
        get stackFrameId(): string;
        set stackFrameId(val: string);

        /**
         * @default null
         */
        get thread_id(): string;
        set thread_id(val: string);

        /**
         * @default null
         */
        get threadId(): string;
        set threadId(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerTrapParams.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerTrapParams.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): DebuggerTrapParams;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerTrapParams.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTrapParams.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerTrapParams.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerTrapParams.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerTrapParams.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerTrapParams.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        copy(): (DebuggerTrapParams | null);

        dup_function(): string;

        dup_path(): string;

        dup_stack_frame_id(): string;

        dup_thread_id(): string;

        get_access(): DebuggerWatchAccess;

        get_disposition(): DebuggerTrapDisposition;

        get_instruction_pointer(): number;

        get_kind(): DebuggerTrapKind;

        get_line(): number;

        get_line_offset(): number;

        /**
         * @param access 
         */
        set_access(access: DebuggerWatchAccess): void;

        /**
         * @param disposition 
         */
        set_disposition(disposition: DebuggerTrapDisposition): void;

        /**
         * @param _function 
         */
        set_function(_function: string): void;

        /**
         * @param instruction_pointer 
         */
        set_instruction_pointer(instruction_pointer: (bigint | number)): void;

        /**
         * @param kind 
         */
        set_kind(kind: DebuggerTrapKind): void;

        /**
         * @param line 
         */
        set_line(line: number): void;

        /**
         * @param line_offset 
         */
        set_line_offset(line_offset: number): void;

        /**
         * @param path 
         */
        set_path(path: string): void;

        /**
         * @param stack_frame_id 
         */
        set_stack_frame_id(stack_frame_id: string): void;

        /**
         * @param thread_id 
         */
        set_thread_id(thread_id: string): void;
    }


    namespace DebuggerVariable {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::type-name": (pspec: GObject.ParamSpec) => void;
            "notify::value": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            name: string;
            type_name: string;
            typeName: string;
            value: string;
        }
    }

    /**
     * Abstract base class for representing variables in the debugger.
     * 
     * FoundryDebuggerVariable provides the core interface for representing
     * variables during debugging sessions. It includes variable name, type,
     * and value information, and provides a unified interface for variable
     * inspection and manipulation across different debugging backends.
     * @gir-type Class
     */
    abstract class DebuggerVariable extends GObject.Object {
        static $gtype: GObject.GType<DebuggerVariable>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         * @default null
         */
        get type_name(): string;

        /**
         * @read-only
         * @default null
         */
        get typeName(): string;

        /**
         * @read-only
         * @default null
         */
        get value(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerVariable.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerVariable.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerVariable.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerVariable.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerVariable.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerVariable.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerVariable.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerVariable.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_name(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_type_name(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_value(): (string | null);

        /**
         * If the number of children is known, it will be set to `n_children`. Otherwise
         * it should be set to zero.
         * @virtual
         */
        vfunc_is_structured(): [boolean, number];

        /**
         * Queries the structured children of the variable.
         * @virtual
         */
        vfunc_list_children(): Dex.Future;

        /**
         * Read `count` bytes at `offset` of the variable.
         * @param offset offset to begin reading from
         * @param count number of bytes to read, must be > 0
         * @virtual
         */
        vfunc_read_memory(offset: number, count: number): Dex.Future;

        // Methods
        dup_name(): (string | null);

        dup_type_name(): (string | null);

        dup_value(): (string | null);

        /**
         * If the number of children is known, it will be set to `n_children`. Otherwise
         * it should be set to zero.
         * @returns `True` if `self` is known to have children that may be queried;   otherwise `False`.
         */
        is_structured(): [boolean, number];

        /**
         * Queries the structured children of the variable.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.DebuggerVariable} or   rejects with error.
         */
        list_children(): Dex.Future;

        /**
         * Read `count` bytes at `offset` of the variable.
         * @param offset offset to begin reading from
         * @param count number of bytes to read, must be > 0
         * @returns a {@link Dex.Future} that resolves to a   {@link GLib.Bytes} or rejects with error. Sicne: 1.1
         */
        read_memory(offset: (bigint | number), count: (bigint | number)): Dex.Future;
    }


    namespace DebuggerWatchpoint {
        // Signal signatures
        interface SignalSignatures extends DebuggerTrap.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends DebuggerTrap.ConstructorProps {

        }
    }

    /**
     * Abstract base class for debuggers to imlement traps upon memory changing.
     * @gir-type Class
     */
    abstract class DebuggerWatchpoint extends DebuggerTrap {
        static $gtype: GObject.GType<DebuggerWatchpoint>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DebuggerWatchpoint.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DebuggerWatchpoint.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DebuggerWatchpoint.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerWatchpoint.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DebuggerWatchpoint.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DebuggerWatchpoint.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DebuggerWatchpoint.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DebuggerWatchpoint.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace Dependency {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::kind": (pspec: GObject.ParamSpec) => void;
            "notify::location": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            kind: string;
            location: string;
            name: string;
            provider: DependencyProvider;
        }
    }

    /**
     * Abstract base class to represent a dependency implemented by plugins.
     * @gir-type Class
     */
    abstract class Dependency extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Dependency>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get kind(): string;

        /**
         * @read-only
         * @default null
         */
        get location(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @construct-only
         */
        get provider(): DependencyProvider;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Dependency.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Dependency.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Dependency.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Dependency.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Dependency.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Dependency.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Dependency.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Dependency.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_kind(): string;

        /**
         * @virtual
         */
        vfunc_dup_location(): string;

        /**
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @virtual
         */
        vfunc_dup_provider(): (DependencyProvider | null);

        // Methods
        dup_kind(): string;

        dup_location(): string;

        dup_name(): string;

        dup_provider(): (DependencyProvider | null);

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DependencyManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages plugins implementing {@link Foundry.DependencyProvider}.
     * @gir-type Class
     */
    class DependencyManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<DependencyManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DependencyManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DependencyManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DependencyManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DependencyManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DependencyManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DependencyManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DependencyManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DependencyManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param config a {@link Foundry.Config}
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel}.
         */
        list_dependencies(config: Config): Dex.Future;

        /**
         * @param config 
         * @param pty_fd 
         * @param cancellable 
         */
        update_dependencies(config: Config, pty_fd: number, cancellable: Dex.Cancellable): Dex.Future;
    }


    namespace DependencyProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for plugins to provide known dependencies.
     * @gir-type Class
     */
    abstract class DependencyProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<DependencyProvider>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DependencyProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DependencyProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DependencyProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DependencyProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DependencyProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DependencyProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DependencyProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DependencyProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Retrieves a list of dependencies for `config`.
         * 
         * `parent` may be set to a previously provided {@link Foundry.Dependency} in
         * which case the provider must list the dependencies of that dependency or
         * reject with error if unsupported. `G_IO_ERROR_NOT_SUPPORTED` is the
         * preferred error code for this.
         * @param config a {@link Foundry.Config}
         * @param parent a {@link Foundry.Dependency} or `null`
         * @virtual
         */
        vfunc_list_dependencies(config: Config, parent: (Dependency | null)): Dex.Future;

        /**
         * @param config 
         * @param dependencies 
         * @param pty_fd 
         * @param cancellable 
         * @virtual
         */
        vfunc_update_dependencies(config: Config, dependencies: Gio.ListModel, pty_fd: number, cancellable: Dex.Cancellable): Dex.Future;

        // Methods
        /**
         * Gets the plugin the provider belongs to.
         * @returns a {@link Peas.PluginInfo} or `null`
         */
        dup_plugin_info(): Peas.PluginInfo;

        /**
         * Retrieves a list of dependencies for `config`.
         * 
         * `parent` may be set to a previously provided {@link Foundry.Dependency} in
         * which case the provider must list the dependencies of that dependency or
         * reject with error if unsupported. `G_IO_ERROR_NOT_SUPPORTED` is the
         * preferred error code for this.
         * @param config a {@link Foundry.Config}
         * @param parent a {@link Foundry.Dependency} or `null`
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.Dependency} or rejects with   an error.
         */
        list_dependencies(config: Config, parent: (Dependency | null)): Dex.Future;

        /**
         * @param config 
         * @param dependencies 
         * @param pty_fd 
         * @param cancellable 
         */
        update_dependencies(config: Config, dependencies: Gio.ListModel, pty_fd: number, cancellable: Dex.Cancellable): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DeployStrategy {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::pipeline": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            pipeline: BuildPipeline;
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Represents how a plugin can deploy a build to a specific device for running.
     * @gir-type Class
     */
    abstract class DeployStrategy extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<DeployStrategy>;

        // Properties
        /**
         * @construct-only
         */
        get pipeline(): BuildPipeline;

        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DeployStrategy.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DeployStrategy.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](pipeline: BuildPipeline): DeployStrategy;

        // Signals
        /** @signal */
        connect<K extends keyof DeployStrategy.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DeployStrategy.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DeployStrategy.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DeployStrategy.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DeployStrategy.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DeployStrategy.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Checks if the deploy strategy is deploy for the current configuration.
         * @param pty_fd 
         * @param cancellable 
         * @virtual
         */
        vfunc_deploy(pty_fd: number, cancellable: Dex.Cancellable): Dex.Future;

        /**
         * Prepares `launcher` to be able to run a command on the device with access
         * to a deployed installation of `pipeline`.
         * @param launcher a {@link Foundry.ProcessLauncher} to prepare
         * @param pipeline a {@link Foundry.BuildPipeline} containing the build stages
         * @param pty_fd the PTY device to use for stdin/stdout/stderr, or -1
         * @param cancellable an optional {@link Dex.Cancellable} for cancellation
         * @virtual
         */
        vfunc_prepare(launcher: ProcessLauncher, pipeline: BuildPipeline, pty_fd: number, cancellable: (Dex.Cancellable | null)): Dex.Future;

        /**
         * Checks if the deploy strategy is supported for the current configuration.
         * @virtual
         */
        vfunc_supported(): Dex.Future;

        // Methods
        /**
         * Checks if the deploy strategy is deploy for the current configuration.
         * @param pty_fd 
         * @param cancellable 
         * @returns a {@link Dex.Future} that resolves the priority of the   strategy or rejects with error if undeploy.
         */
        deploy(pty_fd: number, cancellable: Dex.Cancellable): Dex.Future;

        /**
         * @returns a {@link Foundry.BuildPipeline}
         */
        dup_pipeline(): BuildPipeline;

        /**
         * @returns a {@link Peas.PluginInfo}
         */
        dup_plugin_info(): Peas.PluginInfo;

        /**
         * Prepares `launcher` to be able to run a command on the device with access
         * to a deployed installation of `pipeline`.
         * @param launcher a {@link Foundry.ProcessLauncher} to prepare
         * @param pipeline a {@link Foundry.BuildPipeline} containing the build stages
         * @param pty_fd the PTY device to use for stdin/stdout/stderr, or -1
         * @param cancellable an optional {@link Dex.Cancellable} for cancellation
         * @returns a {@link Dex.Future} that resolves to any value.
         */
        prepare(launcher: ProcessLauncher, pipeline: BuildPipeline, pty_fd: number, cancellable: (Dex.Cancellable | null)): Dex.Future;

        /**
         * Checks if the deploy strategy is supported for the current configuration.
         * @returns a {@link Dex.Future} that resolves the priority of the   strategy or rejects with error if unsupported.
         */
        supported(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Device {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::active": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            active: boolean;
            id: string;
            provider: DeviceProvider;
        }
    }

    /**
     * Represents a local or remote device that can be deployed to.
     * @gir-type Class
     */
    abstract class Device extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Device>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get active(): boolean;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         */
        get provider(): DeviceProvider;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Device.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Device.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Device.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Device.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Device.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Device.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Device.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Device.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets the user-visible id for the device.
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * Loads information about the device.
         * @virtual
         */
        vfunc_load_info(): Dex.Future;

        // Methods
        /**
         * Gets the user-visible id for the device.
         * @returns a newly allocated string
         */
        dup_id(): string;

        get_active(): boolean;

        /**
         * Loads information about the device.
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.DeviceInfo}.
         */
        load_info(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DeviceInfo {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::active": (pspec: GObject.ParamSpec) => void;
            "notify::chassis": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::triplet": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            active: boolean;
            chassis: DeviceChassis;
            device: Device;
            id: string;
            name: string;
            triplet: Triplet;
        }
    }

    /**
     * Minimal descriptive information about a device.
     * @gir-type Class
     */
    abstract class DeviceInfo extends GObject.Object {
        static $gtype: GObject.GType<DeviceInfo>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get active(): boolean;

        /**
         * @read-only
         * @default Foundry.DeviceChassis.OTHER
         */
        get chassis(): DeviceChassis;

        /**
         * @construct-only
         */
        get device(): Device;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         */
        get triplet(): Triplet;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DeviceInfo.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DeviceInfo.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DeviceInfo.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DeviceInfo.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DeviceInfo.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DeviceInfo.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DeviceInfo.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DeviceInfo.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @virtual
         */
        vfunc_dup_triplet(): Triplet;

        /**
         * @virtual
         */
        vfunc_get_chassis(): DeviceChassis;

        // Methods
        dup_id(): string;

        dup_name(): string;

        /**
         * @returns a {@link Foundry.Triplet}
         */
        dup_triplet(): Triplet;

        get_active(): boolean;

        get_chassis(): DeviceChassis;
    }


    namespace DeviceManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            device: Device;
        }
    }

    /**
     * Manage plugins providing device integration and their devices.
     * @gir-type Class
     */
    class DeviceManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<DeviceManager>;

        // Properties
        get device(): Device;
        set device(val: Device);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DeviceManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DeviceManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DeviceManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DeviceManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DeviceManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DeviceManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DeviceManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DeviceManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the active device to build for.
         * 
         * Typically this is a {@link Foundry.LocalDevice} unless targeting a non-local device.
         * @returns a {@link Foundry.Device} or `null`
         */
        dup_device(): (Device | null);

        /**
         * Looks through available devices to find one matching `device_id`.
         * @param device_id an identifier matching a {@link Foundry.Device.id}
         * @returns a {@link Foundry.Device} or `null`
         */
        find_device(device_id: string): (Device | null);

        /**
         * @param device 
         */
        set_device(device: Device): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace DeviceProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class for plugins which want to provide specific device integration.
     * @gir-type Class
     */
    abstract class DeviceProvider<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<DeviceProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DeviceProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DeviceProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DeviceProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DeviceProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DeviceProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DeviceProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DeviceProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DeviceProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @virtual
         */
        vfunc_dup_name(): string;

        // Methods
        /**
         * @param device 
         */
        device_added(device: Device): void;

        /**
         * @param device 
         */
        device_removed(device: Device): void;

        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @returns the name of the provider
         */
        dup_name(): string;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Diagnostic {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::has-fix": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::line-offset": (pspec: GObject.ParamSpec) => void;
            "notify::markup": (pspec: GObject.ParamSpec) => void;
            "notify::message": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::ranges": (pspec: GObject.ParamSpec) => void;
            "notify::rule-id": (pspec: GObject.ParamSpec) => void;
            "notify::severity": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            file: Gio.File;
            has_fix: boolean;
            hasFix: boolean;
            line: number;
            line_offset: number;
            lineOffset: number;
            markup: Markup;
            message: string;
            path: string;
            ranges: Gio.ListModel;
            rule_id: string;
            ruleId: string;
            severity: DiagnosticSeverity;
        }
    }

    /**
     * Represents a single diagnostic.
     * @gir-type Class
     */
    class Diagnostic extends GObject.Object {
        static $gtype: GObject.GType<Diagnostic>;

        // Properties
        /**
         * @read-only
         */
        get file(): Gio.File;

        /**
         * @read-only
         * @default false
         */
        get has_fix(): boolean;

        /**
         * @read-only
         * @default false
         */
        get hasFix(): boolean;

        /**
         * @read-only
         * @default 0
         */
        get line(): number;

        /**
         * @read-only
         * @default 0
         */
        get line_offset(): number;

        /**
         * @read-only
         * @default 0
         */
        get lineOffset(): number;

        /**
         * @read-only
         */
        get markup(): Markup;

        /**
         * @read-only
         * @default null
         */
        get message(): string;

        /**
         * @read-only
         * @default null
         */
        get path(): string;

        /**
         * @read-only
         */
        get ranges(): Gio.ListModel;

        /**
         * The "rule-id" property is an identifier to the type of rule for the
         * diagnostic.
         * 
         * Not all diagnostics may have this, but it is generally useful to provide
         * from diagnostic providers where a known rule is applied. For example, many
         * compilers have a rule identifier which may be silenced by matching the
         * value of this property.
         * @since 1.1
         * @read-only
         * @default null
         */
        get rule_id(): string;

        /**
         * The "rule-id" property is an identifier to the type of rule for the
         * diagnostic.
         * 
         * Not all diagnostics may have this, but it is generally useful to provide
         * from diagnostic providers where a known rule is applied. For example, many
         * compilers have a rule identifier which may be silenced by matching the
         * value of this property.
         * @since 1.1
         * @read-only
         * @default null
         */
        get ruleId(): string;

        /**
         * @read-only
         * @default Foundry.DiagnosticSeverity.NOTE
         */
        get severity(): DiagnosticSeverity;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Diagnostic.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Diagnostic.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Diagnostic.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Diagnostic.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Diagnostic.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Diagnostic.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Diagnostic.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Diagnostic.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * @param data 
         */
        static hash(data: null): number;

        // Methods
        /**
         * @param right 
         */
        compare(right: Diagnostic): number;

        /**
         * @returns a {@link Gio.File} or `null`
         */
        dup_file(): (Gio.File | null);

        /**
         * Get the markup for the diagnostic, if any.
         */
        dup_markup(): (Markup | null);

        /**
         * Gets the message for the diagnostic, if any.
         * @returns a message string or `null`
         */
        dup_message(): (string | null);

        /**
         * Gets the path for the diagnostic, if any.
         * @returns a string or `null`
         */
        dup_path(): (string | null);

        /**
         * Gets the {@link Foundry.Diagnostic.rule_id} property.
         */
        dup_rule_id(): (string | null);

        /**
         * @param right 
         */
        equal(right: Diagnostic): boolean;

        /**
         * Gets the line number of the diagnostic.
         * 
         * A value of 0 indicates that the line is unset.
         * @returns A line number starting from 1, or 0 to indicate unset.
         */
        get_line(): number;

        /**
         * Gets the offset within the line of the diagnostic.
         * 
         * A value of 0 indicates that the line offset is unset.
         * @returns A character offset within the line starting from 1,  or 0 to indicate unset.
         */
        get_line_offset(): number;

        get_severity(): DiagnosticSeverity;

        /**
         * Gets the available {@link Foundry.DiagnosticFix} for the diagnostic.
         * @returns a {@link Gio.ListModel} of   {@link Foundry.DiagnosticFix}
         */
        list_fixes(): (Gio.ListModel | null);

        /**
         * Gets the available ranges as a {@link Gio.ListModel} of {@link Foundry.DiagnosticRange}.
         * @returns a {@link Gio.ListModel} or `null` if there are   no ranges associated with this diagnostic.
         */
        list_ranges(): Gio.ListModel;
    }


    namespace DiagnosticFix {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::text-edits": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            description: string;
            text_edits: Gio.ListModel;
            textEdits: Gio.ListModel;
        }
    }

    /**
     * Represents a fix that may be applied to resolve a diagnostic.
     * @gir-type Class
     */
    class DiagnosticFix extends GObject.Object {
        static $gtype: GObject.GType<DiagnosticFix>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get description(): string;

        /**
         * @read-only
         */
        get text_edits(): Gio.ListModel;

        /**
         * @read-only
         */
        get textEdits(): Gio.ListModel;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DiagnosticFix.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DiagnosticFix.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DiagnosticFix.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticFix.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DiagnosticFix.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticFix.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DiagnosticFix.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DiagnosticFix.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_description(): string;

        /**
         * Gets the list of changes to be applied to fix the diagnostic.
         * @returns a {@link Gio.ListModel} of   {@link Foundry.TextEdit}.
         */
        list_text_edits(): Gio.ListModel;
    }


    namespace DiagnosticManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages diagnostic information and error reporting.
     * 
     * FoundryDiagnosticManager coordinates diagnostic providers and aggregates
     * diagnostic information from various sources such as compilers, linters, and
     * language servers. It provides a unified interface for error reporting and
     * maintains diagnostic state.
     * @gir-type Class
     */
    class DiagnosticManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<DiagnosticManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DiagnosticManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DiagnosticManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DiagnosticManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DiagnosticManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DiagnosticManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DiagnosticManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Diagnoses `file` using the loaded diagnostic providers and produces a
         * {@link Dex.Future} that will resolve to a {@link Gio.ListModel} of
         * {@link Foundry.Diagnostic}.
         * 
         * The resulting {@link Gio.ListModel} may be awaited for population to
         * complete using {@link Foundry.list_model_await}.
         * @param file a {@link Gio.File}
         * @param contents optional {@link GLib.Bytes} for file contents
         * @param language the language identifier for `file`
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.Diagnostic}.
         */
        diagnose(file: (Gio.File | null), contents: (GLib.Bytes | null), language: (string | null)): Dex.Future;

        /**
         * @param file a {@link Gio.File}
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} which may be awaited on for final   completion of all diagnostics using {@link Foundry.list_model_await}.
         */
        diagnose_file(file: Gio.File): Dex.Future;

        /**
         * @param files an array of {@link Gio.File}
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} or `null`
         */
        diagnose_files(files: Gio.File[]): Dex.Future;

        /**
         * Lists all known diagnostics from all providers.
         * 
         * This will call {@link Foundry.DiagnosticProvider.list_all} for every
         * available diagnostic provider.
         * 
         * The list may update after the future resolves if providers implement
         * live updating of models.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.Diagnostic}.
         */
        list_all(): Dex.Future;
    }


    namespace DiagnosticProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for plugins to provide diagnostics.
     * @gir-type Class
     */
    abstract class DiagnosticProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<DiagnosticProvider>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DiagnosticProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DiagnosticProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DiagnosticProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DiagnosticProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DiagnosticProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DiagnosticProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Processes `file` to extract diagnostics.
         * @param file the {@link Gio.File} of the underlying file, if any
         * @param contents the {@link GLib.Bytes} of file contents, or `null`
         * @param language the language code such as "c"
         * @virtual
         */
        vfunc_diagnose(file: (Gio.File | null), contents: (GLib.Bytes | null), language: (string | null)): Dex.Future;

        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * Lists all diagnostics known to the provider for the project.
         * 
         * This is useful for applications which want to show a project-wide list of
         * diagnostics. Providers are encouraged to have this information cached
         * rather than try to scan the whole project when requested.
         * 
         * For example, diagnostics for a build may include results from the most
         * recent build request.
         * 
         * It is encouraged that providers update the listmodel as new diagnostics
         * are made available.
         * @virtual
         */
        vfunc_list_all(): Dex.Future;

        // Methods
        /**
         * Processes `file` to extract diagnostics.
         * @param file the {@link Gio.File} of the underlying file, if any
         * @param contents the {@link GLib.Bytes} of file contents, or `null`
         * @param language the language code such as "c"
         * @returns a {@link Dex.Future} that resolves to a {@link Gio.ListModel}   of {@link Foundry.Diagnostic}.
         */
        diagnose(file: (Gio.File | null), contents: (GLib.Bytes | null), language: (string | null)): Dex.Future;

        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @returns the name of the provider
         */
        dup_name(): string;

        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * Lists all diagnostics known to the provider for the project.
         * 
         * This is useful for applications which want to show a project-wide list of
         * diagnostics. Providers are encouraged to have this information cached
         * rather than try to scan the whole project when requested.
         * 
         * For example, diagnostics for a build may include results from the most
         * recent build request.
         * 
         * It is encouraged that providers update the listmodel as new diagnostics
         * are made available.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.Diagnostic}.
         */
        list_all(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DiagnosticRange {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::end-line": (pspec: GObject.ParamSpec) => void;
            "notify::end-line-offset": (pspec: GObject.ParamSpec) => void;
            "notify::start-line": (pspec: GObject.ParamSpec) => void;
            "notify::start-line-offset": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            end_line: number;
            endLine: number;
            end_line_offset: number;
            endLineOffset: number;
            start_line: number;
            startLine: number;
            start_line_offset: number;
            startLineOffset: number;
        }
    }

    /**
     * Represents a range within a file that a diagnostic applies to.
     * @gir-type Class
     */
    class DiagnosticRange extends GObject.Object {
        static $gtype: GObject.GType<DiagnosticRange>;

        // Properties
        /**
         * @construct-only
         * @default 0
         */
        get end_line(): number;

        /**
         * @construct-only
         * @default 0
         */
        get endLine(): number;

        /**
         * @construct-only
         * @default 0
         */
        get end_line_offset(): number;

        /**
         * @construct-only
         * @default 0
         */
        get endLineOffset(): number;

        /**
         * @construct-only
         * @default 0
         */
        get start_line(): number;

        /**
         * @construct-only
         * @default 0
         */
        get startLine(): number;

        /**
         * @construct-only
         * @default 0
         */
        get start_line_offset(): number;

        /**
         * @construct-only
         * @default 0
         */
        get startLineOffset(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DiagnosticRange.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DiagnosticRange.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DiagnosticRange.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticRange.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DiagnosticRange.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticRange.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DiagnosticRange.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DiagnosticRange.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        get_end_line(): number;

        get_end_line_offset(): number;

        get_start_line(): number;

        get_start_line_offset(): number;
    }


    namespace DiagnosticTool {
        // Signal signatures
        interface SignalSignatures extends DiagnosticProvider.SignalSignatures {
            "notify::argv": (pspec: GObject.ParamSpec) => void;
            "notify::environ": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends DiagnosticProvider.ConstructorProps, Json.Serializable.ConstructorProps {
            argv: string[];
            environ: string[];
        }
    }

    /**
     * Simplified base class for working with diagnostic tooling which provide a
     * structured format.
     * @gir-type Class
     */
    class DiagnosticTool extends DiagnosticProvider implements Json.Serializable {
        static $gtype: GObject.GType<DiagnosticTool>;

        // Properties
        get argv(): string[];
        set argv(val: string[]);

        get environ(): string[];
        set environ(val: string[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DiagnosticTool.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DiagnosticTool.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DiagnosticTool.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticTool.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DiagnosticTool.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DiagnosticTool.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DiagnosticTool.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DiagnosticTool.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        dup_argv(): (string[] | null);

        dup_environ(): (string[] | null);

        /**
         * @param argv 
         */
        set_argv(argv: string): void;

        /**
         * @param environ 
         */
        set_environ(environ: string): void;
    }


    namespace DirectoryItem {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::directory": (pspec: GObject.ParamSpec) => void;
            "notify::display-name": (pspec: GObject.ParamSpec) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::ignored": (pspec: GObject.ParamSpec) => void;
            "notify::info": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::size": (pspec: GObject.ParamSpec) => void;
            "notify::status": (pspec: GObject.ParamSpec) => void;
            "notify::symbolic-icon": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            directory: Gio.File;
            display_name: string;
            displayName: string;
            file: Gio.File;
            ignored: boolean;
            info: Gio.FileInfo;
            name: string;
            size: (bigint | number);
            status: VcsFileStatus;
            symbolic_icon: Gio.Icon;
            symbolicIcon: Gio.Icon;
        }
    }

    /**
     * Represents a single item found with in a directory listing.
     * @gir-type Class
     */
    class DirectoryItem extends GObject.Object {
        static $gtype: GObject.GType<DirectoryItem>;

        // Properties
        /**
         * @construct-only
         */
        get directory(): Gio.File;

        /**
         * @read-only
         * @default null
         */
        get display_name(): string;

        /**
         * @read-only
         * @default null
         */
        get displayName(): string;

        /**
         * @construct-only
         */
        get file(): Gio.File;

        /**
         * @read-only
         * @default false
         */
        get ignored(): boolean;

        /**
         * @construct-only
         */
        get info(): Gio.FileInfo;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         * @default 0
         */
        get size(): number;

        /**
         * @read-only
         * @default Foundry.VcsFileStatus.CURRENT
         */
        get status(): VcsFileStatus;

        /**
         * @read-only
         */
        get symbolic_icon(): Gio.Icon;

        /**
         * @read-only
         */
        get symbolicIcon(): Gio.Icon;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DirectoryItem.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DirectoryItem.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DirectoryItem.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DirectoryItem.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DirectoryItem.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DirectoryItem.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DirectoryItem.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DirectoryItem.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_content_type(): string;

        dup_directory(): Gio.File;

        dup_display_name(): string;

        dup_file(): Gio.File;

        dup_info(): Gio.FileInfo;

        dup_name(): string;

        dup_symbolic_icon(): (Gio.Icon | null);

        get_file_type(): Gio.FileType;

        get_size(): number;

        get_status(): VcsFileStatus;

        is_ignored(): boolean;
    }


    namespace DirectoryListing {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::attributes": (pspec: GObject.ParamSpec) => void;
            "notify::directory": (pspec: GObject.ParamSpec) => void;
            "notify::include-parent": (pspec: GObject.ParamSpec) => void;
            "notify::query-flags": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            attributes: string;
            directory: Gio.File;
            include_parent: boolean;
            includeParent: boolean;
            query_flags: Gio.FileQueryInfoFlags;
            queryFlags: Gio.FileQueryInfoFlags;
        }
    }

    /**
     * Manages directory contents and file listings.
     * 
     * FoundryDirectoryListing provides a model for displaying directory contents
     * with support for file monitoring, VCS integration, and custom attributes.
     * It maintains a sequence of directory items and provides efficient access
     * to file information for the file manager and other components.
     * @gir-type Class
     */
    class DirectoryListing<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<DirectoryListing>;

        // Properties
        /**
         * @construct-only
         * @default null
         */
        get attributes(): string;

        /**
         * @construct-only
         */
        get directory(): Gio.File;

        /**
         * @construct-only
         * @default false
         */
        get include_parent(): boolean;

        /**
         * @construct-only
         * @default false
         */
        get includeParent(): boolean;

        /**
         * @construct-only
         * @default Gio.FileQueryInfoFlags.NONE
         */
        get query_flags(): Gio.FileQueryInfoFlags;

        /**
         * @construct-only
         * @default Gio.FileQueryInfoFlags.NONE
         */
        get queryFlags(): Gio.FileQueryInfoFlags;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DirectoryListing.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DirectoryListing.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, directory: Gio.File, attributes: string, query_flags: Gio.FileQueryInfoFlags): DirectoryListing;

        // Signals
        /** @signal */
        connect<K extends keyof DirectoryListing.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DirectoryListing.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DirectoryListing.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DirectoryListing.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DirectoryListing.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DirectoryListing.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Await the completion of the initial directory listing.
         * @returns a {@link Dex.Future} that resolves to   a value or rejects with error.
         */
        ["await"](): Dex.Future;

        /**
         * Gets the directory for the listing.
         */
        dup_directory(): Gio.File;

        /**
         * Loads a similar directory listing for the parent directory.
         * @returns a {@link Foundry.DirectoryListing}
         */
        load_parent(): DirectoryListing;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DirectoryReaper {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The "remove-file" signal is emitted for each file that is removed by the
             * {@link Foundry.DirectoryReaper} instance. This may be useful if you want to show the
             * user what was processed by the reaper.
             * @signal
             * @run-last
             */
            "remove-file": (arg0: Gio.File) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Simplified interface for deleting files or directories recursively.
     * @gir-type Class
     */
    class DirectoryReaper extends GObject.Object {
        static $gtype: GObject.GType<DirectoryReaper>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DirectoryReaper.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DirectoryReaper.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): DirectoryReaper;

        // Signals
        /** @signal */
        connect<K extends keyof DirectoryReaper.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DirectoryReaper.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DirectoryReaper.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DirectoryReaper.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DirectoryReaper.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DirectoryReaper.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param directory 
         * @param min_age 
         */
        add_directory(directory: Gio.File, min_age: GLib.TimeSpan): void;

        /**
         * @param file 
         * @param min_age 
         */
        add_file(file: Gio.File, min_age: GLib.TimeSpan): void;

        /**
         * @param directory 
         * @param glob 
         * @param min_age 
         */
        add_glob(directory: Gio.File, glob: string, min_age: GLib.TimeSpan): void;

        /**
         * @returns a {@link Dex.Future} that resolves to a boolean.
         */
        execute(): Dex.Future;

        /**
         * @param cancellable 
         */
        execute_async(cancellable: (Gio.Cancellable | null)): globalThis.Promise<boolean>;

        /**
         * @param cancellable 
         * @param callback 
         */
        execute_async(cancellable: (Gio.Cancellable | null), callback: (Gio.AsyncReadyCallback<this> | null)): void;

        /**
         * @param cancellable 
         * @param callback 
         */
        execute_async(cancellable: (Gio.Cancellable | null), callback?: (Gio.AsyncReadyCallback<this> | null)): (globalThis.Promise<boolean> | void);

        /**
         * @param result 
         */
        execute_finish(result: Gio.AsyncResult): boolean;

        /**
         * @param cancellable 
         */
        execute_sync(cancellable: (Gio.Cancellable | null)): boolean;
    }


    namespace DoapFile {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::bug-database": (pspec: GObject.ParamSpec) => void;
            "notify::category": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::download-page": (pspec: GObject.ParamSpec) => void;
            "notify::homepage": (pspec: GObject.ParamSpec) => void;
            "notify::languages": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::shortdesc": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            bug_database: string;
            bugDatabase: string;
            category: string;
            description: string;
            download_page: string;
            downloadPage: string;
            homepage: string;
            languages: string;
            name: string;
            shortdesc: string;
        }
    }

    /**
     * @gir-type Class
     */
    class DoapFile extends GObject.Object {
        static $gtype: GObject.GType<DoapFile>;

        // Properties
        /**
         * @default null
         */
        get bug_database(): string;
        set bug_database(val: string);

        /**
         * @default null
         */
        get bugDatabase(): string;
        set bugDatabase(val: string);

        /**
         * @default null
         */
        get category(): string;
        set category(val: string);

        /**
         * @default null
         */
        get description(): string;
        set description(val: string);

        /**
         * @default null
         */
        get download_page(): string;
        set download_page(val: string);

        /**
         * @default null
         */
        get downloadPage(): string;
        set downloadPage(val: string);

        /**
         * @default null
         */
        get homepage(): string;
        set homepage(val: string);

        /**
         * @default null
         */
        get languages(): string;
        set languages(val: string);

        /**
         * @default null
         */
        get name(): string;
        set name(val: string);

        /**
         * @default null
         */
        get shortdesc(): string;
        set shortdesc(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DoapFile.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DoapFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static new_from_bytes(bytes: (GLib.Bytes | Uint8Array)): DoapFile;

        static new_from_file(file: Gio.File): DoapFile;

        // Signals
        /** @signal */
        connect<K extends keyof DoapFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DoapFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DoapFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DoapFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DoapFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DoapFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        get_bug_database(): string;

        get_category(): string;

        get_description(): string;

        get_download_page(): string;

        get_homepage(): string;

        /**
         * @returns a {@link GObject.Strv}.
         */
        get_languages(): string[];

        /**
         * @returns a {@link GLib.List} of {@link Foundry.DoapPerson}.
         */
        get_maintainers(): DoapPerson[];

        get_name(): string;

        get_shortdesc(): string;
    }


    namespace DoapPerson {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::email": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            email: string;
            name: string;
        }
    }

    /**
     * @gir-type Class
     */
    class DoapPerson extends GObject.Object {
        static $gtype: GObject.GType<DoapPerson>;

        // Properties
        /**
         * @default null
         */
        get email(): string;
        set email(val: string);

        /**
         * @default null
         */
        get name(): string;
        set name(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DoapPerson.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DoapPerson.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): DoapPerson;

        // Signals
        /** @signal */
        connect<K extends keyof DoapPerson.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DoapPerson.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DoapPerson.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DoapPerson.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DoapPerson.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DoapPerson.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        get_email(): string;

        get_name(): string;

        /**
         * @param email 
         */
        set_email(email: string): void;

        /**
         * @param name 
         */
        set_name(name: string): void;
    }


    namespace DoapService {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::doap-file": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {
            doap_file: DoapFile;
            doapFile: DoapFile;
        }
    }

    /**
     * @gir-type Class
     */
    class DoapService extends Service implements Json.Serializable {
        static $gtype: GObject.GType<DoapService>;

        // Properties
        /**
         * @read-only
         */
        get doap_file(): DoapFile;

        /**
         * @read-only
         */
        get doapFile(): DoapFile;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DoapService.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DoapService.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DoapService.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DoapService.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DoapService.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DoapService.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DoapService.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DoapService.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @returns a {@link Foundry.DoapFile} or `null`
         */
        dup_doap_file(): (DoapFile | null);
    }


    namespace Documentation {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::deprecated-in": (pspec: GObject.ParamSpec) => void;
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::menu-icon": (pspec: GObject.ParamSpec) => void;
            "notify::menu-title": (pspec: GObject.ParamSpec) => void;
            "notify::section-title": (pspec: GObject.ParamSpec) => void;
            "notify::since-version": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            deprecated_in: string;
            deprecatedIn: string;
            icon: Gio.Icon;
            menu_icon: Gio.Icon;
            menuIcon: Gio.Icon;
            menu_title: string;
            menuTitle: string;
            section_title: string;
            sectionTitle: string;
            since_version: string;
            sinceVersion: string;
            title: string;
            uri: string;
        }
    }

    /**
     * Abstract base class for plugins to provide a documentation endpoint.
     * 
     * This represents a single documentation endpoint like a class documentation
     * or method documentation.
     * @gir-type Class
     */
    abstract class Documentation extends GObject.Object {
        static $gtype: GObject.GType<Documentation>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get deprecated_in(): string;

        /**
         * @read-only
         * @default null
         */
        get deprecatedIn(): string;

        /**
         * @read-only
         */
        get icon(): Gio.Icon;

        /**
         * @read-only
         */
        get menu_icon(): Gio.Icon;

        /**
         * @read-only
         */
        get menuIcon(): Gio.Icon;

        /**
         * @read-only
         * @default null
         */
        get menu_title(): string;

        /**
         * @read-only
         * @default null
         */
        get menuTitle(): string;

        /**
         * @read-only
         * @default null
         */
        get section_title(): string;

        /**
         * @read-only
         * @default null
         */
        get sectionTitle(): string;

        /**
         * @read-only
         * @default null
         */
        get since_version(): string;

        /**
         * @read-only
         * @default null
         */
        get sinceVersion(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * @read-only
         * @default null
         */
        get uri(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Documentation.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Documentation.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Documentation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Documentation.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Documentation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Documentation.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Documentation.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Documentation.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_deprecated_in(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_icon(): (Gio.Icon | null);

        /**
         * @virtual
         */
        vfunc_dup_menu_icon(): (Gio.Icon | null);

        /**
         * @virtual
         */
        vfunc_dup_menu_title(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_section_title(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_since_version(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_title(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_uri(): (string | null);

        /**
         * @param other 
         * @virtual
         */
        vfunc_equal(other: Documentation): boolean;

        /**
         * @virtual
         */
        vfunc_find_children(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_find_parent(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_find_siblings(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_has_children(): boolean;

        /**
         * Query various attributes which may be part of the documentation
         * but are not required by plugins to implement.
         * @param attribute 
         * @virtual
         */
        vfunc_query_attribute(attribute: string): (string | null);

        // Methods
        dup_deprecated_in(): (string | null);

        dup_icon(): (Gio.Icon | null);

        dup_menu_icon(): (Gio.Icon | null);

        dup_menu_title(): (string | null);

        dup_section_title(): (string | null);

        dup_since_version(): (string | null);

        dup_title(): (string | null);

        dup_uri(): (string | null);

        /**
         * @param other 
         */
        equal(other: Documentation): boolean;

        /**
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.Documentation} or   rejects with error.
         */
        find_children(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.Documentation} or rejects with error.
         */
        find_parent(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.Documentation} or   rejects with error.
         */
        find_siblings(): Dex.Future;

        has_children(): boolean;

        /**
         * Query various attributes which may be part of the documentation
         * but are not required by plugins to implement.
         * @param attribute 
         */
        query_attribute(attribute: string): (string | null);
    }


    namespace DocumentationBundle {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::eol": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::installed": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            eol: boolean;
            id: string;
            installed: boolean;
            subtitle: string;
            title: string;
        }
    }

    /**
     * Abstract base class for plugins to provide installable documentation.
     * 
     * A bundle represents a group of documentation that may be installed such
     * as the docs for a specific SDK.
     * @gir-type Class
     */
    abstract class DocumentationBundle extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<DocumentationBundle>;

        // Properties
        /**
         * Whether the documentation bundle has reached end of life.
         * @since 1.1
         * @read-only
         * @default false
         */
        get eol(): boolean;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default false
         */
        get installed(): boolean;

        /**
         * @read-only
         * @default null
         */
        get subtitle(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DocumentationBundle.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DocumentationBundle.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DocumentationBundle.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationBundle.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DocumentationBundle.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationBundle.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DocumentationBundle.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DocumentationBundle.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_subtitle(): (string | null);

        /**
         * Gets tags for the documentation which may be useful to show the
         * user to help them make better selections.
         * @virtual
         */
        vfunc_dup_tags(): (string[] | null);

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        /**
         * Gets whether the documentation bundle has reached end of life.
         * @virtual
         */
        vfunc_get_eol(): boolean;

        /**
         * @virtual
         */
        vfunc_get_installed(): boolean;

        /**
         * @param operation a {@link Foundry.Operation} to be updated by the provider
         * @param cancellable 
         * @virtual
         */
        vfunc_install(operation: Operation, cancellable: Dex.Cancellable): Dex.Future;

        // Methods
        dup_id(): string;

        dup_subtitle(): (string | null);

        /**
         * Gets tags for the documentation which may be useful to show the
         * user to help them make better selections.
         */
        dup_tags(): (string[] | null);

        dup_title(): string;

        /**
         * Gets whether the documentation bundle has reached end of life.
         * @returns `true` if the bundle has reached end of life, `false` otherwise
         */
        get_eol(): boolean;

        get_installed(): boolean;

        /**
         * @param operation a {@link Foundry.Operation} to be updated by the provider
         * @param cancellable 
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        install(operation: Operation, cancellable: Dex.Cancellable): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DocumentationIntent {
        // Signal signatures
        interface SignalSignatures extends Intent.SignalSignatures {
            "notify::documentation": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Intent.ConstructorProps {
            documentation: Documentation;
        }
    }

    /**
     * Represents an intent to display documentation.
     * 
     * FoundryDocumentationIntent provides functionality for displaying
     * a {@link Foundry.Documentation} object. It integrates with the intent
     * system to provide a unified interface for documentation display
     * operations across different parts of the development environment.
     * @gir-type Class
     * @since 1.1
     */
    class DocumentationIntent extends Intent {
        static $gtype: GObject.GType<DocumentationIntent>;

        // Properties
        /**
         * @construct-only
         */
        get documentation(): Documentation;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DocumentationIntent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DocumentationIntent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](documentation: Documentation): DocumentationIntent;

        // Signals
        /** @signal */
        connect<K extends keyof DocumentationIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationIntent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DocumentationIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationIntent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DocumentationIntent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DocumentationIntent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets a copy of the documentation object.
         * @returns a {@link Foundry.Documentation}
         */
        dup_documentation(): Documentation;
    }


    namespace DocumentationManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            /**
             * @signal
             * @run-last
             */
            changed: () => void;
            "notify::indexing": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {
            indexing: boolean;
        }
    }

    /**
     * Service that manages documentation plugins.
     * @gir-type Class
     */
    class DocumentationManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<DocumentationManager>;

        // Properties
        /**
         * If the documentation is currently indexing documentation.
         * @read-only
         * @default false
         */
        get indexing(): boolean;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DocumentationManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DocumentationManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DocumentationManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DocumentationManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DocumentationManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DocumentationManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param uri 
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.Documentation} or rejects with error.
         */
        find_by_uri(uri: string): Dex.Future;

        /**
         * Ensures the documentation manager has indexed.
         * 
         * This happens on demand but can be forced to start earlier by
         * applications which may want to delay operations until this
         * part has completed. Such an example is to now show a window
         * until the initial indexing has completed.
         */
        index(): Dex.Future;

        /**
         * If the documentation manager is currently indexing.
         */
        is_indexing(): boolean;

        /**
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.DocumentationBundle}   or rejects with error.
         */
        list_bundles(): Dex.Future;

        /**
         * @param parent 
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} or rejects with error.
         */
        list_children(parent: Documentation): Dex.Future;

        /**
         * Consumers can call {@link Foundry.DocumentationMatches.await} to get a
         * future that will complete when all matches have been populated.
         * @param query a {@link Foundry.DocumentationQuery}
         * @returns a {@link Dex.Future} that resolves    to [Foundry.DocumentationMatches].
         */
        query(query: DocumentationQuery): Dex.Future;
    }


    namespace DocumentationMatches {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::query": (pspec: GObject.ParamSpec) => void;
            "notify::sections": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends GObject.Object.ConstructorProps, Gio.ListModel.ConstructorProps {
            query: DocumentationQuery;
            sections: Gio.ListModel;
        }
    }

    /**
     * Represents a set of matches from a documentation query.
     * @gir-type Class
     */
    class DocumentationMatches<A extends GObject.Object = GObject.Object> extends GObject.Object implements Gio.ListModel<A> {
        static $gtype: GObject.GType<DocumentationMatches>;

        // Properties
        /**
         * @construct-only
         */
        get query(): DocumentationQuery;

        /**
         * @read-only
         */
        get sections(): Gio.ListModel;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DocumentationMatches.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DocumentationMatches.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DocumentationMatches.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationMatches.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DocumentationMatches.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationMatches.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DocumentationMatches.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DocumentationMatches.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param section 
         */
        add_section(section: Gio.ListModel): void;

        /**
         * @returns a {@link Dex.Future} that resolves to any value   when populating the list model has completed or rejects with error.
         */
        ["await"](): Dex.Future;

        dup_query(): DocumentationQuery;

        /**
         * @returns an {@link Gio.ListModel} of {@link Foundry.Documentation}
         */
        list_sections(): Gio.ListModel;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace DocumentationProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for plugins to provide documentation.
     * @gir-type Class
     */
    abstract class DocumentationProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<DocumentationProvider>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DocumentationProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DocumentationProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof DocumentationProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DocumentationProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DocumentationProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DocumentationProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param uri 
         * @virtual
         */
        vfunc_find_by_uri(uri: string): Dex.Future;

        /**
         * This method is called when the documentation provider should rescan
         * the provided roots for changes.
         * @param roots a {@link Gio.ListModel} of {@link Foundry.DocumentationRoot}
         * @virtual
         */
        vfunc_index(roots: Gio.ListModel): Dex.Future;

        /**
         * @virtual
         */
        vfunc_list_bundles(): Dex.Future;

        /**
         * @param parent 
         * @virtual
         */
        vfunc_list_children(parent: Documentation): Dex.Future;

        /**
         * Returns a list of {@link Foundry.DocumentationRoot} that may contain
         * documentation to be discovered and ingested. This allows plugins for
         * SDKs to provide information about where documentation is located.
         * 
         * It is expected that this list model will be updated when there are
         * changes to the underlying file-system which will require re-parsing
         * content for updates.
         * @virtual
         */
        vfunc_list_roots(): Gio.ListModel;

        /**
         * Providers are expected to add their search sections to `matches`
         * using {@link Foundry.DocumentationMatches.add_section}.
         * @param query a {@link Foundry.DocumentationQuery}
         * @param matches a {@link Foundry.DocumentationMatches}
         * @virtual
         */
        vfunc_query(query: DocumentationQuery, matches: DocumentationMatches): Dex.Future;

        // Methods
        /**
         * Gets the plugin the provider belongs to.
         * @returns a {@link Peas.PluginInfo} or `null`
         */
        dup_plugin_info(): Peas.PluginInfo;

        /**
         * @param uri 
         * @returns a {@link Dex.Future} that will resolve to   a {@link Foundry.Documentation} or reject with error.
         */
        find_by_uri(uri: string): Dex.Future;

        /**
         * This method is called when the documentation provider should rescan
         * the provided roots for changes.
         * @param roots a {@link Gio.ListModel} of {@link Foundry.DocumentationRoot}
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        index(roots: Gio.ListModel): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that will resolve to   a {@link Gio.ListModel} of {@link Foundry.DocumentationBundle} or   reject with error.
         */
        list_bundles(): Dex.Future;

        /**
         * @param parent 
         * @returns a {@link Dex.Future} that will resolve to   a {@link Gio.ListModel} or reject with error.
         */
        list_children(parent: Documentation): Dex.Future;

        /**
         * Returns a list of {@link Foundry.DocumentationRoot} that may contain
         * documentation to be discovered and ingested. This allows plugins for
         * SDKs to provide information about where documentation is located.
         * 
         * It is expected that this list model will be updated when there are
         * changes to the underlying file-system which will require re-parsing
         * content for updates.
         * @returns a {@link Gio.ListModel} of   {@link Foundry.DocumentationRoot} containing information about   discovering documentation.
         */
        list_roots(): Gio.ListModel;

        /**
         * Providers are expected to add their search sections to `matches`
         * using {@link Foundry.DocumentationMatches.add_section}.
         * @param query a {@link Foundry.DocumentationQuery}
         * @param matches a {@link Foundry.DocumentationMatches}
         * @returns a {@link Dex.Future} that resolves to a   any value or rejects with error.
         */
        query(query: DocumentationQuery, matches: DocumentationMatches): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace DocumentationQuery {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::function-name": (pspec: GObject.ParamSpec) => void;
            "notify::keyword": (pspec: GObject.ParamSpec) => void;
            "notify::prefetch-all": (pspec: GObject.ParamSpec) => void;
            "notify::property-name": (pspec: GObject.ParamSpec) => void;
            "notify::type-name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            function_name: string;
            functionName: string;
            keyword: string;
            prefetch_all: boolean;
            prefetchAll: boolean;
            property_name: string;
            propertyName: string;
            type_name: string;
            typeName: string;
        }
    }

    /**
     * Represents a query into the documentation engine.
     * @gir-type Class
     */
    class DocumentationQuery extends GObject.Object {
        static $gtype: GObject.GType<DocumentationQuery>;

        // Properties
        /**
         * @default null
         */
        get function_name(): string;
        set function_name(val: string);

        /**
         * @default null
         */
        get functionName(): string;
        set functionName(val: string);

        /**
         * @default null
         */
        get keyword(): string;
        set keyword(val: string);

        /**
         * @default false
         */
        get prefetch_all(): boolean;
        set prefetch_all(val: boolean);

        /**
         * @default false
         */
        get prefetchAll(): boolean;
        set prefetchAll(val: boolean);

        /**
         * @default null
         */
        get property_name(): string;
        set property_name(val: string);

        /**
         * @default null
         */
        get propertyName(): string;
        set propertyName(val: string);

        /**
         * @default null
         */
        get type_name(): string;
        set type_name(val: string);

        /**
         * @default null
         */
        get typeName(): string;
        set typeName(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DocumentationQuery.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DocumentationQuery.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): DocumentationQuery;

        // Signals
        /** @signal */
        connect<K extends keyof DocumentationQuery.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationQuery.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DocumentationQuery.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationQuery.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DocumentationQuery.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DocumentationQuery.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_function_name(): string;

        dup_keyword(): string;

        dup_property_name(): string;

        dup_type_name(): string;

        get_prefetch_all(): boolean;

        /**
         * @param function_name 
         */
        set_function_name(function_name: string): void;

        /**
         * @param keyword 
         */
        set_keyword(keyword: string): void;

        /**
         * @param prefetch_all 
         */
        set_prefetch_all(prefetch_all: boolean): void;

        /**
         * @param property_name 
         */
        set_property_name(property_name: string): void;

        /**
         * @param type_name 
         */
        set_type_name(type_name: string): void;
    }


    namespace DocumentationRoot {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::directories": (pspec: GObject.ParamSpec) => void;
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::identifier": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::version": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            directories: Gio.ListModel;
            icon: Gio.Icon;
            identifier: string;
            title: string;
            version: string;
        }
    }

    /**
     * An area where documentation may be ingested from.
     * 
     * Plugins may use this information to scan for documentation that matches
     * their particular format.
     * @gir-type Class
     */
    class DocumentationRoot extends GObject.Object {
        static $gtype: GObject.GType<DocumentationRoot>;

        // Properties
        /**
         * @construct-only
         */
        get directories(): Gio.ListModel;

        /**
         * @construct-only
         */
        get icon(): Gio.Icon;

        /**
         * @construct-only
         * @default null
         */
        get identifier(): string;

        /**
         * @construct-only
         * @default null
         */
        get title(): string;

        /**
         * @construct-only
         * @default null
         */
        get version(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DocumentationRoot.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<DocumentationRoot.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](identifier: string, title: string, version: string, icon: Gio.Icon, directories: Gio.ListModel): DocumentationRoot;

        // Signals
        /** @signal */
        connect<K extends keyof DocumentationRoot.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationRoot.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DocumentationRoot.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DocumentationRoot.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DocumentationRoot.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DocumentationRoot.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_icon(): (Gio.Icon | null);

        dup_identifier(): string;

        dup_title(): string;

        dup_version(): string;

        /**
         * @returns a {@link Gio.ListModel} of   {@link Gio.File}.
         */
        list_directories(): Gio.ListModel;
    }


    namespace Extension {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::engine": (pspec: GObject.ParamSpec) => void;
            "notify::extension": (pspec: GObject.ParamSpec) => void;
            "notify::interface-type": (pspec: GObject.ParamSpec) => void;
            "notify::key": (pspec: GObject.ParamSpec) => void;
            "notify::value": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            engine: Peas.Engine;
            extension: GObject.Object;
            interface_type: GObject.GTypeInput;
            interfaceType: GObject.GTypeInput;
            key: string;
            value: string;
        }
    }

    /**
     * Represents a single plugin extension with FoundryContext support.
     * 
     * Typically Foundry uses libpeas directly. But there are cases where filtering
     * or ordering plugins by priority is needed. FoundryExtension provides this
     * capability in a similar manner to PeasExtension.
     * @gir-type Class
     */
    class Extension extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Extension>;

        // Properties
        /**
         * @construct-only
         */
        get engine(): Peas.Engine;

        /**
         * @read-only
         */
        get extension(): GObject.Object;

        /**
         * @construct-only
         */
        get interface_type(): GObject.GType;

        /**
         * @construct-only
         */
        get interfaceType(): GObject.GType;

        /**
         * @default null
         */
        get key(): string;
        set key(val: string);

        /**
         * @default null
         */
        get value(): string;
        set value(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Extension.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Extension.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: (Context | null), engine: (Peas.Engine | null), interface_type: GObject.GType, key: string, value: (string | null)): Extension;

        // Signals
        /** @signal */
        connect<K extends keyof Extension.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Extension.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Extension.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Extension.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Extension.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Extension.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the {@link Foundry.Extension.engine} property.
         * @returns a {@link Peas.Engine}.
         */
        get_engine(): Peas.Engine;

        /**
         * Gets the extension object managed by the adapter.
         * @returns a {@link GObject.Object} or `null`.
         */
        get_extension<T = GObject.Object>(): T;

        get_interface_type(): GObject.GType;

        get_key(): string;

        get_value(): string;

        /**
         * @param key 
         */
        set_key(key: string): void;

        /**
         * @param value 
         */
        set_value(value: string): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace ExtensionSet {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            /**
             * @signal
             * @run-last
             */
            "extension-added": (arg0: Peas.PluginInfo, arg1: GObject.Object) => void;
            /**
             * @signal
             * @run-last
             */
            "extension-removed": (arg0: Peas.PluginInfo, arg1: GObject.Object) => void;
            /**
             * @signal
             * @run-last
             */
            "extensions-loaded": () => void;
            "notify::engine": (pspec: GObject.ParamSpec) => void;
            "notify::interface-type": (pspec: GObject.ParamSpec) => void;
            "notify::key": (pspec: GObject.ParamSpec) => void;
            "notify::value": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            engine: Peas.Engine;
            interface_type: GObject.GTypeInput;
            interfaceType: GObject.GTypeInput;
            key: string;
            value: string;
        }
    }

    /**
     * Manages a collection of extensions and provides unified access to their functionality.
     * 
     * FoundryExtensionSet provides a model for managing extensions that implement
     * a common interface. It handles extension loading, lifecycle management, and
     * provides efficient access to extension functionality through a unified API.
     * It integrates with the plugin system and supports dynamic extension management.
     * @gir-type Class
     */
    class ExtensionSet<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<ExtensionSet>;

        // Properties
        /**
         * @construct-only
         */
        get engine(): Peas.Engine;

        /**
         * @construct-only
         */
        get interface_type(): GObject.GType;

        /**
         * @construct-only
         */
        get interfaceType(): GObject.GType;

        /**
         * @default null
         */
        get key(): string;
        set key(val: string);

        /**
         * @default null
         */
        get value(): string;
        set value(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ExtensionSet.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ExtensionSet.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ExtensionSet.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ExtensionSet.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ExtensionSet.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ExtensionSet.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ExtensionSet.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ExtensionSet.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Calls `foreach_func` for every extension loaded by the extension set.
         * @param foreach_func A callback
         */
        foreach(foreach_func: ExtensionSetForeachFunc): void;

        /**
         * Calls `foreach_func` for every extension loaded by the extension set.
         * @param foreach_func A callback
         */
        foreach_by_priority(foreach_func: ExtensionSetForeachFunc): void;

        /**
         * Gets the {@link Foundry.ExtensionSet.engine} property.
         * @returns a {@link Peas.Engine}.
         */
        get_engine(): Peas.Engine;

        /**
         * Locates the extension owned by `plugin_info` if such extension exists.
         * @param plugin_info a {@link Peas.PluginInfo}
         * @returns a {@link GObject.Object} or `null`
         */
        get_extension<T = GObject.Object>(plugin_info: Peas.PluginInfo): T;

        get_interface_type(): GObject.GType;

        get_key(): string;

        get_n_extensions(): number;

        get_value(): string;

        /**
         * @param key 
         */
        set_key(key: string): void;

        /**
         * @param value 
         */
        set_value(value: string): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FileManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages file operations and provides file system abstraction.
     * 
     * FoundryFileManager handles file creation, deletion, monitoring, and provides
     * a unified interface for working with files across different backends. It also
     * manages file attributes, search functionality, and integrates with the VCS system
     * for version control operations.
     * @gir-type Class
     */
    class FileManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<FileManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FileManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FileManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FileManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FileManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FileManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FileManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Either `content_type` or `filename` should be provided, or both.
         * 
         * This function is similar to `g_content_type_get_symbolic_icon()` except that
         * it takes our bundled icons into account to ensure that they are taken at a
         * higher priority than the fallbacks from the current icon theme.
         * @param content_type optional content-type to lookup
         * @param filename optional filename
         * @returns A {@link Gio.Icon} or `null`
         */
        find_symbolic_icon(content_type: (string | null), filename: (string | null)): (Gio.Icon | null);

        /**
         * Attempts to guess the language of `file`, `content_type`, or `contents`.
         * 
         * One of `file`, content_type, or `contents` must be set.
         * @param file a {@link Gio.File} or `null`
         * @param content_type the content-type as a string or `null`
         * @param contents a {@link GLib.Bytes} of file contents or `null`
         * @returns a {@link Dex.Future} that resolves to a string   containing the language identifier, or rejects with error.
         */
        guess_language(file: (Gio.File | null), content_type: (string | null), contents: (GLib.Bytes | null)): Dex.Future;

        list_languages(): string[];

        /**
         * Reads the metadata associated with a file.
         * @param file a {@link Gio.File}
         * @param attributes a string containing the `metadata::` attributes to query   separated by a comma ","
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.FileInfo}.
         */
        read_metadata(file: Gio.File, attributes: string): Dex.Future;

        /**
         * This function will complete before the operation has fully completed
         * searching all targets. You may wait for all search results to be
         * populated by using {@link Foundry.list_model_await}.
         * @param options the options for the search
         * @param operation the operation tracker
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.FileSearchMatch}   or rejects with error.
         */
        search(options: FileSearchOptions, operation: Operation): Dex.Future;

        /**
         * Requests that `file` is shown in the users default file-manager.
         * 
         * For example, on GNOME this would open Nautilus.
         * @param file 
         * @returns a {@link Dex.Future} that resolves   to `true` if successful
         */
        show(file: Gio.File): Dex.Future;

        /**
         * `file_info` must only contain attributes starting with 'metadata::'
         * @param file a {@link Gio.File}
         * @param file_info a {@link Gio.FileInfo}
         * @returns a {@link Dex.Future} that resolves to a   boolean or rejects with error.
         */
        write_metadata(file: Gio.File, file_info: Gio.FileInfo): Dex.Future;
    }


    namespace FileMonitor {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            file: Gio.File;
        }
    }

    /**
     * A file monitor which integrates well with DexFuture.
     * @gir-type Class
     */
    class FileMonitor extends GObject.Object {
        static $gtype: GObject.GType<FileMonitor>;

        // Properties
        /**
         * @construct-only
         */
        get file(): Gio.File;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FileMonitor.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FileMonitor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](file: Gio.File): FileMonitor;

        // Signals
        /** @signal */
        connect<K extends keyof FileMonitor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileMonitor.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FileMonitor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileMonitor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FileMonitor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FileMonitor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        cancel(): void;

        dup_file(): Gio.File;

        next(): Dex.Future;
    }


    namespace FileMonitorEvent {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::event": (pspec: GObject.ParamSpec) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::other-file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            event: Gio.FileMonitorEvent;
            file: Gio.File;
            other_file: Gio.File;
            otherFile: Gio.File;
        }
    }

    /**
     * Represents an event that has occurred from a file monitor.
     * @gir-type Class
     */
    class FileMonitorEvent extends GObject.Object {
        static $gtype: GObject.GType<FileMonitorEvent>;

        // Properties
        /**
         * @construct-only
         * @default Gio.FileMonitorEvent.CHANGED
         */
        get event(): Gio.FileMonitorEvent;

        /**
         * @construct-only
         */
        get file(): Gio.File;

        /**
         * @construct-only
         */
        get other_file(): Gio.File;

        /**
         * @construct-only
         */
        get otherFile(): Gio.File;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FileMonitorEvent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FileMonitorEvent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](file: Gio.File, other_file: Gio.File, event: Gio.FileMonitorEvent): FileMonitorEvent;

        // Signals
        /** @signal */
        connect<K extends keyof FileMonitorEvent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileMonitorEvent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FileMonitorEvent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileMonitorEvent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FileMonitorEvent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FileMonitorEvent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_file(): Gio.File;

        dup_other_file(): (Gio.File | null);

        get_event(): Gio.FileMonitorEvent;
    }


    namespace FileSearchMatch {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::after-context": (pspec: GObject.ParamSpec) => void;
            "notify::before-context": (pspec: GObject.ParamSpec) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::length": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::line-offset": (pspec: GObject.ParamSpec) => void;
            "notify::text": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            after_context: string;
            afterContext: string;
            before_context: string;
            beforeContext: string;
            file: Gio.File;
            icon: Gio.Icon;
            length: number;
            line: number;
            line_offset: number;
            lineOffset: number;
            text: string;
            uri: string;
        }
    }

    /**
     * A file search match result.
     * 
     * {@link Foundry.FileSearchMatch} represents a search match result containing
     * information about where a search term was found in a file, including the
     * file location, line number, character offset, and text length.
     * @gir-type Class
     * @since 1.1
     */
    abstract class FileSearchMatch extends GObject.Object {
        static $gtype: GObject.GType<FileSearchMatch>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get after_context(): string;

        /**
         * @read-only
         * @default null
         */
        get afterContext(): string;

        /**
         * @read-only
         * @default null
         */
        get before_context(): string;

        /**
         * @read-only
         * @default null
         */
        get beforeContext(): string;

        /**
         * @read-only
         */
        get file(): Gio.File;

        /**
         * @read-only
         */
        get icon(): Gio.Icon;

        /**
         * @read-only
         * @default 0
         */
        get length(): number;

        /**
         * @read-only
         * @default 0
         */
        get line(): number;

        /**
         * @read-only
         * @default 0
         */
        get line_offset(): number;

        /**
         * @read-only
         * @default 0
         */
        get lineOffset(): number;

        /**
         * @read-only
         * @default null
         */
        get text(): string;

        /**
         * @read-only
         * @default null
         */
        get uri(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FileSearchMatch.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FileSearchMatch.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FileSearchMatch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileSearchMatch.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FileSearchMatch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileSearchMatch.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FileSearchMatch.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FileSearchMatch.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets the text after the search text.
         * @virtual
         */
        vfunc_dup_after_context(): (string | null);

        /**
         * Gets the text before the search text.
         * @virtual
         */
        vfunc_dup_before_context(): (string | null);

        /**
         * Gets a copy of the file associated with the search match.
         * @virtual
         */
        vfunc_dup_file(): Gio.File;

        /**
         * Gets the matched text.
         * @virtual
         */
        vfunc_dup_text(): (string | null);

        /**
         * Gets the length of the search text in characters.
         * @virtual
         */
        vfunc_get_length(): number;

        /**
         * Gets the line number where the search match was found.
         * @virtual
         */
        vfunc_get_line(): number;

        /**
         * Gets the character offset within the line where the search match starts.
         * @virtual
         */
        vfunc_get_line_offset(): number;

        // Methods
        /**
         * Gets the text after the search text.
         * @returns the text after the match, or `null`
         */
        dup_after_context(): (string | null);

        /**
         * Gets the text before the search text.
         * @returns the text before the match, or `null`
         */
        dup_before_context(): (string | null);

        /**
         * Gets a copy of the file associated with the search match.
         * @returns a {@link Gio.File}
         */
        dup_file(): Gio.File;

        /**
         * Gets the icon for the file associated with the search match.
         * @returns a {@link Gio.Icon} or `null`
         */
        dup_icon(): (Gio.Icon | null);

        /**
         * Gets the matched text.
         * @returns the matched text, or `null`
         */
        dup_text(): (string | null);

        /**
         * Gets the length of the search text in characters.
         * @returns the length of the text in characters
         */
        get_length(): number;

        /**
         * Gets the line number where the search match was found.
         * @returns the line number (0-based)
         */
        get_line(): number;

        /**
         * Gets the character offset within the line where the search match starts.
         * @returns the character offset within the line (0-based)
         */
        get_line_offset(): number;
    }


    namespace FileSearchOptions {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::case-sensitive": (pspec: GObject.ParamSpec) => void;
            "notify::context-lines": (pspec: GObject.ParamSpec) => void;
            "notify::excluded-patterns": (pspec: GObject.ParamSpec) => void;
            "notify::match-whole-words": (pspec: GObject.ParamSpec) => void;
            "notify::max-matches": (pspec: GObject.ParamSpec) => void;
            "notify::recursive": (pspec: GObject.ParamSpec) => void;
            "notify::required-patterns": (pspec: GObject.ParamSpec) => void;
            "notify::search-text": (pspec: GObject.ParamSpec) => void;
            "notify::targets": (pspec: GObject.ParamSpec) => void;
            "notify::use-regex": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            case_sensitive: boolean;
            caseSensitive: boolean;
            context_lines: number;
            contextLines: number;
            excluded_patterns: string[];
            excludedPatterns: string[];
            match_whole_words: boolean;
            matchWholeWords: boolean;
            max_matches: number;
            maxMatches: number;
            recursive: boolean;
            required_patterns: string[];
            requiredPatterns: string[];
            search_text: string;
            searchText: string;
            targets: Gio.ListModel;
            use_regex: boolean;
            useRegex: boolean;
        }
    }

    /**
     * Configuration options for file search operations.
     * 
     * This class provides configuration options for searching text within files.
     * It supports various search modes including case sensitivity, whole word
     * matching, regular expressions, and pattern-based file filtering. You can
     * specify search targets, set maximum match limits, and configure context
     * lines around matches.
     * @gir-type Class
     * @since 1.1
     */
    class FileSearchOptions extends GObject.Object {
        static $gtype: GObject.GType<FileSearchOptions>;

        // Properties
        /**
         * @default true
         */
        get case_sensitive(): boolean;
        set case_sensitive(val: boolean);

        /**
         * @default true
         */
        get caseSensitive(): boolean;
        set caseSensitive(val: boolean);

        /**
         * @default 1
         */
        get context_lines(): number;
        set context_lines(val: number);

        /**
         * @default 1
         */
        get contextLines(): number;
        set contextLines(val: number);

        get excluded_patterns(): string[];
        set excluded_patterns(val: string[]);

        get excludedPatterns(): string[];
        set excludedPatterns(val: string[]);

        /**
         * @default false
         */
        get match_whole_words(): boolean;
        set match_whole_words(val: boolean);

        /**
         * @default false
         */
        get matchWholeWords(): boolean;
        set matchWholeWords(val: boolean);

        /**
         * @default 10000
         */
        get max_matches(): number;
        set max_matches(val: number);

        /**
         * @default 10000
         */
        get maxMatches(): number;
        set maxMatches(val: number);

        /**
         * @default false
         */
        get recursive(): boolean;
        set recursive(val: boolean);

        get required_patterns(): string[];
        set required_patterns(val: string[]);

        get requiredPatterns(): string[];
        set requiredPatterns(val: string[]);

        /**
         * @default null
         */
        get search_text(): string;
        set search_text(val: string);

        /**
         * @default null
         */
        get searchText(): string;
        set searchText(val: string);

        /**
         * @read-only
         */
        get targets(): Gio.ListModel;

        /**
         * @default false
         */
        get use_regex(): boolean;
        set use_regex(val: boolean);

        /**
         * @default false
         */
        get useRegex(): boolean;
        set useRegex(val: boolean);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FileSearchOptions.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FileSearchOptions.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): FileSearchOptions;

        // Signals
        /** @signal */
        connect<K extends keyof FileSearchOptions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileSearchOptions.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FileSearchOptions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileSearchOptions.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FileSearchOptions.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FileSearchOptions.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Adds a file as a target for searching.
         * @param file a {@link Gio.File} to add as a search target
         */
        add_target(file: Gio.File): void;

        /**
         * Creates a new {@link Foundry.FileSearchOptions} with a copy of all the values
         * from the original.
         * @returns a new {@link Foundry.FileSearchOptions}
         */
        copy(): FileSearchOptions;

        /**
         * Gets a copy of the excluded file patterns.
         * 
         * These patterns define which files should be excluded from the search.
         * Files that match any of these patterns will be skipped during the search.
         * The patterns use shell-style globbing (e.g. "*.o").
         * @returns a newly allocated array of excluded file   patterns, or `null` if not set. Free with `g_strfreev()` when no longer needed.
         */
        dup_excluded_patterns(): (string[] | null);

        /**
         * Gets a copy of the required file patterns.
         * 
         * These patterns define which files must match to be included in the search.
         * Files that do not match any of these patterns will be excluded from the search.
         * The patterns use shell-style globbing (e.g. "*.c", "*.h").
         * @returns a newly allocated array of required file   patterns, or `null` if not set. Free with `g_strfreev()` when no longer needed.
         */
        dup_required_patterns(): (string[] | null);

        /**
         * Gets a copy of the search text.
         * @returns a newly allocated copy of the search text,   or `null` if not set
         */
        dup_search_text(): (string | null);

        /**
         * Gets whether the search should be case sensitive.
         * @returns `true` if the search should be case sensitive
         */
        get_case_sensitive(): boolean;

        /**
         * Gets the number of context lines to include with each match.
         * A value of 1 means 1 line before and 1 line after the match.
         * @returns the number of context lines
         */
        get_context_lines(): number;

        /**
         * Gets whether the search should match whole words only.
         * @returns `true` if the search should match whole words only
         */
        get_match_whole_words(): boolean;

        /**
         * Gets the maximum number of matches to return.
         * @returns the maximum number of matches
         */
        get_max_matches(): number;

        /**
         * Gets whether the search should be recursive.
         * @returns `true` if the search should be recursive
         */
        get_recursive(): boolean;

        /**
         * Gets whether the search should use regular expressions.
         * @returns `true` if the search should use regular expressions
         */
        get_use_regex(): boolean;

        /**
         * Gets the list of files that are targets for searching.
         * @returns a {@link Gio.ListModel} of   {@link Gio.File} that are targets for searching.
         */
        list_targets(): Gio.ListModel;

        /**
         * Removes a file from the search targets.
         * @param file a {@link Gio.File} to remove from search targets
         */
        remove_target(file: Gio.File): void;

        /**
         * Sets whether the search should be case sensitive.
         * @param case_sensitive whether the search should be case sensitive
         */
        set_case_sensitive(case_sensitive: boolean): void;

        /**
         * Sets the number of context lines to include with each match.
         * A value of 1 means 1 line before and 1 line after the match.
         * @param context_lines the number of context lines to include with each match
         */
        set_context_lines(context_lines: number): void;

        /**
         * Sets the excluded file patterns.
         * 
         * These patterns define which files should be excluded from the search.
         * Files that match any of these patterns will be skipped during the search.
         * The patterns use shell-style globbing (e.g. "*.o").
         * @param excluded_patterns array of excluded file patterns
         */
        set_excluded_patterns(excluded_patterns: (string | null)): void;

        /**
         * Sets whether the search should match whole words only.
         * @param match_whole_words whether the search should match whole words only
         */
        set_match_whole_words(match_whole_words: boolean): void;

        /**
         * Sets the maximum number of matches to return.
         * @param max_matches the maximum number of matches to return
         */
        set_max_matches(max_matches: number): void;

        /**
         * Sets whether the search should be recursive.
         * @param recursive whether the search should be recursive
         */
        set_recursive(recursive: boolean): void;

        /**
         * Sets the required file patterns.
         * 
         * These patterns define which files must match to be included in the search.
         * Files that do not match any of these patterns will be excluded from the search.
         * The patterns use shell-style globbing (e.g. "*.c", "*.h").
         * @param required_patterns array of required file patterns
         */
        set_required_patterns(required_patterns: (string | null)): void;

        /**
         * Sets the search text.
         * @param search_text the text to search for
         */
        set_search_text(search_text: (string | null)): void;

        /**
         * Sets whether the search should use regular expressions.
         * @param use_regex whether the search should use regular expressions
         */
        set_use_regex(use_regex: boolean): void;
    }


    namespace FileSearchProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * A pluggable file search provider.
     * 
     * The {@link Foundry.FileSearchProvider} class allows precise implementation over how
     * file searches are performed. Plugins can provide an alternate implementation
     * to the default `grep` based search provider by implementing this class at
     * a higher priority than the `grep` provider.
     * @gir-type Class
     * @since 1.1
     */
    abstract class FileSearchProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<FileSearchProvider>;

        // Properties
        /**
         * @since 1.1
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @since 1.1
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FileSearchProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FileSearchProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FileSearchProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileSearchProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FileSearchProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileSearchProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FileSearchProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FileSearchProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Performs the requested search.
         * 
         * It is expected that the implementation returns a {@link Gio.ListModel} as
         * early as convenient and asynchronously populate the results after that.
         * 
         * Callers should use {@link Foundry.list_model_await} to wait for completion
         * of the entire result set.
         * 
         * If the provider is missing dependencies (such as a missing search tool in
         * the users path) this method should reject with `G_IO_ERROR_NOT_SUPPORTED`
         * so that the next implementation may be used.
         * @param options a {@link Foundry.FileSearchOptions}
         * @param operation a {@link Foundry.Operation}
         * @virtual
         */
        vfunc_search(options: FileSearchOptions, operation: Operation): Dex.Future;

        // Methods
        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * Performs the requested search.
         * 
         * It is expected that the implementation returns a {@link Gio.ListModel} as
         * early as convenient and asynchronously populate the results after that.
         * 
         * Callers should use {@link Foundry.list_model_await} to wait for completion
         * of the entire result set.
         * 
         * If the provider is missing dependencies (such as a missing search tool in
         * the users path) this method should reject with `G_IO_ERROR_NOT_SUPPORTED`
         * so that the next implementation may be used.
         * @param options a {@link Foundry.FileSearchOptions}
         * @param operation a {@link Foundry.Operation}
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.FileSearchMatch} or    rejects with error.
         */
        search(options: FileSearchOptions, operation: Operation): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FileSearchReplacement {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Perform text replacements across search matches.
     * 
     * This class takes a list of {@link Foundry.FileSearchMatch} objects and
     * applies text replacements to each match location. It supports both literal
     * text replacement and regex-based replacement with back references.
     * 
     * The replacement operation is performed asynchronously using fibers, and all
     * edits are applied through the {@link Foundry.TextManager} to ensure proper
     * undo/redo support. The files are saved after applying changes.
     * @gir-type Class
     * @since 1.1
     */
    class FileSearchReplacement extends GObject.Object {
        static $gtype: GObject.GType<FileSearchReplacement>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FileSearchReplacement.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FileSearchReplacement.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, matches: Gio.ListModel, options: FileSearchOptions, replacement_text: string): FileSearchReplacement;

        // Signals
        /** @signal */
        connect<K extends keyof FileSearchReplacement.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileSearchReplacement.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FileSearchReplacement.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FileSearchReplacement.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FileSearchReplacement.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FileSearchReplacement.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Applies the text replacements to all matched locations.
         * 
         * This method starts a fiber and performs replacements asynchronously.
         * Each replacement is performed using {@link Foundry.TextEdit} and applied
         * using {@link Foundry.TextManager}.
         * @returns a {@link Dex.Future} that completes when   all replacements are finished
         */
        apply(): Dex.Future;
    }


    namespace FlatpakArchOptions {
        // Signal signatures
        interface SignalSignatures extends FlatpakSerializable.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSerializable.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakArchOptions extends FlatpakSerializable implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakArchOptions>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakArchOptions.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakArchOptions.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakArchOptions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakArchOptions.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakArchOptions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakArchOptions.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakArchOptions.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakArchOptions.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param arch 
         */
        dup_arch(arch: string): (FlatpakOptions | null);

        dup_arches(): (string[] | null);

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FlatpakExtension {
        // Signal signatures
        interface SignalSignatures extends FlatpakSerializable.SignalSignatures {
            "notify::add-ld-path": (pspec: GObject.ParamSpec) => void;
            "notify::autodelete": (pspec: GObject.ParamSpec) => void;
            "notify::autoprune-unless": (pspec: GObject.ParamSpec) => void;
            "notify::bundle": (pspec: GObject.ParamSpec) => void;
            "notify::directory": (pspec: GObject.ParamSpec) => void;
            "notify::download-if": (pspec: GObject.ParamSpec) => void;
            "notify::enable-if": (pspec: GObject.ParamSpec) => void;
            "notify::locale-subset": (pspec: GObject.ParamSpec) => void;
            "notify::merge-dirs": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::no-autodownload": (pspec: GObject.ParamSpec) => void;
            "notify::remove-after-build": (pspec: GObject.ParamSpec) => void;
            "notify::subdirectories": (pspec: GObject.ParamSpec) => void;
            "notify::subdirectory-suffix": (pspec: GObject.ParamSpec) => void;
            "notify::version": (pspec: GObject.ParamSpec) => void;
            "notify::versions": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSerializable.ConstructorProps, Json.Serializable.ConstructorProps {
            add_ld_path: string;
            addLdPath: string;
            autodelete: boolean;
            autoprune_unless: string;
            autopruneUnless: string;
            bundle: boolean;
            directory: string;
            download_if: string;
            downloadIf: string;
            enable_if: string;
            enableIf: string;
            locale_subset: boolean;
            localeSubset: boolean;
            merge_dirs: string;
            mergeDirs: string;
            name: string;
            no_autodownload: boolean;
            noAutodownload: boolean;
            remove_after_build: boolean;
            removeAfterBuild: boolean;
            subdirectories: boolean;
            subdirectory_suffix: string;
            subdirectorySuffix: string;
            version: string;
            versions: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakExtension extends FlatpakSerializable implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakExtension>;

        // Properties
        /**
         * @default null
         */
        get add_ld_path(): string;
        set add_ld_path(val: string);

        /**
         * @default null
         */
        get addLdPath(): string;
        set addLdPath(val: string);

        /**
         * @default false
         */
        get autodelete(): boolean;
        set autodelete(val: boolean);

        /**
         * @default null
         */
        get autoprune_unless(): string;
        set autoprune_unless(val: string);

        /**
         * @default null
         */
        get autopruneUnless(): string;
        set autopruneUnless(val: string);

        /**
         * @default false
         */
        get bundle(): boolean;
        set bundle(val: boolean);

        /**
         * @default null
         */
        get directory(): string;
        set directory(val: string);

        /**
         * @default null
         */
        get download_if(): string;
        set download_if(val: string);

        /**
         * @default null
         */
        get downloadIf(): string;
        set downloadIf(val: string);

        /**
         * @default null
         */
        get enable_if(): string;
        set enable_if(val: string);

        /**
         * @default null
         */
        get enableIf(): string;
        set enableIf(val: string);

        /**
         * @default false
         */
        get locale_subset(): boolean;
        set locale_subset(val: boolean);

        /**
         * @default false
         */
        get localeSubset(): boolean;
        set localeSubset(val: boolean);

        /**
         * @default null
         */
        get merge_dirs(): string;
        set merge_dirs(val: string);

        /**
         * @default null
         */
        get mergeDirs(): string;
        set mergeDirs(val: string);

        /**
         * @default null
         */
        get name(): string;
        set name(val: string);

        /**
         * @default false
         */
        get no_autodownload(): boolean;
        set no_autodownload(val: boolean);

        /**
         * @default false
         */
        get noAutodownload(): boolean;
        set noAutodownload(val: boolean);

        /**
         * @default false
         */
        get remove_after_build(): boolean;
        set remove_after_build(val: boolean);

        /**
         * @default false
         */
        get removeAfterBuild(): boolean;
        set removeAfterBuild(val: boolean);

        /**
         * @default false
         */
        get subdirectories(): boolean;
        set subdirectories(val: boolean);

        /**
         * @default null
         */
        get subdirectory_suffix(): string;
        set subdirectory_suffix(val: string);

        /**
         * @default null
         */
        get subdirectorySuffix(): string;
        set subdirectorySuffix(val: string);

        /**
         * @default null
         */
        get version(): string;
        set version(val: string);

        /**
         * @default null
         */
        get versions(): string;
        set versions(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakExtension.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakExtension.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakExtension.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakExtension.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakExtension.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakExtension.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakExtension.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakExtension.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FlatpakExtensions {
        // Signal signatures
        interface SignalSignatures extends FlatpakList.SignalSignatures {
            "notify::item-type": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends FlatpakList.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakExtensions<A extends GObject.Object = GObject.Object> extends FlatpakList implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<FlatpakExtensions>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakExtensions.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakExtensions.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakExtensions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakExtensions.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakExtensions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakExtensions.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakExtensions.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakExtensions.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace FlatpakList {
        // Signal signatures
        interface SignalSignatures extends FlatpakSerializable.SignalSignatures {
            "notify::item-type": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends FlatpakSerializable.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            item_type: GObject.GTypeInput;
            itemType: GObject.GTypeInput;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class FlatpakList<A extends GObject.Object = GObject.Object> extends FlatpakSerializable implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<FlatpakList>;

        // Properties
        /**
         * @read-only
         */
        get item_type(): GObject.GType;

        /**
         * @read-only
         */
        get itemType(): GObject.GType;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakList.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakList.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakList.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakList.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakList.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakList.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakList.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakList.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param instance 
         */
        add(instance: null): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FlatpakManifest {
        // Signal signatures
        interface SignalSignatures extends FlatpakSerializable.SignalSignatures {
            "notify::add-build-extensions": (pspec: GObject.ParamSpec) => void;
            "notify::add-extensions": (pspec: GObject.ParamSpec) => void;
            "notify::app-id": (pspec: GObject.ParamSpec) => void;
            "notify::appdata-license": (pspec: GObject.ParamSpec) => void;
            "notify::appstream-compose": (pspec: GObject.ParamSpec) => void;
            "notify::base": (pspec: GObject.ParamSpec) => void;
            "notify::base-commit": (pspec: GObject.ParamSpec) => void;
            "notify::base-extensions": (pspec: GObject.ParamSpec) => void;
            "notify::base-version": (pspec: GObject.ParamSpec) => void;
            "notify::branch": (pspec: GObject.ParamSpec) => void;
            "notify::build-extension": (pspec: GObject.ParamSpec) => void;
            "notify::build-options": (pspec: GObject.ParamSpec) => void;
            "notify::build-runtime": (pspec: GObject.ParamSpec) => void;
            "notify::cleanup": (pspec: GObject.ParamSpec) => void;
            "notify::cleanup-commands": (pspec: GObject.ParamSpec) => void;
            "notify::cleanup-platform": (pspec: GObject.ParamSpec) => void;
            "notify::cleanup-platform-commands": (pspec: GObject.ParamSpec) => void;
            "notify::collection-id": (pspec: GObject.ParamSpec) => void;
            "notify::command": (pspec: GObject.ParamSpec) => void;
            "notify::copy-icon": (pspec: GObject.ParamSpec) => void;
            "notify::default-branch": (pspec: GObject.ParamSpec) => void;
            "notify::desktop-file-name-prefix": (pspec: GObject.ParamSpec) => void;
            "notify::desktop-file-name-suffix": (pspec: GObject.ParamSpec) => void;
            "notify::extension-tag": (pspec: GObject.ParamSpec) => void;
            "notify::finish-args": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::id-platform": (pspec: GObject.ParamSpec) => void;
            "notify::inherit-extensions": (pspec: GObject.ParamSpec) => void;
            "notify::inherit-sdk-extensions": (pspec: GObject.ParamSpec) => void;
            "notify::metadata": (pspec: GObject.ParamSpec) => void;
            "notify::metadata-platform": (pspec: GObject.ParamSpec) => void;
            "notify::modules": (pspec: GObject.ParamSpec) => void;
            "notify::platform-extensions": (pspec: GObject.ParamSpec) => void;
            "notify::prepare-platform-commands": (pspec: GObject.ParamSpec) => void;
            "notify::rename-appdata-file": (pspec: GObject.ParamSpec) => void;
            "notify::rename-desktop-file": (pspec: GObject.ParamSpec) => void;
            "notify::rename-icon": (pspec: GObject.ParamSpec) => void;
            "notify::rename-mime-file": (pspec: GObject.ParamSpec) => void;
            "notify::rename-mime-icons": (pspec: GObject.ParamSpec) => void;
            "notify::runtime": (pspec: GObject.ParamSpec) => void;
            "notify::runtime-commit": (pspec: GObject.ParamSpec) => void;
            "notify::runtime-version": (pspec: GObject.ParamSpec) => void;
            "notify::sdk": (pspec: GObject.ParamSpec) => void;
            "notify::sdk-commit": (pspec: GObject.ParamSpec) => void;
            "notify::sdk-extensions": (pspec: GObject.ParamSpec) => void;
            "notify::separate-locales": (pspec: GObject.ParamSpec) => void;
            "notify::tags": (pspec: GObject.ParamSpec) => void;
            "notify::var": (pspec: GObject.ParamSpec) => void;
            "notify::writable-sdk": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSerializable.ConstructorProps, Json.Serializable.ConstructorProps {
            add_build_extensions: FlatpakExtensions;
            addBuildExtensions: FlatpakExtensions;
            add_extensions: FlatpakExtensions;
            addExtensions: FlatpakExtensions;
            app_id: string;
            appId: string;
            appdata_license: string;
            appdataLicense: string;
            appstream_compose: boolean;
            appstreamCompose: boolean;
            base: string;
            base_commit: string;
            baseCommit: string;
            base_extensions: string[];
            baseExtensions: string[];
            base_version: string;
            baseVersion: string;
            branch: string;
            build_extension: boolean;
            buildExtension: boolean;
            build_options: FlatpakOptions;
            buildOptions: FlatpakOptions;
            build_runtime: boolean;
            buildRuntime: boolean;
            cleanup: string[];
            cleanup_commands: string[];
            cleanupCommands: string[];
            cleanup_platform: string[];
            cleanupPlatform: string[];
            cleanup_platform_commands: string[];
            cleanupPlatformCommands: string[];
            collection_id: string;
            collectionId: string;
            command: string;
            copy_icon: boolean;
            copyIcon: boolean;
            default_branch: string;
            defaultBranch: string;
            desktop_file_name_prefix: string;
            desktopFileNamePrefix: string;
            desktop_file_name_suffix: string;
            desktopFileNameSuffix: string;
            extension_tag: string;
            extensionTag: string;
            finish_args: string[];
            finishArgs: string[];
            id: string;
            id_platform: string;
            idPlatform: string;
            inherit_extensions: string[];
            inheritExtensions: string[];
            inherit_sdk_extensions: string[];
            inheritSdkExtensions: string[];
            metadata: string;
            metadata_platform: string;
            metadataPlatform: string;
            modules: FlatpakModules;
            platform_extensions: string[];
            platformExtensions: string[];
            prepare_platform_commands: string[];
            preparePlatformCommands: string[];
            rename_appdata_file: string;
            renameAppdataFile: string;
            rename_desktop_file: string;
            renameDesktopFile: string;
            rename_icon: string;
            renameIcon: string;
            rename_mime_file: string;
            renameMimeFile: string;
            rename_mime_icons: string[];
            renameMimeIcons: string[];
            runtime: string;
            runtime_commit: string;
            runtimeCommit: string;
            runtime_version: string;
            runtimeVersion: string;
            sdk: string;
            sdk_commit: string;
            sdkCommit: string;
            sdk_extensions: string[];
            sdkExtensions: string[];
            separate_locales: boolean;
            separateLocales: boolean;
            tags: string[];
            "var": string;
            writable_sdk: boolean;
            writableSdk: boolean;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakManifest extends FlatpakSerializable implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakManifest>;

        // Properties
        get add_build_extensions(): FlatpakExtensions;
        set add_build_extensions(val: FlatpakExtensions);

        get addBuildExtensions(): FlatpakExtensions;
        set addBuildExtensions(val: FlatpakExtensions);

        get add_extensions(): FlatpakExtensions;
        set add_extensions(val: FlatpakExtensions);

        get addExtensions(): FlatpakExtensions;
        set addExtensions(val: FlatpakExtensions);

        /**
         * @default null
         */
        get app_id(): string;
        set app_id(val: string);

        /**
         * @default null
         */
        get appId(): string;
        set appId(val: string);

        /**
         * @default null
         */
        get appdata_license(): string;
        set appdata_license(val: string);

        /**
         * @default null
         */
        get appdataLicense(): string;
        set appdataLicense(val: string);

        /**
         * @default true
         */
        get appstream_compose(): boolean;
        set appstream_compose(val: boolean);

        /**
         * @default true
         */
        get appstreamCompose(): boolean;
        set appstreamCompose(val: boolean);

        /**
         * @default null
         */
        get base(): string;
        set base(val: string);

        /**
         * @default null
         */
        get base_commit(): string;
        set base_commit(val: string);

        /**
         * @default null
         */
        get baseCommit(): string;
        set baseCommit(val: string);

        get base_extensions(): string[];
        set base_extensions(val: string[]);

        get baseExtensions(): string[];
        set baseExtensions(val: string[]);

        /**
         * @default null
         */
        get base_version(): string;
        set base_version(val: string);

        /**
         * @default null
         */
        get baseVersion(): string;
        set baseVersion(val: string);

        /**
         * @default null
         */
        get branch(): string;
        set branch(val: string);

        /**
         * @default false
         */
        get build_extension(): boolean;
        set build_extension(val: boolean);

        /**
         * @default false
         */
        get buildExtension(): boolean;
        set buildExtension(val: boolean);

        get build_options(): FlatpakOptions;
        set build_options(val: FlatpakOptions);

        get buildOptions(): FlatpakOptions;
        set buildOptions(val: FlatpakOptions);

        /**
         * @default false
         */
        get build_runtime(): boolean;
        set build_runtime(val: boolean);

        /**
         * @default false
         */
        get buildRuntime(): boolean;
        set buildRuntime(val: boolean);

        get cleanup(): string[];
        set cleanup(val: string[]);

        get cleanup_commands(): string[];
        set cleanup_commands(val: string[]);

        get cleanupCommands(): string[];
        set cleanupCommands(val: string[]);

        get cleanup_platform(): string[];
        set cleanup_platform(val: string[]);

        get cleanupPlatform(): string[];
        set cleanupPlatform(val: string[]);

        get cleanup_platform_commands(): string[];
        set cleanup_platform_commands(val: string[]);

        get cleanupPlatformCommands(): string[];
        set cleanupPlatformCommands(val: string[]);

        /**
         * @default null
         */
        get collection_id(): string;
        set collection_id(val: string);

        /**
         * @default null
         */
        get collectionId(): string;
        set collectionId(val: string);

        /**
         * @default null
         */
        get command(): string;
        set command(val: string);

        /**
         * @default false
         */
        get copy_icon(): boolean;
        set copy_icon(val: boolean);

        /**
         * @default false
         */
        get copyIcon(): boolean;
        set copyIcon(val: boolean);

        /**
         * @default null
         */
        get default_branch(): string;
        set default_branch(val: string);

        /**
         * @default null
         */
        get defaultBranch(): string;
        set defaultBranch(val: string);

        /**
         * @default null
         */
        get desktop_file_name_prefix(): string;
        set desktop_file_name_prefix(val: string);

        /**
         * @default null
         */
        get desktopFileNamePrefix(): string;
        set desktopFileNamePrefix(val: string);

        /**
         * @default null
         */
        get desktop_file_name_suffix(): string;
        set desktop_file_name_suffix(val: string);

        /**
         * @default null
         */
        get desktopFileNameSuffix(): string;
        set desktopFileNameSuffix(val: string);

        /**
         * @default null
         */
        get extension_tag(): string;
        set extension_tag(val: string);

        /**
         * @default null
         */
        get extensionTag(): string;
        set extensionTag(val: string);

        get finish_args(): string[];
        set finish_args(val: string[]);

        get finishArgs(): string[];
        set finishArgs(val: string[]);

        /**
         * @default null
         */
        get id(): string;
        set id(val: string);

        /**
         * @default null
         */
        get id_platform(): string;
        set id_platform(val: string);

        /**
         * @default null
         */
        get idPlatform(): string;
        set idPlatform(val: string);

        get inherit_extensions(): string[];
        set inherit_extensions(val: string[]);

        get inheritExtensions(): string[];
        set inheritExtensions(val: string[]);

        get inherit_sdk_extensions(): string[];
        set inherit_sdk_extensions(val: string[]);

        get inheritSdkExtensions(): string[];
        set inheritSdkExtensions(val: string[]);

        /**
         * @default null
         */
        get metadata(): string;
        set metadata(val: string);

        /**
         * @default null
         */
        get metadata_platform(): string;
        set metadata_platform(val: string);

        /**
         * @default null
         */
        get metadataPlatform(): string;
        set metadataPlatform(val: string);

        get modules(): FlatpakModules;
        set modules(val: FlatpakModules);

        get platform_extensions(): string[];
        set platform_extensions(val: string[]);

        get platformExtensions(): string[];
        set platformExtensions(val: string[]);

        get prepare_platform_commands(): string[];
        set prepare_platform_commands(val: string[]);

        get preparePlatformCommands(): string[];
        set preparePlatformCommands(val: string[]);

        /**
         * @default null
         */
        get rename_appdata_file(): string;
        set rename_appdata_file(val: string);

        /**
         * @default null
         */
        get renameAppdataFile(): string;
        set renameAppdataFile(val: string);

        /**
         * @default null
         */
        get rename_desktop_file(): string;
        set rename_desktop_file(val: string);

        /**
         * @default null
         */
        get renameDesktopFile(): string;
        set renameDesktopFile(val: string);

        /**
         * @default null
         */
        get rename_icon(): string;
        set rename_icon(val: string);

        /**
         * @default null
         */
        get renameIcon(): string;
        set renameIcon(val: string);

        /**
         * @default null
         */
        get rename_mime_file(): string;
        set rename_mime_file(val: string);

        /**
         * @default null
         */
        get renameMimeFile(): string;
        set renameMimeFile(val: string);

        get rename_mime_icons(): string[];
        set rename_mime_icons(val: string[]);

        get renameMimeIcons(): string[];
        set renameMimeIcons(val: string[]);

        /**
         * @default null
         */
        get runtime(): string;
        set runtime(val: string);

        /**
         * @default null
         */
        get runtime_commit(): string;
        set runtime_commit(val: string);

        /**
         * @default null
         */
        get runtimeCommit(): string;
        set runtimeCommit(val: string);

        /**
         * @default null
         */
        get runtime_version(): string;
        set runtime_version(val: string);

        /**
         * @default null
         */
        get runtimeVersion(): string;
        set runtimeVersion(val: string);

        /**
         * @default null
         */
        get sdk(): string;
        set sdk(val: string);

        /**
         * @default null
         */
        get sdk_commit(): string;
        set sdk_commit(val: string);

        /**
         * @default null
         */
        get sdkCommit(): string;
        set sdkCommit(val: string);

        get sdk_extensions(): string[];
        set sdk_extensions(val: string[]);

        get sdkExtensions(): string[];
        set sdkExtensions(val: string[]);

        /**
         * @default true
         */
        get separate_locales(): boolean;
        set separate_locales(val: boolean);

        /**
         * @default true
         */
        get separateLocales(): boolean;
        set separateLocales(val: boolean);

        get tags(): string[];
        set tags(val: string[]);

        /**
         * @default null
         */
        get "var"(): string;
        set "var"(val: string);

        /**
         * @default false
         */
        get writable_sdk(): boolean;
        set writable_sdk(val: boolean);

        /**
         * @default false
         */
        get writableSdk(): boolean;
        set writableSdk(val: boolean);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakManifest.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakManifest.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakManifest.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakManifest.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakManifest.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakManifest.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakManifest.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakManifest.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_build_options(): (FlatpakOptions | null);

        dup_command(): string;

        dup_finish_args(): (string[] | null);

        dup_id(): string;

        dup_modules(): (FlatpakModules | null);

        dup_runtime(): string;

        dup_runtime_version(): string;

        dup_sdk(): string;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FlatpakManifestLoader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            file: Gio.File;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakManifestLoader extends GObject.Object {
        static $gtype: GObject.GType<FlatpakManifestLoader>;

        // Properties
        /**
         * @construct-only
         */
        get file(): Gio.File;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakManifestLoader.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakManifestLoader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](file: Gio.File): FlatpakManifestLoader;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakManifestLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakManifestLoader.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakManifestLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakManifestLoader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakManifestLoader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakManifestLoader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_base_dir(): Gio.File;

        dup_file(): Gio.File;

        load(): Dex.Future;
    }


    namespace FlatpakModule {
        // Signal signatures
        interface SignalSignatures extends FlatpakSerializable.SignalSignatures {
            "notify::build-commands": (pspec: GObject.ParamSpec) => void;
            "notify::build-options": (pspec: GObject.ParamSpec) => void;
            "notify::builddir": (pspec: GObject.ParamSpec) => void;
            "notify::buildsystem": (pspec: GObject.ParamSpec) => void;
            "notify::cleanup": (pspec: GObject.ParamSpec) => void;
            "notify::cleanup-platform": (pspec: GObject.ParamSpec) => void;
            "notify::cmake": (pspec: GObject.ParamSpec) => void;
            "notify::config-opts": (pspec: GObject.ParamSpec) => void;
            "notify::disabled": (pspec: GObject.ParamSpec) => void;
            "notify::ensure-writable": (pspec: GObject.ParamSpec) => void;
            "notify::install-rule": (pspec: GObject.ParamSpec) => void;
            "notify::make-args": (pspec: GObject.ParamSpec) => void;
            "notify::make-install-args": (pspec: GObject.ParamSpec) => void;
            "notify::modules": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::no-autogen": (pspec: GObject.ParamSpec) => void;
            "notify::no-make-install": (pspec: GObject.ParamSpec) => void;
            "notify::no-parallel-make": (pspec: GObject.ParamSpec) => void;
            "notify::no-python-timestamp-fix": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::post-install": (pspec: GObject.ParamSpec) => void;
            "notify::rm-configure": (pspec: GObject.ParamSpec) => void;
            "notify::run-tests": (pspec: GObject.ParamSpec) => void;
            "notify::secret-env": (pspec: GObject.ParamSpec) => void;
            "notify::secret-opts": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
            "notify::sources": (pspec: GObject.ParamSpec) => void;
            "notify::subdir": (pspec: GObject.ParamSpec) => void;
            "notify::test-commands": (pspec: GObject.ParamSpec) => void;
            "notify::test-rule": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSerializable.ConstructorProps, Json.Serializable.ConstructorProps {
            build_commands: string[];
            buildCommands: string[];
            build_options: FlatpakOptions;
            buildOptions: FlatpakOptions;
            builddir: boolean;
            buildsystem: string;
            cleanup: string[];
            cleanup_platform: string[];
            cleanupPlatform: string[];
            cmake: boolean;
            config_opts: string[];
            configOpts: string[];
            disabled: boolean;
            ensure_writable: string[];
            ensureWritable: string[];
            install_rule: string;
            installRule: string;
            make_args: string[];
            makeArgs: string[];
            make_install_args: string[];
            makeInstallArgs: string[];
            modules: FlatpakModules;
            name: string;
            no_autogen: boolean;
            noAutogen: boolean;
            no_make_install: boolean;
            noMakeInstall: boolean;
            no_parallel_make: boolean;
            noParallelMake: boolean;
            no_python_timestamp_fix: boolean;
            noPythonTimestampFix: boolean;
            only_arches: string[];
            onlyArches: string[];
            post_install: string[];
            postInstall: string[];
            rm_configure: boolean;
            rmConfigure: boolean;
            run_tests: boolean;
            runTests: boolean;
            secret_env: string[];
            secretEnv: string[];
            secret_opts: string[];
            secretOpts: string[];
            skip_arches: string[];
            skipArches: string[];
            sources: FlatpakSources;
            subdir: string;
            test_commands: string[];
            testCommands: string[];
            test_rule: string;
            testRule: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakModule extends FlatpakSerializable implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakModule>;

        // Properties
        get build_commands(): string[];
        set build_commands(val: string[]);

        get buildCommands(): string[];
        set buildCommands(val: string[]);

        get build_options(): FlatpakOptions;
        set build_options(val: FlatpakOptions);

        get buildOptions(): FlatpakOptions;
        set buildOptions(val: FlatpakOptions);

        /**
         * @default false
         */
        get builddir(): boolean;
        set builddir(val: boolean);

        /**
         * @default null
         */
        get buildsystem(): string;
        set buildsystem(val: string);

        get cleanup(): string[];
        set cleanup(val: string[]);

        get cleanup_platform(): string[];
        set cleanup_platform(val: string[]);

        get cleanupPlatform(): string[];
        set cleanupPlatform(val: string[]);

        /**
         * @default false
         */
        get cmake(): boolean;
        set cmake(val: boolean);

        get config_opts(): string[];
        set config_opts(val: string[]);

        get configOpts(): string[];
        set configOpts(val: string[]);

        /**
         * @default false
         */
        get disabled(): boolean;
        set disabled(val: boolean);

        get ensure_writable(): string[];
        set ensure_writable(val: string[]);

        get ensureWritable(): string[];
        set ensureWritable(val: string[]);

        /**
         * @default null
         */
        get install_rule(): string;
        set install_rule(val: string);

        /**
         * @default null
         */
        get installRule(): string;
        set installRule(val: string);

        get make_args(): string[];
        set make_args(val: string[]);

        get makeArgs(): string[];
        set makeArgs(val: string[]);

        get make_install_args(): string[];
        set make_install_args(val: string[]);

        get makeInstallArgs(): string[];
        set makeInstallArgs(val: string[]);

        get modules(): FlatpakModules;
        set modules(val: FlatpakModules);

        /**
         * @default null
         */
        get name(): string;
        set name(val: string);

        /**
         * @default false
         */
        get no_autogen(): boolean;
        set no_autogen(val: boolean);

        /**
         * @default false
         */
        get noAutogen(): boolean;
        set noAutogen(val: boolean);

        /**
         * @default false
         */
        get no_make_install(): boolean;
        set no_make_install(val: boolean);

        /**
         * @default false
         */
        get noMakeInstall(): boolean;
        set noMakeInstall(val: boolean);

        /**
         * @default false
         */
        get no_parallel_make(): boolean;
        set no_parallel_make(val: boolean);

        /**
         * @default false
         */
        get noParallelMake(): boolean;
        set noParallelMake(val: boolean);

        /**
         * @default false
         */
        get no_python_timestamp_fix(): boolean;
        set no_python_timestamp_fix(val: boolean);

        /**
         * @default false
         */
        get noPythonTimestampFix(): boolean;
        set noPythonTimestampFix(val: boolean);

        get only_arches(): string[];
        set only_arches(val: string[]);

        get onlyArches(): string[];
        set onlyArches(val: string[]);

        get post_install(): string[];
        set post_install(val: string[]);

        get postInstall(): string[];
        set postInstall(val: string[]);

        /**
         * @default false
         */
        get rm_configure(): boolean;
        set rm_configure(val: boolean);

        /**
         * @default false
         */
        get rmConfigure(): boolean;
        set rmConfigure(val: boolean);

        /**
         * @default false
         */
        get run_tests(): boolean;
        set run_tests(val: boolean);

        /**
         * @default false
         */
        get runTests(): boolean;
        set runTests(val: boolean);

        get secret_env(): string[];
        set secret_env(val: string[]);

        get secretEnv(): string[];
        set secretEnv(val: string[]);

        get secret_opts(): string[];
        set secret_opts(val: string[]);

        get secretOpts(): string[];
        set secretOpts(val: string[]);

        get skip_arches(): string[];
        set skip_arches(val: string[]);

        get skipArches(): string[];
        set skipArches(val: string[]);

        get sources(): FlatpakSources;
        set sources(val: FlatpakSources);

        /**
         * @default null
         */
        get subdir(): string;
        set subdir(val: string);

        get test_commands(): string[];
        set test_commands(val: string[]);

        get testCommands(): string[];
        set testCommands(val: string[]);

        /**
         * @default null
         */
        get test_rule(): string;
        set test_rule(val: string);

        /**
         * @default null
         */
        get testRule(): string;
        set testRule(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakModule.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakModule.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakModule.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakModule.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakModule.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakModule.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakModule.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakModule.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_build_commands(): (string[] | null);

        dup_build_options(): (FlatpakOptions | null);

        dup_buildsystem(): string;

        dup_config_opts(): (string[] | null);

        dup_modules(): (FlatpakModules | null);

        dup_name(): string;

        dup_sources(): (FlatpakSources | null);

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FlatpakModules {
        // Signal signatures
        interface SignalSignatures extends FlatpakList.SignalSignatures {
            "notify::item-type": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends FlatpakList.ConstructorProps<A>, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakModules<A extends GObject.Object = GObject.Object> extends FlatpakList<A> implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<FlatpakModules>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakModules.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakModules.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakModules.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakModules.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakModules.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakModules.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakModules.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakModules.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param project_dir the directory of the project
         */
        find_primary(project_dir: Gio.File): (FlatpakModule | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace FlatpakOptions {
        // Signal signatures
        interface SignalSignatures extends FlatpakSerializable.SignalSignatures {
            "notify::append-ld-library-path": (pspec: GObject.ParamSpec) => void;
            "notify::append-path": (pspec: GObject.ParamSpec) => void;
            "notify::append-pkg-config-path": (pspec: GObject.ParamSpec) => void;
            "notify::arch": (pspec: GObject.ParamSpec) => void;
            "notify::build-args": (pspec: GObject.ParamSpec) => void;
            "notify::cflags": (pspec: GObject.ParamSpec) => void;
            "notify::cflags-override": (pspec: GObject.ParamSpec) => void;
            "notify::config-opts": (pspec: GObject.ParamSpec) => void;
            "notify::cppflags": (pspec: GObject.ParamSpec) => void;
            "notify::cppflags-override": (pspec: GObject.ParamSpec) => void;
            "notify::cxxflags": (pspec: GObject.ParamSpec) => void;
            "notify::cxxflags-override": (pspec: GObject.ParamSpec) => void;
            "notify::env": (pspec: GObject.ParamSpec) => void;
            "notify::ldflags": (pspec: GObject.ParamSpec) => void;
            "notify::ldflags-override": (pspec: GObject.ParamSpec) => void;
            "notify::libdir": (pspec: GObject.ParamSpec) => void;
            "notify::make-args": (pspec: GObject.ParamSpec) => void;
            "notify::make-install-args": (pspec: GObject.ParamSpec) => void;
            "notify::no-debuginfo": (pspec: GObject.ParamSpec) => void;
            "notify::no-debuginfo-compression": (pspec: GObject.ParamSpec) => void;
            "notify::prefix": (pspec: GObject.ParamSpec) => void;
            "notify::prepend-ld-library-path": (pspec: GObject.ParamSpec) => void;
            "notify::prepend-path": (pspec: GObject.ParamSpec) => void;
            "notify::prepend-pkg-config-path": (pspec: GObject.ParamSpec) => void;
            "notify::secret-env": (pspec: GObject.ParamSpec) => void;
            "notify::secret-opts": (pspec: GObject.ParamSpec) => void;
            "notify::strip": (pspec: GObject.ParamSpec) => void;
            "notify::test-args": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSerializable.ConstructorProps, Json.Serializable.ConstructorProps {
            append_ld_library_path: string;
            appendLdLibraryPath: string;
            append_path: string;
            appendPath: string;
            append_pkg_config_path: string;
            appendPkgConfigPath: string;
            arch: FlatpakArchOptions;
            build_args: string[];
            buildArgs: string[];
            cflags: string;
            cflags_override: boolean;
            cflagsOverride: boolean;
            config_opts: string[];
            configOpts: string[];
            cppflags: string;
            cppflags_override: boolean;
            cppflagsOverride: boolean;
            cxxflags: string;
            cxxflags_override: boolean;
            cxxflagsOverride: boolean;
            env: string[];
            ldflags: string;
            ldflags_override: boolean;
            ldflagsOverride: boolean;
            libdir: string;
            make_args: string[];
            makeArgs: string[];
            make_install_args: string[];
            makeInstallArgs: string[];
            no_debuginfo: boolean;
            noDebuginfo: boolean;
            no_debuginfo_compression: boolean;
            noDebuginfoCompression: boolean;
            prefix: string;
            prepend_ld_library_path: string;
            prependLdLibraryPath: string;
            prepend_path: string;
            prependPath: string;
            prepend_pkg_config_path: string;
            prependPkgConfigPath: string;
            secret_env: string[];
            secretEnv: string[];
            secret_opts: string[];
            secretOpts: string[];
            strip: boolean;
            test_args: string[];
            testArgs: string[];
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakOptions extends FlatpakSerializable implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakOptions>;

        // Properties
        /**
         * @default null
         */
        get append_ld_library_path(): string;
        set append_ld_library_path(val: string);

        /**
         * @default null
         */
        get appendLdLibraryPath(): string;
        set appendLdLibraryPath(val: string);

        /**
         * @default null
         */
        get append_path(): string;
        set append_path(val: string);

        /**
         * @default null
         */
        get appendPath(): string;
        set appendPath(val: string);

        /**
         * @default null
         */
        get append_pkg_config_path(): string;
        set append_pkg_config_path(val: string);

        /**
         * @default null
         */
        get appendPkgConfigPath(): string;
        set appendPkgConfigPath(val: string);

        get arch(): FlatpakArchOptions;
        set arch(val: FlatpakArchOptions);

        get build_args(): string[];
        set build_args(val: string[]);

        get buildArgs(): string[];
        set buildArgs(val: string[]);

        /**
         * @default null
         */
        get cflags(): string;
        set cflags(val: string);

        /**
         * @default false
         */
        get cflags_override(): boolean;
        set cflags_override(val: boolean);

        /**
         * @default false
         */
        get cflagsOverride(): boolean;
        set cflagsOverride(val: boolean);

        get config_opts(): string[];
        set config_opts(val: string[]);

        get configOpts(): string[];
        set configOpts(val: string[]);

        /**
         * @default null
         */
        get cppflags(): string;
        set cppflags(val: string);

        /**
         * @default false
         */
        get cppflags_override(): boolean;
        set cppflags_override(val: boolean);

        /**
         * @default false
         */
        get cppflagsOverride(): boolean;
        set cppflagsOverride(val: boolean);

        /**
         * @default null
         */
        get cxxflags(): string;
        set cxxflags(val: string);

        /**
         * @default false
         */
        get cxxflags_override(): boolean;
        set cxxflags_override(val: boolean);

        /**
         * @default false
         */
        get cxxflagsOverride(): boolean;
        set cxxflagsOverride(val: boolean);

        get env(): string[];
        set env(val: string[]);

        /**
         * @default null
         */
        get ldflags(): string;
        set ldflags(val: string);

        /**
         * @default false
         */
        get ldflags_override(): boolean;
        set ldflags_override(val: boolean);

        /**
         * @default false
         */
        get ldflagsOverride(): boolean;
        set ldflagsOverride(val: boolean);

        /**
         * @default null
         */
        get libdir(): string;
        set libdir(val: string);

        get make_args(): string[];
        set make_args(val: string[]);

        get makeArgs(): string[];
        set makeArgs(val: string[]);

        get make_install_args(): string[];
        set make_install_args(val: string[]);

        get makeInstallArgs(): string[];
        set makeInstallArgs(val: string[]);

        /**
         * @default false
         */
        get no_debuginfo(): boolean;
        set no_debuginfo(val: boolean);

        /**
         * @default false
         */
        get noDebuginfo(): boolean;
        set noDebuginfo(val: boolean);

        /**
         * @default false
         */
        get no_debuginfo_compression(): boolean;
        set no_debuginfo_compression(val: boolean);

        /**
         * @default false
         */
        get noDebuginfoCompression(): boolean;
        set noDebuginfoCompression(val: boolean);

        /**
         * @default null
         */
        get prefix(): string;
        set prefix(val: string);

        /**
         * @default null
         */
        get prepend_ld_library_path(): string;
        set prepend_ld_library_path(val: string);

        /**
         * @default null
         */
        get prependLdLibraryPath(): string;
        set prependLdLibraryPath(val: string);

        /**
         * @default null
         */
        get prepend_path(): string;
        set prepend_path(val: string);

        /**
         * @default null
         */
        get prependPath(): string;
        set prependPath(val: string);

        /**
         * @default null
         */
        get prepend_pkg_config_path(): string;
        set prepend_pkg_config_path(val: string);

        /**
         * @default null
         */
        get prependPkgConfigPath(): string;
        set prependPkgConfigPath(val: string);

        get secret_env(): string[];
        set secret_env(val: string[]);

        get secretEnv(): string[];
        set secretEnv(val: string[]);

        get secret_opts(): string[];
        set secret_opts(val: string[]);

        get secretOpts(): string[];
        set secretOpts(val: string[]);

        /**
         * @default false
         */
        get strip(): boolean;
        set strip(val: boolean);

        get test_args(): string[];
        set test_args(val: string[]);

        get testArgs(): string[];
        set testArgs(val: string[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakOptions.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakOptions.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakOptions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakOptions.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakOptions.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakOptions.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakOptions.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakOptions.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_append_path(): string;

        dup_build_args(): (string[] | null);

        dup_env(): (string[] | null);

        dup_prepend_path(): string;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FlatpakSerializable {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    abstract class FlatpakSerializable extends GObject.Object implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSerializable>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSerializable.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSerializable.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSerializable.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSerializable.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSerializable.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSerializable.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSerializable.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSerializable.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_serialize(): Json.Node;

        // Methods
        /**
         * @param property 
         */
        dup_x_string(property: string): (string | null);

        /**
         * @param property 
         */
        dup_x_strv(property: string): (string[] | null);

        /**
         * @param path 
         * @returns a {@link Gio.File} or `null` and `error` is set
         */
        resolve_file(path: string): Gio.File;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FlatpakSource {
        // Signal signatures
        interface SignalSignatures extends FlatpakSerializable.SignalSignatures {
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSerializable.ConstructorProps, Json.Serializable.ConstructorProps {
            dest: string;
            only_arches: string[];
            onlyArches: string[];
            skip_arches: string[];
            skipArches: string[];
        }
    }

    /**
     * @gir-type Class
     */
    abstract class FlatpakSource extends FlatpakSerializable implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSource>;

        // Properties
        /**
         * @default null
         */
        get dest(): string;
        set dest(val: string);

        get only_arches(): string[];
        set only_arches(val: string[]);

        get onlyArches(): string[];
        set onlyArches(val: string[]);

        get skip_arches(): string[];
        set skip_arches(val: string[]);

        get skipArches(): string[];
        set skipArches(val: string[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSource.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSource.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static new_from_json(node: Json.Node): FlatpakSource;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSource.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSource.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSource.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSource.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_dest(): string;

        dup_only_arches(): (string[] | null);

        dup_skip_arches(): (string[] | null);

        /**
         * @param dest 
         */
        set_dest(dest: string): void;

        /**
         * @param only_arches 
         */
        set_only_arches(only_arches: string): void;

        /**
         * @param skip_arches 
         */
        set_skip_arches(skip_arches: string): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace FlatpakSourceArchive {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::archive-type": (pspec: GObject.ParamSpec) => void;
            "notify::dest-filename": (pspec: GObject.ParamSpec) => void;
            "notify::disable-http-decompression": (pspec: GObject.ParamSpec) => void;
            "notify::git-init": (pspec: GObject.ParamSpec) => void;
            "notify::md5": (pspec: GObject.ParamSpec) => void;
            "notify::mirror-urls": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::referer": (pspec: GObject.ParamSpec) => void;
            "notify::sha1": (pspec: GObject.ParamSpec) => void;
            "notify::sha256": (pspec: GObject.ParamSpec) => void;
            "notify::sha512": (pspec: GObject.ParamSpec) => void;
            "notify::strip-components": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            archive_type: string;
            archiveType: string;
            dest_filename: string;
            destFilename: string;
            disable_http_decompression: boolean;
            disableHttpDecompression: boolean;
            git_init: boolean;
            gitInit: boolean;
            md5: string;
            mirror_urls: string[];
            mirrorUrls: string[];
            path: string;
            referer: string;
            sha1: string;
            sha256: string;
            sha512: string;
            strip_components: number;
            stripComponents: number;
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceArchive extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceArchive>;

        // Properties
        /**
         * @default null
         */
        get archive_type(): string;
        set archive_type(val: string);

        /**
         * @default null
         */
        get archiveType(): string;
        set archiveType(val: string);

        /**
         * @default null
         */
        get dest_filename(): string;
        set dest_filename(val: string);

        /**
         * @default null
         */
        get destFilename(): string;
        set destFilename(val: string);

        /**
         * @default false
         */
        get disable_http_decompression(): boolean;
        set disable_http_decompression(val: boolean);

        /**
         * @default false
         */
        get disableHttpDecompression(): boolean;
        set disableHttpDecompression(val: boolean);

        /**
         * @default false
         */
        get git_init(): boolean;
        set git_init(val: boolean);

        /**
         * @default false
         */
        get gitInit(): boolean;
        set gitInit(val: boolean);

        /**
         * @default null
         */
        get md5(): string;
        set md5(val: string);

        get mirror_urls(): string[];
        set mirror_urls(val: string[]);

        get mirrorUrls(): string[];
        set mirrorUrls(val: string[]);

        /**
         * @default null
         */
        get path(): string;
        set path(val: string);

        /**
         * @default null
         */
        get referer(): string;
        set referer(val: string);

        /**
         * @default null
         */
        get sha1(): string;
        set sha1(val: string);

        /**
         * @default null
         */
        get sha256(): string;
        set sha256(val: string);

        /**
         * @default null
         */
        get sha512(): string;
        set sha512(val: string);

        /**
         * @default 1
         */
        get strip_components(): number;
        set strip_components(val: number);

        /**
         * @default 1
         */
        get stripComponents(): number;
        set stripComponents(val: number);

        /**
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceArchive.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceArchive.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceArchive.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceArchive.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceArchive.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceArchive.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceArchive.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceArchive.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourceBzr {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::revision": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            revision: string;
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceBzr extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceBzr>;

        // Properties
        /**
         * @default null
         */
        get revision(): string;
        set revision(val: string);

        /**
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceBzr.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceBzr.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceBzr.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceBzr.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceBzr.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceBzr.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceBzr.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceBzr.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourceDir {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::skip": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            path: string;
            skip: string[];
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceDir extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceDir>;

        // Properties
        /**
         * @default null
         */
        get path(): string;
        set path(val: string);

        get skip(): string[];
        set skip(val: string[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceDir.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceDir.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceDir.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceDir.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceDir.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceDir.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceDir.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceDir.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourceExtraData {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::sha256": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            filename: string;
            sha256: string;
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceExtraData extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceExtraData>;

        // Properties
        /**
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * @default null
         */
        get sha256(): string;
        set sha256(val: string);

        /**
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceExtraData.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceExtraData.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceExtraData.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceExtraData.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceExtraData.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceExtraData.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceExtraData.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceExtraData.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourceFile {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::dest-filename": (pspec: GObject.ParamSpec) => void;
            "notify::disable-http-decompression": (pspec: GObject.ParamSpec) => void;
            "notify::md5": (pspec: GObject.ParamSpec) => void;
            "notify::mirror-urls": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::referer": (pspec: GObject.ParamSpec) => void;
            "notify::sha1": (pspec: GObject.ParamSpec) => void;
            "notify::sha256": (pspec: GObject.ParamSpec) => void;
            "notify::sha512": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            dest_filename: string;
            destFilename: string;
            disable_http_decompression: boolean;
            disableHttpDecompression: boolean;
            md5: string;
            mirror_urls: string[];
            mirrorUrls: string[];
            path: string;
            referer: string;
            sha1: string;
            sha256: string;
            sha512: string;
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceFile extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceFile>;

        // Properties
        /**
         * @default null
         */
        get dest_filename(): string;
        set dest_filename(val: string);

        /**
         * @default null
         */
        get destFilename(): string;
        set destFilename(val: string);

        /**
         * @default false
         */
        get disable_http_decompression(): boolean;
        set disable_http_decompression(val: boolean);

        /**
         * @default false
         */
        get disableHttpDecompression(): boolean;
        set disableHttpDecompression(val: boolean);

        /**
         * @default null
         */
        get md5(): string;
        set md5(val: string);

        get mirror_urls(): string[];
        set mirror_urls(val: string[]);

        get mirrorUrls(): string[];
        set mirrorUrls(val: string[]);

        /**
         * @default null
         */
        get path(): string;
        set path(val: string);

        /**
         * @default null
         */
        get referer(): string;
        set referer(val: string);

        /**
         * @default null
         */
        get sha1(): string;
        set sha1(val: string);

        /**
         * @default null
         */
        get sha256(): string;
        set sha256(val: string);

        /**
         * @default null
         */
        get sha512(): string;
        set sha512(val: string);

        /**
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceFile.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourceGit {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::branch": (pspec: GObject.ParamSpec) => void;
            "notify::commit": (pspec: GObject.ParamSpec) => void;
            "notify::disable-fsckobjects": (pspec: GObject.ParamSpec) => void;
            "notify::disable-shallow-clone": (pspec: GObject.ParamSpec) => void;
            "notify::disable-submodules": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::tag": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            branch: string;
            commit: string;
            disable_fsckobjects: boolean;
            disableFsckobjects: boolean;
            disable_shallow_clone: boolean;
            disableShallowClone: boolean;
            disable_submodules: boolean;
            disableSubmodules: boolean;
            path: string;
            tag: string;
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceGit extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceGit>;

        // Properties
        /**
         * @default null
         */
        get branch(): string;
        set branch(val: string);

        /**
         * @default null
         */
        get commit(): string;
        set commit(val: string);

        /**
         * @default false
         */
        get disable_fsckobjects(): boolean;
        set disable_fsckobjects(val: boolean);

        /**
         * @default false
         */
        get disableFsckobjects(): boolean;
        set disableFsckobjects(val: boolean);

        /**
         * @default false
         */
        get disable_shallow_clone(): boolean;
        set disable_shallow_clone(val: boolean);

        /**
         * @default false
         */
        get disableShallowClone(): boolean;
        set disableShallowClone(val: boolean);

        /**
         * @default false
         */
        get disable_submodules(): boolean;
        set disable_submodules(val: boolean);

        /**
         * @default false
         */
        get disableSubmodules(): boolean;
        set disableSubmodules(val: boolean);

        /**
         * @default null
         */
        get path(): string;
        set path(val: string);

        /**
         * @default null
         */
        get tag(): string;
        set tag(val: string);

        /**
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceGit.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceGit.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceGit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceGit.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceGit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceGit.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceGit.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceGit.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_branch(): string;

        dup_commit(): string;

        /**
         * @param base_dir 
         */
        dup_location(base_dir: Gio.File): string;

        dup_path(): string;

        dup_tag(): string;

        dup_url(): string;
    }


    namespace FlatpakSourceInline {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::base64": (pspec: GObject.ParamSpec) => void;
            "notify::contents": (pspec: GObject.ParamSpec) => void;
            "notify::dest-filename": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            base64: boolean;
            contents: string;
            dest_filename: string;
            destFilename: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceInline extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceInline>;

        // Properties
        /**
         * @default false
         */
        get base64(): boolean;
        set base64(val: boolean);

        /**
         * @default null
         */
        get contents(): string;
        set contents(val: string);

        /**
         * @default null
         */
        get dest_filename(): string;
        set dest_filename(val: string);

        /**
         * @default null
         */
        get destFilename(): string;
        set destFilename(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceInline.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceInline.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceInline.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceInline.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceInline.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceInline.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceInline.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceInline.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourcePatch {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::options": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::paths": (pspec: GObject.ParamSpec) => void;
            "notify::strip-components": (pspec: GObject.ParamSpec) => void;
            "notify::use-git": (pspec: GObject.ParamSpec) => void;
            "notify::use-git-am": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            options: string[];
            path: string;
            paths: string[];
            strip_components: number;
            stripComponents: number;
            use_git: boolean;
            useGit: boolean;
            use_git_am: boolean;
            useGitAm: boolean;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourcePatch extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourcePatch>;

        // Properties
        get options(): string[];
        set options(val: string[]);

        /**
         * @default null
         */
        get path(): string;
        set path(val: string);

        get paths(): string[];
        set paths(val: string[]);

        /**
         * @default 1
         */
        get strip_components(): number;
        set strip_components(val: number);

        /**
         * @default 1
         */
        get stripComponents(): number;
        set stripComponents(val: number);

        /**
         * @default false
         */
        get use_git(): boolean;
        set use_git(val: boolean);

        /**
         * @default false
         */
        get useGit(): boolean;
        set useGit(val: boolean);

        /**
         * @default false
         */
        get use_git_am(): boolean;
        set use_git_am(val: boolean);

        /**
         * @default false
         */
        get useGitAm(): boolean;
        set useGitAm(val: boolean);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourcePatch.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourcePatch.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourcePatch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourcePatch.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourcePatch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourcePatch.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourcePatch.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourcePatch.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourceScript {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::commands": (pspec: GObject.ParamSpec) => void;
            "notify::dest-filename": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            commands: string[];
            dest_filename: string;
            destFilename: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceScript extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceScript>;

        // Properties
        get commands(): string[];
        set commands(val: string[]);

        /**
         * @default null
         */
        get dest_filename(): string;
        set dest_filename(val: string);

        /**
         * @default null
         */
        get destFilename(): string;
        set destFilename(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceScript.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceScript.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceScript.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceScript.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceScript.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceScript.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceScript.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceScript.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourceShell {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::commands": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            commands: string[];
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceShell extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceShell>;

        // Properties
        get commands(): string[];
        set commands(val: string[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceShell.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceShell.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceShell.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceShell.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceShell.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceShell.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceShell.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceShell.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSourceSvn {
        // Signal signatures
        interface SignalSignatures extends FlatpakSource.SignalSignatures {
            "notify::revision": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
            "notify::dest": (pspec: GObject.ParamSpec) => void;
            "notify::only-arches": (pspec: GObject.ParamSpec) => void;
            "notify::skip-arches": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FlatpakSource.ConstructorProps, Json.Serializable.ConstructorProps {
            revision: string;
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSourceSvn extends FlatpakSource implements Json.Serializable {
        static $gtype: GObject.GType<FlatpakSourceSvn>;

        // Properties
        /**
         * @default null
         */
        get revision(): string;
        set revision(val: string);

        /**
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSourceSvn.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSourceSvn.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSourceSvn.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceSvn.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSourceSvn.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSourceSvn.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSourceSvn.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSourceSvn.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FlatpakSources {
        // Signal signatures
        interface SignalSignatures extends FlatpakList.SignalSignatures {
            "notify::item-type": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends FlatpakList.ConstructorProps<A>, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FlatpakSources<A extends GObject.Object = GObject.Object> extends FlatpakList<A> implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<FlatpakSources>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FlatpakSources.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<FlatpakSources.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FlatpakSources.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSources.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FlatpakSources.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FlatpakSources.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FlatpakSources.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FlatpakSources.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace Forge {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            id: string;
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for forge service implementations.
     * 
     * FoundryForge provides the core interface for interacting with code hosting
     * platforms and forge services. Concrete implementations handle specific forge
     * APIs and provide unified access to projects, issues, and collaboration features
     * across different platforms.
     * @gir-type Class
     * @since 1.1
     */
    abstract class Forge extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Forge>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Forge.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Forge.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Forge.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Forge.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Forge.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Forge.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Forge.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Forge.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Virtual methods
        /**
         * Find the {@link Foundry.ForgeProject} that represents the current project.
         * @virtual
         */
        vfunc_find_project(): Dex.Future;

        /**
         * Find the {@link Foundry.ForgeUser} that represents the current user.
         * @virtual
         */
        vfunc_find_user(): Dex.Future;

        // Methods
        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * Find the {@link Foundry.ForgeProject} that represents the current project.
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.ForgeProject} or rejects with error.
         */
        find_project(): Dex.Future;

        /**
         * Find the {@link Foundry.ForgeUser} that represents the current user.
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.ForgeUser} or rejects with error.
         */
        find_user(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace ForgeIssue {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::author": (pspec: GObject.ParamSpec) => void;
            "notify::created-at": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::online-url": (pspec: GObject.ParamSpec) => void;
            "notify::state": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            author: ForgeUser;
            created_at: GLib.DateTime;
            createdAt: GLib.DateTime;
            description: string;
            id: string;
            online_url: string;
            onlineUrl: string;
            state: string;
            title: string;
        }
    }

    /**
     * Abstract base class for representing issues from forge services.
     * 
     * FoundryForgeIssue provides the core interface for representing issues
     * and bug reports from forge services. It includes common properties
     * like ID, title, state, and creation date, and provides a unified
     * interface for issue management across different forge platforms.
     * @gir-type Class
     */
    abstract class ForgeIssue extends GObject.Object {
        static $gtype: GObject.GType<ForgeIssue>;

        // Properties
        /**
         * @read-only
         */
        get author(): ForgeUser;

        /**
         * @read-only
         */
        get created_at(): GLib.DateTime;

        /**
         * @read-only
         */
        get createdAt(): GLib.DateTime;

        /**
         * @read-only
         * @default null
         */
        get description(): string;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default null
         */
        get online_url(): string;

        /**
         * @read-only
         * @default null
         */
        get onlineUrl(): string;

        /**
         * @read-only
         * @default null
         */
        get state(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ForgeIssue.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ForgeIssue.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ForgeIssue.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeIssue.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ForgeIssue.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeIssue.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ForgeIssue.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ForgeIssue.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a copy of the user who authored the issue.
         * @virtual
         */
        vfunc_dup_author(): (ForgeUser | null);

        /**
         * @virtual
         */
        vfunc_dup_created_at(): (GLib.DateTime | null);

        /**
         * @virtual
         */
        vfunc_dup_description(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_id(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_online_url(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_state(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_title(): (string | null);

        // Methods
        /**
         * Gets a copy of the user who authored the issue.
         * @returns a {@link Foundry.ForgeUser}, or `null`
         */
        dup_author(): (ForgeUser | null);

        /**
         * @returns a {@link GLib.DateTime}
         */
        dup_created_at(): (GLib.DateTime | null);

        dup_description(): (string | null);

        dup_id(): (string | null);

        dup_online_url(): (string | null);

        dup_state(): (string | null);

        dup_title(): (string | null);
    }


    namespace ForgeListing {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::auto-load": (pspec: GObject.ParamSpec) => void;
            "notify::n-pages": (pspec: GObject.ParamSpec) => void;
            "notify::page-size": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends GObject.Object.ConstructorProps, Gio.ListModel.ConstructorProps {
            auto_load: boolean;
            autoLoad: boolean;
            n_pages: number;
            nPages: number;
            page_size: number;
            pageSize: number;
        }
    }

    /**
     * Abstract base class for managing paginated listings from forge services.
     * 
     * FoundryForgeListing provides functionality for handling paginated data
     * from forge services including automatic loading, page management, and
     * model integration. It supports efficient data loading and provides
     * a unified interface for displaying large datasets from forge APIs.
     * @gir-type Class
     * @since 1.1
     */
    abstract class ForgeListing<A extends GObject.Object = GObject.Object> extends GObject.Object implements Gio.ListModel<A> {
        static $gtype: GObject.GType<ForgeListing>;

        // Properties
        /**
         * @default false
         */
        get auto_load(): boolean;
        set auto_load(val: boolean);

        /**
         * @default false
         */
        get autoLoad(): boolean;
        set autoLoad(val: boolean);

        /**
         * @read-only
         * @default 0
         */
        get n_pages(): number;

        /**
         * @read-only
         * @default 0
         */
        get nPages(): number;

        /**
         * @read-only
         * @default 0
         */
        get page_size(): number;

        /**
         * @read-only
         * @default 0
         */
        get pageSize(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ForgeListing.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ForgeListing.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ForgeListing.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeListing.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ForgeListing.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeListing.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ForgeListing.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ForgeListing.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets the number of pages.
         * @virtual
         */
        vfunc_get_n_pages(): number;

        /**
         * Gets the number of items per page.
         * @virtual
         */
        vfunc_get_page_size(): number;

        /**
         * @param page 
         * @virtual
         */
        vfunc_load_page(page: number): Dex.Future;

        // Methods
        /**
         * Gets the auto-load property.
         * @returns `true` if auto-loading is enabled
         */
        get_auto_load(): boolean;

        /**
         * Gets the number of pages.
         */
        get_n_pages(): number;

        /**
         * Gets the number of items per page.
         */
        get_page_size(): number;

        /**
         * Tries to load all pages of results.
         * 
         * This is done sequentially.
         * @returns a {@link Dex.Future} that resolves to `self`   or rejects with error.
         */
        load_all(): Dex.Future;

        /**
         * @param page 
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} or rejects with error.
         */
        load_page(page: number): Dex.Future;

        /**
         * Sets the auto-load property.
         * 
         * When enabled, the listing will automatically fetch the next page
         * when a request for an item falls into the last currently loaded page.
         * @param auto_load `true` to enable auto-loading
         */
        set_auto_load(auto_load: boolean): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace ForgeManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::forge": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            forge: Forge;
        }
    }

    /**
     * Manages plugins providing {@link Foundry.Forge} implementations.
     * @gir-type Class
     * @since 1.1
     */
    class ForgeManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<ForgeManager>;

        // Properties
        /**
         * @read-only
         */
        get forge(): Forge;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ForgeManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ForgeManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ForgeManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ForgeManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ForgeManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ForgeManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_forge(): (Forge | null);

        /**
         * @param forge_id the id of the forge
         */
        find_by_id(forge_id: string): (Forge | null);

        /**
         * @param forge a {@link Foundry.Forge}
         */
        set_forge(forge: (Forge | null)): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace ForgeMergeRequest {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::author": (pspec: GObject.ParamSpec) => void;
            "notify::created-at": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::online-url": (pspec: GObject.ParamSpec) => void;
            "notify::state": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            author: ForgeUser;
            created_at: GLib.DateTime;
            createdAt: GLib.DateTime;
            description: string;
            id: string;
            online_url: string;
            onlineUrl: string;
            state: string;
            title: string;
        }
    }

    /**
     * Abstract base class for representing merge requests from forge services.
     * 
     * FoundryForgeMergeRequest provides the core interface for representing merge
     * requests and pull requests from forge services. It includes common properties
     * like ID, title, state, and creation date, and provides a unified interface
     * for merge request management across different forge platforms.
     * @gir-type Class
     */
    abstract class ForgeMergeRequest extends GObject.Object {
        static $gtype: GObject.GType<ForgeMergeRequest>;

        // Properties
        /**
         * @read-only
         */
        get author(): ForgeUser;

        /**
         * @read-only
         */
        get created_at(): GLib.DateTime;

        /**
         * @read-only
         */
        get createdAt(): GLib.DateTime;

        /**
         * @read-only
         * @default null
         */
        get description(): string;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default null
         */
        get online_url(): string;

        /**
         * @read-only
         * @default null
         */
        get onlineUrl(): string;

        /**
         * @read-only
         * @default null
         */
        get state(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ForgeMergeRequest.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ForgeMergeRequest.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ForgeMergeRequest.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeMergeRequest.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ForgeMergeRequest.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeMergeRequest.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ForgeMergeRequest.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ForgeMergeRequest.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a copy of the user who authored the merge request.
         * @virtual
         */
        vfunc_dup_author(): (ForgeUser | null);

        /**
         * Gets a copy of the date and time when the merge request was created.
         * @virtual
         */
        vfunc_dup_created_at(): (GLib.DateTime | null);

        /**
         * Gets a copy of the merge request description.
         * @virtual
         */
        vfunc_dup_description(): (string | null);

        /**
         * Gets a copy of the merge request identifier.
         * @virtual
         */
        vfunc_dup_id(): (string | null);

        /**
         * Gets a copy of the URL to view the merge request online.
         * @virtual
         */
        vfunc_dup_online_url(): (string | null);

        /**
         * Gets a copy of the merge request state (e.g. "open", "closed", "merged").
         * @virtual
         */
        vfunc_dup_state(): (string | null);

        /**
         * Gets a copy of the merge request title.
         * @virtual
         */
        vfunc_dup_title(): (string | null);

        // Methods
        /**
         * Gets a copy of the user who authored the merge request.
         * @returns a {@link Foundry.ForgeUser}, or `null`
         */
        dup_author(): (ForgeUser | null);

        /**
         * Gets a copy of the date and time when the merge request was created.
         * @returns a {@link GLib.DateTime}, or `null`
         */
        dup_created_at(): (GLib.DateTime | null);

        /**
         * Gets a copy of the merge request description.
         * @returns the description, or `null`
         */
        dup_description(): (string | null);

        /**
         * Gets a copy of the merge request identifier.
         * @returns the merge request ID, or `null`
         */
        dup_id(): (string | null);

        /**
         * Gets a copy of the URL to view the merge request online.
         * @returns the online URL, or `null`
         */
        dup_online_url(): (string | null);

        /**
         * Gets a copy of the merge request state (e.g. "open", "closed", "merged").
         * @returns the state, or `null`
         */
        dup_state(): (string | null);

        /**
         * Gets a copy of the merge request title.
         * @returns the title, or `null`
         */
        dup_title(): (string | null);
    }


    namespace ForgeProject {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::avatar-url": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::issues-url": (pspec: GObject.ParamSpec) => void;
            "notify::merge-requests-url": (pspec: GObject.ParamSpec) => void;
            "notify::online-url": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            avatar_url: string;
            avatarUrl: string;
            description: string;
            issues_url: string;
            issuesUrl: string;
            merge_requests_url: string;
            mergeRequestsUrl: string;
            online_url: string;
            onlineUrl: string;
            title: string;
        }
    }

    /**
     * Represents a project on a forge.
     * 
     * Plugins can implement this as part of their forge implementation to allow
     * the user to access various forge features related to their project.
     * @gir-type Class
     * @since 1.1
     */
    abstract class ForgeProject extends GObject.Object {
        static $gtype: GObject.GType<ForgeProject>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get avatar_url(): string;

        /**
         * @read-only
         * @default null
         */
        get avatarUrl(): string;

        /**
         * @read-only
         * @default null
         */
        get description(): string;

        /**
         * @read-only
         * @default null
         */
        get issues_url(): string;

        /**
         * @read-only
         * @default null
         */
        get issuesUrl(): string;

        /**
         * @read-only
         * @default null
         */
        get merge_requests_url(): string;

        /**
         * @read-only
         * @default null
         */
        get mergeRequestsUrl(): string;

        /**
         * @read-only
         * @default null
         */
        get online_url(): string;

        /**
         * @read-only
         * @default null
         */
        get onlineUrl(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ForgeProject.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ForgeProject.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ForgeProject.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeProject.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ForgeProject.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeProject.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ForgeProject.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ForgeProject.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_avatar_url(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_description(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_issues_url(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_merge_requests_url(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_online_url(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_title(): (string | null);

        /**
         * Queries the forge for a list of issues in the project.
         * @param query 
         * @virtual
         */
        vfunc_list_issues(query: (ForgeQuery | null)): Dex.Future;

        /**
         * Queries the forge for a list of merge requests in the project.
         * @param query 
         * @virtual
         */
        vfunc_list_merge_requests(query: (ForgeQuery | null)): Dex.Future;

        /**
         * @virtual
         */
        vfunc_load_avatar(): Dex.Future;

        // Methods
        dup_avatar_url(): (string | null);

        dup_description(): (string | null);

        dup_issues_url(): (string | null);

        dup_merge_requests_url(): (string | null);

        dup_online_url(): (string | null);

        dup_title(): (string | null);

        /**
         * Queries the forge for a list of issues in the project.
         * @param query 
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.ForgeListing} or rejects with error.
         */
        list_issues(query: (ForgeQuery | null)): Dex.Future;

        /**
         * Queries the forge for a list of merge requests in the project.
         * @param query 
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.ForgeListing} or rejects with error.
         */
        list_merge_requests(query: (ForgeQuery | null)): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link GLib.Bytes} or rejects with error.
         */
        load_avatar(): Dex.Future;
    }


    namespace ForgeQuery {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::keywords": (pspec: GObject.ParamSpec) => void;
            "notify::keywords-scope": (pspec: GObject.ParamSpec) => void;
            "notify::state": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            keywords: string;
            keywords_scope: string;
            keywordsScope: string;
            state: string;
        }
    }

    /**
     * @gir-type Class
     */
    class ForgeQuery extends GObject.Object {
        static $gtype: GObject.GType<ForgeQuery>;

        // Properties
        /**
         * @default null
         */
        get keywords(): string;
        set keywords(val: string);

        /**
         * @default null
         */
        get keywords_scope(): string;
        set keywords_scope(val: string);

        /**
         * @default null
         */
        get keywordsScope(): string;
        set keywordsScope(val: string);

        /**
         * @default null
         */
        get state(): string;
        set state(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ForgeQuery.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ForgeQuery.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): ForgeQuery;

        // Signals
        /** @signal */
        connect<K extends keyof ForgeQuery.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeQuery.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ForgeQuery.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeQuery.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ForgeQuery.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ForgeQuery.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Helper to check {@link Foundry.ForgeQuery.keywords_scope} if it contains
         * `keywords_scope` while handling "," separators.
         * @param keywords_scope the keywords scope to check for
         * @returns `true` if `keywords_scope` was found otherwise `false`
         */
        contains_keywords_scope(keywords_scope: string): boolean;

        /**
         * Helper to check {@link Foundry.ForgeQuery.state} if it contains
         * `state` while handling "," separators.
         * @param state 
         * @returns `true` if `state` was found otherwise `false`
         */
        contains_state(state: string): boolean;

        /**
         * Gets the keywords for the query.
         * @returns the keywords string
         */
        dup_keywords(): (string | null);

        /**
         * Gets the keywords scope for the query.
         * 
         * Multiple scopes are supported by separating with a comma.
         * @returns the keywords scope string
         */
        dup_keywords_scope(): (string | null);

        /**
         * Gets the states for the query.
         * 
         * Multiple states are supported by separating with a comma.
         */
        dup_state(): (string | null);

        /**
         * Sets the keywords for the query.
         * @param keywords the keywords to set
         */
        set_keywords(keywords: (string | null)): void;

        /**
         * Sets the keywords scope for the query.
         * 
         * You may specify multiple scopes with a comma.
         * @param keywords_scope the keywords scope to set
         */
        set_keywords_scope(keywords_scope: (string | null)): void;

        /**
         * Sets the allowed states for the query.
         * 
         * You may specify multiple states with a comma.
         * @param state 
         */
        set_state(state: string): void;
    }


    namespace ForgeUser {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::avatar-url": (pspec: GObject.ParamSpec) => void;
            "notify::bio": (pspec: GObject.ParamSpec) => void;
            "notify::handle": (pspec: GObject.ParamSpec) => void;
            "notify::location": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::online-url": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            avatar_url: string;
            avatarUrl: string;
            bio: string;
            handle: string;
            location: string;
            name: string;
            online_url: string;
            onlineUrl: string;
        }
    }

    /**
     * Represents a user on a forge.
     * @gir-type Class
     */
    abstract class ForgeUser extends GObject.Object {
        static $gtype: GObject.GType<ForgeUser>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get avatar_url(): string;

        /**
         * @read-only
         * @default null
         */
        get avatarUrl(): string;

        /**
         * @read-only
         * @default null
         */
        get bio(): string;

        /**
         * @read-only
         * @default null
         */
        get handle(): string;

        /**
         * @read-only
         * @default null
         */
        get location(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         * @default null
         */
        get online_url(): string;

        /**
         * @read-only
         * @default null
         */
        get onlineUrl(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ForgeUser.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ForgeUser.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ForgeUser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeUser.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ForgeUser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ForgeUser.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ForgeUser.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ForgeUser.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_avatar_url(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_bio(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_handle(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_location(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_name(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_online_url(): (string | null);

        /**
         * @virtual
         */
        vfunc_load_avatar(): Dex.Future;

        // Methods
        dup_avatar_url(): (string | null);

        dup_bio(): (string | null);

        dup_handle(): (string | null);

        dup_location(): (string | null);

        dup_name(): (string | null);

        dup_online_url(): (string | null);

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link GLib.Bytes} or rejects with error.
         */
        load_avatar(): Dex.Future;
    }


    namespace Gir {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Represents a GObject Introspection Repository (GIR) file.
     * 
     * FoundryGir provides functionality for parsing and accessing GIR files which
     * contain metadata about GObject-based libraries. It enables introspection
     * of library APIs and provides information about classes, functions, and
     * other symbols for development tools and documentation generation.
     * @gir-type Class
     */
    class Gir extends GObject.Object {
        static $gtype: GObject.GType<Gir>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Gir.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Gir.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](file: Gio.File): Gir;

        static new_for_path(path: string): Gir;

        // Signals
        /** @signal */
        connect<K extends keyof Gir.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Gir.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Gir.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Gir.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Gir.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Gir.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        get_file(): Gio.File;

        /**
         * @param namespace_name 
         */
        get_namespace(namespace_name: string): (GirNode | null);

        get_repository(): GirNode;

        list_namespaces(): GirNode[];
    }


    namespace GirNode {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Represents a node in a GObject Introspection Repository (GIR) file.
     * 
     * FoundryGirNode provides functionality for representing and manipulating
     * nodes in GIR files including attributes, children, and content. It
     * supports hierarchical navigation and provides efficient access to
     * GIR data for development tools and documentation generation.
     * @gir-type Class
     */
    class GirNode extends GObject.Object {
        static $gtype: GObject.GType<GirNode>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GirNode.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GirNode.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](type: GirNodeType, tag_name: string): GirNode;

        // Signals
        /** @signal */
        connect<K extends keyof GirNode.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GirNode.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GirNode.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GirNode.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GirNode.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GirNode.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * @param type 
         */
        static type_to_string(type: GirNodeType): string;

        // Methods
        /**
         * @param type 
         * @param name 
         */
        find_child(type: GirNodeType, name: string): (GirNode | null);

        /**
         * @param attribute 
         */
        get_attribute(attribute: string): string;

        /**
         * Gets a list of children of this node in the Gir file.
         */
        get_children(): (GirNode[] | null);

        get_content(): string;

        get_name(): string;

        get_node_type(): GirNodeType;

        get_parent(): (GirNode | null);

        get_tag_name(): string;

        /**
         * @param attribute 
         */
        has_attribute(attribute: string): boolean;

        /**
         * Gets the keys of the node.
         * 
         * The caller should free the resulting array but not the elements
         * within it.
         */
        list_attributes(): string[];

        /**
         * Collects all of the children that match `type`.
         * 
         * The result is an array of borrowed pointers to children nodes.
         * In C that means you free the resulting pointer but not the
         * nodes within it.
         * 
         * ```c
         * guint n_nodes = 0;
         * g_autofree FoundryGirNode **nodes = foundry_gir_node_list_children_typed (parent, type, &n_nodes);
         * ```
         * @param type the type of node to collect
         */
        list_children_typed(type: GirNodeType): (GirNode[] | null);
    }


    namespace GitBlame {
        // Signal signatures
        interface SignalSignatures extends VcsBlame.SignalSignatures {
            "notify::file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsBlame.ConstructorProps {

        }
    }

    /**
     * Git implementation of blame information for version control files.
     * 
     * FoundryGitBlame provides Git-specific functionality for retrieving and
     * managing blame information for files in version control. It handles
     * blame data retrieval, caching, and provides efficient access to
     * authorship and modification information for code analysis and display.
     * @gir-type Class
     */
    class GitBlame extends VcsBlame {
        static $gtype: GObject.GType<GitBlame>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitBlame.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitBlame.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitBlame.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitBlame.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitBlame.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitBlame.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitBlame.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitBlame.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the commit ID string for the given line number from the blame
         * information.
         * 
         * The commit ID is the hash of the commit that last modified the line.
         * @param line the line number (0-indexed)
         * @returns a newly allocated string containing the commit ID,   or `null` if the line is not found or not tracked
         */
        dup_commit_id(line: number): string;
    }


    namespace GitBranch {
        // Signal signatures
        interface SignalSignatures extends VcsBranch.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::is-local": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsBranch.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitBranch extends VcsBranch {
        static $gtype: GObject.GType<GitBranch>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitBranch.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitBranch.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitBranch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitBranch.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitBranch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitBranch.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitBranch.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitBranch.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitCloner {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::author-email": (pspec: GObject.ParamSpec) => void;
            "notify::author-name": (pspec: GObject.ParamSpec) => void;
            "notify::bare": (pspec: GObject.ParamSpec) => void;
            "notify::directory": (pspec: GObject.ParamSpec) => void;
            "notify::remote-branch-name": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            author_email: string;
            authorEmail: string;
            author_name: string;
            authorName: string;
            bare: boolean;
            directory: Gio.File;
            remote_branch_name: string;
            remoteBranchName: string;
            uri: string;
        }
    }

    /**
     * @gir-type Class
     */
    class GitCloner extends GObject.Object {
        static $gtype: GObject.GType<GitCloner>;

        // Properties
        /**
         * @default null
         */
        get author_email(): string;
        set author_email(val: string);

        /**
         * @default null
         */
        get authorEmail(): string;
        set authorEmail(val: string);

        /**
         * @default null
         */
        get author_name(): string;
        set author_name(val: string);

        /**
         * @default null
         */
        get authorName(): string;
        set authorName(val: string);

        /**
         * @default false
         */
        get bare(): boolean;
        set bare(val: boolean);

        get directory(): Gio.File;
        set directory(val: Gio.File);

        /**
         * @default null
         */
        get remote_branch_name(): string;
        set remote_branch_name(val: string);

        /**
         * @default null
         */
        get remoteBranchName(): string;
        set remoteBranchName(val: string);

        /**
         * @default null
         */
        get uri(): string;
        set uri(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitCloner.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitCloner.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): GitCloner;

        // Signals
        /** @signal */
        connect<K extends keyof GitCloner.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitCloner.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitCloner.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitCloner.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitCloner.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitCloner.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * `pty_fd` is copied and may be closed after calling this function.
         * @param pty_fd the FD for a PTY, or -1
         * @param operation 
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error
         */
        clone(pty_fd: number, operation: Operation): Dex.Future;

        dup_author_email(): string;

        dup_author_name(): string;

        dup_directory(): (Gio.File | null);

        dup_remote_branch_name(): string;

        dup_uri(): string;

        /**
         * If {@link Foundry.GitCloner.directory} should be used as the destination
         * directory instead of a `.git` subdirectory.
         */
        get_bare(): boolean;

        /**
         * @param author_email 
         */
        set_author_email(author_email: string): void;

        /**
         * @param author_name 
         */
        set_author_name(author_name: string): void;

        /**
         * @param bare 
         */
        set_bare(bare: boolean): void;

        /**
         * @param directory 
         */
        set_directory(directory: Gio.File): void;

        /**
         * @param remote_branch_name 
         */
        set_remote_branch_name(remote_branch_name: string): void;

        /**
         * @param uri 
         */
        set_uri(uri: string): void;

        /**
         * Validates the values of the cloner.
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error
         */
        validate(): Dex.Future;
    }


    namespace GitCommit {
        // Signal signatures
        interface SignalSignatures extends VcsCommit.SignalSignatures {
            "notify::author": (pspec: GObject.ParamSpec) => void;
            "notify::committer": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsCommit.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitCommit extends VcsCommit {
        static $gtype: GObject.GType<GitCommit>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitCommit.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitCommit.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitCommit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitCommit.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitCommit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitCommit.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitCommit.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitCommit.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitCommitBuilder {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::author-email": (pspec: GObject.ParamSpec) => void;
            "notify::author-name": (pspec: GObject.ParamSpec) => void;
            "notify::can-commit": (pspec: GObject.ParamSpec) => void;
            "notify::message": (pspec: GObject.ParamSpec) => void;
            "notify::signing-format": (pspec: GObject.ParamSpec) => void;
            "notify::signing-key": (pspec: GObject.ParamSpec) => void;
            "notify::staged": (pspec: GObject.ParamSpec) => void;
            "notify::unstaged": (pspec: GObject.ParamSpec) => void;
            "notify::untracked": (pspec: GObject.ParamSpec) => void;
            "notify::when": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            author_email: string;
            authorEmail: string;
            author_name: string;
            authorName: string;
            can_commit: boolean;
            canCommit: boolean;
            message: string;
            signing_format: string;
            signingFormat: string;
            signing_key: string;
            signingKey: string;
            staged: Gio.ListModel;
            unstaged: Gio.ListModel;
            untracked: Gio.ListModel;
            when: GLib.DateTime;
        }
    }

    /**
     * @gir-type Class
     */
    class GitCommitBuilder extends GObject.Object {
        static $gtype: GObject.GType<GitCommitBuilder>;

        // Properties
        /**
         * The email address of the commit author.
         * 
         * If not set, the value from git config "user.email" will be used when
         * creating the commit.
         * @default null
         */
        get author_email(): string;
        set author_email(val: string);

        /**
         * The email address of the commit author.
         * 
         * If not set, the value from git config "user.email" will be used when
         * creating the commit.
         * @default null
         */
        get authorEmail(): string;
        set authorEmail(val: string);

        /**
         * The name of the commit author.
         * 
         * If not set, the value from git config "user.name" will be used when
         * creating the commit.
         * @default null
         */
        get author_name(): string;
        set author_name(val: string);

        /**
         * The name of the commit author.
         * 
         * If not set, the value from git config "user.name" will be used when
         * creating the commit.
         * @default null
         */
        get authorName(): string;
        set authorName(val: string);

        /**
         * Whether the builder has sufficient information to create a commit.
         * 
         * This property is `true` when both a non-empty commit message and at least
         * one staged file are present. It is read-only and will be updated
         * automatically as files are staged or unstaged and as the message changes.
         * @read-only
         * @default false
         */
        get can_commit(): boolean;

        /**
         * Whether the builder has sufficient information to create a commit.
         * 
         * This property is `true` when both a non-empty commit message and at least
         * one staged file are present. It is read-only and will be updated
         * automatically as files are staged or unstaged and as the message changes.
         * @read-only
         * @default false
         */
        get canCommit(): boolean;

        /**
         * The commit message.
         * 
         * This must be set to a non-empty string before a commit can be created.
         * @default null
         */
        get message(): string;
        set message(val: string);

        /**
         * The format to use for signing commits.
         * 
         * Common values are "gpg" for GPG signatures or "ssh" for SSH signatures.
         * Defaults to "gpg" if not set.
         * @default gpg
         */
        get signing_format(): string;
        set signing_format(val: string);

        /**
         * The format to use for signing commits.
         * 
         * Common values are "gpg" for GPG signatures or "ssh" for SSH signatures.
         * Defaults to "gpg" if not set.
         * @default gpg
         */
        get signingFormat(): string;
        set signingFormat(val: string);

        /**
         * The key identifier to use for signing the commit.
         * 
         * If set, the commit will be signed using the specified key and the
         * signing format. If not set, the commit will not be signed.
         * @default null
         */
        get signing_key(): string;
        set signing_key(val: string);

        /**
         * The key identifier to use for signing the commit.
         * 
         * If set, the commit will be signed using the specified key and the
         * signing format. If not set, the commit will not be signed.
         * @default null
         */
        get signingKey(): string;
        set signingKey(val: string);

        /**
         * A list model containing all files that are currently staged for commit.
         * 
         * The list model contains {@link Foundry.GitStatusEntry} objects representing
         * files in the working tree that have been staged. The list is updated
         * automatically as files are staged or unstaged.
         * @read-only
         */
        get staged(): Gio.ListModel;

        /**
         * A list model containing all files that have unstaged changes.
         * 
         * The list model contains {@link Foundry.GitStatusEntry} objects representing
         * files in the working tree that have been modified but not staged. The list
         * is updated automatically as files are staged or unstaged.
         * @read-only
         */
        get unstaged(): Gio.ListModel;

        /**
         * A list model containing all untracked files in the working tree.
         * 
         * The list model contains {@link Foundry.GitStatusEntry} objects representing
         * files in the working tree that are not tracked by git. The list is updated
         * automatically as files are staged or untracked files are added.
         * @read-only
         */
        get untracked(): Gio.ListModel;

        /**
         * The timestamp for the commit.
         * 
         * If not set, the current time will be used when creating the commit.
         */
        get when(): GLib.DateTime;
        set when(val: GLib.DateTime);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitCommitBuilder.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitCommitBuilder.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](vcs: GitVcs, parent: (GitCommit | null), context_lines: number): GitCommitBuilder;

        // Signals
        /** @signal */
        connect<K extends keyof GitCommitBuilder.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitCommitBuilder.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitCommitBuilder.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitCommitBuilder.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitCommitBuilder.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitCommitBuilder.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Creates a commit using the fields from the builder.
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.GitCommit} or rejects with error
         */
        commit(): Dex.Future;

        /**
         * Gets the author email address that will be used for the commit.
         * @returns the author email, or `null` if not set.   The caller should free the returned string with `g_free()` when done.
         */
        dup_author_email(): (string | null);

        /**
         * Gets the author name that will be used for the commit.
         * @returns the author name, or `null` if not set.   The caller should free the returned string with `g_free()` when done.
         */
        dup_author_name(): (string | null);

        /**
         * Gets the commit message that will be used for the commit.
         * @returns the commit message, or `null` if not set.   The caller should free the returned string with `g_free()` when done.
         */
        dup_message(): (string | null);

        /**
         * Gets the signing format that will be used for signing the commit.
         * @returns the signing format (e.g. "gpg" or   "ssh"), or `null` if not set. The caller should free the returned string   with `g_free()` when done.
         */
        dup_signing_format(): (string | null);

        /**
         * Gets the signing key identifier that will be used for signing the commit.
         * @returns the signing key identifier, or `null`   if not set. The caller should free the returned string with `g_free()`   when done.
         */
        dup_signing_key(): (string | null);

        /**
         * Gets the timestamp that will be used for the commit.
         * @returns a {@link GLib.DateTime} representing   the commit timestamp, or `null` if not set. The caller should free the   returned object with `g_date_time_unref()` when done.
         */
        dup_when(): (GLib.DateTime | null);

        /**
         * Checks whether the builder has sufficient information to create a commit.
         * 
         * Returns `true` if both a non-empty commit message and at least one staged
         * file are present. Returns `false` otherwise.
         * @returns `true` if a commit can be created, `false` otherwise
         */
        get_can_commit(): boolean;

        /**
         * Checks whether `file` was untracked when the commit builder was created.
         * 
         * This method checks if the file was in the untracked files list at the
         * time the commit builder was initialized. Note that this reflects the
         * state when the builder was created, not the current git status.
         * @param file a {@link Gio.File} to check
         * @returns `true` if `file` was untracked when the builder was created,   `false` otherwise
         */
        is_untracked(file: Gio.File): boolean;

        /**
         * Gets a list model containing all files that are currently staged for commit.
         * 
         * The list model contains {@link Foundry.GitStatusEntry} objects representing
         * files in the working tree that have been staged. The list is updated
         * automatically as files are staged or unstaged.
         * @returns a {@link Gio.ListModel} of   {@link Foundry.GitStatusEntry} objects representing staged files
         */
        list_staged(): Gio.ListModel;

        /**
         * Gets a list model containing all files that have unstaged changes.
         * 
         * The list model contains {@link Foundry.GitStatusEntry} objects representing
         * files in the working tree that have been modified but not staged. The list
         * is updated automatically as files are staged or unstaged.
         * @returns a {@link Gio.ListModel} of   {@link Foundry.GitStatusEntry} objects representing files with unstaged   changes
         */
        list_unstaged(): Gio.ListModel;

        /**
         * Gets a list model containing all untracked files in the working tree.
         * 
         * The list model contains {@link Foundry.GitStatusEntry} objects representing
         * files in the working tree that are not tracked by git. The list is updated
         * automatically as files are staged or untracked files are added.
         * @returns a {@link Gio.ListModel} of   {@link Foundry.GitStatusEntry} objects representing untracked files
         */
        list_untracked(): Gio.ListModel;

        /**
         * Loads the delta for `file` comparing the index against the parent commit.
         * This delta represents staged changes and can be used to toggle individual
         * lines on/off for staging in the background.
         * @param file a {@link Gio.File} in the working tree
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.GitDelta} or rejects with error.
         */
        load_staged_delta(file: Gio.File): Dex.Future;

        /**
         * Loads the delta for `file` comparing the working directory against the index.
         * This delta represents unstaged changes and can be used to toggle individual
         * lines on/off for staging in the background.
         * @param file a {@link Gio.File} in the working tree
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.GitDelta} or rejects with error.
         */
        load_unstaged_delta(file: Gio.File): Dex.Future;

        /**
         * Loads the delta for an untracked `file`. This creates a synthetic delta
         * that represents adding the entire file, allowing the same API semantics
         * for new files as for modified files. The delta can be used to toggle
         * individual lines on/off for staging in the background.
         * 
         * The file must be untracked (not in git's index) when the commit builder
         * was created. Since untracked files are new files, the delta will contain
         * a single hunk with all lines marked as additions.
         * @param file a {@link Gio.File} in the working tree
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.GitDelta} or rejects with error.
         */
        load_untracked_delta(file: Gio.File): Dex.Future;

        /**
         * Creates a new builder similar to `self`, copying all string and GDateTime
         * properties from the existing builder.
         * 
         * The new builder will use the same VCS instance, parent commit (or HEAD if
         * no parent was set), context lines, author name, author email, signing key,
         * signing format, and timestamp as `self`.
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.GitCommitBuilder}.
         */
        new_similar(): Dex.Future;

        /**
         * Sets the author email address that will be used for the commit.
         * 
         * If set to `null` or not set, the value from git config "user.email" will be
         * used when creating the commit.
         * @param author_email the author email to use, or `null` to unset
         */
        set_author_email(author_email: (string | null)): void;

        /**
         * Sets the author name that will be used for the commit.
         * 
         * If set to `null` or not set, the value from git config "user.name" will be
         * used when creating the commit.
         * @param author_name the author name to use, or `null` to unset
         */
        set_author_name(author_name: (string | null)): void;

        /**
         * Sets the commit message that will be used for the commit.
         * 
         * This must be set to a non-empty string before a commit can be created.
         * Setting this will automatically update the {@link Foundry.GitCommitBuilder.can_commit}
         * property.
         * @param message the commit message to use, or `null` to unset
         */
        set_message(message: (string | null)): void;

        /**
         * Sets the signing format that will be used for signing the commit.
         * 
         * Common values are "gpg" for GPG signatures or "ssh" for SSH signatures.
         * If set to `null`, defaults to "gpg".
         * @param signing_format the signing format to use (e.g. "gpg" or "ssh"), or `null` for default
         */
        set_signing_format(signing_format: (string | null)): void;

        /**
         * Sets the signing key identifier that will be used for signing the commit.
         * 
         * If set, the commit will be signed using the specified key and the signing
         * format. If set to `null`, the commit will not be signed.
         * @param signing_key the signing key identifier to use, or `null` to disable signing
         */
        set_signing_key(signing_key: (string | null)): void;

        /**
         * Sets the timestamp that will be used for the commit.
         * 
         * If set to `null` or not set, the current time will be used when creating
         * the commit. The builder takes ownership of `when`.
         * @param when the timestamp to use for the commit, or `null` to use current time
         */
        set_when(when: (GLib.DateTime | null)): void;

        /**
         * Stages the file using the version from the stored diff/delta.
         * This stages the fully applied version (all changes from the delta).
         * @param file a {@link Gio.File} in the working tree
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error
         */
        stage_file(file: Gio.File): Dex.Future;

        /**
         * Stages the selected hunks from the file.
         * @param file a {@link Gio.File} in the working tree
         * @param hunks a {@link Gio.ListModel} of {@link Foundry.GitDiffHunk}
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error
         */
        stage_hunks(file: Gio.File, hunks: Gio.ListModel): Dex.Future;

        /**
         * Stages the selected lines from the file.
         * @param file a {@link Gio.File} in the working tree
         * @param lines a {@link Gio.ListModel} of {@link Foundry.GitDiffLine}
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error
         */
        stage_lines(file: Gio.File, lines: Gio.ListModel): Dex.Future;

        /**
         * Unstages the file using the version from the stored diff/delta.
         * This restores the file to the fully unapplied version (HEAD version).
         * @param file a {@link Gio.File} in the working tree
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error
         */
        unstage_file(file: Gio.File): Dex.Future;

        /**
         * Unstages the selected hunks from the file.
         * @param file a {@link Gio.File} in the working tree
         * @param hunks a {@link Gio.ListModel} of {@link Foundry.GitDiffHunk}
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error
         */
        unstage_hunks(file: Gio.File, hunks: Gio.ListModel): Dex.Future;

        /**
         * Unstages the selected lines from the file.
         * @param file a {@link Gio.File} in the working tree
         * @param lines a {@link Gio.ListModel} of {@link Foundry.GitDiffLine}
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error
         */
        unstage_lines(file: Gio.File, lines: Gio.ListModel): Dex.Future;
    }


    namespace GitDelta {
        // Signal signatures
        interface SignalSignatures extends VcsDelta.SignalSignatures {
            "notify::new-path": (pspec: GObject.ParamSpec) => void;
            "notify::old-path": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsDelta.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitDelta extends VcsDelta {
        static $gtype: GObject.GType<GitDelta>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitDelta.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitDelta.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitDelta.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitDelta.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitDelta.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitDelta.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitDelta.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitDelta.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitDiff {
        // Signal signatures
        interface SignalSignatures extends VcsDiff.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsDiff.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitDiff extends VcsDiff {
        static $gtype: GObject.GType<GitDiff>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitDiff.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitDiff.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitDiff.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitDiff.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitDiff.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitDiff.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitDiff.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitDiff.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitDiffHunk {
        // Signal signatures
        interface SignalSignatures extends VcsDiffHunk.SignalSignatures {
            "notify::header": (pspec: GObject.ParamSpec) => void;
            "notify::new-lines": (pspec: GObject.ParamSpec) => void;
            "notify::new-start": (pspec: GObject.ParamSpec) => void;
            "notify::old-lines": (pspec: GObject.ParamSpec) => void;
            "notify::old-start": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsDiffHunk.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitDiffHunk extends VcsDiffHunk {
        static $gtype: GObject.GType<GitDiffHunk>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitDiffHunk.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitDiffHunk.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitDiffHunk.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitDiffHunk.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitDiffHunk.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitDiffHunk.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitDiffHunk.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitDiffHunk.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitDiffLine {
        // Signal signatures
        interface SignalSignatures extends VcsDiffLine.SignalSignatures {
            "notify::has-newline": (pspec: GObject.ParamSpec) => void;
            "notify::length": (pspec: GObject.ParamSpec) => void;
            "notify::new-line": (pspec: GObject.ParamSpec) => void;
            "notify::old-line": (pspec: GObject.ParamSpec) => void;
            "notify::origin": (pspec: GObject.ParamSpec) => void;
            "notify::text": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsDiffLine.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitDiffLine extends VcsDiffLine {
        static $gtype: GObject.GType<GitDiffLine>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitDiffLine.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitDiffLine.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitDiffLine.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitDiffLine.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitDiffLine.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitDiffLine.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitDiffLine.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitDiffLine.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitFile {
        // Signal signatures
        interface SignalSignatures extends VcsFile.SignalSignatures {
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::relative-path": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsFile.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitFile extends VcsFile {
        static $gtype: GObject.GType<GitFile>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitFile.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitReference {
        // Signal signatures
        interface SignalSignatures extends VcsReference.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::is-symbolic": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsReference.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitReference extends VcsReference {
        static $gtype: GObject.GType<GitReference>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitReference.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitReference.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitReference.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitReference.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitReference.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitReference.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitReference.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitReference.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitRemote {
        // Signal signatures
        interface SignalSignatures extends VcsRemote.SignalSignatures {
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsRemote.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitRemote extends VcsRemote {
        static $gtype: GObject.GType<GitRemote>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitRemote.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitRemote.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitRemote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitRemote.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitRemote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitRemote.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitRemote.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitRemote.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitSignature {
        // Signal signatures
        interface SignalSignatures extends VcsSignature.SignalSignatures {
            "notify::email": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::when": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsSignature.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitSignature extends VcsSignature {
        static $gtype: GObject.GType<GitSignature>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitSignature.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitSignature.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitSignature.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitSignature.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitSignature.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitSignature.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitSignature.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitSignature.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitStats {
        // Signal signatures
        interface SignalSignatures extends VcsStats.SignalSignatures {
            "notify::deletions": (pspec: GObject.ParamSpec) => void;
            "notify::files-changed": (pspec: GObject.ParamSpec) => void;
            "notify::insertions": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsStats.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitStats extends VcsStats {
        static $gtype: GObject.GType<GitStats>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitStats.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitStats.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitStats.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitStats.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitStats.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitStats.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitStats.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitStats.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitStatusEntry {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::has-staged-changes": (pspec: GObject.ParamSpec) => void;
            "notify::has-unstaged-changes": (pspec: GObject.ParamSpec) => void;
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::is-new-file": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            has_staged_changes: boolean;
            hasStagedChanges: boolean;
            has_unstaged_changes: boolean;
            hasUnstagedChanges: boolean;
            icon: Gio.Icon;
            is_new_file: boolean;
            isNewFile: boolean;
            path: string;
        }
    }

    /**
     * @gir-type Class
     */
    class GitStatusEntry extends GObject.Object {
        static $gtype: GObject.GType<GitStatusEntry>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get has_staged_changes(): boolean;

        /**
         * @read-only
         * @default false
         */
        get hasStagedChanges(): boolean;

        /**
         * @read-only
         * @default false
         */
        get has_unstaged_changes(): boolean;

        /**
         * @read-only
         * @default false
         */
        get hasUnstagedChanges(): boolean;

        /**
         * @read-only
         */
        get icon(): Gio.Icon;

        /**
         * @read-only
         * @default false
         */
        get is_new_file(): boolean;

        /**
         * @read-only
         * @default false
         */
        get isNewFile(): boolean;

        /**
         * @read-only
         * @default null
         */
        get path(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitStatusEntry.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitStatusEntry.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitStatusEntry.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitStatusEntry.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitStatusEntry.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitStatusEntry.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitStatusEntry.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitStatusEntry.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_icon(): (Gio.Icon | null);

        dup_path(): string;

        has_staged_changed(): boolean;

        has_unstaged_changed(): boolean;
    }


    namespace GitStatusList {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends GObject.Object.ConstructorProps, Gio.ListModel.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitStatusList<A extends GObject.Object = GObject.Object> extends GObject.Object implements Gio.ListModel<A> {
        static $gtype: GObject.GType<GitStatusList>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitStatusList.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitStatusList.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitStatusList.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitStatusList.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitStatusList.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitStatusList.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitStatusList.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitStatusList.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace GitTag {
        // Signal signatures
        interface SignalSignatures extends VcsTag.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::is-local": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsTag.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitTag extends VcsTag {
        static $gtype: GObject.GType<GitTag>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitTag.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitTag.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitTag.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitTag.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitTag.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitTag.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitTag.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitTag.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitTree {
        // Signal signatures
        interface SignalSignatures extends VcsTree.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends VcsTree.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GitTree extends VcsTree {
        static $gtype: GObject.GType<GitTree>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitTree.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitTree.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitTree.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitTree.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitTree.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitTree.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitTree.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitTree.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GitVcs {
        // Signal signatures
        interface SignalSignatures extends Vcs.SignalSignatures {
            "notify::active": (pspec: GObject.ParamSpec) => void;
            "notify::branch-name": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::priority": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Vcs.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Git implementation of the version control system interface.
     * 
     * FoundryGitVcs provides Git-specific functionality for version control operations
     * including repository management, branch operations, and remote synchronization.
     * It integrates with the Git library to provide a unified interface for Git
     * operations within the Foundry development environment.
     * @gir-type Class
     */
    class GitVcs extends Vcs implements Json.Serializable {
        static $gtype: GObject.GType<GitVcs>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GitVcs.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<GitVcs.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof GitVcs.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitVcs.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof GitVcs.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GitVcs.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GitVcs.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GitVcs.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Simple API to create a new commit from the index.
         * @param message 
         * @param author_name 
         * @param author_email 
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.GitCommit} or rejects with error.
         */
        commit(message: string, author_name: (string | null), author_email: (string | null)): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.GitStatusEntry}.
         */
        list_status(): Dex.Future;

        /**
         * Queries a git configuration value by key from the repository.
         * 
         * The method runs asynchronously in a background thread and returns a
         * {@link Dex.Future} that resolves to the config value as a string.
         * @param key the config key to query
         * @returns a {@link Dex.Future} that resolves to a string   containing the config value, or rejects with an error if the key is not   found or an error occurs
         */
        query_config(key: string): Dex.Future;

        /**
         * Signs `bytes` using the format and key specified.
         * 
         * Use {@link Foundry.GitVcs.query_config} to get the
         * @param signing_format the format such as "gpg" or "ssh" such as from the   config value for "gpg.format".
         * @param signing_key the signing key to use such as from the config   value for "user.signingKey"
         * @param bytes the bytes to sign
         * @returns a {@link Dex.Future} that resolves to a   armor contained string.
         */
        sign_bytes(signing_format: string, signing_key: string, bytes: (GLib.Bytes | Uint8Array)): Dex.Future;

        /**
         * @param entry a {@link Foundry.GitStatusEntry}
         * @param contents optional contents to use instead of what is in   the working tree.
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        stage_entry(entry: GitStatusEntry, contents: (GLib.Bytes | null)): Dex.Future;

        /**
         * Stashes the current working directory changes.
         * 
         * This method saves the current changes in the working directory to the stash,
         * similar to running "git stash". The changes are saved with the default stash
         * options.
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.GitCommit} representing the stash commit or   rejects with error.
         */
        stash(): Dex.Future;

        /**
         * @param entry a {@link Foundry.GitStatusEntry}
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        unstage_entry(entry: GitStatusEntry): Dex.Future;
    }


    namespace HoverProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::document": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            buffer: TextBuffer;
            document: TextDocument;
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for plugins to provide hover tooltip features.
     * @gir-type Class
     */
    abstract class HoverProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<HoverProvider>;

        // Properties
        /**
         * @read-only
         */
        get buffer(): TextBuffer;

        /**
         * @construct-only
         */
        get document(): TextDocument;

        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: HoverProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<HoverProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof HoverProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, HoverProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof HoverProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, HoverProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof HoverProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<HoverProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param location a {@link Foundry.TextIter}
         * @virtual
         */
        vfunc_populate(location: TextIter): (Dex.Future | null);

        // Methods
        dup_buffer(): (TextBuffer | null);

        dup_document(): (TextDocument | null);

        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * @param location a {@link Foundry.TextIter}
         * @returns a {@link Dex.Future} that resolves   to a {@link Gio.ListModel} of {@link Foundry.Markup}.
         */
        populate(location: TextIter): (Dex.Future | null);

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Inhibitor {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            context: Context;
        }
    }

    /**
     * Inhibit shutdown of {@link Foundry.Context} during important operations.
     * @gir-type Class
     */
    class Inhibitor extends GObject.Object {
        static $gtype: GObject.GType<Inhibitor>;

        // Properties
        /**
         * @construct-only
         */
        get context(): Context;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Inhibitor.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Inhibitor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Inhibitor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Inhibitor.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Inhibitor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Inhibitor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Inhibitor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Inhibitor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @returns a {@link Foundry.Context} or `null`
         */
        dup_context(): Context;

        uninhibit(): void;
    }


    namespace Input {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            subtitle: string;
            title: string;
            validator: InputValidator;
        }
    }

    /**
     * Base class for various types of user input.
     * 
     * Since Foundry does not rely on a specific toolkit or platform library
     * input must be abstracted for tooling that requires it. {@link Foundry.Input}
     * is the base class for various types of input which may be requested
     * of the user.
     * 
     * Applications should use a platform library that can convert these
     * input requests into an interface.
     * @gir-type Class
     */
    abstract class Input extends GObject.Object {
        static $gtype: GObject.GType<Input>;

        // Properties
        /**
         * @construct-only
         * @default null
         */
        get subtitle(): string;

        /**
         * @construct-only
         * @default null
         */
        get title(): string;

        /**
         * @construct-only
         */
        get validator(): InputValidator;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Input.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Input.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Input.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Input.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Input.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Input.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Input.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Input.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_subtitle(): string;

        dup_title(): string;

        dup_validator(): (InputValidator | null);

        /**
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        validate(): Dex.Future;
    }


    namespace InputChoice {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::item": (pspec: GObject.ParamSpec) => void;
            "notify::selected": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {
            item: GObject.Object;
            selected: boolean;
        }
    }

    /**
     * @gir-type Class
     */
    class InputChoice extends Input {
        static $gtype: GObject.GType<InputChoice>;

        // Properties
        /**
         * @construct-only
         */
        get item(): GObject.Object;

        /**
         * @default false
         */
        get selected(): boolean;
        set selected(val: boolean);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputChoice.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputChoice.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: string, subtitle: string, item: GObject.Object): InputChoice;

        // Signals
        /** @signal */
        connect<K extends keyof InputChoice.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputChoice.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputChoice.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputChoice.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputChoice.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputChoice.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the item for the choice, if any.
         */
        dup_item<T = GObject.Object>(): T;

        get_selected(): boolean;

        /**
         * @param selected 
         */
        set_selected(selected: boolean): void;
    }


    namespace InputCombo {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::choice": (pspec: GObject.ParamSpec) => void;
            "notify::choices": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {
            choice: InputChoice;
            choices: Gio.ListModel;
        }
    }

    /**
     * @gir-type Class
     */
    class InputCombo extends Input {
        static $gtype: GObject.GType<InputCombo>;

        // Properties
        get choice(): InputChoice;
        set choice(val: InputChoice);

        /**
         * @construct-only
         */
        get choices(): Gio.ListModel;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputCombo.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputCombo.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: string, subtitle: string, validator: (InputValidator | null), choices: Gio.ListModel): InputCombo;

        // Signals
        /** @signal */
        connect<K extends keyof InputCombo.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputCombo.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputCombo.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputCombo.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputCombo.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputCombo.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_choice(): (InputChoice | null);

        /**
         * @returns a {@link Gio.ListModel} of   {@link Foundry.InputChoice}.
         */
        list_choices(): (Gio.ListModel | null);

        /**
         * @param choice 
         */
        set_choice(choice: InputChoice): void;
    }


    namespace InputFile {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::file-type": (pspec: GObject.ParamSpec) => void;
            "notify::value": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {
            file_type: Gio.FileType;
            fileType: Gio.FileType;
            value: Gio.File;
        }
    }

    /**
     * @gir-type Class
     */
    class InputFile extends Input {
        static $gtype: GObject.GType<InputFile>;

        // Properties
        /**
         * @default Gio.FileType.REGULAR
         */
        get file_type(): Gio.FileType;
        set file_type(val: Gio.FileType);

        /**
         * @default Gio.FileType.REGULAR
         */
        get fileType(): Gio.FileType;
        set fileType(val: Gio.FileType);

        get value(): Gio.File;
        set value(val: Gio.File);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputFile.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: string, subtitle: string, validator: (InputValidator | null), file_type: Gio.FileType, value: (Gio.File | null)): InputFile;

        // Signals
        /** @signal */
        connect<K extends keyof InputFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_value(): (Gio.File | null);

        get_file_type(): Gio.FileType;

        /**
         * @param value 
         */
        set_value(value: Gio.File): void;
    }


    namespace InputFont {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::monospace": (pspec: GObject.ParamSpec) => void;
            "notify::value": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {
            monospace: boolean;
            value: string;
        }
    }

    /**
     * @gir-type Class
     */
    class InputFont extends Input {
        static $gtype: GObject.GType<InputFont>;

        // Properties
        /**
         * @construct-only
         * @default false
         */
        get monospace(): boolean;

        /**
         * @default null
         */
        get value(): string;
        set value(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputFont.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputFont.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: string, subtitle: (string | null), validator: (InputValidator | null), value: (string | null), monospace: boolean): InputFont;

        // Signals
        /** @signal */
        connect<K extends keyof InputFont.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputFont.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputFont.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputFont.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputFont.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputFont.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_value(): string;

        get_monospace(): boolean;

        /**
         * @param value 
         */
        set_value(value: string): void;
    }


    namespace InputGroup {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class InputGroup extends Input {
        static $gtype: GObject.GType<InputGroup>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputGroup.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputGroup.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: (string | null), subtitle: (string | null), validator: (InputValidator | null), children: Input[]): InputGroup;

        // Signals
        /** @signal */
        connect<K extends keyof InputGroup.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputGroup.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputGroup.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputGroup.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputGroup.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputGroup.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        list_children(): Gio.ListModel;
    }


    namespace InputPassword {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::value": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {
            value: string;
        }
    }

    /**
     * @gir-type Class
     */
    class InputPassword extends Input {
        static $gtype: GObject.GType<InputPassword>;

        // Properties
        /**
         * @default null
         */
        get value(): string;
        set value(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputPassword.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputPassword.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: string, subtitle: string, validator: (InputValidator | null), value: string): InputPassword;

        // Signals
        /** @signal */
        connect<K extends keyof InputPassword.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputPassword.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputPassword.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputPassword.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputPassword.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputPassword.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_value(): string;

        /**
         * @param value 
         */
        set_value(value: string): void;
    }


    namespace InputSpin {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::lower": (pspec: GObject.ParamSpec) => void;
            "notify::n-digits": (pspec: GObject.ParamSpec) => void;
            "notify::upper": (pspec: GObject.ParamSpec) => void;
            "notify::value": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {
            lower: number;
            n_digits: number;
            nDigits: number;
            upper: number;
            value: number;
        }
    }

    /**
     * @gir-type Class
     */
    class InputSpin extends Input {
        static $gtype: GObject.GType<InputSpin>;

        // Properties
        /**
         * @construct-only
         * @default 0
         */
        get lower(): number;

        /**
         * @construct-only
         * @default 0
         */
        get n_digits(): number;

        /**
         * @construct-only
         * @default 0
         */
        get nDigits(): number;

        /**
         * @construct-only
         * @default 0
         */
        get upper(): number;

        /**
         * @default 0
         */
        get value(): number;
        set value(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputSpin.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputSpin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: string, subtitle: (string | null), validator: (InputValidator | null), value: number, lower: number, upper: number, n_digits: number): InputSpin;

        // Signals
        /** @signal */
        connect<K extends keyof InputSpin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputSpin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputSpin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputSpin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputSpin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputSpin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        get_lower(): number;

        get_n_digits(): number;

        get_upper(): number;

        get_value(): number;

        /**
         * @param value 
         */
        set_value(value: number): void;
    }


    namespace InputSwitch {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::value": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {
            value: boolean;
        }
    }

    /**
     * @gir-type Class
     */
    class InputSwitch extends Input {
        static $gtype: GObject.GType<InputSwitch>;

        // Properties
        /**
         * @default false
         */
        get value(): boolean;
        set value(val: boolean);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputSwitch.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputSwitch.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: string, subtitle: string, validator: (InputValidator | null), value: boolean): InputSwitch;

        // Signals
        /** @signal */
        connect<K extends keyof InputSwitch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputSwitch.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputSwitch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputSwitch.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputSwitch.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputSwitch.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        get_value(): boolean;

        /**
         * @param value 
         */
        set_value(value: boolean): void;
    }


    namespace InputText {
        // Signal signatures
        interface SignalSignatures extends Input.SignalSignatures {
            "notify::value": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::validator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Input.ConstructorProps {
            value: string;
        }
    }

    /**
     * @gir-type Class
     */
    class InputText extends Input {
        static $gtype: GObject.GType<InputText>;

        // Properties
        /**
         * @default null
         */
        get value(): string;
        set value(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputText.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputText.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](title: string, subtitle: (string | null), validator: (InputValidator | null), value: (string | null)): InputText;

        // Signals
        /** @signal */
        connect<K extends keyof InputText.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputText.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputText.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputText.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputText.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputText.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_value(): string;

        /**
         * @param value 
         */
        set_value(value: string): void;
    }


    namespace InputValidator {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    abstract class InputValidator extends GObject.Object {
        static $gtype: GObject.GType<InputValidator>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputValidator.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputValidator.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof InputValidator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputValidator.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputValidator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputValidator.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputValidator.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputValidator.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param input a {@link Foundry.Input}
         * @virtual
         */
        vfunc_validate(input: Input): Dex.Future;

        // Methods
        /**
         * @param input a {@link Foundry.Input}
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        validate(input: Input): Dex.Future;
    }


    namespace InputValidatorDelegate {
        // Signal signatures
        interface SignalSignatures extends InputValidator.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends InputValidator.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class InputValidatorDelegate extends InputValidator {
        static $gtype: GObject.GType<InputValidatorDelegate>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputValidatorDelegate.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputValidatorDelegate.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof InputValidatorDelegate.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputValidatorDelegate.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputValidatorDelegate.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputValidatorDelegate.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputValidatorDelegate.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputValidatorDelegate.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace InputValidatorRegex {
        // Signal signatures
        interface SignalSignatures extends InputValidator.SignalSignatures {
            "notify::regex": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends InputValidator.ConstructorProps {
            regex: GLib.Regex;
        }
    }

    /**
     * @gir-type Class
     */
    class InputValidatorRegex extends InputValidator {
        static $gtype: GObject.GType<InputValidatorRegex>;

        // Properties
        /**
         * @construct-only
         */
        get regex(): GLib.Regex;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: InputValidatorRegex.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<InputValidatorRegex.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](regex: GLib.Regex): InputValidatorRegex;

        // Signals
        /** @signal */
        connect<K extends keyof InputValidatorRegex.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputValidatorRegex.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof InputValidatorRegex.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, InputValidatorRegex.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof InputValidatorRegex.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<InputValidatorRegex.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_regex(): GLib.Regex;
    }


    namespace Intent {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Abstract base class for representing intents.
     * 
     * FoundryIntent provides the core interface for representing user intents
     * and actions within the development environment. It supports attribute
     * storage and provides a unified interface for intent handling across
     * different parts of the development environment.
     * @gir-type Class
     */
    abstract class Intent extends GObject.Object {
        static $gtype: GObject.GType<Intent>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Intent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Intent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Intent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Intent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Intent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Intent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Intent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Intent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the attribute value as a {@link GObject.Object}. The returned object is
         * owned by the intent and should not be unref'd.
         * @param attribute the attribute name
         * @returns the {@link GObject.Object}, or `null`   if not found or not an object
         */
        dup_attribute_object(attribute: string): null;

        /**
         * Gets the attribute value as a string. The returned string is owned
         * by the intent and should not be modified or freed.
         * @param attribute the attribute name
         * @returns the string value, or `null` if not found or not a string
         */
        dup_attribute_string(attribute: string): (string | null);

        /**
         * Gets the attribute value as a string. The returned string is owned
         * by the intent and should not be modified or freed.
         * @param attribute the attribute name
         * @returns the string value, or `null` if not found or not a string
         */
        dup_attribute_strv(attribute: string): string[];

        /**
         * Gets the attribute value as a boolean.
         * @param attribute the attribute name
         * @returns the boolean value, or `false` if not found or not a boolean
         */
        get_attribute_boolean(attribute: string): boolean;

        /**
         * Gets the {@link GObject.GType} of the attribute value.
         * @param attribute the attribute name
         * @returns the {@link GObject.GType} of the attribute, or `G_TYPE_INVALID` if not found
         */
        get_attribute_type(attribute: string): GObject.GType;

        /**
         * @param attribute 
         * @param value 
         */
        get_attribute_uint(attribute: string, value: number): boolean;

        /**
         * Gets the attribute value. The returned value is owned by the intent
         * and should not be modified or freed.
         * @param attribute the attribute name
         * @returns the {@link GObject.Value}, or `null` if not found
         */
        get_attribute_value(attribute: string): unknown;

        /**
         * Checks if the intent has an attribute with the given name.
         * @param attribute the attribute name to check
         * @returns `true` if the attribute exists, `false` otherwise
         */
        has_attribute(attribute: string): boolean;

        /**
         * Sets an attribute value for the intent. The value is copied.
         * @param attribute the attribute name
         * @param value the {@link GObject.Value} to set
         */
        set_attribute_value(attribute: string, value: (GObject.Value | any)): void;
    }


    namespace IntentHandler {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class for handling intents.
     * 
     * FoundryIntentHandler provides the core interface for processing intents
     * and handling specific actions within the development environment. Concrete
     * implementations handle specific intent types and provide specialized
     * functionality for different development workflows and user interactions.
     * @gir-type Class
     */
    abstract class IntentHandler extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<IntentHandler>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: IntentHandler.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<IntentHandler.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof IntentHandler.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, IntentHandler.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof IntentHandler.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, IntentHandler.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof IntentHandler.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<IntentHandler.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Dispatches the intent.
         * 
         * If this handler cannot handle this intent type it just reject
         * with `G_IO_ERROR` and `G_IO_ERROR_NOT_SUPPORTED`.
         * @param intent 
         * @virtual
         */
        vfunc_dispatch(intent: Intent): Dex.Future;

        // Methods
        /**
         * Dispatches the intent.
         * 
         * If this handler cannot handle this intent type it just reject
         * with `G_IO_ERROR` and `G_IO_ERROR_NOT_SUPPORTED`.
         * @param intent 
         * @returns a {@link Dex.Future} that resolves to any   value if successful; otherwise rejects with error.
         */
        dispatch(intent: Intent): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace IntentManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * The intent manager provides a generic mechanism to handle intents
     * within an application.
     * 
     * This could include opening a file with the OpeFileIntent, or other
     * custom intents.
     * 
     * {@link Foundry.IntentHandler} may be registered to handle these
     * intents or ignore them for the next handler to handle them.
     * @gir-type Class
     * @since 1.1
     */
    class IntentManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<IntentManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: IntentManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<IntentManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof IntentManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, IntentManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof IntentManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, IntentManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof IntentManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<IntentManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Dispatch the intent to the first handler which can handle it.
         * @param intent a {@link Foundry.Intent}
         * @returns a {@link Dex.Future} that resolves to any value   if sucessful or rejects with error.
         */
        dispatch(intent: Intent): Dex.Future;
    }


    namespace JsonListLlmResource {
        // Signal signatures
        interface SignalSignatures extends LlmResource.SignalSignatures {
            "notify::content-type": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends LlmResource.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class JsonListLlmResource extends LlmResource {
        static $gtype: GObject.GType<JsonListLlmResource>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: JsonListLlmResource.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<JsonListLlmResource.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](name: string, uri: string, description: string, model: Gio.ListModel): JsonListLlmResource;

        // Signals
        /** @signal */
        connect<K extends keyof JsonListLlmResource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JsonListLlmResource.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof JsonListLlmResource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JsonListLlmResource.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof JsonListLlmResource.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<JsonListLlmResource.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace JsonLlmResource {
        // Signal signatures
        interface SignalSignatures extends LlmResource.SignalSignatures {
            "notify::node": (pspec: GObject.ParamSpec) => void;
            "notify::content-type": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends LlmResource.ConstructorProps {
            node: (Json.Node | null);
        }
    }

    /**
     * @gir-type Class
     */
    class JsonLlmResource extends LlmResource {
        static $gtype: GObject.GType<JsonLlmResource>;

        // Properties
        get node(): (Json.Node | null);
        set node(val: (Json.Node | null));

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: JsonLlmResource.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<JsonLlmResource.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](name: string, uri: string, description: string, node: Json.Node): JsonLlmResource;

        // Signals
        /** @signal */
        connect<K extends keyof JsonLlmResource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JsonLlmResource.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof JsonLlmResource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JsonLlmResource.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof JsonLlmResource.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<JsonLlmResource.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        get_node(): (Json.Node | null);

        /**
         * @param node 
         */
        set_node(node: (Json.Node | null)): void;

        /**
         * @param node 
         */
        take_node(node: (Json.Node | null)): void;
    }


    namespace KeyRotator {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class for rotating API keys.
     * 
     * FoundryKeyRotator provides an interface for rotating API keys
     * associated with a host and service name. Subclasses implement
     * the actual rotation logic for specific services or providers.
     * @gir-type Class
     * @since 1.1
     */
    abstract class KeyRotator extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<KeyRotator>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: KeyRotator.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<KeyRotator.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof KeyRotator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, KeyRotator.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof KeyRotator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, KeyRotator.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof KeyRotator.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<KeyRotator.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Checks if the key rotator can rotate the API key for the given
         * host and service name.
         * @param host the host name for the API key
         * @param service_name the service name for the API key
         * @param secret the current secret value
         * @virtual
         */
        vfunc_can_rotate(host: string, service_name: string, secret: string): boolean;

        /**
         * Checks when the key expires by querying the service.
         * @param host the hostname
         * @param service_name the name of the service such as "gitlab"
         * @param secret the current secret
         * @virtual
         */
        vfunc_check_expires_at(host: string, service_name: string, secret: string): Dex.Future;

        /**
         * Rotates the API key for the given host and service name.
         * @param host the host name for the API key
         * @param service_name the service name for the API key
         * @param secret the current secret value
         * @param expire_at when the new key should expire, or `null`
         * @virtual
         */
        vfunc_rotate(host: string, service_name: string, secret: string, expire_at: (GLib.DateTime | null)): Dex.Future;

        // Methods
        /**
         * Checks if the key rotator can rotate the API key for the given
         * host and service name.
         * @param host the host name for the API key
         * @param service_name the service name for the API key
         * @param secret the current secret value
         * @returns `true` if the key can be rotated, `false` otherwise
         */
        can_rotate(host: string, service_name: string, secret: string): boolean;

        /**
         * Checks when the key expires by querying the service.
         * @param host the hostname
         * @param service_name the name of the service such as "gitlab"
         * @param secret the current secret
         * @returns a {@link Dex.Future} that resolves to   a GDateTime or rejects with error.
         */
        check_expires_at(host: string, service_name: string, secret: string): Dex.Future;

        /**
         * Rotates the API key for the given host and service name.
         * @param host the host name for the API key
         * @param service_name the service name for the API key
         * @param secret the current secret value
         * @param expire_at when the new key should expire, or `null`
         * @returns a {@link Dex.Future} that resolves to a string   containing the replacement secret, or rejects with an error
         */
        rotate(host: string, service_name: string, secret: string, expire_at: (GLib.DateTime | null)): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Language {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::meson-id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
            meson_id: (string | null);
            mesonId: (string | null);
            name: string;
        }
    }

    /**
     * Represents a programming language or syntax.
     * @gir-type Class
     */
    class Language extends GObject.Object {
        static $gtype: GObject.GType<Language>;

        // Properties
        /**
         * @construct-only
         * @default null
         */
        get id(): string;

        /**
         * @construct-only
         * @default null
         */
        get meson_id(): (string | null);

        /**
         * @construct-only
         * @default null
         */
        get mesonId(): (string | null);

        /**
         * @construct-only
         * @default null
         */
        get name(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Language.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Language.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](id: string, name: string, meson_id: string): Language;

        // Signals
        /** @signal */
        connect<K extends keyof Language.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Language.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Language.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Language.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Language.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Language.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * @param id the id of the language
         */
        static find(id: string): Language;

        static list_all(): Gio.ListModel;

        // Methods
        /**
         * @param other 
         */
        equal(other: Language): boolean;

        get_id(): string;

        get_meson_id(): (string | null);

        get_name(): string;

        hash(): number;
    }


    namespace LanguageGuesser {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class that plugins can implement to guess languages based
     * on file content.
     * @gir-type Class
     */
    abstract class LanguageGuesser extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<LanguageGuesser>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LanguageGuesser.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LanguageGuesser.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LanguageGuesser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LanguageGuesser.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LanguageGuesser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LanguageGuesser.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LanguageGuesser.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LanguageGuesser.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Guess the language for a file, content_type, or contents.
         * 
         * One of `file`, `content_type`, or `contents` must be set.
         * @param file a {@link Gio.File} or `null`
         * @param content_type a content-type as a string or `null`
         * @param contents a {@link GLib.Bytes} of file contents or `null`
         * @virtual
         */
        vfunc_guess(file: (Gio.File | null), content_type: (string | null), contents: (GLib.Bytes | null)): Dex.Future;

        /**
         * Gets a list of known languages by their language identifier.
         * @virtual
         */
        vfunc_list_languages(): string[];

        // Methods
        /**
         * Guess the language for a file, content_type, or contents.
         * 
         * One of `file`, `content_type`, or `contents` must be set.
         * @param file a {@link Gio.File} or `null`
         * @param content_type a content-type as a string or `null`
         * @param contents a {@link GLib.Bytes} of file contents or `null`
         * @returns a {@link Dex.Future} that resolves to   a string containing the source code language or rejects with   a new {@link GLib.Error}.
         */
        guess(file: (Gio.File | null), content_type: (string | null), contents: (GLib.Bytes | null)): Dex.Future;

        /**
         * Gets a list of known languages by their language identifier.
         */
        list_languages(): string[];

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace License {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::snippet-text": (pspec: GObject.ParamSpec) => void;
            "notify::snippet-text-bytes": (pspec: GObject.ParamSpec) => void;
            "notify::text": (pspec: GObject.ParamSpec) => void;
            "notify::text-bytes": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
            snippet_text: string;
            snippetText: string;
            snippet_text_bytes: (GLib.Bytes | Uint8Array);
            snippetTextBytes: (GLib.Bytes | Uint8Array);
            text: string;
            text_bytes: (GLib.Bytes | Uint8Array);
            textBytes: (GLib.Bytes | Uint8Array);
            title: string;
        }
    }

    /**
     * Represents a source code license.
     * @gir-type Class
     */
    class License extends GObject.Object {
        static $gtype: GObject.GType<License>;

        // Properties
        /**
         * @construct-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default null
         */
        get snippet_text(): string;

        /**
         * @read-only
         * @default null
         */
        get snippetText(): string;

        /**
         * @construct-only
         */
        get snippet_text_bytes(): GLib.Bytes;

        /**
         * @construct-only
         */
        get snippetTextBytes(): GLib.Bytes;

        /**
         * @read-only
         * @default null
         */
        get text(): string;

        /**
         * @construct-only
         */
        get text_bytes(): GLib.Bytes;

        /**
         * @construct-only
         */
        get textBytes(): GLib.Bytes;

        /**
         * @construct-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: License.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<License.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof License.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, License.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof License.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, License.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof License.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<License.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * @param id the SPDX identifier
         */
        static find(id: string): (License | null);

        /**
         * Get a {@link Gio.ListModel} of all {@link Foundry.License}.
         */
        static list_all(): Gio.ListModel;

        // Methods
        dup_id(): string;

        dup_snippet_text(): (GLib.Bytes | null);

        dup_text(): (GLib.Bytes | null);

        dup_title(): string;
    }


    namespace LinkedPipelineStage {
        // Signal signatures
        interface SignalSignatures extends BuildStage.SignalSignatures {
            "notify::linked-phase": (pspec: GObject.ParamSpec) => void;
            "notify::linked-pipeline": (pspec: GObject.ParamSpec) => void;
            "notify::phase": (pspec: GObject.ParamSpec) => void;
            "notify::completed": (pspec: GObject.ParamSpec) => void;
            "notify::kind": (pspec: GObject.ParamSpec) => void;
            "notify::pipeline": (pspec: GObject.ParamSpec) => void;
            "notify::priority": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends BuildStage.ConstructorProps, Json.Serializable.ConstructorProps {
            linked_phase: BuildPipelinePhase;
            linkedPhase: BuildPipelinePhase;
            linked_pipeline: BuildPipeline;
            linkedPipeline: BuildPipeline;
            phase: BuildPipelinePhase;
        }
    }

    /**
     * A pipeline stage that will execute another pipline before continuing
     * the current pipeline.
     * @gir-type Class
     */
    class LinkedPipelineStage extends BuildStage implements Json.Serializable {
        static $gtype: GObject.GType<LinkedPipelineStage>;

        // Properties
        /**
         * @construct-only
         * @default Foundry.BuildPipelinePhase.NONE
         */
        get linked_phase(): BuildPipelinePhase;

        /**
         * @construct-only
         * @default Foundry.BuildPipelinePhase.NONE
         */
        get linkedPhase(): BuildPipelinePhase;

        /**
         * @construct-only
         */
        get linked_pipeline(): BuildPipeline;

        /**
         * @construct-only
         */
        get linkedPipeline(): BuildPipeline;

        /**
         * @construct-only
         * @default Foundry.BuildPipelinePhase.NONE
         */
        get phase(): BuildPipelinePhase;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LinkedPipelineStage.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LinkedPipelineStage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, linked_pipeline: BuildPipeline, target: BuildPipelinePhase): LinkedPipelineStage;

        static new_full(context: Context, linked_pipeline: BuildPipeline, phase: BuildPipelinePhase, linked_phase: BuildPipelinePhase): LinkedPipelineStage;

        // Signals
        /** @signal */
        connect<K extends keyof LinkedPipelineStage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LinkedPipelineStage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LinkedPipelineStage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LinkedPipelineStage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LinkedPipelineStage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LinkedPipelineStage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace LlmCompletion {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    abstract class LlmCompletion extends GObject.Object {
        static $gtype: GObject.GType<LlmCompletion>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmCompletion.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmCompletion.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmCompletion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmCompletion.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmCompletion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmCompletion.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmCompletion.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmCompletion.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_next_chunk(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_when_finished(): Dex.Future;

        // Methods
        /**
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.LlmCompletionChunk} or rejects with error.
         */
        next_chunk(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to   any value when the LLM completion has finished generating.
         */
        when_finished(): Dex.Future;
    }


    namespace LlmCompletionChunk {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::is-done": (pspec: GObject.ParamSpec) => void;
            "notify::text": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            is_done: boolean;
            isDone: boolean;
            text: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class LlmCompletionChunk extends GObject.Object {
        static $gtype: GObject.GType<LlmCompletionChunk>;

        // Properties
        /**
         * @read-only
         * @default true
         */
        get is_done(): boolean;

        /**
         * @read-only
         * @default true
         */
        get isDone(): boolean;

        /**
         * @read-only
         * @default null
         */
        get text(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmCompletionChunk.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmCompletionChunk.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmCompletionChunk.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmCompletionChunk.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmCompletionChunk.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmCompletionChunk.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmCompletionChunk.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmCompletionChunk.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_text(): (string | null);

        /**
         * @virtual
         */
        vfunc_is_done(): boolean;

        // Methods
        dup_text(): (string | null);
    }


    namespace LlmConversation {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::is-busy": (pspec: GObject.ParamSpec) => void;
            "notify::tools": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            is_busy: boolean;
            isBusy: boolean;
            tools: Gio.ListModel;
        }
    }

    /**
     * Abstract base class for managing conversations with large language models.
     * 
     * FoundryLlmConversation provides the core interface for interacting with
     * language models including message management, tool integration, and
     * conversation state tracking. Concrete implementations handle specific
     * LLM providers and maintain conversation context for development assistance.
     * @gir-type Class
     */
    abstract class LlmConversation extends GObject.Object {
        static $gtype: GObject.GType<LlmConversation>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get is_busy(): boolean;

        /**
         * @read-only
         * @default false
         */
        get isBusy(): boolean;

        get tools(): Gio.ListModel;
        set tools(val: Gio.ListModel);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmConversation.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmConversation.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmConversation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmConversation.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmConversation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmConversation.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmConversation.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmConversation.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Adds context to the conversation.
         * 
         * Generally this applies to the conversation right after the system prompt.
         * @param context 
         * @virtual
         */
        vfunc_add_context(context: string): Dex.Future;

        /**
         * @param call a {@link Foundry.LlmToolCall}
         * @virtual
         */
        vfunc_call(call: LlmToolCall): Dex.Future;

        /**
         * @virtual
         */
        vfunc_is_busy(): boolean;

        /**
         * List the available history of the conversation.
         * @virtual
         */
        vfunc_list_history(): (Gio.ListModel | null);

        /**
         * Reset the conversation to the initial state.
         * @virtual
         */
        vfunc_reset(): void;

        /**
         * Send multiple messages together.
         * 
         * The length of `roles` must be the same as `messages`.
         * @param roles 
         * @param messages 
         * @virtual
         */
        vfunc_send_messages(roles: string, messages: string): Dex.Future;

        // Methods
        /**
         * Adds context to the conversation.
         * 
         * Generally this applies to the conversation right after the system prompt.
         * @param context 
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        add_context(context: string): Dex.Future;

        /**
         * @param call a {@link Foundry.LlmToolCall}
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.LlmMessage} or rejects with error.
         */
        call(call: LlmToolCall): Dex.Future;

        /**
         * Lists tools made available to the conversation.
         * @returns a {@link Gio.ListModel} of {@link Foundry.LlmTool}
         */
        dup_tools(): Gio.ListModel;

        /**
         * List the available history of the conversation.
         * @returns a {@link Gio.ListModel} of   {@link Foundry.LlmMessage}.
         */
        list_history(): (Gio.ListModel | null);

        /**
         * Reset the conversation to the initial state.
         */
        reset(): void;

        /**
         * The role should generally be something like "system", "user", "assistant",
         * or "tool".
         * @param role the role of the message sender
         * @param message the message to be sent
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        send_message(role: string, message: string): Dex.Future;

        /**
         * Send multiple messages together.
         * 
         * The length of `roles` must be the same as `messages`.
         * @param roles 
         * @param messages 
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        send_messages(roles: string, messages: string): Dex.Future;

        /**
         * Set the tools that are allowed to be used by the model.
         * @param tools a list model of {@link Foundry.LlmTool}
         */
        set_tools(tools: Gio.ListModel): void;
    }


    namespace LlmManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class LlmManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<LlmManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Finds the first model which matches `name`.
         * @param name the name of the model
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.LlmModel} or rejects with error.
         */
        find_model(name: string): Dex.Future;

        /**
         * Finds the first resource which matches `uri`.
         * @param uri the URI of the resource
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.LlmResource} or rejects with error.
         */
        find_resource(uri: string): Dex.Future;

        /**
         * List models from all providers.
         * 
         * The resulting {@link Gio.ListModel} is asynchronously populated.
         * If you want to be sure that all providers have completed populating,
         * you may await completion by calling {@link Foundry.list_model_await}.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.LlmModel}.
         */
        list_models(): Dex.Future;

        /**
         * List resources from all providers.
         * 
         * The resulting {@link Gio.ListModel} is asynchronously populated.
         * If you want to be sure that all providers have completed populating,
         * you may await completion by calling {@link Foundry.list_model_await}.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.LlmResource}.
         */
        list_resources(): Dex.Future;

        /**
         * The resulting {@link Gio.ListModel} is asynchronously populated.
         * If you want to be sure that all providers have completed populating,
         * you may await completion by calling {@link Foundry.list_model_await}.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.LlmTool}.
         */
        list_tools(): Dex.Future;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace LlmMessage {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::content": (pspec: GObject.ParamSpec) => void;
            "notify::role": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            content: string;
            role: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class LlmMessage extends GObject.Object {
        static $gtype: GObject.GType<LlmMessage>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get content(): string;

        /**
         * @read-only
         * @default null
         */
        get role(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmMessage.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmMessage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmMessage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmMessage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmMessage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmMessage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * The content of the message.
         * @virtual
         */
        vfunc_dup_content(): string;

        /**
         * The role of the message such as "system", "user", "assistant", or "tool".
         * @virtual
         */
        vfunc_dup_role(): string;

        /**
         * @virtual
         */
        vfunc_has_tool_call(): boolean;

        /**
         * @virtual
         */
        vfunc_list_tool_calls(): (Gio.ListModel | null);

        // Methods
        /**
         * The content of the message.
         */
        dup_content(): string;

        /**
         * The role of the message such as "system", "user", "assistant", or "tool".
         * @returns the role as a string
         */
        dup_role(): string;

        has_tool_call(): boolean;

        /**
         * @returns a {@link Gio.ListModel} of   {@link Foundry.LlmToolCall} or `null`
         */
        list_tool_calls(): (Gio.ListModel | null);
    }


    namespace LlmModel {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::digest": (pspec: GObject.ParamSpec) => void;
            "notify::metered": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            digest: string;
            metered: boolean;
            name: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class LlmModel extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<LlmModel>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get digest(): string;

        /**
         * @read-only
         * @default false
         */
        get metered(): boolean;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmModel.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmModel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmModel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmModel.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmModel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmModel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmModel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmModel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Start a new conversation with the model.
         * @param system the system prompt for the chat
         * @virtual
         */
        vfunc_chat(system: (string | null)): Dex.Future;

        /**
         * Requests completion using `roles` and `messages`.
         * 
         * Each element of `roles` corresponds to the element at the same index
         * of `messages`.
         * 
         * The length of `messages` and `roles` must be the same.
         * @param roles the roles for each message
         * @param messages the message content
         * @virtual
         */
        vfunc_complete(roles: string, messages: string): Dex.Future;

        /**
         * @virtual
         */
        vfunc_dup_digest(): string;

        /**
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * Gets the "metered" property.
         * @virtual
         */
        vfunc_is_metered(): boolean;

        // Methods
        /**
         * Start a new conversation with the model.
         * @param system the system prompt for the chat
         * @returns a {@link Dex.Future} that resolves   to a {@link Foundry.LlmConversation} or rejects with error.
         */
        chat(system: (string | null)): Dex.Future;

        /**
         * Requests completion using `roles` and `messages`.
         * 
         * Each element of `roles` corresponds to the element at the same index
         * of `messages`.
         * 
         * The length of `messages` and `roles` must be the same.
         * @param roles the roles for each message
         * @param messages the message content
         * @returns a {@link Dex.Future} that resolves   to a {@link Foundry.LlmCompletion}.
         */
        complete(roles: string, messages: string): Dex.Future;

        dup_digest(): string;

        dup_name(): string;

        /**
         * Gets the "metered" property.
         * @returns `true` if the model may be metered.
         */
        is_metered(): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace LlmProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class LlmProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<LlmProvider>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Ollama".
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * List the models available from the provider.
         * @virtual
         */
        vfunc_list_models(): Dex.Future;

        /**
         * List resources that may be made available to the model.
         * @virtual
         */
        vfunc_list_resources(): Dex.Future;

        /**
         * List the tools available from the provider.
         * @virtual
         */
        vfunc_list_tools(): Dex.Future;

        // Methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Ollama".
         * @returns the name of the provider
         */
        dup_name(): string;

        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * List the models available from the provider.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.LlmModel}.
         */
        list_models(): Dex.Future;

        /**
         * List resources that may be made available to the model.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.LlmResource}.
         */
        list_resources(): Dex.Future;

        /**
         * List the tools available from the provider.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.LlmTool}.
         */
        list_tools(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace LlmResource {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * Emitted when the resource has changed.
             * 
             * This signal is emitted when the resource's properties or content
             * have been modified. Subclasses should call {@link Foundry.LlmResource.emit_changed}
             * to emit this signal.
             * @signal
             * @since 1.1
             * @run-last
             */
            changed: () => void;
            "notify::content-type": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            content_type: string;
            contentType: string;
            description: string;
            name: string;
            uri: string;
        }
    }

    /**
     * Abstract base class for LLM resources.
     * 
     * A resource represents external content that can be provided to an LLM,
     * such as files, URLs, or other data sources. Resources have metadata
     * including a URI, name, description, and content type.
     * @gir-type Class
     */
    abstract class LlmResource extends GObject.Object {
        static $gtype: GObject.GType<LlmResource>;

        // Properties
        /**
         * The content type of the resource.
         * 
         * This indicates the format of the resource data, such as "text/plain",
         * "application/json", or "image/png".
         * @read-only
         * @default null
         */
        get content_type(): string;

        /**
         * The content type of the resource.
         * 
         * This indicates the format of the resource data, such as "text/plain",
         * "application/json", or "image/png".
         * @read-only
         * @default null
         */
        get contentType(): string;

        /**
         * The description of the resource.
         * 
         * A human-readable description providing additional context about
         * the resource and its contents.
         * @read-only
         * @default null
         */
        get description(): string;

        /**
         * The name of the resource.
         * 
         * A human-readable name for the resource, typically used for display
         * purposes in the UI.
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * The URI of the resource.
         * 
         * This identifies the location or source of the resource, which may
         * be a file path, URL, or other identifier depending on the resource type.
         * @read-only
         * @default null
         */
        get uri(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmResource.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmResource.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmResource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmResource.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmResource.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmResource.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmResource.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmResource.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets the content type for the resource.
         * @virtual
         */
        vfunc_dup_content_type(): (string | null);

        /**
         * Gets the description for the resource.
         * @virtual
         */
        vfunc_dup_description(): (string | null);

        /**
         * Gets the name for the resource.
         * @virtual
         */
        vfunc_dup_name(): (string | null);

        /**
         * Gets the URI for the resource.
         * @virtual
         */
        vfunc_dup_uri(): (string | null);

        /**
         * Loads the bytes for the resource.
         * 
         * This method asynchronously loads the resource content and returns
         * a future that resolves to a {@link GLib.Bytes} containing the data.
         * @virtual
         */
        vfunc_load_bytes(): Dex.Future;

        /**
         * If the content_type is "application/json" then the resource
         * may implement this function to provide a JsonNode instead
         * of a serialized byte buffer. This is helpful in situations
         * where the protocol can pass structured information for JSON
         * such as Model Context Protocol.
         * @virtual
         */
        vfunc_load_json(): Dex.Future;

        // Methods
        /**
         * Gets the content type for the resource.
         * @returns a newly allocated string containing   the content type, or `null` if not available
         */
        dup_content_type(): (string | null);

        /**
         * Gets the description for the resource.
         * @returns a newly allocated string containing   the description, or `null` if not available
         */
        dup_description(): (string | null);

        /**
         * Gets the name for the resource.
         * @returns a newly allocated string containing   the name, or `null` if not available
         */
        dup_name(): (string | null);

        /**
         * Gets the URI for the resource.
         * @returns a newly allocated string containing   the URI, or `null` if not available
         */
        dup_uri(): (string | null);

        /**
         * Emits the "changed" signal.
         * 
         * Subclasses should call this function when the resource's properties
         * or content have been modified to notify listeners of the change.
         */
        emit_changed(): void;

        /**
         * Loads the bytes for the resource.
         * 
         * This method asynchronously loads the resource content and returns
         * a future that resolves to a {@link GLib.Bytes} containing the data.
         * @returns a {@link Dex.Future} that resolves to a   {@link GLib.Bytes}, or rejects with error.
         */
        load_bytes(): Dex.Future;

        /**
         * If the content_type is "application/json" then the resource
         * may implement this function to provide a JsonNode instead
         * of a serialized byte buffer. This is helpful in situations
         * where the protocol can pass structured information for JSON
         * such as Model Context Protocol.
         * @returns a {@link Dex.Future} that resolves to   a {@link Json.Node} or rejects with error.
         */
        load_json(): Dex.Future;
    }


    namespace LlmTool {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            description: string;
            name: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class LlmTool extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<LlmTool>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get description(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmTool.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmTool.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmTool.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmTool.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmTool.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmTool.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmTool.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmTool.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * @param pspec the parameter to add
         */
        static add_parameter(pspec: GObject.ParamSpec): void;

        // Virtual methods
        /**
         * Calls the tool.
         * 
         * `arguments` should be an array of initialized {@link GObject.Value} which contain
         * values set matching each of the parameters specified by
         * {@link Foundry.LlmTool.list_parameters} in the same order.
         * @param _arguments 
         * @virtual
         */
        vfunc_call(_arguments: unknown[]): Dex.Future;

        /**
         * Gets the "description" of a tool which may be provided to the model.
         * 
         * This should describe what the function does.
         * @virtual
         */
        vfunc_dup_description(): (string | null);

        /**
         * Gets the "name" of tool which may be provided to the model.
         * 
         * This is often a descriptive function name like "getWeather".
         * @virtual
         */
        vfunc_dup_name(): (string | null);

        // Methods
        /**
         * Calls the tool.
         * 
         * `arguments` should be an array of initialized {@link GObject.Value} which contain
         * values set matching each of the parameters specified by
         * {@link Foundry.LlmTool.list_parameters} in the same order.
         * @param _arguments 
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.LlmMessage} or rejects with error.
         */
        call(_arguments: (GObject.Value | any)[]): Dex.Future;

        /**
         * Gets the "description" of a tool which may be provided to the model.
         * 
         * This should describe what the function does.
         */
        dup_description(): (string | null);

        /**
         * Gets the "name" of tool which may be provided to the model.
         * 
         * This is often a descriptive function name like "getWeather".
         */
        dup_name(): (string | null);

        /**
         * Gets a list of parmaeters for the tool which must be
         * supplied when calling {@link Foundry.LlmTool.call}.
         */
        list_parameters(): (GObject.ParamSpec[] | null);

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace LlmToolCall {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::is-callable": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            is_callable: boolean;
            isCallable: boolean;
            subtitle: string;
            title: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class LlmToolCall extends GObject.Object {
        static $gtype: GObject.GType<LlmToolCall>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get is_callable(): boolean;

        /**
         * @read-only
         * @default false
         */
        get isCallable(): boolean;

        /**
         * @read-only
         * @default null
         */
        get subtitle(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LlmToolCall.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LlmToolCall.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LlmToolCall.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmToolCall.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LlmToolCall.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LlmToolCall.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LlmToolCall.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LlmToolCall.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Confirms that the tool call is authorized and performs it.
         * 
         * The result should be a {@link Foundry.LlmMessage} that may be sent
         * back to the model to continue the conversation.
         * @virtual
         */
        vfunc_confirm(): Dex.Future;

        /**
         * Denies that the tool call is authorized and returns a message of
         * failure to be sent back to the model.
         * @virtual
         */
        vfunc_deny(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_dup_subtitle(): string;

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        /**
         * @virtual
         */
        vfunc_is_callable(): boolean;

        // Methods
        /**
         * Confirms that the tool call is authorized and performs it.
         * 
         * The result should be a {@link Foundry.LlmMessage} that may be sent
         * back to the model to continue the conversation.
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.LlmMessage} or rejects with error.
         */
        confirm(): Dex.Future;

        /**
         * Denies that the tool call is authorized and returns a message of
         * failure to be sent back to the model.
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.LlmMessage} or rejects with error.
         */
        deny(): Dex.Future;

        dup_subtitle(): string;

        dup_title(): string;
    }


    namespace LocalDevice {
        // Signal signatures
        interface SignalSignatures extends Device.SignalSignatures {
            "notify::active": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Device.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class LocalDevice extends Device implements Json.Serializable {
        static $gtype: GObject.GType<LocalDevice>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LocalDevice.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LocalDevice.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context): LocalDevice;

        static new_full(context: Context, id: string, title: string, triplet: Triplet): LocalDevice;

        // Signals
        /** @signal */
        connect<K extends keyof LocalDevice.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LocalDevice.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LocalDevice.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LocalDevice.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LocalDevice.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LocalDevice.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace LogManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class LogManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<LogManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LogManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LogManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LogManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LogManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LogManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LogManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LogManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LogManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Remove all log entries from the list model
         */
        remove_all(): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace LogMessage {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::created-at": (pspec: GObject.ParamSpec) => void;
            "notify::domain": (pspec: GObject.ParamSpec) => void;
            "notify::message": (pspec: GObject.ParamSpec) => void;
            "notify::severity": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            created_at: GLib.DateTime;
            createdAt: GLib.DateTime;
            domain: string;
            message: string;
            severity: number;
        }
    }

    /**
     * @gir-type Class
     */
    class LogMessage extends GObject.Object {
        static $gtype: GObject.GType<LogMessage>;

        // Properties
        /**
         * @construct-only
         */
        get created_at(): GLib.DateTime;

        /**
         * @construct-only
         */
        get createdAt(): GLib.DateTime;

        /**
         * @construct-only
         * @default null
         */
        get domain(): string;

        /**
         * @construct-only
         * @default null
         */
        get message(): string;

        /**
         * @construct-only
         * @default 0
         */
        get severity(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LogMessage.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LogMessage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LogMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LogMessage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LogMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LogMessage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LogMessage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LogMessage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the time the log item was created.
         * @returns a {@link GLib.DateTime}
         */
        dup_created_at(): GLib.DateTime;

        /**
         * Gets the log message.
         * @returns A string containing the log message
         */
        dup_message(): string;

        /**
         * Get the domain for the log item.
         */
        get_domain(): string;

        /**
         * Gets the log item severity.
         */
        get_severity(): GLib.LogLevelFlags;
    }


    namespace LspClient {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::io-stream": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::subprocess": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            io_stream: Gio.IOStream;
            ioStream: Gio.IOStream;
            provider: LspProvider;
            subprocess: Gio.Subprocess;
        }
    }

    /**
     * @gir-type Class
     */
    class LspClient extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<LspClient>;

        // Properties
        /**
         * @construct-only
         */
        set io_stream(val: Gio.IOStream);

        /**
         * @construct-only
         */
        set ioStream(val: Gio.IOStream);

        /**
         * @construct-only
         */
        get provider(): LspProvider;

        /**
         * @construct-only
         */
        get subprocess(): Gio.Subprocess;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LspClient.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LspClient.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, stream: Gio.IOStream, subprocess: Gio.Subprocess): LspClient;

        static new_with_provider(context: Context, stream: Gio.IOStream, subprocess: Gio.Subprocess, provider: LspProvider): LspClient;

        // Signals
        /** @signal */
        connect<K extends keyof LspClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspClient.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LspClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspClient.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LspClient.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LspClient.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Await completion of the client subprocess.
         * @returns a {@link Dex.Future} that resolves   when the subprocess has exited or rejects with error.
         */
        ["await"](): Dex.Future;

        /**
         * If `params` is floating, the reference will be consumed.
         * @param method the method name to call
         * @param params parameters for the method call
         * @returns a {@link Dex.Future} that resolves when   a reply is received for the method call.
         */
        call(method: string, params: (Json.Node | null)): Dex.Future;

        /**
         * If `params` is floating, the reference will be consumed.
         * @param method the method name to call
         * @param params parameters for the notification
         * @returns a {@link Dex.Future} that resolves when   the notification has been sent.
         */
        notify(method: string, params: (Json.Node | null)): Dex.Future;

        /**
         * @param args 
         */
    // Conflicted with GObject.Object.notify
        notify(...args: never[]): any;

        /**
         * Queries the servers capabilities.
         * @returns a {@link Dex.Future} that resolves when   the query completes or fails.
         */
        query_capabilities(): Dex.Future;

        /**
         * @param language_id 
         */
        supports_language(language_id: string): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace LspCompletionProposal {
        // Signal signatures
        interface SignalSignatures extends CompletionProposal.SignalSignatures {
            "notify::after": (pspec: GObject.ParamSpec) => void;
            "notify::comment": (pspec: GObject.ParamSpec) => void;
            "notify::details": (pspec: GObject.ParamSpec) => void;
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::typed-text": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends CompletionProposal.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class LspCompletionProposal extends CompletionProposal {
        static $gtype: GObject.GType<LspCompletionProposal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LspCompletionProposal.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LspCompletionProposal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LspCompletionProposal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspCompletionProposal.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LspCompletionProposal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspCompletionProposal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LspCompletionProposal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LspCompletionProposal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace LspCompletionProvider {
        // Signal signatures
        interface SignalSignatures extends CompletionProvider.SignalSignatures {
            "notify::document": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-ifo": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends CompletionProvider.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class LspCompletionProvider extends CompletionProvider implements Json.Serializable {
        static $gtype: GObject.GType<LspCompletionProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LspCompletionProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LspCompletionProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LspCompletionProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspCompletionProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LspCompletionProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspCompletionProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LspCompletionProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LspCompletionProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace LspManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class LspManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<LspManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LspManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LspManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LspManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LspManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LspManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LspManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Loads a {@link Foundry.LspClient} for the `language_id`.
         * 
         * If an existing client is already created for this language,
         * that client will be returned.
         * @param language_id 
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.LspClient}.
         */
        load_client(language_id: string): Dex.Future;

        /**
         * @param language_id the language identifier
         * @returns a {@link Foundry.Settings}
         */
        load_language_settings(language_id: string): Settings;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace LspProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::server": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
            server: LspServer;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class LspProvider<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<LspProvider>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        get server(): LspServer;
        set server(val: LspServer);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LspProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LspProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LspProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LspProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LspProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LspProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_initialization_options(): (Json.Node | null);

        // Methods
        dup_initialization_options(): (Json.Node | null);

        /**
         * @returns a {@link Peas.PluginInfo}
         */
        dup_plugin_info(): Peas.PluginInfo;

        /**
         * @returns a {@link Foundry.LspServer} or `null`
         */
        dup_server(): (LspServer | null);

        /**
         * @param server 
         */
        set_server(server: LspServer): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace LspServer {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::languages": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            languages: string[];
            name: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class LspServer extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<LspServer>;

        // Properties
        /**
         * @read-only
         */
        get languages(): string[];

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LspServer.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<LspServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof LspServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspServer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LspServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LspServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LspServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LspServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a string array of languages supported by the LSP.
         * @virtual
         */
        vfunc_dup_languages(): string[];

        /**
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param launcher 
         * @virtual
         */
        vfunc_prepare(pipeline: (BuildPipeline | null), launcher: ProcessLauncher): Dex.Future;

        /**
         * @param language_id 
         * @virtual
         */
        vfunc_supports_language(language_id: string): boolean;

        // Methods
        /**
         * Gets a string array of languages supported by the LSP.
         * @returns a string array of languages
         */
        dup_languages(): string[];

        dup_name(): string;

        /**
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param launcher 
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.LspClient} or rejects with error
         */
        prepare(pipeline: (BuildPipeline | null), launcher: ProcessLauncher): Dex.Future;

        /**
         * @param language_id 
         */
        supports_language(language_id: string): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Markup {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::contents": (pspec: GObject.ParamSpec) => void;
            "notify::kind": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            contents: (GLib.Bytes | Uint8Array);
            kind: MarkupKind;
        }
    }

    /**
     * @gir-type Class
     */
    class Markup extends GObject.Object {
        static $gtype: GObject.GType<Markup>;

        // Properties
        /**
         * @construct-only
         */
        get contents(): GLib.Bytes;

        /**
         * @construct-only
         * @default Foundry.MarkupKind.PLAINTEXT
         */
        get kind(): MarkupKind;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Markup.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Markup.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](contents: (GLib.Bytes | Uint8Array), kind: MarkupKind): Markup;

        static new_plaintext(message: string): Markup;

        // Signals
        /** @signal */
        connect<K extends keyof Markup.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Markup.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Markup.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Markup.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Markup.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Markup.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the contents bytes.
         * @returns a {@link GLib.Bytes} containing the markup
         */
        dup_contents(): GLib.Bytes;

        get_kind(): MarkupKind;

        to_string(): [(string | null), number];
    }


    namespace McpServer {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class McpServer extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<McpServer>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: McpServer.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<McpServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, stream: Gio.IOStream): McpServer;

        // Signals
        /** @signal */
        connect<K extends keyof McpServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, McpServer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof McpServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, McpServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof McpServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<McpServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        start(): void;

        stop(): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace MenuSearchProvider {
        // Signal signatures
        interface SignalSignatures extends SearchProvider.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends SearchProvider.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class MenuSearchProvider extends SearchProvider implements Json.Serializable {
        static $gtype: GObject.GType<MenuSearchProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: MenuSearchProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<MenuSearchProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): MenuSearchProvider;

        // Signals
        /** @signal */
        connect<K extends keyof MenuSearchProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MenuSearchProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof MenuSearchProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MenuSearchProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof MenuSearchProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<MenuSearchProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param menu_model 
         */
        add_menu(menu_model: Gio.MenuModel): void;
    }


    namespace MenuSearchResult {
        // Signal signatures
        interface SignalSignatures extends SearchResult.SignalSignatures {
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::use-underline": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends SearchResult.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class MenuSearchResult extends SearchResult {
        static $gtype: GObject.GType<MenuSearchResult>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: MenuSearchResult.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<MenuSearchResult.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](menu_model: Gio.MenuModel, index: number): MenuSearchResult;

        // Signals
        /** @signal */
        connect<K extends keyof MenuSearchResult.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MenuSearchResult.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof MenuSearchResult.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MenuSearchResult.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof MenuSearchResult.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<MenuSearchResult.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_menu_model(): Gio.MenuModel;

        get_index(): number;
    }


    namespace ModelManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Integrate with platform high-level models such as flatten or map.
     * @gir-type Class
     */
    class ModelManager extends GObject.Object {
        static $gtype: GObject.GType<ModelManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ModelManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ModelManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ModelManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ModelManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ModelManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ModelManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ModelManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ModelManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static dup_default(): ModelManager;

        // Virtual methods
        /**
         * @param model a {@link Gio.ListModel}
         * @virtual
         */
        vfunc_flatten(model: (Gio.ListModel | null)): Gio.ListModel;

        /**
         * @param model a {@link Gio.ListModel}
         * @param map_func 
         * @virtual
         */
        vfunc_map(model: (Gio.ListModel | null), map_func: ListModelMapFunc): Gio.ListModel;

        // Methods
        /**
         * @param model a {@link Gio.ListModel}
         */
        flatten(model: (Gio.ListModel | null)): Gio.ListModel;

        /**
         * @param model a {@link Gio.ListModel}
         * @param map_func 
         */
        map(model: (Gio.ListModel | null), map_func: ListModelMapFunc): Gio.ListModel;

        set_default(): void;
    }


    namespace NoVcs {
        // Signal signatures
        interface SignalSignatures extends Vcs.SignalSignatures {
            "notify::active": (pspec: GObject.ParamSpec) => void;
            "notify::branch-name": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::priority": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Vcs.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * A No-op VCS used to represent no VCS is active.
     * @gir-type Class
     */
    class NoVcs extends Vcs implements Json.Serializable {
        static $gtype: GObject.GType<NoVcs>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: NoVcs.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<NoVcs.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context): NoVcs;

        // Signals
        /** @signal */
        connect<K extends keyof NoVcs.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, NoVcs.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof NoVcs.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, NoVcs.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof NoVcs.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<NoVcs.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OnTypeDiagnostics {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends GObject.Object.ConstructorProps, Gio.ListModel.ConstructorProps {

        }
    }

    /**
     * Represents diagnostics that are continually updated as the contents of
     * a document are changed.
     * @gir-type Class
     */
    class OnTypeDiagnostics<A extends GObject.Object = GObject.Object> extends GObject.Object implements Gio.ListModel<A> {
        static $gtype: GObject.GType<OnTypeDiagnostics>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OnTypeDiagnostics.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<OnTypeDiagnostics.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](document: TextDocument): OnTypeDiagnostics;

        // Signals
        /** @signal */
        connect<K extends keyof OnTypeDiagnostics.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OnTypeDiagnostics.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OnTypeDiagnostics.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OnTypeDiagnostics.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OnTypeDiagnostics.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OnTypeDiagnostics.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Calls `callback` for each diagnostic found between the two lines.
         * @param first_line limit diagnostics to this line or after
         * @param last_line limit diagnostics to this line or before
         * @param callback 
         */
        foreach_in_range(first_line: number, last_line: number, callback: OnTypeDiagnosticsForeachFunc): void;

        /**
         * @param line 
         */
        line_has_diagnostic(line: number): boolean;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace OnTypeFormatter {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Abstract base class for plugins to implement formatting as you type.
     * @gir-type Class
     */
    abstract class OnTypeFormatter extends GObject.Object {
        static $gtype: GObject.GType<OnTypeFormatter>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OnTypeFormatter.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<OnTypeFormatter.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof OnTypeFormatter.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OnTypeFormatter.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OnTypeFormatter.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OnTypeFormatter.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OnTypeFormatter.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OnTypeFormatter.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Indents the text at `iter`.
         * 
         * `iter` should be set to the cursor location after the indent
         * when exiting this function.
         * @param document 
         * @param iter 
         * @virtual
         */
        vfunc_indent(document: TextDocument, iter: TextIter): TextIter;

        /**
         * @param document 
         * @param iter 
         * @param state 
         * @param keyval 
         * @virtual
         */
        vfunc_is_trigger(document: TextDocument, iter: TextIter, state: ModifierType, keyval: number): boolean;

        // Methods
        /**
         * Indents the text at `iter`.
         * 
         * `iter` should be set to the cursor location after the indent
         * when exiting this function.
         * @param document 
         * @param iter 
         */
        indent(document: TextDocument, iter: TextIter): TextIter;

        /**
         * @param document 
         * @param iter 
         * @param state 
         * @param keyval 
         */
        is_trigger(document: TextDocument, iter: TextIter, state: ModifierType, keyval: number): boolean;
    }


    namespace OpenFileIntent {
        // Signal signatures
        interface SignalSignatures extends Intent.SignalSignatures {
            "notify::content-type": (pspec: GObject.ParamSpec) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Intent.ConstructorProps {
            content_type: string;
            contentType: string;
            file: Gio.File;
        }
    }

    /**
     * Represents an intent to open a file.
     * 
     * FoundryOpenFileIntent provides functionality for opening files with
     * specific content types and handling file opening operations. It
     * integrates with the intent system to provide a unified interface
     * for file opening operations across different parts of the development
     * environment.
     * @gir-type Class
     */
    class OpenFileIntent extends Intent {
        static $gtype: GObject.GType<OpenFileIntent>;

        // Properties
        /**
         * @default null
         */
        get content_type(): string;
        set content_type(val: string);

        /**
         * @default null
         */
        get contentType(): string;
        set contentType(val: string);

        get file(): Gio.File;
        set file(val: Gio.File);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OpenFileIntent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<OpenFileIntent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](file: Gio.File, content_type: string): OpenFileIntent;

        // Signals
        /** @signal */
        connect<K extends keyof OpenFileIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenFileIntent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OpenFileIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenFileIntent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OpenFileIntent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OpenFileIntent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_content_type(): (string | null);

        dup_file(): (Gio.File | null);
    }


    namespace Operation {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * Emitted when the operation has completed successfully.
             * 
             * This signal is emitted when {@link Foundry.Operation.complete} is called
             * to indicate that the operation has finished successfully.
             * @signal
             * @since 1.1
             * @run-last
             */
            completed: () => void;
            "notify::auth-provider": (pspec: GObject.ParamSpec) => void;
            "notify::cancelled": (pspec: GObject.ParamSpec) => void;
            "notify::progress": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            auth_provider: AuthProvider;
            authProvider: AuthProvider;
            cancelled: boolean;
            progress: number;
            subtitle: string;
            title: string;
        }
    }

    /**
     * Represents a long-running operation in the development environment with progress tracking.
     * 
     * FoundryOperation provides a base class for operations that require progress
     * reporting and cancellation support. It integrates with the authentication
     * system and provides a unified interface for managing asynchronous operations
     * with user feedback and error handling.
     * @gir-type Class
     */
    class Operation extends GObject.Object {
        static $gtype: GObject.GType<Operation>;

        // Properties
        get auth_provider(): AuthProvider;
        set auth_provider(val: AuthProvider);

        get authProvider(): AuthProvider;
        set authProvider(val: AuthProvider);

        /**
         * @read-only
         * @default false
         */
        get cancelled(): boolean;

        /**
         * @default 0
         */
        get progress(): number;
        set progress(val: number);

        /**
         * @default null
         */
        get subtitle(): string;
        set subtitle(val: string);

        /**
         * @default null
         */
        get title(): string;
        set title(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Operation.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Operation.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Operation;

        // Signals
        /** @signal */
        connect<K extends keyof Operation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Operation.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Operation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Operation.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Operation.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Operation.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * @param current_num_bytes 
         * @param total_num_bytes 
         * @param user_data 
         */
        static file_progress(current_num_bytes: (bigint | number), total_num_bytes: (bigint | number), user_data: null): void;

        // Methods
        /**
         * Gets a {@link Dex.Future} that resolves when the operation
         * has cancelled or completed.
         * @returns a {@link Dex.Future}.
         */
        ["await"](): Dex.Future;

        cancel(): void;

        complete(): void;

        dup_auth_provider(): (AuthProvider | null);

        dup_subtitle(): string;

        dup_title(): string;

        get_progress(): number;

        /**
         * Checks if the operation has been cancelled.
         */
        is_cancelled(): boolean;

        /**
         * @param auth_provider 
         */
        set_auth_provider(auth_provider: AuthProvider): void;

        /**
         * @param progress 
         */
        set_progress(progress: number): void;

        /**
         * @param subtitle 
         */
        set_subtitle(subtitle: string): void;

        /**
         * @param title 
         */
        set_title(title: string): void;
    }


    namespace OperationManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::n-items": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            n_items: number;
            nItems: number;
        }
    }

    /**
     * Manage operations that should be observed by the user as tasks.
     * @gir-type Class
     */
    class OperationManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<OperationManager>;

        // Properties
        /**
         * @read-only
         * @default 0
         */
        get n_items(): number;

        /**
         * @read-only
         * @default 0
         */
        get nItems(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OperationManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<OperationManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof OperationManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OperationManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OperationManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OperationManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OperationManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OperationManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Creates a new operation.
         * @param title a title for the operation
         * @returns a new {@link Foundry.Operation}
         */
        begin(title: string): Operation;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace PathCache {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Thread-safe cache for path lookups
     * 
     * {@link Foundry.PathCache} can be used to cache path lookup entries as often
     * needed by runtimes.
     * 
     * This object is thread-safe and may be accessed from multiple
     * threads simultaneously.
     * @gir-type Class
     */
    class PathCache extends GObject.Object {
        static $gtype: GObject.GType<PathCache>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PathCache.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<PathCache.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): PathCache;

        // Signals
        /** @signal */
        connect<K extends keyof PathCache.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PathCache.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof PathCache.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PathCache.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PathCache.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PathCache.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * This function helps for detecting negative cache entries without
         * copying the program_path string.
         * @param program_name the name of the program to lookup
         * @returns `true` if an entry was found and `had_program_path` is set;   otherwise `false`.
         */
        contains(program_name: string): [boolean, boolean];

        /**
         * Inserts a cache entry for `program_name` for `program_path`.
         * 
         * `program_path` may be `null` to register a negative cache entry. See
         * `foundry_path_cache_lookup()` for handling negative cache entries.
         * @param program_name the name of the program
         * @param program_path the path for the program
         */
        insert(program_name: string, program_path: (string | null)): void;

        /**
         * `true` is returned when an entry is found in the cache. That entry
         * may be `null` indicating a negative cache entry.
         * @param program_name the program name to lookup
         * @returns `true` if `program_name` was found and `program_path` is set;   otherwise `false` is returned.
         */
        lookup(program_name: string): [boolean, string];
    }


    namespace PluginBuildAddin {
        // Signal signatures
        interface SignalSignatures extends BuildAddin.SignalSignatures {
            "notify::pipeline": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends BuildAddin.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * The {@link Foundry.PluginBuildAddin} class is a convenience object that
     * may be used by plug-ins implementing a build system.
     * 
     * It allows you to specify very simple build system commands in your
     * `.plugin` definition without having to implement the class yourself.
     * 
     * This is the preferred method for implementing very simple build system
     * integration within Foundry.
     * @gir-type Class
     */
    class PluginBuildAddin extends BuildAddin implements Json.Serializable {
        static $gtype: GObject.GType<PluginBuildAddin>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PluginBuildAddin.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<PluginBuildAddin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof PluginBuildAddin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PluginBuildAddin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof PluginBuildAddin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PluginBuildAddin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PluginBuildAddin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PluginBuildAddin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace PluginLspProvider {
        // Signal signatures
        interface SignalSignatures extends LspProvider.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::server": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends LspProvider.ConstructorProps<A>, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class PluginLspProvider<A extends GObject.Object = GObject.Object> extends LspProvider<A> implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<PluginLspProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PluginLspProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<PluginLspProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof PluginLspProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PluginLspProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof PluginLspProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PluginLspProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PluginLspProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PluginLspProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace PluginManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * @signal
             * @detailed
             * @run-last
             */
            changed: () => void;
            [key: `changed::${string}`]: () => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class PluginManager extends GObject.Object {
        static $gtype: GObject.GType<PluginManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PluginManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<PluginManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof PluginManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PluginManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof PluginManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PluginManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PluginManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PluginManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static get_default(): PluginManager;

        // Methods
        /**
         * @param plugin_info 
         */
        get_disabled(plugin_info: Peas.PluginInfo): boolean;

        /**
         * @param plugin_info 
         * @param disabled 
         */
        set_disabled(plugin_info: Peas.PluginInfo, disabled: boolean): void;
    }


    namespace ProcessLauncher {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Manages process execution and subprocess launching.
     * 
     * FoundryProcessLauncher provides a unified interface for launching and
     * managing subprocesses with proper environment setup, working directory
     * configuration, and I/O handling. It supports both local and chained
     * subprocesses that may either run remotely or in a remote context such as a
     * container or virtual machine.
     * @gir-type Class
     */
    class ProcessLauncher extends GObject.Object {
        static $gtype: GObject.GType<ProcessLauncher>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ProcessLauncher.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ProcessLauncher.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): ProcessLauncher;

        // Signals
        /** @signal */
        connect<K extends keyof ProcessLauncher.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ProcessLauncher.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ProcessLauncher.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ProcessLauncher.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ProcessLauncher.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ProcessLauncher.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param environ 
         */
        add_environ(environ: string): void;

        /**
         * Adds a minimal set of environment variables.
         * 
         * This is useful to get access to things like the display or other
         * expected variables.
         */
        add_minimal_environment(): void;

        /**
         * @param args 
         */
        append_args(args: string): void;

        /**
         * @param args 
         */
        append_args_parsed(args: string): boolean;

        /**
         * @param arg 
         */
        append_argv(arg: string): void;

        /**
         * Creates a stream to communicate with the subprocess using stdin/stdout.
         * 
         * The stream is created using UNIX pipes which are attached to the
         * stdin/stdout of the child process.
         * @returns a {@link Gio.IOStream} if successful; otherwise   `null` and `error` is set.
         */
        create_stdio_stream(): Gio.IOStream;

        environ_to_argv(): void;

        get_argv(): string[];

        get_cwd(): string;

        get_environ(): string[];

        /**
         * @param key 
         */
        getenv(key: string): string;

        /**
         * Merges the {@link Foundry.UnixFDMap} into the current layer.
         * 
         * If there are collisions in destination FDs, then that may cause an
         * error and `false` is returned.
         * 
         * `unix_fd_map` will have the FDs stolen using `foundry_unix_fd_map_steal_from()`
         * which means that if successful, `unix_fd_map` will not have any open
         * file-descriptors after calling this function.
         * @param unix_fd_map a {@link Foundry.UnixFDMap}
         * @returns `true` if successful; otherwise `false` and `error` is set.
         */
        merge_unix_fd_map(unix_fd_map: UnixFDMap): boolean;

        /**
         * @param args 
         */
        prepend_args(args: string): void;

        /**
         * @param arg 
         */
        prepend_argv(arg: string): void;

        /**
         * @param handler 
         */
        push(handler: ProcessLauncherHandler): void;

        /**
         * @param handler 
         */
        push_at_base(handler: ProcessLauncherHandler): void;

        /**
         * Pushes a new layer that will always fail with `error`.
         * 
         * This is useful if you have an error when attempting to build
         * a run command, but need it to deliver the error when attempting
         * to create a subprocess launcher.
         * @param error a {@link GLib.Error}
         */
        push_error(error: GLib.Error): void;

        /**
         * Pushes a layer to expand known environment variables.
         * 
         * The command argv and cwd will have `$FOO` style environment
         * variables expanded that are known. This can be useful to allow
         * things like `$BUILDDIR` be expanded at this layer.
         * @param environ 
         */
        push_expansion(environ: string): void;

        /**
         * Pushes handler to transform command to run on host.
         * 
         * If necessary, a layer is pushed to ensure the command is run on the
         * host instead of the application container.
         * 
         * If Builder is running on the host already, this function does nothing.
         */
        push_host(): void;

        /**
         * Pushes a shell which can run the upper layer command with -c.
         * @param shell the kind of shell to be used
         */
        push_shell(shell: ProcessLauncherShell): void;

        /**
         * @param shell 
         */
        push_user_shell(shell: ProcessLauncherShell): void;

        /**
         * @param argv 
         */
        set_argv(argv: string): void;

        /**
         * @param cwd 
         */
        set_cwd(cwd: string): void;

        /**
         * @param environ 
         */
        set_environ(environ: string): void;

        /**
         * Sets up a PTY for the run context that will communicate with the
         * consumer. The consumer is the generally the widget that is rendering
         * the PTY contents and the producer is the FD that is connected to the
         * subprocess.
         * @param consumer_fd the FD of the PTY consumer
         */
        set_pty_fd(consumer_fd: number): void;

        /**
         * @param key 
         * @param value 
         */
        setenv(key: string, value: string): void;

        /**
         * Spawns the run command.
         * 
         * If there is a failure to build the command into a subprocess launcher,
         * then `null` is returned and `error` is set.
         * 
         * If the subprocess fails to launch, then `null` is returned and `error` is set.
         * @returns an {@link Gio.Subprocess} if successful; otherwise `null`   and `error` is set.
         */
        spawn(): Gio.Subprocess;

        /**
         * Like `foundry_process_launcher_spawn()` but allows specifying the
         * flags for the GSubprocess which may override other settings.
         * @param flags 
         * @returns a {@link Gio.Subprocess} or `null` upon error.
         */
        spawn_with_flags(flags: Gio.SubprocessFlags): Gio.Subprocess;

        /**
         * @param source_fd 
         * @param dest_fd 
         */
        take_fd(source_fd: number, dest_fd: number): void;

        /**
         * @param key 
         */
        unsetenv(key: string): void;
    }


    namespace ProjectTemplate {
        // Signal signatures
        interface SignalSignatures extends Template.SignalSignatures {
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::input": (pspec: GObject.ParamSpec) => void;
            "notify::tags": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Template.ConstructorProps {

        }
    }

    /**
     * A template for creating a new project.
     * @gir-type Class
     */
    abstract class ProjectTemplate extends Template {
        static $gtype: GObject.GType<ProjectTemplate>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ProjectTemplate.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<ProjectTemplate.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof ProjectTemplate.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ProjectTemplate.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ProjectTemplate.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ProjectTemplate.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ProjectTemplate.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ProjectTemplate.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace PtyDiagnostics {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Custom PTY intermediate that can extract diagnostics.
     * @gir-type Class
     */
    class PtyDiagnostics<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<PtyDiagnostics>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PtyDiagnostics.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<PtyDiagnostics.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, pty_fd: number): PtyDiagnostics;

        // Signals
        /** @signal */
        connect<K extends keyof PtyDiagnostics.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PtyDiagnostics.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof PtyDiagnostics.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PtyDiagnostics.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PtyDiagnostics.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PtyDiagnostics.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Registers `regex` for use when extracting diagnostics from a PTY stream.
         * 
         * The regex should capture values into named groups that the
         * {@link Foundry.PtyDiagnostics} can turn into {@link Foundry.Diagnostic}.
         * 
         * Named groups supported are:
         * 
         *  - message
         *  - filename
         *  - line
         *  - column
         *  - level
         * @param regex 
         */
        static register(regex: GLib.Regex): void;

        // Methods
        create_producer(): number;

        get_fd(): number;

        reset(): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace RenameProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::document": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            buffer: TextBuffer;
            document: TextDocument;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class RenameProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<RenameProvider>;

        // Properties
        /**
         * @construct-only
         */
        get buffer(): TextBuffer;

        /**
         * @construct-only
         */
        get document(): TextDocument;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RenameProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<RenameProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof RenameProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RenameProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RenameProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RenameProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RenameProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RenameProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Determines the list of changes that need to be made to the code-base
         * to rename the word found at `iter`.
         * 
         * A consuming interface should display these edits to the user for
         * validation and approval before applying them using
         * {@link Foundry.TextManager.apply_edits} to apply the approved
         * edits.
         * @param iter the location of the item to semantically rename
         * @param new_name the replacement name
         * @virtual
         */
        vfunc_rename(iter: TextIter, new_name: string): Dex.Future;

        // Methods
        dup_buffer(): (TextBuffer | null);

        dup_document(): TextDocument;

        /**
         * Determines the list of changes that need to be made to the code-base
         * to rename the word found at `iter`.
         * 
         * A consuming interface should display these edits to the user for
         * validation and approval before applying them using
         * {@link Foundry.TextManager.apply_edits} to apply the approved
         * edits.
         * @param iter the location of the item to semantically rename
         * @param new_name the replacement name
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.TextEdit}.
         */
        rename(iter: TextIter, new_name: string): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace RunManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages application execution and runtime operations.
     * 
     * FoundryRunManager coordinates the execution of applications, manages runtime
     * environments, and provides tools for debugging and profiling. It handles
     * process launching, environment configuration, and integrates with build
     * systems for seamless application deployment and testing.
     * @gir-type Class
     */
    class RunManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<RunManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RunManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<RunManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof RunManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RunManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RunManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RunManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RunManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RunManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Deploys the application using the deploy strategy without running it.
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        deploy(): Dex.Future;

        /**
         * Gets the available tools that can be used to run the program.
         * @returns a list of tools supported by the run manager   such as "gdb" or "valgrind" or "sysprof".
         */
        list_tools(): string[];

        /**
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        run(): Dex.Future;

        /**
         * Starts running a program.
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param command a {@link Foundry.Command}
         * @param tool 
         * @param build_pty_fd 
         * @param run_pty_fd 
         * @param cancellable 
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.RunTool}.
         */
        run_command(pipeline: BuildPipeline, command: Command, tool: string, build_pty_fd: number, run_pty_fd: number, cancellable: Dex.Cancellable): Dex.Future;

        /**
         * @param default_pty 
         */
        set_default_pty(default_pty: number): void;
    }


    namespace RunTool {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            /**
             * @signal
             * @run-last
             */
            started: (arg0: Gio.Subprocess) => void;
            /**
             * @signal
             * @run-last
             */
            stopped: () => void;
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Represents a specific tool that can be used to run the application.
     * 
     * This allows implementing specialized tooling like debuggers, profilers,
     * machine emulators, and more.
     * @gir-type Class
     */
    abstract class RunTool extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<RunTool>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RunTool.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<RunTool.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof RunTool.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RunTool.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RunTool.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RunTool.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RunTool.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RunTool.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Requests the application exit.
         * 
         * The future resolves when the signal has been sent or equivalent
         * operation. That does not mean the process has stopped and depends
         * on where the tool is running (such as a remote device).
         * @virtual
         */
        vfunc_force_exit(): Dex.Future;

        /**
         * Sends a signal to the running application.
         * 
         * The future resolves when the signal has been sent. There is no guarantee
         * of signal delivery.
         * @param signum 
         * @virtual
         */
        vfunc_send_signal(signum: number): Dex.Future;

        /**
         * @param subprocess 
         * @virtual
         */
        vfunc_started(subprocess: Gio.Subprocess): void;

        /**
         * @virtual
         */
        vfunc_stopped(): void;

        // Methods
        /**
         * @returns a {@link Dex.Future} that resolves when   the process has completed.
         */
        ["await"](): Dex.Future;

        /**
         * @returns a {@link Peas.PluginInfo}
         */
        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * Requests the application exit.
         * 
         * The future resolves when the signal has been sent or equivalent
         * operation. That does not mean the process has stopped and depends
         * on where the tool is running (such as a remote device).
         * @returns a {@link Dex.Future} that resolves when the   request to force exit has been sent.
         */
        force_exit(): Dex.Future;

        /**
         * Sends a signal to the running application.
         * 
         * The future resolves when the signal has been sent. There is no guarantee
         * of signal delivery.
         * @param signum 
         * @returns a {@link Dex.Future} that resolves when the   signal has been sent.
         */
        send_signal(signum: number): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Sdk {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::active": (pspec: GObject.ParamSpec) => void;
            "notify::arch": (pspec: GObject.ParamSpec) => void;
            "notify::eol": (pspec: GObject.ParamSpec) => void;
            "notify::extension-only": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::installed": (pspec: GObject.ParamSpec) => void;
            "notify::kind": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            active: boolean;
            arch: string;
            eol: boolean;
            extension_only: boolean;
            extensionOnly: boolean;
            id: string;
            installed: boolean;
            kind: string;
            name: string;
            provider: SdkProvider;
        }
    }

    /**
     * Represents a software development kit (SDK).
     * 
     * FoundrySdk provides information about available SDKs including their
     * architecture, installation status, and capabilities. It integrates with
     * SDK providers to manage SDK discovery, installation, and configuration
     * for various development platforms and toolchains.
     * @gir-type Class
     */
    abstract class Sdk extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Sdk>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get active(): boolean;

        /**
         * @default null
         */
        get arch(): string;
        set arch(val: string);

        /**
         * Whether the SDK has reached end of life.
         * @since 1.1
         * @read-only
         * @default false
         */
        get eol(): boolean;

        /**
         * @default false
         */
        get extension_only(): boolean;
        set extension_only(val: boolean);

        /**
         * @default false
         */
        get extensionOnly(): boolean;
        set extensionOnly(val: boolean);

        /**
         * @default null
         */
        get id(): string;
        set id(val: string);

        /**
         * @default false
         */
        get installed(): boolean;
        set installed(val: boolean);

        /**
         * @default null
         */
        get kind(): string;
        set kind(val: string);

        /**
         * @default null
         */
        get name(): string;
        set name(val: string);

        /**
         * @read-only
         */
        get provider(): SdkProvider;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Sdk.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Sdk.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Sdk.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Sdk.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Sdk.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Sdk.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Sdk.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Sdk.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Looks for `program` within the SDK.
         * @param program the program such as "ps"
         * @virtual
         */
        vfunc_contains_program(program: string): Dex.Future;

        /**
         * Gets a config option that should be used as the default to work with
         * this particular SDK.
         * @param option 
         * @virtual
         */
        vfunc_dup_config_option(option: SdkConfigOption): (string | null);

        /**
         * Gets whether the SDK has reached end of life.
         * @virtual
         */
        vfunc_get_eol(): boolean;

        /**
         * Installs an SDK.
         * @param operation a {@link Foundry.Operation}
         * @param cancellable a {@link Dex.Cancellable}
         * @virtual
         */
        vfunc_install(operation: Operation, cancellable: (Dex.Cancellable | null)): Dex.Future;

        /**
         * Prepares `launcher` to be able to build applications.
         * 
         * That may mean setting things up to access a SDK tooling or other compoonents.
         * 
         * `phase` is used to differentiate between different types of launchers. For
         * example, you may apply a different environment for building dependencies
         * than for the project itself.
         * @param pipeline a {@link Foundry.BuildPipeline} or `null`
         * @param launcher the launcher to prepare
         * @param phase the phase of the build
         * @virtual
         */
        vfunc_prepare_to_build(pipeline: (BuildPipeline | null), launcher: ProcessLauncher, phase: BuildPipelinePhase): Dex.Future;

        /**
         * Prepares `launcher` to be able to run applications.
         * 
         * That may mean setting things up to access a display server, network,
         * or other compoonents.
         * @param pipeline a {@link Foundry.BuildPipeline} or `null`
         * @param launcher the launcher to prepare
         * @virtual
         */
        vfunc_prepare_to_run(pipeline: (BuildPipeline | null), launcher: ProcessLauncher): Dex.Future;

        /**
         * This function should be implemented by SDKs so that you can convert
         * a path within the container to a path outside the container.
         * 
         * For example, if we want to know where to find the location of
         * `/usr/share/gir-1.0/GLib-2.0.gir` within the SDK (or pipeline build)
         * we could use this function to get a path available from our current
         * environment which might route through `file:///var/run/host/...`.
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param path the path within the SDK to be translated
         * @virtual
         */
        vfunc_translate_path(pipeline: (BuildPipeline | null), path: string): Dex.Future;

        // Methods
        /**
         * This is a much simplified interface for {@link Foundry.Sdk.prepare_to_build}
         * for consumers that just want to run a simple command and get the stdout
         * output of the command.
         * 
         * Use this when you want to quickly run something like `program --version`
         * when setting up the pipeline.
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param argv the arguments to run
         * @returns a {@link Dex.Future} that resolves to a UTF-8   encoded string or rejects with error.
         */
        build_simple(pipeline: (BuildPipeline | null), argv: string): Dex.Future;

        /**
         * Looks for `program` within the SDK.
         * @param program the program such as "ps"
         * @returns a {@link Dex.Future} that resolves to a string   containing the path of `program`.
         */
        contains_program(program: string): Dex.Future;

        /**
         * Tries to discover the shell to use within the SDK.
         * 
         * This will look at the users preferred shell and try to locate that within
         * the container environment.
         * @returns a {@link Dex.Future}
         */
        discover_shell(): Dex.Future;

        /**
         * Gets the architecture of the SDK.
         * @returns a newly allocated string
         */
        dup_arch(): string;

        /**
         * Gets a config option that should be used as the default to work with
         * this particular SDK.
         * @param option 
         * @returns a string containing the config    option or `null` if unset.
         */
        dup_config_option(option: SdkConfigOption): (string | null);

        /**
         * Gets the user-visible id for the SDK.
         * @returns a newly allocated string
         */
        dup_id(): string;

        /**
         * Gets the user-visible kind for the SDK.
         * @returns a newly allocated string
         */
        dup_kind(): string;

        /**
         * Gets the user-visible name for the SDK.
         * @returns a newly allocated string
         */
        dup_name(): string;

        get_active(): boolean;

        /**
         * Gets whether the SDK has reached end of life.
         * @returns `true` if the SDK has reached end of life, `false` otherwise
         */
        get_eol(): boolean;

        get_extension_only(): boolean;

        get_installed(): boolean;

        /**
         * Installs an SDK.
         * @param operation a {@link Foundry.Operation}
         * @param cancellable a {@link Dex.Cancellable}
         * @returns a {@link Dex.Future} that resolves to   a boolean.
         */
        install(operation: Operation, cancellable: (Dex.Cancellable | null)): Dex.Future;

        /**
         * Prepares `launcher` to be able to build applications.
         * 
         * That may mean setting things up to access a SDK tooling or other compoonents.
         * 
         * `phase` is used to differentiate between different types of launchers. For
         * example, you may apply a different environment for building dependencies
         * than for the project itself.
         * @param pipeline a {@link Foundry.BuildPipeline} or `null`
         * @param launcher the launcher to prepare
         * @param phase the phase of the build
         * @returns a {@link Dex.Future}
         */
        prepare_to_build(pipeline: (BuildPipeline | null), launcher: ProcessLauncher, phase: BuildPipelinePhase): Dex.Future;

        /**
         * Prepares `launcher` to be able to run applications.
         * 
         * That may mean setting things up to access a display server, network,
         * or other compoonents.
         * @param pipeline a {@link Foundry.BuildPipeline} or `null`
         * @param launcher the launcher to prepare
         * @returns a {@link Dex.Future}
         */
        prepare_to_run(pipeline: (BuildPipeline | null), launcher: ProcessLauncher): Dex.Future;

        /**
         * Set the architecture of the SDK.
         * 
         * This should only be called by {@link Foundry.SdkProvider} classes.
         * @param arch 
         */
        set_arch(arch: string): void;

        /**
         * @param extension_only 
         */
        set_extension_only(extension_only: boolean): void;

        /**
         * Set the unique id of the SDK.
         * 
         * This should only be called by implementations of {@link Foundry.SdkProvider}.
         * @param id 
         */
        set_id(id: string): void;

        /**
         * @param installed 
         */
        set_installed(installed: boolean): void;

        /**
         * Set the user-visible kind of the sdk.
         * 
         * This should only be called by implementations of {@link Foundry.SdkProvider}.
         * @param kind 
         */
        set_kind(kind: string): void;

        /**
         * Set the user-visible name of the sdk.
         * 
         * This should only be called by implementations of {@link Foundry.SdkProvider}.
         * @param name 
         */
        set_name(name: string): void;

        /**
         * This function should be implemented by SDKs so that you can convert
         * a path within the container to a path outside the container.
         * 
         * For example, if we want to know where to find the location of
         * `/usr/share/gir-1.0/GLib-2.0.gir` within the SDK (or pipeline build)
         * we could use this function to get a path available from our current
         * environment which might route through `file:///var/run/host/...`.
         * @param pipeline a {@link Foundry.BuildPipeline}
         * @param path the path within the SDK to be translated
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.File} or rejects with error.
         */
        translate_path(pipeline: (BuildPipeline | null), path: string): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace SdkManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::sdk": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            sdk: Sdk;
        }
    }

    /**
     * Manages plugins providing SDK providers and their SDKs.
     * @gir-type Class
     */
    class SdkManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<SdkManager>;

        // Properties
        get sdk(): Sdk;
        set sdk(val: Sdk);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SdkManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SdkManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof SdkManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SdkManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SdkManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SdkManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SdkManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SdkManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the active SDK.
         * 
         * This is generally used for build pipelines, terminal shells, and more.
         * @returns a {@link Foundry.Sdk} or `null`
         */
        dup_sdk(): (Sdk | null);

        /**
         * Find a SDK by its identifier.
         * @param sdk_id the identifier of the SDK
         * @returns a {@link Foundry.Sdk} or `null`
         */
        find_by_id(sdk_id: string): (Dex.Future | null);

        /**
         * @param sdk 
         */
        set_sdk(sdk: Sdk): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace SdkProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class implemented by plugins to provide access to SDKs
     * of a specific type.
     * @gir-type Class
     */
    abstract class SdkProvider<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<SdkProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SdkProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SdkProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof SdkProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SdkProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SdkProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SdkProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SdkProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SdkProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @param sdk_id the identifier for the SDK such as "org.gnome.Sdk/x86_64/master"
         * @virtual
         */
        vfunc_find_by_id(sdk_id: string): Dex.Future;

        // Methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @returns the name of the provider
         */
        dup_name(): string;

        /**
         * @param sdk_id the identifier for the SDK such as "org.gnome.Sdk/x86_64/master"
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.Sdk} or rejects with error.
         */
        find_by_id(sdk_id: string): Dex.Future;

        /**
         * This is a convenience function for SDK providers that need to
         * parse the whole set of SDKs when doing updating. Just provide
         * them all as a list here and only the changes will be applied.
         * @param sdks a {@link GLib.PtrArray} of SDKs
         */
        merge(sdks: Sdk[]): void;

        /**
         * @param sdk 
         */
        sdk_added(sdk: Sdk): void;

        /**
         * @param sdk 
         */
        sdk_removed(sdk: Sdk): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace SearchManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Service that manages plugins providing search capabilities.
     * @gir-type Class
     */
    class SearchManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<SearchManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SearchManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SearchManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof SearchManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SearchManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SearchManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SearchManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SearchManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SearchManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param request 
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.SearchResult}.
         */
        search(request: SearchRequest): Dex.Future;
    }


    namespace SearchProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class allowing plugins to provide search capabilities.
     * @gir-type Class
     */
    abstract class SearchProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<SearchProvider>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SearchProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SearchProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof SearchProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SearchProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SearchProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SearchProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SearchProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SearchProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @param request 
         * @virtual
         */
        vfunc_search(request: SearchRequest): Dex.Future;

        // Methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @returns the name of the provider
         */
        dup_name(): string;

        /**
         * @returns a {@link Peas.PluginInfo}
         */
        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * @param request 
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.SearchResult} or   rejects with error.
         */
        search(request: SearchRequest): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace SearchRequest {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::categories": (pspec: GObject.ParamSpec) => void;
            "notify::search-text": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            categories: string[];
            search_text: string;
            searchText: string;
        }
    }

    /**
     * Abstract base class used by plugins to represent a singlular search result.
     * @gir-type Class
     */
    class SearchRequest extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<SearchRequest>;

        // Properties
        get categories(): string[];
        set categories(val: string[]);

        /**
         * @default null
         */
        get search_text(): string;
        set search_text(val: string);

        /**
         * @default null
         */
        get searchText(): string;
        set searchText(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SearchRequest.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SearchRequest.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, search_text: string): SearchRequest;

        // Signals
        /** @signal */
        connect<K extends keyof SearchRequest.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SearchRequest.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SearchRequest.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SearchRequest.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SearchRequest.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SearchRequest.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_categories(): (string[] | null);

        dup_search_text(): (string | null);

        /**
         * @param category 
         */
        has_category(category: string): boolean;

        /**
         * @param categories 
         */
        set_categories(categories: string): void;

        /**
         * @param search_text 
         */
        set_search_text(search_text: string): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace SearchResult {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::use-underline": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            icon: Gio.Icon;
            subtitle: string;
            title: string;
            use_underline: boolean;
            useUnderline: boolean;
        }
    }

    /**
     * Abstract base class for search results.
     * 
     * FoundrySearchResult provides the core interface for representing search
     * results with common properties like title, subtitle, and icon. Concrete
     * implementations handle specific types of search results and provide
     * specialized functionality for different search contexts and result types.
     * @gir-type Class
     */
    abstract class SearchResult extends GObject.Object {
        static $gtype: GObject.GType<SearchResult>;

        // Properties
        /**
         * @read-only
         */
        get icon(): Gio.Icon;

        /**
         * @read-only
         * @default null
         */
        get subtitle(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * @read-only
         * @default false
         */
        get use_underline(): boolean;

        /**
         * @read-only
         * @default false
         */
        get useUnderline(): boolean;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SearchResult.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SearchResult.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof SearchResult.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SearchResult.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SearchResult.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SearchResult.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SearchResult.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SearchResult.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Create an intent for the search result.
         * 
         * For search results that support intents, this describes an action that
         * should be handled for the user and dispatched via
         * {@link Foundry.IntentManager.dispatch}.
         * @param context a {@link Foundry.Context}
         * @virtual
         */
        vfunc_create_intent(context: Context): (Intent | null);

        /**
         * @virtual
         */
        vfunc_dup_icon(): (Gio.Icon | null);

        /**
         * @virtual
         */
        vfunc_dup_subtitle(): string;

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        /**
         * Gets whether the search result should use underline.
         * @virtual
         */
        vfunc_get_use_underline(): boolean;

        /**
         * Loads the contents of the search result.
         * 
         * The consumer of this should know how to handle the specific
         * object type by checking it's {@link GObject.GType}.
         * 
         * For example, if the result is a {@link Foundry.Documentation} then
         * you may want to check it's URI property to open the documentation.
         * 
         * It is expected that search providers load well known object types
         * which applications can reasonably handle.
         * @virtual
         */
        vfunc_load(): Dex.Future;

        // Methods
        /**
         * Create an intent for the search result.
         * 
         * For search results that support intents, this describes an action that
         * should be handled for the user and dispatched via
         * {@link Foundry.IntentManager.dispatch}.
         * @param context a {@link Foundry.Context}
         */
        create_intent(context: Context): (Intent | null);

        dup_icon(): (Gio.Icon | null);

        dup_subtitle(): string;

        dup_title(): string;

        /**
         * Gets whether the search result should use underline.
         * @returns `true` if underline should be used, `false` otherwise
         */
        get_use_underline(): boolean;

        /**
         * Loads the contents of the search result.
         * 
         * The consumer of this should know how to handle the specific
         * object type by checking it's {@link GObject.GType}.
         * 
         * For example, if the result is a {@link Foundry.Documentation} then
         * you may want to check it's URI property to open the documentation.
         * 
         * It is expected that search providers load well known object types
         * which applications can reasonably handle.
         * @returns a {@link Dex.Future} that resolves to   a {@link GObject.Object}.
         */
        load(): Dex.Future;
    }


    namespace SecretService {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages secure storage and retrieval of API keys and sensitive data.
     * 
     * FoundrySecretService provides a secure interface for storing and retrieving
     * API keys, authentication tokens, and other sensitive information. It integrates
     * with the system's secret storage backend and provides a unified API for
     * managing credentials across different services and platforms.
     * @gir-type Class
     */
    class SecretService extends Service implements Json.Serializable {
        static $gtype: GObject.GType<SecretService>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SecretService.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SecretService.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof SecretService.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SecretService.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SecretService.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SecretService.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SecretService.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SecretService.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Checks when the API key expires for the host/service pair.
         * @param host the hostname of the api server
         * @param service the name of the service such as "gitlab"
         * @returns a {@link Dex.Future} that resolves to   a GDateTime on success; or rejects with error.
         */
        check_expires_at(host: string, service: string): Dex.Future;

        /**
         * Removes an API key from secret storage.
         * @param host the hostname of the api server
         * @param service the name of the service such as "gitlab"
         * @returns a {@link Dex.Future} that resolves to   any value on success or rejects with error.
         */
        delete_api_key(host: string, service: string): Dex.Future;

        /**
         * Retrieves an API key from secret storage.
         * @param host the hostname of the api server
         * @param service the name of the service such as "gitlab"
         * @returns a {@link Dex.Future} that resolves to   a string on success or rejects with error.
         */
        lookup_api_key(host: string, service: string): Dex.Future;

        /**
         * Rotates the API key for the host/service pair.
         * @param host the hostname of the api server
         * @param service the name of the service such as "gitlab"
         * @param expire_at when the new key should expire, or `null`
         * @returns a {@link Dex.Future} that resolves to   the new API key as a string on success; or rejects with error.
         */
        rotate_api_key(host: string, service: string, expire_at: (GLib.DateTime | null)): Dex.Future;

        /**
         * Stores an API key in secret storage.
         * @param host the hostname of the api server
         * @param service the name of the service such as "gitlab"
         * @param api_key the actual API key
         * @returns a {@link Dex.Future} that resolves to   any value on success or rejects with error.
         */
        store_api_key(host: string, service: string, api_key: string): Dex.Future;
    }


    namespace Service {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class for services in the development environment.
     * 
     * FoundryService provides the core interface for services that can be
     * started and stopped within the development environment. It includes
     * lifecycle management, action handling, and provides a unified interface
     * for service coordination and management across the development environment.
     * @gir-type Class
     */
    abstract class Service extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Service>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Service.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Service.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Service.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Service.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Service.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Service.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Service.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Service.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        static get_action_prefix(): string;

        /**
         * @param action_name 
         * @param parameter_type 
         * @param activate 
         */
        static install_action(action_name: string, parameter_type: (string | null), activate: ServiceAction): void;

        /**
         * @param action_prefix 
         */
        static set_action_prefix(action_prefix: string): void;

        // Methods
        /**
         * @param action 
         */
        action_get_enabled(action: string): boolean;

        /**
         * @param action 
         * @param enabled 
         */
        action_set_enabled(action: string, enabled: boolean): void;

        /**
         * Gets a future that resolves when the service has started.
         * @returns a {@link Dex.Future}
         */
        when_ready(): Dex.Future;

        /**
         * Gets a future that resolves when the service has shutdown.
         * @returns A {@link Dex.Future}
         */
        when_shutdown(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Settings {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            /**
             * @signal
             * @detailed
             * @run-last
             */
            changed: (arg0: string) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::schema-id": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
            /**
             * @signal
             * @detailed
             * @run-last
             */
            "changed::path": (arg0: string) => void;
            /**
             * @signal
             * @detailed
             * @run-last
             */
            "changed::schema-id": (arg0: string) => void;
            /**
             * @signal
             * @detailed
             * @run-last
             */
            "changed::context": (arg0: string) => void;
            [key: `changed::${string}`]: (arg0: string) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Gio.ActionGroup.ConstructorProps, Json.Serializable.ConstructorProps {
            path: string;
            schema_id: string;
            schemaId: string;
        }
    }

    /**
     * Abstraction for user, project, and application settings.
     * 
     * This class provides support for settings which may be overridden at
     * multiple levels including per-user, per-project, and application-wide
     * defaults.
     * @gir-type Class
     */
    class Settings extends Contextual implements Gio.ActionGroup, Json.Serializable {
        static $gtype: GObject.GType<Settings>;

        // Properties
        /**
         * @construct-only
         * @default null
         */
        get path(): string;

        /**
         * @construct-only
         * @default null
         */
        get schema_id(): string;

        /**
         * @construct-only
         * @default null
         */
        get schemaId(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Settings.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Settings.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](context: Context, schema_id: string): Settings;

        static new_with_path(context: Context, schema_id: string, path: string): Settings;

        // Signals
        /** @signal */
        connect<K extends keyof Settings.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Settings.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Settings.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Settings.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Settings.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Settings.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param key 
         * @param object 
         * @param property 
         * @param flags 
         */
        bind(key: string, object: null, property: string, flags: Gio.SettingsBindFlags): void;

        /**
         * Like `foundry_settings_bind()` but allows transforming to and from settings storage using
         * `get_mapping` and `set_mapping` transformation functions.
         * 
         * Call `foundry_settings_unbind()` to unbind the mapping.
         * @param key The settings key
         * @param object the object to bind to
         * @param property the property of `object` to bind to
         * @param flags flags for the binding
         * @param get_mapping variant to value mapping
         * @param set_mapping value to variant mapping
         */
        bind_with_mapping(key: string, object: null, property: string, flags: Gio.SettingsBindFlags, get_mapping: (Gio.SettingsBindGetMapping | null), set_mapping: (Gio.SettingsBindSetMapping | null)): void;

        /**
         * Gets the underlying {@link Gio.Settings} used for the respective layer.
         * @param layer the desired layer
         * @returns a {@link Gio.Settings} instance
         */
        dup_layer(layer: SettingsLayer): Gio.Settings;

        /**
         * @param key 
         */
        get_boolean(key: string): boolean;

        /**
         * @param key 
         */
        get_default_value(key: string): GLib.Variant;

        /**
         * @param key 
         */
        get_double(key: string): number;

        /**
         * @param key 
         */
        get_int(key: string): number;

        get_schema_id(): string;

        /**
         * @param key 
         */
        get_string(key: string): string;

        /**
         * @param key 
         */
        get_strv(key: string): (string[] | null);

        /**
         * @param key 
         */
        get_uint(key: string): number;

        /**
         * @param key 
         */
        get_user_value(key: string): GLib.Variant;

        /**
         * @param key 
         */
        get_value(key: string): GLib.Variant;

        /**
         * @param key 
         * @param val 
         */
        set_boolean(key: string, val: boolean): void;

        /**
         * @param key 
         * @param val 
         */
        set_double(key: string, val: number): void;

        /**
         * @param key 
         * @param val 
         */
        set_int(key: string, val: number): void;

        /**
         * @param key 
         * @param val 
         */
        set_string(key: string, val: string): void;

        /**
         * @param key 
         * @param val 
         */
        set_uint(key: string, val: number): void;

        /**
         * @param key 
         * @param value 
         */
        set_value(key: string, value: GLib.Variant): void;

        /**
         * @param property 
         */
        unbind(property: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-added` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         */
        action_added(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-enabled-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param enabled whether the action is now enabled
         */
        action_enabled_changed(action_name: string, enabled: boolean): void;

        /**
         * Emits the `Gio.ActionGroup::action-removed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         */
        action_removed(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-state-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param state the new state of the named action
         */
        action_state_changed(action_name: string, state: GLib.Variant): void;

        /**
         * Activate the named action within `action_group`.
         * 
         * If the action is expecting a parameter, then the correct type of
         * parameter must be given as `parameter`.  If the action is expecting no
         * parameters then `parameter` must be `NULL`.  See
         * {@link Gio.ActionGroup.get_action_parameter_type}.
         * 
         * If the {@link Gio.ActionGroup} implementation supports asynchronous remote
         * activation over D-Bus, this call may return before the relevant
         * D-Bus traffic has been sent, or any replies have been received. In
         * order to block on such asynchronous activation calls,
         * {@link Gio.DBusConnection.flush} should be called prior to the code, which
         * depends on the result of the action activation. Without flushing
         * the D-Bus connection, there is no guarantee that the action would
         * have been activated.
         * 
         * The following code which runs in a remote app instance, shows an
         * example of a ‘quit’ action being activated on the primary app
         * instance over D-Bus. Here {@link Gio.DBusConnection.flush} is called
         * before `exit()`. Without `g_dbus_connection_flush()`, the ‘quit’ action
         * may fail to be activated on the primary instance.
         * 
         * ```c
         * // call ‘quit’ action on primary instance
         * g_action_group_activate_action (G_ACTION_GROUP (app), "quit", NULL);
         * 
         * // make sure the action is activated now
         * g_dbus_connection_flush (…);
         * 
         * g_debug ("Application has been terminated. Exiting.");
         * 
         * exit (0);
         * ```
         * @param action_name the name of the action to activate
         * @param parameter parameters to the activation
         */
        activate_action(action_name: string, parameter: (GLib.Variant | null)): void;

        /**
         * Request for the state of the named action within `action_group` to be
         * changed to `value`.
         * 
         * The action must be stateful and `value` must be of the correct type.
         * See {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * This call merely requests a change.  The action may refuse to change
         * its state or may change its state to something other than `value`.
         * See {@link Gio.ActionGroup.get_action_state_hint}.
         * 
         * If the `value` GVariant is floating, it is consumed.
         * @param action_name the name of the action to request the change on
         * @param value the new state
         */
        change_action_state(action_name: string, value: GLib.Variant): void;

        /**
         * Checks if the named action within `action_group` is currently enabled.
         * 
         * An action must be enabled in order to be activated or in order to
         * have its state changed from outside callers.
         * @param action_name the name of the action to query
         * @returns whether the action is currently enabled
         */
        get_action_enabled(action_name: string): boolean;

        /**
         * Queries the type of the parameter that must be given when activating
         * the named action within `action_group`.
         * 
         * When activating the action using {@link Gio.ActionGroup.activate_action},
         * the {@link GLib.Variant} given to that function must be of the type returned
         * by this function.
         * 
         * In the case that this function returns `NULL`, you must not give any
         * {@link GLib.Variant}, but `NULL` instead.
         * 
         * The parameter type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different parameter type.
         * @param action_name the name of the action to query
         * @returns the parameter type
         */
        get_action_parameter_type(action_name: string): (GLib.VariantType | null);

        /**
         * Queries the current state of the named action within `action_group`.
         * 
         * If the action is not stateful then `NULL` will be returned.  If the
         * action is stateful then the type of the return value is the type
         * given by {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @returns the current state of the action
         */
        get_action_state(action_name: string): (GLib.Variant | null);

        /**
         * Requests a hint about the valid range of values for the state of the
         * named action within `action_group`.
         * 
         * If `NULL` is returned it either means that the action is not stateful
         * or that there is no hint about the valid range of values for the
         * state of the action.
         * 
         * If a {@link GLib.Variant} array is returned then each item in the array is a
         * possible value for the state.  If a {@link GLib.Variant} pair (ie: two-tuple) is
         * returned then the tuple specifies the inclusive lower and upper bound
         * of valid values for the state.
         * 
         * In any case, the information is merely a hint.  It may be possible to
         * have a state value outside of the hinted range and setting a value
         * within the range may fail.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @returns the state range hint
         */
        get_action_state_hint(action_name: string): (GLib.Variant | null);

        /**
         * Queries the type of the state of the named action within
         * `action_group`.
         * 
         * If the action is stateful then this function returns the
         * {@link GLib.VariantType} of the state.  All calls to
         * {@link Gio.ActionGroup.change_action_state} must give a {@link GLib.Variant} of this
         * type and {@link Gio.ActionGroup.get_action_state} will return a {@link GLib.Variant}
         * of the same type.
         * 
         * If the action is not stateful then this function will return `NULL`.
         * In that case, {@link Gio.ActionGroup.get_action_state} will return `NULL`
         * and you must not call {@link Gio.ActionGroup.change_action_state}.
         * 
         * The state type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different state type.
         * @param action_name the name of the action to query
         * @returns the state type, if the action is stateful
         */
        get_action_state_type(action_name: string): (GLib.VariantType | null);

        /**
         * Checks if the named action exists within `action_group`.
         * @param action_name the name of the action to check for
         * @returns whether the named action exists
         */
        has_action(action_name: string): boolean;

        /**
         * Lists the actions contained within `action_group`.
         * 
         * The caller is responsible for freeing the list with {@link GLib.strfreev} when
         * it is no longer required.
         * @returns a `NULL`-terminated array   of the names of the actions in the group
         */
        list_actions(): string[];

        /**
         * Queries all aspects of the named action within an `action_group`.
         * 
         * This function acquires the information available from
         * {@link Gio.ActionGroup.has_action}, {@link Gio.ActionGroup.get_action_enabled},
         * {@link Gio.ActionGroup.get_action_parameter_type},
         * {@link Gio.ActionGroup.get_action_state_type},
         * {@link Gio.ActionGroup.get_action_state_hint} and
         * {@link Gio.ActionGroup.get_action_state} with a single function call.
         * 
         * This provides two main benefits.
         * 
         * The first is the improvement in efficiency that comes with not having
         * to perform repeated lookups of the action in order to discover
         * different things about it.  The second is that implementing
         * {@link Gio.ActionGroup} can now be done by only overriding this one virtual
         * function.
         * 
         * The interface provides a default implementation of this function that
         * calls the individual functions, as required, to fetch the
         * information.  The interface also provides default implementations of
         * those functions that call this function.  All implementations,
         * therefore, must override either this function or all of the others.
         * 
         * If the action exists, `TRUE` is returned and any of the requested
         * fields (as indicated by having a non-`NULL` reference passed in) are
         * filled.  If the action doesn’t exist, `FALSE` is returned and the
         * fields may or may not have been modified.
         * @param action_name the name of an action in the group
         * @returns `TRUE` if the action exists, else `FALSE`
         */
        query_action(action_name: string): [boolean, boolean, GLib.VariantType | null, GLib.VariantType | null, GLib.Variant | null, GLib.Variant | null];

        /**
         * Emits the `Gio.ActionGroup::action-added` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_action_added(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-enabled-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param enabled whether the action is now enabled
         * @virtual
         */
        vfunc_action_enabled_changed(action_name: string, enabled: boolean): void;

        /**
         * Emits the `Gio.ActionGroup::action-removed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_action_removed(action_name: string): void;

        /**
         * Emits the `Gio.ActionGroup::action-state-changed` signal on `action_group`.
         * 
         * This function should only be called by {@link Gio.ActionGroup} implementations.
         * @param action_name the name of an action in the group
         * @param state the new state of the named action
         * @virtual
         */
        vfunc_action_state_changed(action_name: string, state: GLib.Variant): void;

        /**
         * Activate the named action within `action_group`.
         * 
         * If the action is expecting a parameter, then the correct type of
         * parameter must be given as `parameter`.  If the action is expecting no
         * parameters then `parameter` must be `NULL`.  See
         * {@link Gio.ActionGroup.get_action_parameter_type}.
         * 
         * If the {@link Gio.ActionGroup} implementation supports asynchronous remote
         * activation over D-Bus, this call may return before the relevant
         * D-Bus traffic has been sent, or any replies have been received. In
         * order to block on such asynchronous activation calls,
         * {@link Gio.DBusConnection.flush} should be called prior to the code, which
         * depends on the result of the action activation. Without flushing
         * the D-Bus connection, there is no guarantee that the action would
         * have been activated.
         * 
         * The following code which runs in a remote app instance, shows an
         * example of a ‘quit’ action being activated on the primary app
         * instance over D-Bus. Here {@link Gio.DBusConnection.flush} is called
         * before `exit()`. Without `g_dbus_connection_flush()`, the ‘quit’ action
         * may fail to be activated on the primary instance.
         * 
         * ```c
         * // call ‘quit’ action on primary instance
         * g_action_group_activate_action (G_ACTION_GROUP (app), "quit", NULL);
         * 
         * // make sure the action is activated now
         * g_dbus_connection_flush (…);
         * 
         * g_debug ("Application has been terminated. Exiting.");
         * 
         * exit (0);
         * ```
         * @param action_name the name of the action to activate
         * @param parameter parameters to the activation
         * @virtual
         */
        vfunc_activate_action(action_name: string, parameter: (GLib.Variant | null)): void;

        /**
         * Request for the state of the named action within `action_group` to be
         * changed to `value`.
         * 
         * The action must be stateful and `value` must be of the correct type.
         * See {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * This call merely requests a change.  The action may refuse to change
         * its state or may change its state to something other than `value`.
         * See {@link Gio.ActionGroup.get_action_state_hint}.
         * 
         * If the `value` GVariant is floating, it is consumed.
         * @param action_name the name of the action to request the change on
         * @param value the new state
         * @virtual
         */
        vfunc_change_action_state(action_name: string, value: GLib.Variant): void;

        /**
         * Checks if the named action within `action_group` is currently enabled.
         * 
         * An action must be enabled in order to be activated or in order to
         * have its state changed from outside callers.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_enabled(action_name: string): boolean;

        /**
         * Queries the type of the parameter that must be given when activating
         * the named action within `action_group`.
         * 
         * When activating the action using {@link Gio.ActionGroup.activate_action},
         * the {@link GLib.Variant} given to that function must be of the type returned
         * by this function.
         * 
         * In the case that this function returns `NULL`, you must not give any
         * {@link GLib.Variant}, but `NULL` instead.
         * 
         * The parameter type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different parameter type.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_parameter_type(action_name: string): (GLib.VariantType | null);

        /**
         * Queries the current state of the named action within `action_group`.
         * 
         * If the action is not stateful then `NULL` will be returned.  If the
         * action is stateful then the type of the return value is the type
         * given by {@link Gio.ActionGroup.get_action_state_type}.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state(action_name: string): (GLib.Variant | null);

        /**
         * Requests a hint about the valid range of values for the state of the
         * named action within `action_group`.
         * 
         * If `NULL` is returned it either means that the action is not stateful
         * or that there is no hint about the valid range of values for the
         * state of the action.
         * 
         * If a {@link GLib.Variant} array is returned then each item in the array is a
         * possible value for the state.  If a {@link GLib.Variant} pair (ie: two-tuple) is
         * returned then the tuple specifies the inclusive lower and upper bound
         * of valid values for the state.
         * 
         * In any case, the information is merely a hint.  It may be possible to
         * have a state value outside of the hinted range and setting a value
         * within the range may fail.
         * 
         * The return value (if non-`NULL`) should be freed with
         * {@link GLib.Variant.unref} when it is no longer required.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state_hint(action_name: string): (GLib.Variant | null);

        /**
         * Queries the type of the state of the named action within
         * `action_group`.
         * 
         * If the action is stateful then this function returns the
         * {@link GLib.VariantType} of the state.  All calls to
         * {@link Gio.ActionGroup.change_action_state} must give a {@link GLib.Variant} of this
         * type and {@link Gio.ActionGroup.get_action_state} will return a {@link GLib.Variant}
         * of the same type.
         * 
         * If the action is not stateful then this function will return `NULL`.
         * In that case, {@link Gio.ActionGroup.get_action_state} will return `NULL`
         * and you must not call {@link Gio.ActionGroup.change_action_state}.
         * 
         * The state type of a particular action will never change but it is
         * possible for an action to be removed and for a new action to be added
         * with the same name but a different state type.
         * @param action_name the name of the action to query
         * @virtual
         */
        vfunc_get_action_state_type(action_name: string): (GLib.VariantType | null);

        /**
         * Checks if the named action exists within `action_group`.
         * @param action_name the name of the action to check for
         * @virtual
         */
        vfunc_has_action(action_name: string): boolean;

        /**
         * Lists the actions contained within `action_group`.
         * 
         * The caller is responsible for freeing the list with {@link GLib.strfreev} when
         * it is no longer required.
         * @virtual
         */
        vfunc_list_actions(): string[];

        /**
         * Queries all aspects of the named action within an `action_group`.
         * 
         * This function acquires the information available from
         * {@link Gio.ActionGroup.has_action}, {@link Gio.ActionGroup.get_action_enabled},
         * {@link Gio.ActionGroup.get_action_parameter_type},
         * {@link Gio.ActionGroup.get_action_state_type},
         * {@link Gio.ActionGroup.get_action_state_hint} and
         * {@link Gio.ActionGroup.get_action_state} with a single function call.
         * 
         * This provides two main benefits.
         * 
         * The first is the improvement in efficiency that comes with not having
         * to perform repeated lookups of the action in order to discover
         * different things about it.  The second is that implementing
         * {@link Gio.ActionGroup} can now be done by only overriding this one virtual
         * function.
         * 
         * The interface provides a default implementation of this function that
         * calls the individual functions, as required, to fetch the
         * information.  The interface also provides default implementations of
         * those functions that call this function.  All implementations,
         * therefore, must override either this function or all of the others.
         * 
         * If the action exists, `TRUE` is returned and any of the requested
         * fields (as indicated by having a non-`NULL` reference passed in) are
         * filled.  If the action doesn’t exist, `FALSE` is returned and the
         * fields may or may not have been modified.
         * @param action_name the name of an action in the group
         * @virtual
         */
        vfunc_query_action(action_name: string): [boolean, boolean, GLib.VariantType | null, GLib.VariantType | null, GLib.Variant | null, GLib.Variant | null];

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace SimpleLlmMessage {
        // Signal signatures
        interface SignalSignatures extends LlmMessage.SignalSignatures {
            "notify::content": (pspec: GObject.ParamSpec) => void;
            "notify::role": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends LlmMessage.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class SimpleLlmMessage extends LlmMessage {
        static $gtype: GObject.GType<SimpleLlmMessage>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SimpleLlmMessage.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SimpleLlmMessage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](role: string, content: string): SimpleLlmMessage;

        // Signals
        /** @signal */
        connect<K extends keyof SimpleLlmMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SimpleLlmMessage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SimpleLlmMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SimpleLlmMessage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SimpleLlmMessage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SimpleLlmMessage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SimpleTextBuffer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
            "notify::language-id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, TextBuffer.ConstructorProps {
            context: Context;
            language_id: string;
            languageId: string;
        }
    }

    /**
     * Simple implementation of a text buffer.
     * 
     * FoundrySimpleTextBuffer provides a basic text buffer implementation that
     * stores content as a string and provides text editing operations. It
     * implements the FoundryTextBuffer interface and serves as a lightweight
     * alternative to more complex text buffer implementations.
     * @gir-type Class
     */
    class SimpleTextBuffer extends GObject.Object implements TextBuffer {
        static $gtype: GObject.GType<SimpleTextBuffer>;

        // Properties
        /**
         * @construct-only
         */
        get context(): Context;

        /**
         * @read-only
         * @default null
         */
        get language_id(): string;

        /**
         * @read-only
         * @default null
         */
        get languageId(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SimpleTextBuffer.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SimpleTextBuffer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof SimpleTextBuffer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SimpleTextBuffer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SimpleTextBuffer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SimpleTextBuffer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SimpleTextBuffer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SimpleTextBuffer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static ["new"](): TextBuffer;

        /**
         * @param string 
         * @param len 
         */
        static new_for_string(string: string, len: (bigint | number)): TextBuffer;

        // Methods
        /**
         * @param language_id 
         */
        set_language_id(language_id: string): void;

        /**
         * @param text 
         * @param text_len 
         */
        set_text(text: string, text_len: (bigint | number)): void;

        /**
         * Adds a new commit notify handler.
         * 
         * If the implementation does not support this, zero is returned.
         * @param flags non-zero flags to dispatch
         * @param commit_notify 
         * @returns a non-zero commit handler id if supported; otherwise zero.
         */
        add_commit_notify(flags: TextBufferNotifyFlags, commit_notify: TextBufferCommitNotify): number;

        /**
         * @param edit 
         */
        apply_edit(edit: TextEdit): boolean;

        /**
         * Gets the contents of the buffer as a {@link GLib.Bytes}.
         * @returns a {@link GLib.Bytes} or `null`
         */
        dup_contents(): (GLib.Bytes | null);

        /**
         * Gets the GtkSourceView-style identifier for the language of the buffer
         * such as "c" or "js".
         */
        dup_language_id(): (string | null);

        emit_changed(): void;

        /**
         * Gets the number of changes that have occurred to `buffer`.
         * 
         * This is generally just a monotonic number.
         */
        get_change_count(): number;

        /**
         * @param iter 
         * @param offset 
         */
        get_iter_at_offset(iter: TextIter, offset: (bigint | number)): void;

        /**
         * @param iter 
         */
        get_start_iter(iter: TextIter): void;

        /**
         * Remove a commit notify handler.
         * @param commit_notify_handler the previously registered non-zero commit handler
         */
        remove_commit_notify(commit_notify_handler: number): void;

        /**
         * Gets a {@link Dex.Future} that will resolve after short delay when changes
         * have completed.
         * @returns a {@link Dex.Future}
         */
        settle(): Dex.Future;

        /**
         * Adds a new commit notify handler.
         * 
         * If the implementation does not support this, zero is returned.
         * @param flags non-zero flags to dispatch
         * @param commit_notify 
         * @virtual
         */
        vfunc_add_commit_notify(flags: TextBufferNotifyFlags, commit_notify: TextBufferCommitNotify): number;

        /**
         * @param edit 
         * @virtual
         */
        vfunc_apply_edit(edit: TextEdit): boolean;

        /**
         * Gets the contents of the buffer as a {@link GLib.Bytes}.
         * @virtual
         */
        vfunc_dup_contents(): (GLib.Bytes | null);

        /**
         * Gets the GtkSourceView-style identifier for the language of the buffer
         * such as "c" or "js".
         * @virtual
         */
        vfunc_dup_language_id(): (string | null);

        /**
         * Gets the number of changes that have occurred to `buffer`.
         * 
         * This is generally just a monotonic number.
         * @virtual
         */
        vfunc_get_change_count(): (bigint | number);

        /**
         * @param iter 
         * @virtual
         */
        vfunc_iter_init(iter: TextIter): void;

        /**
         * Remove a commit notify handler.
         * @param commit_notify_handler the previously registered non-zero commit handler
         * @virtual
         */
        vfunc_remove_commit_notify(commit_notify_handler: number): void;

        /**
         * Gets a {@link Dex.Future} that will resolve after short delay when changes
         * have completed.
         * @virtual
         */
        vfunc_settle(): Dex.Future;
    }


    namespace Symbol {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::locator": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            icon: Gio.Icon;
            locator: SymbolLocator;
            name: string;
        }
    }

    /**
     * Abstract base class for representing symbols.
     * 
     * FoundrySymbol provides the core interface for representing code symbols
     * such as functions, classes, variables, and other identifiers. Concrete
     * implementations handle specific symbol types and provide specialized
     * functionality for symbol navigation, documentation, and manipulation.
     * @gir-type Class
     */
    abstract class Symbol extends GObject.Object {
        static $gtype: GObject.GType<Symbol>;

        // Properties
        /**
         * @read-only
         */
        get icon(): Gio.Icon;

        /**
         * @read-only
         */
        get locator(): SymbolLocator;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Symbol.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Symbol.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Symbol.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Symbol.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Symbol.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Symbol.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Symbol.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Symbol.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets the icon representing the symbol.
         * @virtual
         */
        vfunc_dup_icon(): (Gio.Icon | null);

        /**
         * Gets the locator to find the location of the symbol.
         * @virtual
         */
        vfunc_dup_locator(): SymbolLocator;

        /**
         * Gets the name of the symbol.
         * @virtual
         */
        vfunc_dup_name(): (string | null);

        /**
         * Find the parent symbol, if any.
         * @virtual
         */
        vfunc_find_parent(): Dex.Future;

        /**
         * Gets if the symbol can have children even if it is not yet known
         * if it does have any children.
         * @virtual
         */
        vfunc_has_children(): boolean;

        /**
         * List all of the children of this symbol.
         * @virtual
         */
        vfunc_list_children(): Dex.Future;

        // Methods
        /**
         * Gets the icon representing the symbol.
         * @returns a {@link Gio.Icon} or `null`
         */
        dup_icon(): (Gio.Icon | null);

        /**
         * Gets the locator to find the location of the symbol.
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.SymbolLocator}.
         */
        dup_locator(): SymbolLocator;

        /**
         * Gets the name of the symbol.
         * @returns a string or `null`
         */
        dup_name(): (string | null);

        /**
         * Find the parent symbol, if any.
         * @returns a {@link Dex.Future} that resolves to  a {@link Foundry.Symbol} or rejects with error.
         */
        find_parent(): Dex.Future;

        /**
         * Gets if the symbol can have children even if it is not yet known
         * if it does have any children.
         * @returns TRUE if children are possible from this symbol.
         */
        has_children(): boolean;

        /**
         * List all of the children of this symbol.
         * @returns a {@link Dex.Future} that resolves to a  {@link Gio.ListModel} of {@link Foundry.Symbol} or rejects with error.
         */
        list_children(): Dex.Future;

        /**
         * Asynchronously populates a {@link Gio.ListModel} of {@link Foundry.Symbol}
         * starting from this symbol to the root symbol by following
         * {@link Foundry.Symbol.find_parent} until there are no more parents.
         * 
         * The root symbol is placed at position 0 and `self` is placed in the last
         * position.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.Symbol}.
         */
        list_to_root(): Dex.Future;
    }


    namespace SymbolIntent {
        // Signal signatures
        interface SignalSignatures extends Intent.SignalSignatures {
            "notify::locator": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Intent.ConstructorProps {
            locator: SymbolLocator;
        }
    }

    /**
     * Represents an intent to navigate to a symbol location.
     * 
     * FoundrySymbolIntent provides functionality for navigating to a symbol
     * location using a {@link Foundry.SymbolLocator} object. It integrates with
     * the intent system to provide a unified interface for symbol navigation
     * operations across different parts of the development environment.
     * @gir-type Class
     * @since 1.1
     */
    class SymbolIntent extends Intent {
        static $gtype: GObject.GType<SymbolIntent>;

        // Properties
        /**
         * @construct-only
         */
        get locator(): SymbolLocator;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SymbolIntent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SymbolIntent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](locator: SymbolLocator): SymbolIntent;

        // Signals
        /** @signal */
        connect<K extends keyof SymbolIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SymbolIntent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SymbolIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SymbolIntent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SymbolIntent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SymbolIntent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets a copy of the symbol locator object.
         * @returns a {@link Foundry.SymbolLocator}
         */
        dup_locator(): SymbolLocator;
    }


    namespace SymbolLocator {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::line-offset": (pspec: GObject.ParamSpec) => void;
            "notify::pattern": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            file: Gio.File;
            line: number;
            line_offset: number;
            lineOffset: number;
            pattern: string;
        }
    }

    /**
     * @gir-type Class
     */
    class SymbolLocator extends GObject.Object {
        static $gtype: GObject.GType<SymbolLocator>;

        // Properties
        /**
         * @construct-only
         */
        get file(): Gio.File;

        /**
         * @construct-only
         * @default 0
         */
        get line(): number;

        /**
         * @construct-only
         * @default 0
         */
        get line_offset(): number;

        /**
         * @construct-only
         * @default 0
         */
        get lineOffset(): number;

        /**
         * @construct-only
         * @default null
         */
        get pattern(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SymbolLocator.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SymbolLocator.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static new_for_file(file: Gio.File): SymbolLocator;

        static new_for_file_and_line(file: Gio.File, line: number): SymbolLocator;

        static new_for_file_and_line_offset(file: Gio.File, line: number, line_offset: number): SymbolLocator;

        static new_for_file_and_pattern(file: Gio.File, pattern: string): SymbolLocator;

        // Signals
        /** @signal */
        connect<K extends keyof SymbolLocator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SymbolLocator.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SymbolLocator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SymbolLocator.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SymbolLocator.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SymbolLocator.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_file(): (Gio.File | null);

        get_line(): number;

        get_line_offset(): number;

        get_line_offset_set(): boolean;

        get_line_set(): boolean;

        get_pattern(): string;

        /**
         * Attempt to resolve `locator` into a fixed position.
         * 
         * If the locator uses a pattern, this will try to locate the pattern
         * within `contents`.
         * @param contents 
         */
        locate(contents: (GLib.Bytes | Uint8Array)): (SymbolLocator | null);
    }


    namespace SymbolProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class allowing plugins to introspect symbols within a
     * specific file or file content.
     * @gir-type Class
     */
    abstract class SymbolProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<SymbolProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SymbolProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<SymbolProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof SymbolProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SymbolProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SymbolProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SymbolProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SymbolProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SymbolProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param file a {@link Gio.File}
         * @param contents 
         * @param line the line number (starting from 0)
         * @param line_offset the character offset (starting from 0)
         * @virtual
         */
        vfunc_find_symbol_at(file: Gio.File, contents: (GLib.Bytes | null), line: number, line_offset: number): Dex.Future;

        /**
         * @param file a {@link Gio.File}
         * @param contents optional modified contents for the file
         * @virtual
         */
        vfunc_list_symbols(file: Gio.File, contents: (GLib.Bytes | null)): Dex.Future;

        // Methods
        /**
         * @param file a {@link Gio.File}
         * @param contents 
         * @param line the line number (starting from 0)
         * @param line_offset the character offset (starting from 0)
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.Symbol} or rejects with error
         */
        find_symbol_at(file: Gio.File, contents: (GLib.Bytes | null), line: number, line_offset: number): Dex.Future;

        /**
         * @param file a {@link Gio.File}
         * @param contents optional modified contents for the file
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.Symbol} or rejects with error
         */
        list_symbols(file: Gio.File, contents: (GLib.Bytes | null)): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace Template {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::input": (pspec: GObject.ParamSpec) => void;
            "notify::tags": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            description: string;
            id: string;
            input: Input;
            tags: string[];
        }
    }

    /**
     * @gir-type Class
     */
    abstract class Template extends GObject.Object {
        static $gtype: GObject.GType<Template>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get description(): string;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         */
        get input(): Input;

        /**
         * @read-only
         */
        get tags(): string[];

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Template.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Template.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Template.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Template.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Template.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Template.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Template.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Template.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_description(): string;

        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_input(): (Input | null);

        /**
         * Gets tags describing the template such as "meson" or "flatpak".
         * @virtual
         */
        vfunc_dup_tags(): (string[] | null);

        /**
         * Expands the template based on the input parameters provided to the template.
         * @virtual
         */
        vfunc_expand(): Dex.Future;

        // Methods
        dup_description(): string;

        dup_id(): string;

        dup_input(): (Input | null);

        /**
         * Gets tags describing the template such as "meson" or "flatpak".
         */
        dup_tags(): (string[] | null);

        /**
         * Expands the template based on the input parameters provided to the template.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.TemplateOutput}.
         */
        expand(): Dex.Future;
    }


    namespace TemplateManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class TemplateManager extends GObject.Object {
        static $gtype: GObject.GType<TemplateManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TemplateManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TemplateManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): TemplateManager;

        // Signals
        /** @signal */
        connect<K extends keyof TemplateManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TemplateManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TemplateManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TemplateManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TemplateManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TemplateManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param context a {@link Foundry.Context} or `null`
         * @param template_id 
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.Template} or rejects with error.
         */
        find_template(context: (Context | null), template_id: string): Dex.Future;

        /**
         * Queries all {@link Foundry.TemplateProvider} for available
         * {@link Foundry.CodeTemplate}.
         * 
         * The resulting module may not be fully populated by all providers
         * by time it resolves. You may await the completion of all providers
         * by awaiting {@link Foundry.list_model_await} for the completion
         * of all providers.
         * 
         * This allows the consumer to get a dynamically populating list model
         * for user interfaces without delay.
         * @param context a {@link Foundry.Context} or `null`
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.CodeTemplate}
         */
        list_code_templates(context: (Context | null)): Dex.Future;

        /**
         * Queries all {@link Foundry.TemplateProvider} for available
         * {@link Foundry.ProjectTemplate}.
         * 
         * The resulting module may not be fully populated by all providers
         * by time it resolves. You may await the completion of all providers
         * by awaiting {@link Foundry.list_model_await} for the completion
         * of all providers.
         * 
         * This allows the consumer to get a dynamically populating list model
         * for user interfaces without delay.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.ProjectTemplate}
         */
        list_project_templates(): Dex.Future;

        /**
         * @param context a {@link Foundry.Context} or `null`
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.Template}.
         */
        list_templates(context: (Context | null)): Dex.Future;
    }


    namespace TemplateOutput {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::contents": (pspec: GObject.ParamSpec) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::mode": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            contents: (GLib.Bytes | Uint8Array);
            file: Gio.File;
            mode: number;
        }
    }

    /**
     * @gir-type Class
     */
    class TemplateOutput extends GObject.Object {
        static $gtype: GObject.GType<TemplateOutput>;

        // Properties
        /**
         * @read-only
         */
        get contents(): GLib.Bytes;

        /**
         * @read-only
         */
        get file(): Gio.File;

        /**
         * @read-only
         * @default 0
         */
        get mode(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TemplateOutput.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TemplateOutput.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](file: Gio.File, contents: (GLib.Bytes | Uint8Array), mode: number): TemplateOutput;

        static new_directory(file: Gio.File): TemplateOutput;

        // Signals
        /** @signal */
        connect<K extends keyof TemplateOutput.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TemplateOutput.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TemplateOutput.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TemplateOutput.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TemplateOutput.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TemplateOutput.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_contents(): GLib.Bytes;

        dup_file(): Gio.File;

        /**
         * The mode for the file or -1 to ignore the mode.
         */
        get_mode(): number;

        /**
         * Writes the output contents to the destination file.
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        write(): Dex.Future;
    }


    namespace TemplateProvider {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    abstract class TemplateProvider extends GObject.Object {
        static $gtype: GObject.GType<TemplateProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TemplateProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TemplateProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TemplateProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TemplateProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TemplateProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TemplateProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TemplateProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TemplateProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * You may specify `context` to ensure that project-specific values are
         * inherited for projects. Some providers may even provide additional
         * templates that are defined within the project which would otherwise
         * not be available.
         * @param context a {@link Foundry.Context} or `null`
         * @virtual
         */
        vfunc_list_code_templates(context: (Context | null)): Dex.Future;

        /**
         * @virtual
         */
        vfunc_list_project_templates(): Dex.Future;

        // Methods
        /**
         * You may specify `context` to ensure that project-specific values are
         * inherited for projects. Some providers may even provide additional
         * templates that are defined within the project which would otherwise
         * not be available.
         * @param context a {@link Foundry.Context} or `null`
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.CodeTemplate} or rejects   with error.
         */
        list_code_templates(context: (Context | null)): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.ProjectTemplate} or rejects   with error.
         */
        list_project_templates(): Dex.Future;
    }


    namespace TerminalIntent {
        // Signal signatures
        interface SignalSignatures extends Intent.SignalSignatures {
            "notify::launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Intent.ConstructorProps {
            launcher: TerminalLauncher;
        }
    }

    /**
     * @gir-type Class
     */
    class TerminalIntent extends Intent {
        static $gtype: GObject.GType<TerminalIntent>;

        // Properties
        /**
         * @construct-only
         */
        get launcher(): TerminalLauncher;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TerminalIntent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TerminalIntent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](launcher: TerminalLauncher): TerminalIntent;

        // Signals
        /** @signal */
        connect<K extends keyof TerminalIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TerminalIntent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TerminalIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TerminalIntent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TerminalIntent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TerminalIntent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_launcher(): TerminalLauncher;
    }


    namespace TerminalLauncher {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::command": (pspec: GObject.ParamSpec) => void;
            "notify::override-environment": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            command: Command;
            override_environment: string[];
            overrideEnvironment: string[];
        }
    }

    /**
     * Simplified interface representing how a terminal should be spawned.
     * @gir-type Class
     */
    class TerminalLauncher extends GObject.Object {
        static $gtype: GObject.GType<TerminalLauncher>;

        // Properties
        /**
         * @construct-only
         */
        get command(): Command;

        /**
         * @construct-only
         */
        get override_environment(): string[];

        /**
         * @construct-only
         */
        get overrideEnvironment(): string[];

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TerminalLauncher.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TerminalLauncher.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](command: Command, override_environment: (string | null)): TerminalLauncher;

        // Signals
        /** @signal */
        connect<K extends keyof TerminalLauncher.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TerminalLauncher.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TerminalLauncher.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TerminalLauncher.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TerminalLauncher.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TerminalLauncher.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        copy(): TerminalLauncher;

        dup_command(): Command;

        dup_override_environment(): (string[] | null);

        /**
         * The `pty_fd` is duplicated and therefore may be closed by the
         * caller after calling this function.
         * @param pty_fd the consumer side of the PTY (e.g. "master")
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.Subprocess}
         */
        run(pty_fd: number): Dex.Future;
    }


    namespace TerminalService {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Service providing convenient access to terminal operations.
     * @gir-type Class
     */
    class TerminalService extends Service implements Json.Serializable {
        static $gtype: GObject.GType<TerminalService>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TerminalService.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TerminalService.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TerminalService.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TerminalService.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TerminalService.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TerminalService.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TerminalService.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TerminalService.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Requests that a new terminal be launched.
         * 
         * This function will create a {@link Foundry.TerminalIntent} which the
         * application may handle to show a terminal.
         * @param lancher 
         * @returns A {@link Dex.Future} that resolves to   any value if succesfull; otherwise rejects with error.
         */
        launch(lancher: TerminalLauncher): Dex.Future;
    }


    namespace Test {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::command": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::suites": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            command: Command;
            id: string;
            suites: string[];
            title: string;
        }
    }

    /**
     * Abstract base class representing a single test.
     * @gir-type Class
     */
    abstract class Test extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Test>;

        // Properties
        /**
         * @read-only
         */
        get command(): Command;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         */
        get suites(): string[];

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Test.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Test.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Test.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Test.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Test.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Test.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Test.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Test.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_command(): Command;

        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * Get the suites for which this test belongs.
         * @virtual
         */
        vfunc_dup_suites(): (string[] | null);

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        // Methods
        dup_command(): Command;

        dup_id(): string;

        /**
         * Get the suites for which this test belongs.
         */
        dup_suites(): (string[] | null);

        dup_title(): string;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace TestManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages plugins providing test providers and their tests.
     * @gir-type Class
     */
    class TestManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<TestManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TestManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TestManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TestManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TestManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TestManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TestManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TestManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TestManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param test_id the identifier of the test
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.Test} or rejects with error.
         */
        find_test(test_id: string): Dex.Future;

        /**
         * Loads available tests and groups them within a {@link Foundry.TestSuite}.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.TestSuite}.
         */
        list_suites(): Dex.Future;

        /**
         * Queries all {@link Foundry.TestProvider} for available unit tests.
         * 
         * The resulting module may not be fully populated by all providers
         * by time it resolves. You may await the completion of all providers
         * by awaiting {@link Foundry.list_model_await} for the completion
         * of all providers.
         * 
         * This allows the consumer to get a dynamically populating list model
         * for user interfaces without delay.
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.Test}
         */
        list_tests(): Dex.Future;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace TestProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class to be implemented by plugins that want to integrate
     * with the unit testing system.
     * @gir-type Class
     */
    abstract class TestProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<TestProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TestProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TestProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TestProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TestProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TestProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TestProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TestProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TestProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * List the available tests as a {@link Gio.ListModel}.
         * @virtual
         */
        vfunc_list_tests(): Dex.Future;

        // Methods
        /**
         * List the available tests as a {@link Gio.ListModel}.
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.Test}.
         */
        list_tests(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace TestSuite {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends GObject.Object.ConstructorProps, Gio.ListModel.ConstructorProps {
            name: string;
        }
    }

    /**
     * A grouping of {@link Foundry.Test}
     * @gir-type Class
     * @since 1.1
     */
    class TestSuite<A extends GObject.Object = GObject.Object> extends GObject.Object implements Gio.ListModel<A> {
        static $gtype: GObject.GType<TestSuite>;

        // Properties
        /**
         * @since 1.1
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TestSuite.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TestSuite.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TestSuite.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TestSuite.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TestSuite.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TestSuite.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TestSuite.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TestSuite.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets the name of the suite if any.
         */
        dup_name(): (string | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace TextBufferProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Interface to be implemented by platform libraries to integrate with their
     * text buffer implementation of choice such as GtkSourceView.
     * @gir-type Class
     */
    abstract class TextBufferProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<TextBufferProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TextBufferProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TextBufferProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TextBufferProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextBufferProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TextBufferProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextBufferProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TextBufferProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TextBufferProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_create_buffer(): TextBuffer;

        /**
         * @param buffer 
         * @param file 
         * @param operation 
         * @param encoding 
         * @param crlf 
         * @virtual
         */
        vfunc_load(buffer: TextBuffer, file: Gio.File, operation: (Operation | null), encoding: (string | null), crlf: (string | null)): Dex.Future;

        /**
         * @param buffer 
         * @param file 
         * @param operation 
         * @param encoding 
         * @param crlf 
         * @virtual
         */
        vfunc_save(buffer: TextBuffer, file: Gio.File, operation: (Operation | null), encoding: (string | null), crlf: (string | null)): Dex.Future;

        // Methods
        create_buffer(): TextBuffer;

        /**
         * @param buffer 
         * @param file 
         * @param operation 
         * @param encoding 
         * @param crlf 
         */
        load(buffer: TextBuffer, file: Gio.File, operation: (Operation | null), encoding: (string | null), crlf: (string | null)): Dex.Future;

        /**
         * @param buffer 
         * @param file 
         * @param operation 
         * @param encoding 
         * @param crlf 
         */
        save(buffer: TextBuffer, file: Gio.File, operation: (Operation | null), encoding: (string | null), crlf: (string | null)): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace TextDocument {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            /**
             * This signal is emitted after the document has been saved to `file`.
             * @signal
             * @since 1.1
             * @run-last
             */
            saved: (arg0: Gio.File) => void;
            "notify::addins": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::draft-id": (pspec: GObject.ParamSpec) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            addins: Gio.ListModel;
            buffer: TextBuffer;
            draft_id: string;
            draftId: string;
            file: Gio.File;
            icon: Gio.Icon;
            title: string;
        }
    }

    /**
     * Represents a text document in the development environment with editing capabilities.
     * 
     * FoundryTextDocument manages the lifecycle of text documents including loading,
     * saving, and modification tracking. It integrates with the text buffer system
     * and provides document-level services such as diagnostics, settings, and
     * extension management for enhanced editing functionality.
     * @gir-type Class
     */
    class TextDocument extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<TextDocument>;

        // Properties
        /**
         * @read-only
         */
        get addins(): Gio.ListModel;

        /**
         * @construct-only
         */
        get buffer(): TextBuffer;

        /**
         * @construct-only
         * @default null
         */
        get draft_id(): string;

        /**
         * @construct-only
         * @default null
         */
        get draftId(): string;

        /**
         * @construct-only
         */
        get file(): Gio.File;

        /**
         * @read-only
         */
        get icon(): Gio.Icon;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TextDocument.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TextDocument.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TextDocument.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextDocument.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TextDocument.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextDocument.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TextDocument.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TextDocument.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param edit 
         */
        apply_edit(edit: TextEdit): boolean;

        /**
         * @param edits 
         * @param n_edits 
         */
        apply_edits(edits: TextEdit, n_edits: number): boolean;

        /**
         * Queries {@link Foundry.DiagnosticProvider} for diagnostics that are
         * relevant to the document.
         * 
         * This does a single-shot run of diagnostics.
         * 
         * To monitor diagnostics for changes, use
         * {@link Foundry.TextDocument.watch_diagnostics} which will update as
         * the document changes.
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.Diagnostic}.
         */
        diagnose(): Dex.Future;

        /**
         * Gets the underlying {@link Foundry.TextBuffer}.
         * @returns a {@link Foundry.TextBuffer}.
         */
        dup_buffer(): TextBuffer;

        /**
         * Gets the underlying {@link Gio.File}.
         * @returns a {@link Gio.File} or `null`
         */
        dup_file(): (Gio.File | null);

        dup_icon(): (Gio.Icon | null);

        /**
         * Gets the title for the document.
         * @returns a string or `null`
         */
        dup_title(): (string | null);

        /**
         * Convenience function to get the URI of a file.
         * @returns a UTF-8 encoded string containing the URI
         */
        dup_uri(): string;

        /**
         * Gets a {@link Foundry.TextDocumentAddin} using the name of the plugin.
         * This is the value for "Module=" in the `.plugin` file.
         * @param module_name the name of the plugin module
         */
        find_addin(module_name: string): (TextDocumentAddin | null);

        /**
         * Queries {@link Foundry.SymbolProvider} for a symbol at the
         * specified position in the document.
         * @param line the line number (starting from 0)
         * @param line_offset the character offset (starting from 0)
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.Symbol} or rejects with error.
         */
        find_symbol_at(line: number, line_offset: number): Dex.Future;

        list_addins(): (Gio.ListModel | null);

        /**
         * Queries {@link Foundry.TextDocumentAddin} for actions that are
         * relevant to the document based on the current diagnostics.
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.CodeAction}.
         */
        list_code_actions(): Dex.Future;

        /**
         * Queries {@link Foundry.SymbolProvider} for symbols that are
         * relevant to the document.
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.Symbol}.
         */
        list_symbols(): Dex.Future;

        /**
         * Loads the settings for the document.
         * 
         * Text editing applications should bind the properties of the resulting
         * {@link Foundry.TextSettings} to their document's text editor.
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.TextSettings} or rejects with error.
         */
        load_settings(): Dex.Future;

        /**
         * @param operation an operation to update with progress
         */
        save(operation: (Operation | null)): Dex.Future;

        /**
         * @param file a {@link Gio.File} of where to save the document
         * @param operation an operation to update with progress
         * @returns a {@link Dex.Future} that resolves to any value   or rejects with error.
         */
        save_as(file: Gio.File, operation: (Operation | null)): Dex.Future;

        /**
         * Watches the document for changes and updates diagnostics in the
         * resulting {@link Foundry.OnTypeDiagnostics} which is a {@link Gio.ListModel}.
         * @returns a {@link Foundry.OnTypeDiagnostics}
         */
        watch_diagnostics(): OnTypeDiagnostics;

        /**
         * This function will resolve a new {@link Dex.Future} when the underlying
         * {@link Foundry.TextBuffer} has changed.
         * 
         * Use this to invalidate external resources that rely on the contents of
         * the document buffer.
         * 
         * This notification is done after a short delay to avoid spurious events
         * during rapid modification to the buffer.
         * 
         * If the document is dispoed, this future will reject with error.
         * @returns a {@link Dex.Future}
         */
        when_changed(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace TextDocumentAddin {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::document": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            document: TextDocument;
        }
    }

    /**
     * Abstract base class for plugins to alter how documents work.
     * @gir-type Class
     */
    abstract class TextDocumentAddin extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<TextDocumentAddin>;

        // Properties
        /**
         * @construct-only
         */
        get document(): TextDocument;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TextDocumentAddin.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TextDocumentAddin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TextDocumentAddin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextDocumentAddin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TextDocumentAddin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextDocumentAddin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TextDocumentAddin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TextDocumentAddin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_list_code_actions(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_load(): Dex.Future;

        /**
         * Called after the document is loadd.
         * @virtual
         */
        vfunc_post_load(): Dex.Future;

        /**
         * Called after the document is saved.
         * @virtual
         */
        vfunc_post_save(): Dex.Future;

        /**
         * Called before the document is loadd.
         * @virtual
         */
        vfunc_pre_load(): Dex.Future;

        /**
         * Called before the document is saved.
         * @virtual
         */
        vfunc_pre_save(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_unload(): Dex.Future;

        // Methods
        dup_document(): TextDocument;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.CodeAction} or rejects with error
         */
        list_code_actions(): Dex.Future;

        load(): Dex.Future;

        /**
         * Called after the document is loadd.
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        post_load(): Dex.Future;

        /**
         * Called after the document is saved.
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        post_save(): Dex.Future;

        /**
         * Called before the document is loadd.
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        pre_load(): Dex.Future;

        /**
         * Called before the document is saved.
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        pre_save(): Dex.Future;

        unload(): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace TextEdit {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * Represents a single text edit to a document.
     * @gir-type Class
     */
    class TextEdit extends GObject.Object {
        static $gtype: GObject.GType<TextEdit>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TextEdit.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TextEdit.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](file: Gio.File, begin_line: number, begin_line_offset: number, end_line: number, end_line_offset: number, replacement: string): TextEdit;

        // Signals
        /** @signal */
        connect<K extends keyof TextEdit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextEdit.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TextEdit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextEdit.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TextEdit.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TextEdit.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param b 
         */
        compare(b: TextEdit): number;

        /**
         * Gets the underlying {@link Gio.File} if any.
         * @returns a {@link Gio.File} or `null`
         */
        dup_file(): (Gio.File | null);

        /**
         * Gets the replacement text if any.
         * @returns a string or `null`
         */
        dup_replacement(): (string | null);

        /**
         * @param begin_line 
         * @param begin_line_offset 
         * @param end_line 
         * @param end_line_offset 
         */
        get_range(begin_line: number, begin_line_offset: number, end_line: number, end_line_offset: number): void;
    }


    namespace TextFormatter {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::document": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            buffer: TextBuffer;
            document: TextDocument;
        }
    }

    /**
     * Abstract base class for plugins to implement custom text formatting.
     * @gir-type Class
     */
    abstract class TextFormatter extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<TextFormatter>;

        // Properties
        /**
         * @read-only
         */
        get buffer(): TextBuffer;

        /**
         * @construct-only
         */
        get document(): TextDocument;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TextFormatter.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TextFormatter.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TextFormatter.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextFormatter.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TextFormatter.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextFormatter.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TextFormatter.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TextFormatter.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Determines if the formatter can do range formatting of the document.
         * @virtual
         */
        vfunc_can_format_range(): boolean;

        /**
         * @virtual
         */
        vfunc_format(): Dex.Future;

        /**
         * @param begin the start of the region to format
         * @param end the end of the region to format
         * @virtual
         */
        vfunc_format_range(begin: TextIter, end: TextIter): Dex.Future;

        // Methods
        /**
         * Determines if the formatter can do range formatting of the document.
         * @returns `true` if the formatter can handle format_range requests
         */
        can_format_range(): boolean;

        dup_buffer(): (TextBuffer | null);

        dup_document(): TextDocument;

        /**
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error
         */
        format(): Dex.Future;

        /**
         * @param begin the start of the region to format
         * @param end the end of the region to format
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error
         */
        format_range(begin: TextIter, end: TextIter): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace TextManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            /**
             * @signal
             * @run-last
             */
            "document-added": (arg0: Gio.File, arg1: TextDocument) => void;
            /**
             * @signal
             * @run-last
             */
            "document-removed": (arg0: Gio.File) => void;
            /**
             * This signal is emitted whenever a {@link Foundry.TextDocument} managed
             * by the manager emits "saved".
             * @signal
             * @since 1.1
             * @run-last
             */
            "document-saved": (arg0: TextDocument) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages text documents and provides text editing services.
     * 
     * FoundryTextManager coordinates text buffer creation, document management, and
     * provides a unified interface for text editing operations. It handles document
     * loading, saving, and maintains relationships between files and their text representations.
     * @gir-type Class
     */
    class TextManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<TextManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TextManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TextManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TextManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TextManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TextManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TextManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Applies all of `edits` to the respective files.
         * @param edits a {@link Gio.ListModel} of {@link Foundry.TextEdit}
         * @param operation a {@link Foundry.Operation}
         * @returns a {@link Dex.Future} that resolves to   any value or rejects with error.
         */
        apply_edits(edits: Gio.ListModel, operation: (Operation | null)): Dex.Future;

        /**
         * @returns a {@link Gio.ListModel} of {@link Foundry.TextDocument}
         */
        list_documents(): Gio.ListModel;

        /**
         * Loads the file as a text document.
         * @param file a {@link Gio.File}
         * @param operation an operation for progress
         * @param encoding an optional encoding charset
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.TextDocument}.
         */
        load(file: Gio.File, operation: Operation, encoding: (string | null)): Dex.Future;
    }


    namespace TextSettings {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::auto-indent": (pspec: GObject.ParamSpec) => void;
            "notify::completion-auto-select": (pspec: GObject.ParamSpec) => void;
            "notify::completion-page-size": (pspec: GObject.ParamSpec) => void;
            "notify::custom-font": (pspec: GObject.ParamSpec) => void;
            "notify::document": (pspec: GObject.ParamSpec) => void;
            "notify::enable-completion": (pspec: GObject.ParamSpec) => void;
            "notify::enable-snippets": (pspec: GObject.ParamSpec) => void;
            "notify::enable-spell-check": (pspec: GObject.ParamSpec) => void;
            "notify::highlight-current-line": (pspec: GObject.ParamSpec) => void;
            "notify::highlight-matching-brackets": (pspec: GObject.ParamSpec) => void;
            "notify::implicit-trailing-newline": (pspec: GObject.ParamSpec) => void;
            "notify::indent-on-tab": (pspec: GObject.ParamSpec) => void;
            "notify::indent-width": (pspec: GObject.ParamSpec) => void;
            "notify::insert-matching-brace": (pspec: GObject.ParamSpec) => void;
            "notify::insert-spaces-instead-of-tabs": (pspec: GObject.ParamSpec) => void;
            "notify::line-height": (pspec: GObject.ParamSpec) => void;
            "notify::override-indent-width": (pspec: GObject.ParamSpec) => void;
            "notify::overwrite-matching-brace": (pspec: GObject.ParamSpec) => void;
            "notify::right-margin-position": (pspec: GObject.ParamSpec) => void;
            "notify::show-diagnostics": (pspec: GObject.ParamSpec) => void;
            "notify::show-line-changes": (pspec: GObject.ParamSpec) => void;
            "notify::show-line-changes-overview": (pspec: GObject.ParamSpec) => void;
            "notify::show-line-numbers": (pspec: GObject.ParamSpec) => void;
            "notify::show-right-margin": (pspec: GObject.ParamSpec) => void;
            "notify::smart-backspace": (pspec: GObject.ParamSpec) => void;
            "notify::smart-home-end": (pspec: GObject.ParamSpec) => void;
            "notify::tab-width": (pspec: GObject.ParamSpec) => void;
            "notify::use-custom-font": (pspec: GObject.ParamSpec) => void;
            "notify::wrap": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            auto_indent: boolean;
            autoIndent: boolean;
            completion_auto_select: boolean;
            completionAutoSelect: boolean;
            completion_page_size: number;
            completionPageSize: number;
            custom_font: string;
            customFont: string;
            document: TextDocument;
            enable_completion: boolean;
            enableCompletion: boolean;
            enable_snippets: boolean;
            enableSnippets: boolean;
            enable_spell_check: boolean;
            enableSpellCheck: boolean;
            highlight_current_line: boolean;
            highlightCurrentLine: boolean;
            highlight_matching_brackets: boolean;
            highlightMatchingBrackets: boolean;
            implicit_trailing_newline: boolean;
            implicitTrailingNewline: boolean;
            indent_on_tab: boolean;
            indentOnTab: boolean;
            indent_width: number;
            indentWidth: number;
            insert_matching_brace: boolean;
            insertMatchingBrace: boolean;
            insert_spaces_instead_of_tabs: boolean;
            insertSpacesInsteadOfTabs: boolean;
            line_height: number;
            lineHeight: number;
            override_indent_width: boolean;
            overrideIndentWidth: boolean;
            overwrite_matching_brace: boolean;
            overwriteMatchingBrace: boolean;
            right_margin_position: number;
            rightMarginPosition: number;
            show_diagnostics: boolean;
            showDiagnostics: boolean;
            show_line_changes: boolean;
            showLineChanges: boolean;
            show_line_changes_overview: boolean;
            showLineChangesOverview: boolean;
            show_line_numbers: boolean;
            showLineNumbers: boolean;
            show_right_margin: boolean;
            showRightMargin: boolean;
            smart_backspace: boolean;
            smartBackspace: boolean;
            smart_home_end: boolean;
            smartHomeEnd: boolean;
            tab_width: number;
            tabWidth: number;
            use_custom_font: boolean;
            useCustomFont: boolean;
            wrap: TextWrap;
        }
    }

    /**
     * Manages text editor settings and configuration.
     * 
     * FoundryTextSettings provides a unified interface for managing text editor
     * preferences including font settings, indentation, completion behavior, and
     * other editor-specific options. It integrates with the settings system
     * and provides per-document configuration capabilities.
     * @gir-type Class
     */
    class TextSettings extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<TextSettings>;

        // Properties
        /**
         * @default true
         */
        get auto_indent(): boolean;
        set auto_indent(val: boolean);

        /**
         * @default true
         */
        get autoIndent(): boolean;
        set autoIndent(val: boolean);

        /**
         * @default false
         */
        get completion_auto_select(): boolean;
        set completion_auto_select(val: boolean);

        /**
         * @default false
         */
        get completionAutoSelect(): boolean;
        set completionAutoSelect(val: boolean);

        /**
         * @default 5
         */
        get completion_page_size(): number;
        set completion_page_size(val: number);

        /**
         * @default 5
         */
        get completionPageSize(): number;
        set completionPageSize(val: number);

        /**
         * @default Monospace 11
         */
        get custom_font(): string;
        set custom_font(val: string);

        /**
         * @default Monospace 11
         */
        get customFont(): string;
        set customFont(val: string);

        /**
         * @read-only
         */
        get document(): TextDocument;

        /**
         * @default true
         */
        get enable_completion(): boolean;
        set enable_completion(val: boolean);

        /**
         * @default true
         */
        get enableCompletion(): boolean;
        set enableCompletion(val: boolean);

        /**
         * @default true
         */
        get enable_snippets(): boolean;
        set enable_snippets(val: boolean);

        /**
         * @default true
         */
        get enableSnippets(): boolean;
        set enableSnippets(val: boolean);

        /**
         * @default true
         */
        get enable_spell_check(): boolean;
        set enable_spell_check(val: boolean);

        /**
         * @default true
         */
        get enableSpellCheck(): boolean;
        set enableSpellCheck(val: boolean);

        /**
         * @default true
         */
        get highlight_current_line(): boolean;
        set highlight_current_line(val: boolean);

        /**
         * @default true
         */
        get highlightCurrentLine(): boolean;
        set highlightCurrentLine(val: boolean);

        /**
         * @default true
         */
        get highlight_matching_brackets(): boolean;
        set highlight_matching_brackets(val: boolean);

        /**
         * @default true
         */
        get highlightMatchingBrackets(): boolean;
        set highlightMatchingBrackets(val: boolean);

        /**
         * @default true
         */
        get implicit_trailing_newline(): boolean;
        set implicit_trailing_newline(val: boolean);

        /**
         * @default true
         */
        get implicitTrailingNewline(): boolean;
        set implicitTrailingNewline(val: boolean);

        /**
         * @default true
         */
        get indent_on_tab(): boolean;
        set indent_on_tab(val: boolean);

        /**
         * @default true
         */
        get indentOnTab(): boolean;
        set indentOnTab(val: boolean);

        /**
         * @default 8
         */
        get indent_width(): number;
        set indent_width(val: number);

        /**
         * @default 8
         */
        get indentWidth(): number;
        set indentWidth(val: number);

        /**
         * @default false
         */
        get insert_matching_brace(): boolean;
        set insert_matching_brace(val: boolean);

        /**
         * @default false
         */
        get insertMatchingBrace(): boolean;
        set insertMatchingBrace(val: boolean);

        /**
         * @default false
         */
        get insert_spaces_instead_of_tabs(): boolean;
        set insert_spaces_instead_of_tabs(val: boolean);

        /**
         * @default false
         */
        get insertSpacesInsteadOfTabs(): boolean;
        set insertSpacesInsteadOfTabs(val: boolean);

        /**
         * @default 1.1
         */
        get line_height(): number;
        set line_height(val: number);

        /**
         * @default 1.1
         */
        get lineHeight(): number;
        set lineHeight(val: number);

        /**
         * @default false
         */
        get override_indent_width(): boolean;
        set override_indent_width(val: boolean);

        /**
         * @default false
         */
        get overrideIndentWidth(): boolean;
        set overrideIndentWidth(val: boolean);

        /**
         * @default false
         */
        get overwrite_matching_brace(): boolean;
        set overwrite_matching_brace(val: boolean);

        /**
         * @default false
         */
        get overwriteMatchingBrace(): boolean;
        set overwriteMatchingBrace(val: boolean);

        /**
         * @default 80
         */
        get right_margin_position(): number;
        set right_margin_position(val: number);

        /**
         * @default 80
         */
        get rightMarginPosition(): number;
        set rightMarginPosition(val: number);

        /**
         * @default true
         */
        get show_diagnostics(): boolean;
        set show_diagnostics(val: boolean);

        /**
         * @default true
         */
        get showDiagnostics(): boolean;
        set showDiagnostics(val: boolean);

        /**
         * @default true
         */
        get show_line_changes(): boolean;
        set show_line_changes(val: boolean);

        /**
         * @default true
         */
        get showLineChanges(): boolean;
        set showLineChanges(val: boolean);

        /**
         * @default true
         */
        get show_line_changes_overview(): boolean;
        set show_line_changes_overview(val: boolean);

        /**
         * @default true
         */
        get showLineChangesOverview(): boolean;
        set showLineChangesOverview(val: boolean);

        /**
         * @default true
         */
        get show_line_numbers(): boolean;
        set show_line_numbers(val: boolean);

        /**
         * @default true
         */
        get showLineNumbers(): boolean;
        set showLineNumbers(val: boolean);

        /**
         * @default false
         */
        get show_right_margin(): boolean;
        set show_right_margin(val: boolean);

        /**
         * @default false
         */
        get showRightMargin(): boolean;
        set showRightMargin(val: boolean);

        /**
         * @default true
         */
        get smart_backspace(): boolean;
        set smart_backspace(val: boolean);

        /**
         * @default true
         */
        get smartBackspace(): boolean;
        set smartBackspace(val: boolean);

        /**
         * @default true
         */
        get smart_home_end(): boolean;
        set smart_home_end(val: boolean);

        /**
         * @default true
         */
        get smartHomeEnd(): boolean;
        set smartHomeEnd(val: boolean);

        /**
         * @default 8
         */
        get tab_width(): number;
        set tab_width(val: number);

        /**
         * @default 8
         */
        get tabWidth(): number;
        set tabWidth(val: number);

        /**
         * @default false
         */
        get use_custom_font(): boolean;
        set use_custom_font(val: boolean);

        /**
         * @default false
         */
        get useCustomFont(): boolean;
        set useCustomFont(val: boolean);

        /**
         * @default Foundry.TextWrap.NONE
         */
        get wrap(): TextWrap;
        set wrap(val: TextWrap);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TextSettings.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TextSettings.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TextSettings.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextSettings.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TextSettings.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextSettings.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TextSettings.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TextSettings.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        dup_custom_font(): string;

        get_auto_indent(): boolean;

        get_completion_auto_select(): boolean;

        get_completion_page_size(): number;

        get_enable_completion(): boolean;

        get_enable_snippets(): boolean;

        get_enable_spell_check(): boolean;

        get_highlight_current_line(): boolean;

        get_highlight_matching_brackets(): boolean;

        get_implicit_trailing_newline(): boolean;

        get_indent_on_tab(): boolean;

        get_indent_width(): number;

        get_insert_matching_brace(): boolean;

        get_insert_spaces_instead_of_tabs(): boolean;

        get_line_height(): number;

        get_override_indent_width(): boolean;

        get_overwrite_matching_brace(): boolean;

        get_right_margin_position(): number;

        get_show_diagnostics(): boolean;

        get_show_line_changes(): boolean;

        get_show_line_changes_overview(): boolean;

        get_show_line_numbers(): boolean;

        get_show_right_margin(): boolean;

        get_smart_backspace(): boolean;

        get_smart_home_end(): boolean;

        get_tab_width(): number;

        get_use_custom_font(): boolean;

        get_wrap(): TextWrap;

        /**
         * @param auto_indent 
         */
        set_auto_indent(auto_indent: boolean): void;

        /**
         * @param completion_auto_select 
         */
        set_completion_auto_select(completion_auto_select: boolean): void;

        /**
         * @param completion_page_size 
         */
        set_completion_page_size(completion_page_size: number): void;

        /**
         * @param custom_font 
         */
        set_custom_font(custom_font: string): void;

        /**
         * @param enable_completion 
         */
        set_enable_completion(enable_completion: boolean): void;

        /**
         * @param enable_snippets 
         */
        set_enable_snippets(enable_snippets: boolean): void;

        /**
         * @param enable_spell_check 
         */
        set_enable_spell_check(enable_spell_check: boolean): void;

        /**
         * @param highlight_current_line 
         */
        set_highlight_current_line(highlight_current_line: boolean): void;

        /**
         * @param highlight_matching_brackets 
         */
        set_highlight_matching_brackets(highlight_matching_brackets: boolean): void;

        /**
         * @param implicit_trailing_newline 
         */
        set_implicit_trailing_newline(implicit_trailing_newline: boolean): void;

        /**
         * @param indent_on_tab 
         */
        set_indent_on_tab(indent_on_tab: boolean): void;

        /**
         * @param indent_width 
         */
        set_indent_width(indent_width: number): void;

        /**
         * @param insert_matching_brace 
         */
        set_insert_matching_brace(insert_matching_brace: boolean): void;

        /**
         * @param insert_spaces_instead_of_tabs 
         */
        set_insert_spaces_instead_of_tabs(insert_spaces_instead_of_tabs: boolean): void;

        /**
         * @param line_height 
         */
        set_line_height(line_height: number): void;

        /**
         * @param override_indent_width 
         */
        set_override_indent_width(override_indent_width: boolean): void;

        /**
         * @param overwrite_matching_brace 
         */
        set_overwrite_matching_brace(overwrite_matching_brace: boolean): void;

        /**
         * @param right_margin_position 
         */
        set_right_margin_position(right_margin_position: number): void;

        /**
         * @param show_diagnostics 
         */
        set_show_diagnostics(show_diagnostics: boolean): void;

        /**
         * @param show_line_changes 
         */
        set_show_line_changes(show_line_changes: boolean): void;

        /**
         * @param show_line_changes_overview 
         */
        set_show_line_changes_overview(show_line_changes_overview: boolean): void;

        /**
         * @param show_line_numbers 
         */
        set_show_line_numbers(show_line_numbers: boolean): void;

        /**
         * @param show_right_margin 
         */
        set_show_right_margin(show_right_margin: boolean): void;

        /**
         * @param smart_backspace 
         */
        set_smart_backspace(smart_backspace: boolean): void;

        /**
         * @param smart_home_end 
         */
        set_smart_home_end(smart_home_end: boolean): void;

        /**
         * @param tab_width 
         */
        set_tab_width(tab_width: number): void;

        /**
         * @param use_custom_font 
         */
        set_use_custom_font(use_custom_font: boolean): void;

        /**
         * @param wrap 
         */
        set_wrap(wrap: TextWrap): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace TextSettingsProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            /**
             * @signal
             * @run-last
             */
            changed: (arg0: TextSetting) => void;
            "notify::document": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            document: TextDocument;
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for providing text editor settings.
     * 
     * FoundryTextSettingsProvider provides the core interface for managing text
     * editor settings and configuration. Concrete implementations handle specific
     * settings sources and provide specialized functionality for different
     * editor contexts and configuration management systems.
     * @gir-type Class
     */
    abstract class TextSettingsProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<TextSettingsProvider>;

        // Properties
        /**
         * @read-only
         */
        get document(): TextDocument;

        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TextSettingsProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TextSettingsProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TextSettingsProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextSettingsProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TextSettingsProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TextSettingsProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TextSettingsProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TextSettingsProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param setting 
         * @virtual
         */
        vfunc_changed(setting: TextSetting): void;

        /**
         * @param setting 
         * @param value 
         * @virtual
         */
        vfunc_get_setting(setting: TextSetting, value: unknown): boolean;

        // Methods
        /**
         * The document the settings should represent, if any.
         */
        dup_document(): (TextDocument | null);

        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * @param setting 
         */
        emit_changed(setting: TextSetting): void;

        /**
         * @param setting 
         * @param value 
         */
        get_setting(setting: TextSetting, value: (GObject.Value | any)): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace TtyAuthProvider {
        // Signal signatures
        interface SignalSignatures extends AuthProvider.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends AuthProvider.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Auth provider which requests credentials from a TTY.
     * @gir-type Class
     */
    class TtyAuthProvider extends AuthProvider implements Json.Serializable {
        static $gtype: GObject.GType<TtyAuthProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TtyAuthProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TtyAuthProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](pty_fd: number): TtyAuthProvider;

        // Signals
        /** @signal */
        connect<K extends keyof TtyAuthProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TtyAuthProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TtyAuthProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TtyAuthProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TtyAuthProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TtyAuthProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace Tweak {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::display-hint": (pspec: GObject.ParamSpec) => void;
            "notify::icon": (pspec: GObject.ParamSpec) => void;
            "notify::path": (pspec: GObject.ParamSpec) => void;
            "notify::section": (pspec: GObject.ParamSpec) => void;
            "notify::subtitle": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            display_hint: string;
            displayHint: string;
            icon: Gio.Icon;
            path: string;
            section: string;
            subtitle: string;
            title: string;
        }
    }

    /**
     * Represents a tweak that may be altered by a user.
     * @gir-type Class
     */
    abstract class Tweak extends GObject.Object {
        static $gtype: GObject.GType<Tweak>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get display_hint(): string;

        /**
         * @read-only
         * @default null
         */
        get displayHint(): string;

        /**
         * @read-only
         */
        get icon(): Gio.Icon;

        /**
         * @read-only
         * @default null
         */
        get path(): string;

        /**
         * @read-only
         * @default null
         */
        get section(): string;

        /**
         * @read-only
         * @default null
         */
        get subtitle(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Tweak.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Tweak.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Tweak.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Tweak.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Tweak.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Tweak.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Tweak.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Tweak.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param context a {@link Foundry.Context}
         * @virtual
         */
        vfunc_create_input(context: Context): (Input | null);

        /**
         * @virtual
         */
        vfunc_dup_display_hint(): string;

        /**
         * @virtual
         */
        vfunc_dup_icon(): (Gio.Icon | null);

        /**
         * @virtual
         */
        vfunc_dup_path(): string;

        /**
         * @virtual
         */
        vfunc_dup_section(): string;

        /**
         * @virtual
         */
        vfunc_dup_sort_key(): string;

        /**
         * @virtual
         */
        vfunc_dup_subtitle(): string;

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        // Methods
        /**
         * @param context a {@link Foundry.Context}
         */
        create_input(context: Context): (Input | null);

        dup_display_hint(): string;

        dup_icon(): (Gio.Icon | null);

        dup_path(): string;

        dup_section(): string;

        dup_sort_key(): string;

        dup_subtitle(): string;

        dup_title(): string;
    }


    namespace TweakManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Service.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Manages tweaks and configuration adjustments.
     * 
     * FoundryTweakManager coordinates tweak providers and manages the application
     * of configuration tweaks. It provides a unified interface for managing
     * development environment customizations and integrates with the settings
     * system for persistent configuration management.
     * @gir-type Class
     */
    class TweakManager extends Service implements Json.Serializable {
        static $gtype: GObject.GType<TweakManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TweakManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TweakManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TweakManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TweakManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TweakManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TweakManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TweakManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TweakManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param path the tweaks path
         * @returns A {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.Tweak}.
         */
        list_children(path: string): (Dex.Future | null);
    }


    namespace TweakProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {

        }
    }

    /**
     * Abstract base class for plugins to provide tweaks.
     * @gir-type Class
     */
    abstract class TweakProvider extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<TweakProvider>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TweakProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<TweakProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof TweakProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TweakProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TweakProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TweakProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TweakProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TweakProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param gettext_package 
         * @param base_path 
         * @param info 
         * @param n_infos 
         * @param environment 
         */
        register(gettext_package: string, base_path: string, info: TweakInfo, n_infos: number, environment: string): number;

        /**
         * @param registration 
         */
        unregister(registration: number): void;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace UnixFDMap {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class UnixFDMap extends GObject.Object {
        static $gtype: GObject.GType<UnixFDMap>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: UnixFDMap.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<UnixFDMap.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): UnixFDMap;

        // Signals
        /** @signal */
        connect<K extends keyof UnixFDMap.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, UnixFDMap.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof UnixFDMap.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, UnixFDMap.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof UnixFDMap.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<UnixFDMap.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Creates a {@link Gio.IOStream} to communicate with another process.
         * 
         * Use this to create a {@link Gio.IOStream} to use from the calling process to communicate
         * with a subprocess. Generally, you should pass STDIN_FILENO for `dest_read_fd`
         * and STDOUT_FILENO for `dest_write_fd`.
         * @param dest_read_fd the FD number in the destination process for the read side (stdin)
         * @param dest_write_fd the FD number in the destination process for the write side (stdout)
         * @returns a {@link Gio.IOStream} if successful; otherwise `null` and   `error` is set.
         */
        create_stream(dest_read_fd: number, dest_write_fd: number): Gio.IOStream;

        /**
         * @param index 
         * @param dest_fd 
         */
        get(index: number, dest_fd: number): number;

        get_length(): number;

        get_max_dest_fd(): number;

        /**
         * @param filename 
         * @param mode 
         * @param dest_fd 
         */
        open_file(filename: string, mode: number, dest_fd: number): boolean;

        /**
         * @param index 
         * @param dest_fd 
         */
        peek(index: number, dest_fd: number): number;

        peek_stderr(): number;

        peek_stdin(): number;

        peek_stdout(): number;

        /**
         * @param dest_fd 
         */
        silence_fd(dest_fd: number): boolean;

        stderr_isatty(): boolean;

        stdin_isatty(): boolean;

        stdout_isatty(): boolean;

        /**
         * @param index 
         * @param dest_fd 
         */
        steal(index: number, dest_fd: number): number;

        /**
         * @param other 
         */
        steal_from(other: UnixFDMap): boolean;

        steal_stderr(): number;

        steal_stdin(): number;

        steal_stdout(): number;

        /**
         * @param source_fd 
         * @param dest_fd 
         */
        take(source_fd: number, dest_fd: number): void;
    }


    namespace Vcs {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            /**
             * @signal
             * @run-last
             */
            "tip-changed": () => void;
            "notify::active": (pspec: GObject.ParamSpec) => void;
            "notify::branch-name": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::priority": (pspec: GObject.ParamSpec) => void;
            "notify::provider": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Contextual.ConstructorProps, Json.Serializable.ConstructorProps {
            active: boolean;
            branch_name: string;
            branchName: string;
            id: string;
            name: string;
            priority: number;
            provider: VcsProvider;
        }
    }

    /**
     * Abstract base class for plugins to implement support for a VCS.
     * @gir-type Class
     */
    abstract class Vcs extends Contextual implements Json.Serializable {
        static $gtype: GObject.GType<Vcs>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get active(): boolean;

        /**
         * @read-only
         * @default null
         */
        get branch_name(): string;

        /**
         * @read-only
         * @default null
         */
        get branchName(): string;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         * @default 0
         */
        get priority(): number;

        /**
         * @read-only
         */
        get provider(): VcsProvider;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Vcs.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<Vcs.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof Vcs.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Vcs.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Vcs.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Vcs.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Vcs.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Vcs.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param file a {@link Foundry.VcsFile}
         * @param bytes optional contents for the file
         * @virtual
         */
        vfunc_blame(file: VcsFile, bytes: (GLib.Bytes | null)): Dex.Future;

        /**
         * @param file a {@link Foundry.VcsFile}
         * @param contents the contents of the file
         * @virtual
         */
        vfunc_describe_line_changes(file: VcsFile, contents: GLib.Bytes): Dex.Future;

        /**
         * Diffs two {@link Foundry.VcsTree} resulting in a {@link Foundry.VcsDiff}
         * @param tree_a the old tree
         * @param tree_b the new tree
         * @virtual
         */
        vfunc_diff(tree_a: VcsTree, tree_b: VcsTree): Dex.Future;

        /**
         * Gets the name of the branch such as "main".
         * @virtual
         */
        vfunc_dup_branch_name(): string;

        /**
         * Gets the identifier for the VCS such as "git" or "none".
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * Gets the name of the vcs in title format such as "Git"
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * @param remote a {@link Foundry.VcsRemote}
         * @param operation a {@link Foundry.Operation}
         * @virtual
         */
        vfunc_fetch(remote: VcsRemote, operation: Operation): Dex.Future;

        /**
         * Finds a commit by identifier
         * @param id the identifier of the commit
         * @virtual
         */
        vfunc_find_commit(id: string): Dex.Future;

        /**
         * @param file the file to locate
         * @virtual
         */
        vfunc_find_file(file: Gio.File): Dex.Future;

        /**
         * @param name the name of the remote
         * @virtual
         */
        vfunc_find_remote(name: string): Dex.Future;

        /**
         * Finds a {@link Foundry.VcsTree} by tree identifier
         * @param id the identifier of the tree
         * @virtual
         */
        vfunc_find_tree(id: string): Dex.Future;

        /**
         * @virtual
         */
        vfunc_get_priority(): number;

        /**
         * @param file 
         * @virtual
         */
        vfunc_is_file_ignored(file: Gio.File): boolean;

        /**
         * @param relative_path 
         * @virtual
         */
        vfunc_is_ignored(relative_path: string): boolean;

        /**
         * @virtual
         */
        vfunc_list_branches(): Dex.Future;

        /**
         * @param file a {@link Foundry.VcsFile}
         * @virtual
         */
        vfunc_list_commits_with_file(file: VcsFile): Dex.Future;

        /**
         * List all files in the repository.
         * 
         * It is not required that implementations return files that are not
         * indexed in their caches from this method.
         * @virtual
         */
        vfunc_list_files(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_list_remotes(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_list_tags(): Dex.Future;

        /**
         * Loads the tip (most recent commit) from the repository.
         * @virtual
         */
        vfunc_load_tip(): Dex.Future;

        /**
         * Queries the state of `file` in the repository.
         * @param file 
         * @virtual
         */
        vfunc_query_file_status(file: Gio.File): Dex.Future;

        // Methods
        /**
         * @param file a {@link Foundry.VcsFile}
         * @param bytes optional contents for the file
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsBlame} or rejects with error
         */
        blame(file: VcsFile, bytes: (GLib.Bytes | null)): Dex.Future;

        /**
         * @param file a {@link Foundry.VcsFile}
         * @param contents the contents of the file
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsLineChanges} or rejects with error.
         */
        describe_line_changes(file: VcsFile, contents: (GLib.Bytes | Uint8Array)): Dex.Future;

        /**
         * Diffs two {@link Foundry.VcsTree} resulting in a {@link Foundry.VcsDiff}
         * @param tree_a the old tree
         * @param tree_b the new tree
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsDiff} or rejects with error.
         */
        diff(tree_a: VcsTree, tree_b: VcsTree): Dex.Future;

        /**
         * Gets the name of the branch such as "main".
         * @returns a string containing the branch name
         */
        dup_branch_name(): string;

        /**
         * Gets the identifier for the VCS such as "git" or "none".
         * @returns a string containing the identifier
         */
        dup_id(): string;

        /**
         * Gets the name of the vcs in title format such as "Git"
         * @returns a string containing the name
         */
        dup_name(): string;

        /**
         * Emits the "tip-changed" signal. This method is intended for use
         * by subclasses to notify when the tip (most recent commit) has changed.
         */
        emit_tip_changed(): void;

        /**
         * @param remote a {@link Foundry.VcsRemote}
         * @param operation a {@link Foundry.Operation}
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        fetch(remote: VcsRemote, operation: Operation): Dex.Future;

        /**
         * Finds a commit by identifier
         * @param id the identifier of the commit
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsCommit}.
         */
        find_commit(id: string): Dex.Future;

        /**
         * @param file the file to locate
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsFile} or rejects with error
         */
        find_file(file: Gio.File): Dex.Future;

        /**
         * @param name the name of the remote
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsRemote}.
         */
        find_remote(name: string): Dex.Future;

        /**
         * Finds a {@link Foundry.VcsTree} by tree identifier
         * @param id the identifier of the tree
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsTree}.
         */
        find_tree(id: string): Dex.Future;

        get_active(): boolean;

        get_priority(): number;

        /**
         * @param file 
         */
        is_file_ignored(file: Gio.File): boolean;

        /**
         * @param relative_path 
         */
        is_ignored(relative_path: string): boolean;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.VcsBranch}.
         */
        list_branches(): Dex.Future;

        /**
         * @param file a {@link Foundry.VcsFile}
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.VcsCommit}
         */
        list_commits_with_file(file: VcsFile): Dex.Future;

        /**
         * List all files in the repository.
         * 
         * It is not required that implementations return files that are not
         * indexed in their caches from this method.
         * @returns a {@link Dex.Future} that resolves   to {@link Gio.ListModel} of {@link Foundry.VcsFile}.
         */
        list_files(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.VcsRemote}.
         */
        list_remotes(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.VcsTag}.
         */
        list_tags(): Dex.Future;

        /**
         * Loads the tip (most recent commit) from the repository.
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsCommit} or rejects with error.
         */
        load_tip(): Dex.Future;

        /**
         * Queries the state of `file` in the repository.
         * @param file 
         * @returns a {@link Dex.Future} that resolves to   a {@link Foundry.VcsFileStatus} or rejects with error.
         */
        query_file_status(file: Gio.File): Dex.Future;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace VcsBlame {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            file: VcsFile;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsBlame extends GObject.Object {
        static $gtype: GObject.GType<VcsBlame>;

        // Properties
        /**
         * @construct-only
         */
        get file(): VcsFile;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsBlame.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsBlame.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsBlame.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsBlame.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsBlame.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsBlame.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsBlame.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsBlame.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets the number of lines contained in the blame.
         * @virtual
         */
        vfunc_get_n_lines(): number;

        /**
         * Queries the signature of the commit that modified `line`.
         * @param line the line number, starting from 0
         * @virtual
         */
        vfunc_query_line(line: number): (VcsSignature | null);

        /**
         * Update the blame using the contents in `bytes`.
         * 
         * If `bytes` is `null` then the underlying file contents will be used as if
         * no modifications were provided.
         * @param bytes data for the blame or `null` to reset to file defaults
         * @virtual
         */
        vfunc_update(bytes: (GLib.Bytes | null)): Dex.Future;

        // Methods
        /**
         * Gets the underlying file being blamed
         * @returns a {@link Foundry.VcsFile}
         */
        dup_file(): VcsFile;

        /**
         * Gets the number of lines contained in the blame.
         * @returns A value < `G_MAXUINT`
         */
        get_n_lines(): number;

        /**
         * Queries the signature of the commit that modified `line`.
         * @param line the line number, starting from 0
         * @returns a {@link Foundry.VcsSignature} or `null`   if there is no commit related to the changes on `line`.
         */
        query_line(line: number): (VcsSignature | null);

        /**
         * Update the blame using the contents in `bytes`.
         * 
         * If `bytes` is `null` then the underlying file contents will be used as if
         * no modifications were provided.
         * @param bytes data for the blame or `null` to reset to file defaults
         * @returns a {@link Dex.Future} that resolves   to any value or rejects with error
         */
        update(bytes: (GLib.Bytes | null)): Dex.Future;
    }


    namespace VcsBranch {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::is-local": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
            is_local: boolean;
            isLocal: boolean;
            title: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsBranch extends GObject.Object {
        static $gtype: GObject.GType<VcsBranch>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default true
         */
        get is_local(): boolean;

        /**
         * @read-only
         * @default true
         */
        get isLocal(): boolean;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsBranch.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsBranch.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsBranch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsBranch.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsBranch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsBranch.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsBranch.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsBranch.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        /**
         * @virtual
         */
        vfunc_is_local(): boolean;

        /**
         * Get the target reference the branch currently points at.
         * @virtual
         */
        vfunc_load_target(): Dex.Future;

        // Methods
        dup_id(): string;

        dup_title(): string;

        /**
         * Get the target reference the branch currently points at.
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsReference} or rejects with error.
         */
        load_target(): Dex.Future;
    }


    namespace VcsCommit {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::author": (pspec: GObject.ParamSpec) => void;
            "notify::committer": (pspec: GObject.ParamSpec) => void;
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            author: VcsSignature;
            committer: VcsSignature;
            id: string;
            title: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsCommit extends GObject.Object {
        static $gtype: GObject.GType<VcsCommit>;

        // Properties
        /**
         * @read-only
         */
        get author(): VcsSignature;

        /**
         * @read-only
         */
        get committer(): VcsSignature;

        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsCommit.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsCommit.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsCommit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsCommit.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsCommit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsCommit.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsCommit.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsCommit.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_author(): (VcsSignature | null);

        /**
         * @virtual
         */
        vfunc_dup_committer(): (VcsSignature | null);

        /**
         * @virtual
         */
        vfunc_dup_id(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_title(): (string | null);

        /**
         * Counts the number of parents of the commit
         * @virtual
         */
        vfunc_get_n_parents(): number;

        /**
         * Loads the delta for a single file comparing this commit to its parent.
         * This extracts the changes for `relative_path` from the commit's parent
         * commit.
         * @param relative_path the relative path of the file to get the delta for
         * @virtual
         */
        vfunc_load_delta(relative_path: string): Dex.Future;

        /**
         * @param index the index of the parent
         * @virtual
         */
        vfunc_load_parent(index: number): Dex.Future;

        /**
         * @virtual
         */
        vfunc_load_tree(): Dex.Future;

        // Methods
        dup_author(): (VcsSignature | null);

        dup_committer(): (VcsSignature | null);

        dup_id(): (string | null);

        dup_title(): (string | null);

        /**
         * Counts the number of parents of the commit
         */
        get_n_parents(): number;

        /**
         * Loads the delta for a single file comparing this commit to its parent.
         * This extracts the changes for `relative_path` from the commit's parent
         * commit.
         * @param relative_path the relative path of the file to get the delta for
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsDelta} or rejects with error
         */
        load_delta(relative_path: string): Dex.Future;

        /**
         * @param index the index of the parent
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsCommit} or rejects with error
         */
        load_parent(index: number): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsTree} or rejects with error
         */
        load_tree(): Dex.Future;
    }


    namespace VcsDelta {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::new-path": (pspec: GObject.ParamSpec) => void;
            "notify::old-path": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            new_path: string;
            newPath: string;
            old_path: string;
            oldPath: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsDelta extends GObject.Object {
        static $gtype: GObject.GType<VcsDelta>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get new_path(): string;

        /**
         * @read-only
         * @default null
         */
        get newPath(): string;

        /**
         * @read-only
         * @default null
         */
        get old_path(): string;

        /**
         * @read-only
         * @default null
         */
        get oldPath(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsDelta.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsDelta.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsDelta.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsDelta.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsDelta.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsDelta.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsDelta.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsDelta.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_new_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_new_path(): string;

        /**
         * @virtual
         */
        vfunc_dup_old_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_old_path(): string;

        /**
         * @virtual
         */
        vfunc_get_new_mode(): number;

        /**
         * @virtual
         */
        vfunc_get_old_mode(): number;

        /**
         * @virtual
         */
        vfunc_get_status(): VcsDeltaStatus;

        /**
         * Queries the VCS for the list of hunks that are part of this delta.
         * @virtual
         */
        vfunc_list_hunks(): Dex.Future;

        /**
         * Serializes the delta to a unified diff format string.
         * @param context_lines the number of context lines to include
         * @virtual
         */
        vfunc_serialize(context_lines: number): Dex.Future;

        // Methods
        dup_new_id(): string;

        dup_new_path(): string;

        dup_old_id(): string;

        dup_old_path(): string;

        get_new_mode(): number;

        get_old_mode(): number;

        get_status(): VcsDeltaStatus;

        /**
         * Queries the VCS for the list of hunks that are part of this delta.
         * @returns a {@link Dex.Future} that resolves to   a {@link Gio.ListModel} of {@link Foundry.VcsDiffHunk}.
         */
        list_hunks(): Dex.Future;

        /**
         * Serializes the delta to a unified diff format string.
         * @param context_lines the number of context lines to include
         * @returns a {@link Dex.Future} that resolves to   a string containing the serialized diff
         */
        serialize(context_lines: number): Dex.Future;
    }


    namespace VcsDiff {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsDiff extends GObject.Object {
        static $gtype: GObject.GType<VcsDiff>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsDiff.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsDiff.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsDiff.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsDiff.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsDiff.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsDiff.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsDiff.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsDiff.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_list_deltas(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_load_stats(): Dex.Future;

        // Methods
        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Gio.ListModel} of {@link Foundry.VcsDelta} or rejects with error
         */
        list_deltas(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsStats} or rejects with error
         */
        load_stats(): Dex.Future;
    }


    namespace VcsDiffHunk {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::header": (pspec: GObject.ParamSpec) => void;
            "notify::new-lines": (pspec: GObject.ParamSpec) => void;
            "notify::new-start": (pspec: GObject.ParamSpec) => void;
            "notify::old-lines": (pspec: GObject.ParamSpec) => void;
            "notify::old-start": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            header: string;
            new_lines: number;
            newLines: number;
            new_start: number;
            newStart: number;
            old_lines: number;
            oldLines: number;
            old_start: number;
            oldStart: number;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsDiffHunk extends GObject.Object {
        static $gtype: GObject.GType<VcsDiffHunk>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get header(): string;

        /**
         * @read-only
         * @default 0
         */
        get new_lines(): number;

        /**
         * @read-only
         * @default 0
         */
        get newLines(): number;

        /**
         * @read-only
         * @default 0
         */
        get new_start(): number;

        /**
         * @read-only
         * @default 0
         */
        get newStart(): number;

        /**
         * @read-only
         * @default 0
         */
        get old_lines(): number;

        /**
         * @read-only
         * @default 0
         */
        get oldLines(): number;

        /**
         * @read-only
         * @default 0
         */
        get old_start(): number;

        /**
         * @read-only
         * @default 0
         */
        get oldStart(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsDiffHunk.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsDiffHunk.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsDiffHunk.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsDiffHunk.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsDiffHunk.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsDiffHunk.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsDiffHunk.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsDiffHunk.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * The header for the hunk which is the part after the second "@@".
         * @virtual
         */
        vfunc_dup_header(): (string | null);

        /**
         * Gets the number of lines in the new file for this hunk.
         * @virtual
         */
        vfunc_get_new_lines(): number;

        /**
         * Gets the starting line number in the new file for this hunk.
         * @virtual
         */
        vfunc_get_new_start(): number;

        /**
         * Gets the number of lines in the old file for this hunk.
         * @virtual
         */
        vfunc_get_old_lines(): number;

        /**
         * Gets the starting line number in the old file for this hunk.
         * @virtual
         */
        vfunc_get_old_start(): number;

        /**
         * List the lines within a hunk.
         * @virtual
         */
        vfunc_list_lines(): Dex.Future;

        // Methods
        /**
         * The header for the hunk which is the part after the second "@@".
         */
        dup_header(): (string | null);

        /**
         * Gets the number of lines in the new file for this hunk.
         * @returns the number of lines in the new file
         */
        get_new_lines(): number;

        /**
         * Gets the starting line number in the new file for this hunk.
         * @returns the starting line number in the new file
         */
        get_new_start(): number;

        /**
         * Gets the number of lines in the old file for this hunk.
         * @returns the number of lines in the old file
         */
        get_old_lines(): number;

        /**
         * Gets the starting line number in the old file for this hunk.
         * @returns the starting line number in the old file
         */
        get_old_start(): number;

        /**
         * List the lines within a hunk.
         * @returns a {@link Dex.Future} that resolves   to an {@link Gio.ListModel} of {@link Foundry.VcsDiffLine}.
         */
        list_lines(): Dex.Future;
    }


    namespace VcsDiffLine {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::has-newline": (pspec: GObject.ParamSpec) => void;
            "notify::length": (pspec: GObject.ParamSpec) => void;
            "notify::new-line": (pspec: GObject.ParamSpec) => void;
            "notify::old-line": (pspec: GObject.ParamSpec) => void;
            "notify::origin": (pspec: GObject.ParamSpec) => void;
            "notify::text": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            has_newline: boolean;
            hasNewline: boolean;
            length: (bigint | number);
            new_line: number;
            newLine: number;
            old_line: number;
            oldLine: number;
            origin: VcsDiffLineOrigin;
            text: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsDiffLine extends GObject.Object {
        static $gtype: GObject.GType<VcsDiffLine>;

        // Properties
        /**
         * @read-only
         * @default false
         */
        get has_newline(): boolean;

        /**
         * @read-only
         * @default false
         */
        get hasNewline(): boolean;

        /**
         * @read-only
         * @default 0
         */
        get length(): number;

        /**
         * @read-only
         * @default 0
         */
        get new_line(): number;

        /**
         * @read-only
         * @default 0
         */
        get newLine(): number;

        /**
         * @read-only
         * @default 0
         */
        get old_line(): number;

        /**
         * @read-only
         * @default 0
         */
        get oldLine(): number;

        /**
         * @read-only
         * @default Foundry.VcsDiffLineOrigin.CONTEXT
         */
        get origin(): VcsDiffLineOrigin;

        /**
         * @read-only
         * @default null
         */
        get text(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsDiffLine.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsDiffLine.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsDiffLine.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsDiffLine.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsDiffLine.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsDiffLine.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsDiffLine.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsDiffLine.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_text(): string;

        /**
         * @virtual
         */
        vfunc_get_has_newline(): boolean;

        /**
         * @virtual
         */
        vfunc_get_length(): (bigint | number);

        /**
         * @virtual
         */
        vfunc_get_new_line(): number;

        /**
         * @virtual
         */
        vfunc_get_old_line(): number;

        /**
         * @virtual
         */
        vfunc_get_origin(): VcsDiffLineOrigin;

        // Methods
        dup_text(): string;

        get_has_newline(): boolean;

        get_length(): number;

        get_new_line(): number;

        get_old_line(): number;

        get_origin(): VcsDiffLineOrigin;
    }


    namespace VcsFile {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::relative-path": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            file: Gio.File;
            relative_path: string;
            relativePath: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsFile extends GObject.Object {
        static $gtype: GObject.GType<VcsFile>;

        // Properties
        /**
         * @read-only
         */
        get file(): Gio.File;

        /**
         * @read-only
         * @default null
         */
        get relative_path(): string;

        /**
         * @read-only
         * @default null
         */
        get relativePath(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsFile.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_file(): Gio.File;

        /**
         * @virtual
         */
        vfunc_dup_relative_path(): string;

        // Methods
        /**
         * @returns a {@link Gio.File}
         */
        dup_file(): Gio.File;

        dup_relative_path(): string;
    }


    namespace VcsLineChanges {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsLineChanges extends GObject.Object {
        static $gtype: GObject.GType<VcsLineChanges>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsLineChanges.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsLineChanges.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsLineChanges.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsLineChanges.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsLineChanges.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsLineChanges.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsLineChanges.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsLineChanges.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param first_line 
         * @param last_line 
         * @param foreach 
         * @virtual
         */
        vfunc_foreach(first_line: number, last_line: number, foreach: VcsLineChangesForeach): void;

        /**
         * @param line 
         * @virtual
         */
        vfunc_query_line(line: number): VcsLineChange;

        // Methods
        /**
         * @param first_line 
         * @param last_line 
         * @param foreach 
         */
        foreach(first_line: number, last_line: number, foreach: VcsLineChangesForeach): void;

        /**
         * @param line 
         */
        query_line(line: number): VcsLineChange;
    }


    namespace VcsManager {
        // Signal signatures
        interface SignalSignatures extends Service.SignalSignatures {
            "notify::vcs": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Service.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            vcs: Vcs;
        }
    }

    /**
     * Manages VCS implementations provided by plugins.
     * @gir-type Class
     */
    class VcsManager<A extends GObject.Object = GObject.Object> extends Service implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<VcsManager>;

        // Properties
        get vcs(): Vcs;
        set vcs(val: Vcs);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsManager.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get the active {@link Foundry.Vcs}.
         * @returns a {@link Foundry.Vcs}
         */
        dup_vcs(): (Vcs | null);

        /**
         * Looks through available VCS providers to find one matching `module_name`.
         * 
         * The function matches providers by comparing `module_name` with the
         * {@link Peas.PluginInfo.module_name} of each provider's plugin info.
         * @param module_name the module name to match against plugin info
         * @returns a {@link Foundry.VcsProvider} or `null`
         */
        find_provider(module_name: string): (VcsProvider | null);

        /**
         * Looks through available vcss to find one matching `vcs_id`.
         * @param vcs_id an identifier matching a {@link Foundry.Vcs.id}
         * @returns a {@link Foundry.Vcs} or `null`
         */
        find_vcs(vcs_id: string): (Vcs | null);

        /**
         * @param vcs 
         */
        set_vcs(vcs: Vcs): void;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;
    }


    namespace VcsProvider {
        // Signal signatures
        interface SignalSignatures extends Contextual.SignalSignatures {
            "notify::plugin-info": (pspec: GObject.ParamSpec) => void;
            "notify::context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps<A extends GObject.Object = GObject.Object> extends Contextual.ConstructorProps, Gio.ListModel.ConstructorProps, Json.Serializable.ConstructorProps {
            plugin_info: Peas.PluginInfo;
            pluginInfo: Peas.PluginInfo;
        }
    }

    /**
     * Abstract base class for plugins to implement a version control system
     * such as Git, Mercurial, Subversion, or CVS.
     * @gir-type Class
     */
    abstract class VcsProvider<A extends GObject.Object = GObject.Object> extends Contextual implements Gio.ListModel<A>, Json.Serializable {
        static $gtype: GObject.GType<VcsProvider>;

        // Properties
        /**
         * @construct-only
         */
        get plugin_info(): Peas.PluginInfo;

        /**
         * @construct-only
         */
        get pluginInfo(): Peas.PluginInfo;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsProvider.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsProvider.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsProvider.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsProvider.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsProvider.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsProvider.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsProvider.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @virtual
         */
        vfunc_dup_name(): string;

        /**
         * Initialize a repository for the current project.
         * 
         * The provider should also update the VcsManager to the new Vcs
         * that is created/initialized as part of this.
         * @virtual
         */
        vfunc_initialize(): Dex.Future;

        /**
         * Checks if a URI is supported by the VCS provider.
         * 
         * This is useful to determine if you can get a downloader for a URI
         * to clone the repository.
         * @param uri_string 
         * @virtual
         */
        vfunc_supports_uri(uri_string: string): boolean;

        // Methods
        /**
         * Gets a name for the provider that is expected to be displayed to
         * users such as "Flatpak".
         * @returns the name of the provider
         */
        dup_name(): string;

        /**
         * Gets the plugin the provider belongs to.
         * @returns a {@link Peas.PluginInfo} or `null`
         */
        dup_plugin_info(): (Peas.PluginInfo | null);

        /**
         * Initialize a repository for the current project.
         * 
         * The provider should also update the VcsManager to the new Vcs
         * that is created/initialized as part of this.
         * @returns a {@link Dex.Future} that resolves to any   value or rejects with error.
         */
        initialize(): Dex.Future;

        /**
         * @param vcs 
         */
        set_vcs(vcs: Vcs): void;

        /**
         * Checks if a URI is supported by the VCS provider.
         * 
         * This is useful to determine if you can get a downloader for a URI
         * to clone the repository.
         * @param uri_string 
         * @returns `true` if the URI is supported
         */
        supports_uri(uri_string: string): boolean;

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @returns the {@link GObject.GType} of the items contained in `list`.
         */
        get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @returns the number of items in `list`.
         */
        get_n_items(): number;

        /**
         * Get the item at `position`.
         * 
         * If `position` is greater than the number of items in `list`, `null` is
         * returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.
         * 
         * This function is meant to be used by language bindings in place
         * of `g_list_model_get_item()`.
         * 
         * See also: `g_list_model_get_n_items()`
         * @param position the position of the item to fetch
         * @returns the object at `position`.
         */
        get_item(position: number): (A | null);

        /**
         * Emits the {@link Gio.ListModel.SignalSignatures.items_changed | Gio.ListModel::items-changed} signal on `list`.
         * 
         * This function should only be called by classes implementing
         * {@link Gio.ListModel}. It has to be called after the internal representation
         * of `list` has been updated, because handlers connected to this signal
         * might query the new state of the list.
         * 
         * Implementations must only make changes to the model (as visible to
         * its consumer) in places that will not cause problems for that
         * consumer.  For models that are driven directly by a write API (such
         * as {@link Gio.ListStore}), changes can be reported in response to uses of that
         * API.  For models that represent remote data, changes should only be
         * made from a fresh mainloop dispatch.  It is particularly not
         * permitted to make changes in response to a call to the {@link Gio.ListModel}
         * consumer API.
         * 
         * Stated another way: in general, it is assumed that code making a
         * series of accesses to the model via the API, without returning to the
         * mainloop, and without calling other code, will continue to view the
         * same contents of the model.
         * @param position the position at which `list` changed
         * @param removed the number of items removed
         * @param added the number of items added
         */
        items_changed(position: number, removed: number, added: number): void;

        /**
         * Get the item at `position`. If `position` is greater than the number of
         * items in `list`, `null` is returned.
         * 
         * `null` is never returned for an index that is smaller than the length
         * of the list.  See `g_list_model_get_n_items()`.
         * 
         * The same {@link GObject.Object} instance may not appear more than once in a {@link Gio.ListModel}.
         * @param position the position of the item to fetch
         * @virtual
         */
        vfunc_get_item(position: number): (A | null);

        /**
         * Gets the type of the items in `list`.
         * 
         * All items returned from `g_list_model_get_item()` are of the type
         * returned by this function, or a subtype, or if the type is an
         * interface, they are an implementation of that interface.
         * 
         * The item type of a {@link Gio.ListModel} can not change during the life of the
         * model.
         * @virtual
         */
        vfunc_get_item_type(): GObject.GType;

        /**
         * Gets the number of items in `list`.
         * 
         * Depending on the model implementation, calling this function may be
         * less efficient than iterating the list with increasing values for
         * `position` until `g_list_model_get_item()` returns `null`.
         * @virtual
         */
        vfunc_get_n_items(): number;

        /**
         * Calls the default implementation of the {@link Json.Serializable.deserialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `deserialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * gboolean res;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * res = iface->deserialize_property (serializable, property_name,
         *                                    value,
         *                                    pspec,
         *                                    property_node);
         * ```
         * @param property_name the name of the property to deserialize
         * @param value a pointer to an uninitialized value
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        default_deserialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec, property_node: Json.Node): boolean;

        /**
         * Calls the default implementation of the {@link Json.Serializable.serialize_property}
         * virtual function.
         * 
         * This function can be used inside a custom implementation of the
         * `serialize_property()` virtual function in lieu of calling the
         * default implementation through `g_type_default_interface_peek()`:
         * 
         * ```c
         * JsonSerializable *iface;
         * JsonNode *node;
         * 
         * iface = g_type_default_interface_peek (JSON_TYPE_SERIALIZABLE);
         * node = iface->serialize_property (serializable, property_name,
         *                                   value,
         *                                   pspec);
         * ```
         * 
         * This function will return `NULL` if the property could not be
         * serialized.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the   serialized property
         */
        default_serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @returns `TRUE` if the property was successfully deserialized
         */
        deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, unknown];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @returns the property description
         */
        find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         */
        get_property(pspec: GObject.ParamSpec): unknown;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.get_property
        get_property(...args: never[]): any;

        /**
         * Calls the {@link Json.Serializable.list_properties} implementation on
         * the {@link Json.Serializable} instance, which will return the list of serializable
         * properties.
         * @returns the serializable   properties of the object
         */
        list_properties(): GObject.ParamSpec[];

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @returns a node containing the serialized property
         */
        serialize_property(property_name: string, value: (GObject.Value | any), pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         */
        set_property(pspec: GObject.ParamSpec, value: (GObject.Value | any)): void;

        /**
         * @param args 
         */
        // Conflicted with GObject.Object.set_property
        set_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to deserialize the
         * property contained inside `property_node` and place its value
         * into `value`.
         * 
         * The `value` can be:
         * 
         * - an empty {@link GObject.Value} initialized by `G_VALUE_INIT`, which will be automatically
         *   initialized with the expected type of the property by using the given
         *   property description (since JSON-GLib 1.6)
         * - a {@link GObject.Value} initialized with the expected type of the property
         * 
         * This function will not be called for properties that are marked as
         * as `G_PARAM_CONSTRUCT_ONLY`.
         * @param property_name the name of the property to serialize
         * @param pspec a property description
         * @param property_node the JSON node containing the serialized property
         * @virtual
         */
        vfunc_deserialize_property(property_name: string, pspec: GObject.ParamSpec, property_node: Json.Node): [boolean, GObject.Value | any];

        /**
         * Calls the {@link Json.Serializable.find_property} implementation on
         * the {@link Json.Serializable} instance, which will return the property
         * description for the given name.
         * @param name the name of the property
         * @virtual
         */
        vfunc_find_property(name: string): (GObject.ParamSpec | null);

        /**
         * Calls the {@link Json.Serializable.get_property} implementation
         * on the {@link Json.Serializable} instance, which will get the value of
         * the given property.
         * @param pspec a property description
         * @virtual
         */
        vfunc_get_property(pspec: GObject.ParamSpec): GObject.Value | any;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_get_property
        vfunc_get_property(...args: never[]): any;

        /**
         * Asks a {@link Json.Serializable} implementation to serialize an object
         * property into a JSON node.
         * @param property_name the name of the property to serialize
         * @param value the value of the property to serialize
         * @param pspec a property description
         * @virtual
         */
        vfunc_serialize_property(property_name: string, value: unknown, pspec: GObject.ParamSpec): (Json.Node | null);

        /**
         * Calls the {@link Json.Serializable.set_property} implementation
         * on the {@link Json.Serializable} instance, which will set the property
         * with the given value.
         * @param pspec a property description
         * @param value the property value to set
         * @virtual
         */
        vfunc_set_property(pspec: GObject.ParamSpec, value: unknown): void;

        /**
         * @param args 
         * @virtual
         */
        // Conflicted with GObject.Object.vfunc_set_property
        vfunc_set_property(...args: never[]): any;
    }


    namespace VcsReference {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::is-symbolic": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
            is_symbolic: boolean;
            isSymbolic: boolean;
            title: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsReference extends GObject.Object {
        static $gtype: GObject.GType<VcsReference>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default false
         */
        get is_symbolic(): boolean;

        /**
         * @read-only
         * @default false
         */
        get isSymbolic(): boolean;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsReference.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsReference.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsReference.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsReference.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsReference.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsReference.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsReference.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsReference.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        /**
         * @virtual
         */
        vfunc_is_symbolic(): boolean;

        /**
         * @virtual
         */
        vfunc_load_commit(): Dex.Future;

        /**
         * @virtual
         */
        vfunc_resolve(): Dex.Future;

        // Methods
        dup_id(): string;

        dup_title(): string;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsCommit} or rejects with error.
         */
        load_commit(): Dex.Future;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsReference} or rejects with error.
         */
        resolve(): Dex.Future;
    }


    namespace VcsRemote {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            name: string;
            uri: string;
        }
    }

    /**
     * Abstract base class for version control system remote repository implementations.
     * 
     * FoundryVcsRemote provides the core interface for interacting with remote
     * repositories in version control systems. Concrete implementations handle
     * specific VCS protocols and provide unified access to remote repository
     * operations such as fetching, pushing, and cloning.
     * @gir-type Class
     */
    abstract class VcsRemote extends GObject.Object {
        static $gtype: GObject.GType<VcsRemote>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         * @default null
         */
        get uri(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsRemote.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsRemote.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsRemote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsRemote.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsRemote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsRemote.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsRemote.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsRemote.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_name(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_uri(): (string | null);

        // Methods
        dup_name(): (string | null);

        dup_uri(): (string | null);
    }


    namespace VcsSignature {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::email": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
            "notify::when": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            email: string;
            name: string;
            when: GLib.DateTime;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsSignature extends GObject.Object {
        static $gtype: GObject.GType<VcsSignature>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get email(): string;

        /**
         * @read-only
         * @default null
         */
        get name(): string;

        /**
         * @read-only
         */
        get when(): GLib.DateTime;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsSignature.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsSignature.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsSignature.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsSignature.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsSignature.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsSignature.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsSignature.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsSignature.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_email(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_name(): (string | null);

        /**
         * @virtual
         */
        vfunc_dup_when(): (GLib.DateTime | null);

        // Methods
        dup_email(): (string | null);

        dup_name(): (string | null);

        dup_when(): (GLib.DateTime | null);
    }


    namespace VcsStats {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::deletions": (pspec: GObject.ParamSpec) => void;
            "notify::files-changed": (pspec: GObject.ParamSpec) => void;
            "notify::insertions": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            deletions: (bigint | number);
            files_changed: (bigint | number);
            filesChanged: (bigint | number);
            insertions: (bigint | number);
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsStats extends GObject.Object {
        static $gtype: GObject.GType<VcsStats>;

        // Properties
        /**
         * @read-only
         * @default 0
         */
        get deletions(): number;

        /**
         * @read-only
         * @default 0
         */
        get files_changed(): number;

        /**
         * @read-only
         * @default 0
         */
        get filesChanged(): number;

        /**
         * @read-only
         * @default 0
         */
        get insertions(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsStats.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsStats.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsStats.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsStats.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsStats.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsStats.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsStats.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsStats.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_get_deletions(): (bigint | number);

        /**
         * @virtual
         */
        vfunc_get_files_changed(): (bigint | number);

        /**
         * @virtual
         */
        vfunc_get_insertions(): (bigint | number);

        // Methods
        get_deletions(): number;

        get_files_changed(): number;

        get_insertions(): number;
    }


    namespace VcsTag {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
            "notify::is-local": (pspec: GObject.ParamSpec) => void;
            "notify::title": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
            is_local: boolean;
            isLocal: boolean;
            title: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsTag extends GObject.Object {
        static $gtype: GObject.GType<VcsTag>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * @read-only
         * @default true
         */
        get is_local(): boolean;

        /**
         * @read-only
         * @default true
         */
        get isLocal(): boolean;

        /**
         * @read-only
         * @default null
         */
        get title(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsTag.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsTag.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsTag.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsTag.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsTag.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsTag.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsTag.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsTag.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        /**
         * @virtual
         */
        vfunc_dup_title(): string;

        /**
         * @virtual
         */
        vfunc_is_local(): boolean;

        /**
         * @virtual
         */
        vfunc_load_target(): Dex.Future;

        // Methods
        dup_id(): string;

        dup_title(): string;

        /**
         * @returns a {@link Dex.Future} that resolves to a   {@link Foundry.VcsReference}.
         */
        load_target(): Dex.Future;
    }


    namespace VcsTree {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            id: string;
        }
    }

    /**
     * @gir-type Class
     */
    abstract class VcsTree extends GObject.Object {
        static $gtype: GObject.GType<VcsTree>;

        // Properties
        /**
         * @read-only
         * @default null
         */
        get id(): string;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: VcsTree.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<VcsTree.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof VcsTree.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsTree.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof VcsTree.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, VcsTree.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof VcsTree.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<VcsTree.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_dup_id(): string;

        // Methods
        dup_id(): string;
    }


    namespace WebIntent {
        // Signal signatures
        interface SignalSignatures extends Intent.SignalSignatures {
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Intent.ConstructorProps {
            uri: string;
        }
    }

    /**
     * Represents an intent to open a URI with a web browser.
     * 
     * FoundryWebIntent provides functionality for opening URIs in web browsers.
     * The URI is stored as a plain UTF-8 string and is up to the caller to
     * ensure that the URI is valid.
     * @gir-type Class
     */
    class WebIntent extends Intent {
        static $gtype: GObject.GType<WebIntent>;

        // Properties
        /**
         * @default null
         */
        get uri(): string;
        set uri(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: WebIntent.SignalSignatures;

        // Constructors
        constructor(properties?: Partial<WebIntent.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](uri: string): WebIntent;

        // Signals
        /** @signal */
        connect<K extends keyof WebIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, WebIntent.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof WebIntent.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, WebIntent.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof WebIntent.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<WebIntent.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Gets a copy of the URI string.
         * @returns a newly allocated copy of the URI string,   or `null` if no URI is set
         */
        dup_uri(): (string | null);
    }


    /**
     * @gir-type Struct
     */
    class Action {
        static $gtype: GObject.GType<Action>;

        // Fields
        next: null;

        name: string;

        owner: GObject.GType;

        pspec: GObject.ParamSpec;

        activate: ActionActivateFunc;

        position: number;
    }


    /**
     * @gir-type Alias
     */
    type ActionIntentClass = typeof ActionIntent;

    /**
     * @gir-type Struct
     */
    class ActionMixin {
        static $gtype: GObject.GType<ActionMixin>;

        // Fields
        n_actions: number;

        // Static methods
        /**
         * @param instance 
         * @param action 
         */
        static get_enabled(instance: null, action: string): boolean;

        /**
         * @param instance 
         * @param action 
         * @param enabled 
         */
        static set_enabled(instance: null, action: string, enabled: boolean): void;

        // Methods
        /**
         * @param instance 
         */
        constructed(instance: null): void;

        /**
         * @param object_class 
         */
        init(object_class: typeof GObject.Object): void;
    }


    /**
     * @gir-type Alias
     */
    type ActionMuxerClass = typeof ActionMuxer;

    /**
     * @gir-type Alias
     */
    type AuthProviderClass = typeof AuthProvider;

    /**
     * @gir-type Alias
     */
    type BuildAddinClass = typeof BuildAddin;

    /**
     * @gir-type Alias
     */
    type BuildFlagsClass = typeof BuildFlags;

    /**
     * @gir-type Alias
     */
    type BuildManagerClass = typeof BuildManager;

    /**
     * @gir-type Alias
     */
    type BuildPipelineClass = typeof BuildPipeline;

    /**
     * @gir-type Alias
     */
    type BuildProgressClass = typeof BuildProgress;

    /**
     * @gir-type Alias
     */
    type BuildStageClass = typeof BuildStage;

    /**
     * @gir-type Alias
     */
    type BuildTargetClass = typeof BuildTarget;

    /**
     * @gir-type Struct
     */
    class CliCommand {
        static $gtype: GObject.GType<CliCommand>;

        // Fields
        gettext_package: string;

        description: string;
    }


    /**
     * @gir-type Alias
     */
    type CliCommandTreeAddinClass = typeof CliCommandTreeAddin;

    /**
     * @gir-type Alias
     */
    type CliCommandTreeClass = typeof CliCommandTree;

    /**
     * @gir-type Struct
     */
    class CliOptions {
        static $gtype: GObject.GType<CliOptions>;

        // Constructors

        constructor(properties?: Partial<{

        }>);

        static ["new"](): CliOptions;

        // Methods
        /**
         * @param key 
         * @param value 
         */
        get_boolean(key: string, value: boolean): boolean;

        /**
         * @param key 
         * @param value 
         */
        get_double(key: string, value: number): boolean;

        /**
         * @param key 
         */
        get_filename(key: string): string;

        /**
         * @param key 
         */
        get_filename_array(key: string): string[];

        /**
         * @param key 
         * @param value 
         */
        get_int(key: string, value: number): boolean;

        /**
         * @param key 
         * @param value 
         */
        get_int64(key: string, value: (bigint | number)): boolean;

        /**
         * @param key 
         */
        get_string(key: string): string;

        /**
         * @param key 
         */
        get_string_array(key: string): string[];

        help(): boolean;

        /**
         * Loads the {@link Foundry.Context} based on option values.
         * @param command_line 
         * @returns a {@link Dex.Future} that resolves to a {@link Foundry.Context}
         */
        load_context(command_line: CommandLine): Dex.Future;

        /**
         * @returns `self`
         */
        ref(): CliOptions;

        /**
         * @param key 
         * @param value 
         */
        set_boolean(key: string, value: boolean): void;

        /**
         * @param key 
         * @param value 
         */
        set_double(key: string, value: number): void;

        /**
         * @param key 
         * @param value 
         */
        set_filename(key: string, value: string): void;

        /**
         * @param key 
         * @param value 
         */
        set_filename_array(key: string, value: string): void;

        /**
         * @param key 
         * @param value 
         */
        set_int(key: string, value: number): void;

        /**
         * @param key 
         * @param value 
         */
        set_int64(key: string, value: (bigint | number)): void;

        /**
         * @param key 
         * @param value 
         */
        set_string(key: string, value: string): void;

        /**
         * @param key 
         * @param value 
         */
        set_string_array(key: string, value: string): void;

        unref(): void;
    }


    /**
     * @gir-type Struct
     */
    abstract class CliTool {
        static $gtype: GObject.GType<CliTool>;
    }


    /**
     * @gir-type Alias
     */
    type CodeActionClass = typeof CodeAction;

    /**
     * @gir-type Alias
     */
    type CodeTemplateClass = typeof CodeTemplate;

    /**
     * @gir-type Alias
     */
    type CommandClass = typeof Command;

    /**
     * @gir-type Alias
     */
    type CommandLineClass = typeof CommandLine;

    /**
     * @gir-type Alias
     */
    type CommandManagerClass = typeof CommandManager;

    /**
     * @gir-type Alias
     */
    type CommandProviderClass = typeof CommandProvider;

    /**
     * @gir-type Alias
     */
    type CommandStageClass = typeof CommandStage;

    /**
     * @gir-type Alias
     */
    type CompileCommandsClass = typeof CompileCommands;

    /**
     * @gir-type Alias
     */
    type CompletionProposalClass = typeof CompletionProposal;

    /**
     * @gir-type Alias
     */
    type CompletionProviderClass = typeof CompletionProvider;

    /**
     * @gir-type Alias
     */
    type CompletionRequestClass = typeof CompletionRequest;

    /**
     * @gir-type Alias
     */
    type ConfigClass = typeof Config;

    /**
     * @gir-type Alias
     */
    type ConfigManagerClass = typeof ConfigManager;

    /**
     * @gir-type Alias
     */
    type ConfigProviderClass = typeof ConfigProvider;

    /**
     * @gir-type Alias
     */
    type ContextClass = typeof Context;

    /**
     * @gir-type Alias
     */
    type ContextualClass = typeof Contextual;

    /**
     * @gir-type Alias
     */
    type DBusServiceClass = typeof DBusService;

    /**
     * @gir-type Alias
     */
    type DapDebuggerClass = typeof DapDebugger;

    /**
     * @gir-type Alias
     */
    type DebuggerActionsClass = typeof DebuggerActions;

    /**
     * @gir-type Alias
     */
    type DebuggerBreakpointClass = typeof DebuggerBreakpoint;

    /**
     * @gir-type Alias
     */
    type DebuggerClass = typeof Debugger;

    /**
     * @gir-type Alias
     */
    type DebuggerCountpointClass = typeof DebuggerCountpoint;

    /**
     * @gir-type Alias
     */
    type DebuggerEventClass = typeof DebuggerEvent;

    /**
     * @gir-type Alias
     */
    type DebuggerInstructionClass = typeof DebuggerInstruction;

    /**
     * @gir-type Alias
     */
    type DebuggerLogMessageClass = typeof DebuggerLogMessage;

    /**
     * @gir-type Alias
     */
    type DebuggerManagerClass = typeof DebuggerManager;

    /**
     * @gir-type Alias
     */
    type DebuggerMappedRegionClass = typeof DebuggerMappedRegion;

    /**
     * @gir-type Alias
     */
    type DebuggerModuleClass = typeof DebuggerModule;

    /**
     * @gir-type Alias
     */
    type DebuggerProviderClass = typeof DebuggerProvider;

    /**
     * @gir-type Alias
     */
    type DebuggerSourceClass = typeof DebuggerSource;

    /**
     * @gir-type Alias
     */
    type DebuggerStackFrameClass = typeof DebuggerStackFrame;

    /**
     * @gir-type Alias
     */
    type DebuggerStopEventClass = typeof DebuggerStopEvent;

    /**
     * @gir-type Alias
     */
    type DebuggerTargetClass = typeof DebuggerTarget;

    /**
     * @gir-type Alias
     */
    type DebuggerTargetCommandClass = typeof DebuggerTargetCommand;

    /**
     * @gir-type Alias
     */
    type DebuggerTargetProcessClass = typeof DebuggerTargetProcess;

    /**
     * @gir-type Alias
     */
    type DebuggerTargetRemoteClass = typeof DebuggerTargetRemote;

    /**
     * @gir-type Alias
     */
    type DebuggerThreadClass = typeof DebuggerThread;

    /**
     * @gir-type Alias
     */
    type DebuggerThreadGroupClass = typeof DebuggerThreadGroup;

    /**
     * @gir-type Alias
     */
    type DebuggerTrapClass = typeof DebuggerTrap;

    /**
     * @gir-type Alias
     */
    type DebuggerTrapParamsClass = typeof DebuggerTrapParams;

    /**
     * @gir-type Alias
     */
    type DebuggerVariableClass = typeof DebuggerVariable;

    /**
     * @gir-type Alias
     */
    type DebuggerWatchpointClass = typeof DebuggerWatchpoint;

    /**
     * @gir-type Alias
     */
    type DependencyClass = typeof Dependency;

    /**
     * @gir-type Alias
     */
    type DependencyManagerClass = typeof DependencyManager;

    /**
     * @gir-type Alias
     */
    type DependencyProviderClass = typeof DependencyProvider;

    /**
     * @gir-type Alias
     */
    type DeployStrategyClass = typeof DeployStrategy;

    /**
     * @gir-type Alias
     */
    type DeviceClass = typeof Device;

    /**
     * @gir-type Alias
     */
    type DeviceInfoClass = typeof DeviceInfo;

    /**
     * @gir-type Alias
     */
    type DeviceManagerClass = typeof DeviceManager;

    /**
     * @gir-type Alias
     */
    type DeviceProviderClass = typeof DeviceProvider;

    /**
     * @gir-type Struct
     */
    class DiagnosticBuilder {
        static $gtype: GObject.GType<DiagnosticBuilder>;

        // Constructors
        constructor(context: Context);

        static ["new"](context: Context): DiagnosticBuilder;

        // Methods
        /**
         * Adds a potential fix to the diagnostic.
         * @param description 
         * @param text_edits {@link Gio.ListModel} of {@link Foundry.TextEdit}
         */
        add_fix(description: (string | null), text_edits: Gio.ListModel): void;

        /**
         * @param begin_line 
         * @param begin_line_offset 
         * @param end_line 
         * @param end_line_offset 
         */
        add_range(begin_line: number, begin_line_offset: number, end_line: number, end_line_offset: number): void;

        /**
         * @returns a {@link Foundry.Diagnostic} or `null`
         */
        end(): (Diagnostic | null);

        ref(): DiagnosticBuilder;

        /**
         * @param file 
         */
        set_file(file: Gio.File): void;

        /**
         * @param line 
         */
        set_line(line: number): void;

        /**
         * @param line_offset 
         */
        set_line_offset(line_offset: number): void;

        /**
         * @param markup 
         */
        set_markup(markup: Markup): void;

        /**
         * @param text 
         */
        set_message(text: string): void;

        /**
         * @param path 
         */
        set_path(path: string): void;

        /**
         * @param rule_id 
         */
        set_rule_id(rule_id: string): void;

        /**
         * @param severity 
         */
        set_severity(severity: DiagnosticSeverity): void;

        /**
         * @param markup 
         */
        take_markup(markup: Markup): void;

        /**
         * @param text 
         */
        take_message(text: string): void;

        unref(): void;
    }


    /**
     * @gir-type Alias
     */
    type DiagnosticClass = typeof Diagnostic;

    /**
     * @gir-type Alias
     */
    type DiagnosticFixClass = typeof DiagnosticFix;

    /**
     * @gir-type Alias
     */
    type DiagnosticManagerClass = typeof DiagnosticManager;

    /**
     * @gir-type Alias
     */
    type DiagnosticProviderClass = typeof DiagnosticProvider;

    /**
     * @gir-type Alias
     */
    type DiagnosticRangeClass = typeof DiagnosticRange;

    /**
     * @gir-type Alias
     */
    type DiagnosticToolClass = typeof DiagnosticTool;

    /**
     * @gir-type Alias
     */
    type DirectoryItemClass = typeof DirectoryItem;

    /**
     * @gir-type Alias
     */
    type DirectoryListingClass = typeof DirectoryListing;

    /**
     * @gir-type Alias
     */
    type DirectoryReaperClass = typeof DirectoryReaper;

    /**
     * @gir-type Alias
     */
    type DoapFileClass = typeof DoapFile;

    /**
     * @gir-type Alias
     */
    type DoapPersonClass = typeof DoapPerson;

    /**
     * @gir-type Alias
     */
    type DoapServiceClass = typeof DoapService;

    /**
     * @gir-type Alias
     */
    type DocumentationBundleClass = typeof DocumentationBundle;

    /**
     * @gir-type Alias
     */
    type DocumentationClass = typeof Documentation;

    /**
     * @gir-type Alias
     */
    type DocumentationIntentClass = typeof DocumentationIntent;

    /**
     * @gir-type Alias
     */
    type DocumentationManagerClass = typeof DocumentationManager;

    /**
     * @gir-type Alias
     */
    type DocumentationMatchesClass = typeof DocumentationMatches;

    /**
     * @gir-type Alias
     */
    type DocumentationProviderClass = typeof DocumentationProvider;

    /**
     * @gir-type Alias
     */
    type DocumentationQueryClass = typeof DocumentationQuery;

    /**
     * @gir-type Alias
     */
    type DocumentationRootClass = typeof DocumentationRoot;

    /**
     * @gir-type Alias
     */
    type ExtensionClass = typeof Extension;

    /**
     * @gir-type Alias
     */
    type ExtensionSetClass = typeof ExtensionSet;

    /**
     * @gir-type Alias
     */
    type FileManagerClass = typeof FileManager;

    /**
     * @gir-type Alias
     */
    type FileMonitorClass = typeof FileMonitor;

    /**
     * @gir-type Alias
     */
    type FileMonitorEventClass = typeof FileMonitorEvent;

    /**
     * @gir-type Struct
     */
    abstract class FileOpenIntent {
        static $gtype: GObject.GType<FileOpenIntent>;
    }


    /**
     * @gir-type Alias
     */
    type FileSearchMatchClass = typeof FileSearchMatch;

    /**
     * @gir-type Alias
     */
    type FileSearchOptionsClass = typeof FileSearchOptions;

    /**
     * @gir-type Alias
     */
    type FileSearchProviderClass = typeof FileSearchProvider;

    /**
     * @gir-type Alias
     */
    type FileSearchReplacementClass = typeof FileSearchReplacement;

    /**
     * @gir-type Alias
     */
    type FlatpakArchOptionsClass = typeof FlatpakArchOptions;

    /**
     * @gir-type Alias
     */
    type FlatpakExtensionClass = typeof FlatpakExtension;

    /**
     * @gir-type Alias
     */
    type FlatpakExtensionsClass = typeof FlatpakExtensions;

    /**
     * @gir-type Alias
     */
    type FlatpakListClass = typeof FlatpakList;

    /**
     * @gir-type Alias
     */
    type FlatpakManifestClass = typeof FlatpakManifest;

    /**
     * @gir-type Alias
     */
    type FlatpakManifestLoaderClass = typeof FlatpakManifestLoader;

    /**
     * @gir-type Alias
     */
    type FlatpakModuleClass = typeof FlatpakModule;

    /**
     * @gir-type Alias
     */
    type FlatpakModulesClass = typeof FlatpakModules;

    /**
     * @gir-type Alias
     */
    type FlatpakOptionsClass = typeof FlatpakOptions;

    /**
     * @gir-type Alias
     */
    type FlatpakSerializableClass = typeof FlatpakSerializable;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceArchiveClass = typeof FlatpakSourceArchive;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceBzrClass = typeof FlatpakSourceBzr;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceClass = typeof FlatpakSource;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceDirClass = typeof FlatpakSourceDir;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceExtraDataClass = typeof FlatpakSourceExtraData;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceFileClass = typeof FlatpakSourceFile;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceGitClass = typeof FlatpakSourceGit;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceInlineClass = typeof FlatpakSourceInline;

    /**
     * @gir-type Alias
     */
    type FlatpakSourcePatchClass = typeof FlatpakSourcePatch;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceScriptClass = typeof FlatpakSourceScript;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceShellClass = typeof FlatpakSourceShell;

    /**
     * @gir-type Alias
     */
    type FlatpakSourceSvnClass = typeof FlatpakSourceSvn;

    /**
     * @gir-type Alias
     */
    type FlatpakSourcesClass = typeof FlatpakSources;

    /**
     * @gir-type Alias
     */
    type ForgeClass = typeof Forge;

    /**
     * @gir-type Alias
     */
    type ForgeIssueClass = typeof ForgeIssue;

    /**
     * @gir-type Alias
     */
    type ForgeListingClass = typeof ForgeListing;

    /**
     * @gir-type Alias
     */
    type ForgeManagerClass = typeof ForgeManager;

    /**
     * @gir-type Alias
     */
    type ForgeMergeRequestClass = typeof ForgeMergeRequest;

    /**
     * @gir-type Alias
     */
    type ForgeProjectClass = typeof ForgeProject;

    /**
     * @gir-type Alias
     */
    type ForgeQueryClass = typeof ForgeQuery;

    /**
     * @gir-type Alias
     */
    type ForgeUserClass = typeof ForgeUser;

    /**
     * @gir-type Alias
     */
    type GirClass = typeof Gir;

    /**
     * @gir-type Alias
     */
    type GirNodeClass = typeof GirNode;

    /**
     * @gir-type Alias
     */
    type GitBlameClass = typeof GitBlame;

    /**
     * @gir-type Alias
     */
    type GitBranchClass = typeof GitBranch;

    /**
     * @gir-type Alias
     */
    type GitClonerClass = typeof GitCloner;

    /**
     * @gir-type Alias
     */
    type GitCommitBuilderClass = typeof GitCommitBuilder;

    /**
     * @gir-type Alias
     */
    type GitCommitClass = typeof GitCommit;

    /**
     * @gir-type Alias
     */
    type GitDeltaClass = typeof GitDelta;

    /**
     * @gir-type Alias
     */
    type GitDiffClass = typeof GitDiff;

    /**
     * @gir-type Alias
     */
    type GitDiffHunkClass = typeof GitDiffHunk;

    /**
     * @gir-type Alias
     */
    type GitDiffLineClass = typeof GitDiffLine;

    /**
     * @gir-type Alias
     */
    type GitFileClass = typeof GitFile;

    /**
     * @gir-type Alias
     */
    type GitReferenceClass = typeof GitReference;

    /**
     * @gir-type Alias
     */
    type GitRemoteClass = typeof GitRemote;

    /**
     * @gir-type Alias
     */
    type GitSignatureClass = typeof GitSignature;

    /**
     * @gir-type Alias
     */
    type GitStatsClass = typeof GitStats;

    /**
     * @gir-type Alias
     */
    type GitStatusEntryClass = typeof GitStatusEntry;

    /**
     * @gir-type Alias
     */
    type GitStatusListClass = typeof GitStatusList;

    /**
     * @gir-type Alias
     */
    type GitTagClass = typeof GitTag;

    /**
     * @gir-type Alias
     */
    type GitTreeClass = typeof GitTree;

    /**
     * @gir-type Struct
     */
    class GitUri {
        static $gtype: GObject.GType<GitUri>;

        // Constructors
        constructor(uri: string);

        static ["new"](uri: string): GitUri;

        // Static methods
        /**
         * @param uri_string 
         */
        static is_valid(uri_string: string): boolean;

        // Methods
        /**
         * Determines a suggested name for the checkout directory. Some special
         * handling of suffixes such as ".git" are performed to improve the the
         * quality of results.
         * @returns a string containing the suggested   clone directory name, or `null`.
         */
        get_clone_name(): (string | null);

        get_host(): string;

        get_path(): string;

        get_port(): number;

        get_scheme(): string;

        get_user(): string;

        ref(): GitUri;

        /**
         * @param host 
         */
        set_host(host: string): void;

        /**
         * @param path 
         */
        set_path(path: string): void;

        /**
         * @param port 
         */
        set_port(port: number): void;

        /**
         * @param scheme 
         */
        set_scheme(scheme: string): void;

        /**
         * @param user 
         */
        set_user(user: string): void;

        to_string(): string;

        unref(): void;
    }


    /**
     * @gir-type Alias
     */
    type GitVcsClass = typeof GitVcs;

    /**
     * @gir-type Alias
     */
    type HoverProviderClass = typeof HoverProvider;

    /**
     * @gir-type Alias
     */
    type InhibitorClass = typeof Inhibitor;

    /**
     * @gir-type Alias
     */
    type InputChoiceClass = typeof InputChoice;

    /**
     * @gir-type Alias
     */
    type InputClass = typeof Input;

    /**
     * @gir-type Alias
     */
    type InputComboClass = typeof InputCombo;

    /**
     * @gir-type Alias
     */
    type InputFileClass = typeof InputFile;

    /**
     * @gir-type Alias
     */
    type InputFontClass = typeof InputFont;

    /**
     * @gir-type Alias
     */
    type InputGroupClass = typeof InputGroup;

    /**
     * @gir-type Alias
     */
    type InputPasswordClass = typeof InputPassword;

    /**
     * @gir-type Alias
     */
    type InputSpinClass = typeof InputSpin;

    /**
     * @gir-type Alias
     */
    type InputSwitchClass = typeof InputSwitch;

    /**
     * @gir-type Alias
     */
    type InputTextClass = typeof InputText;

    /**
     * @gir-type Alias
     */
    type InputValidatorClass = typeof InputValidator;

    /**
     * @gir-type Alias
     */
    type InputValidatorDelegateClass = typeof InputValidatorDelegate;

    /**
     * @gir-type Alias
     */
    type InputValidatorRegexClass = typeof InputValidatorRegex;

    /**
     * @gir-type Alias
     */
    type IntentClass = typeof Intent;

    /**
     * @gir-type Alias
     */
    type IntentHandlerClass = typeof IntentHandler;

    /**
     * @gir-type Alias
     */
    type IntentManagerClass = typeof IntentManager;

    /**
     * @gir-type Alias
     */
    type JsonListLlmResourceClass = typeof JsonListLlmResource;

    /**
     * @gir-type Alias
     */
    type JsonLlmResourceClass = typeof JsonLlmResource;

    /**
     * @gir-type Alias
     */
    type KeyRotatorClass = typeof KeyRotator;

    /**
     * @gir-type Alias
     */
    type LanguageClass = typeof Language;

    /**
     * @gir-type Alias
     */
    type LanguageGuesserClass = typeof LanguageGuesser;

    /**
     * @gir-type Alias
     */
    type LicenseClass = typeof License;

    /**
     * @gir-type Alias
     */
    type LinkedPipelineStageClass = typeof LinkedPipelineStage;

    /**
     * @gir-type Alias
     */
    type LlmCompletionChunkClass = typeof LlmCompletionChunk;

    /**
     * @gir-type Alias
     */
    type LlmCompletionClass = typeof LlmCompletion;

    /**
     * @gir-type Alias
     */
    type LlmConversationClass = typeof LlmConversation;

    /**
     * @gir-type Alias
     */
    type LlmManagerClass = typeof LlmManager;

    /**
     * @gir-type Alias
     */
    type LlmMessageClass = typeof LlmMessage;

    /**
     * @gir-type Alias
     */
    type LlmModelClass = typeof LlmModel;

    /**
     * @gir-type Alias
     */
    type LlmProviderClass = typeof LlmProvider;

    /**
     * @gir-type Alias
     */
    type LlmResourceClass = typeof LlmResource;

    /**
     * @gir-type Alias
     */
    type LlmToolCallClass = typeof LlmToolCall;

    /**
     * @gir-type Alias
     */
    type LlmToolClass = typeof LlmTool;

    /**
     * @gir-type Alias
     */
    type LocalDeviceClass = typeof LocalDevice;

    /**
     * @gir-type Alias
     */
    type LogManagerClass = typeof LogManager;

    /**
     * @gir-type Alias
     */
    type LogMessageClass = typeof LogMessage;

    /**
     * @gir-type Alias
     */
    type LspClientClass = typeof LspClient;

    /**
     * @gir-type Alias
     */
    type LspCompletionProposalClass = typeof LspCompletionProposal;

    /**
     * @gir-type Alias
     */
    type LspCompletionProviderClass = typeof LspCompletionProvider;

    /**
     * @gir-type Alias
     */
    type LspManagerClass = typeof LspManager;

    /**
     * @gir-type Alias
     */
    type LspProviderClass = typeof LspProvider;

    /**
     * @gir-type Alias
     */
    type LspServerClass = typeof LspServer;

    /**
     * @gir-type Alias
     */
    type MarkupClass = typeof Markup;

    /**
     * @gir-type Alias
     */
    type McpServerClass = typeof McpServer;

    /**
     * @gir-type Alias
     */
    type MenuSearchProviderClass = typeof MenuSearchProvider;

    /**
     * @gir-type Alias
     */
    type MenuSearchResultClass = typeof MenuSearchResult;

    /**
     * @gir-type Alias
     */
    type ModelManagerClass = typeof ModelManager;

    /**
     * @gir-type Alias
     */
    type NoVcsClass = typeof NoVcs;

    /**
     * Used to determine what properties to serialize in command line data.
     * @gir-type Struct
     */
    class ObjectSerializerEntry {
        static $gtype: GObject.GType<ObjectSerializerEntry>;

        // Fields
        property: string;

        heading: string;

        // Constructors

        constructor(properties?: Partial<{
            property: string;
            heading: string;
        }>);
    }


    /**
     * @gir-type Alias
     */
    type OnTypeDiagnosticsClass = typeof OnTypeDiagnostics;

    /**
     * @gir-type Alias
     */
    type OnTypeFormatterClass = typeof OnTypeFormatter;

    /**
     * @gir-type Alias
     */
    type OpenFileIntentClass = typeof OpenFileIntent;

    /**
     * @gir-type Alias
     */
    type OperationClass = typeof Operation;

    /**
     * @gir-type Alias
     */
    type OperationManagerClass = typeof OperationManager;

    /**
     * @gir-type Alias
     */
    type PathCacheClass = typeof PathCache;

    /**
     * @gir-type Struct
     */
    abstract class Pipeline {
        static $gtype: GObject.GType<Pipeline>;
    }


    /**
     * @gir-type Alias
     */
    type PluginBuildAddinClass = typeof PluginBuildAddin;

    /**
     * @gir-type Alias
     */
    type PluginLspProviderClass = typeof PluginLspProvider;

    /**
     * @gir-type Alias
     */
    type PluginManagerClass = typeof PluginManager;

    /**
     * @gir-type Alias
     */
    type ProcessLauncherClass = typeof ProcessLauncher;

    /**
     * @gir-type Alias
     */
    type ProjectTemplateClass = typeof ProjectTemplate;

    /**
     * @gir-type Alias
     */
    type PtyDiagnosticsClass = typeof PtyDiagnostics;

    /**
     * @gir-type Alias
     */
    type RenameProviderClass = typeof RenameProvider;

    /**
     * @gir-type Alias
     */
    type RunManagerClass = typeof RunManager;

    /**
     * @gir-type Alias
     */
    type RunToolClass = typeof RunTool;

    /**
     * @gir-type Alias
     */
    type SdkClass = typeof Sdk;

    /**
     * @gir-type Alias
     */
    type SdkManagerClass = typeof SdkManager;

    /**
     * @gir-type Alias
     */
    type SdkProviderClass = typeof SdkProvider;

    /**
     * @gir-type Alias
     */
    type SearchManagerClass = typeof SearchManager;

    /**
     * @gir-type Alias
     */
    type SearchProviderClass = typeof SearchProvider;

    /**
     * @gir-type Alias
     */
    type SearchRequestClass = typeof SearchRequest;

    /**
     * @gir-type Alias
     */
    type SearchResultClass = typeof SearchResult;

    /**
     * @gir-type Alias
     */
    type SecretServiceClass = typeof SecretService;

    /**
     * @gir-type Alias
     */
    type ServiceClass = typeof Service;

    /**
     * @gir-type Alias
     */
    type SettingsClass = typeof Settings;

    /**
     * @gir-type Alias
     */
    type SimpleLlmMessageClass = typeof SimpleLlmMessage;

    /**
     * @gir-type Alias
     */
    type SimpleTextBufferClass = typeof SimpleTextBuffer;

    /**
     * @gir-type Alias
     */
    type SymbolClass = typeof Symbol;

    /**
     * @gir-type Alias
     */
    type SymbolIntentClass = typeof SymbolIntent;

    /**
     * @gir-type Alias
     */
    type SymbolLocatorClass = typeof SymbolLocator;

    /**
     * @gir-type Alias
     */
    type SymbolProviderClass = typeof SymbolProvider;

    /**
     * @gir-type Alias
     */
    type TemplateClass = typeof Template;

    /**
     * @gir-type Alias
     */
    type TemplateManagerClass = typeof TemplateManager;

    /**
     * @gir-type Alias
     */
    type TemplateOutputClass = typeof TemplateOutput;

    /**
     * @gir-type Alias
     */
    type TemplateProviderClass = typeof TemplateProvider;

    /**
     * @gir-type Alias
     */
    type TerminalIntentClass = typeof TerminalIntent;

    /**
     * @gir-type Alias
     */
    type TerminalLauncherClass = typeof TerminalLauncher;

    /**
     * @gir-type Alias
     */
    type TerminalServiceClass = typeof TerminalService;

    /**
     * @gir-type Alias
     */
    type TestClass = typeof Test;

    /**
     * @gir-type Alias
     */
    type TestManagerClass = typeof TestManager;

    /**
     * @gir-type Alias
     */
    type TestProviderClass = typeof TestProvider;

    /**
     * @gir-type Alias
     */
    type TestSuiteClass = typeof TestSuite;

    /**
     * @gir-type Alias
     */
    type TextBufferInterface = typeof TextBuffer;

    /**
     * @gir-type Alias
     */
    type TextBufferProviderClass = typeof TextBufferProvider;

    /**
     * @gir-type Alias
     */
    type TextDocumentAddinClass = typeof TextDocumentAddin;

    /**
     * @gir-type Alias
     */
    type TextDocumentClass = typeof TextDocument;

    /**
     * @gir-type Alias
     */
    type TextEditClass = typeof TextEdit;

    /**
     * @gir-type Alias
     */
    type TextFormatterClass = typeof TextFormatter;

    /**
     * @gir-type Struct
     */
    class TextIter {
        static $gtype: GObject.GType<TextIter>;

        // Methods
        backward_char(): boolean;

        ends_line(): boolean;

        forward_char(): boolean;

        forward_line(): boolean;

        get_line(): number;

        get_line_offset(): number;

        get_offset(): number;

        /**
         * @param end 
         */
        get_slice(end: TextIter): string;

        /**
         * @param buffer 
         * @param vtable 
         */
        init(buffer: TextBuffer, vtable: TextIterVTable): void;

        is_end(): boolean;

        is_start(): boolean;

        /**
         * @param line 
         * @param line_offset 
         */
        move_to_line_and_offset(line: (bigint | number), line_offset: (bigint | number)): boolean;

        /**
         * @param offset 
         */
        set_offset(offset: (bigint | number)): void;

        starts_line(): boolean;
    }


    /**
     * @gir-type Struct
     */
    class TextIterVTable {
        static $gtype: GObject.GType<TextIterVTable>;
    }


    /**
     * @gir-type Alias
     */
    type TextManagerClass = typeof TextManager;

    /**
     * @gir-type Alias
     */
    type TextSettingsClass = typeof TextSettings;

    /**
     * @gir-type Alias
     */
    type TextSettingsProviderClass = typeof TextSettingsProvider;

    /**
     * @gir-type Struct
     */
    class Triplet {
        static $gtype: GObject.GType<Triplet>;

        // Constructors

        constructor(properties?: Partial<{

        }>);

        static ["new"](full_name: string): Triplet;

        static new_from_system(): Triplet;

        static new_with_quadruplet(arch: string, vendor: (string | null), kernel: (string | null), operating_system: (string | null)): Triplet;

        static new_with_triplet(arch: string, kernel: (string | null), operating_system: (string | null)): Triplet;

        // Methods
        /**
         * Gets the architecture name of the machine
         * @returns The architecture name of the machine
         */
        get_arch(): string;

        /**
         * Gets the full name of the machine configuration name (can be an architecture name,
         * a duet, a triplet or a quadruplet).
         * @returns The full name of the machine configuration name
         */
        get_full_name(): string;

        /**
         * Gets name of the kernel of the machine
         * @returns The name of the kernel of the machine
         */
        get_kernel(): (string | null);

        /**
         * Gets name of the operating system of the machine
         * @returns The name of the operating system of the machine
         */
        get_operating_system(): (string | null);

        /**
         * Gets the vendor name of the machine
         * @returns The vendor name of the machine
         */
        get_vendor(): (string | null);

        /**
         * Gets whether this is the same architecture as the system
         * @returns `true` if this is the same architecture as the system, `false` otherwise
         */
        is_system(): boolean;

        /**
         * Increases the reference count of `self`
         * @returns An {@link Foundry.Triplet}.
         */
        ref(): Triplet;

        /**
         * Decreases the reference count of `self`
         * Once the reference count reaches 0, the object is freed.
         */
        unref(): void;
    }


    /**
     * @gir-type Alias
     */
    type TtyAuthProviderClass = typeof TtyAuthProvider;

    /**
     * @gir-type Alias
     */
    type TweakClass = typeof Tweak;

    /**
     * @gir-type Struct
     */
    abstract class TweakInfo {
        static $gtype: GObject.GType<TweakInfo>;
    }


    /**
     * @gir-type Alias
     */
    type TweakManagerClass = typeof TweakManager;

    /**
     * @gir-type Struct
     */
    abstract class TweakPath {
        static $gtype: GObject.GType<TweakPath>;
    }


    /**
     * @gir-type Alias
     */
    type TweakProviderClass = typeof TweakProvider;

    /**
     * @gir-type Alias
     */
    type UnixFDMapClass = typeof UnixFDMap;

    /**
     * @gir-type Alias
     */
    type VcsBlameClass = typeof VcsBlame;

    /**
     * @gir-type Alias
     */
    type VcsBranchClass = typeof VcsBranch;

    /**
     * @gir-type Alias
     */
    type VcsClass = typeof Vcs;

    /**
     * @gir-type Alias
     */
    type VcsCommitClass = typeof VcsCommit;

    /**
     * @gir-type Alias
     */
    type VcsDeltaClass = typeof VcsDelta;

    /**
     * @gir-type Alias
     */
    type VcsDiffClass = typeof VcsDiff;

    /**
     * @gir-type Alias
     */
    type VcsDiffHunkClass = typeof VcsDiffHunk;

    /**
     * @gir-type Alias
     */
    type VcsDiffLineClass = typeof VcsDiffLine;

    /**
     * @gir-type Alias
     */
    type VcsFileClass = typeof VcsFile;

    /**
     * @gir-type Alias
     */
    type VcsLineChangesClass = typeof VcsLineChanges;

    /**
     * @gir-type Alias
     */
    type VcsManagerClass = typeof VcsManager;

    /**
     * @gir-type Alias
     */
    type VcsProviderClass = typeof VcsProvider;

    /**
     * @gir-type Alias
     */
    type VcsReferenceClass = typeof VcsReference;

    /**
     * @gir-type Alias
     */
    type VcsRemoteClass = typeof VcsRemote;

    /**
     * @gir-type Alias
     */
    type VcsSignatureClass = typeof VcsSignature;

    /**
     * @gir-type Alias
     */
    type VcsStatsClass = typeof VcsStats;

    /**
     * @gir-type Alias
     */
    type VcsTagClass = typeof VcsTag;

    /**
     * @gir-type Alias
     */
    type VcsTreeClass = typeof VcsTree;

    /**
     * @gir-type Alias
     */
    type WebIntentClass = typeof WebIntent;

    namespace TextBuffer {
        /**
         * Interface for implementing TextBuffer.
         * Contains only the virtual methods that need to be implemented.
         */
        interface Interface {

            // Virtual methods
            /**
             * Adds a new commit notify handler.
             * 
             * If the implementation does not support this, zero is returned.
             * @param flags non-zero flags to dispatch
             * @param commit_notify 
             * @virtual
             */
            vfunc_add_commit_notify(flags: TextBufferNotifyFlags, commit_notify: TextBufferCommitNotify): number;

            /**
             * @param edit 
             * @virtual
             */
            vfunc_apply_edit(edit: TextEdit): boolean;

            /**
             * Gets the contents of the buffer as a {@link GLib.Bytes}.
             * @virtual
             */
            vfunc_dup_contents(): (GLib.Bytes | null);

            /**
             * Gets the GtkSourceView-style identifier for the language of the buffer
             * such as "c" or "js".
             * @virtual
             */
            vfunc_dup_language_id(): (string | null);

            /**
             * Gets the number of changes that have occurred to `buffer`.
             * 
             * This is generally just a monotonic number.
             * @virtual
             */
            vfunc_get_change_count(): (bigint | number);

            /**
             * @param iter 
             * @virtual
             */
            vfunc_iter_init(iter: TextIter): void;

            /**
             * Remove a commit notify handler.
             * @param commit_notify_handler the previously registered non-zero commit handler
             * @virtual
             */
            vfunc_remove_commit_notify(commit_notify_handler: number): void;

            /**
             * Gets a {@link Dex.Future} that will resolve after short delay when changes
             * have completed.
             * @virtual
             */
            vfunc_settle(): Dex.Future;
        }


        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            context: Context;
            language_id: string;
            languageId: string;
        }
    }

    export interface TextBufferNamespace {
        $gtype: GObject.GType<TextBuffer>;
        prototype: TextBuffer;
    }
    /**
     * @gir-type Interface
     */
    interface TextBuffer extends GObject.Object, TextBuffer.Interface {

        // Properties
        /**
         * @construct-only
         */
        get context(): Context;

        /**
         * @read-only
         * @default null
         */
        get language_id(): string;

        /**
         * @read-only
         * @default null
         */
        get languageId(): string;

        // Methods
        /**
         * Adds a new commit notify handler.
         * 
         * If the implementation does not support this, zero is returned.
         * @param flags non-zero flags to dispatch
         * @param commit_notify 
         * @returns a non-zero commit handler id if supported; otherwise zero.
         */
        add_commit_notify(flags: TextBufferNotifyFlags, commit_notify: TextBufferCommitNotify): number;

        /**
         * @param edit 
         */
        apply_edit(edit: TextEdit): boolean;

        /**
         * Gets the contents of the buffer as a {@link GLib.Bytes}.
         * @returns a {@link GLib.Bytes} or `null`
         */
        dup_contents(): (GLib.Bytes | null);

        /**
         * Gets the GtkSourceView-style identifier for the language of the buffer
         * such as "c" or "js".
         */
        dup_language_id(): (string | null);

        emit_changed(): void;

        /**
         * Gets the number of changes that have occurred to `buffer`.
         * 
         * This is generally just a monotonic number.
         */
        get_change_count(): number;

        /**
         * @param iter 
         * @param offset 
         */
        get_iter_at_offset(iter: TextIter, offset: (bigint | number)): void;

        /**
         * @param iter 
         */
        get_start_iter(iter: TextIter): void;

        /**
         * Remove a commit notify handler.
         * @param commit_notify_handler the previously registered non-zero commit handler
         */
        remove_commit_notify(commit_notify_handler: number): void;

        /**
         * Gets a {@link Dex.Future} that will resolve after short delay when changes
         * have completed.
         * @returns a {@link Dex.Future}
         */
        settle(): Dex.Future;
    }


    export const TextBuffer: TextBufferNamespace & {
        new (): TextBuffer; // This allows `obj instanceof TextBuffer`
    };

    /**
     * Name of the imported GIR library
     * `see` https://gitlab.gnome.org/GNOME/gjs/-/blob/master/gi/ns.cpp#L188
     */
    const __name__: string;

    /**
     * Version of the imported GIR library
     * `see` https://gitlab.gnome.org/GNOME/gjs/-/blob/master/gi/ns.cpp#L189
     */
    const __version__: string;
}

export default Foundry;

// END
