
/**
 * Type Definitions for Gjs (https://gjs.guide/)
 *
 * These type definitions are automatically generated, do not edit them by hand.
 * If you found a bug fix it in `ts-for-gir` or create a bug report on https://github.com/gjsify/ts-for-gir
 *
 * The based EJS template file is used for the generated .d.ts file of each GIR module like Gtk-4.0, GObject-2.0, ...
 */

import '@girs/gjs';

// Module dependencies
import type libxml2 from '@girs/libxml2-2.0';
import type Soup from '@girs/soup-3.0';
import type Gio from '@girs/gio-2.0';
import type GObject from '@girs/gobject-2.0';
import type GLib from '@girs/glib-2.0';
import type GModule from '@girs/gmodule-2.0';
import type GstBase from '@girs/gstbase-1.0';
import type Gst from '@girs/gst-1.0';
import type GstAudio from '@girs/gstaudio-1.0';
import type Ags from '@girs/ags-6.0';

export namespace AgsAudio {

    /**
     * AgsAudio-6.0
     */


    /**
     * Enum values to indicate internal state of {@link AgsAudio.AlsaDevin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class AlsaDevinAppBufferMode {
        static $gtype: GObject.GType<AlsaDevinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.AlsaDevin} backend buffer by
     * setting mode.
     * @gir-type Struct
     */
    class AlsaDevinBackendBufferMode {
        static $gtype: GObject.GType<AlsaDevinBackendBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum AlsaDevinError {
        LOCKED_SOUNDCARD,
        BROKEN_CONFIGURATION,
        ACCESS_TYPE_NOT_AVAILABLE,
        SAMPLE_FORMAT_NOT_AVAILABLE,
        CHANNELS_NOT_AVAILABLE,
        SAMPLERATE_NOT_AVAILABLE,
        BUFFER_SIZE_NOT_AVAILABLE,
        HW_PARAMETERS_NOT_AVAILABLE,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.AlsaDevout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class AlsaDevoutAppBufferMode {
        static $gtype: GObject.GType<AlsaDevoutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.AlsaDevout} backend buffer by
     * setting mode.
     * @gir-type Struct
     */
    class AlsaDevoutBackendBufferMode {
        static $gtype: GObject.GType<AlsaDevoutBackendBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum AlsaDevoutError {
        LOCKED_SOUNDCARD,
        BROKEN_CONFIGURATION,
        ACCESS_TYPE_NOT_AVAILABLE,
        SAMPLE_FORMAT_NOT_AVAILABLE,
        CHANNELS_NOT_AVAILABLE,
        SAMPLERATE_NOT_AVAILABLE,
        BUFFER_SIZE_NOT_AVAILABLE,
        HW_PARAMETERS_NOT_AVAILABLE,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.AlsaMidiin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class AlsaMidiinAppBufferMode {
        static $gtype: GObject.GType<AlsaMidiinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.AlsaMidiin} backend buffer by
     * setting mode.
     * @gir-type Struct
     */
    class AlsaMidiinBackendBufferMode {
        static $gtype: GObject.GType<AlsaMidiinBackendBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum AlsaMidiinError {
        ALSA_MIDIIN_ERROR_LOCKED_SEQUENCER,
    }


    /**
     * Copy modes.
     * @gir-type Enum
     */
    enum AudioBufferUtilCopyMode {
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_S8
         */
        S8_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_S16
         */
        S8_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_S24
         */
        S8_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_S32
         */
        S8_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_S64
         */
        S8_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        S8_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        S8_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_S8
         */
        S16_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_S16
         */
        S16_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_S24
         */
        S16_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_S32
         */
        S16_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_S64
         */
        S16_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        S16_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        S16_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_S8
         */
        S24_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_S16
         */
        S24_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_S24
         */
        S24_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_S32
         */
        S24_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_S64
         */
        S24_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        S24_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        S24_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_S8
         */
        S32_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_S16
         */
        S32_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_S24
         */
        S32_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_S32
         */
        S32_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_S64
         */
        S32_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        S32_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        S32_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_S8
         */
        S64_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_S16
         */
        S64_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_S24
         */
        S64_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_S32
         */
        S64_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_S64
         */
        S64_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        S64_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        S64_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_S8
         */
        FLOAT_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_S16
         */
        FLOAT_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_S24
         */
        FLOAT_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_S32
         */
        FLOAT_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_S64
         */
        FLOAT_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        FLOAT_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        FLOAT_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_S8
         */
        DOUBLE_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_S16
         */
        DOUBLE_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_S24
         */
        DOUBLE_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_S32
         */
        DOUBLE_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_S64
         */
        DOUBLE_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        DOUBLE_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        DOUBLE_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_FLOAT32
         */
        S8_TO_FLOAT32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_FLOAT32
         */
        S16_TO_FLOAT32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_FLOAT32
         */
        S24_TO_FLOAT32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_FLOAT32
         */
        S32_TO_FLOAT32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_FLOAT32
         */
        S64_TO_FLOAT32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        FLOAT_TO_FLOAT32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        DOUBLE_TO_FLOAT32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_FLOAT32
         */
        FLOAT32_TO_FLOAT32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_S8
         */
        FLOAT32_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_S16
         */
        FLOAT32_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_S24
         */
        FLOAT32_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_S32
         */
        FLOAT32_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_S64
         */
        FLOAT32_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        FLOAT32_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        FLOAT32_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S8 to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        S8_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S16 to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        S16_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S24 to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        S24_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S32 to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        S32_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_S64 to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        S64_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        FLOAT_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_DOUBLE to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        DOUBLE_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_FLOAT32 to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        FLOAT32_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_COMPLEX
         */
        COMPLEX_TO_COMPLEX,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_S8
         */
        COMPLEX_TO_S8,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_S16
         */
        COMPLEX_TO_S16,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_S24
         */
        COMPLEX_TO_S24,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_S32
         */
        COMPLEX_TO_S32,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_S64
         */
        COMPLEX_TO_S64,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_FLOAT
         */
        COMPLEX_TO_FLOAT,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_DOUBLE
         */
        COMPLEX_TO_DOUBLE,
        /**
         * copy AGS_AUDIO_BUFFER_UTIL_COMPLEX to AGS_AUDIO_BUFFER_UTIL_FLOAT32
         */
        COMPLEX_TO_FLOAT32,
    }


    /**
     * {@link AgsAudio.AudioBufferUtilFormat} specifies the audio data representation.
     * @gir-type Enum
     */
    enum AudioBufferUtilFormat {
        /**
         * signed 8 bit raw pcm
         */
        S8,
        /**
         * signed 16 bit raw pcm
         */
        S16,
        /**
         * signed 24 bit raw pcm
         */
        S24,
        /**
         * signed 32 bit raw pcm
         */
        S32,
        /**
         * signed 64 bit raw pcm
         */
        S64,
        /**
         * signed float raw pcm
         */
        FLOAT,
        /**
         * signed double raw pcm
         */
        DOUBLE,
        /**
         * signed float32 raw pcm
         */
        FLOAT32,
        /**
         * complex data type
         */
        COMPLEX,
    }


    /**
     * Some common samplerates in Hertz.
     * @gir-type Enum
     */
    enum AudioBufferUtilSamplerate {
        /**
         * 8000 Hz
         */
        "8000HZ",
        /**
         * 44100 Hz
         */
        "44100HZ",
        /**
         * 48000 Hz
         */
        "48000HZ",
        /**
         * 96000 Hz
         */
        "96000HZ",
        /**
         * 176400 Hz
         */
        "176400HZ",
        /**
         * 192000 Hz
         */
        "192000HZ",
        /**
         * 352800 Hz
         */
        "352800HZ",
        /**
         * 2822400 Hz
         */
        "2822400HZ",
        /**
         * 5644800 Hz
         */
        "5644800HZ",
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AudioFile} by
     * enable/disable as flags.
     * @gir-type Enum
     */
    enum AudioFileFlags {
        /**
         * read all audio sample data at once
         */
        AUDIO_FILE_READ_SAMPLE_AT_ONCE,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.AudioUnitDevin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class AudioUnitDevinAppBufferMode {
        static $gtype: GObject.GType<AudioUnitDevinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum AudioUnitDevinError {
        AUDIO_UNIT_DEVIN_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.AudioUnitDevout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class AudioUnitDevoutAppBufferMode {
        static $gtype: GObject.GType<AudioUnitDevoutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum AudioUnitDevoutError {
        AUDIO_UNIT_DEVOUT_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * @gir-type Enum
     */
    enum ChannelError {
        CHANNEL_ERROR_LOOP_IN_LINK,
    }


    /**
     * Copy modes.
     * @gir-type Enum
     */
    enum CharBufferUtilCopyMode {
        /**
         * copy signed 8 bit to char buffer
         */
        S8_TO_CBUFFER,
        /**
         * copy signed 16 bit to char buffer
         */
        S16_TO_CBUFFER,
        /**
         * copy signed 24 bit to char buffer
         */
        S24_TO_CBUFFER,
        /**
         * copy signed 32 bit to char buffer
         */
        S32_TO_CBUFFER,
        /**
         * copy signed 64 bit to char buffer
         */
        S64_TO_CBUFFER,
        /**
         * copy float to char buffer
         */
        FLOAT_TO_CBUFFER,
        /**
         * copy double to char buffer
         */
        DOUBLE_TO_CBUFFER,
        /**
         * copy char buffer to signed 8 bit
         */
        CBUFFER_TO_S8,
        /**
         * copy char buffer to signed 16 bit
         */
        CBUFFER_TO_S16,
        /**
         * copy char buffer to signed 24 bit
         */
        CBUFFER_TO_S24,
        /**
         * copy char buffer to signed 32 bit
         */
        CBUFFER_TO_S32,
        /**
         * copy char buffer to signed 64 bit
         */
        CBUFFER_TO_S64,
        /**
         * copy char buffer to float
         */
        CBUFFER_TO_FLOAT,
        /**
         * copy char buffer to double
         */
        CBUFFER_TO_DOUBLE,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.CoreAudioDevin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class CoreAudioDevinAppBufferMode {
        static $gtype: GObject.GType<CoreAudioDevinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum CoreAudioDevinError {
        CORE_AUDIO_DEVIN_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.CoreAudioDevout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class CoreAudioDevoutAppBufferMode {
        static $gtype: GObject.GType<CoreAudioDevoutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum CoreAudioDevoutError {
        CORE_AUDIO_DEVOUT_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.CoreAudioMidiin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class CoreAudioMidiinAppBufferMode {
        static $gtype: GObject.GType<CoreAudioMidiinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * @gir-type Enum
     */
    enum CoreAudioMidiinError {
        CORE_AUDIO_MIDIIN_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to describe the different levels of a DLS2 file.
     * @gir-type Enum
     */
    enum DLS2Level {
        /**
         * filename
         */
        FILENAME,
        /**
         * instrument header
         */
        IHDR,
        /**
         * sample header
         */
        SHDR,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.Fifoout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class FifooutAppBufferMode {
        static $gtype: GObject.GType<FifooutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * @gir-type Enum
     */
    enum FifooutError {
        FIFOOUT_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * @gir-type Enum
     */
    enum FrequencyMapFlags {
        /**
         * the frequency map was processed
         */
        FREQUENCY_MAP_PROCESSED,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.FrequencyMapManager} by
     * enable/disable as flags.
     * @gir-type Enum
     */
    enum FrequencyMapManagerFlags {
        /**
         * if set preserve frequency maps, otherwise destroy
         */
        FREQUENCY_MAP_MANAGER_PRESERVE_FREQUENCY_MAP,
    }


    /**
     * @gir-type Enum
     */
    enum FxDssiAudioFlags {
        FX_DSSI_AUDIO_LIVE_INSTRUMENT,
    }


    /**
     * @gir-type Enum
     */
    enum FxEnvelopeChannelMode {
        STREAM_LENGTH,
        NOTE_LENGTH,
        FIXED_LENGTH,
    }


    /**
     * @gir-type Enum
     */
    enum FxLv2AudioFlags {
        FX_LV2_AUDIO_LIVE_INSTRUMENT,
    }


    /**
     * @gir-type Enum
     */
    enum FxNotationAudioProcessorKeyMode {
        PLAY,
        RECORD,
        FEED,
    }


    /**
     * Enum values to specify capture mode of {@link AgsAudio.FxPlaybackAudio}.
     * @gir-type Enum
     */
    enum FxPlaybackAudioCaptureMode {
        /**
         * discard capture
         */
        DISCARD,
        /**
         * capture none and preserve data
         */
        NONE,
        /**
         * capture replace any sound
         */
        REPLACE,
        /**
         * capture mix with existing sound
         */
        MIX,
    }


    /**
     * Enum values to enable specific data mode of {@link AgsAudio.FxPlaybackAudioProcessor}.
     * @gir-type Enum
     */
    enum FxPlaybackAudioProcessorDataMode {
        /**
         * data mode play sound
         */
        PLAY,
        /**
         * data mode record sound
         */
        RECORD,
        /**
         * data mode feed audio signal
         */
        FEED,
        /**
         * data mode master audio signal
         */
        MASTER,
    }


    /**
     * @gir-type Enum
     */
    enum FxWahWahChannelMode {
        STREAM_LENGTH,
        NOTE_LENGTH,
        FIXED_LENGTH,
    }


    /**
     * @gir-type Enum
     */
    enum FxWahWahLengthMode {
        "1_1_TH",
        "2_2_TH",
        "4_4_TH",
        "8_8_TH",
        "16_16_TH",
    }


    /**
     * Enum values to describe the different levels of a Soundfont2 file.
     * @gir-type Enum
     */
    enum GigLevel {
        /**
         * filename
         */
        FILENAME,
        /**
         * instrument header
         */
        IHDR,
        /**
         * sample header
         */
        SHDR,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.GstreamerDevin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class GstreamerDevinAppBufferMode {
        static $gtype: GObject.GType<GstreamerDevinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum GstreamerDevinError {
        GSTREAMER_DEVIN_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.GstreamerDevout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class GstreamerDevoutAppBufferMode {
        static $gtype: GObject.GType<GstreamerDevoutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum GstreamerDevoutError {
        GSTREAMER_DEVOUT_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.JackDevin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class JackDevinAppBufferMode {
        static $gtype: GObject.GType<JackDevinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * @gir-type Enum
     */
    enum JackDevinError {
        JACK_DEVIN_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.JackDevout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class JackDevoutAppBufferMode {
        static $gtype: GObject.GType<JackDevoutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * @gir-type Enum
     */
    enum JackDevoutError {
        JACK_DEVOUT_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.JackMidiin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class JackMidiinAppBufferMode {
        static $gtype: GObject.GType<JackMidiinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * @gir-type Enum
     */
    enum JackMidiinError {
        JACK_MIDIIN_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * @gir-type Enum
     */
    enum Midi1PortMode {
        NOT_MIDI_1_0,
        YES_DONT_RESTRICT_BANDWIDTH,
        YES_RESTRICT_BANDWIDTH,
    }


    /**
     * @gir-type Enum
     */
    enum Midi2CCIndex {
        "84_PORTAMENTO",
        "126_OMNI_OFF",
    }


    /**
     * @gir-type Enum
     */
    enum Midi2PerNoteManagementOptionsFlags {
        DETACH_PER_NOTE_CONTROLLERS,
        RESET_PER_NOTE_CONTROLLERS,
    }


    /**
     * @gir-type Enum
     */
    enum MidiAttributeType {
        NO_ATTRIBUTE_DATA,
        MANUFACTURER_SPECIFIC,
        PROFILE_SPECIFIC,
        PITCH_7_9,
    }


    /**
     * @gir-type Enum
     */
    enum MidiCIAuthorityLevel {
        TRANSPORT,
        EVENT_PROCESSOR,
        ENDPOINT,
        TRANSLATOR,
        GATEWAY,
        NODE_SERVER,
    }


    /**
     * @gir-type Enum
     */
    enum MidiCICategory {
        PROTOCOL_NEGOTIATION,
        PROFILE_CONFIGURATION,
        PROPERTY_EXCHANGE,
        PROPERTY_MANAGEMENT,
    }


    /**
     * @gir-type Enum
     */
    enum MidiCIUtilProcessSupportedFeatures {
        MIDI_CI_PROCESS_MESSAGE_REPORT,
    }


    /**
     * @gir-type Enum
     */
    enum MidiCIUtilStatusCode {
        SUCCESS,
        ACCEPTED,
        RESOURCE_UNAVAILABLE,
        BAD_DATA,
        TOO_MANY_REQUESTS,
        BAD_REQUEST,
        REPLY_NOT_AVAILABLE,
        RESOURCE_NOT_FOUND,
        RESOURCE_NOT_ALLOWED,
        PAYLOAD_TOO_LARGE,
        UNSUPPORTED_ENCODING,
        INVALID_VERSION,
        INTERNAL_ERROR,
    }


    /**
     * @gir-type Enum
     */
    enum MidiDirection {
        INPUT_RX_ONLY,
        OUTPUT_TX_ONLY,
        BIDIRECTIONAL_MATCHING_GROUPS,
    }


    /**
     * @gir-type Enum
     */
    enum MidiFileError {
        MIDI_FILE_ERROR_PREMATURE_EOF,
    }


    /**
     * @gir-type Enum
     */
    enum MidiFileStatus {
        KEY_OFF,
        KEY_ON,
        KEY_PRESSURE,
        CHANGE_PARAMETER,
        CHANGE_PROGRAM,
        CHANNEL_PRESSURE,
        CHANGE_PITCH_BEND,
        SYSEX,
        QUARTER_FRAME,
        SONG_POSITION,
        SONG_SELECT,
        UNDEFINED_0,
        UNDEFINED_1,
        TUNE_REQUEST,
        SYSEX_END,
        META_EVENT,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Midi} by
     * enable/disable as flags.
     * @gir-type Enum
     */
    enum MidiFlags {
        /**
         * ignore any midi data
         */
        MIDI_BYPASS,
    }


    /**
     * @gir-type Enum
     */
    enum MidiProtocol {
        V1_0,
        V2_0,
    }


    /**
     * @gir-type Enum
     */
    enum MidiSmfUtilMtcQuarterFrameMessageType {
        FRAME_NUMBER_LSB,
        FRAME_NUMBER_MSB,
        SECONDS_LSB,
        SECONDS_MSB,
        MINUTES_LSB,
        MINUTES_MSB,
        HOURS_LSB,
        HOURS_MSB,
    }


    /**
     * @gir-type Enum
     */
    enum MidiSmfUtilSmtpeFrameRate {
        "24_FPS",
        "25_FPS",
        "30_FPS",
        "30_FPS_DROP_FRAME",
    }


    /**
     * @gir-type Enum
     */
    enum MidiUmpStreamMessageFormat {
        COMPLETE,
        START,
        CONTINUE,
        END,
    }


    /**
     * Enum values to specify noise mode.
     * @gir-type Enum
     */
    enum NoiseuUtilMode {
        /**
         * white noise
         */
        WHITE_NOISE,
        /**
         * pink noise
         */
        PINK_NOISE,
    }


    /**
     * @gir-type Enum
     */
    enum OscBuilderFlags {
        OSC_BUILDER_ADD_VALUE,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.OscParser} by
     * enable/disable as flags.
     * @gir-type Enum
     */
    enum OscParserFlags {
        /**
         * the parser reached EOF
         */
        OSC_PARSER_EOF,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.OssDevin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class OssDevinAppBufferMode {
        static $gtype: GObject.GType<OssDevinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.OssDevin} backend buffer by
     * setting mode.
     * @gir-type Struct
     */
    class OssDevinBackendBufferMode {
        static $gtype: GObject.GType<OssDevinBackendBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum OssDevinError {
        LOCKED_SOUNDCARD,
        BROKEN_CONFIGURATION,
        ACCESS_TYPE_NOT_AVAILABLE,
        SAMPLE_FORMAT_NOT_AVAILABLE,
        CHANNELS_NOT_AVAILABLE,
        SAMPLERATE_NOT_AVAILABLE,
        BUFFER_SIZE_NOT_AVAILABLE,
        HW_PARAMETERS_NOT_AVAILABLE,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.OssDevout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class OssDevoutAppBufferMode {
        static $gtype: GObject.GType<OssDevoutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.OssDevout} backend buffer by
     * setting mode.
     * @gir-type Struct
     */
    class OssDevoutBackendBufferMode {
        static $gtype: GObject.GType<OssDevoutBackendBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum OssDevoutError {
        LOCKED_SOUNDCARD,
        BROKEN_CONFIGURATION,
        ACCESS_TYPE_NOT_AVAILABLE,
        SAMPLE_FORMAT_NOT_AVAILABLE,
        CHANNELS_NOT_AVAILABLE,
        SAMPLERATE_NOT_AVAILABLE,
        BUFFER_SIZE_NOT_AVAILABLE,
        HW_PARAMETERS_NOT_AVAILABLE,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.OssMidiin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class OssMidiinAppBufferMode {
        static $gtype: GObject.GType<OssMidiinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.OssMidiin} backend buffer by
     * setting mode.
     * @gir-type Struct
     */
    class OssMidiinBackendBufferMode {
        static $gtype: GObject.GType<OssMidiinBackendBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum OssMidiinError {
        OSS_MIDIIN_ERROR_LOCKED_SEQUENCER,
    }


    /**
     * @gir-type Enum
     */
    enum PresetError {
        PRESET_ERROR_NO_SUCH_PARAMETER,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.PulseDevin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class PulseDevinAppBufferMode {
        static $gtype: GObject.GType<PulseDevinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum PulseDevinError {
        PULSE_DEVIN_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.PulseDevout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class PulseDevoutAppBufferMode {
        static $gtype: GObject.GType<PulseDevoutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum PulseDevoutError {
        PULSE_DEVOUT_ERROR_LOCKED_SOUNDCARD,
    }


    /**
     * Enum values to describe the different levels of a Soundfont2 file.
     * @gir-type Enum
     */
    enum SF2Level {
        /**
         * filename
         */
        FILENAME,
        /**
         * preset header
         */
        PHDR,
        /**
         * instrument header
         */
        IHDR,
        /**
         * sample header
         */
        SHDR,
    }


    /**
     * @gir-type Enum
     */
    export namespace SF2SynthUtilLoopMode {
        export const $gtype: GObject.GType<SF2SynthUtilLoopMode>;
    }

    /**
     * Enum values to specify loop mode.
     * @gir-type Enum
     */
    enum SF2SynthUtilLoopMode {
        /**
         * loop none
         */
        LOOP_NONE,
        /**
         * loop standard
         */
        LOOP_STANDARD,
        /**
         * loop release
         */
        LOOP_RELEASE,
        /**
         * loop pingpong
         */
        LOOP_PINGPONG,
    }


    /**
     * Enum values to describe the different levels of a SFZ file.
     * @gir-type Enum
     */
    enum SFZLevel {
        /**
         * filename
         */
        FILENAME,
        /**
         * sample
         */
        SAMPLE,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.SFZSample} by
     * enable/disable as flags.
     * @gir-type Enum
     */
    enum SFZSampleLoopMode {
        /**
         * one shot
         */
        ONE_SHOT,
        /**
         * loop continuous
         */
        CONTINUOUS,
        /**
         * loop sustain
         */
        SUSTAIN,
    }


    /**
     * @gir-type Enum
     */
    export namespace SFZSynthUtilLoopMode {
        export const $gtype: GObject.GType<SFZSynthUtilLoopMode>;
    }

    /**
     * Enum values to specify loop mode.
     * @gir-type Enum
     */
    enum SFZSynthUtilLoopMode {
        /**
         * loop none
         */
        LOOP_NONE,
        /**
         * loop standard
         */
        LOOP_STANDARD,
        /**
         * loop release
         */
        LOOP_RELEASE,
        /**
         * loop pingpong
         */
        LOOP_PINGPONG,
    }


    /**
     * @gir-type Enum
     */
    export namespace SoundKeyFormat {
        export const $gtype: GObject.GType<SoundKeyFormat>;
    }

    /**
     * Enum values to specify key format.
     * @gir-type Enum
     */
    enum SoundKeyFormat {
        /**
         * key format 16th
         */
        SOUND_KEY_FORMAT_16TH,
        /**
         * key format 256th
         */
        SOUND_KEY_FORMAT_256TH,
    }


    /**
     * @gir-type Enum
     */
    export namespace SoundScope {
        export const $gtype: GObject.GType<SoundScope>;
    }

    /**
     * Enum values to specify current scope or request current scope.
     * @gir-type Enum
     */
    enum SoundScope {
        /**
         * playback scope
         */
        SOUND_SCOPE_PLAYBACK,
        /**
         * sequencer scope
         */
        SOUND_SCOPE_SEQUENCER,
        /**
         * notation ability
         */
        SOUND_SCOPE_NOTATION,
        /**
         * wave ability
         */
        SOUND_SCOPE_WAVE,
        /**
         * MIDI scope
         */
        SOUND_SCOPE_MIDI,
        /**
         * the last scope
         */
        SOUND_SCOPE_LAST,
    }


    /**
     * @gir-type Enum
     */
    export namespace SynthKeyMode {
        export const $gtype: GObject.GType<SynthKeyMode>;
    }

    /**
     * Enum values to specify key mode.
     * @gir-type Enum
     */
    enum SynthKeyMode {
        SYNTH_KEY_1_1,
        /**
         * 2/2 key
         */
        SYNTH_KEY_2_2,
        /**
         * 4/4 key
         */
        SYNTH_KEY_4_4,
        /**
         * 8/8 key
         */
        SYNTH_KEY_8_8,
        /**
         * 16/16 key
         */
        SYNTH_KEY_16_16,
    }


    /**
     * @gir-type Enum
     */
    export namespace SynthOscillatorMode {
        export const $gtype: GObject.GType<SynthOscillatorMode>;
    }

    /**
     * Enum values to specify oscillator mode.
     * @gir-type Enum
     */
    enum SynthOscillatorMode {
        /**
         * sine oscillator
         */
        SYNTH_OSCILLATOR_SIN,
        /**
         * sawtooth oscillator
         */
        SYNTH_OSCILLATOR_SAWTOOTH,
        /**
         * triangle oscillator
         */
        SYNTH_OSCILLATOR_TRIANGLE,
        /**
         * square oscillator
         */
        SYNTH_OSCILLATOR_SQUARE,
        /**
         * impulse oscillator
         */
        SYNTH_OSCILLATOR_IMPULSE,
        /**
         * last mode
         */
        SYNTH_OSCILLATOR_LAST,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.WasapiDevin} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class WasapiDevinAppBufferMode {
        static $gtype: GObject.GType<WasapiDevinAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum WasapiDevinError {
        LOCKED_SOUNDCARD,
        BROKEN_CONFIGURATION,
    }


    /**
     * Enum values to indicate internal state of {@link AgsAudio.WasapiDevout} application buffer by
     * setting mode.
     * @gir-type Struct
     */
    class WasapiDevoutAppBufferMode {
        static $gtype: GObject.GType<WasapiDevoutAppBufferMode>;

        // Static fields
        /**
         * ring-buffer 0
         */
        static "0": number;

        /**
         * ring-buffer 1
         */
        static "1": number;

        /**
         * ring-buffer 2
         */
        static "2": number;

        /**
         * ring-buffer 3
         */
        static "3": number;

        /**
         * ring-buffer 4
         */
        static "4": number;

        /**
         * ring-buffer 5
         */
        static "5": number;

        /**
         * ring-buffer 6
         */
        static "6": number;

        /**
         * ring-buffer 7
         */
        static "7": number;
    }


    /**
     * @gir-type Enum
     */
    enum WasapiDevoutError {
        LOCKED_SOUNDCARD,
        BROKEN_CONFIGURATION,
    }


    const ALSA_DEVIN_DEFAULT_ALSA_DEVICE: string;

    const ALSA_DEVIN_DEFAULT_APP_BUFFER_SIZE: number;

    const ALSA_DEVIN_DEFAULT_BACKEND_BUFFER_SIZE: number;

    const ALSA_DEVOUT_DEFAULT_ALSA_DEVICE: string;

    const ALSA_DEVOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const ALSA_DEVOUT_DEFAULT_BACKEND_BUFFER_SIZE: number;

    const ALSA_MIDIIN_DEFAULT_ALSA_DEVICE: string;

    const ALSA_MIDIIN_DEFAULT_APP_BUFFER_SIZE: number;

    const ALSA_MIDIIN_DEFAULT_BACKEND_BUFFER_SIZE: number;

    const ALSA_MIDIIN_DEFAULT_BUFFER_SIZE: number;

    const AUDIO_APPLICATION_CONTEXT_DEFAULT_LOADER_INTERVAL: number;

    const AUDIO_BUILD_ID: string;

    const AUDIO_DEFAULT_VERSION: string;

    const AUDIO_LOOP_DEFAULT_JIFFIE: number;

    const AUDIO_THREAD_DEFAULT_JIFFIE: number;

    const AUDIO_UNIT_DEVIN_DEFAULT_APP_BUFFER_SIZE: number;

    const AUDIO_UNIT_DEVOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const AUDIO_UNIT_PORT_DEFAULT_CACHE_BUFFER_SIZE: number;

    const AUTOMATION_CLIPBOARD_FORMAT: string;

    const AUTOMATION_CLIPBOARD_LEGACY_FORMAT: string;

    const AUTOMATION_CLIPBOARD_TYPE: string;

    const AUTOMATION_CLIPBOARD_VERSION: string;

    const AUTOMATION_DEFAULT_BPM: number;

    const AUTOMATION_DEFAULT_DURATION: number;

    const AUTOMATION_DEFAULT_JIFFIE: number;

    const AUTOMATION_DEFAULT_LENGTH: number;

    const AUTOMATION_DEFAULT_OFFSET: number;

    const AUTOMATION_DEFAULT_PRECISION: number;

    const AUTOMATION_MAXIMUM_ACCELERATION_LENGTH: number;

    const AUTOMATION_MAXIMUM_STEPS: number;

    const AUTOMATION_MINIMUM_ACCELERATION_LENGTH: number;

    const AUTOMATION_TICS_PER_BEAT: number;

    const BUFFER_DEFAULT_TICKS_PER_QUARTER_BUFFER: number;

    const CHANNEL_MAXIMUM_MIDI_NOTE: number;

    const CHANNEL_MAXIMUM_NOTE_FREQUENCY: number;

    const CHANNEL_MAXIMUM_OCTAVE: number;

    const CHANNEL_MAXIMUM_OCTAVE_SEMITONE: number;

    const CHANNEL_MAXIMUM_SEMITONE: number;

    const CHANNEL_MINIMUM_MIDI_NOTE: number;

    const CHANNEL_MINIMUM_NOTE_FREQUENCY: number;

    const CHANNEL_MINIMUM_OCTAVE: number;

    const CHANNEL_MINIMUM_OCTAVE_SEMITONE: number;

    const CHANNEL_MINIMUM_SEMITONE: number;

    const CHANNEL_THREAD_DEFAULT_JIFFIE: number;

    const CHORUS_UTIL_DEFAULT_DELAY: number;

    const CHORUS_UTIL_DEFAULT_DEPTH: number;

    const CHORUS_UTIL_DEFAULT_HISTORY_BUFFER_LENGTH: number;

    const CHORUS_UTIL_DEFAULT_LFO_FREQUENCY: number;

    const CHORUS_UTIL_DEFAULT_MIX: number;

    const CHORUS_UTIL_DEFAULT_PITCH_MIX_BUFFER_SIZE: number;

    const CORE_AUDIO_DEVIN_DEFAULT_APP_BUFFER_SIZE: number;

    const CORE_AUDIO_DEVOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const CORE_AUDIO_MIDIIN_DEFAULT_BUFFER_SIZE: number;

    const CORE_AUDIO_PORT_DEFAULT_CACHE_BUFFER_SIZE: number;

    const CROP_NOTE_DEFAULT_X_LENGTH: number;

    const EFFECTS_DEFAULT_VERSION: string;

    const EXPORT_THREAD_DEFAULT_JIFFIE: number;

    const FAST_PITCH_UTIL_DEFAULT_MAX_BUFFER_SIZE: number;

    const FIFOOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const FIFOOUT_DEFAULT_DEVICE: string;

    const FIFOOUT_DEFAULT_RING_BUFFER_SIZE: number;

    const FLUID_CENTS_HZ_SIZE: number;

    const FLUID_FRACT_MAX: number;

    const FLUID_INTERP_BITS: number;

    const FLUID_INTERP_BITS_MASK: number;

    const FLUID_INTERP_BITS_SHIFT: number;

    const FLUID_INTERP_MAX: number;

    const FLUID_SINC_INTERP_ORDER: number;

    const FM_SYNTH_UTIL_DEFAULT_FREQUENCY: number;

    const FM_SYNTH_UTIL_DEFAULT_LFO_DEPTH: number;

    const FM_SYNTH_UTIL_DEFAULT_LFO_FREQUENCY: number;

    const FM_SYNTH_UTIL_DEFAULT_TUNING: number;

    const FREQUENCY_MAP_DEFAULT_FREQ: number;

    const FREQUENCY_MAP_MANAGER_EQUINOX: number;

    const FX_EQ10_CHANNEL_INPUT_DATA_CACHE_SIZE: number;

    const FX_FM_SYNTH_AUDIO_DEFAULT_BUFFER_SIZE: number;

    const FX_LV2_AUDIO_DEFAULT_MIDI_LENGHT: number;

    const FX_NOTATION_AUDIO_DEFAULT_LOOP_END: number;

    const FX_NOTATION_AUDIO_DEFAULT_LOOP_START: number;

    const FX_NOTATION_AUDIO_PROCESSOR_DEFAULT_KEY_ON_VELOCITY: number;

    const FX_PATTERN_AUDIO_DEFAULT_LOOP_END: number;

    const FX_PATTERN_AUDIO_DEFAULT_LOOP_START: number;

    const FX_PATTERN_AUDIO_PROCESSOR_DEFAULT_KEY_ON_VELOCITY: number;

    const FX_PLAYBACK_AUDIO_DEFAULT_LOOP_END: number;

    const FX_PLAYBACK_AUDIO_DEFAULT_LOOP_START: number;

    const FX_SF2_SYNTH_AUDIO_DEFAULT_BUFFER_SIZE: number;

    const FX_SYNTH_AUDIO_DEFAULT_BUFFER_SIZE: number;

    const GSTREAMER_DEVIN_DEFAULT_APP_BUFFER_SIZE: number;

    const GSTREAMER_DEVOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const GSTREAMER_FILE_DEFAULT_AUDIO_CHANNELS: number;

    const GSTREAMER_FILE_DEFAULT_DISCOVERER_TIMEOUT: number;

    const GSTREAMER_FILE_DEFAULT_FLUSH_IDLE: number;

    const GSTREAMER_PORT_DEFAULT_CACHE_BUFFER_SIZE: number;

    const HQ_PITCH_UTIL_DEFAULT_MAX_BUFFER_SIZE: number;

    const INPUT_SYNTH_BASE_NOTE: number;

    const IPATCH_DEFAULT_CHANNELS: number;

    const IPATCH_READ: string;

    const IPATCH_WRITE: string;

    const JACK_DEVOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const JACK_MIDIIN_DEFAULT_BUFFER_SIZE: number;

    const JACK_SERVER_DEFAULT_HOST: string;

    const JACK_SERVER_DEFAULT_PORT: number;

    const JACK_SERVER_DEFAULT_PROTOCOL: string;

    const LADSPA_CONVERSION_DEFAULT_LOWER: number;

    const LADSPA_CONVERSION_DEFAULT_STEP_COUNT: number;

    const LADSPA_CONVERSION_DEFAULT_UPPER: number;

    const LFO_SYNTH_UTIL_DEFAULT_FREQUENCY: number;

    const LFO_SYNTH_UTIL_DEFAULT_LFO_DEPTH: number;

    const LFO_SYNTH_UTIL_DEFAULT_TUNING: number;

    const LV2_CONVERSION_DEFAULT_LOWER: number;

    const LV2_CONVERSION_DEFAULT_STEP_COUNT: number;

    const LV2_CONVERSION_DEFAULT_UPPER: number;

    const LV2_MANAGER_DEFAULT_LOCALE: string;

    const MIDI_CI_UTIL_BROADCAST_MUID: number;

    const MIDI_CI_UTIL_BROADCAST_MUID_ENCODED: string;

    const MIDI_CI_UTIL_DISCOVERY_TIMEOUT_USEC: number;

    const MIDI_CI_UTIL_MAX_BROADCAST_MESSAGE_SIZE: number;

    const MIDI_CI_UTIL_PROTOCOL_TYPE_MIDI_1_0: number;

    const MIDI_CI_UTIL_PROTOCOL_TYPE_MIDI_2_0: number;

    const MIDI_CI_UTIL_VERSION_MIDI_1_0: number;

    const MIDI_CLIPBOARD_FORMAT: string;

    const MIDI_CLIPBOARD_TYPE: string;

    const MIDI_CLIPBOARD_VERSION: string;

    const MIDI_DEFAULT_BPM: number;

    const MIDI_DEFAULT_DURATION: number;

    const MIDI_DEFAULT_JIFFIE: number;

    const MIDI_DEFAULT_LENGTH: number;

    const MIDI_DEFAULT_OFFSET: number;

    const MIDI_EVENT: string;

    const MIDI_FILE_DEFAULT_BEATS: number;

    const MIDI_FILE_DEFAULT_FORMAT: number;

    const MIDI_FILE_DEFAULT_FPS: number;

    const MIDI_FILE_DEFAULT_OFFSET: number;

    const MIDI_FILE_DEFAULT_TICKS: number;

    const MIDI_FILE_MAX_TEXT_LENGTH: number;

    const MIDI_FILE_MTHD: string;

    const MIDI_FILE_MTRK: string;

    const MIDI_MINIMUM_TRACK_LENGTH: number;

    const MIDI_PARSER_MAX_TEXT_LENGTH: number;

    const MIDI_PARSER_MTCK: string;

    const MIDI_PARSER_MTHD: string;

    const MIDI_TICS_PER_BEAT: number;

    const MOVE_NOTE_DEFAULT_X_LENGTH: number;

    const MOVE_NOTE_DEFAULT_Y_LENGTH: number;

    const NOISE_UTIL_DEFAULT_FREQUENCY: number;

    const NOTATION_CLIPBOARD_FORMAT: string;

    const NOTATION_CLIPBOARD_TYPE: string;

    const NOTATION_CLIPBOARD_VERSION: string;

    const NOTATION_DEFAULT_BPM: number;

    const NOTATION_DEFAULT_DIVISION: number;

    const NOTATION_DEFAULT_DURATION: number;

    const NOTATION_DEFAULT_END: number;

    const NOTATION_DEFAULT_JIFFIE: number;

    const NOTATION_DEFAULT_LENGTH: number;

    const NOTATION_DEFAULT_OFFSET: number;

    const NOTATION_MAXIMUM_NOTE_LENGTH: number;

    const NOTATION_MINIMUM_NOTE_LENGTH: number;

    const NOTATION_TICS_PER_BEAT: number;

    const NOTE_DEFAULT_TICKS_PER_QUARTER_NOTE: number;

    const OSC_BUILDER_DEFAULT_CHUNK_SIZE: number;

    const OSC_BUILDER_MESSAGE_DEFAULT_CHUNK_SIZE: number;

    const OSC_CLIENT_CHUNK_SIZE: number;

    const OSC_CLIENT_DEFAULT_CACHE_DATA_LENGTH: number;

    const OSC_CLIENT_DEFAULT_DOMAIN: string;

    const OSC_CLIENT_DEFAULT_INET4_ADDRESS: string;

    const OSC_CLIENT_DEFAULT_INET6_ADDRESS: string;

    const OSC_CLIENT_DEFAULT_MAX_ADDRESS_LENGTH: number;

    const OSC_CLIENT_DEFAULT_MAX_RETRY: number;

    const OSC_CLIENT_DEFAULT_SERVER_PORT: number;

    const OSC_CONNECTION_CHUNK_SIZE: number;

    const OSC_CONNECTION_DEAD_LINE_USEC: number;

    const OSC_CONNECTION_DEFAULT_CACHE_DATA_LENGTH: number;

    const OSC_CONNECTION_TIMEOUT_USEC: number;

    const OSC_METER_CONTROLLER_DEFAULT_MONITOR_TIMEOUT: number;

    const OSC_NODE_CONTROLLER_CONTEXT_PATH_LENGTH: number;

    const OSC_PARSER_MAX_TEXT_LENGTH: number;

    const OSC_RENEW_CONTROLLER_CONTEXT_PATH_LENGTH: number;

    const OSC_RESPONSE_DEFAULT_CHUNK_SIZE: number;

    const OSC_RESPONSE_ERROR_MESSAGE_CHUNK_SIZE_EXCEEDED: string;

    const OSC_RESPONSE_ERROR_MESSAGE_MALFORMED_REQUEST: string;

    const OSC_RESPONSE_ERROR_MESSAGE_MISSING_INDEX: string;

    const OSC_RESPONSE_ERROR_MESSAGE_SERVER_FAILURE: string;

    const OSC_RESPONSE_ERROR_MESSAGE_UNKNOWN_ARGUMENT: string;

    const OSC_RESPONSE_ERROR_MESSAGE_UNKNOWN_PATH: string;

    const OSC_SERVER_DEFAULT_BACKLOG: number;

    const OSC_SERVER_DEFAULT_DOMAIN: string;

    const OSC_SERVER_DEFAULT_INET4_ADDRESS: string;

    const OSC_SERVER_DEFAULT_INET6_ADDRESS: string;

    const OSC_SERVER_DEFAULT_MAX_ADDRESS_LENGTH: number;

    const OSC_SERVER_DEFAULT_MAX_CONNECTIONS: number;

    const OSC_SERVER_DEFAULT_SERVER_PORT: number;

    const OSC_UTIL_ADDRESS_PATTERN_VALID_CHARS_PATTERN: string;

    const OSC_UTIL_SLIP_CHUNK_LENGTH: number;

    const OSC_UTIL_SLIP_END: number;

    const OSC_UTIL_SLIP_ESC: number;

    const OSC_UTIL_SLIP_ESC_END: number;

    const OSC_UTIL_SLIP_ESC_ESC: number;

    const OSC_UTIL_TYPE_TAG_STRING_ALL: string;

    const OSC_UTIL_TYPE_TAG_STRING_ARRAY_END: number;

    const OSC_UTIL_TYPE_TAG_STRING_ARRAY_START: number;

    const OSC_UTIL_TYPE_TAG_STRING_BLOB: number;

    const OSC_UTIL_TYPE_TAG_STRING_CHAR: number;

    const OSC_UTIL_TYPE_TAG_STRING_DOUBLE: number;

    const OSC_UTIL_TYPE_TAG_STRING_FALSE: number;

    const OSC_UTIL_TYPE_TAG_STRING_FLOAT: number;

    const OSC_UTIL_TYPE_TAG_STRING_INFINITE: number;

    const OSC_UTIL_TYPE_TAG_STRING_INT32: number;

    const OSC_UTIL_TYPE_TAG_STRING_INT64: number;

    const OSC_UTIL_TYPE_TAG_STRING_MIDI: number;

    const OSC_UTIL_TYPE_TAG_STRING_NIL: number;

    const OSC_UTIL_TYPE_TAG_STRING_RGBA: number;

    const OSC_UTIL_TYPE_TAG_STRING_STRING: number;

    const OSC_UTIL_TYPE_TAG_STRING_SYMBOL: number;

    const OSC_UTIL_TYPE_TAG_STRING_TIMETAG: number;

    const OSC_UTIL_TYPE_TAG_STRING_TRUE: number;

    const OSC_XMLRPC_CONTROLLER_DEFAULT_DELEGATE_TIMEOUT: number;

    const OSC_XMLRPC_SERVER_DEFAULT_CHUNK_SIZE: number;

    const OSS_DEVIN_DEFAULT_APP_BUFFER_SIZE: number;

    const OSS_DEVIN_DEFAULT_BACKEND_BUFFER_SIZE: number;

    const OSS_DEVIN_DEFAULT_OSS_DEVICE: string;

    const OSS_DEVIN_DEFAULT_OSS_MIXER_DEVICE: string;

    const OSS_DEVOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const OSS_DEVOUT_DEFAULT_BACKEND_BUFFER_SIZE: number;

    const OSS_DEVOUT_DEFAULT_OSS_DEVICE: string;

    const OSS_DEVOUT_DEFAULT_OSS_MIXER_DEVICE: string;

    const OSS_MIDIIN_DEFAULT_APP_BUFFER_SIZE: number;

    const OSS_MIDIIN_DEFAULT_BACKEND_BUFFER_SIZE: number;

    const OSS_MIDIIN_DEFAULT_BUFFER_SIZE: number;

    const OSS_MIDIIN_DEFAULT_OSS_DEVICE: string;

    const OSS_MIDIIN_DEFAULT_OSS_MIXER_DEVICE: string;

    const PATTERN_DEFAULT_BPM: number;

    const PATTERN_DEFAULT_DURATION: number;

    const PATTERN_DEFAULT_JIFFIE: number;

    const PATTERN_DEFAULT_LENGTH: number;

    const PATTERN_DEFAULT_OFFSET: number;

    const PATTERN_MAXIMUM_NOTE_LENGTH: number;

    const PATTERN_MINIMUM_NOTE_LENGTH: number;

    const PATTERN_TICS_PER_BEAT: number;

    const PEAK_UTIL_DEFAULT_HARMONIC_RATE: number;

    const PEAK_UTIL_DEFAULT_PRESSURE_FACTOR: number;

    const PLUGIN_NAME_VOLUME: string;

    const PROGRAM_CLIPBOARD_FORMAT: string;

    const PROGRAM_CLIPBOARD_LEGACY_FORMAT: string;

    const PROGRAM_CLIPBOARD_TYPE: string;

    const PROGRAM_CLIPBOARD_VERSION: string;

    const PROGRAM_DEFAULT_BPM: number;

    const PROGRAM_DEFAULT_DURATION: number;

    const PROGRAM_DEFAULT_JIFFIE: number;

    const PROGRAM_DEFAULT_LENGTH: number;

    const PROGRAM_DEFAULT_OFFSET: number;

    const PROGRAM_DEFAULT_PRECISION: number;

    const PROGRAM_MAXIMUM_MARKER_LENGTH: number;

    const PROGRAM_MAXIMUM_STEPS: number;

    const PROGRAM_MINIMUM_MARKER_LENGTH: number;

    const PROGRAM_TICS_PER_BEAT: number;

    const PULSE_DEVIN_DEFAULT_APP_BUFFER_SIZE: number;

    const PULSE_DEVOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const PULSE_PORT_DEFAULT_CACHE_BUFFER_SIZE: number;

    const PULSE_PORT_DEFAULT_CACHE_COUNT: number;

    const RECALL_DEFAULT_BUILD_ID: string;

    const RECALL_DEFAULT_VERSION: string;

    const SEQUENCER_THREAD_DEFAULT_JIFFIE: number;

    const SF2_SYNTH_GENERATOR_DEFAULT_BANK: number;

    const SF2_SYNTH_GENERATOR_DEFAULT_BASE_KEY: number;

    const SF2_SYNTH_GENERATOR_DEFAULT_PITCH_TYPE: string;

    const SF2_SYNTH_GENERATOR_DEFAULT_PROGRAM: number;

    const SF2_SYNTH_GENERATOR_DEFAULT_TUNING: number;

    const SF2_SYNTH_GENERATOR_DEFAULT_VOLUME: number;

    const SFZ_FILE_DEFAULT_CHANNELS: number;

    const SFZ_FILE_LOOP_MAX: number;

    const SFZ_FILE_READ: string;

    const SFZ_FILE_WRITE: string;

    const SFZ_SYNTH_GENERATOR_DEFAULT_BASE_KEY: number;

    const SFZ_SYNTH_GENERATOR_DEFAULT_PITCH_TYPE: string;

    const SFZ_SYNTH_GENERATOR_DEFAULT_TUNING: number;

    const SFZ_SYNTH_GENERATOR_DEFAULT_VOLUME: number;

    const SOUNDCARD_THREAD_DEFAULT_JIFFIE: number;

    const SYNTH_GENERATOR_DEFAULT_FM_LFO_DEPTH: number;

    const SYNTH_GENERATOR_DEFAULT_FM_LFO_FREQUENCY: number;

    const SYNTH_GENERATOR_DEFAULT_FM_TUNING: number;

    const SYNTH_GENERATOR_DEFAULT_FREQUENCY: number;

    const SYNTH_GENERATOR_DEFAULT_LFO_DEPTH: number;

    const SYNTH_GENERATOR_DEFAULT_PHASE: number;

    const SYNTH_GENERATOR_DEFAULT_TUNING: number;

    const SYNTH_GENERATOR_DEFAULT_VOLUME: number;

    const SYNTH_UTIL_DEFAULT_FREQUENCY: number;

    const TIME_STRETCH_UTIL_DEFAULT_FREQUENCY: number;

    const TRACK_DEFAULT_TICKS_PER_QUARTER_TRACK: number;

    const WASAPI_DEVIN_DEFAULT_APP_BUFFER_SIZE: number;

    const WASAPI_DEVIN_DEFAULT_WASAPI_BUFFER_SIZE: number;

    const WASAPI_DEVOUT_DEFAULT_APP_BUFFER_SIZE: number;

    const WASAPI_DEVOUT_DEFAULT_WASAPI_BUFFER_SIZE: number;

    const WAVE_CLIPBOARD_FORMAT: string;

    const WAVE_CLIPBOARD_TYPE: string;

    const WAVE_CLIPBOARD_VERSION: string;

    const WAVE_DEFAULT_BPM: number;

    const WAVE_DEFAULT_BUFFER_LENGTH: number;

    const WAVE_DEFAULT_DURATION: number;

    const WAVE_DEFAULT_JIFFIE: number;

    const WAVE_DEFAULT_LENGTH: number;

    const WAVE_DEFAULT_OFFSET: number;

    const WAVE_MINIMUM_BUFFER_LENGTH: number;

    const WAVE_TICS_PER_BEAT: number;

    /**
     * Clears the audio data.
     * @param buffer the audio data
     * @param channels the number of channels
     * @param count number of frames
     * @param format the format
     * @since 3.0.0
     */
    function audio_buffer_util_clear_buffer(buffer: (any | null), channels: number, count: number, format: number): void;

    /**
     * Clears a buffer.
     * @param buffer the buffer to clear
     * @param channels number of channels
     * @param count count frames
     * @since 3.0.0
     */
    function audio_buffer_util_clear_complex(buffer: Ags.Complex, channels: number, count: number): void;

    /**
     * Clears a buffer.
     * @param buffer the buffer to clear
     * @param channels number of channels
     * @param count count frames
     * @since 3.0.0
     */
    function audio_buffer_util_clear_double(buffer: number, channels: number, count: number): void;

    /**
     * Clears a buffer.
     * @param buffer the buffer to clear
     * @param channels number of channels
     * @param count count frames
     * @since 3.0.0
     */
    function audio_buffer_util_clear_float(buffer: number, channels: number, count: number): void;

    /**
     * Wrapper function to copy functions. Doing type conversion.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param doffset start frame of destination
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param soffset start frame of source
     * @param count number of frames to copy
     * @param mode specified type conversion as described
     * @since 3.0.0
     */
    function audio_buffer_util_copy_buffer_to_buffer(destination: (any | null), dchannels: number, doffset: number, source: (any | null), schannels: number, soffset: number, count: number, mode: AudioBufferUtilCopyMode): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_complex_to_complex(destination: Ags.Complex, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_complex_to_double(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_complex_to_float(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_complex_to_s16(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_complex_to_s24(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_complex_to_s32(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_complex_to_s64(destination: (bigint | number), dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_complex_to_s8(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_double_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_double_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_double_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_double_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_double_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_double_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_double_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_double_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_float_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_float_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_float_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_float_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_float_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_float_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_float_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_float_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s16_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s16_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s16_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s16_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s16_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s16_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s16_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s16_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s24_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s24_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s24_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s24_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s24_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s24_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s24_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s24_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s32_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s32_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s32_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s32_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s32_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s32_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s32_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s32_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s64_to_complex(destination: Ags.Complex, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s64_to_double(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s64_to_float(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s64_to_s16(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s64_to_s24(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s64_to_s32(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s64_to_s64(destination: (bigint | number), dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s64_to_s8(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s8_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s8_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s8_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s8_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s8_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s8_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s8_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Copy audio data using additive strategy.
     * @param destination destination buffer
     * @param dchannels destination buffer's count of channels
     * @param source source buffer
     * @param schannels source buffer's count of channels
     * @param count number of frames to copy
     * @since 3.0.0
     */
    function audio_buffer_util_copy_s8_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Translate soundcard format to audio buffer util format.
     * @param soundcard_format the soundcard bit mode
     * @returns the converted format
     * @since 3.0.0
     */
    function audio_buffer_util_format_from_soundcard(soundcard_format: Ags.SoundcardFormat): AudioBufferUtilFormat;

    /**
     * Get copy mode for given destination and source format.
     * @param destination_format the destination buffer format
     * @param source_format the source buffer format
     * @returns the desired copy mode
     * @since 3.0.0
     */
    function audio_buffer_util_get_copy_mode(destination_format: AudioBufferUtilFormat, source_format: AudioBufferUtilFormat): AudioBufferUtilCopyMode;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @param format the format
     * @since 3.4.0
     */
    function audio_buffer_util_pong(destination: (any | null), dchannels: number, source: (any | null), schannels: number, count: number, format: number): void;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @since 3.4.0
     */
    function audio_buffer_util_pong_complex(destination: Ags.Complex, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @since 3.4.0
     */
    function audio_buffer_util_pong_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @since 3.4.0
     */
    function audio_buffer_util_pong_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @since 3.4.0
     */
    function audio_buffer_util_pong_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @since 3.4.0
     */
    function audio_buffer_util_pong_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @since 3.4.0
     */
    function audio_buffer_util_pong_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @since 3.4.0
     */
    function audio_buffer_util_pong_s64(destination: (bigint | number), dchannels: number, source: (bigint | number), schannels: number, count: number): void;

    /**
     * Pingpong copy `source` to `destination`.
     * @param destination the destination
     * @param dchannels the destination channels
     * @param source the source
     * @param schannels the source channels
     * @param count the count of frames
     * @since 3.4.0
     */
    function audio_buffer_util_pong_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

    /**
     * @param destination 
     * @param dchannels 
     * @param doffset 
     * @param source 
     * @param schannels 
     * @param soffset 
     * @param frame_count 
     * @param byte_order 
     * @param word_size 
     * @param mode 
     */
    function char_buffer_util_copy_buffer_to_buffer(destination: (any | null), dchannels: number, doffset: number, source: (any | null), schannels: number, soffset: number, frame_count: number, byte_order: number, word_size: number, mode: CharBufferUtilCopyMode): void;

    /**
     * Copy char to double buffer.
     * @param destination destination buffer
     * @param dchannels destination channels
     * @param source source buffer
     * @param word_size word size
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_cbuffer_to_double(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy char to float buffer.
     * @param destination destination buffer
     * @param dchannels destination channels
     * @param source source buffer
     * @param word_size word size
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_cbuffer_to_float(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy char to gint16 buffer.
     * @param destination destination buffer
     * @param dchannels destination channels
     * @param source source buffer
     * @param word_size word size
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_cbuffer_to_s16(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy char to gint32 buffer.
     * @param destination destination buffer
     * @param dchannels destination channels
     * @param source source buffer
     * @param word_size word size
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_cbuffer_to_s24(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy char to gint32 buffer.
     * @param destination destination buffer
     * @param dchannels destination channels
     * @param source source buffer
     * @param word_size word size
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_cbuffer_to_s32(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy char to gint64 buffer.
     * @param destination destination buffer
     * @param dchannels destination channels
     * @param source source buffer
     * @param word_size word size
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_cbuffer_to_s64(destination: (bigint | number), dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy char to gint8 buffer.
     * @param destination destination buffer
     * @param dchannels destination channels
     * @param source source buffer
     * @param word_size word size
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_cbuffer_to_s8(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy double to char buffer.
     * @param destination destination buffer
     * @param word_size word size
     * @param dchannels destination channels
     * @param source source buffer
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_double_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy float to char buffer.
     * @param destination destination buffer
     * @param word_size word size
     * @param dchannels destination channels
     * @param source source buffer
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_float_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy gint16 to char buffer.
     * @param destination destination buffer
     * @param word_size word size
     * @param dchannels destination channels
     * @param source source buffer
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_s16_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy gint32 to char buffer.
     * @param destination destination buffer
     * @param word_size word size
     * @param dchannels destination channels
     * @param source source buffer
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_s24_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy gint32 to char buffer.
     * @param destination destination buffer
     * @param word_size word size
     * @param dchannels destination channels
     * @param source source buffer
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_s32_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy gint64 to char buffer.
     * @param destination destination buffer
     * @param word_size word size
     * @param dchannels destination channels
     * @param source source buffer
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_s64_to_cbuffer(destination: number, word_size: number, dchannels: number, source: (bigint | number), schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Copy gint8 to char buffer.
     * @param destination destination buffer
     * @param word_size word size
     * @param dchannels destination channels
     * @param source source buffer
     * @param schannels source channels
     * @param frame_count frame count
     * @param byte_order the byte-order
     * @since 3.0.0
     */
    function char_buffer_util_copy_s8_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

    /**
     * Allocate {@link AgsAudio.ChorusUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.ChorusUtil}-struct
     * @since 3.13.4
     */
    function chorus_util_alloc(): ChorusUtil;

    /**
     * Get base key of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the base key of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_base_key(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get buffer length of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the buffer length of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_buffer_length(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get destination of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the destination of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_destination(pitch_util: (any | null), pitch_type: GObject.GType): (any | null);

    /**
     * Get destination stride of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the destination stride of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_destination_stride(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get format of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the format of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_format(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get frame count of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the frame count of `pitch_util`
     * @since 6.1.0
     */
    function common_pitch_util_get_frame_count(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get note 256th mode of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns `true` if note 256th mode of `pitch_util`, otherwise `false`
     * @since 6.1.0
     */
    function common_pitch_util_get_note_256th_mode(pitch_util: (any | null), pitch_type: GObject.GType): boolean;

    /**
     * Get offset of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the offset of `pitch_util`
     * @since 6.1.0
     */
    function common_pitch_util_get_offset(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get offset note 256th of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the offset note 256th of `pitch_util`
     * @since 6.1.0
     */
    function common_pitch_util_get_offset_256th(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get samplerate of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the samplerate of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_samplerate(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get source of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the source of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_source(pitch_util: (any | null), pitch_type: GObject.GType): (any | null);

    /**
     * Get source stride of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the source stride of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_source_stride(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get tuning of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the tuning of `pitch_util`
     * @since 4.0.0
     */
    function common_pitch_util_get_tuning(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get vibrato enabled of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the vibrato enabled of `pitch_util`
     * @since 5.2.0
     */
    function common_pitch_util_get_vibrato_enabled(pitch_util: (any | null), pitch_type: GObject.GType): boolean;

    /**
     * Get vibrato_gain of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the vibrato_gain of `pitch_util`
     * @since 5.2.0
     */
    function common_pitch_util_get_vibrato_gain(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get vibrato LFO depth of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the vibrato LFO depth of `pitch_util`
     * @since 5.2.0
     */
    function common_pitch_util_get_vibrato_lfo_depth(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get vibrato LFO frequency of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the vibrato LFO frequency of `pitch_util`
     * @since 5.2.0
     */
    function common_pitch_util_get_vibrato_lfo_freq(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get vibrato LFO offset of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the vibrato LFO offset of `pitch_util`
     * @since 5.2.4
     */
    function common_pitch_util_get_vibrato_lfo_offset(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Get vibrato tuning of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @returns the vibrato tuning of `pitch_util`
     * @since 5.2.0
     */
    function common_pitch_util_get_vibrato_tuning(pitch_util: (any | null), pitch_type: GObject.GType): number;

    /**
     * Pitch `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @since 4.0.0
     */
    function common_pitch_util_pitch(pitch_util: (any | null), pitch_type: GObject.GType): void;

    /**
     * Set `base_key` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param base_key the base key
     * @since 4.0.0
     */
    function common_pitch_util_set_base_key(pitch_util: (any | null), pitch_type: GObject.GType, base_key: number): void;

    /**
     * Set `buffer_length` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param buffer_length the buffer length
     * @since 4.0.0
     */
    function common_pitch_util_set_buffer_length(pitch_util: (any | null), pitch_type: GObject.GType, buffer_length: number): void;

    /**
     * Set `destination` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param destination the destination
     * @since 4.0.0
     */
    function common_pitch_util_set_destination(pitch_util: (any | null), pitch_type: GObject.GType, destination: (any | null)): void;

    /**
     * Set `destination_stride` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param destination_stride the destination stride
     * @since 4.0.0
     */
    function common_pitch_util_set_destination_stride(pitch_util: (any | null), pitch_type: GObject.GType, destination_stride: number): void;

    /**
     * Set `format` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param format the format
     * @since 4.0.0
     */
    function common_pitch_util_set_format(pitch_util: (any | null), pitch_type: GObject.GType, format: number): void;

    /**
     * Set `frame_count` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param frame_count the frame count
     * @since 6.1.0
     */
    function common_pitch_util_set_frame_count(pitch_util: (any | null), pitch_type: GObject.GType, frame_count: number): void;

    /**
     * Set `note_256th_mode` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param note_256th_mode `true` if note 256th mode, otherwise `false`
     * @since 6.1.0
     */
    function common_pitch_util_set_note_256th_mode(pitch_util: (any | null), pitch_type: GObject.GType, note_256th_mode: boolean): void;

    /**
     * Set `offset` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param offset the offset
     * @since 6.1.0
     */
    function common_pitch_util_set_offset(pitch_util: (any | null), pitch_type: GObject.GType, offset: number): void;

    /**
     * Set `offset_256th` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param offset_256th the offset note 256th
     * @since 6.1.0
     */
    function common_pitch_util_set_offset_256th(pitch_util: (any | null), pitch_type: GObject.GType, offset_256th: number): void;

    /**
     * Set `samplerate` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param samplerate the samplerate
     * @since 4.0.0
     */
    function common_pitch_util_set_samplerate(pitch_util: (any | null), pitch_type: GObject.GType, samplerate: number): void;

    /**
     * Set `source` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param source the source
     * @since 4.0.0
     */
    function common_pitch_util_set_source(pitch_util: (any | null), pitch_type: GObject.GType, source: (any | null)): void;

    /**
     * Set `source_stride` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param source_stride the source stride
     * @since 4.0.0
     */
    function common_pitch_util_set_source_stride(pitch_util: (any | null), pitch_type: GObject.GType, source_stride: number): void;

    /**
     * Set `tuning` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param tuning the tuning
     * @since 4.0.0
     */
    function common_pitch_util_set_tuning(pitch_util: (any | null), pitch_type: GObject.GType, tuning: number): void;

    /**
     * Set `vibrato_enabled` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param vibrato_enabled the vibrato enabled
     * @since 5.2.0
     */
    function common_pitch_util_set_vibrato_enabled(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_enabled: boolean): void;

    /**
     * Set `vibrato_gain` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param vibrato_gain the vibrato_gain
     * @since 5.2.0
     */
    function common_pitch_util_set_vibrato_gain(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_gain: number): void;

    /**
     * Set `vibrato_lfo_depth` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param vibrato_lfo_depth the vibrato LFO depth
     * @since 5.2.0
     */
    function common_pitch_util_set_vibrato_lfo_depth(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_lfo_depth: number): void;

    /**
     * Set `vibrato_lfo_freq` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param vibrato_lfo_freq the vibrato LFO frequency
     * @since 5.2.0
     */
    function common_pitch_util_set_vibrato_lfo_freq(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_lfo_freq: number): void;

    /**
     * Set `vibrato_lfo_offset` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param vibrato_lfo_offset the vibrato LFO offset
     * @since 5.2.4
     */
    function common_pitch_util_set_vibrato_lfo_offset(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_lfo_offset: number): void;

    /**
     * Set `vibrato_tuning` of `pitch_util`.
     * @param pitch_util the pitch util
     * @param pitch_type the pitch type
     * @param vibrato_tuning the vibrato tuning
     * @since 5.2.0
     */
    function common_pitch_util_set_vibrato_tuning(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_tuning: number): void;

    /**
     * Convert MIDI `key` to note name and store in `note`.
     * @param key MIDI key
     * @param note return location of note name
     * @returns the number of chars written
     * @since 3.0.0
     */
    function diatonic_scale_midi_key_to_note(key: (bigint | number), note: string): number;

    /**
     * Convert `note` to MIDI key and store in `key`.
     * @param note the note name
     * @param key return location of MIDI key
     * @returns the number of chars read
     * @since 3.0.0
     */
    function diatonic_scale_note_to_midi_key(note: string, key: (bigint | number)): number;

    /**
     * Allocate {@link AgsAudio.EnvelopeUtil}-struct
     * @returns a new {@link AgsAudio.EnvelopeUtil}-struct
     * @since 3.9.2
     */
    function envelope_util_alloc(): EnvelopeUtil;

    /**
     * Allocate {@link AgsAudio.FastPitchUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.FastPitchUtil}-struct
     * @since 3.9.6
     */
    function fast_pitch_util_alloc(): FastPitchUtil;

    /**
     * @param cents 
     */
    function fluid_ct2hz(cents: number): number;

    /**
     * @param cents 
     */
    function fluid_ct2hz_real(cents: number): number;

    /**
     * Allocate {@link AgsAudio.FluidIIRFilterUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.FluidIIRFilterUtil}-struct
     * @since 3.9.6
     */
    function fluid_iir_filter_util_alloc(): FluidIIRFilterUtil;

    /**
     * Allocate {@link AgsAudio.FluidInterpolate4thOrderUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.FluidInterpolate4thOrderUtil}-struct
     * @since 3.9.6
     */
    function fluid_interpolate_4th_order_util_alloc(): FluidInterpolate4thOrderUtil;

    /**
     * Allocate {@link AgsAudio.FluidInterpolate7thOrderUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.FluidInterpolate7thOrderUtil}-struct
     * @since 3.9.6
     */
    function fluid_interpolate_7th_order_util_alloc(): FluidInterpolate7thOrderUtil;

    /**
     * Allocate {@link AgsAudio.FluidInterpolateLinearUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.FluidInterpolateLinearUtil}-struct
     * @since 3.9.6
     */
    function fluid_interpolate_linear_util_alloc(): FluidInterpolateLinearUtil;

    /**
     * Allocate {@link AgsAudio.FluidInterpolateNoneUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.FluidInterpolateNoneUtil}-struct
     * @since 3.9.6
     */
    function fluid_interpolate_none_util_alloc(): FluidInterpolateNoneUtil;

    /**
     * Allocate {@link AgsAudio.FMSynthUtil}-struct
     * @returns a new {@link AgsAudio.FMSynthUtil}-struct
     * @since 3.9.3
     */
    function fm_synth_util_alloc(): FMSynthUtil;

    /**
     * Compute fourier transform of `buffer`.
     * @param buffer the audio buffer
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.6.0
     */
    function fourier_transform_util_compute_stft_complex(buffer: Ags.Complex, channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute fourier transform of `buffer`.
     * @param buffer the audio buffer
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_compute_stft_double(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute fourier transform of `buffer`.
     * @param buffer the audio buffer
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_compute_stft_float(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute fourier transform of `buffer`.
     * @param buffer the audio buffer
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_compute_stft_s16(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute fourier transform of `buffer`.
     * @param buffer the audio buffer
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_compute_stft_s24(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute fourier transform of `buffer`.
     * @param buffer the audio buffer
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_compute_stft_s32(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute fourier transform of `buffer`.
     * @param buffer the audio buffer
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_compute_stft_s64(buffer: (bigint | number), channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute fourier transform of `buffer`.
     * @param buffer the audio buffer
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_compute_stft_s8(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute inverse fourier transform of `buffer`.
     * @param buffer the complex data
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.6.0
     */
    function fourier_transform_util_inverse_stft_complex(buffer: Ags.Complex, channels: number, buffer_length: number, retval: Ags.Complex): void;

    /**
     * Compute inverse fourier transform of `buffer`.
     * @param buffer the complex data
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_inverse_stft_double(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

    /**
     * Compute inverse fourier transform of `buffer`.
     * @param buffer the complex data
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_inverse_stft_float(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

    /**
     * Compute inverse fourier transform of `buffer`.
     * @param buffer the complex data
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_inverse_stft_s16(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

    /**
     * Compute inverse fourier transform of `buffer`.
     * @param buffer the complex data
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_inverse_stft_s24(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

    /**
     * Compute inverse fourier transform of `buffer`.
     * @param buffer the complex data
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_inverse_stft_s32(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

    /**
     * Compute inverse fourier transform of `buffer`.
     * @param buffer the complex data
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_inverse_stft_s64(buffer: Ags.Complex, channels: number, buffer_length: number, retval: (bigint | number)): void;

    /**
     * Compute inverse fourier transform of `buffer`.
     * @param buffer the complex data
     * @param channels number of audio channels
     * @param buffer_length the buffer's length
     * @param retval the return location of result
     * @since 3.0.0
     */
    function fourier_transform_util_inverse_stft_s8(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

    /**
     * Allocate {@link AgsAudio.FrequencyAliaseUtil}-struct
     * @returns a new {@link AgsAudio.FrequencyAliaseUtil}-struct
     * @since 4.0.0
     */
    function frequency_aliase_util_alloc(): FrequencyAliaseUtil;

    /**
     * Allocate {@link AgsAudio.FxAnalyseChannelInputData}-struct
     * @returns the new {@link AgsAudio.FxAnalyseChannelInputData}-struct
     * @since 3.3.0
     */
    function fx_analyse_channel_input_data_alloc(): (any | null);

    /**
     * Free `input_data`.
     * @param input_data the {@link AgsAudio.FxAnalyseChannelInputData}-struct
     * @since 3.3.0
     */
    function fx_analyse_channel_input_data_free(input_data: (any | null)): void;

    /**
     * Get structure mutex.
     * @param input_data the {@link AgsAudio.FxAnalyseChannelInputData}
     * @returns the {@link GLib.RecMutex} to lock `input_data`
     * @since 3.3.0
     */
    function fx_analyse_channel_input_data_get_strct_mutex(input_data: (any | null)): (any | null);

    /**
     * Allocate {@link AgsAudio.FxBufferChannelInputData}-struct
     * @returns the new {@link AgsAudio.FxBufferChannelInputData}-struct
     * @since 3.3.0
     */
    function fx_buffer_channel_input_data_alloc(): (any | null);

    /**
     * Free `input_data`.
     * @param input_data the {@link AgsAudio.FxBufferChannelInputData}-struct
     * @since 3.3.0
     */
    function fx_buffer_channel_input_data_free(input_data: (any | null)): void;

    /**
     * Get structure mutex.
     * @param input_data the {@link AgsAudio.FxBufferChannelInputData}
     * @returns the {@link GLib.RecMutex} to lock `input_data`
     * @since 3.3.0
     */
    function fx_buffer_channel_input_data_get_strct_mutex(input_data: (any | null)): (any | null);

    /**
     * Allocate {@link AgsAudio.FxEq10ChannelInputData}-struct
     * @returns the new {@link AgsAudio.FxEq10ChannelInputData}-struct
     * @since 3.3.0
     */
    function fx_eq10_channel_input_data_alloc(): (any | null);

    /**
     * Free `input_data`.
     * @param input_data the {@link AgsAudio.FxEq10ChannelInputData}-struct
     * @since 3.3.0
     */
    function fx_eq10_channel_input_data_free(input_data: (any | null)): void;

    /**
     * Allocate {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
     * @returns the new {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
     * @since 3.8.0
     */
    function fx_two_pass_aliase_channel_input_data_alloc(): (any | null);

    /**
     * Free `input_data`.
     * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
     * @since 3.8.0
     */
    function fx_two_pass_aliase_channel_input_data_free(input_data: (any | null)): void;

    /**
     * Get structure mutex.
     * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}
     * @returns the {@link GLib.RecMutex} to lock `input_data`
     * @since 3.8.0
     */
    function fx_two_pass_aliase_channel_input_data_get_strct_mutex(input_data: (any | null)): (any | null);

    /**
     * Allocate {@link AgsAudio.HQPitchUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.HQPitchUtil}-struct
     * @since 3.9.6
     */
    function hq_pitch_util_alloc(): HQPitchUtil;

    /**
     * Allocate {@link AgsAudio.LFOSynthUtil}-struct
     * @returns a new {@link AgsAudio.LFOSynthUtil}-struct
     * @since 4.0.0
     */
    function lfo_synth_util_alloc(): LFOSynthUtil;

    /**
     * Allocate {@link AgsAudio.LinearInterpolateUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.LinearInterpolateUtil}-struct
     * @since 3.9.7
     */
    function linear_interpolate_util_alloc(): LinearInterpolateUtil;

    /**
     * Allocate an {@link AgsAudio.Lv2OptionRessource}.
     * @returns the newly created {@link AgsAudio.Lv2OptionRessource}-struct
     * @since 3.0.0
     */
    function lv2_option_ressource_alloc(): (any | null);

    /**
     * Allocated {@link AgsAudio.Lv2PortPreset}-struct.
     * @param port_symbol the port symbol
     * @param port_type the port type
     * @returns the new {@link AgsAudio.Lv2PortPreset}-struct.
     * @since 3.0.0
     */
    function lv2_port_preset_alloc(port_symbol: string, port_type: GObject.GType): (any | null);

    /**
     * Free `lv2_port_preset`.
     * @param lv2_port_preset the {@link AgsAudio.Lv2PortPreset}-struct
     * @since 3.0.0
     */
    function lv2_port_preset_free(lv2_port_preset: (any | null)): void;

    /**
     * @param midi_file 
     */
    function mid_file_read_midi(midi_file: MidiFile): void;

    /**
     * Allocate MIDI builder header.
     * @returns the newly allocated {@link AgsAudio.MidiBuilderHeader}-struct
     * @since 3.0.0
     */
    function midi_builder_header_alloc(): (any | null);

    /**
     * Free MIDI builder header.
     * @param midi_builder_header the {@link AgsAudio.MidiBuilderHeader}-struct
     * @since 3.0.0
     */
    function midi_builder_header_free(midi_builder_header: (any | null)): void;

    /**
     * Allocate MIDI builder track.
     * @returns the newly allocated {@link AgsAudio.MidiBuilderTrack}-struct
     * @since 3.0.0
     */
    function midi_builder_track_alloc(): (any | null);

    /**
     * Free MIDI builder track.
     * @param midi_builder_track the {@link AgsAudio.MidiBuilderTrack}-struct
     * @since 3.0.0
     */
    function midi_builder_track_free(midi_builder_track: (any | null)): void;

    /**
     * Allocate MIDI CI util.
     * @returns the newly allocated {@link AgsAudio.MidiCIUtil}-struct
     * @since 5.5.0
     */
    function midi_ci_util_alloc(): MidiCIUtil;

    /**
     * Allocate MIDI util.
     * @returns the newly allocated {@link AgsAudio.MidiSmfUtil}-struct
     * @since 6.0.0
     */
    function midi_smf_util_alloc(): MidiSmfUtil;

    /**
     * Allocate MIDI UMP util.
     * @returns the newly allocated {@link AgsAudio.MidiUmpUtil}-struct
     * @since 5.5.4
     */
    function midi_ump_util_alloc(): MidiUmpUtil;

    /**
     * Allocate MIDI util.
     * @returns the newly allocated {@link AgsAudio.MidiUtil}-struct
     * @since 5.5.0
     */
    function midi_util_alloc(): MidiUtil;

    /**
     * Allocate {@link AgsAudio.NoiseUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.NoiseUtil}-struct
     * @since 3.15.0
     */
    function noise_util_alloc(): NoiseUtil;

    /**
     * Allocate OSC util.
     * @returns the newly allocated {@link AgsAudio.OscBufferUtil}-struct
     * @since 6.0.0
     */
    function osc_buffer_util_alloc(): OscBufferUtil;

    /**
     * Allocate {@link AgsAudio.OscBuilderBundle}-struct.
     * @param offset the current offset
     * @returns the newly allocated {@link AgsAudio.OscBuilderBundle}-struct
     * @since 3.0.0
     */
    function osc_builder_bundle_alloc(offset: (bigint | number)): (any | null);

    /**
     * Free `bundle`.
     * @param bundle the {@link AgsAudio.OscBuilderBundle}-struct
     * @since 3.0.0
     */
    function osc_builder_bundle_free(bundle: (any | null)): void;

    /**
     * Allocate {@link AgsAudio.OscBuilderMessage}-struct.
     * @param offset the current offset
     * @returns the newly allocated {@link AgsAudio.OscBuilderMessage}-struct
     * @since 3.0.0
     */
    function osc_builder_message_alloc(offset: (bigint | number)): (any | null);

    /**
     * Free `message`.
     * @param message the {@link AgsAudio.OscBuilderMessage}-struct
     * @since 3.0.0
     */
    function osc_builder_message_free(message: (any | null)): void;

    /**
     * Allocate {@link AgsAudio.OscBuilderPacket}-struct.
     * @param offset the current offset
     * @returns the newly allocated {@link AgsAudio.OscBuilderPacket}-struct
     * @since 3.0.0
     */
    function osc_builder_packet_alloc(offset: (bigint | number)): (any | null);

    /**
     * Free `packet`.
     * @param packet the {@link AgsAudio.OscBuilderPacket}-struct
     * @since 3.0.0
     */
    function osc_builder_packet_free(packet: (any | null)): void;

    /**
     * Allocate {@link AgsAudio.OscMeterControllerMonitor}-struct.
     * @returns the newly allocate {@link AgsAudio.OscMeterControllerMonitor}-struct
     * @since 3.0.0
     */
    function osc_meter_controller_monitor_alloc(): (any | null);

    /**
     * Free `monitor`.
     * @param monitor the {@link AgsAudio.OscMeterControllerMonitor}-struct
     * @since 3.0.0
     */
    function osc_meter_controller_monitor_free(monitor: (any | null)): void;

    /**
     * Increase reference count of `monitor`.
     * @param monitor the {@link AgsAudio.OscMeterControllerMonitor}-struct
     * @since 3.0.0
     */
    function osc_meter_controller_monitor_ref(monitor: (any | null)): void;

    /**
     * Decrease reference count of `monitor`. If ref count is less or equal 0
     * `monitor` is freed.
     * @param monitor the {@link AgsAudio.OscMeterControllerMonitor}-struct
     * @since 3.0.0
     */
    function osc_meter_controller_monitor_unref(monitor: (any | null)): void;

    /**
     * Allocate OSC util.
     * @returns the newly allocated {@link AgsAudio.OscUtil}-struct
     * @since 6.0.0
     */
    function osc_util_alloc(): OscUtil;

    /**
     * Get meta data.
     * @param uri the URI
     * @param meta_data_length the return location of meta data's length
     * @returns the byte array containing meta data
     * @since 3.0.0
     */
    function osc_util_meta_data(uri: string, meta_data_length: number): number;

    /**
     * Get MIME header.
     * @param uri the URI
     * @param mime_header_length the return location of MIME header's length
     * @returns the byte array containing MIME header
     * @since 3.0.0
     */
    function osc_util_mime_header(uri: string, mime_header_length: number): number;

    /**
     * Decode `slip_buffer` from SLIP encoded format.
     * See SLIP (RFC1055).
     * @param slip_buffer the SLIP encoded OSC buffer
     * @param slip_buffer_length the buffer length of SLIP encoded OSC buffer
     * @param returned_buffer_length the returned buffer's length
     * @returns the OSC buffer as byte array
     * @since 3.0.0
     */
    function osc_util_slip_decode(slip_buffer: number, slip_buffer_length: number, returned_buffer_length: number): number;

    /**
     * Encode `osc_buffer` to be suitable for network transmission.
     * See SLIP (RFC1055).
     * @param osc_buffer the OSC buffer
     * @param buffer_length the OSC buffer's length
     * @param returned_buffer_length the returned buffer's length
     * @returns the encoded byte array
     * @since 3.0.0
     */
    function osc_util_slip_encode(osc_buffer: number, buffer_length: number, returned_buffer_length: number): number;

    /**
     * Get current time.
     * @param tv_sec the return location of number of seconds since midnight on January 1, 1900
     * @param tv_fraction the return location of fraction of seconds to a precision of about 200 picoseconds
     * @since 3.0.0
     */
    function osc_util_timetag_now(tv_sec: number, tv_fraction: number): void;

    /**
     * Count types in `type_tag`.
     * @param type_tag the type tag string
     * @returns the count of types specified
     * @since 3.0.0
     */
    function osc_util_type_tag_string_count_type(type_tag: string): number;

    /**
     * Allocate {@link AgsAudio.PeakUtil}-struct
     * @returns a new {@link AgsAudio.PeakUtil}-struct
     * @since 3.9.2
     */
    function peak_util_alloc(): PeakUtil;

    /**
     * Allocate {@link AgsAudio.PhaseShiftUtil}-struct
     * @returns a new {@link AgsAudio.PhaseShiftUtil}-struct
     * @since 4.0.0
     */
    function phase_shift_util_alloc(): PhaseShiftUtil;

    /**
     * Allocates {@link AgsAudio.RecallHandler}-struct.
     * @param signal_name signal's name to connect
     * @param callback the {@link GObject.Callback} function
     * @param data the data to pass the callback
     * @returns the newly allocated {@link AgsAudio.RecallHandler}-struct
     * @since 3.0.0
     */
    function recall_handler_alloc(signal_name: string, callback: GObject.Callback, data: GObject.Object): (any | null);

    /**
     * Free `recall_hanlder`.
     * @param recall_handler the {@link AgsAudio.RecallHandler}-struct
     * @since 3.0.0
     */
    function recall_handler_free(recall_handler: (any | null)): void;

    /**
     * Allocate {@link AgsAudio.ResampleUtil}-struct
     * @returns a new {@link AgsAudio.ResampleUtil}-struct
     * @since 3.9.2
     */
    function resample_util_alloc(): ResampleUtil;

    /**
     * Get object mutex of `sequencer`.
     * @param sequencer the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
     * @returns GRecMutex pointer
     * @since 3.0.0
     */
    function sequencer_util_get_obj_mutex(sequencer: GObject.Object): GLib.RecMutex;

    /**
     * Allocate {@link AgsAudio.SF2SynthUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.SF2SynthUtil}-struct
     * @since 3.9.6
     */
    function sf2_synth_util_alloc(): SF2SynthUtil;

    /**
     * Find sample near MIDI key.
     * @param ipatch the {@link AgsAudio.Ipatch}
     * @param bank the bank
     * @param program the program
     * @param midi_key the MIDI key
     * @returns the {@link AgsAudio.IpatchSample}
     * @since 3.4.0
     */
    function sf2_synth_util_midi_locale_find_sample_near_midi_key(ipatch: Ipatch, bank: number, program: number, midi_key: number): [IpatchSample, string, string, string];

    /**
     * Allocate {@link AgsAudio.SFZSynthUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.SFZSynthUtil}-struct
     * @since 3.9.6
     */
    function sfz_synth_util_alloc(): SFZSynthUtil;

    /**
     * Adjust delay and attack of `soundcard`.
     * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
     * @since 3.3.0
     */
    function soundcard_util_adjust_delay_and_attack(soundcard: GObject.Object): void;

    /**
     * @param soundcard 
     * @param note_256th_attack_lower 
     * @param note_256th_attack_upper 
     */
    function soundcard_util_calc_next_note_256th_attack(soundcard: GObject.Object, note_256th_attack_lower: number, note_256th_attack_upper: number): void;

    /**
     * Calc next note 256th offset of `soundcard`.
     * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
     * @since 6.9.0
     */
    function soundcard_util_calc_next_note_256th_offset(soundcard: GObject.Object): [number, number];

    /**
     * Offset of `soundcard` to system time.
     * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
     * @returns the system time
     * @since 5.0.0
     */
    function soundcard_util_calc_system_time(soundcard: GObject.Object): number;

    /**
     * Offset of `soundcard` to system time.
     * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
     * @returns the system time
     * @since 5.0.0
     */
    function soundcard_util_calc_time_samples(soundcard: GObject.Object): number;

    /**
     * Offset of `soundcard` to system time.
     * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
     * @returns the system time
     * @since 5.0.0
     */
    function soundcard_util_calc_time_samples_absolute(soundcard: GObject.Object): number;

    /**
     * Get object mutex of `soundcard`.
     * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
     * @returns GRecMutex pointer
     * @since 3.0.0
     */
    function soundcard_util_get_obj_mutex(soundcard: GObject.Object): GLib.RecMutex;

    /**
     * Allocs an audio buffer.
     * @param buffer_size the buffer size
     * @param format the format
     * @returns the audio data array
     * @since 3.0.0
     */
    function stream_alloc(buffer_size: number, format: Ags.SoundcardFormat): (any | null);

    /**
     * Frees an audio buffer.
     * @param buffer the buffer
     * @since 3.0.0
     */
    function stream_free(buffer: (any | null)): void;

    /**
     * Allocs an audio buffer.
     * @param buffer_size the buffer size
     * @param format the format
     * @returns the audio data array
     * @since 3.3.0
     */
    function stream_slice_alloc(buffer_size: number, format: Ags.SoundcardFormat): (any | null);

    /**
     * Frees an audio buffer.
     * @param buffer_size the buffer size
     * @param format the format
     * @param buffer the buffer
     * @since 3.3.0
     */
    function stream_slice_free(buffer_size: number, format: Ags.SoundcardFormat, buffer: (any | null)): void;

    /**
     * Allocate {@link AgsAudio.SynthUtil}-struct
     * @returns a new {@link AgsAudio.SynthUtil}-struct
     * @since 3.9.3
     */
    function synth_util_alloc(): SynthUtil;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param format the format
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count(buffer: (any | null), format: number, buffer_size: number): number;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count_complex(buffer: Ags.Complex, buffer_size: number): number;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count_double(buffer: number, buffer_size: number): number;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count_float(buffer: number, buffer_size: number): number;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count_s16(buffer: number, buffer_size: number): number;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count_s24(buffer: number, buffer_size: number): number;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count_s32(buffer: number, buffer_size: number): number;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count_s64(buffer: (bigint | number), buffer_size: number): number;

    /**
     * Get zero-cross count.
     * @param buffer the buffer containing audio data
     * @param buffer_size the buffer size
     * @returns the count of zero-crossings
     * @since 3.0.0
     */
    function synth_util_get_xcross_count_s8(buffer: number, buffer_size: number): number;

    /**
     * Allocate {@link AgsAudio.TimeStretchUtil}-struct.
     * @returns the newly allocated {@link AgsAudio.TimeStretchUtil}-struct
     * @since 3.13.0
     */
    function time_stretch_util_alloc(): TimeStretchUtil;

    /**
     * Allocate {@link AgsAudio.TremoloUtil}-struct
     * @returns a new {@link AgsAudio.TremoloUtil}-struct
     * @since 5.2.0
     */
    function tremolo_util_alloc(): TremoloUtil;

    /**
     * Allocate {@link AgsAudio.VolumeUtil}-struct
     * @returns a new {@link AgsAudio.VolumeUtil}-struct
     * @since 3.9.2
     */
    function volume_util_alloc(): VolumeUtil;

    /**
     * Allocate {@link AgsAudio.WahWahUtil}-struct
     * @returns a new {@link AgsAudio.WahWahUtil}-struct
     * @since 3.9.2
     */
    function wah_wah_util_alloc(): WahWahUtil;

    /**
     * @gir-type Flags
     */
    export namespace AccelerationFlags {
        export const $gtype: GObject.GType<AccelerationFlags>;
    }

    /**
     * @gir-type Flags
     */
    enum AccelerationFlags {
        /**
         * if start is default start point
         */
        ACCELERATION_DEFAULT_START,
        /**
         * if end is default end point
         */
        ACCELERATION_DEFAULT_END,
        /**
         * interpret x and y as GUI format
         */
        ACCELERATION_GUI,
        /**
         * interpret x and y as runtime formant
         */
        ACCELERATION_RUNTIME,
        /**
         * interpret x and y as human readable
         */
        ACCELERATION_HUMAN_READABLE,
        /**
         * if default length applies
         */
        ACCELERATION_DEFAULT_LENGTH,
        /**
         * if the acceleration is selected
         */
        ACCELERATION_IS_SELECTED,
    }


    /**
     * @gir-type Flags
     */
    export namespace AlsaDevinFlags {
        export const $gtype: GObject.GType<AlsaDevinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AlsaDevin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AlsaDevinFlags {
        /**
         * the soundcard was initialized
         */
        ALSA_DEVIN_INITIALIZED,
        /**
         * capture starting
         */
        ALSA_DEVIN_START_RECORD,
        /**
         * do capture
         */
        ALSA_DEVIN_RECORD,
        /**
         * stop capture
         */
        ALSA_DEVIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        ALSA_DEVIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        ALSA_DEVIN_ATTACK_FIRST,
    }


    /**
     * @gir-type Flags
     */
    export namespace AlsaDevoutFlags {
        export const $gtype: GObject.GType<AlsaDevoutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AlsaDevout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AlsaDevoutFlags {
        /**
         * the soundcard was initialized
         */
        ALSA_DEVOUT_INITIALIZED,
        /**
         * playback starting
         */
        ALSA_DEVOUT_START_PLAY,
        /**
         * devout is running
         */
        ALSA_DEVOUT_PLAY,
        /**
         * stop playback
         */
        ALSA_DEVOUT_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        ALSA_DEVOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        ALSA_DEVOUT_ATTACK_FIRST,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AlsaMidiin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AlsaMidiinFlags {
        /**
         * the alsa_midiin was added to registry, see {@link Ags.Connectable.SignalSignatures.add_to_registry | Ags.Connectable::add_to_registry}()
         */
        ADDED_TO_REGISTRY,
        /**
         * indicates the alsa_midiin was connected by calling {@link Ags.Connectable.SignalSignatures.connect | Ags.Connectable::connect}()
         */
        CONNECTED,
        /**
         * recording is initialized
         */
        INITIALIZED,
        /**
         * is recording
         */
        RECORD,
        /**
         * stop recording
         */
        SHUTDOWN,
        /**
         * just started recording
         */
        START_RECORD,
        /**
         * do non-blocking calls
         */
        NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between sequencer and poll.
     * @gir-type Flags
     */
    enum AlsaMidiinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial poll
         */
        INITIAL_POLL,
        /**
         * sync wait, sequencer conditional lock
         */
        POLL_WAIT,
        /**
         * sync done, sequencer conditional lock
         */
        POLL_DONE,
        /**
         * sync wait, client conditional lock
         */
        POLL_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        POLL_FINISH_DONE,
        /**
         * switch buffer
         */
        POLL_SWITCH_BUFFER,
    }


    /**
     * @gir-type Flags
     */
    export namespace AudioFlags {
        export const $gtype: GObject.GType<AudioFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Audio} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AudioFlags {
        /**
         * no output provided
         */
        AUDIO_NO_OUTPUT,
        /**
         * no input provided
         */
        AUDIO_NO_INPUT,
        /**
         * input/output is mapped synchronously
         */
        AUDIO_SYNC,
        /**
         * input/output is mapped asynchronously
         */
        AUDIO_ASYNC,
        /**
         * output has recycling
         */
        AUDIO_OUTPUT_HAS_RECYCLING,
        /**
         * output has synth
         */
        AUDIO_OUTPUT_HAS_SYNTH,
        /**
         * input has recycling
         */
        AUDIO_INPUT_HAS_RECYCLING,
        /**
         * input has synth
         */
        AUDIO_INPUT_HAS_SYNTH,
        /**
         * input takes file
         */
        AUDIO_INPUT_HAS_FILE,
        /**
         * dispatch can next active
         */
        AUDIO_CAN_NEXT_ACTIVE,
        /**
         * skip output as processing audio data
         */
        AUDIO_SKIP_OUTPUT,
        /**
         * skip input as processing audio data
         */
        AUDIO_SKIP_INPUT,
        /**
         * don't apply any data
         */
        AUDIO_BYPASS,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AudioLoop} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AudioLoopFlags {
        /**
         * play channel
         */
        PLAY_CHANNEL,
        /**
         * playing channnel
         */
        PLAYING_CHANNEL,
        /**
         * play channe terminating
         */
        PLAY_CHANNEL_TERMINATING,
        /**
         * play audio
         */
        PLAY_AUDIO,
        /**
         * playing audio
         */
        PLAYING_AUDIO,
        /**
         * play audio terminating
         */
        PLAY_AUDIO_TERMINATING,
    }


    /**
     * @gir-type Flags
     */
    export namespace AudioSignalFlags {
        export const $gtype: GObject.GType<AudioSignalFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AudioSignal} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AudioSignalFlags {
        /**
         * the audio signal acts as template
         */
        AUDIO_SIGNAL_DEFAULT_TEMPLATE,
        /**
         * the audio signal acts as a realtime template
         */
        AUDIO_SIGNAL_RT_TEMPLATE,
        /**
         * the audio signal needs master
         */
        AUDIO_SIGNAL_MASTER,
        /**
         * the audio signal needs feed
         */
        AUDIO_SIGNAL_FEED,
        /**
         * the audio signal is recycled
         */
        AUDIO_SIGNAL_RECYCLED,
        /**
         * the audio signal needs stream
         */
        AUDIO_SIGNAL_STREAM,
        /**
         * the audio signal allocates from slice
         */
        AUDIO_SIGNAL_SLICE_ALLOC,
    }


    /**
     * @gir-type Flags
     */
    export namespace AudioSignalStreamMode {
        export const $gtype: GObject.GType<AudioSignalStreamMode>;
    }

    /**
     * Enum values to set the mode of {@link AgsAudio.AudioSignal} stream behaviour by
     * enable/disable setting mode.
     * @gir-type Flags
     */
    enum AudioSignalStreamMode {
        AUDIO_SIGNAL_STREAM_CONTINUES_FEED,
        AUDIO_SIGNAL_STREAM_DUAL_BUFFERED,
    }


    /**
     * @gir-type Flags
     */
    enum AudioThreadNestedSyncFlags {
        DONE,
        WAIT,
        DONE_SYNC,
        WAIT_SYNC,
    }


    /**
     * @gir-type Flags
     */
    export namespace AudioUnitClientFlags {
        export const $gtype: GObject.GType<AudioUnitClientFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AudioUnitClient} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AudioUnitClientFlags {
        /**
         * the client was activated
         */
        AUDIO_UNIT_CLIENT_ACTIVATED,
        /**
         * the client is ready
         */
        AUDIO_UNIT_CLIENT_READY,
    }


    /**
     * @gir-type Flags
     */
    export namespace AudioUnitDevinFlags {
        export const $gtype: GObject.GType<AudioUnitDevinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AudioUnitDevin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AudioUnitDevinFlags {
        /**
         * the soundcard was initialized
         */
        AUDIO_UNIT_DEVIN_INITIALIZED,
        /**
         * capture starting
         */
        AUDIO_UNIT_DEVIN_START_RECORD,
        /**
         * do capture
         */
        AUDIO_UNIT_DEVIN_RECORD,
        /**
         * stop capture
         */
        AUDIO_UNIT_DEVIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        AUDIO_UNIT_DEVIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        AUDIO_UNIT_DEVIN_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum AudioUnitDevinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace AudioUnitDevoutFlags {
        export const $gtype: GObject.GType<AudioUnitDevoutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AudioUnitDevout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AudioUnitDevoutFlags {
        /**
         * the soundcard was initialized
         */
        AUDIO_UNIT_DEVOUT_INITIALIZED,
        /**
         * playback starting
         */
        AUDIO_UNIT_DEVOUT_START_PLAY,
        /**
         * do playback
         */
        AUDIO_UNIT_DEVOUT_PLAY,
        /**
         * stop playback
         */
        AUDIO_UNIT_DEVOUT_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        AUDIO_UNIT_DEVOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        AUDIO_UNIT_DEVOUT_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum AudioUnitDevoutSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace AudioUnitPortFlags {
        export const $gtype: GObject.GType<AudioUnitPortFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.AudioUnitPort} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AudioUnitPortFlags {
        /**
         * the port was registered
         */
        AUDIO_UNIT_PORT_REGISTERED,
        /**
         * the port provides audio data
         */
        AUDIO_UNIT_PORT_IS_AUDIO,
        /**
         * the port provides midi data
         */
        AUDIO_UNIT_PORT_IS_MIDI,
        /**
         * the port does output
         */
        AUDIO_UNIT_PORT_IS_OUTPUT,
        /**
         * the port does input
         */
        AUDIO_UNIT_PORT_IS_INPUT,
    }


    /**
     * @gir-type Flags
     */
    export namespace AutomationFlags {
        export const $gtype: GObject.GType<AutomationFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Automation} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum AutomationFlags {
        /**
         * ignore any automation data
         */
        AUTOMATION_BYPASS,
    }


    /**
     * @gir-type Flags
     */
    export namespace BasePluginFlags {
        export const $gtype: GObject.GType<BasePluginFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.BasePlugin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum BasePluginFlags {
        /**
         * plugin is instrument
         */
        BASE_PLUGIN_IS_INSTRUMENT,
    }


    /**
     * @gir-type Flags
     */
    export namespace BufferFlags {
        export const $gtype: GObject.GType<BufferFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Buffer} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum BufferFlags {
        /**
         * is selected
         */
        BUFFER_IS_SELECTED,
    }


    /**
     * @gir-type Flags
     */
    export namespace ChannelFlags {
        export const $gtype: GObject.GType<ChannelFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Channel} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum ChannelFlags {
        /**
         * don't apply any data
         */
        CHANNEL_BYPASS,
    }


    /**
     * @gir-type Flags
     */
    enum ChannelThreadNestedSyncFlags {
        DONE,
        WAIT,
        DONE_SYNC,
        WAIT_SYNC,
    }


    /**
     * @gir-type Flags
     */
    export namespace CoreAudioClientFlags {
        export const $gtype: GObject.GType<CoreAudioClientFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.CoreAudioClient} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum CoreAudioClientFlags {
        /**
         * the client was activated
         */
        CORE_AUDIO_CLIENT_ACTIVATED,
        /**
         * the client is ready
         */
        CORE_AUDIO_CLIENT_READY,
    }


    /**
     * @gir-type Flags
     */
    export namespace CoreAudioDevinFlags {
        export const $gtype: GObject.GType<CoreAudioDevinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.CoreAudioDevin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum CoreAudioDevinFlags {
        /**
         * the soundcard was initialized
         */
        CORE_AUDIO_DEVIN_INITIALIZED,
        /**
         * capture starting
         */
        CORE_AUDIO_DEVIN_START_RECORD,
        /**
         * do capture
         */
        CORE_AUDIO_DEVIN_RECORD,
        /**
         * stop capture
         */
        CORE_AUDIO_DEVIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        CORE_AUDIO_DEVIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        CORE_AUDIO_DEVIN_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum CoreAudioDevinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace CoreAudioDevoutFlags {
        export const $gtype: GObject.GType<CoreAudioDevoutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.CoreAudioDevout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum CoreAudioDevoutFlags {
        /**
         * the soundcard was initialized
         */
        CORE_AUDIO_DEVOUT_INITIALIZED,
        /**
         * playback starting
         */
        CORE_AUDIO_DEVOUT_START_PLAY,
        /**
         * do playback
         */
        CORE_AUDIO_DEVOUT_PLAY,
        /**
         * stop playback
         */
        CORE_AUDIO_DEVOUT_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        CORE_AUDIO_DEVOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        CORE_AUDIO_DEVOUT_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum CoreAudioDevoutSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace CoreAudioMidiinFlags {
        export const $gtype: GObject.GType<CoreAudioMidiinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.CoreAudioMidiin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum CoreAudioMidiinFlags {
        /**
         * recording is initialized
         */
        CORE_AUDIO_MIDIIN_INITIALIZED,
        /**
         * just started recording
         */
        CORE_AUDIO_MIDIIN_START_RECORD,
        /**
         * is recording
         */
        CORE_AUDIO_MIDIIN_RECORD,
        /**
         * stop recording
         */
        CORE_AUDIO_MIDIIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        CORE_AUDIO_MIDIIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        CORE_AUDIO_MIDIIN_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum CoreAudioMidiinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
        /**
         * do synchronize
         */
        DO_SYNC,
    }


    /**
     * @gir-type Flags
     */
    export namespace CoreAudioPortFlags {
        export const $gtype: GObject.GType<CoreAudioPortFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.CoreAudioPort} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum CoreAudioPortFlags {
        /**
         * the port was registered
         */
        CORE_AUDIO_PORT_REGISTERED,
        /**
         * the port provides audio data
         */
        CORE_AUDIO_PORT_IS_AUDIO,
        /**
         * the port provides midi data
         */
        CORE_AUDIO_PORT_IS_MIDI,
        /**
         * the port does output
         */
        CORE_AUDIO_PORT_IS_OUTPUT,
        /**
         * the port does input
         */
        CORE_AUDIO_PORT_IS_INPUT,
    }


    /**
     * Enum values to control output format of {@link AgsAudio.ExportOutput}.
     * @gir-type Flags
     */
    enum ExportOutputFormat {
        /**
         * use WAV format
         */
        WAV,
        /**
         * use OGG format
         */
        OGG,
        /**
         * use FLAC format
         */
        FLAC,
        /**
         * use AIFF format
         */
        AIFF,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.ExportThread} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum ExportThreadFlags {
        /**
         * do live export
         */
        LIVE_PERFORMANCE,
        /**
         * is exporting
         */
        IS_EXPORTING,
    }


    /**
     * @gir-type Flags
     */
    export namespace FifooutFlags {
        export const $gtype: GObject.GType<FifooutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Fifoout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum FifooutFlags {
        /**
         * the soundcard was initialized
         */
        FIFOOUT_INITIALIZED,
        /**
         * playback starting
         */
        FIFOOUT_START_PLAY,
        /**
         * use first attack, instead of second one
         */
        FIFOOUT_PLAY,
        /**
         * stop playback
         */
        FIFOOUT_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        FIFOOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        FIFOOUT_ATTACK_FIRST,
    }


    /**
     * @gir-type Flags
     */
    export namespace FluidIIRFilterType {
        export const $gtype: GObject.GType<FluidIIRFilterType>;
    }

    /**
     * Enum values to specify filter type of {@link AgsAudio.FluidIIRFilterUtil}.
     * @gir-type Flags
     */
    enum FluidIIRFilterType {
        /**
         * disabled
         */
        FLUID_IIR_DISABLED,
        /**
         * low-pass
         */
        FLUID_IIR_LOWPASS,
        /**
         * high-pass
         */
        FLUID_IIR_HIGHPASS,
        /**
         * last entry
         */
        FLUID_IIR_LAST,
    }


    /**
     * @gir-type Flags
     */
    export namespace FxFactoryCreateFlags {
        export const $gtype: GObject.GType<FxFactoryCreateFlags>;
    }

    /**
     * Enum values controlling instantiating the recall implementation.
     * @gir-type Flags
     */
    enum FxFactoryCreateFlags {
        /**
         * output related
         */
        FX_FACTORY_OUTPUT,
        /**
         * input related
         */
        FX_FACTORY_INPUT,
        /**
         * remap
         */
        FX_FACTORY_REMAP,
        /**
         * do add
         */
        FX_FACTORY_ADD,
        /**
         * bounded to play
         */
        FX_FACTORY_PLAY,
        /**
         * bounded to recall
         */
        FX_FACTORY_RECALL,
        /**
         * operates on bulk mode
         */
        FX_FACTORY_BULK,
        /**
         * is live version
         */
        FX_FACTORY_LIVE,
    }


    /**
     * Enum values to enable specific feature of {@link AgsAudio.FxNotationAudio}.
     * @gir-type Flags
     */
    enum FxNotationAudioFlags {
        /**
         * do play notation
         */
        PLAY,
        /**
         * do record notation
         */
        RECORD,
        /**
         * do feed note
         */
        FEED,
    }


    /**
     * Enum values to enable specific feature of {@link AgsAudio.FxPlaybackAudio}.
     * @gir-type Flags
     */
    enum FxPlaybackAudioFlags {
        /**
         * do play sound
         */
        PLAY,
        /**
         * do record sound
         */
        RECORD,
        /**
         * do feed audio signal
         */
        FEED,
        /**
         * do master audio signal
         */
        MASTER,
    }


    /**
     * @gir-type Flags
     */
    export namespace GstreamerClientFlags {
        export const $gtype: GObject.GType<GstreamerClientFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.GstreamerClient} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum GstreamerClientFlags {
        /**
         * the client was activated
         */
        GSTREAMER_CLIENT_ACTIVATED,
        /**
         * the client is ready
         */
        GSTREAMER_CLIENT_READY,
    }


    /**
     * @gir-type Flags
     */
    export namespace GstreamerDevinFlags {
        export const $gtype: GObject.GType<GstreamerDevinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.GstreamerDevin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum GstreamerDevinFlags {
        /**
         * the soundcard was initialized
         */
        GSTREAMER_DEVIN_INITIALIZED,
        /**
         * capture starting
         */
        GSTREAMER_DEVIN_START_RECORD,
        /**
         * do capture
         */
        GSTREAMER_DEVIN_RECORD,
        /**
         * stop capture
         */
        GSTREAMER_DEVIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        GSTREAMER_DEVIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        GSTREAMER_DEVIN_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum GstreamerDevinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace GstreamerDevoutFlags {
        export const $gtype: GObject.GType<GstreamerDevoutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.GstreamerDevout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum GstreamerDevoutFlags {
        /**
         * the soundcard was initialized
         */
        GSTREAMER_DEVOUT_INITIALIZED,
        /**
         * playback starting
         */
        GSTREAMER_DEVOUT_START_PLAY,
        /**
         * do playback
         */
        GSTREAMER_DEVOUT_PLAY,
        /**
         * stop playback
         */
        GSTREAMER_DEVOUT_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        GSTREAMER_DEVOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        GSTREAMER_DEVOUT_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum GstreamerDevoutSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace GstreamerPortFlags {
        export const $gtype: GObject.GType<GstreamerPortFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.GstreamerPort} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum GstreamerPortFlags {
        /**
         * the port was registered
         */
        GSTREAMER_PORT_REGISTERED,
        /**
         * the port provides audio data
         */
        GSTREAMER_PORT_IS_AUDIO,
        /**
         * the port provides midi data
         */
        GSTREAMER_PORT_IS_MIDI,
        /**
         * the port does output
         */
        GSTREAMER_PORT_IS_OUTPUT,
        /**
         * the port does input
         */
        GSTREAMER_PORT_IS_INPUT,
    }


    /**
     * @gir-type Flags
     */
    export namespace IpatchFlags {
        export const $gtype: GObject.GType<IpatchFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Fifoout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum IpatchFlags {
        /**
         * DLS2 format
         */
        IPATCH_DLS2,
        /**
         * Soundfont2 format
         */
        IPATCH_SF2,
        /**
         * Gigasampler format
         */
        IPATCH_GIG,
    }


    /**
     * @gir-type Flags
     */
    export namespace JackClientFlags {
        export const $gtype: GObject.GType<JackClientFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.JackClient} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum JackClientFlags {
        /**
         * the client was activated
         */
        JACK_CLIENT_ACTIVATED,
    }


    /**
     * @gir-type Flags
     */
    export namespace JackDevinFlags {
        export const $gtype: GObject.GType<JackDevinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.JackDevin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum JackDevinFlags {
        /**
         * the soundcard was initialized
         */
        JACK_DEVIN_INITIALIZED,
        /**
         * capture starting
         */
        JACK_DEVIN_START_RECORD,
        /**
         * do capture
         */
        JACK_DEVIN_RECORD,
        /**
         * stop capture
         */
        JACK_DEVIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        JACK_DEVIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        JACK_DEVIN_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum JackDevinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace JackDevoutFlags {
        export const $gtype: GObject.GType<JackDevoutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.JackDevout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum JackDevoutFlags {
        /**
         * the soundcard was initialized
         */
        JACK_DEVOUT_INITIALIZED,
        /**
         * playback starting
         */
        JACK_DEVOUT_START_PLAY,
        /**
         * do playback
         */
        JACK_DEVOUT_PLAY,
        /**
         * stop playback
         */
        JACK_DEVOUT_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        JACK_DEVOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        JACK_DEVOUT_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum JackDevoutSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace JackMidiinFlags {
        export const $gtype: GObject.GType<JackMidiinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.JackMidiin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum JackMidiinFlags {
        /**
         * recording is initialized
         */
        JACK_MIDIIN_INITIALIZED,
        /**
         * just started recording
         */
        JACK_MIDIIN_START_RECORD,
        /**
         * is recording
         */
        JACK_MIDIIN_RECORD,
        /**
         * stop recording
         */
        JACK_MIDIIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        JACK_MIDIIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        JACK_MIDIIN_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between sequencer and client.
     * @gir-type Flags
     */
    enum JackMidiinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, sequencer conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, sequencer conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace JackPortFlags {
        export const $gtype: GObject.GType<JackPortFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.JackPort} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum JackPortFlags {
        /**
         * the port was registered
         */
        JACK_PORT_REGISTERED,
        /**
         * the port provides audio data
         */
        JACK_PORT_IS_AUDIO,
        /**
         * the port provides midi data
         */
        JACK_PORT_IS_MIDI,
        /**
         * the port does output
         */
        JACK_PORT_IS_OUTPUT,
        /**
         * the port does input
         */
        JACK_PORT_IS_INPUT,
    }


    /**
     * @gir-type Flags
     */
    export namespace LadspaConversionFlags {
        export const $gtype: GObject.GType<LadspaConversionFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.LadspaConversion} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum LadspaConversionFlags {
        /**
         * samplerate
         */
        LADSPA_CONVERSION_SAMPLERATE,
        /**
         * bounded below
         */
        LADSPA_CONVERSION_BOUNDED_BELOW,
        /**
         * bounded above
         */
        LADSPA_CONVERSION_BOUNDED_ABOVE,
        /**
         * logarithmic
         */
        LADSPA_CONVERSION_LOGARITHMIC,
    }


    /**
     * @gir-type Flags
     */
    export namespace Lv2ConversionFlags {
        export const $gtype: GObject.GType<Lv2ConversionFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Lv2Conversion} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum Lv2ConversionFlags {
        /**
         * logarithmic
         */
        LV2_CONVERSION_LOGARITHMIC,
    }


    /**
     * @gir-type Flags
     */
    export namespace Lv2PluginFlags {
        export const $gtype: GObject.GType<Lv2PluginFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Lv2Plugin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum Lv2PluginFlags {
        /**
         * plugin is synthesizer
         */
        LV2_PLUGIN_IS_SYNTHESIZER,
        /**
         * plugin needs worker
         */
        LV2_PLUGIN_NEEDS_WORKER,
        /**
         * plugin has program interface
         */
        LV2_PLUGIN_HAS_PROGRAM_INTERFACE,
    }


    /**
     * @gir-type Flags
     */
    export namespace Lv2TurtleScannerStatusFlags {
        export const $gtype: GObject.GType<Lv2TurtleScannerStatusFlags>;
    }

    /**
     * Enum values to indicate internal state of {@link AgsAudio.Lv2TurtleScanner} by
     * enable/disable as status flags.
     * @gir-type Flags
     */
    enum Lv2TurtleScannerStatusFlags {
        LV2_TURTLE_SCANNER_A,
        LV2_TURTLE_SCANNER_SEEALSO,
        LV2_TURTLE_SCANNER_BINARY,
        /**
         * scanner name
         */
        LV2_TURTLE_SCANNER_NAME,
    }


    /**
     * @gir-type Flags
     */
    export namespace Lv2WorkerFlags {
        export const $gtype: GObject.GType<Lv2WorkerFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Lv2Worker} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum Lv2WorkerFlags {
        /**
         * the worker is running
         */
        LV2_WORKER_RUN,
    }


    /**
     * @gir-type Flags
     */
    export namespace Lv2uiPluginFlags {
        export const $gtype: GObject.GType<Lv2uiPluginFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Lv2uiPlugin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum Lv2uiPluginFlags {
        /**
         * is synthesizer
         */
        LV2UI_PLUGIN_IS_SYNTHESIZER,
        /**
         * has Gtk+-2.0 UI
         */
        LV2UI_PLUGIN_GTK2,
        /**
         * has Gtk3 UI
         */
        LV2UI_PLUGIN_GTK3,
        /**
         * has Qt4 UI
         */
        LV2UI_PLUGIN_QT4,
        /**
         * has Qt5 UI
         */
        LV2UI_PLUGIN_QT5,
    }


    /**
     * @gir-type Flags
     */
    export namespace MarkerFlags {
        export const $gtype: GObject.GType<MarkerFlags>;
    }

    /**
     * @gir-type Flags
     */
    enum MarkerFlags {
        /**
         * if start is default start point
         */
        MARKER_DEFAULT_START,
        /**
         * if end is default end point
         */
        MARKER_DEFAULT_END,
        /**
         * interpret x and y as GUI format
         */
        MARKER_GUI,
        /**
         * interpret x and y as runtime formant
         */
        MARKER_RUNTIME,
        /**
         * interpret x and y as human readable
         */
        MARKER_HUMAN_READABLE,
        /**
         * if default length applies
         */
        MARKER_DEFAULT_LENGTH,
        /**
         * if the marker is selected
         */
        MARKER_IS_SELECTED,
    }


    /**
     * @gir-type Flags
     */
    enum MidiBuilderFlags {
        EOF,
        EOT,
    }


    /**
     * @gir-type Flags
     */
    enum MidiCICategorySupport {
        PROTOCOL_NEGOTIATION_SUPPORTED,
        PROFILE_CONFIGURATION_SUPPORTED,
        PROPERTY_EXCHANGE_SUPPORTED,
    }


    /**
     * @gir-type Flags
     */
    enum MidiChunkFlags {
        HEADER,
        TRACK,
        UNKNOWN,
    }


    /**
     * @gir-type Flags
     */
    enum MidiFileFlags {
        EOF,
        SMTPE,
        DROP_FRAME,
    }


    /**
     * @gir-type Flags
     */
    enum MidiParserFlags {
        EOF,
        EOT,
    }


    /**
     * @gir-type Flags
     */
    enum MidiUmpEndpointDiscoveryFilterBitmap {
        ENDPOINT_INFO,
        DEVICE_IDENTITY,
        ENDPOINT_NAME,
        PRODUCT_INSTANCE_ID,
        STREAM_CONFIGURATION,
    }


    /**
     * @gir-type Flags
     */
    enum MidiUmpFunctionBlockDiscoveryFilterBitmap {
        INFO_NOTIFICATION,
        NAME_NOTIFICATION,
    }


    /**
     * @gir-type Flags
     */
    export namespace NotationFlags {
        export const $gtype: GObject.GType<NotationFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Notation} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum NotationFlags {
        /**
         * pattern mode
         */
        NOTATION_PATTERN_MODE,
        /**
         * ignore any notation data
         */
        NOTATION_BYPASS,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Note} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum NoteFlags {
        /**
         * gui format
         */
        GUI,
        /**
         * runtime format
         */
        RUNTIME,
        /**
         * human readable format
         */
        HUMAN_READABLE,
        /**
         * default length
         */
        DEFAULT_LENGTH,
        /**
         * is selected
         */
        IS_SELECTED,
        /**
         * feed note
         */
        FEED,
        /**
         * do envelope
         */
        ENVELOPE,
    }


    /**
     * @gir-type Flags
     */
    export namespace OscClientFlags {
        export const $gtype: GObject.GType<OscClientFlags>;
    }

    /**
     * Enum values to configure OSC client.
     * @gir-type Flags
     */
    enum OscClientFlags {
        /**
         * use IPv4 socket
         */
        OSC_CLIENT_INET4,
        /**
         * use IPv6 socket
         */
        OSC_CLIENT_INET6,
        /**
         * use UDP transport protocol
         */
        OSC_CLIENT_UDP,
        /**
         * used TCP transport protocol
         */
        OSC_CLIENT_TCP,
    }


    /**
     * Enum values to configure OSC connection.
     * @gir-type Flags
     */
    enum OscConnectionFlags {
        /**
         * is active
         */
        ACTIVE,
        /**
         * IPv4 connection
         */
        INET4,
        /**
         * IPv6 connection
         */
        INET6,
    }


    /**
     * @gir-type Flags
     */
    export namespace OscFrontControllerFlags {
        export const $gtype: GObject.GType<OscFrontControllerFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.OscFrontController} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum OscFrontControllerFlags {
        /**
         * delegate started
         */
        OSC_FRONT_CONTROLLER_DELEGATE_STARTED,
        /**
         * delegate running
         */
        OSC_FRONT_CONTROLLER_DELEGATE_RUNNING,
        /**
         * delegate terminating
         */
        OSC_FRONT_CONTROLLER_DELEGATE_TERMINATING,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.OscMeterController} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum OscMeterControllerFlags {
        /**
         * monitor started
         */
        STARTED,
        /**
         * monitor running
         */
        RUNNING,
        /**
         * monitor terminating
         */
        TERMINATING,
    }


    /**
     * Enum values to configure OSC response.
     * @gir-type Flags
     */
    enum OscResponseFlags {
        /**
         * is empty
         */
        EMPTY,
        /**
         * is OK
         */
        OK,
        /**
         * has error
         */
        ERROR,
    }


    /**
     * @gir-type Flags
     */
    export namespace OscServerFlags {
        export const $gtype: GObject.GType<OscServerFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.OscServer} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum OscServerFlags {
        /**
         * run on UNIX socket
         */
        OSC_SERVER_STARTED,
        /**
         * the server is up and running
         */
        OSC_SERVER_RUNNING,
        /**
         * the server is closing connections and terminating
         */
        OSC_SERVER_TERMINATING,
        /**
         * use IPv4
         */
        OSC_SERVER_INET4,
        /**
         * use IPv6
         */
        OSC_SERVER_INET6,
        /**
         * run using UDP transport
         */
        OSC_SERVER_UDP,
        /**
         * run using TCP transport
         */
        OSC_SERVER_TCP,
        /**
         * listen on any address
         */
        OSC_SERVER_ANY_ADDRESS,
        /**
         * start the server
         */
        OSC_SERVER_AUTO_START,
    }


    /**
     * @gir-type Flags
     */
    export namespace OscXmlrpcControllerFlags {
        export const $gtype: GObject.GType<OscXmlrpcControllerFlags>;
    }

    /**
     * AGS_OSC_XMLRPC_CONTROLLER_DELEGATE_STARTED:
     * AGS_OSC_XMLRPC_CONTROLLER_DELEGATE_RUNNING:
     * AGS_OSC_XMLRPC_CONTROLLER_DELEGATE_TERMINATING:
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.OscXmlrpcController} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum OscXmlrpcControllerFlags {
        OSC_XMLRPC_CONTROLLER_DELEGATE_STARTED,
        OSC_XMLRPC_CONTROLLER_DELEGATE_RUNNING,
        OSC_XMLRPC_CONTROLLER_DELEGATE_TERMINATING,
    }


    /**
     * @gir-type Flags
     */
    export namespace OssDevinFlags {
        export const $gtype: GObject.GType<OssDevinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.OssDevin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum OssDevinFlags {
        /**
         * the soundcard was initialized
         */
        OSS_DEVIN_INITIALIZED,
        /**
         * capture starting
         */
        OSS_DEVIN_START_RECORD,
        /**
         * do capture
         */
        OSS_DEVIN_RECORD,
        /**
         * stop capture
         */
        OSS_DEVIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        OSS_DEVIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        OSS_DEVIN_ATTACK_FIRST,
    }


    /**
     * @gir-type Flags
     */
    export namespace OssDevoutFlags {
        export const $gtype: GObject.GType<OssDevoutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.OssDevout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum OssDevoutFlags {
        /**
         * the soundcard was initialized
         */
        OSS_DEVOUT_INITIALIZED,
        /**
         * playback starting
         */
        OSS_DEVOUT_START_PLAY,
        /**
         * devout is running
         */
        OSS_DEVOUT_PLAY,
        /**
         * stop playback
         */
        OSS_DEVOUT_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        OSS_DEVOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        OSS_DEVOUT_ATTACK_FIRST,
    }


    /**
     * @gir-type Flags
     */
    export namespace OssMidiinFlags {
        export const $gtype: GObject.GType<OssMidiinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.OssMidiin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum OssMidiinFlags {
        /**
         * recording is initialized
         */
        OSS_MIDIIN_INITIALIZED,
        /**
         * just started recording
         */
        OSS_MIDIIN_START_RECORD,
        /**
         * is recording
         */
        OSS_MIDIIN_RECORD,
        /**
         * stop recording
         */
        OSS_MIDIIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        OSS_MIDIIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        OSS_MIDIIN_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between sequencer and poll.
     * @gir-type Flags
     */
    enum OssMidiinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial poll
         */
        INITIAL_POLL,
        /**
         * sync wait, sequencer conditional lock
         */
        POLL_WAIT,
        /**
         * sync done, sequencer conditional lock
         */
        POLL_DONE,
        /**
         * sync wait, client conditional lock
         */
        POLL_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        POLL_FINISH_DONE,
        /**
         * switch buffer
         */
        POLL_SWITCH_BUFFER,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.PlaybackDomain} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum PlaybackDomainFlags {
        /**
         * single threaded
         */
        SINGLE_THREADED,
        /**
         * super threaded audio
         */
        SUPER_THREADED_AUDIO,
    }


    /**
     * @gir-type Flags
     */
    export namespace PlaybackFlags {
        export const $gtype: GObject.GType<PlaybackFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Playback} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum PlaybackFlags {
        /**
         * single threaded
         */
        PLAYBACK_SINGLE_THREADED,
        /**
         * super threaded channel
         */
        PLAYBACK_SUPER_THREADED_CHANNEL,
    }


    /**
     * @gir-type Flags
     */
    export namespace PluginPortFlags {
        export const $gtype: GObject.GType<PluginPortFlags>;
    }

    /**
     * Common port attributes.
     * @gir-type Flags
     */
    enum PluginPortFlags {
        /**
         * atom port
         */
        PLUGIN_PORT_ATOM,
        /**
         * audio port
         */
        PLUGIN_PORT_AUDIO,
        /**
         * control port
         */
        PLUGIN_PORT_CONTROL,
        /**
         * MIDI port
         */
        PLUGIN_PORT_MIDI,
        /**
         * event port
         */
        PLUGIN_PORT_EVENT,
        /**
         * is output
         */
        PLUGIN_PORT_OUTPUT,
        /**
         * is input
         */
        PLUGIN_PORT_INPUT,
        /**
         * toggle control
         */
        PLUGIN_PORT_TOGGLED,
        /**
         * enumerated
         */
        PLUGIN_PORT_ENUMERATION,
        /**
         * logarithmic
         */
        PLUGIN_PORT_LOGARITHMIC,
        /**
         * integer
         */
        PLUGIN_PORT_INTEGER,
        /**
         * samplerate
         */
        PLUGIN_PORT_SAMPLERATE,
        /**
         * bounded below
         */
        PLUGIN_PORT_BOUNDED_BELOW,
        /**
         * bounded above
         */
        PLUGIN_PORT_BOUNDED_ABOVE,
        /**
         * ui notification
         */
        PLUGIN_PORT_UI_NOTIFICATION,
        /**
         * hidden port
         */
        PLUGIN_PORT_HIDDEN,
    }


    /**
     * @gir-type Flags
     */
    export namespace PortFlags {
        export const $gtype: GObject.GType<PortFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Port} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum PortFlags {
        /**
         * convert always
         */
        PORT_CONVERT_ALWAYS,
        /**
         * use ladspa float
         */
        PORT_USE_LADSPA_FLOAT,
        /**
         * is output
         */
        PORT_IS_OUTPUT,
        /**
         * infinite range
         */
        PORT_INFINITE_RANGE,
    }


    /**
     * @gir-type Flags
     */
    export namespace ProgramFlags {
        export const $gtype: GObject.GType<ProgramFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Program} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum ProgramFlags {
        /**
         * ignore any program data
         */
        PROGRAM_BYPASS,
    }


    /**
     * @gir-type Flags
     */
    export namespace PulseClientFlags {
        export const $gtype: GObject.GType<PulseClientFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.PulseClient} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum PulseClientFlags {
        /**
         * the client was activated
         */
        PULSE_CLIENT_ACTIVATED,
        /**
         * the client is ready
         */
        PULSE_CLIENT_READY,
    }


    /**
     * @gir-type Flags
     */
    export namespace PulseDevinFlags {
        export const $gtype: GObject.GType<PulseDevinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.PulseDevin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum PulseDevinFlags {
        /**
         * the soundcard was initialized
         */
        PULSE_DEVIN_INITIALIZED,
        /**
         * capture starting
         */
        PULSE_DEVIN_START_RECORD,
        /**
         * do capture
         */
        PULSE_DEVIN_RECORD,
        /**
         * stop capture
         */
        PULSE_DEVIN_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        PULSE_DEVIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        PULSE_DEVIN_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum PulseDevinSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace PulseDevoutFlags {
        export const $gtype: GObject.GType<PulseDevoutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.PulseDevout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum PulseDevoutFlags {
        /**
         * the soundcard was initialized
         */
        PULSE_DEVOUT_INITIALIZED,
        /**
         * playback starting
         */
        PULSE_DEVOUT_START_PLAY,
        /**
         * do playback
         */
        PULSE_DEVOUT_PLAY,
        /**
         * stop playback
         */
        PULSE_DEVOUT_SHUTDOWN,
        /**
         * do non-blocking calls
         */
        PULSE_DEVOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        PULSE_DEVOUT_ATTACK_FIRST,
    }


    /**
     * Enum values to control the synchronization between soundcard and client.
     * @gir-type Flags
     */
    enum PulseDevoutSyncFlags {
        /**
         * do not sync
         */
        PASS_THROUGH,
        /**
         * initial callback
         */
        INITIAL_CALLBACK,
        /**
         * sync wait, soundcard conditional lock
         */
        CALLBACK_WAIT,
        /**
         * sync done, soundcard conditional lock
         */
        CALLBACK_DONE,
        /**
         * sync wait, client conditional lock
         */
        CALLBACK_FINISH_WAIT,
        /**
         * sync done, client conditional lock
         */
        CALLBACK_FINISH_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace PulsePortFlags {
        export const $gtype: GObject.GType<PulsePortFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.PulsePort} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum PulsePortFlags {
        /**
         * the port was registered
         */
        PULSE_PORT_REGISTERED,
        /**
         * the port provides audio data
         */
        PULSE_PORT_IS_AUDIO,
        /**
         * the port provides midi data
         */
        PULSE_PORT_IS_MIDI,
        /**
         * the port does output
         */
        PULSE_PORT_IS_OUTPUT,
        /**
         * the port does input
         */
        PULSE_PORT_IS_INPUT,
    }


    /**
     * Enum values to specify find criteria.
     * @gir-type Flags
     */
    enum RecallContainerFindFlags {
        /**
         * find type
         */
        TYPE,
        /**
         * find template
         */
        TEMPLATE,
        /**
         * find recall id
         */
        RECALL_ID,
    }


    /**
     * @gir-type Flags
     */
    export namespace RecallContainerFlags {
        export const $gtype: GObject.GType<RecallContainerFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.RecallContainer} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum RecallContainerFlags {
        /**
         * bound to play context
         */
        RECALL_CONTAINER_PLAY,
    }


    /**
     * @gir-type Flags
     */
    export namespace RecallFlags {
        export const $gtype: GObject.GType<RecallFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Recall} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum RecallFlags {
        /**
         * is template
         */
        RECALL_TEMPLATE,
        /**
         * is default template
         */
        RECALL_DEFAULT_TEMPLATE,
        /**
         * has output port
         */
        RECALL_HAS_OUTPUT_PORT,
        /**
         * don't apply effect processing
         */
        RECALL_BYPASS,
        /**
         * initial run, first attack to audio data
         */
        RECALL_INITIAL_RUN,
    }


    /**
     * @gir-type Flags
     */
    export namespace RecallNotifyDependencyMode {
        export const $gtype: GObject.GType<RecallNotifyDependencyMode>;
    }

    /**
     * Modes to notify of dependencies.
     * @gir-type Flags
     */
    enum RecallNotifyDependencyMode {
        /**
         * notify dependency as calling run
         */
        RECALL_NOTIFY_RUN,
        /**
         * notify dependency audio
         */
        RECALL_NOTIFY_AUDIO,
        /**
         * notifiy dependency audio run
         */
        RECALL_NOTIFY_AUDIO_RUN,
        /**
         * notifiy dependency channel
         */
        RECALL_NOTIFY_CHANNEL,
        /**
         * notifiy dependency channel run
         */
        RECALL_NOTIFY_CHANNEL_RUN,
        /**
         * notifiy dependency recall
         */
        RECALL_NOTIFY_RECALL,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.RecallRecycling} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum RecallRecyclingFlags {
        /**
         * not used
         */
        GARBAGE_COLLECTOR,
        /**
         * not used
         */
        MAP_CHILD_DESTINATION,
        /**
         * not used
         */
        MAP_CHILD_SOURCE,
        /**
         * not used
         */
        CREATE_DESTINATION_ON_MAP_SOURCE,
    }


    /**
     * @gir-type Flags
     */
    export namespace RecyclingContextFlags {
        export const $gtype: GObject.GType<RecyclingContextFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.RecyclingContext} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum RecyclingContextFlags {
        /**
         * chained to output
         */
        RECYCLING_CONTEXT_CHAINED_TO_OUTPUT,
        /**
         * chained to input
         */
        RECYCLING_CONTEXT_CHAINED_TO_INPUT,
    }


    /**
     * @gir-type Flags
     */
    export namespace RecyclingFlags {
        export const $gtype: GObject.GType<RecyclingFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Recycling} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum RecyclingFlags {
        /**
         * recycling is muted
         */
        RECYCLING_MUTED,
    }


    /**
     * Enum values to configure SF2 loader.
     * @gir-type Flags
     */
    enum SF2LoaderFlags {
        /**
         * has completed
         */
        HAS_COMPLETED,
        /**
         * run apply synth
         */
        RUN_APPLY_SYNTH,
    }


    /**
     * Enum values to configure Soundfont2 MIDI locale loader.
     * @gir-type Flags
     */
    enum SF2MidiLocaleLoaderFlags {
        /**
         * has completed
         */
        HAS_COMPLETED,
        /**
         * run apply midi locale
         */
        RUN_APPLY_MIDI_LOCALE,
    }


    /**
     * @gir-type Flags
     */
    enum SF2SynthGeneratorFlags {
        INSTRUMENT,
        MIDI_LOCALE,
    }


    /**
     * @gir-type Flags
     */
    enum SF2SynthUtilFlags {
        INSTRUMENT,
        MIDI_LOCALE,
    }


    /**
     * Enum values to configure SFZ instrument loader.
     * @gir-type Flags
     */
    enum SFZInstrumentLoaderFlags {
        /**
         * has completed
         */
        HAS_COMPLETED,
        /**
         * run apply instrument
         */
        RUN_APPLY_INSTRUMENT,
    }


    /**
     * Enum values to configure SFZ loader.
     * @gir-type Flags
     */
    enum SFZLoaderFlags {
        /**
         * do replace audio signal
         */
        DO_REPLACE,
        /**
         * has completed
         */
        HAS_COMPLETED,
        /**
         * run apply synth
         */
        RUN_APPLY_SYNTH,
    }


    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.SFZSample} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum SFZSampleFlags {
        /**
         * the sfz sample was added to registry, see {@link Ags.Connectable.SignalSignatures.add_to_registry | Ags.Connectable::add_to_registry}()
         */
        ADDED_TO_REGISTRY,
        /**
         * indicates the sfz sample was connected by calling {@link Ags.Connectable.SignalSignatures.connect | Ags.Connectable::connect}()
         */
        CONNECTED,
    }


    /**
     * @gir-type Flags
     */
    export namespace SndfileFlags {
        export const $gtype: GObject.GType<SndfileFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Sndfile} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum SndfileFlags {
        /**
         * virtual IO
         */
        SNDFILE_VIRTUAL,
        /**
         * fill cache
         */
        SNDFILE_FILL_CACHE,
    }


    /**
     * @gir-type Flags
     */
    export namespace SoundAbilityFlags {
        export const $gtype: GObject.GType<SoundAbilityFlags>;
    }

    /**
     * Enum values to specify particular ability. Mark an object to be
     * able to handle matching scope.
     * @gir-type Flags
     */
    enum SoundAbilityFlags {
        /**
         * playback ability
         */
        SOUND_ABILITY_PLAYBACK,
        /**
         * sequenecer ability
         */
        SOUND_ABILITY_SEQUENCER,
        /**
         * notation abilitiy
         */
        SOUND_ABILITY_NOTATION,
        /**
         * wave abilitiy
         */
        SOUND_ABILITY_WAVE,
        /**
         * MIDI ability
         */
        SOUND_ABILITY_MIDI,
    }


    /**
     * @gir-type Flags
     */
    export namespace SoundBehaviourFlags {
        export const $gtype: GObject.GType<SoundBehaviourFlags>;
    }

    /**
     * Enum values to describe behaviour.
     * @gir-type Flags
     */
    enum SoundBehaviourFlags {
        /**
         * pattern mode
         */
        SOUND_BEHAVIOUR_PATTERN_MODE,
        /**
         * bulk mode
         */
        SOUND_BEHAVIOUR_BULK_MODE,
        /**
         * reverse mapping
         */
        SOUND_BEHAVIOUR_REVERSE_MAPPING,
        /**
         * defaults to output
         */
        SOUND_BEHAVIOUR_DEFAULTS_TO_OUTPUT,
        /**
         * defaults to input
         */
        SOUND_BEHAVIOUR_DEFAULTS_TO_INPUT,
        /**
         * chained to output
         */
        SOUND_BEHAVIOUR_CHAINED_TO_OUTPUT,
        /**
         * chained to input
         */
        SOUND_BEHAVIOUR_CHAINED_TO_INPUT,
        /**
         * persistent
         */
        SOUND_BEHAVIOUR_PERSISTENT,
        /**
         * persistent playback
         */
        SOUND_BEHAVIOUR_PERSISTENT_PLAYBACK,
        /**
         * persistent notation
         */
        SOUND_BEHAVIOUR_PERSISTENT_NOTATION,
        /**
         * persistent sequencer
         */
        SOUND_BEHAVIOUR_PERSISTENT_SEQUENCER,
        /**
         * persistent wave
         */
        SOUND_BEHAVIOUR_PERSISTENT_WAVE,
        /**
         * persistent MIDI
         */
        SOUND_BEHAVIOUR_PERSISTENT_MIDI,
        /**
         * propagate done
         */
        SOUND_BEHAVIOUR_PROPAGATE_DONE,
    }


    /**
     * @gir-type Flags
     */
    export namespace SoundStagingFlags {
        export const $gtype: GObject.GType<SoundStagingFlags>;
    }

    /**
     * Enum values to stage the specified action.
     * @gir-type Flags
     */
    enum SoundStagingFlags {
        /**
         * check rt-data
         */
        SOUND_STAGING_CHECK_RT_DATA,
        /**
         * run init pre
         */
        SOUND_STAGING_RUN_INIT_PRE,
        /**
         * run init inter
         */
        SOUND_STAGING_RUN_INIT_INTER,
        /**
         * run init post
         */
        SOUND_STAGING_RUN_INIT_POST,
        /**
         * feed input queue
         */
        SOUND_STAGING_FEED_INPUT_QUEUE,
        /**
         * automate
         */
        SOUND_STAGING_AUTOMATE,
        /**
         * run pre
         */
        SOUND_STAGING_RUN_PRE,
        /**
         * run inter
         */
        SOUND_STAGING_RUN_INTER,
        /**
         * run post
         */
        SOUND_STAGING_RUN_POST,
        /**
         * do feedback
         */
        SOUND_STAGING_DO_FEEDBACK,
        /**
         * feed output queue
         */
        SOUND_STAGING_FEED_OUTPUT_QUEUE,
        /**
         * fini
         */
        SOUND_STAGING_FINI,
        /**
         * cancel
         */
        SOUND_STAGING_CANCEL,
        /**
         * done
         */
        SOUND_STAGING_DONE,
        /**
         * remove
         */
        SOUND_STAGING_REMOVE,
        /**
         * reset
         */
        SOUND_STAGING_RESET,
        /**
         * fx staging
         */
        SOUND_STAGING_FX,
    }


    /**
     * @gir-type Flags
     */
    export namespace SoundStateFlags {
        export const $gtype: GObject.GType<SoundStateFlags>;
    }

    /**
     * Enum values to indicate state.
     * @gir-type Flags
     */
    enum SoundStateFlags {
        SOUND_STATE_IS_WAITING,
        /**
         * is active
         */
        SOUND_STATE_IS_ACTIVE,
        /**
         * is processing
         */
        SOUND_STATE_IS_PROCESSING,
        /**
         * is terminating
         */
        SOUND_STATE_IS_TERMINATING,
    }


    /**
     * @gir-type Flags
     */
    export namespace TrackFlags {
        export const $gtype: GObject.GType<TrackFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Track} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum TrackFlags {
        /**
         * is selected
         */
        TRACK_IS_SELECTED,
    }


    /**
     * @gir-type Flags
     */
    export namespace WasapiDevinFlags {
        export const $gtype: GObject.GType<WasapiDevinFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.WasapiDevin} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum WasapiDevinFlags {
        /**
         * the soundcard was initialized
         */
        WASAPI_DEVIN_INITIALIZED,
        /**
         * capture starting
         */
        WASAPI_DEVIN_START_RECORD,
        /**
         * do capture
         */
        WASAPI_DEVIN_RECORD,
        /**
         * do non-blocking calls
         */
        WASAPI_DEVIN_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        WASAPI_DEVIN_ATTACK_FIRST,
        /**
         * share mode exclusive
         */
        WASAPI_DEVIN_SHARE_MODE_EXCLUSIVE,
    }


    /**
     * @gir-type Flags
     */
    export namespace WasapiDevoutFlags {
        export const $gtype: GObject.GType<WasapiDevoutFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.WasapiDevout} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum WasapiDevoutFlags {
        /**
         * the soundcard was initialized
         */
        WASAPI_DEVOUT_INITIALIZED,
        /**
         * playback starting
         */
        WASAPI_DEVOUT_START_PLAY,
        /**
         * do playback
         */
        WASAPI_DEVOUT_PLAY,
        /**
         * do non-blocking calls
         */
        WASAPI_DEVOUT_NONBLOCKING,
        /**
         * use first attack, instead of second one
         */
        WASAPI_DEVOUT_ATTACK_FIRST,
        /**
         * share mode exclusive
         */
        WASAPI_DEVOUT_SHARE_MODE_EXCLUSIVE,
    }


    /**
     * @gir-type Flags
     */
    export namespace WaveFlags {
        export const $gtype: GObject.GType<WaveFlags>;
    }

    /**
     * Enum values to control the behavior or indicate internal state of {@link AgsAudio.Wave} by
     * enable/disable as flags.
     * @gir-type Flags
     */
    enum WaveFlags {
        /**
         * ignore any wave data
         */
        WAVE_BYPASS,
    }


    /**
     * @gir-type Flags
     */
    enum WaveLoaderFlags {
        DO_REPLACE,
        HAS_COMPLETED,
    }


    namespace Acceleration {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::acceleration-name": (pspec: GObject.ParamSpec) => void;
            "notify::x": (pspec: GObject.ParamSpec) => void;
            "notify::y": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            acceleration_name: string;
            accelerationName: string;
            x: number;
            y: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Acceleration extends GObject.Object {
        static $gtype: GObject.GType<Acceleration>;

        // Properties
        /**
         * The acceleration's name.
         * @since 3.0.0
         * @default null
         */
        get acceleration_name(): string;
        set acceleration_name(val: string);

        /**
         * The acceleration's name.
         * @since 3.0.0
         * @default null
         */
        get accelerationName(): string;
        set accelerationName(val: string);

        /**
         * Acceleration offset x.
         * @since 3.0.0
         * @default 0
         */
        get x(): number;
        set x(val: number);

        /**
         * Acceleration value y.
         * @since 3.0.0
         * @default 0
         */
        get y(): number;
        set y(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Acceleration.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        // Constructors
        constructor(properties?: Partial<Acceleration.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Acceleration;

        // Signals
        /** @signal */
        connect<K extends keyof Acceleration.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Acceleration.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Acceleration.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Acceleration.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Acceleration.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Acceleration.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Sort accelerations.
         * @param a an {@link AgsAudio.Acceleration}
         * @param b an other {@link AgsAudio.Acceleration}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Duplicate a acceleration.
         * @returns the duplicated {@link AgsAudio.Acceleration}.
         */
        duplicate(): Acceleration;

        /**
         * Gets x.
         * @returns the x
         */
        get_x(): number;

        /**
         * Gets y.
         * @returns the y
         */
        get_y(): number;

        /**
         * Set `flags` on `acceleration`.
         * @param flags the flags
         */
        set_flags(flags: AccelerationFlags): void;

        /**
         * Sets x.
         * @param x the x
         */
        set_x(x: number): void;

        /**
         * Sets y.
         * @param y the y
         */
        set_y(y: number): void;

        /**
         * Test `flags` to be set on `acceleration`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AccelerationFlags): boolean;

        /**
         * Unset `flags` on `acceleration`.
         * @param flags the flags
         */
        unset_flags(flags: AccelerationFlags): void;
    }


    namespace AddAudio {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio: Audio;
        }
    }

    /**
     * @gir-type Class
     */
    class AddAudio extends Ags.Task implements Ags.Connectable {
        static $gtype: GObject.GType<AddAudio>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AddAudio.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<AddAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): AddAudio;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof AddAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AddAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AddAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AddAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace AddAudioSignal {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio-signal": (pspec: GObject.ParamSpec) => void;
            "notify::audio-signal-flags": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::recycling": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio_signal: AudioSignal;
            audioSignal: AudioSignal;
            audio_signal_flags: number;
            audioSignalFlags: number;
            recall_id: RecallID;
            recallId: RecallID;
            recycling: Recycling;
            soundcard: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class AddAudioSignal extends Ags.Task {
        static $gtype: GObject.GType<AddAudioSignal>;

        // Properties
        /**
         * The assigned {@link AgsAudio.AudioSignal}
         * @since 3.0.0
         */
        get audio_signal(): AudioSignal;
        set audio_signal(val: AudioSignal);

        /**
         * The assigned {@link AgsAudio.AudioSignal}
         * @since 3.0.0
         */
        get audioSignal(): AudioSignal;
        set audioSignal(val: AudioSignal);

        /**
         * The audio signal's flags.
         * @since 3.0.0
         * @default 0
         */
        get audio_signal_flags(): number;
        set audio_signal_flags(val: number);

        /**
         * The audio signal's flags.
         * @since 3.0.0
         * @default 0
         */
        get audioSignalFlags(): number;
        set audioSignalFlags(val: number);

        /**
         * The assigned {@link AgsAudio.RecallID}
         * @since 3.0.0
         */
        get recall_id(): RecallID;
        set recall_id(val: RecallID);

        /**
         * The assigned {@link AgsAudio.RecallID}
         * @since 3.0.0
         */
        get recallId(): RecallID;
        set recallId(val: RecallID);

        /**
         * The assigned {@link AgsAudio.Recycling}
         * @since 3.0.0
         */
        get recycling(): Recycling;
        set recycling(val: Recycling);

        /**
         * The assigned {@link Ags.Soundcard}
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AddAudioSignal.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<AddAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling, audio_signal: AudioSignal, soundcard: GObject.Object, recall_id: RecallID, audio_signal_flags: number): AddAudioSignal;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof AddAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AddAudioSignal.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof AddAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AddAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AddAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AddAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace AddNote {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::note": (pspec: GObject.ParamSpec) => void;
            "notify::use-selection-list": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            audio_channel: number;
            audioChannel: number;
            note: Note;
            use_selection_list: boolean;
            useSelectionList: boolean;
        }
    }

    /**
     * @gir-type Class
     */
    class AddNote extends Ags.Task {
        static $gtype: GObject.GType<AddNote>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The assigned audio channel
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The assigned audio channel
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The assigned {@link AgsAudio.Note}
         * @since 3.0.0
         */
        get note(): Note;
        set note(val: Note);

        /**
         * The notation's use-selection-list.
         * @since 3.0.0
         * @default false
         */
        get use_selection_list(): boolean;
        set use_selection_list(val: boolean);

        /**
         * The notation's use-selection-list.
         * @since 3.0.0
         * @default false
         */
        get useSelectionList(): boolean;
        set useSelectionList(val: boolean);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AddNote.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<AddNote.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, note: Note, audio_channel: number, use_selection_list: boolean): AddNote;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof AddNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AddNote.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof AddNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AddNote.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AddNote.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AddNote.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace AddSoundcard {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            soundcard: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class AddSoundcard extends Ags.Task {
        static $gtype: GObject.GType<AddSoundcard>;

        // Properties
        /**
         * The assigned {@link Ags.Soundcard}
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AddSoundcard.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<AddSoundcard.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](soundcard: GObject.Object): AddSoundcard;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof AddSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AddSoundcard.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof AddSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AddSoundcard.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AddSoundcard.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AddSoundcard.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace AlsaDevin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class AlsaDevin extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<AlsaDevin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.13.2
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.13.2
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.13.2
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.13.2
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.13.2
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The alsa soundcard indentifier
         * @since 3.13.2
         * @default hw:0,0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.13.2
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.13.2
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.13.2
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.13.2
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.13.2
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.13.2
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AlsaDevin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AlsaDevinFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        app_buffer_mode: AlsaDevinAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        available: boolean;

        backend_buffer_mode: AlsaDevinBackendBufferMode;

        backend_buffer: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        rc: number;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<AlsaDevin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AlsaDevin;

        // Signals
        /** @signal */
        connect_after<K extends keyof AlsaDevin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AlsaDevin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AlsaDevin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AlsaDevin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `alsa_devin`.
         * @param flags see {@link AgsAudio.AlsaDevinFlags}-enum
         */
        set_flags(flags: AlsaDevinFlags): void;

        /**
         * The buffer flag indicates the currently recorded buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `alsa_devin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AlsaDevinFlags): boolean;

        /**
         * Disable a feature of `alsa_devin`.
         * @param flags see {@link AgsAudio.AlsaDevinFlags}-enum
         */
        unset_flags(flags: AlsaDevinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace AlsaDevout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class AlsaDevout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<AlsaDevout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.13.2
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.13.2
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.13.2
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.13.2
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.13.2
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The alsa soundcard indentifier
         * @since 3.13.2
         * @default hw:0,0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.13.2
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.13.2
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.13.2
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.13.2
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.13.2
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.13.2
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AlsaDevout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AlsaDevoutFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        app_buffer_mode: AlsaDevoutAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        available: boolean;

        backend_buffer_mode: AlsaDevoutBackendBufferMode;

        backend_buffer: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        rc: number;

        io_channel: any[];

        tag: any[];

        poll_timeout: number;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<AlsaDevout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AlsaDevout;

        // Signals
        /** @signal */
        connect_after<K extends keyof AlsaDevout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AlsaDevout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AlsaDevout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AlsaDevout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `alsa_devout`.
         * @param flags see {@link AgsAudio.AlsaDevoutFlags}-enum
         */
        set_flags(flags: AlsaDevoutFlags): void;

        /**
         * Switch currently played buffer.
         */
        switch_buffer(): void;

        /**
         * Test `flags` to be set on `alsa_devout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AlsaDevoutFlags): boolean;

        /**
         * Disable a feature of `alsa_devout`.
         * @param flags see {@link AgsAudio.AlsaDevoutFlags}-enum
         */
        unset_flags(flags: AlsaDevoutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace AlsaMidiin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Sequencer.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            delay_factor: number;
            delayFactor: number;
            device: string;
        }
    }

    /**
     * @gir-type Class
     */
    class AlsaMidiin extends GObject.Object implements Ags.Connectable, Ags.Sequencer {
        static $gtype: GObject.GType<AlsaMidiin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.13.2
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.13.2
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.13.2
         * @read-only
         */
        get buffer(): any;

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The alsa sequencer indentifier
         * @since 3.13.2
         * @default hw:0,0
         */
        get device(): string;
        set device(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AlsaMidiin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AlsaMidiinFlags;

        sync_flags: AlsaMidiinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: AlsaMidiinAppBufferMode;

        app_buffer: string;

        app_buffer_size: number[];

        backend_buffer_mode: AlsaMidiinBackendBufferMode;

        backend_buffer: string;

        backend_buffer_size: number[];

        delay: number;

        latency: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        rc: number;

        // Constructors
        constructor(properties?: Partial<AlsaMidiin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AlsaMidiin;

        // Signals
        /** @signal */
        connect_after<K extends keyof AlsaMidiin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AlsaMidiin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AlsaMidiin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AlsaMidiin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Enable a feature of `alsa_midiin`.
         * @param flags see {@link AgsAudio.AlsaMidiinFlags}-enum
         */
        set_flags(flags: AlsaMidiinFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `alsa_midiin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AlsaMidiinFlags): boolean;

        /**
         * Disable a feature of `alsa_midiin`.
         * @param flags see {@link AgsAudio.AlsaMidiinFlags}-enum
         */
        unset_flags(flags: AlsaMidiinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @param buffer_length the buffer's length
         * @returns current playback buffer
         */
        get_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get future playback buffer.
         * @param buffer_length the buffer's length
         * @returns next playback buffer
         */
        get_next_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset(): number;

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Plays the current buffer of sequencer.
         */
        play(): void;

        /**
         * Initializes the sequencer for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of sequencer.
         */
        record(): void;

        /**
         * Initializes the sequencer for recording.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the sequencer from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @param buffer_length the buffer's length
         * @virtual
         */
        vfunc_get_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get future playback buffer.
         * @param buffer_length the buffer's length
         * @virtual
         */
        vfunc_get_next_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Plays the current buffer of sequencer.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the sequencer for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of sequencer.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the sequencer for recording.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the sequencer from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;
    }


    namespace ApplyBpm {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            bpm: number;
            scope: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplyBpm extends Ags.Task {
        static $gtype: GObject.GType<ApplyBpm>;

        // Properties
        /**
         * The bpm to apply to scope.
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplyBpm.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplyBpm.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, bpm: number): ApplyBpm;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplyBpm.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplyBpm.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplyBpm.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplyBpm.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplyBpm.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplyBpm.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ApplyPresets {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            buffer_size: number;
            bufferSize: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
            scope: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplyPresets extends Ags.Task {
        static $gtype: GObject.GType<ApplyPresets>;

        // Properties
        /**
         * The count of buffer-size to apply.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The count of buffer-size to apply.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The count of format to apply.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The count of pcm-channels to apply.
         * @since 3.0.0
         * @default 0
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The count of pcm-channels to apply.
         * @since 3.0.0
         * @default 0
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The count of samplerate to apply.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The assigned {@link GObject.Object}
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplyPresets.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplyPresets.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, pcm_channels: number, samplerate: number, buffer_size: number, format: number): ApplyPresets;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplyPresets.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplyPresets.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplyPresets.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplyPresets.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplyPresets.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplyPresets.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param soundcard 
         */
        soundcard(soundcard: GObject.Object): void;
    }


    namespace ApplySF2MidiLocale {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::synth": (pspec: GObject.ParamSpec) => void;
            "notify::synth-template": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            synth: any;
            synth_template: any;
            synthTemplate: any;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplySF2MidiLocale extends Ags.Task {
        static $gtype: GObject.GType<ApplySF2MidiLocale>;

        // Properties
        /**
         * The synth to apply.
         * @since 3.16.0
         */
        get synth(): any;
        set synth(val: any);

        /**
         * The synth template to apply.
         * @since 3.16.0
         */
        get synth_template(): any;
        set synth_template(val: any);

        /**
         * The synth template to apply.
         * @since 3.16.0
         */
        get synthTemplate(): any;
        set synthTemplate(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplySF2MidiLocale.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplySF2MidiLocale.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](synth_template: SF2SynthUtil, synth: SF2SynthUtil): ApplySF2MidiLocale;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplySF2MidiLocale.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySF2MidiLocale.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplySF2MidiLocale.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySF2MidiLocale.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplySF2MidiLocale.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplySF2MidiLocale.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ApplySF2Synth {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::base-note": (pspec: GObject.ParamSpec) => void;
            "notify::count": (pspec: GObject.ParamSpec) => void;
            "notify::requested-frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::sf2-synth-generator": (pspec: GObject.ParamSpec) => void;
            "notify::start-channel": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            base_note: number;
            baseNote: number;
            count: number;
            requested_frame_count: number;
            requestedFrameCount: number;
            sf2_synth_generator: SF2SynthGenerator;
            sf2SynthGenerator: SF2SynthGenerator;
            start_channel: Channel;
            startChannel: Channel;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplySF2Synth extends Ags.Task {
        static $gtype: GObject.GType<ApplySF2Synth>;

        // Properties
        /**
         * The base-note to ramp up from.
         * @since 3.4.0
         * @default -48
         */
        get base_note(): number;
        set base_note(val: number);

        /**
         * The base-note to ramp up from.
         * @since 3.4.0
         * @default -48
         */
        get baseNote(): number;
        set baseNote(val: number);

        /**
         * The count of channels to apply.
         * @since 3.4.0
         * @default 0
         */
        get count(): number;
        set count(val: number);

        /**
         * The frame count of audio signal to apply.
         * @since 3.4.0
         * @default 0
         */
        get requested_frame_count(): number;
        set requested_frame_count(val: number);

        /**
         * The frame count of audio signal to apply.
         * @since 3.4.0
         * @default 0
         */
        get requestedFrameCount(): number;
        set requestedFrameCount(val: number);

        get sf2_synth_generator(): SF2SynthGenerator;
        set sf2_synth_generator(val: SF2SynthGenerator);

        get sf2SynthGenerator(): SF2SynthGenerator;
        set sf2SynthGenerator(val: SF2SynthGenerator);

        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.4.0
         */
        get start_channel(): Channel;
        set start_channel(val: Channel);

        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.4.0
         */
        get startChannel(): Channel;
        set startChannel(val: Channel);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplySF2Synth.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplySF2Synth.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](sf2_synth_generator: SF2SynthGenerator, start_channel: Channel, base_note: number, count: number): ApplySF2Synth;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplySF2Synth.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySF2Synth.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplySF2Synth.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySF2Synth.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplySF2Synth.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplySF2Synth.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ApplySFZInstrument {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::synth": (pspec: GObject.ParamSpec) => void;
            "notify::synth-template": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            synth: any;
            synth_template: any;
            synthTemplate: any;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplySFZInstrument extends Ags.Task {
        static $gtype: GObject.GType<ApplySFZInstrument>;

        // Properties
        /**
         * The synth to apply.
         * @since 3.17.0
         */
        get synth(): any;
        set synth(val: any);

        /**
         * The synth template to apply.
         * @since 3.17.0
         */
        get synth_template(): any;
        set synth_template(val: any);

        /**
         * The synth template to apply.
         * @since 3.17.0
         */
        get synthTemplate(): any;
        set synthTemplate(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplySFZInstrument.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplySFZInstrument.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](synth_template: SFZSynthUtil, synth: SFZSynthUtil): ApplySFZInstrument;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplySFZInstrument.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySFZInstrument.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplySFZInstrument.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySFZInstrument.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplySFZInstrument.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplySFZInstrument.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ApplySFZSynth {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::base-note": (pspec: GObject.ParamSpec) => void;
            "notify::count": (pspec: GObject.ParamSpec) => void;
            "notify::requested-frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::sfz-synth-generator": (pspec: GObject.ParamSpec) => void;
            "notify::start-channel": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            base_note: number;
            baseNote: number;
            count: number;
            requested_frame_count: number;
            requestedFrameCount: number;
            sfz_synth_generator: SFZSynthGenerator;
            sfzSynthGenerator: SFZSynthGenerator;
            start_channel: Channel;
            startChannel: Channel;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplySFZSynth extends Ags.Task {
        static $gtype: GObject.GType<ApplySFZSynth>;

        // Properties
        /**
         * The base-note to ramp up from.
         * @since 3.4.0
         * @default -48
         */
        get base_note(): number;
        set base_note(val: number);

        /**
         * The base-note to ramp up from.
         * @since 3.4.0
         * @default -48
         */
        get baseNote(): number;
        set baseNote(val: number);

        /**
         * The count of channels to apply.
         * @since 3.4.0
         * @default 0
         */
        get count(): number;
        set count(val: number);

        /**
         * The frame count of audio signal to apply.
         * @since 3.4.0
         * @default 0
         */
        get requested_frame_count(): number;
        set requested_frame_count(val: number);

        /**
         * The frame count of audio signal to apply.
         * @since 3.4.0
         * @default 0
         */
        get requestedFrameCount(): number;
        set requestedFrameCount(val: number);

        get sfz_synth_generator(): SFZSynthGenerator;
        set sfz_synth_generator(val: SFZSynthGenerator);

        get sfzSynthGenerator(): SFZSynthGenerator;
        set sfzSynthGenerator(val: SFZSynthGenerator);

        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.4.0
         */
        get start_channel(): Channel;
        set start_channel(val: Channel);

        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.4.0
         */
        get startChannel(): Channel;
        set startChannel(val: Channel);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplySFZSynth.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplySFZSynth.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](sfz_synth_generator: SFZSynthGenerator, start_channel: Channel, base_note: number, count: number): ApplySFZSynth;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplySFZSynth.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySFZSynth.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplySFZSynth.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySFZSynth.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplySFZSynth.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplySFZSynth.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ApplySequencerLength {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::sequencer-length": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            scope: GObject.Object;
            sequencer_length: number;
            sequencerLength: number;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplySequencerLength extends Ags.Task {
        static $gtype: GObject.GType<ApplySequencerLength>;

        // Properties
        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * The sequencer length to apply to scope.
         * @since 3.0.0
         * @default 0
         */
        get sequencer_length(): number;
        set sequencer_length(val: number);

        /**
         * The sequencer length to apply to scope.
         * @since 3.0.0
         * @default 0
         */
        get sequencerLength(): number;
        set sequencerLength(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplySequencerLength.SignalSignatures;

        // Fields
        task: Ags.Task;

        length: number;

        // Constructors
        constructor(properties?: Partial<ApplySequencerLength.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, length: number): ApplySequencerLength;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplySequencerLength.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySequencerLength.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplySequencerLength.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySequencerLength.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplySequencerLength.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplySequencerLength.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ApplySoundConfig {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::config-data": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            config_data: string;
            configData: string;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplySoundConfig extends Ags.Task {
        static $gtype: GObject.GType<ApplySoundConfig>;

        // Properties
        /**
         * The assigned sound config data as string.
         * @since 3.0.0
         * @default null
         */
        get config_data(): string;
        set config_data(val: string);

        /**
         * The assigned sound config data as string.
         * @since 3.0.0
         * @default null
         */
        get configData(): string;
        set configData(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplySoundConfig.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplySoundConfig.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](config_data: string): ApplySoundConfig;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplySoundConfig.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySoundConfig.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplySoundConfig.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySoundConfig.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplySoundConfig.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplySoundConfig.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ApplySynth {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::base-note": (pspec: GObject.ParamSpec) => void;
            "notify::count": (pspec: GObject.ParamSpec) => void;
            "notify::requested-frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::start-channel": (pspec: GObject.ParamSpec) => void;
            "notify::synth-generator": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            base_note: number;
            baseNote: number;
            count: number;
            requested_frame_count: number;
            requestedFrameCount: number;
            start_channel: Channel;
            startChannel: Channel;
            synth_generator: SynthGenerator;
            synthGenerator: SynthGenerator;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplySynth extends Ags.Task {
        static $gtype: GObject.GType<ApplySynth>;

        // Properties
        /**
         * The base-note to ramp up from.
         * @since 3.0.0
         * @default -48
         */
        get base_note(): number;
        set base_note(val: number);

        /**
         * The base-note to ramp up from.
         * @since 3.0.0
         * @default -48
         */
        get baseNote(): number;
        set baseNote(val: number);

        /**
         * The count of channels to apply.
         * @since 3.0.0
         * @default 0
         */
        get count(): number;
        set count(val: number);

        /**
         * The frame count of audio signal to apply.
         * @since 3.3.0
         * @default 0
         */
        get requested_frame_count(): number;
        set requested_frame_count(val: number);

        /**
         * The frame count of audio signal to apply.
         * @since 3.3.0
         * @default 0
         */
        get requestedFrameCount(): number;
        set requestedFrameCount(val: number);

        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.0.0
         */
        get start_channel(): Channel;
        set start_channel(val: Channel);

        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.0.0
         */
        get startChannel(): Channel;
        set startChannel(val: Channel);

        /**
         * The assigned {@link AgsAudio.SynthGenerator}
         * @since 3.0.0
         */
        get synth_generator(): SynthGenerator;
        set synth_generator(val: SynthGenerator);

        /**
         * The assigned {@link AgsAudio.SynthGenerator}
         * @since 3.0.0
         */
        get synthGenerator(): SynthGenerator;
        set synthGenerator(val: SynthGenerator);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplySynth.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplySynth.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](synth_generator: SynthGenerator, start_channel: Channel, base_note: number, count: number): ApplySynth;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplySynth.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySynth.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplySynth.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplySynth.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplySynth.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplySynth.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ApplyTact {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            scope: GObject.Object;
            tact: number;
        }
    }

    /**
     * @gir-type Class
     */
    class ApplyTact extends Ags.Task {
        static $gtype: GObject.GType<ApplyTact>;

        // Properties
        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * The tact to apply to scope.
         * @since 3.0.0
         * @default 0
         */
        get tact(): number;
        set tact(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ApplyTact.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ApplyTact.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, tact: number): ApplyTact;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ApplyTact.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplyTact.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ApplyTact.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ApplyTact.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ApplyTact.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ApplyTact.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace Audio {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::cancel-recall signal notifies about canceld recalls.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "cancel-recall": (arg0: GObject.Object) => void;
            /**
             * The ::check-scope method returns the appropriate recall id of `sound_scope`.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "check-scope": (arg0: number) => (any | null);
            /**
             * The ::cleanup-recall signal notifies about cleanup recalls.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "cleanup-recall": (arg0: GObject.Object) => void;
            /**
             * The ::done-recall signal notifies about doned recalls.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "done-recall": (arg0: GObject.Object) => void;
            /**
             * The ::duplicate-recall signal notifies about duplicated recalls.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "duplicate-recall": (arg0: GObject.Object, arg1: number, arg2: number, arg3: number) => void;
            /**
             * The ::init-recall signal notifies about initd recalls.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "init-recall": (arg0: GObject.Object, arg1: number) => void;
            /**
             * The ::play-recall signal notifies about playd recalls.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "play-recall": (arg0: GObject.Object, arg1: number) => void;
            /**
             * The ::recursive-run-stage signal is invoked while run staging
             * of `audio` for `sound_scope`.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "recursive-run-stage": (arg0: number, arg1: number) => void;
            /**
             * The ::resolve-recall signal notifies about resolved recalls.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "resolve-recall": (arg0: GObject.Object) => void;
            /**
             * The ::set-audio-channels signal notifies about changes in channel
             * alignment.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "set-audio-channels": (arg0: number, arg1: number) => void;
            /**
             * The ::set-pads signal notifies about changes in channel
             * alignment.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "set-pads": (arg0: (any | null), arg1: number, arg2: number) => void;
            /**
             * The ::start signal is invoked while starting playback
             * of `audio`.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            start: (arg0: number) => (any | null);
            /**
             * The ::stop signal is invoked while stoping playback
             * of `audio`.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            stop: (arg0: (any | null), arg1: number) => void;
            "notify::absolute-key": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::audio-end-mapping": (pspec: GObject.ParamSpec) => void;
            "notify::audio-name": (pspec: GObject.ParamSpec) => void;
            "notify::audio-start-mapping": (pspec: GObject.ParamSpec) => void;
            "notify::automation": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::cursor": (pspec: GObject.ParamSpec) => void;
            "notify::denominator": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input": (pspec: GObject.ParamSpec) => void;
            "notify::input-audio-file": (pspec: GObject.ParamSpec) => void;
            "notify::input-lines": (pspec: GObject.ParamSpec) => void;
            "notify::input-midi-file": (pspec: GObject.ParamSpec) => void;
            "notify::input-pads": (pspec: GObject.ParamSpec) => void;
            "notify::input-sequencer": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::is-minor": (pspec: GObject.ParamSpec) => void;
            "notify::key": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::max-audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::max-input-pads": (pspec: GObject.ParamSpec) => void;
            "notify::max-output-pads": (pspec: GObject.ParamSpec) => void;
            "notify::midi": (pspec: GObject.ParamSpec) => void;
            "notify::midi-channel": (pspec: GObject.ParamSpec) => void;
            "notify::midi-end-mapping": (pspec: GObject.ParamSpec) => void;
            "notify::midi-start-mapping": (pspec: GObject.ParamSpec) => void;
            "notify::min-audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::min-input-pads": (pspec: GObject.ParamSpec) => void;
            "notify::min-output-pads": (pspec: GObject.ParamSpec) => void;
            "notify::notation": (pspec: GObject.ParamSpec) => void;
            "notify::numerator": (pspec: GObject.ParamSpec) => void;
            "notify::octave": (pspec: GObject.ParamSpec) => void;
            "notify::offset": (pspec: GObject.ParamSpec) => void;
            "notify::output": (pspec: GObject.ParamSpec) => void;
            "notify::output-audio-file": (pspec: GObject.ParamSpec) => void;
            "notify::output-lines": (pspec: GObject.ParamSpec) => void;
            "notify::output-midi-file": (pspec: GObject.ParamSpec) => void;
            "notify::output-pads": (pspec: GObject.ParamSpec) => void;
            "notify::output-sequencer": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::play": (pspec: GObject.ParamSpec) => void;
            "notify::playback-domain": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
            "notify::recall": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::recycling-context": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::sf2-synth-generator": (pspec: GObject.ParamSpec) => void;
            "notify::sfz-synth-generator": (pspec: GObject.ParamSpec) => void;
            "notify::sharp-flats": (pspec: GObject.ParamSpec) => void;
            "notify::synth-generator": (pspec: GObject.ParamSpec) => void;
            "notify::time-signature": (pspec: GObject.ParamSpec) => void;
            "notify::wave": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            absolute_key: number;
            absoluteKey: number;
            audio_channels: number;
            audioChannels: number;
            audio_end_mapping: number;
            audioEndMapping: number;
            audio_name: string;
            audioName: string;
            audio_start_mapping: number;
            audioStartMapping: number;
            automation: Automation[];
            bpm: number;
            buffer_size: number;
            bufferSize: number;
            cursor: GObject.Object[];
            denominator: number;
            format: number;
            input: Input;
            input_audio_file: GObject.Object;
            inputAudioFile: GObject.Object;
            input_lines: number;
            inputLines: number;
            input_midi_file: GObject.Object;
            inputMidiFile: GObject.Object;
            input_pads: number;
            inputPads: number;
            input_sequencer: GObject.Object;
            inputSequencer: GObject.Object;
            input_soundcard: GObject.Object;
            inputSoundcard: GObject.Object;
            is_minor: boolean;
            isMinor: boolean;
            key: number;
            loop_end: (bigint | number);
            loopEnd: (bigint | number);
            loop_start: (bigint | number);
            loopStart: (bigint | number);
            max_audio_channels: number;
            maxAudioChannels: number;
            max_input_pads: number;
            maxInputPads: number;
            max_output_pads: number;
            maxOutputPads: number;
            midi: Midi[];
            midi_channel: number;
            midiChannel: number;
            midi_end_mapping: number;
            midiEndMapping: number;
            midi_start_mapping: number;
            midiStartMapping: number;
            min_audio_channels: number;
            minAudioChannels: number;
            min_input_pads: number;
            minInputPads: number;
            min_output_pads: number;
            minOutputPads: number;
            notation: Notation[];
            numerator: number;
            octave: number;
            offset: (bigint | number);
            output: Output;
            output_audio_file: GObject.Object;
            outputAudioFile: GObject.Object;
            output_lines: number;
            outputLines: number;
            output_midi_file: GObject.Object;
            outputMidiFile: GObject.Object;
            output_pads: number;
            outputPads: number;
            output_sequencer: GObject.Object;
            outputSequencer: GObject.Object;
            output_soundcard: GObject.Object;
            outputSoundcard: GObject.Object;
            play: Recall[];
            playback_domain: PlaybackDomain;
            playbackDomain: PlaybackDomain;
            preset: Preset[];
            recall: Recall[];
            recall_container: RecallContainer[];
            recallContainer: RecallContainer[];
            recall_id: RecallID[];
            recallId: RecallID[];
            recycling_context: RecyclingContext[];
            recyclingContext: RecyclingContext[];
            samplerate: number;
            sf2_synth_generator: SF2SynthGenerator[];
            sf2SynthGenerator: SF2SynthGenerator[];
            sfz_synth_generator: SFZSynthGenerator[];
            sfzSynthGenerator: SFZSynthGenerator[];
            sharp_flats: number;
            sharpFlats: number;
            synth_generator: SynthGenerator[];
            synthGenerator: SynthGenerator[];
            time_signature: string;
            timeSignature: string;
            wave: Wave[];
        }
    }

    /**
     * @gir-type Class
     */
    class Audio extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<Audio>;

        // Properties
        /**
         * The absolute key lower.
         * @since 3.0.0
         * @default 0
         */
        get absolute_key(): number;
        set absolute_key(val: number);

        /**
         * The absolute key lower.
         * @since 3.0.0
         * @default 0
         */
        get absoluteKey(): number;
        set absoluteKey(val: number);

        /**
         * The audio channels count.
         * @since 3.0.0
         * @default 0
         */
        get audio_channels(): number;
        set audio_channels(val: number);

        /**
         * The audio channels count.
         * @since 3.0.0
         * @default 0
         */
        get audioChannels(): number;
        set audioChannels(val: number);

        /**
         * The audio end mapping.
         * @since 3.0.0
         * @default 0
         */
        get audio_end_mapping(): number;
        set audio_end_mapping(val: number);

        /**
         * The audio end mapping.
         * @since 3.0.0
         * @default 0
         */
        get audioEndMapping(): number;
        set audioEndMapping(val: number);

        /**
         * The name of audio object.
         * @since 3.0.0
         * @default null
         */
        get audio_name(): string;
        set audio_name(val: string);

        /**
         * The name of audio object.
         * @since 3.0.0
         * @default null
         */
        get audioName(): string;
        set audioName(val: string);

        /**
         * The audio start mapping.
         * @since 3.0.0
         * @default 0
         */
        get audio_start_mapping(): number;
        set audio_start_mapping(val: number);

        /**
         * The audio start mapping.
         * @since 3.0.0
         * @default 0
         */
        get audioStartMapping(): number;
        set audioStartMapping(val: number);

        /**
         * The {@link AgsAudio.Automation} it contains.
         * @since 3.0.0
         */
        get automation(): Automation[];
        set automation(val: Automation[]);

        /**
         * The bpm.
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer length.
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer length.
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The {@link GObject.Object} implementing {@link Ags.Cursor} interface.
         * @since 3.0.0
         */
        get cursor(): GObject.Object[];
        set cursor(val: GObject.Object[]);

        /**
         * The denominator of time signature.
         * @since 3.0.0
         * @default 4
         */
        get denominator(): number;
        set denominator(val: number);

        /**
         * The format.
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The {@link AgsAudio.Input} it contains.
         * @since 3.0.0
         * @read-only
         */
        get input(): Input;

        /**
         * The assigned {@link AgsAudio.AudioFile} acting as default sink.
         * @since 3.0.0
         */
        get input_audio_file(): GObject.Object;
        set input_audio_file(val: GObject.Object);

        /**
         * The assigned {@link AgsAudio.AudioFile} acting as default sink.
         * @since 3.0.0
         */
        get inputAudioFile(): GObject.Object;
        set inputAudioFile(val: GObject.Object);

        /**
         * The input lines count.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get input_lines(): number;

        /**
         * The input lines count.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get inputLines(): number;

        /**
         * The assigned {@link AgsAudio.MidiFile} acting as default sink.
         * @since 3.0.0
         */
        get input_midi_file(): GObject.Object;
        set input_midi_file(val: GObject.Object);

        /**
         * The assigned {@link AgsAudio.MidiFile} acting as default sink.
         * @since 3.0.0
         */
        get inputMidiFile(): GObject.Object;
        set inputMidiFile(val: GObject.Object);

        /**
         * The input pads count.
         * @since 3.0.0
         * @default 0
         */
        get input_pads(): number;
        set input_pads(val: number);

        /**
         * The input pads count.
         * @since 3.0.0
         * @default 0
         */
        get inputPads(): number;
        set inputPads(val: number);

        /**
         * The assigned {@link Ags.Sequencer} acting as default source.
         * @since 3.0.0
         */
        get input_sequencer(): GObject.Object;
        set input_sequencer(val: GObject.Object);

        /**
         * The assigned {@link Ags.Sequencer} acting as default source.
         * @since 3.0.0
         */
        get inputSequencer(): GObject.Object;
        set inputSequencer(val: GObject.Object);

        /**
         * The assigned {@link Ags.Soundcard} acting as default source.
         * @since 3.0.0
         */
        get input_soundcard(): GObject.Object;
        set input_soundcard(val: GObject.Object);

        /**
         * The assigned {@link Ags.Soundcard} acting as default source.
         * @since 3.0.0
         */
        get inputSoundcard(): GObject.Object;
        set inputSoundcard(val: GObject.Object);

        /**
         * Is minor.
         * @since 3.0.0
         * @default false
         */
        get is_minor(): boolean;
        set is_minor(val: boolean);

        /**
         * Is minor.
         * @since 3.0.0
         * @default false
         */
        get isMinor(): boolean;
        set isMinor(val: boolean);

        /**
         * The key relative to octave.
         * @since 3.0.0
         * @default 0
         */
        get key(): number;
        set key(val: number);

        /**
         * The audio's loop end.
         * @since 3.0.0
         * @default 0
         */
        get loop_end(): number;
        set loop_end(val: (bigint | number));

        /**
         * The audio's loop end.
         * @since 3.0.0
         * @default 0
         */
        get loopEnd(): number;
        set loopEnd(val: (bigint | number));

        /**
         * The audio's loop start.
         * @since 3.0.0
         * @default 0
         */
        get loop_start(): number;
        set loop_start(val: (bigint | number));

        /**
         * The audio's loop start.
         * @since 3.0.0
         * @default 0
         */
        get loopStart(): number;
        set loopStart(val: (bigint | number));

        /**
         * The maximum audio channels count.
         * @since 3.0.0
         * @default 0
         */
        get max_audio_channels(): number;
        set max_audio_channels(val: number);

        /**
         * The maximum audio channels count.
         * @since 3.0.0
         * @default 0
         */
        get maxAudioChannels(): number;
        set maxAudioChannels(val: number);

        /**
         * The maximum input pads count.
         * @since 3.0.0
         * @default 0
         */
        get max_input_pads(): number;
        set max_input_pads(val: number);

        /**
         * The maximum input pads count.
         * @since 3.0.0
         * @default 0
         */
        get maxInputPads(): number;
        set maxInputPads(val: number);

        /**
         * The maximum output pads count.
         * @since 3.0.0
         * @default 0
         */
        get max_output_pads(): number;
        set max_output_pads(val: number);

        /**
         * The maximum output pads count.
         * @since 3.0.0
         * @default 0
         */
        get maxOutputPads(): number;
        set maxOutputPads(val: number);

        /**
         * The {@link AgsAudio.Midi} it contains.
         * @since 3.0.0
         */
        get midi(): Midi[];
        set midi(val: Midi[]);

        /**
         * The midi channel.
         * @since 3.0.0
         * @default 0
         */
        get midi_channel(): number;
        set midi_channel(val: number);

        /**
         * The midi channel.
         * @since 3.0.0
         * @default 0
         */
        get midiChannel(): number;
        set midiChannel(val: number);

        /**
         * The midi end mapping.
         * @since 3.0.0
         * @default 0
         */
        get midi_end_mapping(): number;
        set midi_end_mapping(val: number);

        /**
         * The midi end mapping.
         * @since 3.0.0
         * @default 0
         */
        get midiEndMapping(): number;
        set midiEndMapping(val: number);

        /**
         * The midi start mapping.
         * @since 3.0.0
         * @default 0
         */
        get midi_start_mapping(): number;
        set midi_start_mapping(val: number);

        /**
         * The midi start mapping.
         * @since 3.0.0
         * @default 0
         */
        get midiStartMapping(): number;
        set midiStartMapping(val: number);

        /**
         * The minimum audio channels count.
         * @since 3.0.0
         * @default 0
         */
        get min_audio_channels(): number;
        set min_audio_channels(val: number);

        /**
         * The minimum audio channels count.
         * @since 3.0.0
         * @default 0
         */
        get minAudioChannels(): number;
        set minAudioChannels(val: number);

        /**
         * The minimum input pads count.
         * @since 3.0.0
         * @default 0
         */
        get min_input_pads(): number;
        set min_input_pads(val: number);

        /**
         * The minimum input pads count.
         * @since 3.0.0
         * @default 0
         */
        get minInputPads(): number;
        set minInputPads(val: number);

        /**
         * The minimum output pads count.
         * @since 3.0.0
         * @default 0
         */
        get min_output_pads(): number;
        set min_output_pads(val: number);

        /**
         * The minimum output pads count.
         * @since 3.0.0
         * @default 0
         */
        get minOutputPads(): number;
        set minOutputPads(val: number);

        /**
         * The {@link AgsAudio.Notation} it contains.
         * @since 3.0.0
         */
        get notation(): Notation[];
        set notation(val: Notation[]);

        /**
         * The numerator of time signature.
         * @since 3.0.0
         * @default 4
         */
        get numerator(): number;
        set numerator(val: number);

        /**
         * The octave lower.
         * @since 3.0.0
         * @default 0
         */
        get octave(): number;
        set octave(val: number);

        /**
         * The audio's offset.
         * @since 3.0.0
         * @default 0
         */
        get offset(): number;
        set offset(val: (bigint | number));

        /**
         * The {@link AgsAudio.Output} it contains.
         * @since 3.0.0
         * @read-only
         */
        get output(): Output;

        /**
         * The assigned {@link AgsAudio.AudioFile} acting as default sink.
         * @since 3.0.0
         */
        get output_audio_file(): GObject.Object;
        set output_audio_file(val: GObject.Object);

        /**
         * The assigned {@link AgsAudio.AudioFile} acting as default sink.
         * @since 3.0.0
         */
        get outputAudioFile(): GObject.Object;
        set outputAudioFile(val: GObject.Object);

        /**
         * The output lines count.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get output_lines(): number;

        /**
         * The output lines count.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get outputLines(): number;

        /**
         * The assigned {@link AgsAudio.MidiFile} acting as default sink.
         * @since 3.0.0
         */
        get output_midi_file(): GObject.Object;
        set output_midi_file(val: GObject.Object);

        /**
         * The assigned {@link AgsAudio.MidiFile} acting as default sink.
         * @since 3.0.0
         */
        get outputMidiFile(): GObject.Object;
        set outputMidiFile(val: GObject.Object);

        /**
         * The output pads count.
         * @since 3.0.0
         * @default 0
         */
        get output_pads(): number;
        set output_pads(val: number);

        /**
         * The output pads count.
         * @since 3.0.0
         * @default 0
         */
        get outputPads(): number;
        set outputPads(val: number);

        /**
         * The assigned {@link Ags.Sequencer} acting as default source.
         * @since 3.0.0
         */
        get output_sequencer(): GObject.Object;
        set output_sequencer(val: GObject.Object);

        /**
         * The assigned {@link Ags.Sequencer} acting as default source.
         * @since 3.0.0
         */
        get outputSequencer(): GObject.Object;
        set outputSequencer(val: GObject.Object);

        /**
         * The assigned {@link Ags.Soundcard} acting as default sink.
         * @since 3.0.0
         */
        get output_soundcard(): GObject.Object;
        set output_soundcard(val: GObject.Object);

        /**
         * The assigned {@link Ags.Soundcard} acting as default sink.
         * @since 3.0.0
         */
        get outputSoundcard(): GObject.Object;
        set outputSoundcard(val: GObject.Object);

        /**
         * The {@link AgsAudio.Recall} it contains in play-context.
         * @since 3.0.0
         */
        get play(): Recall[];
        set play(val: Recall[]);

        /**
         * The assigned {@link AgsAudio.PlaybackDomain}.
         * @since 3.0.0
         */
        get playback_domain(): PlaybackDomain;
        set playback_domain(val: PlaybackDomain);

        /**
         * The assigned {@link AgsAudio.PlaybackDomain}.
         * @since 3.0.0
         */
        get playbackDomain(): PlaybackDomain;
        set playbackDomain(val: PlaybackDomain);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.Preset} information.
         * @since 3.0.0
         */
        get preset(): Preset[];
        set preset(val: Preset[]);

        /**
         * The {@link AgsAudio.Recall} it contains in recall-context.
         * @since 3.0.0
         */
        get recall(): Recall[];
        set recall(val: Recall[]);

        /**
         * The {@link AgsAudio.RecallContainer} it contains in container-context.
         * @since 3.0.0
         */
        get recall_container(): RecallContainer[];
        set recall_container(val: RecallContainer[]);

        /**
         * The {@link AgsAudio.RecallContainer} it contains in container-context.
         * @since 3.0.0
         */
        get recallContainer(): RecallContainer[];
        set recallContainer(val: RecallContainer[]);

        /**
         * The assigned {@link AgsAudio.RecallID}.
         * @since 3.0.0
         */
        get recall_id(): RecallID[];
        set recall_id(val: RecallID[]);

        /**
         * The assigned {@link AgsAudio.RecallID}.
         * @since 3.0.0
         */
        get recallId(): RecallID[];
        set recallId(val: RecallID[]);

        /**
         * The assigned {@link AgsAudio.RecyclingContext}.
         * @since 3.0.0
         */
        get recycling_context(): RecyclingContext[];
        set recycling_context(val: RecyclingContext[]);

        /**
         * The assigned {@link AgsAudio.RecyclingContext}.
         * @since 3.0.0
         */
        get recyclingContext(): RecyclingContext[];
        set recyclingContext(val: RecyclingContext[]);

        /**
         * The samplerate.
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.SF2SynthGenerator} information.
         * @since 3.4.0
         */
        get sf2_synth_generator(): SF2SynthGenerator[];
        set sf2_synth_generator(val: SF2SynthGenerator[]);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.SF2SynthGenerator} information.
         * @since 3.4.0
         */
        get sf2SynthGenerator(): SF2SynthGenerator[];
        set sf2SynthGenerator(val: SF2SynthGenerator[]);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.SFZSynthGenerator} information.
         * @since 3.4.0
         */
        get sfz_synth_generator(): SFZSynthGenerator[];
        set sfz_synth_generator(val: SFZSynthGenerator[]);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.SFZSynthGenerator} information.
         * @since 3.4.0
         */
        get sfzSynthGenerator(): SFZSynthGenerator[];
        set sfzSynthGenerator(val: SFZSynthGenerator[]);

        /**
         * The sharp/flats count.
         * @since 3.0.0
         * @default 0
         */
        get sharp_flats(): number;
        set sharp_flats(val: number);

        /**
         * The sharp/flats count.
         * @since 3.0.0
         * @default 0
         */
        get sharpFlats(): number;
        set sharpFlats(val: number);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.SynthGenerator} information.
         * @since 3.0.0
         */
        get synth_generator(): SynthGenerator[];
        set synth_generator(val: SynthGenerator[]);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.SynthGenerator} information.
         * @since 3.0.0
         */
        get synthGenerator(): SynthGenerator[];
        set synthGenerator(val: SynthGenerator[]);

        /**
         * The time signature.
         * @since 3.0.0
         * @read-only
         * @default 4/4
         */
        get time_signature(): string;

        /**
         * The time signature.
         * @since 3.0.0
         * @read-only
         * @default 4/4
         */
        get timeSignature(): string;

        /**
         * The {@link AgsAudio.Wave} it contains.
         * @since 3.0.0
         */
        get wave(): Wave[];
        set wave(val: Wave[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Audio.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AudioFlags;

        connectable_flags: Ags.ConnectableFlags;

        ability_flags: SoundAbilityFlags;

        behaviour_flags: SoundBehaviourFlags;

        staging_flags: SoundStagingFlags[];

        staging_completed: boolean[];

        uuid: Ags.UUID;

        output_soundcard_channel_map: number;

        input_soundcard_channel_map: number;

        bank_dim: number[];

        automation_port: string;

        instrument_name: string;

        track_name: string;

        machine_widget: any;

        file_data: any;

        // Constructors
        constructor(properties?: Partial<Audio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](output_soundcard: GObject.Object): Audio;

        // Signals
        /** @signal */
        connect_after<K extends keyof Audio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Audio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Audio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Audio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find `audio_name` in `audio`.
         * @param audio the {@link GLib.List}-struct containing {@link AgsAudio.Audio}
         * @param audio_name the audio name to find
         */
        static find_name(audio: Audio[], audio_name: string): Audio[];

        // Virtual methods
        /**
         * Cancel processing audio data.
         * @param recall_id the {@link AgsAudio.RecallID} to apply to
         * @virtual
         */
        vfunc_cancel_recall(recall_id: RecallID): void;

        /**
         * Check scope's recall id.
         * @param sound_scope the scope
         * @virtual
         */
        vfunc_check_scope(sound_scope: number): RecallID[];

        /**
         * Cleanup processing audio data.
         * @param recall_id the {@link AgsAudio.RecallID} to apply to
         * @virtual
         */
        vfunc_cleanup_recall(recall_id: RecallID): void;

        /**
         * Done processing audio data.
         * @param recall_id the {@link AgsAudio.RecallID} to apply to
         * @virtual
         */
        vfunc_done_recall(recall_id: RecallID): void;

        /**
         * Duplicate all {@link AgsAudio.Recall} templates of `audio`.
         * @param recall_id an {@link AgsAudio.RecallID}
         * @param pad the pad
         * @param audio_channel the audio channel
         * @param line the line
         * @virtual
         */
        vfunc_duplicate_recall(recall_id: RecallID, pad: number, audio_channel: number, line: number): void;

        /**
         * Initializes the recalls of `audio`
         * @param recall_id the {@link AgsAudio.RecallID} to use or `NULL`
         * @param staging_flags staging flags, see {@link AgsAudio.SoundStagingFlags}-enum
         * @virtual
         */
        vfunc_init_recall(recall_id: RecallID, staging_flags: SoundStagingFlags): void;

        /**
         * Performs play for the specified `staging_flags`.
         * @param recall_id the {@link AgsAudio.RecallID} to apply to
         * @param staging_flags staging flags, see {@link AgsAudio.SoundStagingFlags}-enum
         * @virtual
         */
        vfunc_play_recall(recall_id: RecallID, staging_flags: SoundStagingFlags): void;

        /**
         * Resets `audio`'s `sound_scope` specified by `staging_flags`.
         * @param sound_scope the scope to reset
         * @param staging_flags the stage to enable
         * @virtual
         */
        vfunc_recursive_run_stage(sound_scope: number, staging_flags: SoundStagingFlags): void;

        /**
         * Performs resolving of recalls.
         * @param recall_id the {@link AgsAudio.RecallID} to use
         * @virtual
         */
        vfunc_resolve_recall(recall_id: RecallID): void;

        /**
         * Resize audio channels AgsInput will be allocated first.
         * @param audio_channels new audio channels
         * @param audio_channels_old old audio channels
         * @virtual
         */
        vfunc_set_audio_channels(audio_channels: number, audio_channels_old: number): void;

        /**
         * Set pad count for the apropriate `channel_type`
         * @param channel_type AGS_TYPE_INPUT or AGS_TYPE_OUTPUT
         * @param pads new pad count
         * @param pads_old old pad count
         * @virtual
         */
        vfunc_set_pads(channel_type: GObject.GType, pads: number, pads_old: number): void;

        /**
         * Is emitted as audio is started.
         * @param sound_scope the scope
         * @virtual
         */
        vfunc_start(sound_scope: number): RecallID[];

        /**
         * Is emitted as playing audio is stopped.
         * @param recall_id the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         * @param sound_scope the scope
         * @virtual
         */
        vfunc_stop(recall_id: RecallID[], sound_scope: number): void;

        // Methods
        /**
         * Adds an automation.
         * @param automation the {@link AgsAudio.Automation}
         */
        add_automation(automation: GObject.Object): void;

        /**
         * Adds an automation port.
         * @param control_name the control name
         */
        add_automation_port(control_name: string): void;

        /**
         * Adds a cursor.
         * @param cursor the {@link GObject.Object} implementing {@link Ags.Cursor}
         */
        add_cursor(cursor: GObject.Object): void;

        /**
         * Adds a midi.
         * @param midi the {@link AgsAudio.Midi}
         */
        add_midi(midi: GObject.Object): void;

        /**
         * Adds a notation.
         * @param notation the {@link AgsAudio.Notation}
         */
        add_notation(notation: GObject.Object): void;

        /**
         * Adds a preset.
         * @param preset an {@link AgsAudio.Preset}
         */
        add_preset(preset: GObject.Object): void;

        /**
         * Adds a recall to `audio`.
         * @param recall the {@link AgsAudio.Recall}
         * @param play_context if `true` play context, else if `false` recall context
         */
        add_recall(recall: GObject.Object, play_context: boolean): void;

        /**
         * Adds a recall container.
         * @param recall_container the {@link AgsAudio.RecallContainer}
         */
        add_recall_container(recall_container: GObject.Object): void;

        /**
         * Adds a recall id.
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        add_recall_id(recall_id: GObject.Object): void;

        /**
         * Adds a recycling context.
         * @param recycling_context the {@link AgsAudio.RecyclingContext}
         */
        add_recycling_context(recycling_context: GObject.Object): void;

        /**
         * Adds a SF2 synth generator.
         * @param sf2_synth_generator an {@link AgsAudio.SF2SynthGenerator}
         */
        add_sf2_synth_generator(sf2_synth_generator: GObject.Object): void;

        /**
         * Adds a SFZ synth generator.
         * @param sfz_synth_generator an {@link AgsAudio.SFZSynthGenerator}
         */
        add_sfz_synth_generator(sfz_synth_generator: GObject.Object): void;

        /**
         * Adds a synth generator.
         * @param synth_generator an {@link AgsAudio.SynthGenerator}
         */
        add_synth_generator(synth_generator: GObject.Object): void;

        /**
         * Adds a wave.
         * @param wave the {@link AgsAudio.Wave}
         */
        add_wave(wave: GObject.Object): void;

        /**
         * Cancel processing audio data.
         * @param recall_id the {@link AgsAudio.RecallID} to apply to
         */
        cancel_recall(recall_id: RecallID): void;

        /**
         * Check scope's recall id.
         * @param sound_scope the scope
         * @returns the scope's recall id of `audio`
         */
        check_scope(sound_scope: number): RecallID[];

        /**
         * Cleanup processing audio data.
         * @param recall_id the {@link AgsAudio.RecallID} to apply to
         */
        cleanup_recall(recall_id: RecallID): void;

        /**
         * Retrieve all ports of {@link AgsAudio.Audio}.
         * @returns a new {@link GLib.List} containing {@link AgsAudio.Port}
         */
        collect_all_audio_ports(): Port[];

        /**
         * Retrieve specified port of {@link AgsAudio.Audio}
         * @param specifier the port's name
         * @param play_context either `true` for play or `false` for recall
         * @returns a {@link GLib.List}-struct of {@link AgsAudio.Port} if found, otherwise `null`
         */
        collect_all_audio_ports_by_specifier_and_context(specifier: string, play_context: boolean): Port[];

        /**
         * Done processing audio data.
         * @param recall_id the {@link AgsAudio.RecallID} to apply to
         */
        done_recall(recall_id: RecallID): void;

        /**
         * Duplicate all {@link AgsAudio.Recall} templates of `audio`.
         * @param recall_id an {@link AgsAudio.RecallID}
         * @param pad the pad
         * @param audio_channel the audio channel
         * @param line the line
         */
        duplicate_recall(recall_id: RecallID, pad: number, audio_channel: number, line: number): void;

        /**
         * Gets absolute key.
         * @returns the absolute key
         */
        get_absolute_key(): number;

        /**
         * Get audio channels.
         * @returns the audio channels
         */
        get_audio_channels(): number;

        /**
         * Get audio name of `audio`.
         * @returns the audio name
         */
        get_audio_name(): string;

        /**
         * Gets audio start mapping.
         * @returns the audio start mapping
         */
        get_audio_start_mapping(): number;

        /**
         * Get automation.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Automation}
         */
        get_automation(): Automation[];

        /**
         * Get automation port.
         * @returns the string vector containing automation ports
         */
        get_automation_port(): string[];

        /**
         * Gets bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Get cursor.
         * @returns the {@link GLib.List}-struct containig {@link GObject.Object} implementing {@link Ags.Cursor}
         */
        get_cursor(): GObject.Object[];

        /**
         * Gets denominator.
         * @returns the denominator
         */
        get_denominator(): number;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get the input object of `audio`.
         * @returns the input
         */
        get_input(): Channel;

        /**
         * Get input audio file.
         * @returns the {@link GObject.Object}
         */
        get_input_audio_file<T = GObject.Object>(): T;

        /**
         * Get input lines.
         * @returns the input lines
         */
        get_input_lines(): number;

        /**
         * Get input MIDI file.
         * @returns the {@link GObject.Object}
         */
        get_input_midi_file<T = GObject.Object>(): T;

        /**
         * Get input pads.
         * @returns the input pads
         */
        get_input_pads(): number;

        /**
         * Get the input sequencer object of `audio`.
         * @returns the input sequencer
         */
        get_input_sequencer<T = GObject.Object>(): T;

        /**
         * Get the input soundcard object of `audio`.
         * @returns the input soundcard
         */
        get_input_soundcard<T = GObject.Object>(): T;

        /**
         * Gets instrument name.
         * @returns the instrument name
         */
        get_instrument_name(): string;

        /**
         * Gets is minor.
         * @returns is minor
         */
        get_is_minor(): boolean;

        /**
         * Gets key.
         * @returns the key
         */
        get_key(): number;

        /**
         * Get maximum audio channels.
         * @returns the maximum audio channels
         */
        get_max_audio_channels(): number;

        /**
         * Get maximum input pads.
         * @returns the maximum input pads
         */
        get_max_input_pads(): number;

        /**
         * Get maximum output pads.
         * @returns the maximum output pads
         */
        get_max_output_pads(): number;

        /**
         * Get midi.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Midi}
         */
        get_midi(): Midi[];

        /**
         * Gets midi channel.
         * @returns the midi channel
         */
        get_midi_channel(): number;

        /**
         * Gets midi start mapping.
         * @returns the midi start mapping
         */
        get_midi_start_mapping(): number;

        /**
         * Get notation.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Notation}
         */
        get_notation(): Notation[];

        /**
         * Gets numerator.
         * @returns the numerator
         */
        get_numerator(): number;

        /**
         * Gets octave.
         * @returns the octave
         */
        get_octave(): number;

        /**
         * Get the output object of `audio`.
         * @returns the output
         */
        get_output(): Channel;

        /**
         * Get output audio file.
         * @returns the {@link GObject.Object}
         */
        get_output_audio_file<T = GObject.Object>(): T;

        /**
         * Get output lines.
         * @returns the output lines
         */
        get_output_lines(): number;

        /**
         * Get output MIDI file.
         * @returns the {@link GObject.Object}
         */
        get_output_midi_file<T = GObject.Object>(): T;

        /**
         * Get output pads.
         * @returns the output pads
         */
        get_output_pads(): number;

        /**
         * Get the output sequencer object of `audio`.
         * @returns the output sequencer
         */
        get_output_sequencer<T = GObject.Object>(): T;

        /**
         * Get the output soundcard object of `audio`.
         * @returns the output soundcard
         */
        get_output_soundcard<T = GObject.Object>(): T;

        /**
         * Get play.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Recall}
         */
        get_play(): Recall[];

        /**
         * Get playback domain.
         * @returns the {@link AgsAudio.PlaybackDomain}
         */
        get_playback_domain<T = GObject.Object>(): T;

        /**
         * Get preset.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Preset}
         */
        get_preset(): Preset[];

        /**
         * Get recall.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Recall}
         */
        get_recall(): Recall[];

        /**
         * Get recall_container.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.RecallContainer}
         */
        get_recall_container(): RecallContainer[];

        /**
         * Get recall id.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.RecallID}
         */
        get_recall_id(): RecallID[];

        /**
         * Get recycling_context.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.RecyclingContext}
         */
        get_recycling_context(): RecyclingContext[];

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get SF2 synth generator.
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.SF2SynthGenerator}
         */
        get_sf2_synth_generator(): SF2SynthGenerator[];

        /**
         * Get SFZ synth generator.
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.SFZSynthGenerator}
         */
        get_sfz_synth_generator(): SFZSynthGenerator[];

        /**
         * Gets sharp flats.
         * @returns the sharp flats
         */
        get_sharp_flats(): number;

        /**
         * Get synth generator.
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.SynthGenerator}
         */
        get_synth_generator(): SynthGenerator[];

        /**
         * Gets time signature.
         * @returns the time signature
         */
        get_time_signature(): string;

        /**
         * Gets track name.
         * @returns the track name
         */
        get_track_name(): string;

        /**
         * Get wave.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Wave}
         */
        get_wave(): Wave[];

        /**
         * Initializes the recalls of `audio`
         * @param recall_id the {@link AgsAudio.RecallID} to use or `NULL`
         * @param staging_flags staging flags, see {@link AgsAudio.SoundStagingFlags}-enum
         */
        init_recall(recall_id: RecallID, staging_flags: SoundStagingFlags): void;

        /**
         * Insert `recall` at `position` in `audio`'s `play_context`.
         * @param recall the {@link AgsAudio.Recall}
         * @param play_context if `true` play context, else if `false` recall context
         * @param position the position
         */
        insert_recall(recall: GObject.Object, play_context: boolean, position: number): void;

        /**
         * Open some files.
         * @param filename the files to open
         * @param overwrite_channels if existing channels should be assigned
         * @param create_channels if new channels should be created as not fitting if combined with `overwrite_channels`
         */
        open_audio_file_as_channel(filename: string[], overwrite_channels: boolean, create_channels: boolean): void;

        /**
         * @param filename 
         * @param overwrite_channels 
         * @param create_channels 
         */
        open_audio_file_as_wave(filename: string, overwrite_channels: boolean, create_channels: boolean): void;

        /**
         * @param filename 
         * @param instrument 
         * @param track_name 
         * @param midi_channel 
         */
        open_midi_file_as_midi(filename: string, instrument: string, track_name: string, midi_channel: number): void;

        /**
         * @param filename 
         * @param instrument 
         * @param track_name 
         * @param midi_channel 
         */
        open_midi_file_as_notation(filename: string, instrument: string, track_name: string, midi_channel: number): void;

        /**
         * Performs play for the specified `staging_flags`.
         * @param recall_id the {@link AgsAudio.RecallID} to apply to
         * @param staging_flags staging flags, see {@link AgsAudio.SoundStagingFlags}-enum
         */
        play_recall(recall_id: RecallID, staging_flags: SoundStagingFlags): void;

        /**
         * Resets `audio`'s `sound_scope` specified by `staging_flags`.
         * @param sound_scope the scope to reset
         * @param staging_flags the stage to enable
         */
        recursive_run_stage(sound_scope: number, staging_flags: SoundStagingFlags): void;

        /**
         * Recursive set property for {@link AgsAudio.Audio}.
         * @param n_params the count of elements in following arrays
         * @param parameter_name the parameter's name array
         * @param value the value array
         */
        recursive_set_property(n_params: number, parameter_name: string, value: (GObject.Value | any)): void;

        /**
         * Remove all empty automation.
         * @param line the line
         * @param channel_type channel type
         * @param control_name the control name
         */
        remove_all_empty_automation(line: number, channel_type: GObject.GType, control_name: string): void;

        /**
         * Removes an automation.
         * @param automation the {@link AgsAudio.Automation}
         */
        remove_automation(automation: GObject.Object): void;

        /**
         * Removes an automation port.
         * @param control_name the control name
         */
        remove_automation_port(control_name: string): void;

        /**
         * Removes a cursor.
         * @param cursor the {@link GObject.Object} implementing {@link Ags.Cursor}
         */
        remove_cursor(cursor: GObject.Object): void;

        /**
         * Removes a midi.
         * @param midi the {@link AgsAudio.Midi}
         */
        remove_midi(midi: GObject.Object): void;

        /**
         * Removes a notation.
         * @param notation the {@link AgsAudio.Notation}
         */
        remove_notation(notation: GObject.Object): void;

        /**
         * Removes a preset.
         * @param preset an {@link AgsAudio.Preset}
         */
        remove_preset(preset: GObject.Object): void;

        /**
         * Removes a recall from `audio`.
         * @param recall the {@link AgsAudio.Recall}
         * @param play_context if `true` play context, else if `false` recall context
         */
        remove_recall(recall: GObject.Object, play_context: boolean): void;

        /**
         * Removes a recall container.
         * @param recall_container the {@link AgsAudio.RecallContainer}
         */
        remove_recall_container(recall_container: GObject.Object): void;

        /**
         * Removes a recall id.
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        remove_recall_id(recall_id: GObject.Object): void;

        /**
         * Removes a recycling context.
         * @param recycling_context the {@link AgsAudio.RecyclingContext}
         */
        remove_recycling_context(recycling_context: GObject.Object): void;

        /**
         * Removes a SF2 synth generator.
         * @param sf2_synth_generator an {@link AgsAudio.SF2SynthGenerator}
         */
        remove_sf2_synth_generator(sf2_synth_generator: GObject.Object): void;

        /**
         * Removes a SFZ synth generator.
         * @param sfz_synth_generator an {@link AgsAudio.SFZSynthGenerator}
         */
        remove_sfz_synth_generator(sfz_synth_generator: GObject.Object): void;

        /**
         * Removes a synth generator.
         * @param synth_generator an {@link AgsAudio.SynthGenerator}
         */
        remove_synth_generator(synth_generator: GObject.Object): void;

        /**
         * Removes a wave.
         * @param wave the {@link AgsAudio.Wave}
         */
        remove_wave(wave: GObject.Object): void;

        /**
         * Performs resolving of recalls.
         * @param recall_id the {@link AgsAudio.RecallID} to use
         */
        resolve_recall(recall_id: RecallID): void;

        /**
         * Enable an ability of AgsAudio.
         * @param ability_flags see enum AgsSoundAbilityFlags
         */
        set_ability_flags(ability_flags: SoundAbilityFlags): void;

        /**
         * Sets absolute key.
         * @param absolute_key the absolute key
         */
        set_absolute_key(absolute_key: number): void;

        /**
         * Resize audio channels AgsInput will be allocated first.
         * @param audio_channels new audio channels
         * @param audio_channels_old old audio channels
         */
        set_audio_channels(audio_channels: number, audio_channels_old: number): void;

        /**
         * Set audio name of `audio`.
         * @param audio_name the audio name
         */
        set_audio_name(audio_name: string): void;

        /**
         * Sets audio start mapping.
         * @param audio_start_mapping the audio start mapping
         */
        set_audio_start_mapping(audio_start_mapping: number): void;

        /**
         * Set automation by replacing existing.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         */
        set_automation(automation: Automation[]): void;

        /**
         * Get automation port.
         * @param automation_port the string vector containing automation ports
         */
        set_automation_port(automation_port: string): void;

        /**
         * Set behaviour flags.
         * @param behaviour_flags the behaviour flags
         */
        set_behaviour_flags(behaviour_flags: SoundBehaviourFlags): void;

        /**
         * Sets bpm.
         * @param bpm the bpm
         */
        set_bpm(bpm: number): void;

        /**
         * Sets buffer length.
         * @param buffer_size the buffer length
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Set cursor by replacing existing.
         * @param cursor the {@link GLib.List}-struct containing {@link Ags.Cursor}
         */
        set_cursor(cursor: GObject.Object[]): void;

        /**
         * Sets denominator.
         * @param denominator the denominator
         */
        set_denominator(denominator: number): void;

        /**
         * Enable a feature of {@link AgsAudio.Audio}.
         * @param flags see enum AgsAudioFlags
         */
        set_flags(flags: AudioFlags): void;

        /**
         * Sets buffer length.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set input audio file.
         * @param input_audio_file the {@link GObject.Object}
         */
        set_input_audio_file(input_audio_file: GObject.Object): void;

        /**
         * Set input MIDI file.
         * @param input_midi_file the {@link GObject.Object}
         */
        set_input_midi_file(input_midi_file: GObject.Object): void;

        /**
         * Set input pads.
         * @param input_pads input pads
         */
        set_input_pads(input_pads: number): void;

        /**
         * Set the input sequencer object of `audio`.
         * @param input_sequencer the {@link GObject.Object} implementing {@link Ags.Sequencer}
         */
        set_input_sequencer(input_sequencer: GObject.Object): void;

        /**
         * Set the input soundcard object of `audio`.
         * @param input_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_input_soundcard(input_soundcard: GObject.Object): void;

        /**
         * Sets instrument name.
         * @param instrument_name the instrument name
         */
        set_instrument_name(instrument_name: string): void;

        /**
         * Sets is minor.
         * @param is_minor is minor
         */
        set_is_minor(is_minor: boolean): void;

        /**
         * Sets key.
         * @param key the key
         */
        set_key(key: number): void;

        /**
         * Set maximum audio channels.
         * @param max_audio_channels maximum of audio channels
         */
        set_max_audio_channels(max_audio_channels: number): void;

        /**
         * Set maximum input pads.
         * @param max_input_pads maximum of input pads
         */
        set_max_input_pads(max_input_pads: number): void;

        /**
         * Set maximum output pads.
         * @param max_output_pads maximum of output pads
         */
        set_max_output_pads(max_output_pads: number): void;

        /**
         * Set maximum pads of `channel_type`.
         * @param channel_type the {@link GObject.GType} of channel, either AGS_TYPE_OUTPUT or AGS_TYPE_INPUT
         * @param max_pads maximum of pads
         */
        set_max_pads(channel_type: GObject.GType, max_pads: number): void;

        /**
         * Set midi by replacing existing.
         * @param midi the {@link GLib.List}-struct containing {@link AgsAudio.Midi}
         */
        set_midi(midi: Midi[]): void;

        /**
         * Sets midi channel.
         * @param midi_channel the midi channel
         */
        set_midi_channel(midi_channel: number): void;

        /**
         * Sets midi start mapping.
         * @param midi_start_mapping the midi start mapping
         */
        set_midi_start_mapping(midi_start_mapping: number): void;

        /**
         * Set notation by replacing existing.
         * @param notation the {@link GLib.List}-struct containing {@link AgsAudio.Notation}
         */
        set_notation(notation: Notation[]): void;

        /**
         * Sets numerator.
         * @param numerator the numerator
         */
        set_numerator(numerator: number): void;

        /**
         * Sets octave.
         * @param octave the octave
         */
        set_octave(octave: number): void;

        /**
         * Set output audio file.
         * @param output_audio_file the {@link GObject.Object}
         */
        set_output_audio_file(output_audio_file: GObject.Object): void;

        /**
         * Set output MIDI file.
         * @param output_midi_file the {@link GObject.Object}
         */
        set_output_midi_file(output_midi_file: GObject.Object): void;

        /**
         * Set output pads.
         * @param output_pads output pads
         */
        set_output_pads(output_pads: number): void;

        /**
         * Set the output sequencer object of `audio`.
         * @param output_sequencer the {@link GObject.Object} implementing {@link Ags.Sequencer}
         */
        set_output_sequencer(output_sequencer: GObject.Object): void;

        /**
         * Set the output soundcard object of `audio`.
         * @param output_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_output_soundcard(output_soundcard: GObject.Object): void;

        /**
         * Set pad count for the apropriate `channel_type`
         * @param channel_type AGS_TYPE_INPUT or AGS_TYPE_OUTPUT
         * @param pads new pad count
         * @param pads_old old pad count
         */
        set_pads(channel_type: GObject.GType, pads: number, pads_old: number): void;

        /**
         * Set play by replacing existing.
         * @param play the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         */
        set_play(play: Recall[]): void;

        /**
         * Set playback domain.
         * @param playback_domain the {@link AgsAudio.PlaybackDomain}
         */
        set_playback_domain(playback_domain: GObject.Object): void;

        /**
         * Set preset by replacing existing.
         * @param preset the {@link GLib.List}-struct containing {@link AgsAudio.Preset}
         */
        set_preset(preset: Preset[]): void;

        /**
         * Set recall by replacing existing.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         */
        set_recall(recall: Recall[]): void;

        /**
         * Set recall_container by replacing existing.
         * @param recall_container the {@link GLib.List}-struct containing {@link AgsAudio.RecallContainer}
         */
        set_recall_container(recall_container: RecallContainer[]): void;

        /**
         * Set recall id by replacing existing.
         * @param recall_id the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         */
        set_recall_id(recall_id: RecallID[]): void;

        /**
         * Set recycling_context by replacing existing.
         * @param recycling_context the {@link GLib.List}-struct containing {@link AgsAudio.RecyclingContext}
         */
        set_recycling_context(recycling_context: RecyclingContext[]): void;

        /**
         * Sets samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set SF2 synth generator by replacing existing.
         * @param sf2_synth_generator the {@link GLib.List}-struct containing {@link AgsAudio.SF2SynthGenerator}
         */
        set_sf2_synth_generator(sf2_synth_generator: SF2SynthGenerator[]): void;

        /**
         * Set SFZ synth generator by replacing existing.
         * @param sfz_synth_generator the {@link GLib.List}-struct containing {@link AgsAudio.SFZSynthGenerator}
         */
        set_sfz_synth_generator(sfz_synth_generator: SFZSynthGenerator[]): void;

        /**
         * Sets sharp flats.
         * @param sharp_flats the sharp flats
         */
        set_sharp_flats(sharp_flats: number): void;

        /**
         * Set `sound_scope` to be completed.
         * @param sound_scope the {@link AgsAudio.SoundScope} to apply, or -1 to apply to all
         */
        set_staging_completed(sound_scope: number): void;

        /**
         * Set staging flags.
         * @param sound_scope the {@link AgsAudio.SoundScope} to apply, or -1 to apply to all
         * @param staging_flags the staging flags
         */
        set_staging_flags(sound_scope: number, staging_flags: SoundStagingFlags): void;

        /**
         * Set synth generator by replacing existing.
         * @param synth_generator the {@link GLib.List}-struct containing {@link AgsAudio.SynthGenerator}
         */
        set_synth_generator(synth_generator: SynthGenerator[]): void;

        /**
         * Sets time signature.
         * @param time_signature the time signature
         */
        set_time_signature(time_signature: string): void;

        /**
         * Sets track name.
         * @param track_name the track name
         */
        set_track_name(track_name: string): void;

        /**
         * Set wave by replacing existing.
         * @param wave the {@link GLib.List}-struct containing {@link AgsAudio.Wave}
         */
        set_wave(wave: Wave[]): void;

        /**
         * Is emitted as audio is started.
         * @param sound_scope the scope
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         */
        start(sound_scope: number): RecallID[];

        /**
         * Is emitted as playing audio is stopped.
         * @param recall_id the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         * @param sound_scope the scope
         */
        stop(recall_id: RecallID[], sound_scope: number): void;

        /**
         * Test `ability_flags` to be set on `audio`.
         * @param ability_flags the ability flags
         * @returns `true` if flags are set, else `false`
         */
        test_ability_flags(ability_flags: SoundAbilityFlags): boolean;

        /**
         * Test `behaviour_flags` to be set on `audio`.
         * @param behaviour_flags the behaviour flags
         * @returns `true` if flags are set, else `false`
         */
        test_behaviour_flags(behaviour_flags: SoundBehaviourFlags): boolean;

        /**
         * Test `flags` to be set on `audio`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AudioFlags): boolean;

        /**
         * Test `sound_scope` to be completed on `audio`.
         * @param sound_scope the {@link AgsAudio.SoundScope} to test
         * @returns `true` if completed, otherwise `false`
         */
        test_staging_completed(sound_scope: number): boolean;

        /**
         * Test `staging_flags` to be set on `audio`.
         * @param sound_scope the {@link AgsAudio.SoundScope} to test
         * @param staging_flags the staging flags
         * @returns `true` if flags are set, else `false`
         */
        test_staging_flags(sound_scope: number, staging_flags: SoundStagingFlags): boolean;

        /**
         * Disable an ability of AgsAudio.
         * @param ability_flags see enum AgsSoundAbilityFlags
         */
        unset_ability_flags(ability_flags: SoundAbilityFlags): void;

        /**
         * Unset behaviour flags.
         * @param behaviour_flags the behaviour flags
         */
        unset_behaviour_flags(behaviour_flags: SoundBehaviourFlags): void;

        /**
         * Disable a feature of AgsAudio.
         * @param flags see enum AgsAudioFlags
         */
        unset_flags(flags: AudioFlags): void;

        /**
         * Unset `sound_scope` to be completed.
         * @param sound_scope the {@link AgsAudio.SoundScope} to apply, or -1 to apply to all
         */
        unset_staging_completed(sound_scope: number): void;

        /**
         * Unset staging flags.
         * @param sound_scope the {@link AgsAudio.SoundScope} to apply, or -1 to apply to all
         * @param staging_flags the staging flags
         */
        unset_staging_flags(sound_scope: number, staging_flags: SoundStagingFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace AudioApplicationContext {
        // Signal signatures
        interface SignalSignatures extends Ags.ApplicationContext.SignalSignatures {
            "notify::config": (pspec: GObject.ParamSpec) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::main-loop": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.ApplicationContext.ConstructorProps, Ags.ConcurrencyProvider.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.ServiceProvider.ConstructorProps, SoundProvider.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class AudioApplicationContext extends Ags.ApplicationContext implements Ags.ConcurrencyProvider, Ags.Connectable, Ags.ServiceProvider, SoundProvider {
        static $gtype: GObject.GType<AudioApplicationContext>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioApplicationContext.SignalSignatures;

        // Fields
        application_context: Ags.ApplicationContext;

        flags: number;

        thread_pool: Ags.ThreadPool;

        worker: any[];

        
    // This field conflicts with a function in a parent class or interface.
    is_operating: (boolean | any);

        server_status: Ags.ServerStatus;

        registry: Ags.Registry;

        server: any[];

        default_soundcard: GObject.Object;

        default_soundcard_thread: Ags.Thread;

        default_export_thread: Ags.Thread;

        soundcard: any[];

        sequencer: any[];

        audio: any[];

        sound_server: any[];

        osc_server: any[];

        program: any[];

        tempo: any[];

        start_loader: boolean;

        setup_ready: boolean;

        loader_ready: boolean;

        ladspa_loading: boolean;

        dssi_loading: boolean;

        lv2_loading: boolean;

        vst3_loading: boolean;

        ladspa_loader: any[];

        dssi_loader: any[];

        lv2_loader: any[];

        vst3_loader: any[];

        lv2_turtle_scanner: Lv2TurtleScanner;

        // Constructors
        constructor(properties?: Partial<AudioApplicationContext.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AudioApplicationContext;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioApplicationContext.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioApplicationContext.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioApplicationContext.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioApplicationContext.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        loader_timeout(): boolean;

        /**
         * Get main loop of application context.
         * @returns the {@link Ags.Thread} implementing {@link Ags.MainLoop}
         */
        get_main_loop(): Ags.Thread;

        /**
         * Get task launcher of application context.
         * @returns the {@link Ags.TaskLauncher}
         */
        get_task_launcher(): Ags.TaskLauncher;

        /**
         * Get thread pool of application context.
         * @returns the {@link Ags.ThreadPool}
         */
        get_thread_pool(): Ags.ThreadPool;

        /**
         * Get workers of application context.
         * @returns the {@link GLib.List}-struct containing workers
         */
        get_worker(): Ags.WorkerThread[];

        /**
         * Set main loop of application context.
         * @param main_loop the {@link Ags.Thread} implementing {@link Ags.MainLoop}
         */
        set_main_loop(main_loop: Ags.Thread): void;

        /**
         * Set task launcher of application context.
         * @param task_launcher the {@link Ags.TaskLauncher}
         */
        set_task_launcher(task_launcher: Ags.TaskLauncher): void;

        /**
         * Set thread pool of application context.
         * @param thread_pool the {@link Ags.ThreadPool}
         */
        set_thread_pool(thread_pool: Ags.ThreadPool): void;

        /**
         * Set workers of application context.
         * @param worker the {@link GLib.List}-struct containing workers
         */
        set_worker(worker: Ags.WorkerThread[]): void;

        /**
         * Get main loop of application context.
         * @virtual
         */
        vfunc_get_main_loop(): Ags.Thread;

        /**
         * Get task launcher of application context.
         * @virtual
         */
        vfunc_get_task_launcher(): Ags.TaskLauncher;

        /**
         * Get thread pool of application context.
         * @virtual
         */
        vfunc_get_thread_pool(): Ags.ThreadPool;

        /**
         * Get workers of application context.
         * @virtual
         */
        vfunc_get_worker(): Ags.WorkerThread[];

        /**
         * Set main loop of application context.
         * @param main_loop the {@link Ags.Thread} implementing {@link Ags.MainLoop}
         * @virtual
         */
        vfunc_set_main_loop(main_loop: Ags.Thread): void;

        /**
         * Set task launcher of application context.
         * @param task_launcher the {@link Ags.TaskLauncher}
         * @virtual
         */
        vfunc_set_task_launcher(task_launcher: Ags.TaskLauncher): void;

        /**
         * Set thread pool of application context.
         * @param thread_pool the {@link Ags.ThreadPool}
         * @virtual
         */
        vfunc_set_thread_pool(thread_pool: Ags.ThreadPool): void;

        /**
         * Set workers of application context.
         * @param worker the {@link GLib.List}-struct containing workers
         * @virtual
         */
        vfunc_set_worker(worker: Ags.WorkerThread[]): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get server.
         * @returns the {@link GLib.List}-struct containing {@link Ags.Server}
         */
        get_server(): Ags.Server[];

        /**
         * Set registry.
         * @param registry the {@link Ags.Registry}
         */
        set_registry(registry: Ags.Registry): void;

        /**
         * Set server.
         * @param server the {@link GLib.List}-struct containing {@link Ags.Server}
         */
        set_server(server: Ags.Server[]): void;

        /**
         * Get server.
         * @virtual
         */
        vfunc_get_server(): Ags.Server[];

        /**
         * Check if is operating.
         * @virtual
         */
        vfunc_is_operating(): boolean;

        /**
         * Set registry.
         * @param registry the {@link Ags.Registry}
         * @virtual
         */
        vfunc_set_registry(registry: Ags.Registry): void;

        /**
         * Set server.
         * @param server the {@link GLib.List}-struct containing {@link Ags.Server}
         * @virtual
         */
        vfunc_set_server(server: Ags.Server[]): void;

        /**
         * Get audio objects.
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.Audio}
         */
        get_audio(): Audio[];

        /**
         * Get default soundcard thread.
         * @returns the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        get_default_soundcard<T = GObject.Object>(): T;

        /**
         * Get default soundcard thread.
         * @returns the {@link Ags.Thread}
         */
        get_default_soundcard_thread<T = GObject.Object>(): T;

        /**
         * Set distributed manager.
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.OscServer}
         */
        get_osc_server(): GObject.Object[];

        /**
         * Get sequencers.
         * @returns a {@link GLib.List}-struct containing {@link Ags.Sequencer}
         */
        get_sequencer(): GObject.Object[];

        /**
         * Set sound server.
         * @returns a {@link GLib.List}-struct containing {@link Ags.SoundServer}
         */
        get_sound_server(): GObject.Object[];

        /**
         * Get soundcards.
         * @returns a {@link GLib.List}-struct containing {@link Ags.Soundcard}
         */
        get_soundcard(): GObject.Object[];

        /**
         * Set audio objects.
         * @param audio a {@link GLib.List}-struct containing {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio[]): void;

        /**
         * Set default soundcard.
         * @param soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_default_soundcard(soundcard: GObject.Object): void;

        /**
         * Set default soundcard thread.
         * @param soundcard_thread the default {@link Ags.Thread}
         */
        set_default_soundcard_thread(soundcard_thread: GObject.Object): void;

        /**
         * Set OSC server.
         * @param osc_server a {@link GLib.List}-struct containing {@link AgsAudio.OscServer}
         */
        set_osc_server(osc_server: GObject.Object[]): void;

        /**
         * Set sequencers.
         * @param sequencer a {@link GLib.List}-struct containing {@link Ags.Sequencer}
         */
        set_sequencer(sequencer: GObject.Object[]): void;

        /**
         * Get sound server.
         * @param sound_server a {@link GLib.List}-struct containing {@link Ags.SoundServer}
         */
        set_sound_server(sound_server: GObject.Object[]): void;

        /**
         * Set soundcards.
         * @param soundcard a {@link GLib.List}-struct containing {@link Ags.Soundcard}
         */
        set_soundcard(soundcard: GObject.Object[]): void;

        /**
         * Get audio objects.
         * @virtual
         */
        vfunc_get_audio(): Audio[];

        /**
         * Get default soundcard thread.
         * @virtual
         */
        vfunc_get_default_soundcard<T = GObject.Object>(): T;

        /**
         * Get default soundcard thread.
         * @virtual
         */
        vfunc_get_default_soundcard_thread<T = GObject.Object>(): T;

        /**
         * Set distributed manager.
         * @virtual
         */
        vfunc_get_osc_server(): GObject.Object[];

        /**
         * Get sequencers.
         * @virtual
         */
        vfunc_get_sequencer(): GObject.Object[];

        /**
         * Set sound server.
         * @virtual
         */
        vfunc_get_sound_server(): GObject.Object[];

        /**
         * Get soundcards.
         * @virtual
         */
        vfunc_get_soundcard(): GObject.Object[];

        /**
         * Set audio objects.
         * @param audio a {@link GLib.List}-struct containing {@link AgsAudio.Audio}
         * @virtual
         */
        vfunc_set_audio(audio: Audio[]): void;

        /**
         * Set default soundcard.
         * @param soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         * @virtual
         */
        vfunc_set_default_soundcard(soundcard: GObject.Object): void;

        /**
         * Set default soundcard thread.
         * @param soundcard_thread the default {@link Ags.Thread}
         * @virtual
         */
        vfunc_set_default_soundcard_thread(soundcard_thread: GObject.Object): void;

        /**
         * Set OSC server.
         * @param osc_server a {@link GLib.List}-struct containing {@link AgsAudio.OscServer}
         * @virtual
         */
        vfunc_set_osc_server(osc_server: GObject.Object[]): void;

        /**
         * Set sequencers.
         * @param sequencer a {@link GLib.List}-struct containing {@link Ags.Sequencer}
         * @virtual
         */
        vfunc_set_sequencer(sequencer: GObject.Object[]): void;

        /**
         * Get sound server.
         * @param sound_server a {@link GLib.List}-struct containing {@link Ags.SoundServer}
         * @virtual
         */
        vfunc_set_sound_server(sound_server: GObject.Object[]): void;

        /**
         * Set soundcards.
         * @param soundcard a {@link GLib.List}-struct containing {@link Ags.Soundcard}
         * @virtual
         */
        vfunc_set_soundcard(soundcard: GObject.Object[]): void;
    }


    namespace AudioContainer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::audio-signal": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::file-audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::file-frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::file-samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::instrument": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
            "notify::sample": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::wave": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio_channel: number;
            audioChannel: number;
            audio_signal: AudioSignal[];
            audioSignal: AudioSignal[];
            buffer_size: number;
            bufferSize: number;
            file_audio_channels: number;
            fileAudioChannels: number;
            file_frame_count: number;
            fileFrameCount: number;
            file_samplerate: number;
            fileSamplerate: number;
            filename: string;
            format: number;
            instrument: string;
            preset: string;
            sample: string;
            samplerate: number;
            soundcard: GObject.Object;
            wave: Wave[];
        }
    }

    /**
     * @gir-type Class
     */
    class AudioContainer extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<AudioContainer>;

        // Properties
        /**
         * The audio channel to be read.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The audio channel to be read.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The containing {@link AgsAudio.AudioSignal}.
         * @since 3.0.0
         */
        get audio_signal(): AudioSignal[];
        set audio_signal(val: AudioSignal[]);

        /**
         * The containing {@link AgsAudio.AudioSignal}.
         * @since 3.0.0
         */
        get audioSignal(): AudioSignal[];
        set audioSignal(val: AudioSignal[]);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The audio channel count of this file.
         * @since 3.0.0
         * @default 0
         */
        get file_audio_channels(): number;
        set file_audio_channels(val: number);

        /**
         * The audio channel count of this file.
         * @since 3.0.0
         * @default 0
         */
        get fileAudioChannels(): number;
        set fileAudioChannels(val: number);

        /**
         * @default 0
         */
        get file_frame_count(): number;
        set file_frame_count(val: number);

        /**
         * @default 0
         */
        get fileFrameCount(): number;
        set fileFrameCount(val: number);

        /**
         * The samplerate of this file.
         * @since 3.0.0
         * @default 0
         */
        get file_samplerate(): number;
        set file_samplerate(val: number);

        /**
         * The samplerate of this file.
         * @since 3.0.0
         * @default 0
         */
        get fileSamplerate(): number;
        set fileSamplerate(val: number);

        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The format to be used.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned instrument.
         * @since 3.0.0
         * @default null
         */
        get instrument(): string;
        set instrument(val: string);

        /**
         * The assigned preset.
         * @since 3.0.0
         * @default null
         */
        get preset(): string;
        set preset(val: string);

        /**
         * The assigned sample.
         * @since 3.0.0
         * @default null
         */
        get sample(): string;
        set sample(val: string);

        /**
         * The samplerate to be used.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The assigned soundcard.
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * The containing {@link AgsAudio.Wave}.
         * @since 3.0.0
         */
        get wave(): Wave[];
        set wave(val: Wave[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioContainer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        sound_container: GObject.Object;

        // Constructors
        constructor(properties?: Partial<AudioContainer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](filename: string, preset: string, instrument: string, sample: string, soundcard: GObject.Object, audio_channel: number): AudioContainer;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioContainer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioContainer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioContainer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioContainer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Check suffix.
         * @param filename the filename
         */
        static check_suffix(filename: string): boolean;

        // Methods
        /**
         * Add `audio_signal` to `audio_container`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        add_audio_signal(audio_signal: GObject.Object): void;

        /**
         * Add `wave` to `audio_container`.
         * @param wave the {@link AgsAudio.Wave}
         */
        add_wave(wave: GObject.Object): void;

        /**
         * Close the {@link AgsAudio.AudioContainer}.
         */
        close(): void;

        /**
         * Find resource specified by parameters.
         * @param preset the preset
         * @param instrument the instrument
         * @param sample the sample
         * @returns the {@link GLib.List}-struct containing {@link GObject.Object} sample or `null`. WARNING the parameters need to be valid in order to return the correct list.
         */
        find_sound_resource(preset: string, instrument: string, sample: string): GObject.Object[];

        flush(): void;

        /**
         * Open the {@link AgsAudio.AudioContainer} in read mode.
         * @returns `true` on success, otherwise `false`
         */
        open(): boolean;

        /**
         * @param data 
         */
        open_from_data(data: string): boolean;

        /**
         * Read audio buffer.
         * @param audio_channel nth channel
         * @param format the format
         */
        read(audio_channel: number, format: Ags.SoundcardFormat): (any | null);

        /**
         * Convert the {@link AgsAudio.AudioContainer} to a {@link GLib.List}-struct of {@link AgsAudio.AudioSignal}.
         * @returns the new {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal} or `null`
         */
        read_audio_signal(): AudioSignal[];

        /**
         * Convert the {@link AgsAudio.AudioContainer} to a {@link GLib.List}-struct of {@link AgsAudio.Wave}.
         * @param x_offset the x offset
         * @param delay the delay
         * @param attack the attack
         * @returns the new {@link GLib.List}-struct containing {@link AgsAudio.Wave} or `null`
         */
        read_wave(x_offset: (bigint | number), delay: number, attack: number): Wave[];

        /**
         * Remove `audio_signal` from `audio_container`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        remove_audio_signal(audio_signal: GObject.Object): void;

        /**
         * Remove `wave` from `audio_container`.
         * @param wave the {@link AgsAudio.Wave}
         */
        remove_wave(wave: GObject.Object): void;

        /**
         * @param create 
         */
        rw_open(create: boolean): boolean;

        /**
         * @param frames 
         * @param whence 
         */
        seek(frames: number, whence: number): void;

        /**
         * @param buffer 
         * @param buffer_size 
         * @param format 
         */
        write(buffer: (any | null), buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace AudioContainerManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-container": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio_container: any;
            audioContainer: any;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioContainerManager extends GObject.Object {
        static $gtype: GObject.GType<AudioContainerManager>;

        // Properties
        /**
         * The {@link GLib.List}-struct containing {@link AgsAudio.AudioContainer}.
         * @since 3.4.0
         */
        get audio_container(): any;
        set audio_container(val: any);

        /**
         * The {@link GLib.List}-struct containing {@link AgsAudio.AudioContainer}.
         * @since 3.4.0
         */
        get audioContainer(): any;
        set audioContainer(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioContainerManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<AudioContainerManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AudioContainerManager;

        // Signals
        /** @signal */
        connect<K extends keyof AudioContainerManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioContainerManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof AudioContainerManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioContainerManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioContainerManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioContainerManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get ags audio container manager instance.
         */
        static get_instance(): AudioContainerManager;

        // Methods
        /**
         * Add `audio_container` to `audio_container_manager`.
         * @param audio_container the {@link AgsAudio.AudioContainer}
         */
        add_audio_container(audio_container: GObject.Object): void;

        /**
         * Find audio container by `filename`.
         * @param filename the string identifier
         * @returns the matching {@link AgsAudio.AudioContainer} if found, otherwise `null`
         */
        find_audio_container<T = GObject.Object>(filename: string): T;

        /**
         * Remove `audio_container` from `audio_container_manager`.
         * @param audio_container the {@link AgsAudio.AudioContainer}
         */
        remove_audio_container(audio_container: GObject.Object): void;
    }


    namespace AudioFile {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::audio-signal": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::file-audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::file-frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::file-samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::sound-resource": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::wave": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio_channel: number;
            audioChannel: number;
            audio_signal: AudioSignal[];
            audioSignal: AudioSignal[];
            buffer_size: number;
            bufferSize: number;
            file_audio_channels: number;
            fileAudioChannels: number;
            file_frame_count: number;
            fileFrameCount: number;
            file_samplerate: number;
            fileSamplerate: number;
            filename: string;
            format: number;
            samplerate: number;
            sound_resource: GObject.Object;
            soundResource: GObject.Object;
            soundcard: GObject.Object;
            wave: Wave[];
        }
    }

    /**
     * @gir-type Class
     */
    class AudioFile extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<AudioFile>;

        // Properties
        /**
         * The audio channel to be read.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The audio channel to be read.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The containing {@link AgsAudio.AudioSignal}.
         * @since 3.0.0
         */
        get audio_signal(): AudioSignal[];
        set audio_signal(val: AudioSignal[]);

        /**
         * The containing {@link AgsAudio.AudioSignal}.
         * @since 3.0.0
         */
        get audioSignal(): AudioSignal[];
        set audioSignal(val: AudioSignal[]);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The audio channel count of this file.
         * @since 3.0.0
         * @default 0
         */
        get file_audio_channels(): number;
        set file_audio_channels(val: number);

        /**
         * The audio channel count of this file.
         * @since 3.0.0
         * @default 0
         */
        get fileAudioChannels(): number;
        set fileAudioChannels(val: number);

        /**
         * @default 0
         */
        get file_frame_count(): number;
        set file_frame_count(val: number);

        /**
         * @default 0
         */
        get fileFrameCount(): number;
        set fileFrameCount(val: number);

        /**
         * The samplerate of this file.
         * @since 3.0.0
         * @default 0
         */
        get file_samplerate(): number;
        set file_samplerate(val: number);

        /**
         * The samplerate of this file.
         * @since 3.0.0
         * @default 0
         */
        get fileSamplerate(): number;
        set fileSamplerate(val: number);

        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The format to be used.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The samplerate to be used.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The assigned sound resource.
         * @since 5.3.0
         */
        get sound_resource(): GObject.Object;
        set sound_resource(val: GObject.Object);

        /**
         * The assigned sound resource.
         * @since 5.3.0
         */
        get soundResource(): GObject.Object;
        set soundResource(val: GObject.Object);

        /**
         * The assigned soundcard.
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * The containing {@link AgsAudio.Wave}.
         * @since 3.0.0
         */
        get wave(): Wave[];
        set wave(val: Wave[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioFile.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        // Constructors
        constructor(properties?: Partial<AudioFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](filename: string, soundcard: GObject.Object, audio_channel: number): AudioFile;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Check suffix.
         * @param filename the filename
         */
        static check_suffix(filename: string): boolean;

        // Methods
        /**
         * Add `audio_signal` to `audio_file`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        add_audio_signal(audio_signal: GObject.Object): void;

        /**
         * Add `wave` to `audio_file`.
         * @param wave the {@link AgsAudio.Wave}
         */
        add_wave(wave: GObject.Object): void;

        /**
         * Close the {@link AgsAudio.AudioFile}.
         */
        close(): void;

        /**
         * Flushes the {@link AgsAudio.AudioFile}'s internal buffer.
         */
        flush(): void;

        /**
         * Get audio channel of `audio_file`.
         * @returns the audio channel
         */
        get_audio_channel(): number;

        /**
         * Get file audio channels of `audio_file`.
         * @returns the file audio channels
         */
        get_file_audio_channels(): number;

        /**
         * Get file frame count of `audio_file`.
         * @returns the file frame count
         */
        get_file_frame_count(): number;

        /**
         * Get file samplerate of `audio_file`.
         * @returns the file samplerate
         */
        get_file_samplerate(): number;

        /**
         * Get filename of `audio_file`.
         * @returns the filename
         */
        get_filename(): string;

        /**
         * Get format of `audio_file`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame_count of `audio_file`.
         * @returns the frame_count
         */
        get_frame_count(): number;

        /**
         * Get samplerate of `audio_file`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get sound resource of `audio_file`.
         * @returns the {@link GObject.Object} implementing {@link AgsAudio.SoundResource}
         */
        get_sound_resource<T = GObject.Object>(): T;

        /**
         * Get soundcard of `audio_file`.
         * @returns the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        get_soundcard<T = GObject.Object>(): T;

        /**
         * Open the {@link AgsAudio.AudioFile} in read mode.
         * @returns `true` on success, otherwise `false`
         */
        open(): boolean;

        /**
         * Open {@link AgsAudio.AudioFile} using virtual functions.
         * @param data the audio data
         * @returns `true` on success, otherwise `false`
         */
        open_from_data(data: string): boolean;

        /**
         * Read audio buffer.
         * @param audio_channel nth channel
         * @param format the format
         */
        read(audio_channel: number, format: number): (any | null);

        /**
         * Convert the {@link AgsAudio.AudioFile} to a {@link GLib.List}-struct of {@link AgsAudio.AudioSignal}.
         */
        read_audio_signal(): void;

        /**
         * Convert the {@link AgsAudio.AudioFile} to a {@link GLib.List}-struct of buffers.
         * @param x_offset the x offset
         * @param delay the delay
         * @param attack the attack
         */
        read_wave(x_offset: (bigint | number), delay: number, attack: number): void;

        /**
         * Remove `audio_signal` from `audio_file`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        remove_audio_signal(audio_signal: GObject.Object): void;

        /**
         * Remove `wave` from `audio_file`.
         * @param wave the {@link AgsAudio.Wave}
         */
        remove_wave(wave: GObject.Object): void;

        /**
         * Open the {@link AgsAudio.AudioFile} in read/write mode.
         * @param create create the file
         * @returns `true` on success, otherwise `false`
         */
        rw_open(create: boolean): boolean;

        /**
         * Position the {@link AgsAudio.AudioFile}'s internal data address.
         * @param frames number of frames to seek
         * @param whence SEEK_SET, SEEK_CUR, or SEEK_END
         */
        seek(frames: number, whence: number): void;

        /**
         * Get audio channel of `audio_file`.
         * @param audio_channel the audio channel
         */
        set_audio_channel(audio_channel: number): void;

        /**
         * Get file audio channels of `audio_file`.
         * @param file_audio_channels the file audio channels
         */
        set_file_audio_channels(file_audio_channels: number): void;

        /**
         * Get file frame count of `audio_file`.
         * @param file_frame_count the file frame count
         */
        set_file_frame_count(file_frame_count: number): void;

        /**
         * Get file samplerate of `audio_file`.
         * @param file_samplerate the file samplerate
         */
        set_file_samplerate(file_samplerate: number): void;

        /**
         * Set filename of `audio_file`.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Enable a feature of `audio_file`.
         * @param flags see {@link AgsAudio.AudioFileFlags}-enum
         */
        set_flags(flags: number): void;

        /**
         * Get format of `audio_file`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Get frame_count of `audio_file`.
         * @param frame_count the frame_count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Get samplerate of `audio_file`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set sound resource of `audio_file`.
         * @param sound_resource the {@link GObject.Object} implementing {@link AgsAudio.SoundResource}
         */
        set_sound_resource(sound_resource: GObject.Object): void;

        /**
         * Set soundcard of `audio_file`.
         * @param soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_soundcard(soundcard: GObject.Object): void;

        /**
         * Test `flags` to be set on `audio_file`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Disable a feature of `audio_file`.
         * @param flags see {@link AgsAudio.AudioFileFlags}-enum
         */
        unset_flags(flags: number): void;

        /**
         * Write the buffer to {@link AgsAudio.AudioFile}.
         * @param buffer the audio data
         * @param buffer_size the count of frames to write
         * @param format the format
         */
        write(buffer: (any | null), buffer_size: number, format: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace AudioFileLink {
        // Signal signatures
        interface SignalSignatures extends Ags.FileLink.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::instrument": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
            "notify::sample": (pspec: GObject.ParamSpec) => void;
            "notify::timestamp": (pspec: GObject.ParamSpec) => void;
            "notify::data": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.FileLink.ConstructorProps, Ags.Plugin.ConstructorProps {
            audio_channel: number;
            audioChannel: number;
            instrument: string;
            preset: string;
            sample: string;
            timestamp: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioFileLink extends Ags.FileLink implements Ags.Plugin {
        static $gtype: GObject.GType<AudioFileLink>;

        // Properties
        /**
         * The assigned audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The assigned audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The assigned instrument.
         * @since 3.0.0
         * @default null
         */
        get instrument(): string;
        set instrument(val: string);

        /**
         * The assigned preset.
         * @since 3.0.0
         * @default null
         */
        get preset(): string;
        set preset(val: string);

        /**
         * The assigned sample.
         * @since 3.0.0
         * @default null
         */
        get sample(): string;
        set sample(val: string);

        /**
         * The assigned timestamp.
         * @since 3.0.0
         */
        get timestamp(): GObject.Object;
        set timestamp(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioFileLink.SignalSignatures;

        // Fields
        file_link: Ags.FileLink;

        // Constructors
        constructor(properties?: Partial<AudioFileLink.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AudioFileLink;

        // Signals
        /** @signal */
        connect<K extends keyof AudioFileLink.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioFileLink.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof AudioFileLink.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioFileLink.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioFileLink.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioFileLink.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Retrieve the build id of the plugin.
         * @returns the plugins build id
         */
        get_build_id(): string;

        /**
         * Retrieve the name of the plugin.
         * @returns the plugins name
         */
        get_name(): string;

        /**
         * Retrieve the ports of the plugin.
         * @returns the plugins ports
         */
        get_ports(): GObject.Object[];

        /**
         * Retrieve the version of the plugin.
         * @returns the plugins version
         */
        get_version(): string;

        /**
         * Retrieve the xml type of the plugin.
         * @returns the plugins xml type
         */
        get_xml_type(): string;

        /**
         * Set the build id of the plugin.
         * @param build_id the build id of plugin
         */
        set_build_id(build_id: string): void;

        /**
         * Set the name of the plugin.
         * @param name the name of plugin
         */
        set_name(name: string): void;

        /**
         * Set the build id of the plugin.
         * @param ports the build id of plugin
         */
        set_ports(ports: GObject.Object[]): void;

        /**
         * Set the version of the plugin.
         * @param version the version of plugin
         */
        set_version(version: string): void;

        /**
         * Set the build id of the plugin.
         * @param xml_type the build id of plugin
         */
        set_xml_type(xml_type: string): void;

        /**
         * Retrieve the build id of the plugin.
         * @virtual
         */
        vfunc_get_build_id(): string;

        /**
         * Retrieve the name of the plugin.
         * @virtual
         */
        vfunc_get_name(): string;

        /**
         * Retrieve the ports of the plugin.
         * @virtual
         */
        vfunc_get_ports(): GObject.Object[];

        /**
         * Retrieve the version of the plugin.
         * @virtual
         */
        vfunc_get_version(): string;

        /**
         * Retrieve the xml type of the plugin.
         * @virtual
         */
        vfunc_get_xml_type(): string;

        /**
         * Set the build id of the plugin.
         * @param build_id the build id of plugin
         * @virtual
         */
        vfunc_set_build_id(build_id: string): void;

        /**
         * Set the name of the plugin.
         * @param name the name of plugin
         * @virtual
         */
        vfunc_set_name(name: string): void;

        /**
         * Set the build id of the plugin.
         * @param ports the build id of plugin
         * @virtual
         */
        vfunc_set_ports(ports: GObject.Object[]): void;

        /**
         * Set the version of the plugin.
         * @param version the version of plugin
         * @virtual
         */
        vfunc_set_version(version: string): void;

        /**
         * Set the build id of the plugin.
         * @param xml_type the build id of plugin
         * @virtual
         */
        vfunc_set_xml_type(xml_type: string): void;

        /**
         * Gets a named field from the objects table of associations (see `g_object_set_data()`).
         * @param key name of the key for that association
         * @returns the data if found,          or `null` if no such data exists.
         */
        get_data(key: string): (any | null);

        /**
         * @param args 
         */
        // Conflicted with Ags.FileLink.get_data
        get_data(...args: never[]): any;

        /**
         * Each object carries around a table of associations from
         * strings to pointers.  This function lets you set an association.
         * 
         * If the object already had an association with that name,
         * the old association will be destroyed.
         * 
         * Internally, the `key` is converted to a {@link GLib.Quark} using `g_quark_from_string()`.
         * This means a copy of `key` is kept permanently (even after `object` has been
         * finalized) — so it is recommended to only use a small, bounded set of values
         * for `key` in your program, to avoid the {@link GLib.Quark} storage growing unbounded.
         * @param key name of the key
         * @param data data to associate with that key
         */
        set_data(key: string, data: (any | null)): void;

        /**
         * @param args 
         */
        // Conflicted with Ags.FileLink.set_data
        set_data(...args: never[]): any;
    }


    namespace AudioFileManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio_file: any;
            audioFile: any;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioFileManager extends GObject.Object {
        static $gtype: GObject.GType<AudioFileManager>;

        // Properties
        /**
         * The {@link GLib.List}-struct containing {@link AgsAudio.AudioFile}.
         * @since 3.4.0
         */
        get audio_file(): any;
        set audio_file(val: any);

        /**
         * The {@link GLib.List}-struct containing {@link AgsAudio.AudioFile}.
         * @since 3.4.0
         */
        get audioFile(): any;
        set audioFile(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioFileManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<AudioFileManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AudioFileManager;

        // Signals
        /** @signal */
        connect<K extends keyof AudioFileManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioFileManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof AudioFileManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioFileManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioFileManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioFileManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get ags audio file manager instance.
         */
        static get_instance(): AudioFileManager;

        // Methods
        /**
         * Add `audio_file` to `audio_file_manager`.
         * @param audio_file the {@link AgsAudio.AudioFile}
         */
        add_audio_file(audio_file: GObject.Object): void;

        /**
         * Find audio file by `filename`.
         * @param filename the string identifier
         * @returns the matching {@link AgsAudio.AudioFile} if found, otherwise `null`
         */
        find_audio_file<T = GObject.Object>(filename: string): T;

        /**
         * Remove `audio_file` from `audio_file_manager`.
         * @param audio_file the {@link AgsAudio.AudioFile}
         */
        remove_audio_file(audio_file: GObject.Object): void;
    }


    namespace AudioLoop {
        // Signal signatures
        interface SignalSignatures extends Ags.Thread.SignalSignatures {
            "notify::play-audio": (pspec: GObject.ParamSpec) => void;
            "notify::play-channel": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::max-precision": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Thread.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.MainLoop.ConstructorProps {
            play_audio: PlaybackDomain[];
            playAudio: PlaybackDomain[];
            play_channel: Playback[];
            playChannel: Playback[];
        }
    }

    /**
     * @gir-type Class
     */
    class AudioLoop extends Ags.Thread implements Ags.Connectable, Ags.MainLoop {
        static $gtype: GObject.GType<AudioLoop>;

        // Properties
        /**
         * An {@link AgsAudio.Audio} to add for playback.
         * @since 3.0.0
         * @write-only
         */
        set play_audio(val: PlaybackDomain[]);

        /**
         * An {@link AgsAudio.Audio} to add for playback.
         * @since 3.0.0
         * @write-only
         */
        set playAudio(val: PlaybackDomain[]);

        /**
         * An {@link AgsAudio.Channel} to add for playback.
         * @since 3.0.0
         * @write-only
         */
        set play_channel(val: Playback[]);

        /**
         * An {@link AgsAudio.Channel} to add for playback.
         * @since 3.0.0
         * @write-only
         */
        set playChannel(val: Playback[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioLoop.SignalSignatures;

        // Fields
        thread: Ags.Thread;

        flags: AudioLoopFlags;

        
    // This field conflicts with a function in a parent class or interface.
    is_syncing: (boolean | any);

        
    // This field conflicts with a function in a parent class or interface.
    is_critical_region: (boolean | any);

        critical_region_ref: number;

        play_channel_ref: number;

        play_audio_ref: number;

        sync_thread: any[];

        do_fx_staging: boolean;

        staging_program: number;

        staging_program_count: number;

        // Constructors
        constructor(properties?: Partial<AudioLoop.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AudioLoop;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioLoop.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioLoop.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioLoop.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioLoop.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Add audio for playback.
         * @param audio an {@link AgsAudio.Audio}
         */
        add_audio(audio: GObject.Object): void;

        /**
         * Add channel for playback.
         * @param channel an {@link AgsAudio.Channel}
         */
        add_channel(channel: GObject.Object): void;

        /**
         * Get do fx staging.
         * @returns `true` if set, otherwise `false`
         */
        get_do_fx_staging(): boolean;

        /**
         * Get staging program.
         * @returns the staging program
         */
        get_staging_program(): [number, number];

        /**
         * Remove audio of playback.
         * @param audio an {@link AgsAudio.Audio}
         */
        remove_audio(audio: GObject.Object): void;

        /**
         * Remove channel of playback.
         * @param channel an {@link AgsAudio.Channel}
         */
        remove_channel(channel: GObject.Object): void;

        /**
         * Set do fx staging.
         * @param do_fx_staging `true` if do fx staging, else `false`
         */
        set_do_fx_staging(do_fx_staging: boolean): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: AudioLoopFlags): void;

        /**
         * @param args 
         */
    // Conflicted with Ags.Thread.set_flags
        set_flags(...args: never[]): any;

        /**
         * Set staging program.
         * @param staging_program the staging program
         * @param staging_program_count the staging program count
         */
        set_staging_program(staging_program: number, staging_program_count: number): void;

        /**
         * Test `flags` to be set on `audio_loop`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AudioLoopFlags): boolean;

        /**
         * @param args 
         */
    // Conflicted with Ags.Thread.test_flags
        test_flags(...args: never[]): any;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: AudioLoopFlags): void;

        /**
         * @param args 
         */
    // Conflicted with Ags.Thread.unset_flags
        unset_flags(...args: never[]): any;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Change frequency.
         * @param frequency the new frequency
         */
        change_frequency(frequency: number): void;

        /**
         * Decrement thread needs access to main loop's critical region field.
         */
        dec_queued_critical_region(): void;

        /**
         * Retrieve the tree mutex.
         * @returns the mutex
         */
        get_tree_lock(): GLib.RecMutex;

        /**
         * Increment thread needs access to main loop's critical region field.
         */
        inc_queued_critical_region(): void;

        /**
         * Set main loop is in critical region.
         * @param is_critical_region set `true` if critical region
         */
        set_critical_region(is_critical_region: boolean): void;

        /**
         * Set thread tree is syncing.
         * @param is_syncing set `true` if syncing
         */
        set_syncing(is_syncing: boolean): void;

        /**
         * Test main loop may enter critical region.
         * @returns 0 if main loop may enter critical region, otherwise not
         */
        test_queued_critical_region(): number;

        /**
         * Change frequency.
         * @param frequency the new frequency
         * @virtual
         */
        vfunc_change_frequency(frequency: number): void;

        /**
         * Decrement thread needs access to main loop's critical region field.
         * @virtual
         */
        vfunc_dec_queued_critical_region(): void;

        /**
         * Retrieve the tree mutex.
         * @virtual
         */
        vfunc_get_tree_lock(): GLib.RecMutex;

        /**
         * Increment thread needs access to main loop's critical region field.
         * @virtual
         */
        vfunc_inc_queued_critical_region(): void;

        /**
         * Check if main loop is in critical region.
         * @virtual
         */
        vfunc_is_critical_region(): boolean;

        /**
         * Check if thread tree is syncing.
         * @virtual
         */
        vfunc_is_syncing(): boolean;

        /**
         * Set main loop is in critical region.
         * @param is_critical_region set `true` if critical region
         * @virtual
         */
        vfunc_set_critical_region(is_critical_region: boolean): void;

        /**
         * Set thread tree is syncing.
         * @param is_syncing set `true` if syncing
         * @virtual
         */
        vfunc_set_syncing(is_syncing: boolean): void;

        /**
         * Test main loop may enter critical region.
         * @virtual
         */
        vfunc_test_queued_critical_region(): number;
    }


    namespace AudioSignal {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::add-note signal notifies about adding `note`.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "add-note": (arg0: GObject.Object) => void;
            /**
             * The ::refresh-data signal notifies about requesting to refresh data.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "refresh-data": () => void;
            /**
             * The ::remove-note signal notifies about removing `note`.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "remove-note": (arg0: GObject.Object) => void;
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::damping": (pspec: GObject.ParamSpec) => void;
            "notify::default-template": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::first-frame": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::last-frame": (pspec: GObject.ParamSpec) => void;
            "notify::length": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::note": (pspec: GObject.ParamSpec) => void;
            "notify::note-256th-attack": (pspec: GObject.ParamSpec) => void;
            "notify::note256th-attack": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::recycling": (pspec: GObject.ParamSpec) => void;
            "notify::rt-template": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::stream": (pspec: GObject.ParamSpec) => void;
            "notify::stream-current": (pspec: GObject.ParamSpec) => void;
            "notify::stream-end": (pspec: GObject.ParamSpec) => void;
            "notify::timbre-end": (pspec: GObject.ParamSpec) => void;
            "notify::timbre-start": (pspec: GObject.ParamSpec) => void;
            "notify::vibration": (pspec: GObject.ParamSpec) => void;
            "notify::word-size": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            attack: number;
            buffer_size: number;
            bufferSize: number;
            damping: Ags.Complex;
            default_template: AudioSignal;
            defaultTemplate: AudioSignal;
            delay: number;
            first_frame: number;
            firstFrame: number;
            format: number;
            frame_count: number;
            frameCount: number;
            input_soundcard: GObject.Object;
            inputSoundcard: GObject.Object;
            input_soundcard_channel: number;
            inputSoundcardChannel: number;
            last_frame: number;
            lastFrame: number;
            length: number;
            loop_end: number;
            loopEnd: number;
            loop_start: number;
            loopStart: number;
            note: Note[];
            note_256th_attack: number;
            note256thAttack: number;
            output_soundcard: GObject.Object;
            outputSoundcard: GObject.Object;
            output_soundcard_channel: number;
            outputSoundcardChannel: number;
            recall_id: RecallID;
            recallId: RecallID;
            recycling: Recycling;
            rt_template: AudioSignal;
            rtTemplate: AudioSignal;
            samplerate: number;
            stream: any;
            stream_current: any;
            streamCurrent: any;
            stream_end: any;
            streamEnd: any;
            timbre_end: number;
            timbreEnd: number;
            timbre_start: number;
            timbreStart: number;
            vibration: Ags.Complex;
            word_size: number;
            wordSize: number;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioSignal extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<AudioSignal>;

        // Properties
        /**
         * The attack to be used.
         * @since 3.0.0
         * @default 0
         */
        get attack(): number;
        set attack(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * Damping of timbre.
         * @since 3.0.0
         */
        get damping(): Ags.Complex;
        set damping(val: Ags.Complex);

        /**
         * The assigned {@link AgsAudio.AudioSignal} default template.
         * @since 5.0.0
         */
        get default_template(): AudioSignal;
        set default_template(val: AudioSignal);

        /**
         * The assigned {@link AgsAudio.AudioSignal} default template.
         * @since 5.0.0
         */
        get defaultTemplate(): AudioSignal;
        set defaultTemplate(val: AudioSignal);

        /**
         * The delay to be used.
         * @since 3.0.0
         * @default 0
         */
        get delay(): number;
        set delay(val: number);

        /**
         * The first frame of stream.
         * @since 3.0.0
         * @default 0
         */
        get first_frame(): number;
        set first_frame(val: number);

        /**
         * The first frame of stream.
         * @since 3.0.0
         * @default 0
         */
        get firstFrame(): number;
        set firstFrame(val: number);

        /**
         * The format to be used.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The initial size of audio data.
         * @since 3.0.0
         * @default 0
         */
        get frame_count(): number;
        set frame_count(val: number);

        /**
         * The initial size of audio data.
         * @since 3.0.0
         * @default 0
         */
        get frameCount(): number;
        set frameCount(val: number);

        /**
         * The assigned input {@link Ags.Soundcard}.
         * @since 3.0.0
         */
        get input_soundcard(): GObject.Object;
        set input_soundcard(val: GObject.Object);

        /**
         * The assigned input {@link Ags.Soundcard}.
         * @since 3.0.0
         */
        get inputSoundcard(): GObject.Object;
        set inputSoundcard(val: GObject.Object);

        /**
         * The input soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get input_soundcard_channel(): number;
        set input_soundcard_channel(val: number);

        /**
         * The input soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get inputSoundcardChannel(): number;
        set inputSoundcardChannel(val: number);

        /**
         * The last frame of stream.
         * @since 3.0.0
         * @default 0
         */
        get last_frame(): number;
        set last_frame(val: number);

        /**
         * The last frame of stream.
         * @since 3.0.0
         * @default 0
         */
        get lastFrame(): number;
        set lastFrame(val: number);

        /**
         * The length of the stream.
         * @since 3.0.0
         * @default 0
         */
        get length(): number;
        set length(val: number);

        /**
         * The loop end of stream.
         * @since 3.0.0
         * @default 0
         */
        get loop_end(): number;
        set loop_end(val: number);

        /**
         * The loop end of stream.
         * @since 3.0.0
         * @default 0
         */
        get loopEnd(): number;
        set loopEnd(val: number);

        /**
         * The loop start of stream.
         * @since 3.0.0
         * @default 0
         */
        get loop_start(): number;
        set loop_start(val: number);

        /**
         * The loop start of stream.
         * @since 3.0.0
         * @default 0
         */
        get loopStart(): number;
        set loopStart(val: number);

        /**
         * The assigned {@link AgsAudio.Note} providing default settings.
         * @since 3.0.0
         */
        get note(): Note[];
        set note(val: Note[]);

        /**
         * The note 256th attack to be used.
         * @since 6.2.0
         * @default 0
         */
        get note_256th_attack(): number;
        set note_256th_attack(val: number);

        /**
         * The note 256th attack to be used.
         * @since 6.2.0
         * @default 0
         */
        get note256thAttack(): number;
        set note256thAttack(val: number);

        /**
         * The assigned output {@link Ags.Soundcard} providing default settings.
         * @since 3.0.0
         */
        get output_soundcard(): GObject.Object;
        set output_soundcard(val: GObject.Object);

        /**
         * The assigned output {@link Ags.Soundcard} providing default settings.
         * @since 3.0.0
         */
        get outputSoundcard(): GObject.Object;
        set outputSoundcard(val: GObject.Object);

        /**
         * The output soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get output_soundcard_channel(): number;
        set output_soundcard_channel(val: number);

        /**
         * The output soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get outputSoundcardChannel(): number;
        set outputSoundcardChannel(val: number);

        /**
         * The assigned {@link AgsAudio.RecallID} providing context.
         * @since 3.0.0
         */
        get recall_id(): RecallID;
        set recall_id(val: RecallID);

        /**
         * The assigned {@link AgsAudio.RecallID} providing context.
         * @since 3.0.0
         */
        get recallId(): RecallID;
        set recallId(val: RecallID);

        /**
         * The assigned {@link AgsAudio.Recycling} linking tree.
         * @since 3.0.0
         */
        get recycling(): Recycling;
        set recycling(val: Recycling);

        /**
         * The assigned {@link AgsAudio.AudioSignal} realtime template.
         * @since 3.0.0
         */
        get rt_template(): AudioSignal;
        set rt_template(val: AudioSignal);

        /**
         * The assigned {@link AgsAudio.AudioSignal} realtime template.
         * @since 3.0.0
         */
        get rtTemplate(): AudioSignal;
        set rtTemplate(val: AudioSignal);

        /**
         * The samplerate to be used.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The stream it contains.
         * @since 3.0.0
         * @read-only
         */
        get stream(): any;

        /**
         * The current stream.
         * @since 3.0.0
         * @read-only
         */
        get stream_current(): any;

        /**
         * The current stream.
         * @since 3.0.0
         * @read-only
         */
        get streamCurrent(): any;

        /**
         * The end of stream.
         * @since 3.0.0
         * @read-only
         */
        get stream_end(): any;

        /**
         * The end of stream.
         * @since 3.0.0
         * @read-only
         */
        get streamEnd(): any;

        /**
         * The timbre's end frame.
         * @since 3.0.0
         * @default 0
         */
        get timbre_end(): number;
        set timbre_end(val: number);

        /**
         * The timbre's end frame.
         * @since 3.0.0
         * @default 0
         */
        get timbreEnd(): number;
        set timbreEnd(val: number);

        /**
         * The timbre's start frame.
         * @since 3.0.0
         * @default 0
         */
        get timbre_start(): number;
        set timbre_start(val: number);

        /**
         * The timbre's start frame.
         * @since 3.0.0
         * @default 0
         */
        get timbreStart(): number;
        set timbreStart(val: number);

        /**
         * Vibration of timbre.
         * @since 3.0.0
         */
        get vibration(): Ags.Complex;
        set vibration(val: Ags.Complex);

        /**
         * The word size of frame.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get word_size(): number;

        /**
         * The word size of frame.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get wordSize(): number;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioSignal.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AudioSignalFlags;

        connectable_flags: Ags.ConnectableFlags;

        key_format: SoundKeyFormat;

        uuid: Ags.UUID;

        stream_mode: AudioSignalStreamMode;

        has_backlog: boolean;

        // Constructors
        constructor(properties?: Partial<AudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](output_soundcard: GObject.Object, recycling: GObject.Object, recall_id: GObject.Object): AudioSignal;

        static new_with_length(output_soundcard: GObject.Object, recycling: GObject.Object, recall_id: GObject.Object, length: number): AudioSignal;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Retrieve next audio signal refering to `recall_id`
         * @param audio_signal a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         * @param recall_id matching {@link AgsAudio.RecallID}
         */
        static find_by_recall_id(audio_signal: AudioSignal[], recall_id: GObject.Object): AudioSignal[];

        /**
         * Retrieve next current stream of {@link AgsAudio.AudioSignal} list. Warning this function does not
         * lock the stream mutex.
         * @param audio_signal the {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         * @param recall_id the matching {@link AgsAudio.RecallID}
         */
        static find_stream_current(audio_signal: AudioSignal[], recall_id: GObject.Object): AudioSignal[];

        /**
         * Retrieve the default template audio signal.
         * @param audio_signal the {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        static get_default_template(audio_signal: AudioSignal[]): AudioSignal;

        /**
         * Retrieve the realtime template audio signal.
         * @param audio_signal a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        static get_rt_template(audio_signal: AudioSignal[]): AudioSignal[];

        /**
         * Check if is active.
         * @param audio_signal the {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        static is_active(audio_signal: AudioSignal[], recall_id: GObject.Object): boolean;

        // Virtual methods
        /**
         * Add note to `audio_signal`.
         * @param note the {@link AgsAudio.Note}
         * @virtual
         */
        vfunc_add_note(note: GObject.Object): void;

        /**
         * @virtual
         */
        vfunc_refresh_data(): void;

        /**
         * Remove note from `audio_signal`.
         * @param note the {@link AgsAudio.Note}
         * @virtual
         */
        vfunc_remove_note(note: GObject.Object): void;

        // Methods
        /**
         * Add note to `audio_signal`.
         * @param note the {@link AgsAudio.Note}
         */
        add_note(note: GObject.Object): void;

        /**
         * Adds a buffer at the end of the stream.
         */
        add_stream(): void;

        /**
         * Clear `audio_signal`.
         */
        clear(): void;

        /**
         * Feed audio signal to grow upto frame count.
         * @param default_template the default template {@link AgsAudio.AudioSignal}
         * @param frame_count the new frame count
         * @param old_frame_count the old frame count
         */
        close_feed(default_template: AudioSignal, frame_count: number, old_frame_count: number): void;

        /**
         * Check if `audio_signal` contains `note`.
         * @param note the {@link AgsAudio.Note}
         * @returns `true` on success, otherwise `false`
         */
        contains_note(note: Note): boolean;

        /**
         * Feed audio signal to grow upto frame count.
         * @param default_template the default template {@link AgsAudio.AudioSignal}
         * @param frame_count the new frame count
         * @param old_frame_count the old frame count
         */
        continue_feed(default_template: AudioSignal, frame_count: number, old_frame_count: number): void;

        /**
         * Apply `default_template` audio data to `audio_signal`. Note should only be invoked
         * by proper recall context because only the stream of `default_template` is locked.
         * @param default_template the default template {@link AgsAudio.AudioSignal}
         */
        duplicate_stream(default_template: AudioSignal): void;

        /**
         * Feed audio signal to grow upto frame count.
         * @param default_template the default template {@link AgsAudio.AudioSignal}
         * @param frame_count the new frame count
         */
        feed(default_template: AudioSignal, frame_count: number): void;

        /**
         * Feed audio signal to grow upto frame count.
         * @param default_template the default template {@link AgsAudio.AudioSignal}
         * @param frame_count the new frame count
         * @param old_frame_count the old frame count
         * @param do_open open feed
         * @param do_close close feed
         */
        feed_extended(default_template: AudioSignal, frame_count: number, old_frame_count: number, do_open: boolean, do_close: boolean): void;

        /**
         * Get attack of `audio_signal`.
         * @returns the attack
         */
        get_attack(): number;

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get the input soundcard object of `audio_signal`.
         * @returns the input soundcard
         */
        get_input_soundcard<T = GObject.Object>(): T;

        /**
         * Counts the buffers from :stream upto :stream-current.
         * @returns the counted length.
         */
        get_length_till_current(): number;

        /**
         * Get note.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Note}
         */
        get_note(): Note[];

        /**
         * Get note 256th attack of `audio_signal`.
         * @returns the note 256th attack
         */
        get_note_256th_attack(): number;

        /**
         * Get the output soundcard object of `audio_signal`.
         * @returns the output soundcard
         */
        get_output_soundcard<T = GObject.Object>(): T;

        /**
         * Get recycling.
         * @returns the {@link AgsAudio.Recycling}
         */
        get_recycling<T = GObject.Object>(): T;

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get stream.
         * @returns the {@link GLib.List}-struct containig audio data
         */
        get_stream(): Uint8Array;

        /**
         * Feed audio signal to grow upto frame count.
         * @param default_template the default template {@link AgsAudio.AudioSignal}
         * @param frame_count the new frame count
         * @param old_frame_count the old frame count
         */
        open_feed(default_template: AudioSignal, frame_count: number, old_frame_count: number): void;

        refresh_data(): void;

        /**
         * Remove note from `audio_signal`.
         * @param note the {@link AgsAudio.Note}
         */
        remove_note(note: GObject.Object): void;

        /**
         * Set `attack` of `audio_signal`.
         * @param attack the attack
         */
        set_attack(attack: number): void;

        /**
         * Set buffer size.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Enable a feature of `audio_signal`.
         * @param flags see {@link AgsAudio.AudioSignalFlags}-enum
         */
        set_flags(flags: AudioSignalFlags): void;

        /**
         * Set format.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set the input soundcard object of `audio_signal`.
         * @param input_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_input_soundcard(input_soundcard: GObject.Object): void;

        /**
         * Set `key_format` on `audio_signal`.
         * @param key_format the key format
         */
        set_key_format(key_format: SoundKeyFormat): void;

        /**
         * Set note by replacing existing.
         * @param note the {@link GLib.List}-struct containing {@link AgsAudio.Note}
         */
        set_note(note: Note[]): void;

        /**
         * Set `note_256th_attack` of `audio_signal`.
         * @param note_256th_attack the note 256th attack
         */
        set_note_256th_attack(note_256th_attack: number): void;

        /**
         * Set the output soundcard object of `audio_signal`.
         * @param output_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_output_soundcard(output_soundcard: GObject.Object): void;

        /**
         * Set recycling.
         * @param recycling the {@link AgsAudio.Recycling}
         */
        set_recycling(recycling: GObject.Object): void;

        /**
         * Set samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set stream by replacing existing.
         * @param stream the {@link GLib.List}-struct containing audio data
         */
        set_stream(stream: (Uint8Array | string)): void;

        /**
         * Set stream mode of `audio_signal`.
         * @param stream_mode see {@link AgsAudio.AudioSignalStreamMode}-enum
         */
        set_stream_mode(stream_mode: AudioSignalStreamMode): void;

        /**
         * Lock stream mutex.
         */
        stream_lock(): void;

        /**
         * Resize stream of `audio_signal` to `length` number of buffers.
         * @param length the new length
         */
        stream_resize(length: number): void;

        /**
         * Resize stream of `audio_signal` to `length` number of buffers. But doesn't shrink
         * more than the current stream position.
         * @param length the new length
         */
        stream_safe_resize(length: number): void;

        /**
         * Unlock stream mutex.
         */
        stream_unlock(): void;

        /**
         * Test `flags` to be set on `audio_signal`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AudioSignalFlags): boolean;

        /**
         * Test `key_format` to be set on `audio_signal`.
         * @param key_format the key format
         * @returns `true` if key format is set, else `false`
         */
        test_key_format(key_format: SoundKeyFormat): boolean;

        /**
         * Test `stream_mode` to be set on `audio_signal`.
         * @param stream_mode the stream mode
         * @returns `true` if stream mode are set, else `false`
         */
        test_stream_mode(stream_mode: AudioSignalStreamMode): boolean;

        /**
         * Disable a feature of `audio_signal`.
         * @param flags see {@link AgsAudio.AudioSignalFlags}-enum
         */
        unset_flags(flags: AudioSignalFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace AudioThread {
        // Signal signatures
        interface SignalSignatures extends Ags.Thread.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::default-output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::processing": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::max-precision": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Thread.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio: Audio;
            default_output_soundcard: GObject.Object;
            defaultOutputSoundcard: GObject.Object;
            processing: boolean;
            task_launcher: Ags.TaskLauncher;
            taskLauncher: Ags.TaskLauncher;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioThread extends Ags.Thread implements Ags.Connectable {
        static $gtype: GObject.GType<AudioThread>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}.
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The assigned default soundcard.
         * @since 3.0.0
         */
        get default_output_soundcard(): GObject.Object;
        set default_output_soundcard(val: GObject.Object);

        /**
         * The assigned default soundcard.
         * @since 3.0.0
         */
        get defaultOutputSoundcard(): GObject.Object;
        set defaultOutputSoundcard(val: GObject.Object);

        /**
         * The processing state.
         * @since 3.11.0
         * @default false
         */
        get processing(): boolean;
        set processing(val: boolean);

        /**
         * The assigned {@link Ags.TaskLauncher}.
         * @since 3.11.0
         * @read-only
         */
        get task_launcher(): Ags.TaskLauncher;

        /**
         * The assigned {@link Ags.TaskLauncher}.
         * @since 3.11.0
         * @read-only
         */
        get taskLauncher(): Ags.TaskLauncher;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioThread.SignalSignatures;

        // Fields
        thread: Ags.Thread;

        nested_sync_flags: AudioThreadNestedSyncFlags;

        sound_scope: number;

        sync_thread: any[];

        do_fx_staging: boolean;

        staging_program: number;

        staging_program_count: number;

        scope_data: AudioThreadScopeData[];

        // Constructors
        constructor(properties?: Partial<AudioThread.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](default_output_soundcard: GObject.Object, audio: GObject.Object): AudioThread;

        // Conflicted with Ags.Thread.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioThread.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioThread.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioThread.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get do fx staging.
         * @returns `true` if set, otherwise `false`
         */
        get_do_fx_staging(): boolean;

        /**
         * Get processing.
         * @returns `true` if do processing, otherwise `false`
         */
        get_processing(): boolean;

        /**
         * Get staging program.
         * @returns the staging program
         */
        get_staging_program(): [number, number];

        /**
         * Get task launcher.
         * @returns the {@link Ags.TaskLauncher}
         */
        get_task_launcher(): Ags.TaskLauncher;

        /**
         * Set do fx staging.
         * @param do_fx_staging `true` if do fx staging, else `false`
         */
        set_do_fx_staging(do_fx_staging: boolean): void;

        /**
         * Set status flags.
         * @param nested_sync_flags status flags
         */
        set_nested_sync_flags(nested_sync_flags: AudioThreadNestedSyncFlags): void;

        /**
         * Set processing.
         * @param processing `true` enables and `false` disables processing
         */
        set_processing(processing: boolean): void;

        /**
         * Set sound scope.
         * @param sound_scope the sound scope
         */
        set_sound_scope(sound_scope: number): void;

        /**
         * Set staging program.
         * @param staging_program the staging program
         * @param staging_program_count the staging program count
         */
        set_staging_program(staging_program: number, staging_program_count: number): void;

        /**
         * Test `nested_sync_flags` of `audio_thread`.
         * @param nested_sync_flags status flags
         * @returns `true` if status flags set, otherwise `false`
         */
        test_nested_sync_flags(nested_sync_flags: AudioThreadNestedSyncFlags): boolean;

        /**
         * Unset status flags.
         * @param nested_sync_flags status flags
         */
        unset_nested_sync_flags(nested_sync_flags: AudioThreadNestedSyncFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace AudioUnitClient {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-unit-server": (pspec: GObject.ParamSpec) => void;
            "notify::client-name": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio_unit_server: AudioUnitServer;
            audioUnitServer: AudioUnitServer;
            client_name: string;
            clientName: string;
            device: GObject.Object[];
            port: AudioUnitPort[];
        }
    }

    /**
     * @gir-type Class
     */
    class AudioUnitClient extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<AudioUnitClient>;

        // Properties
        /**
         * The assigned {@link AgsAudio.AudioUnitServer}.
         * @since 3.0.0
         */
        get audio_unit_server(): AudioUnitServer;
        set audio_unit_server(val: AudioUnitServer);

        /**
         * The assigned {@link AgsAudio.AudioUnitServer}.
         * @since 3.0.0
         */
        get audioUnitServer(): AudioUnitServer;
        set audioUnitServer(val: AudioUnitServer);

        /**
         * The audio-unit client name.
         * @since 3.0.0
         * @default null
         */
        get client_name(): string;
        set client_name(val: string);

        /**
         * The audio-unit client name.
         * @since 3.0.0
         * @default null
         */
        get clientName(): string;
        set clientName(val: string);

        /**
         * The assigned devices.
         * @since 3.0.0
         */
        get device(): GObject.Object[];
        set device(val: GObject.Object[]);

        /**
         * The assigned ports.
         * @since 3.0.0
         */
        get port(): AudioUnitPort[];
        set port(val: AudioUnitPort[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioUnitClient.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AudioUnitClientFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        client_uuid: string;

        // Constructors
        constructor(properties?: Partial<AudioUnitClient.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_unit_server: GObject.Object): AudioUnitClient;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioUnitClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioUnitClient.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioUnitClient.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioUnitClient.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `client_name` in `audio_unit_client`.
         * @param audio_unit_client the {@link GLib.List}-struct containing {@link AgsAudio.AudioUnitClient}
         * @param client_name the client name to find
         */
        static find(audio_unit_client: AudioUnitClient[], client_name: string): AudioUnitClient[];

        /**
         * Finds next match of `client_uuid` in `audio_unit_client`.
         * @param audio_unit_client the {@link GLib.List}-struct containing {@link AgsAudio.AudioUnitClient}
         * @param client_uuid the client uuid to find
         */
        static find_uuid(audio_unit_client: AudioUnitClient[], client_uuid: string): AudioUnitClient[];

        // Methods
        /**
         * Activate client.
         */
        activate(): void;

        /**
         * Add `audio_unit_device` to `audio_unit_client`.
         * @param audio_unit_device an {@link AgsAudio.AudioUnitDevout} or {@link AgsAudio.AudioUnitDevin}
         */
        add_device(audio_unit_device: GObject.Object): void;

        /**
         * Add `audio_unit_port` to `audio_unit_client`.
         * @param audio_unit_port an {@link AgsAudio.AudioUnitPort}
         */
        add_port(audio_unit_port: GObject.Object): void;

        /**
         * Deactivate client.
         */
        deactivate(): void;

        /**
         * Open the core audio client's connection and read uuid.
         * @param client_name the client's name
         */
        open(client_name: string): void;

        /**
         * Remove `audio_unit_device` from `audio_unit_client`.
         * @param audio_unit_device an {@link AgsAudio.AudioUnitDevout} or {@link AgsAudio.AudioUnitDevin}
         */
        remove_device(audio_unit_device: GObject.Object): void;

        /**
         * Remove `audio_unit_port` from `audio_unit_client`.
         * @param audio_unit_port an {@link AgsAudio.AudioUnitPort}
         */
        remove_port(audio_unit_port: GObject.Object): void;

        /**
         * Enable a feature of `audio_unit_client`.
         * @param flags see {@link AgsAudio.AudioUnitClientFlags}-enum
         */
        set_flags(flags: AudioUnitClientFlags): void;

        /**
         * Test `flags` to be set on `audio_unit_client`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AudioUnitClientFlags): boolean;

        /**
         * Disable a feature of `audio_unit_client`.
         * @param flags see {@link AgsAudio.AudioUnitClientFlags}-enum
         */
        unset_flags(flags: AudioUnitClientFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace AudioUnitDevin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::audio-unit-client": (pspec: GObject.ParamSpec) => void;
            "notify::audio-unit-port": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            audio_unit_client: AudioUnitClient;
            audioUnitClient: AudioUnitClient;
            audio_unit_port: any;
            audioUnitPort: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioUnitDevin extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<AudioUnitDevin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * The assigned {@link AgsAudio.AudioUnitClient}
         * @since 3.0.0
         */
        get audio_unit_client(): AudioUnitClient;
        set audio_unit_client(val: AudioUnitClient);

        /**
         * The assigned {@link AgsAudio.AudioUnitClient}
         * @since 3.0.0
         */
        get audioUnitClient(): AudioUnitClient;
        set audioUnitClient(val: AudioUnitClient);

        /**
         * The assigned {@link AgsAudio.AudioUnitPort}
         * @since 3.0.0
         */
        get audio_unit_port(): any;
        set audio_unit_port(val: any);

        /**
         * The assigned {@link AgsAudio.AudioUnitPort}
         * @since 3.0.0
         */
        get audioUnitPort(): any;
        set audioUnitPort(val: any);

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The audio unit soundcard indentifier
         * @since 3.0.0
         * @default ags-audio-unit-devin-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 44100
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioUnitDevin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AudioUnitDevinFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: AudioUnitDevinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: AudioUnitDevinAppBufferMode;

        app_buffer: any;

        sub_block_count: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        notify_soundcard: GObject.Object;

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_tic_size: number;

        // Constructors
        constructor(properties?: Partial<AudioUnitDevin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AudioUnitDevin;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioUnitDevin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioUnitDevin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioUnitDevin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioUnitDevin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `audio_unit_devin`.
         * @param flags see {@link AgsAudio.AudioUnitDevinFlags}-enum
         */
        set_flags(flags: number): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `audio_unit_devin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Disable a feature of `audio_unit_devin`.
         * @param flags see {@link AgsAudio.AudioUnitDevinFlags}-enum
         */
        unset_flags(flags: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace AudioUnitDevout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::audio-unit-client": (pspec: GObject.ParamSpec) => void;
            "notify::audio-unit-port": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            audio_unit_client: AudioUnitClient;
            audioUnitClient: AudioUnitClient;
            audio_unit_port: any;
            audioUnitPort: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioUnitDevout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<AudioUnitDevout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * The assigned {@link AgsAudio.AudioUnitClient}
         * @since 3.0.0
         */
        get audio_unit_client(): AudioUnitClient;
        set audio_unit_client(val: AudioUnitClient);

        /**
         * The assigned {@link AgsAudio.AudioUnitClient}
         * @since 3.0.0
         */
        get audioUnitClient(): AudioUnitClient;
        set audioUnitClient(val: AudioUnitClient);

        /**
         * The assigned {@link AgsAudio.AudioUnitPort}
         * @since 3.0.0
         */
        get audio_unit_port(): any;
        set audio_unit_port(val: any);

        /**
         * The assigned {@link AgsAudio.AudioUnitPort}
         * @since 3.0.0
         */
        get audioUnitPort(): any;
        set audioUnitPort(val: any);

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The audio unit soundcard indentifier
         * @since 3.0.0
         * @default ags-audio-unit-devout-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 44100
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioUnitDevout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AudioUnitDevoutFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: AudioUnitDevoutSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: AudioUnitDevoutAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        notify_soundcard: GObject.Object;

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_tic_size: number;

        // Constructors
        constructor(properties?: Partial<AudioUnitDevout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AudioUnitDevout;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioUnitDevout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioUnitDevout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioUnitDevout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioUnitDevout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `audio_unit_devout`.
         * @param flags see {@link AgsAudio.AudioUnitDevoutFlags}-enum
         */
        set_flags(flags: AudioUnitDevoutFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `audio_unit_devout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AudioUnitDevoutFlags): boolean;

        /**
         * Disable a feature of `audio_unit_devout`.
         * @param flags see {@link AgsAudio.AudioUnitDevoutFlags}-enum
         */
        unset_flags(flags: AudioUnitDevoutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace AudioUnitPort {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-unit-client": (pspec: GObject.ParamSpec) => void;
            "notify::audio-unit-device": (pspec: GObject.ParamSpec) => void;
            "notify::port-name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio_unit_client: AudioUnitClient;
            audioUnitClient: AudioUnitClient;
            audio_unit_device: GObject.Object;
            audioUnitDevice: GObject.Object;
            port_name: string;
            portName: string;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioUnitPort extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<AudioUnitPort>;

        // Properties
        /**
         * The assigned {@link AgsAudio.AudioUnitClient}.
         * @since 3.0.0
         */
        get audio_unit_client(): AudioUnitClient;
        set audio_unit_client(val: AudioUnitClient);

        /**
         * The assigned {@link AgsAudio.AudioUnitClient}.
         * @since 3.0.0
         */
        get audioUnitClient(): AudioUnitClient;
        set audioUnitClient(val: AudioUnitClient);

        /**
         * The assigned {@link AgsAudio.AudioUnitDevout}.
         * @since 3.0.0
         */
        get audio_unit_device(): GObject.Object;
        set audio_unit_device(val: GObject.Object);

        /**
         * The assigned {@link AgsAudio.AudioUnitDevout}.
         * @since 3.0.0
         */
        get audioUnitDevice(): GObject.Object;
        set audioUnitDevice(val: GObject.Object);

        /**
         * The core audio soundcard indentifier
         * @since 3.0.0
         * @default hw:0
         */
        get port_name(): string;
        set port_name(val: string);

        /**
         * The core audio soundcard indentifier
         * @since 3.0.0
         * @default hw:0
         */
        get portName(): string;
        set portName(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioUnitPort.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AudioUnitPortFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        port_uuid: string;

        pcm_channels: number;

        samplerate: number;

        buffer_size: number;

        format: Ags.SoundcardFormat;

        data_format: any;

        graph: any;

        description: any;

        node: any;

        audio_component: any;

        audio_unit: any;

        render_callback: any;

        midi_port_number: number;

        output_running: boolean;

        input_running: boolean;

        is_empty: boolean;

        queued: number;

        // Constructors
        constructor(properties?: Partial<AudioUnitPort.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_unit_client: GObject.Object): AudioUnitPort;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioUnitPort.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioUnitPort.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioUnitPort.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioUnitPort.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `port_name` in `audio_unit_port`.
         * @param audio_unit_port the {@link GLib.List}-struct containing {@link AgsAudio.AudioUnitPort}
         * @param port_name the port name to find
         */
        static find(audio_unit_port: AudioUnitPort[], port_name: string): AudioUnitPort[];

        // Methods
        /**
         * @param port_name 
         * @param is_audio 
         * @param is_midi 
         * @param is_output 
         */
        register(port_name: string, is_audio: boolean, is_midi: boolean, is_output: boolean): void;

        /**
         * @param buffer_size 
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Enable a feature of `audio_unit_port`.
         * @param flags see {@link AgsAudio.AudioUnitPortFlags}-enum
         */
        set_flags(flags: AudioUnitPortFlags): void;

        /**
         * @param format 
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * @param pcm_channels 
         */
        set_pcm_channels(pcm_channels: number): void;

        /**
         * @param samplerate 
         */
        set_samplerate(samplerate: number): void;

        /**
         * Test `flags` to be set on `audio_unit_port`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AudioUnitPortFlags): boolean;

        unregister(): void;

        /**
         * Disable a feature of `audio_unit_port`.
         * @param flags see {@link AgsAudio.AudioUnitPortFlags}-enum
         */
        unset_flags(flags: AudioUnitPortFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace AudioUnitServer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-unit-client": (pspec: GObject.ParamSpec) => void;
            "notify::default-audio-unit-client": (pspec: GObject.ParamSpec) => void;
            "notify::default-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-audio-unit-client": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.SoundServer.ConstructorProps {
            audio_unit_client: AudioUnitClient[];
            audioUnitClient: AudioUnitClient[];
            default_audio_unit_client: AudioUnitClient;
            defaultAudioUnitClient: AudioUnitClient;
            default_soundcard: GObject.Object;
            defaultSoundcard: GObject.Object;
            input_audio_unit_client: AudioUnitClient;
            inputAudioUnitClient: AudioUnitClient;
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class AudioUnitServer extends GObject.Object implements Ags.Connectable, Ags.SoundServer {
        static $gtype: GObject.GType<AudioUnitServer>;

        // Properties
        /**
         * The audio unit client list.
         * @since 3.0.0
         */
        get audio_unit_client(): AudioUnitClient[];
        set audio_unit_client(val: AudioUnitClient[]);

        /**
         * The audio unit client list.
         * @since 3.0.0
         */
        get audioUnitClient(): AudioUnitClient[];
        set audioUnitClient(val: AudioUnitClient[]);

        /**
         * The default audio unit client.
         * @since 3.0.0
         */
        get default_audio_unit_client(): AudioUnitClient;
        set default_audio_unit_client(val: AudioUnitClient);

        /**
         * The default audio unit client.
         * @since 3.0.0
         */
        get defaultAudioUnitClient(): AudioUnitClient;
        set defaultAudioUnitClient(val: AudioUnitClient);

        /**
         * The default soundcard.
         * @since 3.0.0
         */
        get default_soundcard(): GObject.Object;
        set default_soundcard(val: GObject.Object);

        /**
         * The default soundcard.
         * @since 3.0.0
         */
        get defaultSoundcard(): GObject.Object;
        set defaultSoundcard(val: GObject.Object);

        /**
         * The input audio unit client.
         * @since 3.0.0
         */
        get input_audio_unit_client(): AudioUnitClient;
        set input_audio_unit_client(val: AudioUnitClient);

        /**
         * The input audio unit client.
         * @since 3.0.0
         */
        get inputAudioUnitClient(): AudioUnitClient;
        set inputAudioUnitClient(val: AudioUnitClient);

        /**
         * The assigned URL.
         * @since 3.0.0
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AudioUnitServer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        running: boolean;

        application_context: Ags.ApplicationContext;

        uuid: Ags.UUID;

        port: number;

        port_count: number;

        n_soundcards: number;

        n_sequencers: number;

        default_client: GObject.Object;

        input_client: GObject.Object;

        client: any[];

        // Constructors
        constructor(properties?: Partial<AudioUnitServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](url: string): AudioUnitServer;

        // Signals
        /** @signal */
        connect_after<K extends keyof AudioUnitServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AudioUnitServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AudioUnitServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AudioUnitServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find {@link AgsAudio.AudioUnitServer} by url.
         * @param audio_unit_server the {@link GLib.List}-struct containing {@link AgsAudio.AudioUnitServer}
         * @param url the url to find
         */
        static find_url(audio_unit_server: AudioUnitServer[], url: string): AudioUnitServer[];

        // Methods
        /**
         * Add `audio_unit_client` to `audio_unit_server`
         * @param audio_unit_client the {@link AgsAudio.AudioUnitClient} to add
         */
        add_client(audio_unit_client: GObject.Object): void;

        /**
         * Connect all clients.
         */
        connect_client(): void;

        /**
         * Find {@link AgsAudio.AudioUnitClient} by uuid.
         * @param client_uuid the uuid to find
         * @returns the {@link AgsAudio.AudioUnitClient} found or `null`
         */
        find_client<T = GObject.Object>(client_uuid: string): T;

        /**
         * Find {@link AgsAudio.AudioUnitPort} by uuid.
         * @param port_uuid the uuid to find
         * @returns the {@link AgsAudio.AudioUnitPort} found or `null`
         */
        find_port<T = GObject.Object>(port_uuid: string): T;

        /**
         * Remove `audio_unit_client` to `audio_unit_server`
         * @param audio_unit_client the {@link AgsAudio.AudioUnitClient} to remove
         */
        remove_client(audio_unit_client: GObject.Object): void;

        start_poll(): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @returns the port as string `null`-terminated array
         */
        get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the sequencer as {@link GLib.List}-struct
         */
        get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the soundcard as {@link GLib.List}-struct
         */
        get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @returns the URL as string
         */
        get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Sequencer}
         */
        register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Soundcard}
         */
        register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param port 
         * @param port_count 
         */
        set_ports(port: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         */
        set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         */
        set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         */
        set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         */
        unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         */
        unregister_soundcard(soundcard: GObject.Object): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @virtual
         */
        vfunc_get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @virtual
         */
        vfunc_get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param ports 
         * @param port_count 
         * @virtual
         */
        vfunc_set_ports(ports: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         * @virtual
         */
        vfunc_set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         * @virtual
         */
        vfunc_set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         * @virtual
         */
        vfunc_set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         * @virtual
         */
        vfunc_unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         * @virtual
         */
        vfunc_unregister_soundcard(soundcard: GObject.Object): void;
    }


    namespace Automation {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::acceleration": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::channel-type": (pspec: GObject.ParamSpec) => void;
            "notify::control-key": (pspec: GObject.ParamSpec) => void;
            "notify::control-name": (pspec: GObject.ParamSpec) => void;
            "notify::default-value": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::lower": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::steps": (pspec: GObject.ParamSpec) => void;
            "notify::timestamp": (pspec: GObject.ParamSpec) => void;
            "notify::upper": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            acceleration: Acceleration[];
            audio: Audio;
            channel_type: GObject.GTypeInput;
            channelType: GObject.GTypeInput;
            control_key: (any | null);
            controlKey: (any | null);
            control_name: string;
            controlName: string;
            default_value: number;
            defaultValue: number;
            line: number;
            lower: number;
            port: Port;
            steps: number;
            timestamp: Ags.Timestamp;
            upper: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Automation extends GObject.Object {
        static $gtype: GObject.GType<Automation>;

        // Properties
        /**
         * The acceleration list.
         * @since 3.0.0
         */
        get acceleration(): Acceleration[];
        set acceleration(val: Acceleration[]);

        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The effect's assigned channel type.
         * @since 3.0.0
         */
        get channel_type(): GObject.GType;
        set channel_type(val: GObject.GType);

        /**
         * The effect's assigned channel type.
         * @since 3.0.0
         */
        get channelType(): GObject.GType;
        set channelType(val: GObject.GType);

        /**
         * The effect's assigned control key.
         * @since 5.4.0
         */
        get control_key(): (any | null);
        set control_key(val: (any | null));

        /**
         * The effect's assigned control key.
         * @since 5.4.0
         */
        get controlKey(): (any | null);
        set controlKey(val: (any | null));

        /**
         * The effect's assigned control name.
         * @since 3.0.0
         * @default null
         */
        get control_name(): string;
        set control_name(val: string);

        /**
         * The effect's assigned control name.
         * @since 3.0.0
         * @default null
         */
        get controlName(): string;
        set controlName(val: string);

        /**
         * The effect's default-value.
         * @since 3.0.0
         * @default 0
         */
        get default_value(): number;
        set default_value(val: number);

        /**
         * The effect's default-value.
         * @since 3.0.0
         * @default 0
         */
        get defaultValue(): number;
        set defaultValue(val: number);

        /**
         * The effect's line.
         * @since 3.0.0
         * @default 0
         */
        get line(): number;
        set line(val: number);

        /**
         * The effect's lower.
         * @since 3.0.0
         * @default 0
         */
        get lower(): number;
        set lower(val: number);

        /**
         * The assigned {@link AgsAudio.Port}
         * @since 3.0.0
         */
        get port(): Port;
        set port(val: Port);

        /**
         * The effect's steps.
         * @since 3.0.0
         * @default 0
         */
        get steps(): number;
        set steps(val: number);

        /**
         * The automation's timestamp.
         * @since 3.0.0
         */
        get timestamp(): Ags.Timestamp;
        set timestamp(val: Ags.Timestamp);

        /**
         * The effect's upper.
         * @since 3.0.0
         * @default 0
         */
        get upper(): number;
        set upper(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Automation.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: AutomationFlags;

        source_function: Ags.Function;

        selection: any[];

        // Constructors
        constructor(properties?: Partial<Automation.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: GObject.Object, line: number, channel_type: GObject.GType, control_name: string): Automation;

        // Signals
        /** @signal */
        connect<K extends keyof Automation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Automation.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Automation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Automation.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Automation.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Automation.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Add `new_automation` sorted to `automation`
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param new_automation the {@link AgsAudio.Automation} to add
         */
        static add(automation: Automation[], new_automation: Automation): Automation[];

        /**
         * Filter `automation` by `specifier`, `channel_type` and `line`.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param specifier the string specifier to find
         * @param channel_type the channel {@link GObject.GType}
         * @param line the line
         */
        static filter(automation: Automation[], specifier: string, channel_type: GObject.GType, line: number): Automation[];

        /**
         * Find automation by `channel_type`.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param channel_type the {@link GObject.GType} to match
         * @param specifier the control name
         */
        static find_channel_type_with_control_name(automation: Automation[], channel_type: GObject.GType, specifier: string): Automation[];

        /**
         * Retrieve appropriate automation for timestamp.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param line the matching audio channel
         * @param timestamp the matching timestamp
         */
        static find_near_timestamp(automation: Automation[], line: number, timestamp: Ags.Timestamp): Automation[];

        /**
         * Retrieve appropriate automation for timestamp.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param line the matching audio channel
         * @param channel_type the matching channel type
         * @param control_name the matching control name
         * @param timestamp the matching timestamp
         */
        static find_near_timestamp_extended(automation: Automation[], line: number, channel_type: GObject.GType, control_name: string, timestamp: Ags.Timestamp): Automation[];

        /**
         * Find automation by port.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param port the {@link AgsAudio.Port} to match
         */
        static find_port(automation: Automation[], port: GObject.Object): Automation[];

        /**
         * Find port specifier.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param specifier the string specifier to find
         */
        static find_specifier(automation: Automation[], specifier: string): Automation[];

        /**
         * Find port specifier with channel type and line.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param specifier the string specifier to find
         * @param channel_type the channel {@link GObject.GType}
         * @param line the line
         */
        static find_specifier_with_type_and_line(automation: Automation[], specifier: string, channel_type: GObject.GType, line: number): Automation[];

        /**
         * Retrieve automation port specifier.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         */
        static get_specifier_unique(automation: Automation[]): string[];

        /**
         * Retrieve automation port specifier.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         * @param channel_type the channel's {@link GObject.GType}
         */
        static get_specifier_unique_with_channel_type(automation: Automation[], channel_type: GObject.GType): string[];

        /**
         * Remove all empty `automation`.
         * @param automation the {@link GLib.List}-struct containing {@link AgsAudio.Automation}
         */
        static remove_all_empty(automation: Automation[]): Automation[];

        /**
         * Compare `a` to `b`.
         * @param a an {@link AgsAudio.Automation}
         * @param b an other {@link AgsAudio.Automation}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Adds `acceleration` to `automation`.
         * @param acceleration the {@link AgsAudio.Acceleration} to add
         * @param use_selection_list if `true` add to selection, else to default automation
         */
        add_acceleration(acceleration: Acceleration, use_selection_list: boolean): void;

        /**
         * Add all acceleration to selection.
         */
        add_all_to_selection(): void;

        /**
         * Select acceleration at position.
         * @param x x offset
         * @param y y acceleration value
         * @param replace_current_selection if `true` selection is replaced
         */
        add_point_to_selection(x: number, y: number, replace_current_selection: boolean): void;

        /**
         * @param x0 
         * @param y0 
         * @param x1 
         * @param y1 
         * @param replace_current_selection 
         */
        add_region_to_selection(x0: number, y0: number, x1: number, y1: number, replace_current_selection: boolean): void;

        /**
         * Copy selection to clipboard.
         * @returns the selection as xmlNode
         */
        copy_selection(): libxml2.Node;

        /**
         * Cut selection to clipboard.
         * @returns the selection as xmlNod
         */
        cut_selection(): libxml2.Node;

        /**
         * Find acceleration by offset and acceleration.
         * @param x x offset
         * @param y y value acceleration, will be ignored
         * @param use_selection_list if `true` selection is searched
         * @returns the matching acceleration.
         */
        find_point(x: number, y: number, use_selection_list: boolean): Acceleration;

        /**
         * Find acceleration by offset and value region.
         * @param x0 start offset
         * @param y0 value start
         * @param x1 end offset
         * @param y1 value end
         * @param use_selection_list if `true` selection is searched
         * @returns the matching accelerations as {@link GLib.List}-struct
         */
        find_region(x0: number, y0: number, x1: number, y1: number, use_selection_list: boolean): Acceleration[];

        /**
         * Clear selection.
         */
        free_selection(): void;

        /**
         * Get acceleration.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Acceleration}
         */
        get_acceleration(): Acceleration[];

        /**
         * Get audio.
         * @returns the {@link AgsAudio.Audio}
         */
        get_audio<T = GObject.Object>(): T;

        /**
         * Gets channel type.
         * @returns the channel type
         */
        get_channel_type(): GObject.GType;

        /**
         * Get control key.
         * @returns the control key
         */
        get_control_key(): (any | null);

        /**
         * Get control name.
         * @returns the control name
         */
        get_control_name(): string;

        /**
         * Gets default value.
         * @returns the default value
         */
        get_default_value(): number;

        /**
         * Gets line.
         * @returns the line
         */
        get_line(): number;

        /**
         * Gets lower.
         * @returns the lower
         */
        get_lower(): number;

        /**
         * Get port.
         * @returns the {@link AgsAudio.Port}
         */
        get_port<T = GObject.Object>(): T;

        /**
         * Retrieve selection.
         * @returns the selection.
         */
        get_selection(): Acceleration[];

        /**
         * Gets steps.
         * @returns the steps
         */
        get_steps(): number;

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * Gets upper.
         * @returns the upper
         */
        get_upper(): number;

        /**
         * Get automation value.
         * @param x the x-offset
         * @param x_end the x-end-offset
         * @param use_prev_on_failure if `true` use previous value, else return G_MAXUINT
         * @returns the x_offset
         */
        get_value(x: number, x_end: number, use_prev_on_failure: boolean): [number, unknown];

        /**
         * Insert clipboard `automation_node` to `automation`.
         * @param automation_node the xmlNode
         * @param reset_x_offset if `true` reset x offset
         * @param x_offset the x offset to use
         * @param reset_y_offset if `true` reset y offset
         * @param y_offset the y offset to use
         */
        insert_from_clipboard(automation_node: libxml2.Node, reset_x_offset: boolean, x_offset: number, reset_y_offset: boolean, y_offset: number): void;

        /**
         * Insert clipboard `automation_node` to `automation`.
         * @param automation_node the xmlNode
         * @param reset_x_offset if `true` reset x offset
         * @param x_offset the x offset to use
         * @param reset_y_offset if `true` reset y offset
         * @param y_offset the y offset to use
         * @param match_line if `true` match line or discard
         * @param no_duplicates if `true` eliminate duplicates
         */
        insert_from_clipboard_extended(automation_node: libxml2.Node, reset_x_offset: boolean, x_offset: number, reset_y_offset: boolean, y_offset: number, match_line: boolean, no_duplicates: boolean): void;

        /**
         * Check selection for acceleration.
         * @param acceleration the {@link AgsAudio.Acceleration} to check for
         * @returns `true` if selected, else `false`
         */
        is_acceleration_selected(acceleration: Acceleration): boolean;

        /**
         * Removes `acceleration` from `automation`.
         * @param acceleration the {@link AgsAudio.Acceleration} to remove
         * @param use_selection_list if `true` remove from selection, else from default automation
         */
        remove_acceleration(acceleration: Acceleration, use_selection_list: boolean): void;

        /**
         * Removes one {@link AgsAudio.Acceleration} of automation.
         * @param x x offset
         * @param y y value of acceleration
         * @returns `true` if successfully removed acceleration, else `false`
         */
        remove_acceleration_at_position(x: number, y: number): boolean;

        /**
         * Remove acceleration at position of selection.
         * @param x x offset
         * @param y y acceleration value
         */
        remove_point_from_selection(x: number, y: number): void;

        /**
         * Remove acceleration within region of selection.
         * @param x0 x start offset
         * @param y0 y start acceleration
         * @param x1 x end offset
         * @param y1 y end acceleration
         */
        remove_region_from_selection(x0: number, y0: number, x1: number, y1: number): void;

        /**
         * Set acceleration by replacing existing.
         * @param acceleration the {@link GLib.List}-struct containing {@link AgsAudio.Acceleration}
         */
        set_acceleration(acceleration: Acceleration[]): void;

        /**
         * Set audio.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: GObject.Object): void;

        /**
         * Sets channel type.
         * @param channel_type the channel type
         */
        set_channel_type(channel_type: GObject.GType): void;

        /**
         * Set control key.
         * @param control_key the control key
         */
        set_control_key(control_key: (any | null)): void;

        /**
         * Set control name.
         * @param control_name the control name
         */
        set_control_name(control_name: string): void;

        /**
         * Sets default value.
         * @param default_value the default value
         */
        set_default_value(default_value: number): void;

        /**
         * Set `flags` on `automation`.
         * @param flags the flags
         */
        set_flags(flags: AutomationFlags): void;

        /**
         * Sets line.
         * @param line the line
         */
        set_line(line: number): void;

        /**
         * Sets lower.
         * @param lower the lower
         */
        set_lower(lower: number): void;

        /**
         * Set port.
         * @param port the {@link AgsAudio.Port}
         */
        set_port(port: GObject.Object): void;

        /**
         * Sets steps.
         * @param steps the steps
         */
        set_steps(steps: number): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Sets upper.
         * @param upper the upper
         */
        set_upper(upper: number): void;

        /**
         * Test `flags` to be set on `automation`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: AutomationFlags): boolean;

        /**
         * Unset `flags` on `automation`.
         * @param flags the flags
         */
        unset_flags(flags: AutomationFlags): void;
    }


    namespace AutomationControlNameKeyManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::control-name-key": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            control_name_key: any;
            controlNameKey: any;
        }
    }

    /**
     * @gir-type Class
     */
    class AutomationControlNameKeyManager extends GObject.Object {
        static $gtype: GObject.GType<AutomationControlNameKeyManager>;

        // Properties
        /**
         * The {@link GLib.HashTable}-struct containing gpointer of key.
         * @since 5.4.0
         */
        get control_name_key(): any;
        set control_name_key(val: any);

        /**
         * The {@link GLib.HashTable}-struct containing gpointer of key.
         * @since 5.4.0
         */
        get controlNameKey(): any;
        set controlNameKey(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: AutomationControlNameKeyManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<AutomationControlNameKeyManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): AutomationControlNameKeyManager;

        // Signals
        /** @signal */
        connect<K extends keyof AutomationControlNameKeyManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AutomationControlNameKeyManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof AutomationControlNameKeyManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, AutomationControlNameKeyManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof AutomationControlNameKeyManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<AutomationControlNameKeyManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get ags audio file manager instance.
         */
        static get_instance(): AutomationControlNameKeyManager;

        // Methods
        /**
         * @param control_name 
         */
        find_automation(control_name: string): (any | null);
    }


    namespace BasePlugin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::activate signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            activate: (arg0: (any | null)) => void;
            /**
             * The ::connect-port signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "connect-port": (arg0: (any | null), arg1: number, arg2: (any | null)) => void;
            /**
             * The ::deactivate signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            deactivate: (arg0: (any | null)) => void;
            /**
             * The ::instantiate signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            instantiate: (arg0: number, arg1: number) => (any | null);
            /**
             * The ::instantiate-with-params signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "instantiate-with-params": (arg0: (any | null), arg1: (any | null), arg2: (any | null)) => (any | null);
            /**
             * The ::load-plugin signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "load-plugin": () => void;
            /**
             * The ::run signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            run: (arg0: (any | null), arg1: (any | null), arg2: number) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-port": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::ui-filename": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::uuid": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            effect: string;
            effect_index: number;
            effectIndex: number;
            filename: string;
            plugin_port: PluginPort[];
            pluginPort: PluginPort[];
            plugin_so: (any | null);
            pluginSo: (any | null);
            ui_effect: string;
            uiEffect: string;
            ui_effect_index: number;
            uiEffectIndex: number;
            ui_filename: string;
            uiFilename: string;
            ui_plugin: BasePlugin;
            uiPlugin: BasePlugin;
            ui_plugin_so: (any | null);
            uiPluginSo: (any | null);
            uuid: any;
        }
    }

    /**
     * @gir-type Class
     */
    class BasePlugin extends GObject.Object {
        static $gtype: GObject.GType<BasePlugin>;

        // Properties
        /**
         * The assigned effect.
         * @since 3.0.0
         * @default null
         */
        get effect(): string;
        set effect(val: string);

        /**
         * The assigned effect-index.
         * @since 3.0.0
         * @default 0
         */
        get effect_index(): number;
        set effect_index(val: number);

        /**
         * The assigned effect-index.
         * @since 3.0.0
         * @default 0
         */
        get effectIndex(): number;
        set effectIndex(val: number);

        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.PluginPort}
         * @since 3.0.0
         */
        get plugin_port(): PluginPort[];
        set plugin_port(val: PluginPort[]);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.PluginPort}
         * @since 3.0.0
         */
        get pluginPort(): PluginPort[];
        set pluginPort(val: PluginPort[]);

        /**
         * The assigned plugin.so
         * @since 3.0.0
         */
        get plugin_so(): (any | null);
        set plugin_so(val: (any | null));

        /**
         * The assigned plugin.so
         * @since 3.0.0
         */
        get pluginSo(): (any | null);
        set pluginSo(val: (any | null));

        /**
         * The assigned ui-effect.
         * @since 3.0.0
         * @default null
         */
        get ui_effect(): string;
        set ui_effect(val: string);

        /**
         * The assigned ui-effect.
         * @since 3.0.0
         * @default null
         */
        get uiEffect(): string;
        set uiEffect(val: string);

        /**
         * The assigned ui-effect-index.
         * @since 3.0.0
         * @default 0
         */
        get ui_effect_index(): number;
        set ui_effect_index(val: number);

        /**
         * The assigned ui-effect-index.
         * @since 3.0.0
         * @default 0
         */
        get uiEffectIndex(): number;
        set uiEffectIndex(val: number);

        /**
         * The assigned UI filename.
         * @since 3.0.0
         * @default null
         */
        get ui_filename(): string;
        set ui_filename(val: string);

        /**
         * The assigned UI filename.
         * @since 3.0.0
         * @default null
         */
        get uiFilename(): string;
        set uiFilename(val: string);

        /**
         * The assigned ui-plugin.
         * @since 3.0.0
         */
        get ui_plugin(): BasePlugin;
        set ui_plugin(val: BasePlugin);

        /**
         * The assigned ui-plugin.
         * @since 3.0.0
         */
        get uiPlugin(): BasePlugin;
        set uiPlugin(val: BasePlugin);

        /**
         * The assigned ui_plugin.so
         * @since 3.0.0
         */
        get ui_plugin_so(): (any | null);
        set ui_plugin_so(val: (any | null));

        /**
         * The assigned ui_plugin.so
         * @since 3.0.0
         */
        get uiPluginSo(): (any | null);
        set uiPluginSo(val: (any | null));

        /**
         * The assigned AgsUUID
         * @since 3.0.0
         */
        get uuid(): any;
        set uuid(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: BasePlugin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        port_group_count: number;

        port_group: number;

        plugin_descriptor: any;

        plugin_handle: any;

        ui_plugin_descriptor: any;

        ui_plugin_handle: any;

        // Constructors
        constructor(properties?: Partial<BasePlugin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](filename: string, effect: string, effect_index: number): BasePlugin;

        // Signals
        /** @signal */
        connect<K extends keyof BasePlugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BasePlugin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof BasePlugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, BasePlugin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof BasePlugin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<BasePlugin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find `filename` and `effect` in `base_plugin` {@link GLib.List}-struct of {@link AgsAudio.BasePlugin}.
         * @param base_plugin the {@link GLib.List}-struct containing {@link AgsAudio.BasePlugin}
         * @param filename the filename as string
         * @param effect the effect as string
         */
        static find_effect(base_plugin: BasePlugin[], filename: string, effect: string): BasePlugin[];

        /**
         * Find filename in `base_plugin` {@link GLib.List}-struct of {@link AgsAudio.BasePlugin}.
         * @param base_plugin the {@link GLib.List}-struct containing {@link AgsAudio.BasePlugin}
         * @param filename the filename as string
         */
        static find_filename(base_plugin: BasePlugin[], filename: string): BasePlugin[];

        /**
         * Find `ui_filename` and `ui_effect_index` in `base_plugin` {@link GLib.List}-struct of {@link AgsAudio.BasePlugin}.
         * @param base_plugin the {@link GLib.List}-struct containing {@link AgsAudio.BasePlugin}
         * @param ui_filename the UI filename as string
         * @param ui_effect_index the UI effect index
         */
        static find_ui_effect_index(base_plugin: BasePlugin[], ui_filename: string, ui_effect_index: number): BasePlugin[];

        /**
         * Sort `base_plugin` alphabetically.
         * @param base_plugin the {@link GLib.List}-struct containing {@link AgsAudio.BasePlugin}
         */
        static sort(base_plugin: BasePlugin[]): BasePlugin[];

        /**
         * Sort function.
         * @param a a {@link AgsAudio.BasePlugin}
         * @param b an other {@link AgsAudio.BasePlugin}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Virtual methods
        /**
         * Activate a plugin instance
         * @param plugin_handle the plugin instance handle
         * @virtual
         */
        vfunc_activate(plugin_handle: (any | null)): void;

        /**
         * Connect a plugin instance.
         * @param plugin_handle the plugin instance handle
         * @param port_index the port's index to connect
         * @param data_location the data location to connect
         * @virtual
         */
        vfunc_connect_port(plugin_handle: (any | null), port_index: number, data_location: (any | null)): void;

        /**
         * Deactivat a plugin instance
         * @param plugin_handle the plugin instance handle
         * @virtual
         */
        vfunc_deactivate(plugin_handle: (any | null)): void;

        /**
         * Instantiate the plugin
         * @param samplerate the samplerate
         * @param buffer_size the fixed buffer size
         * @virtual
         */
        vfunc_instantiate(samplerate: number, buffer_size: number): (any | null);

        /**
         * Instantiate the plugin
         * @param n_params guint pointer to parameter count
         * @param parameter_name string vector containing parameter names
         * @param value the {@link GObject.Value}-struct array
         * @virtual
         */
        vfunc_instantiate_with_params(n_params: number, parameter_name: string, value: unknown): (any | null);

        /**
         * Load the plugin
         * @virtual
         */
        vfunc_load_plugin(): void;

        /**
         * Deactivat a plugin instance
         * @param plugin_handle the plugin instance handle
         * @param seq_event the alsa sequencer events
         * @param frame_count the frame counts
         * @virtual
         */
        vfunc_run(plugin_handle: (any | null), seq_event: (any | null), frame_count: number): void;

        // Methods
        /**
         * Activate a plugin instance
         * @param plugin_handle the plugin instance handle
         */
        activate(plugin_handle: (any | null)): void;

        apply_port_group_by_prefix(): void;

        /**
         * Connect a plugin instance.
         * @param plugin_handle the plugin instance handle
         * @param port_index the port's index to connect
         * @param data_location the data location to connect
         */
        connect_port(plugin_handle: (any | null), port_index: number, data_location: (any | null)): void;

        /**
         * Deactivat a plugin instance
         * @param plugin_handle the plugin instance handle
         */
        deactivate(plugin_handle: (any | null)): void;

        /**
         * Get effect.
         * @returns the effect
         */
        get_effect(): string;

        /**
         * Get effect index.
         * @returns the effect index
         */
        get_effect_index(): number;

        /**
         * Get filename.
         * @returns the filename
         */
        get_filename(): string;

        /**
         * Get plugin so.
         * @returns the plugin so
         */
        get_plugin_so(): (any | null);

        /**
         * Get UI effect.
         * @returns the UI effect
         */
        get_ui_effect(): string;

        /**
         * Get UI effect index.
         * @returns the UI effect index
         */
        get_ui_effect_index(): number;

        /**
         * Get UI filename.
         * @returns the UI filename
         */
        get_ui_filename(): string;

        /**
         * Get plugin so.
         * @returns the plugin so
         */
        get_ui_plugin_so(): (any | null);

        /**
         * Instantiate the plugin
         * @param samplerate the samplerate
         * @param buffer_size the fixed buffer size
         * @returns the new plugin instance handle
         */
        instantiate(samplerate: number, buffer_size: number): (any | null);

        /**
         * Instantiate the plugin
         * @param n_params guint pointer to parameter count
         * @param parameter_name string vector containing parameter names
         * @param value the {@link GObject.Value}-struct array
         * @returns the new plugin instance handle
         */
        instantiate_with_params(n_params: number, parameter_name: string, value: (GObject.Value | any)): (any | null);

        /**
         * Load the plugin
         */
        load_plugin(): void;

        /**
         * Deactivat a plugin instance
         * @param plugin_handle the plugin instance handle
         * @param seq_event the alsa sequencer events
         * @param frame_count the frame counts
         */
        run(plugin_handle: (any | null), seq_event: (any | null), frame_count: number): void;

        /**
         * Set effect.
         * @param effect the effect
         */
        set_effect(effect: string): void;

        /**
         * Set effect index.
         * @param effect_index the effect index
         */
        set_effect_index(effect_index: number): void;

        /**
         * Set filename.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: BasePluginFlags): void;

        /**
         * Set recall dependency by replacing existing.
         * @param plugin_port the {@link GLib.List}-struct containing {@link AgsAudio.PluginPort}
         */
        set_plugin_port(plugin_port: PluginPort[]): void;

        /**
         * Set plugin_so.
         * @param plugin_so the plugin so
         */
        set_plugin_so(plugin_so: (any | null)): void;

        /**
         * Set UI effect.
         * @param ui_effect the UI effect
         */
        set_ui_effect(ui_effect: string): void;

        /**
         * Set UI effect index.
         * @param ui_effect_index the UI effect index
         */
        set_ui_effect_index(ui_effect_index: number): void;

        /**
         * Set UI filename.
         * @param ui_filename the UI filename
         */
        set_ui_filename(ui_filename: string): void;

        /**
         * @param ui_plugin 
         */
        set_ui_plugin(ui_plugin: GObject.Object): void;

        /**
         * Set ui_plugin_so.
         * @param ui_plugin_so the plugin so
         */
        set_ui_plugin_so(ui_plugin_so: (any | null)): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: BasePluginFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: BasePluginFlags): void;
    }


    namespace Buffer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::data": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::selection-x0": (pspec: GObject.ParamSpec) => void;
            "notify::selection-x1": (pspec: GObject.ParamSpec) => void;
            "notify::x": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            buffer_size: number;
            bufferSize: number;
            data: (any | null);
            format: number;
            samplerate: number;
            selection_x0: (bigint | number);
            selectionX0: (bigint | number);
            selection_x1: (bigint | number);
            selectionX1: (bigint | number);
            x: (bigint | number);
        }
    }

    /**
     * @gir-type Class
     */
    class Buffer extends GObject.Object {
        static $gtype: GObject.GType<Buffer>;

        // Properties
        /**
         * Buffer's audio data buffer size.
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * Buffer's audio data buffer size.
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * Buffer's audio data.
         * @since 3.0.0
         */
        get data(): (any | null);
        set data(val: (any | null));

        /**
         * Buffer's audio data format.
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * Buffer's audio data samplerate.
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Buffer's selection x0 offset.
         * @since 3.0.0
         * @default 0
         */
        get selection_x0(): number;
        set selection_x0(val: (bigint | number));

        /**
         * Buffer's selection x0 offset.
         * @since 3.0.0
         * @default 0
         */
        get selectionX0(): number;
        set selectionX0(val: (bigint | number));

        /**
         * Buffer's selection x1 offset.
         * @since 3.0.0
         * @default 0
         */
        get selection_x1(): number;
        set selection_x1(val: (bigint | number));

        /**
         * Buffer's selection x1 offset.
         * @since 3.0.0
         * @default 0
         */
        get selectionX1(): number;
        set selectionX1(val: (bigint | number));

        /**
         * Buffer's x offset.
         * @since 3.0.0
         * @default 0
         */
        get x(): number;
        set x(val: (bigint | number));

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Buffer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: BufferFlags;

        // Constructors
        constructor(properties?: Partial<Buffer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Buffer;

        // Signals
        /** @signal */
        connect<K extends keyof Buffer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Buffer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Buffer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Buffer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Buffer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Buffer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Sort buffers.
         * @param a an {@link AgsAudio.Buffer}
         * @param b an other {@link AgsAudio.Buffer}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Duplicate a buffer.
         * @returns the duplicated {@link AgsAudio.Buffer}.
         */
        duplicate(): Buffer;

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Gets data.
         * @returns the data
         */
        get_data(): (any | null);

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Gets x.
         * @returns the x
         */
        get_x(): number;

        /**
         * Lock object mutex.
         */
        lock(): void;

        /**
         * Set buffer size.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Set `flags` on `buffer`.
         * @param flags the flags
         */
        set_flags(flags: BufferFlags): void;

        /**
         * Set format.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Sets x.
         * @param x the x
         */
        set_x(x: (bigint | number)): void;

        /**
         * Test `flags` to be set on `buffer`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: BufferFlags): boolean;

        /**
         * Unlock object mutex.
         */
        unlock(): void;

        /**
         * Unset `flags` on `buffer`.
         * @param flags the flags
         */
        unset_flags(flags: BufferFlags): void;
    }


    namespace CancelAudio {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::sound-scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            sound_scope: number;
            soundScope: number;
        }
    }

    /**
     * @gir-type Class
     */
    class CancelAudio extends Ags.Task {
        static $gtype: GObject.GType<CancelAudio>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The effects sound-scope.
         * @since 3.0.0
         * @default -1
         */
        get sound_scope(): number;
        set sound_scope(val: number);

        /**
         * The effects sound-scope.
         * @since 3.0.0
         * @default -1
         */
        get soundScope(): number;
        set soundScope(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CancelAudio.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<CancelAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, sound_scope: number): CancelAudio;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof CancelAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CancelAudio.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CancelAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CancelAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CancelAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CancelAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace CancelChannel {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::sound-scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            channel: Channel;
            sound_scope: number;
            soundScope: number;
        }
    }

    /**
     * @gir-type Class
     */
    class CancelChannel extends Ags.Task {
        static $gtype: GObject.GType<CancelChannel>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.0.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * The effects sound-scope.
         * @since 3.0.0
         * @default -1
         */
        get sound_scope(): number;
        set sound_scope(val: number);

        /**
         * The effects sound-scope.
         * @since 3.0.0
         * @default -1
         */
        get soundScope(): number;
        set soundScope(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CancelChannel.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<CancelChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel, sound_scope: number): CancelChannel;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof CancelChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CancelChannel.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CancelChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CancelChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CancelChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CancelChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace Channel {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::cancel-recall signal is invoked during termination of playback.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "cancel-recall": (arg0: GObject.Object) => void;
            /**
             * The ::check-scope signal gives you control of checking scope.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "check-scope": (arg0: number) => (any | null);
            /**
             * The ::cleanup-recall signal is invoked during termination of playback.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "cleanup-recall": (arg0: GObject.Object) => void;
            /**
             * The ::done-recall signal is invoked during termination of playback.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "done-recall": (arg0: GObject.Object) => void;
            /**
             * The ::duplicate-recall signal is invoked during playback initialization.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "duplicate-recall": (arg0: GObject.Object) => void;
            /**
             * The ::init-recall signal is invoked during playback initialization.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "init-recall": (arg0: GObject.Object, arg1: number) => void;
            /**
             * The ::play-recall signal is invoked during playback run.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "play-recall": (arg0: GObject.Object, arg1: number) => void;
            /**
             * The ::recursive-run-stage signal gives you control of checking scope.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "recursive-run-stage": (arg0: number, arg1: number) => void;
            /**
             * The ::recycling-changed signal is invoked to notify modified recycling tree.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "recycling-changed": (arg0: GObject.Object, arg1: GObject.Object, arg2: GObject.Object, arg3: GObject.Object, arg4: GObject.Object, arg5: GObject.Object, arg6: GObject.Object, arg7: GObject.Object) => void;
            /**
             * The ::resolve-recall signal is invoked during playback initialization.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "resolve-recall": (arg0: GObject.Object) => void;
            /**
             * The ::start signal is invoked as playback starts.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            start: (arg0: number) => RecallID[];
            /**
             * The ::stop signal is invoked as playback stops.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            stop: (arg0: (any | null), arg1: number) => void;
            "notify::absolute-key": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::first-recycling": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::key": (pspec: GObject.ParamSpec) => void;
            "notify::last-recycling": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::link": (pspec: GObject.ParamSpec) => void;
            "notify::midi-note": (pspec: GObject.ParamSpec) => void;
            "notify::next": (pspec: GObject.ParamSpec) => void;
            "notify::next-pad": (pspec: GObject.ParamSpec) => void;
            "notify::note-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::note-key": (pspec: GObject.ParamSpec) => void;
            "notify::octave": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::play": (pspec: GObject.ParamSpec) => void;
            "notify::playback": (pspec: GObject.ParamSpec) => void;
            "notify::prev": (pspec: GObject.ParamSpec) => void;
            "notify::prev-pad": (pspec: GObject.ParamSpec) => void;
            "notify::recall": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::recycling-context": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            absolute_key: number;
            absoluteKey: number;
            audio: Audio;
            audio_channel: number;
            audioChannel: number;
            buffer_size: number;
            bufferSize: number;
            first_recycling: Recycling;
            firstRecycling: Recycling;
            format: number;
            input_soundcard: GObject.Object;
            inputSoundcard: GObject.Object;
            input_soundcard_channel: number;
            inputSoundcardChannel: number;
            key: number;
            last_recycling: Recycling;
            lastRecycling: Recycling;
            line: number;
            link: Channel;
            midi_note: number;
            midiNote: number;
            next: Channel;
            next_pad: Channel;
            nextPad: Channel;
            note_frequency: number;
            noteFrequency: number;
            note_key: string;
            noteKey: string;
            octave: number;
            output_soundcard: GObject.Object;
            outputSoundcard: GObject.Object;
            output_soundcard_channel: number;
            outputSoundcardChannel: number;
            pad: number;
            play: Recall[];
            playback: Playback;
            prev: Channel;
            prev_pad: Channel;
            prevPad: Channel;
            recall: Recall[];
            recall_container: RecallContainer[];
            recallContainer: RecallContainer[];
            recall_id: RecallID[];
            recallId: RecallID[];
            recycling_context: RecyclingContext[];
            recyclingContext: RecyclingContext[];
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Channel extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<Channel>;

        // Properties
        /**
         * The nth absolute key.
         * @since 3.0.0
         * @default 0
         */
        get absolute_key(): number;
        set absolute_key(val: number);

        /**
         * The nth absolute key.
         * @since 3.0.0
         * @default 0
         */
        get absoluteKey(): number;
        set absoluteKey(val: number);

        /**
         * The assigned {@link AgsAudio.Audio} aligning channels.
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The nth audio channel.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get audio_channel(): number;

        /**
         * The nth audio channel.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get audioChannel(): number;

        /**
         * The buffer size.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The containing {@link AgsAudio.Recycling} it takes it {@link AgsAudio.AudioSignal} from.
         * @since 3.0.0
         * @read-only
         */
        get first_recycling(): Recycling;

        /**
         * The containing {@link AgsAudio.Recycling} it takes it {@link AgsAudio.AudioSignal} from.
         * @since 3.0.0
         * @read-only
         */
        get firstRecycling(): Recycling;

        /**
         * The format.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned input {@link Ags.Soundcard}.
         * @since 3.0.0
         */
        get input_soundcard(): GObject.Object;
        set input_soundcard(val: GObject.Object);

        /**
         * The assigned input {@link Ags.Soundcard}.
         * @since 3.0.0
         */
        get inputSoundcard(): GObject.Object;
        set inputSoundcard(val: GObject.Object);

        /**
         * The input soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get input_soundcard_channel(): number;
        set input_soundcard_channel(val: number);

        /**
         * The input soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get inputSoundcardChannel(): number;
        set inputSoundcardChannel(val: number);

        /**
         * The nth key.
         * @since 3.0.0
         * @default 0
         */
        get key(): number;
        set key(val: number);

        /**
         * The containing {@link AgsAudio.Recycling} it takes it {@link AgsAudio.AudioSignal} from.
         * @since 3.0.0
         * @read-only
         */
        get last_recycling(): Recycling;

        /**
         * The containing {@link AgsAudio.Recycling} it takes it {@link AgsAudio.AudioSignal} from.
         * @since 3.0.0
         * @read-only
         */
        get lastRecycling(): Recycling;

        /**
         * The nth line.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get line(): number;

        /**
         * The assigned link as {@link AgsAudio.Channel}.
         * @since 3.0.0
         */
        get link(): Channel;
        set link(val: Channel);

        /**
         * The nth midi note.
         * @since 3.0.0
         * @default 0
         */
        get midi_note(): number;
        set midi_note(val: number);

        /**
         * The nth midi note.
         * @since 3.0.0
         * @default 0
         */
        get midiNote(): number;
        set midiNote(val: number);

        /**
         * The assigned next {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @read-only
         */
        get next(): Channel;

        /**
         * The assigned next pad {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @read-only
         */
        get next_pad(): Channel;

        /**
         * The assigned next pad {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @read-only
         */
        get nextPad(): Channel;

        /**
         * The note frequency.
         * @since 3.0.0
         * @default 8.175799
         */
        get note_frequency(): number;
        set note_frequency(val: number);

        /**
         * The note frequency.
         * @since 3.0.0
         * @default 8.175799
         */
        get noteFrequency(): number;
        set noteFrequency(val: number);

        /**
         * The assigned note key representing this channel.
         * @since 3.0.0
         * @default null
         */
        get note_key(): string;
        set note_key(val: string);

        /**
         * The assigned note key representing this channel.
         * @since 3.0.0
         * @default null
         */
        get noteKey(): string;
        set noteKey(val: string);

        /**
         * The nth octave.
         * @since 3.0.0
         * @default 0
         */
        get octave(): number;
        set octave(val: number);

        /**
         * The assigned output {@link Ags.Soundcard}.
         * @since 3.0.0
         */
        get output_soundcard(): GObject.Object;
        set output_soundcard(val: GObject.Object);

        /**
         * The assigned output {@link Ags.Soundcard}.
         * @since 3.0.0
         */
        get outputSoundcard(): GObject.Object;
        set outputSoundcard(val: GObject.Object);

        /**
         * The output soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get output_soundcard_channel(): number;
        set output_soundcard_channel(val: number);

        /**
         * The output soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get outputSoundcardChannel(): number;
        set outputSoundcardChannel(val: number);

        /**
         * The nth pad.
         * @since 3.0.0
         * @read-only
         * @default 0
         */
        get pad(): number;

        /**
         * The containing {@link AgsAudio.Recall} in play-context.
         * @since 3.0.0
         */
        get play(): Recall[];
        set play(val: Recall[]);

        /**
         * The assigned {@link AgsAudio.Playback}.
         * @since 3.0.0
         */
        get playback(): Playback;
        set playback(val: Playback);

        /**
         * The assigned prev {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @read-only
         */
        get prev(): Channel;

        /**
         * The assigned prev pad {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @read-only
         */
        get prev_pad(): Channel;

        /**
         * The assigned prev pad {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @read-only
         */
        get prevPad(): Channel;

        /**
         * The containing {@link AgsAudio.Recall} in recall-context.
         * @since 3.0.0
         */
        get recall(): Recall[];
        set recall(val: Recall[]);

        /**
         * The containing {@link AgsAudio.RecallContainer}.
         * @since 3.0.0
         */
        get recall_container(): RecallContainer[];
        set recall_container(val: RecallContainer[]);

        /**
         * The containing {@link AgsAudio.RecallContainer}.
         * @since 3.0.0
         */
        get recallContainer(): RecallContainer[];
        set recallContainer(val: RecallContainer[]);

        /**
         * The assigned {@link AgsAudio.RecallID}.
         * @since 3.0.0
         */
        get recall_id(): RecallID[];
        set recall_id(val: RecallID[]);

        /**
         * The assigned {@link AgsAudio.RecallID}.
         * @since 3.0.0
         */
        get recallId(): RecallID[];
        set recallId(val: RecallID[]);

        /**
         * The containing {@link AgsAudio.RecyclingContext}.
         * @since 3.0.0
         */
        get recycling_context(): RecyclingContext[];
        set recycling_context(val: RecyclingContext[]);

        /**
         * The containing {@link AgsAudio.RecyclingContext}.
         * @since 3.0.0
         */
        get recyclingContext(): RecyclingContext[];
        set recyclingContext(val: RecyclingContext[]);

        /**
         * The samplerate.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Channel.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: ChannelFlags;

        connectable_flags: Ags.ConnectableFlags;

        ability_flags: SoundAbilityFlags;

        behaviour_flags: SoundBehaviourFlags;

        staging_flags: SoundStagingFlags[];

        staging_completed: boolean[];

        uuid: Ags.UUID;

        line_widget: any;

        file_data: any;

        // Constructors
        constructor(properties?: Partial<Channel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: GObject.Object): Channel;

        // Signals
        /** @signal */
        connect_after<K extends keyof Channel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Channel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Channel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Channel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Virtual methods
        /**
         * Cancel processing specified by `recall_id`.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @virtual
         */
        vfunc_cancel_recall(recall_id: RecallID): void;

        /**
         * Check `channel`'s `sound_scope`.
         * @param sound_scope the sound scope
         * @virtual
         */
        vfunc_check_scope(sound_scope: number): RecallID[];

        /**
         * Cleanup processing specified by `recall_id`.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @virtual
         */
        vfunc_cleanup_recall(recall_id: RecallID): void;

        /**
         * Done processing specified by `recall_id`.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @virtual
         */
        vfunc_done_recall(recall_id: RecallID): void;

        /**
         * Duplicate {@link AgsAudio.Recall} template and assign `recall_id` to it.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @virtual
         */
        vfunc_duplicate_recall(recall_id: RecallID): void;

        /**
         * Prepare {@link AgsAudio.Recall} objects and invoke {@link AgsAudio.Recall.SignalSignatures.run_init_pre | AgsAudio.Recall::run-init-pre}, {@link AgsAudio.Recall.SignalSignatures.run_init_inter | AgsAudio.Recall::run-init-inter} or
         * {@link AgsAudio.Recall.SignalSignatures.run_init_post | AgsAudio.Recall::run-init-post} as specified by `staging_flags`.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @param staging_flags the stages to invoke
         * @virtual
         */
        vfunc_init_recall(recall_id: RecallID, staging_flags: SoundStagingFlags): void;

        /**
         * Run the specified steps by `recall_id` of `channel`.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @param staging_flags the stages to invoke
         * @virtual
         */
        vfunc_play_recall(recall_id: RecallID, staging_flags: SoundStagingFlags): void;

        /**
         * Recursive run stage specified by `staging_flags` for matching `sound_scope`.
         * @param sound_scope the sound scope
         * @param staging_flags the staging flags
         * @virtual
         */
        vfunc_recursive_run_stage(sound_scope: number, staging_flags: SoundStagingFlags): void;

        /**
         * Modify recycling. Asynchronously only.
         * @param old_start_region first recycling
         * @param old_end_region last recycling
         * @param new_start_region new first recycling
         * @param new_end_region new last recycling
         * @param old_start_changed_region modified link recycling start
         * @param old_end_changed_region modified link recyclig end
         * @param new_start_changed_region replacing link recycling start
         * @param new_end_changed_region replacing link recycling end
         * @virtual
         */
        vfunc_recycling_changed(old_start_region: Recycling, old_end_region: Recycling, new_start_region: Recycling, new_end_region: Recycling, old_start_changed_region: Recycling, old_end_changed_region: Recycling, new_start_changed_region: Recycling, new_end_changed_region: Recycling): void;

        /**
         * Resolve step of initialization.
         * @param recall_id appropriate {@link AgsAudio.RecallID}
         * @virtual
         */
        vfunc_resolve_recall(recall_id: RecallID): void;

        /**
         * Start `channel`'s `sound_scope` to do playback.
         * @param sound_scope the sound scope
         * @virtual
         */
        vfunc_start(sound_scope: number): RecallID[];

        /**
         * Stop `channel`'s `sound_scope` playback specified by `recall_id`.
         * @param recall_id the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         * @param sound_scope the sound scope
         * @virtual
         */
        vfunc_stop(recall_id: RecallID[], sound_scope: number): void;

        // Methods
        /**
         * Removes a pattern.
         * @param pattern the {@link AgsAudio.Pattern}
         */
        add_pattern(pattern: GObject.Object): void;

        /**
         * Adds a recall.
         * @param recall the {@link AgsAudio.Recall}
         * @param play_context `true` if play context, else if `false` recall context
         */
        add_recall(recall: GObject.Object, play_context: boolean): void;

        /**
         * Adds a recall container.
         * @param recall_container the {@link AgsAudio.RecallContainer}
         */
        add_recall_container(recall_container: GObject.Object): void;

        /**
         * Adds a recall id.
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        add_recall_id(recall_id: RecallID): void;

        /**
         * Cancel processing specified by `recall_id`.
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        cancel_recall(recall_id: RecallID): void;

        /**
         * Check `channel`'s `sound_scope`.
         * @param sound_scope the sound scope
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.RecallID} or `null` if not playing
         */
        check_scope(sound_scope: number): RecallID[];

        /**
         * Cleanup processing specified by `recall_id`.
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        cleanup_recall(recall_id: RecallID): void;

        /**
         * Retrieve all ports of {@link AgsAudio.Channel}.
         * @returns a new {@link GLib.List} containing {@link AgsAudio.Port}
         */
        collect_all_channel_ports(): Port[];

        /**
         * Retrieve specified port of {@link AgsAudio.Channel}
         * @param specifier the port's name
         * @param play_context either `true` for play or `false` for recall
         * @returns a {@link GLib.List}-struct of {@link AgsAudio.Port} if found, otherwise `null`
         */
        collect_all_channel_ports_by_specifier_and_context(specifier: string, play_context: boolean): Port[];

        /**
         * Done processing specified by `recall_id`.
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        done_recall(recall_id: RecallID): void;

        /**
         * Duplicate {@link AgsAudio.Recall} template and assign `recall_id` to it.
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        duplicate_recall(recall_id: RecallID): void;

        /**
         * Iterates until the first {@link AgsAudio.Channel} was found.
         * @returns the first {@link AgsAudio.Channel}
         */
        first(): Channel;

        /**
         * Find first recycling related to `channel`.
         * @returns the first channel with an {@link AgsAudio.Recycling}
         */
        first_with_recycling(): Channel;

        /**
         * Gets absolute key.
         * @returns the absolute key
         */
        get_absolute_key(): number;

        /**
         * Get audio.
         * @returns the {@link AgsAudio.Audio}
         */
        get_audio<T = GObject.Object>(): T;

        /**
         * Gets audio_channel.
         * @returns the audio_channel
         */
        get_audio_channel(): number;

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get the input soundcard object of `channel`.
         * @returns the input soundcard
         */
        get_input_soundcard<T = GObject.Object>(): T;

        /**
         * Get the input soundcard object of `channel`.
         * @returns the input soundcard
         */
        get_input_soundcard_channel(): number;

        /**
         * Gets key.
         * @returns the key
         */
        get_key(): number;

        /**
         * Get level.
         * @returns the level of `channel` as {@link AgsAudio.Channel} or `null` if no parent recycling.
         */
        get_level(): Channel;

        /**
         * Gets line.
         * @returns the line
         */
        get_line(): number;

        /**
         * Ascend/descend `channel`.
         * @returns the link of {@link AgsAudio.Channel} if available, otherwise `null`
         */
        get_link(): Channel;

        /**
         * Gets octave.
         * @returns the octave
         */
        get_octave(): number;

        /**
         * Get the output soundcard object of `channel`.
         * @returns the output soundcard
         */
        get_output_soundcard<T = GObject.Object>(): T;

        /**
         * Get the output soundcard object of `channel`.
         * @returns the output soundcard
         */
        get_output_soundcard_channel(): number;

        /**
         * Gets pad.
         * @returns the pad
         */
        get_pad(): number;

        /**
         * Get pattern.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Pattern}
         */
        get_pattern(): Pattern[];

        /**
         * Get play.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Recall}
         */
        get_play(): Recall[];

        /**
         * Get playback.
         * @returns the {@link AgsAudio.Playback}
         */
        get_playback<T = GObject.Object>(): T;

        /**
         * Get recall.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Recall}
         */
        get_recall(): Recall[];

        /**
         * Get recall_container.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.RecallContainer}
         */
        get_recall_container(): RecallContainer[];

        /**
         * Get recall id.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.RecallID}
         */
        get_recall_id(): RecallID[];

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Prepare {@link AgsAudio.Recall} objects and invoke {@link AgsAudio.Recall.SignalSignatures.run_init_pre | AgsAudio.Recall::run-init-pre}, {@link AgsAudio.Recall.SignalSignatures.run_init_inter | AgsAudio.Recall::run-init-inter} or
         * {@link AgsAudio.Recall.SignalSignatures.run_init_post | AgsAudio.Recall::run-init-post} as specified by `staging_flags`.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @param staging_flags the stages to invoke
         */
        init_recall(recall_id: RecallID, staging_flags: SoundStagingFlags): void;

        /**
         * Insert `recall` at `position` in `channel`'s `play_context`.
         * @param recall the {@link AgsAudio.Recall}
         * @param play_context `true` if play context, else if `false` recall context
         * @param position the position
         */
        insert_recall(recall: GObject.Object, play_context: boolean, position: number): void;

        /**
         * Iterates until the last {@link AgsAudio.Channel} was found.
         * @returns the last {@link AgsAudio.Channel}
         */
        last(): Channel;

        /**
         * Find last recycling related to `channel`.
         * @returns the last channel with an {@link AgsAudio.Recycling}
         */
        last_with_recycling(): Channel;

        /**
         * Find next {@link AgsAudio.Recycling} of `channel`.
         * @returns the next channel with an {@link AgsAudio.Recycling}
         */
        next_with_recycling(): Channel;

        /**
         * Iterates `nth` times forward.
         * @param nth the count to iterate
         * @returns the nth {@link AgsAudio.Channel}
         */
        nth(nth: number): Channel;

        /**
         * Iterates until the first pad has been reached.
         * @returns the first {@link AgsAudio.Channel} with the same audio channel as `channel`
         */
        pad_first(): Channel;

        /**
         * Iterates until the last pad has been reached.
         * @returns the last {@link AgsAudio.Channel} with the same audio channel as `channel`
         */
        pad_last(): Channel;

        /**
         * Iterates nth times.
         * @param nth the count of pads to step
         * @returns the nth pad
         */
        pad_nth(nth: number): Channel;

        /**
         * Run the specified steps by `recall_id` of `channel`.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @param staging_flags the stages to invoke
         */
        play_recall(recall_id: RecallID, staging_flags: SoundStagingFlags): void;

        /**
         * Find previous {@link AgsAudio.Recycling} of `channel`.
         * @returns the prev channel with an {@link AgsAudio.Recycling}
         */
        prev_with_recycling(): Channel;

        /**
         * Recursive run stage specified by `staging_flags` for matching `sound_scope`.
         * @param sound_scope the sound scope
         * @param staging_flags the staging flags
         */
        recursive_run_stage(sound_scope: number, staging_flags: SoundStagingFlags): void;

        /**
         * Recursive set property for {@link AgsAudio.Channel}.
         * @param n_params the count of paramter name and value pairs
         * @param parameter_name a string vector containing parameter names
         * @param value the value array
         */
        recursive_set_property(n_params: number, parameter_name: string, value: (GObject.Value | any)): void;

        /**
         * Modify recycling. Asynchronously only.
         * @param old_start_region first recycling
         * @param old_end_region last recycling
         * @param new_start_region new first recycling
         * @param new_end_region new last recycling
         * @param old_start_changed_region modified link recycling start
         * @param old_end_changed_region modified link recyclig end
         * @param new_start_changed_region replacing link recycling start
         * @param new_end_changed_region replacing link recycling end
         */
        recycling_changed(old_start_region: Recycling, old_end_region: Recycling, new_start_region: Recycling, new_end_region: Recycling, old_start_changed_region: Recycling, old_end_changed_region: Recycling, new_start_changed_region: Recycling, new_end_changed_region: Recycling): void;

        /**
         * Removes a pattern.
         * @param pattern the {@link AgsAudio.Pattern}
         */
        remove_pattern(pattern: GObject.Object): void;

        /**
         * Removes a recall.
         * @param recall the {@link AgsAudio.Recall}
         * @param play_context `true` if play context, else if `false` recall context
         */
        remove_recall(recall: GObject.Object, play_context: boolean): void;

        /**
         * Removes a recall container.
         * @param recall_container the {@link AgsAudio.RecallContainer}
         */
        remove_recall_container(recall_container: GObject.Object): void;

        /**
         * Removes a recall id.
         * @param recall_id the {@link AgsAudio.RecallID}
         */
        remove_recall_id(recall_id: RecallID): void;

        /**
         * Called by `ags_channel_set_link()` to handle outdated {@link AgsAudio.Recycling} references.
         * Invoke only by a task.
         * @param first_recycling the recycling to set for channel->first_recycling
         * @param last_recycling the recycling to set for channel->last_recycling
         */
        reset_recycling(first_recycling: Recycling, last_recycling: Recycling): void;

        /**
         * Resolve step of initialization.
         * @param recall_id appropriate {@link AgsAudio.RecallID}
         */
        resolve_recall(recall_id: RecallID): void;

        /**
         * Enable an ability of AgsChannel.
         * @param ability_flags see enum AgsSoundAbilityFlags
         */
        set_ability_flags(ability_flags: SoundAbilityFlags): void;

        /**
         * Sets absolute key.
         * @param absolute_key the absolute key
         */
        set_absolute_key(absolute_key: number): void;

        /**
         * Set audio.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: GObject.Object): void;

        /**
         * Set audio_channel.
         * @param audio_channel the audio_channel
         */
        set_audio_channel(audio_channel: number): void;

        /**
         * Set behaviour flags.
         * @param behaviour_flags the behaviour flags
         */
        set_behaviour_flags(behaviour_flags: SoundBehaviourFlags): void;

        /**
         * Set buffer-size.
         * @param buffer_size the buffer_size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Enable a feature of `channel`.
         * @param flags see {@link AgsAudio.ChannelFlags}-enum
         */
        set_flags(flags: ChannelFlags): void;

        /**
         * Set format.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set the input soundcard object of `channel`.
         * @param input_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_input_soundcard(input_soundcard: GObject.Object): void;

        /**
         * Set the input soundcard channel of `channel`.
         * @param input_soundcard_channel the input soundcard channel
         */
        set_input_soundcard_channel(input_soundcard_channel: number): void;

        /**
         * Sets key.
         * @param key the key
         */
        set_key(key: number): void;

        /**
         * Set line.
         * @param line the line
         */
        set_line(line: number): void;

        /**
         * Change the linking of {@link AgsAudio.Channel} objects. Sets link, calls `ags_channel_reset_recycling()`.
         * Further it does loop detection and makes your machine running.
         * @param link an other {@link AgsAudio.Channel} to link with
         */
        set_link(link: Channel): void;

        /**
         * Sets octave.
         * @param octave the octave
         */
        set_octave(octave: number): void;

        /**
         * Set the output soundcard object of `channel`.
         * @param output_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_output_soundcard(output_soundcard: GObject.Object): void;

        /**
         * Set the output soundcard channel of `channel`.
         * @param output_soundcard_channel the output soundcard channel
         */
        set_output_soundcard_channel(output_soundcard_channel: number): void;

        /**
         * Set pad.
         * @param pad the pad
         */
        set_pad(pad: number): void;

        /**
         * Set pattern by replacing existing.
         * @param pattern the {@link GLib.List}-struct containing {@link AgsAudio.Pattern}
         */
        set_pattern(pattern: Pattern[]): void;

        /**
         * Set play by replacing existing.
         * @param play the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         */
        set_play(play: Recall[]): void;

        /**
         * Set playback.
         * @param playback the {@link AgsAudio.Playback}
         */
        set_playback(playback: GObject.Object): void;

        /**
         * Set recall by replacing existing.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         */
        set_recall(recall: Recall[]): void;

        /**
         * Set recall_container by replacing existing.
         * @param recall_container the {@link GLib.List}-struct containing {@link AgsAudio.RecallContainer}
         */
        set_recall_container(recall_container: RecallContainer[]): void;

        /**
         * Set recall id by replacing existing.
         * @param recall_id the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         */
        set_recall_id(recall_id: RecallID[]): void;

        /**
         * Set samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `sound_scope` to be completed.
         * @param sound_scope the {@link AgsAudio.SoundScope} to apply, or -1 to apply to all
         */
        set_staging_completed(sound_scope: number): void;

        /**
         * Set staging flags.
         * @param sound_scope the {@link AgsAudio.SoundScope} to apply, or -1 to apply to all
         * @param staging_flags the staging flags
         */
        set_staging_flags(sound_scope: number, staging_flags: SoundStagingFlags): void;

        /**
         * Start `channel`'s `sound_scope` to do playback.
         * @param sound_scope the sound scope
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         */
        start(sound_scope: number): RecallID[];

        /**
         * Stop `channel`'s `sound_scope` playback specified by `recall_id`.
         * @param recall_id the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         * @param sound_scope the sound scope
         */
        stop(recall_id: RecallID[], sound_scope: number): void;

        /**
         * Test `ability_flags` to be set on `channel`.
         * @param ability_flags the ability flags
         * @returns `true` if flags are set, else `false`
         */
        test_ability_flags(ability_flags: SoundAbilityFlags): boolean;

        /**
         * Test `behaviour_flags` to be set on `channel`.
         * @param behaviour_flags the behaviour flags
         * @returns `true` if flags are set, else `false`
         */
        test_behaviour_flags(behaviour_flags: SoundBehaviourFlags): boolean;

        /**
         * Test `flags` to be set on `channel`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: ChannelFlags): boolean;

        /**
         * Test `sound_scope` to be completed on `channel`.
         * @param sound_scope the {@link AgsAudio.SoundScope} to test
         * @returns `true` if completed, otherwise `false`
         */
        test_staging_completed(sound_scope: number): boolean;

        /**
         * Test `staging_flags` to be set on `channel`.
         * @param sound_scope the {@link AgsAudio.SoundScope} to test
         * @param staging_flags the staging flags
         * @returns `true` if flags are set, else `false`
         */
        test_staging_flags(sound_scope: number, staging_flags: SoundStagingFlags): boolean;

        /**
         * Disable an ability of AgsChannel.
         * @param ability_flags see enum AgsSoundAbilityFlags
         */
        unset_ability_flags(ability_flags: SoundAbilityFlags): void;

        /**
         * Unset behaviour flags.
         * @param behaviour_flags the behaviour flags
         */
        unset_behaviour_flags(behaviour_flags: SoundBehaviourFlags): void;

        /**
         * Disable a feature of `channel`.
         * @param flags see {@link AgsAudio.ChannelFlags}-enum
         */
        unset_flags(flags: ChannelFlags): void;

        /**
         * Unset `sound_scope` to be completed.
         * @param sound_scope the {@link AgsAudio.SoundScope} to apply, or -1 to apply to all
         */
        unset_staging_completed(sound_scope: number): void;

        /**
         * Unset staging flags.
         * @param sound_scope the {@link AgsAudio.SoundScope} to apply, or -1 to apply to all
         * @param staging_flags the staging flags
         */
        unset_staging_flags(sound_scope: number, staging_flags: SoundStagingFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace ChannelThread {
        // Signal signatures
        interface SignalSignatures extends Ags.Thread.SignalSignatures {
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::default-output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::processing": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::max-precision": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Thread.ConstructorProps, Ags.Connectable.ConstructorProps {
            channel: Channel;
            default_output_soundcard: GObject.Object;
            defaultOutputSoundcard: GObject.Object;
            processing: boolean;
            task_launcher: Ags.TaskLauncher;
            taskLauncher: Ags.TaskLauncher;
        }
    }

    /**
     * @gir-type Class
     */
    class ChannelThread extends Ags.Thread implements Ags.Connectable {
        static $gtype: GObject.GType<ChannelThread>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Channel}.
         * @since 3.0.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * The assigned default soundcard.
         * @since 3.0.0
         */
        get default_output_soundcard(): GObject.Object;
        set default_output_soundcard(val: GObject.Object);

        /**
         * The assigned default soundcard.
         * @since 3.0.0
         */
        get defaultOutputSoundcard(): GObject.Object;
        set defaultOutputSoundcard(val: GObject.Object);

        /**
         * The processing state.
         * @since 3.11.0
         * @default false
         */
        get processing(): boolean;
        set processing(val: boolean);

        /**
         * The assigned {@link Ags.TaskLauncher}.
         * @since 3.11.0
         * @read-only
         */
        get task_launcher(): Ags.TaskLauncher;

        /**
         * The assigned {@link Ags.TaskLauncher}.
         * @since 3.11.0
         * @read-only
         */
        get taskLauncher(): Ags.TaskLauncher;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ChannelThread.SignalSignatures;

        // Fields
        thread: Ags.Thread;

        nested_sync_flags: ChannelThreadNestedSyncFlags;

        sound_scope: number;

        do_fx_staging: boolean;

        staging_program: number;

        staging_program_count: number;

        // Constructors
        constructor(properties?: Partial<ChannelThread.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](default_output_soundcard: GObject.Object, channel: GObject.Object): ChannelThread;

        // Conflicted with Ags.Thread.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof ChannelThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ChannelThread.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ChannelThread.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ChannelThread.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get do fx staging.
         * @returns `true` if set, otherwise `false`
         */
        get_do_fx_staging(): boolean;

        /**
         * Get processing.
         * @returns `true` if do processing, otherwise `false`
         */
        get_processing(): boolean;

        /**
         * Get staging program.
         * @returns the staging program
         */
        get_staging_program(): [number, number];

        /**
         * Get task launcher.
         * @returns the {@link Ags.TaskLauncher}
         */
        get_task_launcher(): Ags.TaskLauncher;

        /**
         * Set do fx staging.
         * @param do_fx_staging `true` if do fx staging, else %FALSe
         */
        set_do_fx_staging(do_fx_staging: boolean): void;

        /**
         * Set status flags.
         * @param nested_sync_flags status flags
         */
        set_nested_sync_flags(nested_sync_flags: ChannelThreadNestedSyncFlags): void;

        /**
         * Set processing.
         * @param processing `true` enables and `false` disables processing
         */
        set_processing(processing: boolean): void;

        /**
         * Set sound scope.
         * @param sound_scope the sound scope
         */
        set_sound_scope(sound_scope: number): void;

        /**
         * Set staging program.
         * @param staging_program the staging program
         * @param staging_program_count the staging program count
         */
        set_staging_program(staging_program: number, staging_program_count: number): void;

        /**
         * Test `nested_sync_flags` of `channel_thread`.
         * @param nested_sync_flags nested sync flags
         * @returns `true` if status flags set, otherwise `false`
         */
        test_nested_sync_flags(nested_sync_flags: ChannelThreadNestedSyncFlags): boolean;

        /**
         * Unset status flags.
         * @param nested_sync_flags status flags
         */
        unset_nested_sync_flags(nested_sync_flags: ChannelThreadNestedSyncFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace ClearAudioSignal {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio-signal": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio_signal: AudioSignal;
            audioSignal: AudioSignal;
        }
    }

    /**
     * @gir-type Class
     */
    class ClearAudioSignal extends Ags.Task {
        static $gtype: GObject.GType<ClearAudioSignal>;

        // Properties
        /**
         * The assigned {@link AgsAudio.AudioSignal}
         * @since 3.0.0
         */
        get audio_signal(): AudioSignal;
        set audio_signal(val: AudioSignal);

        /**
         * The assigned {@link AgsAudio.AudioSignal}
         * @since 3.0.0
         */
        get audioSignal(): AudioSignal;
        set audioSignal(val: AudioSignal);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ClearAudioSignal.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ClearAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): ClearAudioSignal;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ClearAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ClearAudioSignal.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ClearAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ClearAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ClearAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ClearAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ClearBuffer {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            device: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class ClearBuffer extends Ags.Task {
        static $gtype: GObject.GType<ClearBuffer>;

        // Properties
        /**
         * The assigned {@link Ags.Soundcard} or {@link Ags.Sequencer}
         * @since 3.0.0
         */
        get device(): GObject.Object;
        set device(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ClearBuffer.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ClearBuffer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](device: GObject.Object): ClearBuffer;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ClearBuffer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ClearBuffer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ClearBuffer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ClearBuffer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ClearBuffer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ClearBuffer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace CoreAudioClient {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::client-name": (pspec: GObject.ParamSpec) => void;
            "notify::core-audio-server": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            client_name: string;
            clientName: string;
            core_audio_server: CoreAudioServer;
            coreAudioServer: CoreAudioServer;
            device: GObject.Object[];
            port: CoreAudioPort[];
        }
    }

    /**
     * @gir-type Class
     */
    class CoreAudioClient extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<CoreAudioClient>;

        // Properties
        /**
         * The core-audio client name.
         * @since 3.0.0
         * @default null
         */
        get client_name(): string;
        set client_name(val: string);

        /**
         * The core-audio client name.
         * @since 3.0.0
         * @default null
         */
        get clientName(): string;
        set clientName(val: string);

        /**
         * The assigned {@link AgsAudio.CoreAudioServer}.
         * @since 3.0.0
         */
        get core_audio_server(): CoreAudioServer;
        set core_audio_server(val: CoreAudioServer);

        /**
         * The assigned {@link AgsAudio.CoreAudioServer}.
         * @since 3.0.0
         */
        get coreAudioServer(): CoreAudioServer;
        set coreAudioServer(val: CoreAudioServer);

        /**
         * The assigned devices.
         * @since 3.0.0
         */
        get device(): GObject.Object[];
        set device(val: GObject.Object[]);

        /**
         * The assigned ports.
         * @since 3.0.0
         */
        get port(): CoreAudioPort[];
        set port(val: CoreAudioPort[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CoreAudioClient.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: CoreAudioClientFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        client_uuid: string;

        graph: any;

        // Constructors
        constructor(properties?: Partial<CoreAudioClient.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](core_audio_server: GObject.Object): CoreAudioClient;

        // Signals
        /** @signal */
        connect_after<K extends keyof CoreAudioClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CoreAudioClient.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CoreAudioClient.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CoreAudioClient.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `client_name` in `core_audio_client`.
         * @param core_audio_client the {@link GLib.List}-struct containing {@link AgsAudio.CoreAudioClient}
         * @param client_name the client name to find
         */
        static find(core_audio_client: CoreAudioClient[], client_name: string): CoreAudioClient[];

        /**
         * Finds next match of `client_uuid` in `core_audio_client`.
         * @param core_audio_client the {@link GLib.List}-struct containing {@link AgsAudio.CoreAudioClient}
         * @param client_uuid the client uuid to find
         */
        static find_uuid(core_audio_client: CoreAudioClient[], client_uuid: string): CoreAudioClient[];

        // Methods
        /**
         * Activate client.
         */
        activate(): void;

        /**
         * Add `core_audio_device` to `core_audio_client`.
         * @param core_audio_device an {@link AgsAudio.CoreAudioDevout} or {@link AgsAudio.CoreAudioDevin}
         */
        add_device(core_audio_device: GObject.Object): void;

        /**
         * Add `core_audio_port` to `core_audio_client`.
         * @param core_audio_port an {@link AgsAudio.CoreAudioPort}
         */
        add_port(core_audio_port: GObject.Object): void;

        /**
         * Deactivate client.
         */
        deactivate(): void;

        /**
         * Open the core audio client's connection and read uuid.
         * @param client_name the client's name
         */
        open(client_name: string): void;

        /**
         * Remove `core_audio_device` from `core_audio_client`.
         * @param core_audio_device an {@link AgsAudio.CoreAudioDevout} or {@link AgsAudio.CoreAudioDevin}
         */
        remove_device(core_audio_device: GObject.Object): void;

        /**
         * Remove `core_audio_port` from `core_audio_client`.
         * @param core_audio_port an {@link AgsAudio.CoreAudioPort}
         */
        remove_port(core_audio_port: GObject.Object): void;

        /**
         * Enable a feature of `core_audio_client`.
         * @param flags see {@link AgsAudio.CoreAudioClientFlags}-enum
         */
        set_flags(flags: CoreAudioClientFlags): void;

        /**
         * Test `flags` to be set on `core_audio_client`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: CoreAudioClientFlags): boolean;

        /**
         * Disable a feature of `core_audio_client`.
         * @param flags see {@link AgsAudio.CoreAudioClientFlags}-enum
         */
        unset_flags(flags: CoreAudioClientFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace CoreAudioDevin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::core-audio-client": (pspec: GObject.ParamSpec) => void;
            "notify::core-audio-port": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            core_audio_client: CoreAudioClient;
            coreAudioClient: CoreAudioClient;
            core_audio_port: any;
            coreAudioPort: any;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class CoreAudioDevin extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<CoreAudioDevin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The assigned {@link AgsAudio.CoreAudioClient}
         * @since 3.0.0
         */
        get core_audio_client(): CoreAudioClient;
        set core_audio_client(val: CoreAudioClient);

        /**
         * The assigned {@link AgsAudio.CoreAudioClient}
         * @since 3.0.0
         */
        get coreAudioClient(): CoreAudioClient;
        set coreAudioClient(val: CoreAudioClient);

        /**
         * The assigned {@link AgsAudio.CoreAudioPort}
         * @since 3.0.0
         */
        get core_audio_port(): any;
        set core_audio_port(val: any);

        /**
         * The assigned {@link AgsAudio.CoreAudioPort}
         * @since 3.0.0
         */
        get coreAudioPort(): any;
        set coreAudioPort(val: any);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The core audio soundcard indentifier
         * @since 3.0.0
         * @default ags-core-audio-devin-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CoreAudioDevin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: CoreAudioDevinFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: CoreAudioDevinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: CoreAudioDevinAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<CoreAudioDevin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): CoreAudioDevin;

        // Signals
        /** @signal */
        connect_after<K extends keyof CoreAudioDevin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CoreAudioDevin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CoreAudioDevin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CoreAudioDevin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `core_audio_devin`.
         * @param flags see {@link AgsAudio.CoreAudioDevinFlags}-enum
         */
        set_flags(flags: CoreAudioDevinFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `core_audio_devin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: CoreAudioDevinFlags): boolean;

        /**
         * Disable a feature of `core_audio_devin`.
         * @param flags see {@link AgsAudio.CoreAudioDevinFlags}-enum
         */
        unset_flags(flags: CoreAudioDevinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace CoreAudioDevout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::core-audio-client": (pspec: GObject.ParamSpec) => void;
            "notify::core-audio-port": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            core_audio_client: CoreAudioClient;
            coreAudioClient: CoreAudioClient;
            core_audio_port: any;
            coreAudioPort: any;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class CoreAudioDevout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<CoreAudioDevout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The assigned {@link AgsAudio.CoreAudioClient}
         * @since 3.0.0
         */
        get core_audio_client(): CoreAudioClient;
        set core_audio_client(val: CoreAudioClient);

        /**
         * The assigned {@link AgsAudio.CoreAudioClient}
         * @since 3.0.0
         */
        get coreAudioClient(): CoreAudioClient;
        set coreAudioClient(val: CoreAudioClient);

        /**
         * The assigned {@link AgsAudio.CoreAudioPort}
         * @since 3.0.0
         */
        get core_audio_port(): any;
        set core_audio_port(val: any);

        /**
         * The assigned {@link AgsAudio.CoreAudioPort}
         * @since 3.0.0
         */
        get coreAudioPort(): any;
        set coreAudioPort(val: any);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The core audio soundcard indentifier
         * @since 3.0.0
         * @default ags-core-audio-devout-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CoreAudioDevout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: CoreAudioDevoutFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: CoreAudioDevoutSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: CoreAudioDevoutAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<CoreAudioDevout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): CoreAudioDevout;

        // Signals
        /** @signal */
        connect_after<K extends keyof CoreAudioDevout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CoreAudioDevout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CoreAudioDevout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CoreAudioDevout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `core_audio_devout`.
         * @param flags see {@link AgsAudio.CoreAudioDevoutFlags}-enum
         */
        set_flags(flags: CoreAudioDevoutFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `core_audio_devout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: CoreAudioDevoutFlags): boolean;

        /**
         * Disable a feature of `core_audio_devout`.
         * @param flags see {@link AgsAudio.CoreAudioDevoutFlags}-enum
         */
        unset_flags(flags: CoreAudioDevoutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace CoreAudioMidiin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::core-audio-client": (pspec: GObject.ParamSpec) => void;
            "notify::core-audio-port": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Sequencer.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            core_audio_client: CoreAudioClient;
            coreAudioClient: CoreAudioClient;
            core_audio_port: CoreAudioPort;
            coreAudioPort: CoreAudioPort;
            delay_factor: number;
            delayFactor: number;
            device: string;
        }
    }

    /**
     * @gir-type Class
     */
    class CoreAudioMidiin extends GObject.Object implements Ags.Connectable, Ags.Sequencer {
        static $gtype: GObject.GType<CoreAudioMidiin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The assigned {@link AgsAudio.CoreAudioClient}
         * @since 3.0.0
         */
        get core_audio_client(): CoreAudioClient;
        set core_audio_client(val: CoreAudioClient);

        /**
         * The assigned {@link AgsAudio.CoreAudioClient}
         * @since 3.0.0
         */
        get coreAudioClient(): CoreAudioClient;
        set coreAudioClient(val: CoreAudioClient);

        /**
         * The assigned {@link AgsAudio.CoreAudioPort}
         * @since 3.0.0
         */
        get core_audio_port(): CoreAudioPort;
        set core_audio_port(val: CoreAudioPort);

        /**
         * The assigned {@link AgsAudio.CoreAudioPort}
         * @since 3.0.0
         */
        get coreAudioPort(): CoreAudioPort;
        set coreAudioPort(val: CoreAudioPort);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The core audio sequencer indentifier
         * @since 3.0.0
         * @default hw:0
         */
        get device(): string;
        set device(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CoreAudioMidiin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: CoreAudioMidiinFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: CoreAudioMidiinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: CoreAudioMidiinAppBufferMode;

        app_buffer: string;

        app_buffer_size: number[];

        delay: number;

        latency: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        card_uri: string;

        port_name: string;

        // Constructors
        constructor(properties?: Partial<CoreAudioMidiin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): CoreAudioMidiin;

        // Signals
        /** @signal */
        connect_after<K extends keyof CoreAudioMidiin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CoreAudioMidiin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CoreAudioMidiin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CoreAudioMidiin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Enable a feature of `core_audio_midiin`.
         * @param flags see {@link AgsAudio.CoreAudioMidiinFlags}-enum
         */
        set_flags(flags: CoreAudioMidiinFlags): void;

        /**
         * The buffer flag indicates the currently recorded buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `core_audio_midiin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: CoreAudioMidiinFlags): boolean;

        /**
         * Disable a feature of `core_audio_midiin`.
         * @param flags see {@link AgsAudio.CoreAudioMidiinFlags}-enum
         */
        unset_flags(flags: CoreAudioMidiinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @param buffer_length the buffer's length
         * @returns current playback buffer
         */
        get_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get future playback buffer.
         * @param buffer_length the buffer's length
         * @returns next playback buffer
         */
        get_next_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset(): number;

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Plays the current buffer of sequencer.
         */
        play(): void;

        /**
         * Initializes the sequencer for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of sequencer.
         */
        record(): void;

        /**
         * Initializes the sequencer for recording.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the sequencer from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @param buffer_length the buffer's length
         * @virtual
         */
        vfunc_get_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get future playback buffer.
         * @param buffer_length the buffer's length
         * @virtual
         */
        vfunc_get_next_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Plays the current buffer of sequencer.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the sequencer for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of sequencer.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the sequencer for recording.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the sequencer from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;
    }


    namespace CoreAudioPort {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::core-audio-client": (pspec: GObject.ParamSpec) => void;
            "notify::core-audio-device": (pspec: GObject.ParamSpec) => void;
            "notify::port-name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            core_audio_client: CoreAudioClient;
            coreAudioClient: CoreAudioClient;
            core_audio_device: GObject.Object;
            coreAudioDevice: GObject.Object;
            port_name: string;
            portName: string;
        }
    }

    /**
     * @gir-type Class
     */
    class CoreAudioPort extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<CoreAudioPort>;

        // Properties
        /**
         * The assigned {@link AgsAudio.CoreAudioClient}.
         * @since 3.0.0
         */
        get core_audio_client(): CoreAudioClient;
        set core_audio_client(val: CoreAudioClient);

        /**
         * The assigned {@link AgsAudio.CoreAudioClient}.
         * @since 3.0.0
         */
        get coreAudioClient(): CoreAudioClient;
        set coreAudioClient(val: CoreAudioClient);

        /**
         * The assigned {@link AgsAudio.CoreAudioDevout}.
         * @since 3.0.0
         */
        get core_audio_device(): GObject.Object;
        set core_audio_device(val: GObject.Object);

        /**
         * The assigned {@link AgsAudio.CoreAudioDevout}.
         * @since 3.0.0
         */
        get coreAudioDevice(): GObject.Object;
        set coreAudioDevice(val: GObject.Object);

        /**
         * The core audio soundcard indentifier
         * @since 3.0.0
         * @default null
         */
        get port_name(): string;
        set port_name(val: string);

        /**
         * The core audio soundcard indentifier
         * @since 3.0.0
         * @default null
         */
        get portName(): string;
        set portName(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CoreAudioPort.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: CoreAudioPortFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        port_uuid: string;

        pcm_channels: number;

        samplerate: number;

        buffer_size: number;

        format: Ags.SoundcardFormat;

        use_cache: boolean;

        cache_buffer_size: number;

        current_cache: number;

        completed_cache: number;

        cache_offset: number;

        cache: any;

        aq_ref: any;

        data_format: any;

        buffer: any;

        midi_client: any;

        midi_port: any;

        midi_port_number: number;

        output_running: boolean;

        input_running: boolean;

        is_empty: boolean;

        queued: number;

        output_samplerate_property_address: any;

        output_buffer_size_property_address: any;

        output_property_address: any;

        output_device: number;

        output_proc_id: number;

        input_samplerate_property_address: any;

        input_buffer_size_property_address: any;

        input_property_address: any;

        input_device: number;

        input_proc_id: number;

        // Constructors
        constructor(properties?: Partial<CoreAudioPort.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](core_audio_client: GObject.Object): CoreAudioPort;

        // Signals
        /** @signal */
        connect_after<K extends keyof CoreAudioPort.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CoreAudioPort.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CoreAudioPort.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CoreAudioPort.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `port_name` in `core_audio_port`.
         * @param core_audio_port the {@link GLib.List}-struct containing {@link AgsAudio.CoreAudioPort}
         * @param port_name the port name to find
         */
        static find(core_audio_port: CoreAudioPort[], port_name: string): CoreAudioPort[];

        // Methods
        /**
         * Gets latency.
         */
        get_latency(): number;

        /**
         * Register a new core audio port and read uuid. Creates a new AgsSequencer or AgsSoundcard
         * object.
         * @param port_name the name as string
         * @param is_audio if `true` interpreted as audio port
         * @param is_midi if `true` interpreted as midi port
         * @param is_output if `true` port is acting as output, otherwise as input
         */
        register(port_name: string, is_audio: boolean, is_midi: boolean, is_output: boolean): void;

        /**
         * @param buffer_size 
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * @param cache_buffer_size 
         */
        set_cache_buffer_size(cache_buffer_size: number): void;

        /**
         * Enable a feature of `core_audio_port`.
         * @param flags see {@link AgsAudio.CoreAudioPortFlags}-enum
         */
        set_flags(flags: CoreAudioPortFlags): void;

        /**
         * @param format 
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * @param pcm_channels 
         */
        set_pcm_channels(pcm_channels: number): void;

        /**
         * @param samplerate 
         */
        set_samplerate(samplerate: number): void;

        /**
         * Test `flags` to be set on `core_audio_port`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: CoreAudioPortFlags): boolean;

        unregister(): void;

        /**
         * Disable a feature of `core_audio_port`.
         * @param flags see {@link AgsAudio.CoreAudioPortFlags}-enum
         */
        unset_flags(flags: CoreAudioPortFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace CoreAudioServer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::core-audio-client": (pspec: GObject.ParamSpec) => void;
            "notify::default-core-audio-client": (pspec: GObject.ParamSpec) => void;
            "notify::default-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.SoundServer.ConstructorProps {
            core_audio_client: CoreAudioClient[];
            coreAudioClient: CoreAudioClient[];
            default_core_audio_client: CoreAudioClient;
            defaultCoreAudioClient: CoreAudioClient;
            default_soundcard: GObject.Object;
            defaultSoundcard: GObject.Object;
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class CoreAudioServer extends GObject.Object implements Ags.Connectable, Ags.SoundServer {
        static $gtype: GObject.GType<CoreAudioServer>;

        // Properties
        /**
         * The core audio client list.
         * @since 3.0.0
         */
        get core_audio_client(): CoreAudioClient[];
        set core_audio_client(val: CoreAudioClient[]);

        /**
         * The core audio client list.
         * @since 3.0.0
         */
        get coreAudioClient(): CoreAudioClient[];
        set coreAudioClient(val: CoreAudioClient[]);

        /**
         * The default core audio client.
         * @since 3.0.0
         */
        get default_core_audio_client(): CoreAudioClient;
        set default_core_audio_client(val: CoreAudioClient);

        /**
         * The default core audio client.
         * @since 3.0.0
         */
        get defaultCoreAudioClient(): CoreAudioClient;
        set defaultCoreAudioClient(val: CoreAudioClient);

        /**
         * The default soundcard.
         * @since 3.0.0
         */
        get default_soundcard(): GObject.Object;
        set default_soundcard(val: GObject.Object);

        /**
         * The default soundcard.
         * @since 3.0.0
         */
        get defaultSoundcard(): GObject.Object;
        set defaultSoundcard(val: GObject.Object);

        /**
         * The assigned URL.
         * @since 3.0.0
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CoreAudioServer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        running: boolean;

        uuid: Ags.UUID;

        port: number;

        port_count: number;

        n_soundcards: number;

        n_sequencers: number;

        default_client: GObject.Object;

        client: any[];

        // Constructors
        constructor(properties?: Partial<CoreAudioServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](url: string): CoreAudioServer;

        // Signals
        /** @signal */
        connect_after<K extends keyof CoreAudioServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CoreAudioServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CoreAudioServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CoreAudioServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find {@link AgsAudio.CoreAudioServer} by url.
         * @param core_audio_server the {@link GLib.List}-struct containing {@link AgsAudio.CoreAudioServer}
         * @param url the url to find
         */
        static find_url(core_audio_server: CoreAudioServer[], url: string): CoreAudioServer[];

        // Methods
        /**
         * Add `core_audio_client` to `core_audio_server`
         * @param core_audio_client the {@link AgsAudio.CoreAudioClient} to add
         */
        add_client(core_audio_client: GObject.Object): void;

        /**
         * Connect all clients.
         */
        connect_client(): void;

        /**
         * Find {@link AgsAudio.CoreAudioClient} by uuid.
         * @param client_uuid the uuid to find
         * @returns the {@link AgsAudio.CoreAudioClient} found or `null`
         */
        find_client<T = GObject.Object>(client_uuid: string): T;

        /**
         * Find {@link AgsAudio.CoreAudioPort} by uuid.
         * @param port_uuid the uuid to find
         * @returns the {@link AgsAudio.CoreAudioPort} found or `null`
         */
        find_port<T = GObject.Object>(port_uuid: string): T;

        /**
         * Remove `core_audio_client` to `core_audio_server`
         * @param core_audio_client the {@link AgsAudio.CoreAudioClient} to remove
         */
        remove_client(core_audio_client: GObject.Object): void;

        start_poll(): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @returns the port as string `null`-terminated array
         */
        get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the sequencer as {@link GLib.List}-struct
         */
        get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the soundcard as {@link GLib.List}-struct
         */
        get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @returns the URL as string
         */
        get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Sequencer}
         */
        register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Soundcard}
         */
        register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param port 
         * @param port_count 
         */
        set_ports(port: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         */
        set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         */
        set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         */
        set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         */
        unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         */
        unregister_soundcard(soundcard: GObject.Object): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @virtual
         */
        vfunc_get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @virtual
         */
        vfunc_get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param ports 
         * @param port_count 
         * @virtual
         */
        vfunc_set_ports(ports: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         * @virtual
         */
        vfunc_set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         * @virtual
         */
        vfunc_set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         * @virtual
         */
        vfunc_set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         * @virtual
         */
        vfunc_unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         * @virtual
         */
        vfunc_unregister_soundcard(soundcard: GObject.Object): void;
    }


    namespace CropNote {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::absolute": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::do-resize": (pspec: GObject.ParamSpec) => void;
            "notify::in-place": (pspec: GObject.ParamSpec) => void;
            "notify::notation": (pspec: GObject.ParamSpec) => void;
            "notify::selection": (pspec: GObject.ParamSpec) => void;
            "notify::x-crop": (pspec: GObject.ParamSpec) => void;
            "notify::x-padding": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            absolute: boolean;
            audio: Audio;
            do_resize: boolean;
            doResize: boolean;
            in_place: boolean;
            inPlace: boolean;
            notation: Notation;
            selection: Note[];
            x_crop: number;
            xCrop: number;
            x_padding: number;
            xPadding: number;
        }
    }

    /**
     * @gir-type Class
     */
    class CropNote extends Ags.Task {
        static $gtype: GObject.GType<CropNote>;

        // Properties
        /**
         * Crop notation by absolute position.
         * @since 3.0.0
         * @default false
         */
        get absolute(): boolean;
        set absolute(val: boolean);

        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * Crop notation do resize.
         * @since 3.0.0
         * @default false
         */
        get do_resize(): boolean;
        set do_resize(val: boolean);

        /**
         * Crop notation do resize.
         * @since 3.0.0
         * @default false
         */
        get doResize(): boolean;
        set doResize(val: boolean);

        /**
         * Crop notation in place.
         * @since 3.0.0
         * @default false
         */
        get in_place(): boolean;
        set in_place(val: boolean);

        /**
         * Crop notation in place.
         * @since 3.0.0
         * @default false
         */
        get inPlace(): boolean;
        set inPlace(val: boolean);

        /**
         * The assigned {@link AgsAudio.Notation}
         * @since 3.0.0
         */
        get notation(): Notation;
        set notation(val: Notation);

        /**
         * The assigned {@link AgsAudio.Note}
         * @since 3.0.0
         */
        get selection(): Note[];
        set selection(val: Note[]);

        /**
         * Crop notation by x-crop amount.
         * @since 3.0.0
         * @default 0
         */
        get x_crop(): number;
        set x_crop(val: number);

        /**
         * Crop notation by x-crop amount.
         * @since 3.0.0
         * @default 0
         */
        get xCrop(): number;
        set xCrop(val: number);

        /**
         * Crop notation with x padding.
         * @since 3.0.0
         * @default 0
         */
        get x_padding(): number;
        set x_padding(val: number);

        /**
         * Crop notation with x padding.
         * @since 3.0.0
         * @default 0
         */
        get xPadding(): number;
        set xPadding(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: CropNote.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<CropNote.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, notation: Notation, selection: Note[], x_padding: number, x_crop: number, absolute: boolean, in_place: boolean, do_resize: boolean): CropNote;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof CropNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CropNote.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof CropNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, CropNote.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof CropNote.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<CropNote.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Set `selection` of `crop_note`.
         * @param selection the selection as {@link GLib.List}-struct
         */
        set_selection(selection: Note[]): void;
    }


    namespace DssiManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class DssiManager extends GObject.Object {
        static $gtype: GObject.GType<DssiManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DssiManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        dssi_plugin_blacklist: any[];

        dssi_plugin: any[];

        // Constructors
        constructor(properties?: Partial<DssiManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): DssiManager;

        // Signals
        /** @signal */
        connect<K extends keyof DssiManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DssiManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DssiManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DssiManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DssiManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DssiManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get dssi manager default plugin path.
         */
        static get_default_path(): string[];

        /**
         * Get instance.
         */
        static get_instance(): DssiManager;

        /**
         * Set dssi manager default plugin path.
         * @param default_path the string vector array to use as default path
         */
        static set_default_path(default_path: string[]): void;

        // Methods
        /**
         * Lookup filename in loaded plugins.
         * @param filename the filename of the plugin
         * @param effect the effect's name
         * @returns the matching {@link AgsAudio.DssiPlugin}
         */
        find_dssi_plugin(filename: string, effect: string): DssiPlugin;

        /**
         * Lookup filename in loaded plugins.
         * @param filename the filename of the plugin
         * @param effect the effect's name
         * @returns the matching {@link AgsAudio.DssiPlugin}
         */
        find_dssi_plugin_with_fallback(filename: string, effect: string): DssiPlugin;

        /**
         * Retrieve all filenames
         * @returns a `null`-terminated array of filenames
         */
        get_filenames(): string[];

        /**
         * Load blacklisted plugin filenames.
         * @param blacklist_filename the filename as string
         */
        load_blacklist(blacklist_filename: string): void;

        /**
         * Loads all available plugins.
         */
        load_default_directory(): void;

        /**
         * Load `filename` specified plugin.
         * @param dssi_path the dssi path
         * @param filename the filename of the plugin
         */
        load_file(dssi_path: string, filename: string): void;
    }


    namespace DssiPlugin {
        // Signal signatures
        interface SignalSignatures extends BasePlugin.SignalSignatures {
            /**
             * The ::change-program signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "change-program": (arg0: (any | null), arg1: number, arg2: number) => void;
            "notify::program": (pspec: GObject.ParamSpec) => void;
            "notify::unique-id": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-port": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::ui-filename": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::uuid": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends BasePlugin.ConstructorProps {
            program: string;
            unique_id: number;
            uniqueId: number;
        }
    }

    /**
     * @gir-type Class
     */
    class DssiPlugin extends BasePlugin {
        static $gtype: GObject.GType<DssiPlugin>;

        // Properties
        /**
         * @default null
         */
        get program(): string;
        set program(val: string);

        /**
         * The assigned unique-id.
         * @since 3.0.0
         * @default 0
         */
        get unique_id(): number;
        set unique_id(val: number);

        /**
         * The assigned unique-id.
         * @since 3.0.0
         * @default 0
         */
        get uniqueId(): number;
        set uniqueId(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: DssiPlugin.SignalSignatures;

        // Fields
        base_plugin: BasePlugin;

        // Constructors
        constructor(properties?: Partial<DssiPlugin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](filename: string, effect: string, effect_index: number): DssiPlugin;

        // Signals
        /** @signal */
        connect<K extends keyof DssiPlugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DssiPlugin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof DssiPlugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, DssiPlugin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof DssiPlugin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<DssiPlugin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param ladspa_handle 
         * @param bank_index 
         * @param program_index 
         * @virtual
         */
        vfunc_change_program(ladspa_handle: (any | null), bank_index: number, program_index: number): void;

        // Methods
        /**
         * @param ladspa_handle 
         * @param bank_index 
         * @param program_index 
         */
        change_program(ladspa_handle: (any | null), bank_index: number, program_index: number): void;
    }


    namespace ExportOutput {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::export-thread": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::live-performance": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::tic": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            export_thread: ExportThread;
            exportThread: ExportThread;
            filename: string;
            format: number;
            live_performance: boolean;
            livePerformance: boolean;
            soundcard: GObject.Object;
            tic: number;
        }
    }

    /**
     * @gir-type Class
     */
    class ExportOutput extends Ags.Task {
        static $gtype: GObject.GType<ExportOutput>;

        // Properties
        /**
         * The assigned {@link AgsAudio.ExportThread}
         * @since 3.0.0
         */
        get export_thread(): ExportThread;
        set export_thread(val: ExportThread);

        /**
         * The assigned {@link AgsAudio.ExportThread}
         * @since 3.0.0
         */
        get exportThread(): ExportThread;
        set exportThread(val: ExportThread);

        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * Format to use.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * Do output the audio export live.
         * @since 3.0.0
         * @default false
         */
        get live_performance(): boolean;
        set live_performance(val: boolean);

        /**
         * Do output the audio export live.
         * @since 3.0.0
         * @default false
         */
        get livePerformance(): boolean;
        set livePerformance(val: boolean);

        /**
         * The assigned {@link Ags.Soundcard}
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * Tic offset of output as end tic of it.
         * @since 3.0.0
         * @default 0
         */
        get tic(): number;
        set tic(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ExportOutput.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ExportOutput.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](export_thread: ExportThread, soundcard: GObject.Object, filename: string, tic: number, live_performance: boolean): ExportOutput;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ExportOutput.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ExportOutput.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ExportOutput.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ExportOutput.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ExportOutput.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ExportOutput.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ExportThread {
        // Signal signatures
        interface SignalSignatures extends Ags.Thread.SignalSignatures {
            "notify::audio-file": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::tic": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::max-precision": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Thread.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio_file: AudioFile;
            audioFile: AudioFile;
            soundcard: GObject.Object;
            tic: number;
        }
    }

    /**
     * @gir-type Class
     */
    class ExportThread extends Ags.Thread implements Ags.Connectable {
        static $gtype: GObject.GType<ExportThread>;

        // Properties
        /**
         * The assigned {@link AgsAudio.AudioFile}.
         * @since 3.0.0
         */
        get audio_file(): AudioFile;
        set audio_file(val: AudioFile);

        /**
         * The assigned {@link AgsAudio.AudioFile}.
         * @since 3.0.0
         */
        get audioFile(): AudioFile;
        set audioFile(val: AudioFile);

        /**
         * The assigned {@link Ags.Soundcard}.
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * The tic.
         * @since 3.0.0
         * @default 0
         */
        get tic(): number;
        set tic(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ExportThread.SignalSignatures;

        // Fields
        thread: Ags.Thread;

        flags: ExportThreadFlags;

        counter: number;

        // Constructors
        constructor(properties?: Partial<ExportThread.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](soundcard: GObject.Object, audio_file: AudioFile): ExportThread;

        // Conflicted with Ags.Thread.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof ExportThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ExportThread.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ExportThread.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ExportThread.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * @param soundcard the {@link Ags.Soundcard} to find
         * @returns the matching {@link AgsAudio.ExportThread}, if not found `null`.
         */
        find_soundcard(soundcard: GObject.Object): ExportThread;

        /**
         * Enable a feature of `export_thread`.
         * @param flags see {@link AgsAudio.ExportThreadFlags}-enum
         */
        set_flags(flags: ExportThreadFlags): void;

        /**
         * @param args 
         */
    // Conflicted with Ags.Thread.set_flags
        set_flags(...args: never[]): any;

        /**
         * Test `flags` to be set on `export_thread`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: ExportThreadFlags): boolean;

        /**
         * @param args 
         */
    // Conflicted with Ags.Thread.test_flags
        test_flags(...args: never[]): any;

        /**
         * Disable a feature of `export_thread`.
         * @param flags see {@link AgsAudio.ExportThreadFlags}-enum
         */
        unset_flags(flags: ExportThreadFlags): void;

        /**
         * @param args 
         */
    // Conflicted with Ags.Thread.unset_flags
        unset_flags(...args: never[]): any;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace Fifoout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Fifoout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<Fifoout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The fifo soundcard indentifier
         * @since 3.0.0
         * @default /dev/null
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Fifoout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: FifooutFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        app_buffer_mode: FifooutAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        available: boolean;

        ring_buffer_size: number;

        nth_ring_buffer: number;

        ring_buffer: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        fifo_fd: number;

        // Constructors
        constructor(properties?: Partial<Fifoout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Fifoout;

        // Signals
        /** @signal */
        connect_after<K extends keyof Fifoout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Fifoout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Fifoout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Fifoout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `fifoout`.
         * @param flags see {@link AgsAudio.FifooutFlags}-enum
         */
        set_flags(flags: FifooutFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `fifoout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: FifooutFlags): boolean;

        /**
         * Disable a feature of `fifoout`.
         * @param flags see {@link AgsAudio.FifooutFlags}-enum
         */
        unset_flags(flags: FifooutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace FreeSelection {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::notation": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            notation: Notation;
        }
    }

    /**
     * @gir-type Class
     */
    class FreeSelection extends Ags.Task {
        static $gtype: GObject.GType<FreeSelection>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Notation}
         * @since 3.0.0
         */
        get notation(): Notation;
        set notation(val: Notation);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FreeSelection.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<FreeSelection.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](notation: Notation): FreeSelection;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof FreeSelection.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FreeSelection.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FreeSelection.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FreeSelection.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FreeSelection.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FreeSelection.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FrequencyMap {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::compute-max-likelihood signal notifies about max likelihooded commputed.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "compute-max-likelihood": (arg0: (any | null), arg1: (any | null)) => void;
            /**
             * The ::factorize signal notifies about factorizing map.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            factorize: (arg0: GObject.Object) => void;
            /**
             * The ::process signal notifies about processed buffer.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            process: () => void;
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::freq": (pspec: GObject.ParamSpec) => void;
            "notify::oscillator-mode": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::window-count": (pspec: GObject.ParamSpec) => void;
            "notify::z-index": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            attack: number;
            buffer_size: number;
            bufferSize: number;
            frame_count: number;
            frameCount: number;
            freq: number;
            oscillator_mode: number;
            oscillatorMode: number;
            samplerate: number;
            window_count: number;
            windowCount: number;
            z_index: number;
            zIndex: number;
        }
    }

    /**
     * @gir-type Class
     */
    class FrequencyMap extends GObject.Object {
        static $gtype: GObject.GType<FrequencyMap>;

        // Properties
        /**
         * The attack to be used.
         * @since 3.0.0
         * @default 0
         */
        get attack(): number;
        set attack(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The initial size of audio data.
         * @since 3.0.0
         * @default 0
         */
        get frame_count(): number;
        set frame_count(val: number);

        /**
         * The initial size of audio data.
         * @since 3.0.0
         * @default 0
         */
        get frameCount(): number;
        set frameCount(val: number);

        /**
         * The freq to be used.
         * @since 3.0.0
         * @default 440
         */
        get freq(): number;
        set freq(val: number);

        /**
         * The oscillator mode to be used.
         * @since 3.0.0
         * @default 0
         */
        get oscillator_mode(): number;
        set oscillator_mode(val: number);

        /**
         * The oscillator mode to be used.
         * @since 3.0.0
         * @default 0
         */
        get oscillatorMode(): number;
        set oscillatorMode(val: number);

        /**
         * The samplerate to be used.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The the window count of total z-indexes.
         * @since 3.0.0
         * @default 0
         */
        get window_count(): number;
        set window_count(val: number);

        /**
         * The the window count of total z-indexes.
         * @since 3.0.0
         * @default 0
         */
        get windowCount(): number;
        set windowCount(val: number);

        /**
         * The z-index as position of this frequency map.
         * @since 3.0.0
         * @default 0
         */
        get z_index(): number;
        set z_index(val: number);

        /**
         * The z-index as position of this frequency map.
         * @since 3.0.0
         * @default 0
         */
        get zIndex(): number;
        set zIndex(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FrequencyMap.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: FrequencyMapFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        buffer: Ags.Complex;

        // Constructors
        constructor(properties?: Partial<FrequencyMap.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): FrequencyMap;

        // Signals
        /** @signal */
        connect<K extends keyof FrequencyMap.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FrequencyMap.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FrequencyMap.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FrequencyMap.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FrequencyMap.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FrequencyMap.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Sort frequency maps.
         * @param a an {@link AgsAudio.FrequencyMap}
         * @param b an {@link AgsAudio.FrequencyMap}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Virtual methods
        /**
         * Compute maximum likelihood `source` and store in `retval`.
         * @param source the source as {@link Ags.Complex} buffer
         * @param retval the return location of result as {@link Ags.Complex} buffer
         * @virtual
         */
        vfunc_compute_max_likelihood(source: Ags.Complex, retval: Ags.Complex): void;

        /**
         * Factorize `frequency_map`.
         * @param factorized_frequency_map the factorized {@link AgsAudio.FrequencyMap}
         * @virtual
         */
        vfunc_factorize(factorized_frequency_map: FrequencyMap): void;

        /**
         * Process `frequency_map`.
         * @virtual
         */
        vfunc_process(): void;

        // Methods
        /**
         * Compute maximum likelihood `source` and store in `retval`.
         * @param source the source as {@link Ags.Complex} buffer
         * @param retval the return location of result as {@link Ags.Complex} buffer
         */
        compute_max_likelihood(source: Ags.Complex, retval: Ags.Complex): void;

        /**
         * Factorize `frequency_map`.
         * @param factorized_frequency_map the factorized {@link AgsAudio.FrequencyMap}
         */
        factorize(factorized_frequency_map: FrequencyMap): void;

        /**
         * Process `frequency_map`.
         */
        process(): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: FrequencyMapFlags): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: FrequencyMapFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: FrequencyMapFlags): void;
    }


    namespace FrequencyMapManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FrequencyMapManager extends GObject.Object {
        static $gtype: GObject.GType<FrequencyMapManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FrequencyMapManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: FrequencyMapManagerFlags;

        frequency_map: any[];

        factorized_frequency_map: any[];

        // Constructors
        constructor(properties?: Partial<FrequencyMapManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): FrequencyMapManager;

        // Signals
        /** @signal */
        connect<K extends keyof FrequencyMapManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FrequencyMapManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FrequencyMapManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FrequencyMapManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FrequencyMapManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FrequencyMapManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get instance.
         */
        static get_instance(): FrequencyMapManager;

        // Methods
        /**
         * Add `frequency_map` to `frequency_map_manager`.
         * @param frequency_map the {@link AgsAudio.FrequencyMap}
         */
        add_factorized_frequency_map(frequency_map: FrequencyMap): void;

        /**
         * Add `frequency_map` to `frequency_map_manager`.
         * @param frequency_map the {@link AgsAudio.FrequencyMap}
         */
        add_frequency_map(frequency_map: FrequencyMap): void;

        /**
         * Find {@link AgsAudio.FrequencyMap} by `samplerate` and `buffer_size`.
         * @param samplerate the samplerate to match
         * @param buffer_size the buffer size to match
         * @returns the matching {@link AgsAudio.FrequencyMap} if found, otherwise `null`
         */
        find_factorized_frequency_map(samplerate: number, buffer_size: number): FrequencyMap;

        /**
         * Find {@link AgsAudio.FrequencyMap} by `samplerate`, `buffer_size` and `freq`.
         * @param samplerate the samplerate to match
         * @param buffer_size the buffer size to match
         * @param freq the frequency to match
         * @returns the matching {@link AgsAudio.FrequencyMap} if found, otherwise `null`
         */
        find_frequency_map(samplerate: number, buffer_size: number, freq: number): FrequencyMap;

        /**
         * Load default {@link AgsAudio.FrequencyMap}.
         */
        load_default(): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: FrequencyMapManagerFlags): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: FrequencyMapManagerFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: FrequencyMapManagerFlags): void;
    }


    namespace FxAnalyseAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxAnalyseAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxAnalyseAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxAnalyseAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxAnalyseAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxAnalyseAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxAnalyseAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxAnalyseAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxAnalyseAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxAnalyseAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxAnalyseAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxAnalyseAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxAnalyseAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxAnalyseAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxAnalyseAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxAnalyseAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxAnalyseAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxAnalyseAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxAnalyseAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxAnalyseAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxAnalyseAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxAnalyseAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxAnalyseAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxAnalyseAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxAnalyseAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxAnalyseAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxAnalyseAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxAnalyseAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxAnalyseAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxAnalyseAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxAnalyseChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::magnitude": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            frequency: Port;
            magnitude: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxAnalyseChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxAnalyseChannel>;

        // Properties
        /**
         * The beats per minute.
         * @since 3.3.0
         */
        get frequency(): Port;
        set frequency(val: Port);

        /**
         * The beats per minute.
         * @since 3.3.0
         */
        get magnitude(): Port;
        set magnitude(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxAnalyseChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        magnitude_cleared: boolean;

        // Constructors
        constructor(properties?: Partial<FxAnalyseChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxAnalyseChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxAnalyseChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxAnalyseChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxAnalyseChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxAnalyseChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get input of `input_data`.
         * @param input_data the {@link AgsAudio.FxAnalyseChannelInputData}-struct
         */
        static input_get_in(input_data: (any | null)): (any | null);

        /**
         * Get output of `input_data`.
         * @param input_data the {@link AgsAudio.FxAnalyseChannelInputData}-struct
         */
        static input_get_out(input_data: (any | null)): (any | null);

        /**
         * Get parent of `input_data`.
         * @param input_data the {@link AgsAudio.FxAnalyseChannelInputData}-struct
         */
        static input_get_parent(input_data: (any | null)): (any | null);

        // Methods
        /**
         * Get input data from `fx_analyse_channel` by `sound_scope`.
         * @param sound_scope the sound scope
         * @returns the matching {@link AgsAudio.FxAnalyseChannelInputData}-struct
         */
        get_input_data(sound_scope: number): (any | null);
    }


    namespace FxAnalyseChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxAnalyseChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxAnalyseChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxAnalyseChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxAnalyseChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxAnalyseChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxAnalyseChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxAnalyseChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxAnalyseChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxAnalyseChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxAnalyseRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxAnalyseRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxAnalyseRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxAnalyseRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxAnalyseRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxAnalyseRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxAnalyseRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxAnalyseRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxAnalyseRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxAnalyseRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxBufferAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxBufferAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxBufferAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxBufferAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxBufferAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxBufferAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxBufferAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxBufferAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxBufferAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxBufferAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxBufferAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxBufferAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxBufferAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxBufferAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxBufferAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxBufferAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxBufferAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxBufferAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxBufferAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxBufferAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxBufferAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxBufferAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxBufferAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxBufferAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxBufferAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxBufferAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxBufferAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxBufferAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxBufferAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxBufferAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxBufferChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxBufferChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxBufferChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxBufferChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        input_data: FxBufferChannelInputData[];

        // Constructors
        constructor(properties?: Partial<FxBufferChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxBufferChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxBufferChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxBufferChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxBufferChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxBufferChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get destination of `input_data`.
         * @param input_data the {@link AgsAudio.FxBufferChannelInputData}-struct
         */
        static input_get_destination(input_data: (any | null)): (any | null);

        /**
         * Get parent of `input_data`.
         * @param input_data the {@link AgsAudio.FxBufferChannelInputData}-struct
         */
        static input_get_parent(input_data: (any | null)): (any | null);

        // Methods
        /**
         * Get input data from `fx_buffer_channel` by `sound_scope`.
         * @param sound_scope the sound scope
         * @returns the matching {@link AgsAudio.FxBufferChannelInputData}-struct
         */
        get_input_data(sound_scope: number): (any | null);
    }


    namespace FxBufferChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxBufferChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxBufferChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxBufferChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxBufferChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxBufferChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxBufferChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxBufferChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxBufferChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxBufferChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxBufferRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxBufferRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxBufferRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxBufferRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxBufferRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxBufferRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxBufferRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxBufferRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxBufferRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxBufferRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxDssiAudio {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudio.SignalSignatures {
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxDssiAudio extends FxNotationAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxDssiAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxDssiAudio.SignalSignatures;

        // Fields
        fx_notation_audio: FxNotationAudio;

        flags: number;

        output_port_count: number;

        output_port: number;

        input_port_count: number;

        input_port: number;

        bank: number;

        program: number;

        scope_data: FxDssiAudioScopeData[];

        dssi_plugin: DssiPlugin;

        dssi_port: Port;

        // Constructors
        constructor(properties?: Partial<FxDssiAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxDssiAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxDssiAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxDssiAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxDssiAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxDssiAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Change program of `fx_dssi_audio`.
         * @param bank_index the bank index
         * @param program_index the program index
         */
        change_program(bank_index: number, program_index: number): void;

        /**
         * Load plugin of `fx_dssi_audio`.
         */
        load_plugin(): void;

        /**
         * Load port of `fx_dssi_audio`.
         */
        load_port(): void;

        /**
         * Set `flags` of `fx_dssi_audio`.
         * @param flags the flags
         */
        set_flags(flags: number): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.set_flags
        set_flags(...args: never[]): any;

        /**
         * Test `flags` of `fx_dssi_audio`.
         * @param flags the flags
         * @returns `true` on success, otherwise `false`
         */
        test_flags(flags: number): boolean;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.test_flags
        test_flags(...args: never[]): any;

        /**
         * Unset `flags` of `fx_dssi_audio`.
         * @param flags the flags
         */
        unset_flags(flags: number): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.unset_flags
        unset_flags(...args: never[]): any;
    }


    namespace FxDssiAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioProcessor.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioProcessor.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxDssiAudioProcessor extends FxNotationAudioProcessor implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxDssiAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxDssiAudioProcessor.SignalSignatures;

        // Fields
        fx_notation_audio_processor: FxNotationAudioProcessor;

        // Constructors
        constructor(properties?: Partial<FxDssiAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxDssiAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxDssiAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxDssiAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxDssiAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxDssiAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxDssiAudioSignal {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxDssiAudioSignal extends FxNotationAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxDssiAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxDssiAudioSignal.SignalSignatures;

        // Fields
        fx_notation_audio_signal: FxNotationAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxDssiAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxDssiAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxDssiAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxDssiAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxDssiAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxDssiAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxDssiChannel {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxDssiChannel extends FxNotationChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxDssiChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxDssiChannel.SignalSignatures;

        // Fields
        fx_notation_channel: FxNotationChannel;

        dssi_port: Port;

        // Constructors
        constructor(properties?: Partial<FxDssiChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxDssiChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxDssiChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxDssiChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxDssiChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxDssiChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Load port of `fx_dssi_channel`.
         */
        load_port(): void;
    }


    namespace FxDssiChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannelProcessor.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannelProcessor.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxDssiChannelProcessor extends FxNotationChannelProcessor implements Ags.Connectable {
        static $gtype: GObject.GType<FxDssiChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxDssiChannelProcessor.SignalSignatures;

        // Fields
        fx_notation_channel_processor: FxNotationChannelProcessor;

        // Constructors
        constructor(properties?: Partial<FxDssiChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxDssiChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxDssiChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxDssiChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxDssiChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxDssiChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxDssiRecycling {
        // Signal signatures
        interface SignalSignatures extends FxNotationRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxDssiRecycling extends FxNotationRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxDssiRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxDssiRecycling.SignalSignatures;

        // Fields
        fx_notation_recycling: FxNotationRecycling;

        // Constructors
        constructor(properties?: Partial<FxDssiRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxDssiRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxDssiRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxDssiRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxDssiRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxDssiRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEnvelopeAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEnvelopeAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxEnvelopeAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEnvelopeAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxEnvelopeAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxEnvelopeAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEnvelopeAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEnvelopeAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEnvelopeAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEnvelopeAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEnvelopeAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEnvelopeAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxEnvelopeAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEnvelopeAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxEnvelopeAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxEnvelopeAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEnvelopeAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEnvelopeAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEnvelopeAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEnvelopeAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEnvelopeAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEnvelopeAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxEnvelopeAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEnvelopeAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        envelope_util: EnvelopeUtil;

        // Constructors
        constructor(properties?: Partial<FxEnvelopeAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxEnvelopeAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEnvelopeAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEnvelopeAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEnvelopeAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEnvelopeAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEnvelopeChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::decay": (pspec: GObject.ParamSpec) => void;
            "notify::fixed-length": (pspec: GObject.ParamSpec) => void;
            "notify::ratio": (pspec: GObject.ParamSpec) => void;
            "notify::release": (pspec: GObject.ParamSpec) => void;
            "notify::sustain": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            attack: Port;
            decay: Port;
            fixed_length: Port;
            fixedLength: Port;
            ratio: Port;
            release: Port;
            sustain: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxEnvelopeChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxEnvelopeChannel>;

        // Properties
        /**
         * The attack.
         * @since 3.3.0
         */
        get attack(): Port;
        set attack(val: Port);

        /**
         * The decay.
         * @since 3.3.0
         */
        get decay(): Port;
        set decay(val: Port);

        /**
         * The fixed length.
         * @since 3.3.0
         */
        get fixed_length(): Port;
        set fixed_length(val: Port);

        /**
         * The fixed length.
         * @since 3.3.0
         */
        get fixedLength(): Port;
        set fixedLength(val: Port);

        /**
         * The ratio.
         * @since 3.3.0
         */
        get ratio(): Port;
        set ratio(val: Port);

        /**
         * The release.
         * @since 3.3.0
         */
        get release(): Port;
        set release(val: Port);

        /**
         * The sustain.
         * @since 3.3.0
         */
        get sustain(): Port;
        set sustain(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEnvelopeChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        envelope_mode: number;

        envelope_enabled: Port;

        // Constructors
        constructor(properties?: Partial<FxEnvelopeChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxEnvelopeChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEnvelopeChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEnvelopeChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEnvelopeChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEnvelopeChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEnvelopeChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEnvelopeChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxEnvelopeChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEnvelopeChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxEnvelopeChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxEnvelopeChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEnvelopeChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEnvelopeChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEnvelopeChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEnvelopeChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEnvelopeRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEnvelopeRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxEnvelopeRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEnvelopeRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxEnvelopeRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxEnvelopeRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEnvelopeRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEnvelopeRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEnvelopeRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEnvelopeRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEq10Audio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEq10Audio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxEq10Audio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEq10Audio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxEq10Audio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxEq10Audio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEq10Audio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEq10Audio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEq10Audio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEq10Audio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEq10AudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEq10AudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxEq10AudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEq10AudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxEq10AudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxEq10AudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEq10AudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEq10AudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEq10AudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEq10AudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEq10AudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEq10AudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxEq10AudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEq10AudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxEq10AudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxEq10AudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEq10AudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEq10AudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEq10AudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEq10AudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEq10Channel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::peak-112hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak112hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-14336hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak14336hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-1792hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak1792hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-224hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak224hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-28hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak28hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-3584hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak3584hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-448hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak448hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-56hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak56hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-7168hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak7168hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak-896hz": (pspec: GObject.ParamSpec) => void;
            "notify::peak896hz": (pspec: GObject.ParamSpec) => void;
            "notify::pressure": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            peak_112hz: Port;
            peak112hz: Port;
            peak_14336hz: Port;
            peak14336hz: Port;
            peak_1792hz: Port;
            peak1792hz: Port;
            peak_224hz: Port;
            peak224hz: Port;
            peak_28hz: Port;
            peak28hz: Port;
            peak_3584hz: Port;
            peak3584hz: Port;
            peak_448hz: Port;
            peak448hz: Port;
            peak_56hz: Port;
            peak56hz: Port;
            peak_7168hz: Port;
            peak7168hz: Port;
            peak_896hz: Port;
            peak896hz: Port;
            pressure: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxEq10Channel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxEq10Channel>;

        // Properties
        /**
         * The peak 112Hz port.
         * @since 3.3.0
         */
        get peak_112hz(): Port;
        set peak_112hz(val: Port);

        /**
         * The peak 112Hz port.
         * @since 3.3.0
         */
        get peak112hz(): Port;
        set peak112hz(val: Port);

        /**
         * The peak 14336Hz port.
         * @since 3.3.0
         */
        get peak_14336hz(): Port;
        set peak_14336hz(val: Port);

        /**
         * The peak 14336Hz port.
         * @since 3.3.0
         */
        get peak14336hz(): Port;
        set peak14336hz(val: Port);

        /**
         * The peak 1792Hz port.
         * @since 3.3.0
         */
        get peak_1792hz(): Port;
        set peak_1792hz(val: Port);

        /**
         * The peak 1792Hz port.
         * @since 3.3.0
         */
        get peak1792hz(): Port;
        set peak1792hz(val: Port);

        /**
         * The peak 224Hz port.
         * @since 3.3.0
         */
        get peak_224hz(): Port;
        set peak_224hz(val: Port);

        /**
         * The peak 224Hz port.
         * @since 3.3.0
         */
        get peak224hz(): Port;
        set peak224hz(val: Port);

        /**
         * The peak 28Hz port.
         * @since 3.3.0
         */
        get peak_28hz(): Port;
        set peak_28hz(val: Port);

        /**
         * The peak 28Hz port.
         * @since 3.3.0
         */
        get peak28hz(): Port;
        set peak28hz(val: Port);

        /**
         * The peak 3584Hz port.
         * @since 3.3.0
         */
        get peak_3584hz(): Port;
        set peak_3584hz(val: Port);

        /**
         * The peak 3584Hz port.
         * @since 3.3.0
         */
        get peak3584hz(): Port;
        set peak3584hz(val: Port);

        /**
         * The peak 448Hz port.
         * @since 3.3.0
         */
        get peak_448hz(): Port;
        set peak_448hz(val: Port);

        /**
         * The peak 448Hz port.
         * @since 3.3.0
         */
        get peak448hz(): Port;
        set peak448hz(val: Port);

        /**
         * The peak 56Hz port.
         * @since 3.3.0
         */
        get peak_56hz(): Port;
        set peak_56hz(val: Port);

        /**
         * The peak 56Hz port.
         * @since 3.3.0
         */
        get peak56hz(): Port;
        set peak56hz(val: Port);

        /**
         * The peak 7168Hz port.
         * @since 3.3.0
         */
        get peak_7168hz(): Port;
        set peak_7168hz(val: Port);

        /**
         * The peak 7168Hz port.
         * @since 3.3.0
         */
        get peak7168hz(): Port;
        set peak7168hz(val: Port);

        /**
         * The peak 896Hz port.
         * @since 3.3.0
         */
        get peak_896hz(): Port;
        set peak_896hz(val: Port);

        /**
         * The peak 896Hz port.
         * @since 3.3.0
         */
        get peak896hz(): Port;
        set peak896hz(val: Port);

        /**
         * The pressure port.
         * @since 3.3.0
         */
        get pressure(): Port;
        set pressure(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEq10Channel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        input_data: FxEq10ChannelInputData[];

        // Constructors
        constructor(properties?: Partial<FxEq10Channel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxEq10Channel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEq10Channel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEq10Channel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEq10Channel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEq10Channel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEq10ChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEq10ChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxEq10ChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEq10ChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxEq10ChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxEq10ChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEq10ChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEq10ChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEq10ChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEq10ChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxEq10Recycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxEq10Recycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxEq10Recycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxEq10Recycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxEq10Recycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxEq10Recycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxEq10Recycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxEq10Recycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxEq10Recycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxEq10Recycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxFMSynthAudio {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudio.SignalSignatures {
            "notify::chorus-delay": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-depth": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-input-volume": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-mix": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-output-volume": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::high-pass-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::high-pass-filter-gain": (pspec: GObject.ParamSpec) => void;
            "notify::high-pass-q-lin": (pspec: GObject.ParamSpec) => void;
            "notify::high-pass-qlin": (pspec: GObject.ParamSpec) => void;
            "notify::low-pass-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::low-pass-filter-gain": (pspec: GObject.ParamSpec) => void;
            "notify::low-pass-q-lin": (pspec: GObject.ParamSpec) => void;
            "notify::low-pass-qlin": (pspec: GObject.ParamSpec) => void;
            "notify::noise-gain": (pspec: GObject.ParamSpec) => void;
            "notify::pitch-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::sequencer-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::sequencer-sign": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-lfo-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-lfo-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-volume": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-volume": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-lfo-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-lfo-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-volume": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-volume": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-lfo-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-lfo-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth-2-volume": (pspec: GObject.ParamSpec) => void;
            "notify::synth2-volume": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudio.ConstructorProps, Ags.Connectable.ConstructorProps {
            chorus_delay: Port;
            chorusDelay: Port;
            chorus_depth: Port;
            chorusDepth: Port;
            chorus_enabled: Port;
            chorusEnabled: Port;
            chorus_input_volume: Port;
            chorusInputVolume: Port;
            chorus_lfo_frequency: Port;
            chorusLfoFrequency: Port;
            chorus_lfo_oscillator: Port;
            chorusLfoOscillator: Port;
            chorus_mix: Port;
            chorusMix: Port;
            chorus_output_volume: Port;
            chorusOutputVolume: Port;
            chorus_pitch_type: Port;
            chorusPitchType: Port;
            high_pass_enabled: Port;
            highPassEnabled: Port;
            high_pass_filter_gain: Port;
            highPassFilterGain: Port;
            high_pass_q_lin: Port;
            highPassQLin: Port;
            low_pass_enabled: Port;
            lowPassEnabled: Port;
            low_pass_filter_gain: Port;
            lowPassFilterGain: Port;
            low_pass_q_lin: Port;
            lowPassQLin: Port;
            noise_gain: Port;
            noiseGain: Port;
            pitch_tuning: Port;
            pitchTuning: Port;
            pitch_type: Port;
            pitchType: Port;
            sequencer_enabled: Port;
            sequencerEnabled: Port;
            sequencer_sign: Port;
            sequencerSign: Port;
            synth_0_key: Port;
            synth0Key: Port;
            synth_0_lfo_depth: Port;
            synth0LfoDepth: Port;
            synth_0_lfo_frequency: Port;
            synth0LfoFrequency: Port;
            synth_0_lfo_oscillator: Port;
            synth0LfoOscillator: Port;
            synth_0_lfo_tuning: Port;
            synth0LfoTuning: Port;
            synth_0_octave: Port;
            synth0Octave: Port;
            synth_0_oscillator: Port;
            synth0Oscillator: Port;
            synth_0_phase: Port;
            synth0Phase: Port;
            synth_0_volume: Port;
            synth0Volume: Port;
            synth_1_key: Port;
            synth1Key: Port;
            synth_1_lfo_depth: Port;
            synth1LfoDepth: Port;
            synth_1_lfo_frequency: Port;
            synth1LfoFrequency: Port;
            synth_1_lfo_oscillator: Port;
            synth1LfoOscillator: Port;
            synth_1_lfo_tuning: Port;
            synth1LfoTuning: Port;
            synth_1_octave: Port;
            synth1Octave: Port;
            synth_1_oscillator: Port;
            synth1Oscillator: Port;
            synth_1_phase: Port;
            synth1Phase: Port;
            synth_1_volume: Port;
            synth1Volume: Port;
            synth_2_key: Port;
            synth2Key: Port;
            synth_2_lfo_depth: Port;
            synth2LfoDepth: Port;
            synth_2_lfo_frequency: Port;
            synth2LfoFrequency: Port;
            synth_2_lfo_oscillator: Port;
            synth2LfoOscillator: Port;
            synth_2_lfo_tuning: Port;
            synth2LfoTuning: Port;
            synth_2_octave: Port;
            synth2Octave: Port;
            synth_2_oscillator: Port;
            synth2Oscillator: Port;
            synth_2_phase: Port;
            synth2Phase: Port;
            synth_2_volume: Port;
            synth2Volume: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxFMSynthAudio extends FxNotationAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxFMSynthAudio>;

        // Properties
        /**
         * The chorus delay.
         * @since 3.15.0
         */
        get chorus_delay(): Port;
        set chorus_delay(val: Port);

        /**
         * The chorus delay.
         * @since 3.15.0
         */
        get chorusDelay(): Port;
        set chorusDelay(val: Port);

        /**
         * The chorus depth.
         * @since 3.15.0
         */
        get chorus_depth(): Port;
        set chorus_depth(val: Port);

        /**
         * The chorus depth.
         * @since 3.15.0
         */
        get chorusDepth(): Port;
        set chorusDepth(val: Port);

        /**
         * The chorus enabled.
         * @since 3.15.0
         */
        get chorus_enabled(): Port;
        set chorus_enabled(val: Port);

        /**
         * The chorus enabled.
         * @since 3.15.0
         */
        get chorusEnabled(): Port;
        set chorusEnabled(val: Port);

        /**
         * The chorus input volume.
         * @since 3.15.0
         */
        get chorus_input_volume(): Port;
        set chorus_input_volume(val: Port);

        /**
         * The chorus input volume.
         * @since 3.15.0
         */
        get chorusInputVolume(): Port;
        set chorusInputVolume(val: Port);

        /**
         * The chorus lfo frequency.
         * @since 3.15.0
         */
        get chorus_lfo_frequency(): Port;
        set chorus_lfo_frequency(val: Port);

        /**
         * The chorus lfo frequency.
         * @since 3.15.0
         */
        get chorusLfoFrequency(): Port;
        set chorusLfoFrequency(val: Port);

        /**
         * The chorus lfo oscillator.
         * @since 3.15.0
         */
        get chorus_lfo_oscillator(): Port;
        set chorus_lfo_oscillator(val: Port);

        /**
         * The chorus lfo oscillator.
         * @since 3.15.0
         */
        get chorusLfoOscillator(): Port;
        set chorusLfoOscillator(val: Port);

        /**
         * The chorus mix.
         * @since 3.15.0
         */
        get chorus_mix(): Port;
        set chorus_mix(val: Port);

        /**
         * The chorus mix.
         * @since 3.15.0
         */
        get chorusMix(): Port;
        set chorusMix(val: Port);

        /**
         * The chorus output volume.
         * @since 3.15.0
         */
        get chorus_output_volume(): Port;
        set chorus_output_volume(val: Port);

        /**
         * The chorus output volume.
         * @since 3.15.0
         */
        get chorusOutputVolume(): Port;
        set chorusOutputVolume(val: Port);

        /**
         * The chorus pitch type.
         * @since 4.0.0
         */
        get chorus_pitch_type(): Port;
        set chorus_pitch_type(val: Port);

        /**
         * The chorus pitch type.
         * @since 4.0.0
         */
        get chorusPitchType(): Port;
        set chorusPitchType(val: Port);

        /**
         * The high pass enabled.
         * @since 3.15.0
         */
        get high_pass_enabled(): Port;
        set high_pass_enabled(val: Port);

        /**
         * The high pass enabled.
         * @since 3.15.0
         */
        get highPassEnabled(): Port;
        set highPassEnabled(val: Port);

        /**
         * The high pass filter gain.
         * @since 3.15.0
         */
        get high_pass_filter_gain(): Port;
        set high_pass_filter_gain(val: Port);

        /**
         * The high pass filter gain.
         * @since 3.15.0
         */
        get highPassFilterGain(): Port;
        set highPassFilterGain(val: Port);

        /**
         * The high pass q-lin.
         * @since 3.15.0
         */
        get high_pass_q_lin(): Port;
        set high_pass_q_lin(val: Port);

        /**
         * The high pass q-lin.
         * @since 3.15.0
         */
        get highPassQLin(): Port;
        set highPassQLin(val: Port);

        /**
         * The low pass enabled.
         * @since 3.15.0
         */
        get low_pass_enabled(): Port;
        set low_pass_enabled(val: Port);

        /**
         * The low pass enabled.
         * @since 3.15.0
         */
        get lowPassEnabled(): Port;
        set lowPassEnabled(val: Port);

        /**
         * The low pass filter gain.
         * @since 3.15.0
         */
        get low_pass_filter_gain(): Port;
        set low_pass_filter_gain(val: Port);

        /**
         * The low pass filter gain.
         * @since 3.15.0
         */
        get lowPassFilterGain(): Port;
        set lowPassFilterGain(val: Port);

        /**
         * The low pass q-lin.
         * @since 3.15.0
         */
        get low_pass_q_lin(): Port;
        set low_pass_q_lin(val: Port);

        /**
         * The low pass q-lin.
         * @since 3.15.0
         */
        get lowPassQLin(): Port;
        set lowPassQLin(val: Port);

        /**
         * The noise gain.
         * @since 3.15.0
         */
        get noise_gain(): Port;
        set noise_gain(val: Port);

        /**
         * The noise gain.
         * @since 3.15.0
         */
        get noiseGain(): Port;
        set noiseGain(val: Port);

        /**
         * The pitch tuning.
         * @since 3.15.0
         */
        get pitch_tuning(): Port;
        set pitch_tuning(val: Port);

        /**
         * The pitch tuning.
         * @since 3.15.0
         */
        get pitchTuning(): Port;
        set pitchTuning(val: Port);

        /**
         * The pitch type.
         * @since 4.0.0
         */
        get pitch_type(): Port;
        set pitch_type(val: Port);

        /**
         * The pitch type.
         * @since 4.0.0
         */
        get pitchType(): Port;
        set pitchType(val: Port);

        /**
         * The sequencer enabled.
         * @since 3.15.0
         */
        get sequencer_enabled(): Port;
        set sequencer_enabled(val: Port);

        /**
         * The sequencer enabled.
         * @since 3.15.0
         */
        get sequencerEnabled(): Port;
        set sequencerEnabled(val: Port);

        /**
         * The sequencer sign.
         * @since 3.15.0
         */
        get sequencer_sign(): Port;
        set sequencer_sign(val: Port);

        /**
         * The sequencer sign.
         * @since 3.15.0
         */
        get sequencerSign(): Port;
        set sequencerSign(val: Port);

        /**
         * The synth-0 key.
         * @since 3.15.0
         */
        get synth_0_key(): Port;
        set synth_0_key(val: Port);

        /**
         * The synth-0 key.
         * @since 3.15.0
         */
        get synth0Key(): Port;
        set synth0Key(val: Port);

        /**
         * The synth-0 LFO depth.
         * @since 3.15.0
         */
        get synth_0_lfo_depth(): Port;
        set synth_0_lfo_depth(val: Port);

        /**
         * The synth-0 LFO depth.
         * @since 3.15.0
         */
        get synth0LfoDepth(): Port;
        set synth0LfoDepth(val: Port);

        /**
         * The synth-0 LFO frequency.
         * @since 3.15.0
         */
        get synth_0_lfo_frequency(): Port;
        set synth_0_lfo_frequency(val: Port);

        /**
         * The synth-0 LFO frequency.
         * @since 3.15.0
         */
        get synth0LfoFrequency(): Port;
        set synth0LfoFrequency(val: Port);

        /**
         * The synth-0 lfo-oscillator.
         * @since 3.15.0
         */
        get synth_0_lfo_oscillator(): Port;
        set synth_0_lfo_oscillator(val: Port);

        /**
         * The synth-0 lfo-oscillator.
         * @since 3.15.0
         */
        get synth0LfoOscillator(): Port;
        set synth0LfoOscillator(val: Port);

        /**
         * The synth-0 LFO tuning.
         * @since 3.15.0
         */
        get synth_0_lfo_tuning(): Port;
        set synth_0_lfo_tuning(val: Port);

        /**
         * The synth-0 LFO tuning.
         * @since 3.15.0
         */
        get synth0LfoTuning(): Port;
        set synth0LfoTuning(val: Port);

        /**
         * The synth-0 octave.
         * @since 3.15.0
         */
        get synth_0_octave(): Port;
        set synth_0_octave(val: Port);

        /**
         * The synth-0 octave.
         * @since 3.15.0
         */
        get synth0Octave(): Port;
        set synth0Octave(val: Port);

        /**
         * The synth-0 oscillator.
         * @since 3.15.0
         */
        get synth_0_oscillator(): Port;
        set synth_0_oscillator(val: Port);

        /**
         * The synth-0 oscillator.
         * @since 3.15.0
         */
        get synth0Oscillator(): Port;
        set synth0Oscillator(val: Port);

        /**
         * The synth-0 phase.
         * @since 3.15.0
         */
        get synth_0_phase(): Port;
        set synth_0_phase(val: Port);

        /**
         * The synth-0 phase.
         * @since 3.15.0
         */
        get synth0Phase(): Port;
        set synth0Phase(val: Port);

        /**
         * The synth-0 volume.
         * @since 3.15.0
         */
        get synth_0_volume(): Port;
        set synth_0_volume(val: Port);

        /**
         * The synth-0 volume.
         * @since 3.15.0
         */
        get synth0Volume(): Port;
        set synth0Volume(val: Port);

        /**
         * The synth-1 key.
         * @since 3.15.0
         */
        get synth_1_key(): Port;
        set synth_1_key(val: Port);

        /**
         * The synth-1 key.
         * @since 3.15.0
         */
        get synth1Key(): Port;
        set synth1Key(val: Port);

        /**
         * The synth-1 LFO depth.
         * @since 3.15.0
         */
        get synth_1_lfo_depth(): Port;
        set synth_1_lfo_depth(val: Port);

        /**
         * The synth-1 LFO depth.
         * @since 3.15.0
         */
        get synth1LfoDepth(): Port;
        set synth1LfoDepth(val: Port);

        /**
         * The synth-1 LFO frequency.
         * @since 3.15.0
         */
        get synth_1_lfo_frequency(): Port;
        set synth_1_lfo_frequency(val: Port);

        /**
         * The synth-1 LFO frequency.
         * @since 3.15.0
         */
        get synth1LfoFrequency(): Port;
        set synth1LfoFrequency(val: Port);

        /**
         * The synth-1 lfo-oscillator.
         * @since 3.15.0
         */
        get synth_1_lfo_oscillator(): Port;
        set synth_1_lfo_oscillator(val: Port);

        /**
         * The synth-1 lfo-oscillator.
         * @since 3.15.0
         */
        get synth1LfoOscillator(): Port;
        set synth1LfoOscillator(val: Port);

        /**
         * The synth-1 LFO tuning.
         * @since 3.15.0
         */
        get synth_1_lfo_tuning(): Port;
        set synth_1_lfo_tuning(val: Port);

        /**
         * The synth-1 LFO tuning.
         * @since 3.15.0
         */
        get synth1LfoTuning(): Port;
        set synth1LfoTuning(val: Port);

        /**
         * The synth-1 octave.
         * @since 3.15.0
         */
        get synth_1_octave(): Port;
        set synth_1_octave(val: Port);

        /**
         * The synth-1 octave.
         * @since 3.15.0
         */
        get synth1Octave(): Port;
        set synth1Octave(val: Port);

        /**
         * The synth-1 oscillator.
         * @since 3.15.0
         */
        get synth_1_oscillator(): Port;
        set synth_1_oscillator(val: Port);

        /**
         * The synth-1 oscillator.
         * @since 3.15.0
         */
        get synth1Oscillator(): Port;
        set synth1Oscillator(val: Port);

        /**
         * The synth-1 phase.
         * @since 3.15.0
         */
        get synth_1_phase(): Port;
        set synth_1_phase(val: Port);

        /**
         * The synth-1 phase.
         * @since 3.15.0
         */
        get synth1Phase(): Port;
        set synth1Phase(val: Port);

        /**
         * The synth-1 volume.
         * @since 3.15.0
         */
        get synth_1_volume(): Port;
        set synth_1_volume(val: Port);

        /**
         * The synth-1 volume.
         * @since 3.15.0
         */
        get synth1Volume(): Port;
        set synth1Volume(val: Port);

        /**
         * The synth-2 key.
         * @since 3.15.0
         */
        get synth_2_key(): Port;
        set synth_2_key(val: Port);

        /**
         * The synth-2 key.
         * @since 3.15.0
         */
        get synth2Key(): Port;
        set synth2Key(val: Port);

        /**
         * The synth-2 LFO depth.
         * @since 3.15.0
         */
        get synth_2_lfo_depth(): Port;
        set synth_2_lfo_depth(val: Port);

        /**
         * The synth-2 LFO depth.
         * @since 3.15.0
         */
        get synth2LfoDepth(): Port;
        set synth2LfoDepth(val: Port);

        /**
         * The synth-2 LFO frequency.
         * @since 3.15.0
         */
        get synth_2_lfo_frequency(): Port;
        set synth_2_lfo_frequency(val: Port);

        /**
         * The synth-2 LFO frequency.
         * @since 3.15.0
         */
        get synth2LfoFrequency(): Port;
        set synth2LfoFrequency(val: Port);

        /**
         * The synth-2 lfo-oscillator.
         * @since 3.15.0
         */
        get synth_2_lfo_oscillator(): Port;
        set synth_2_lfo_oscillator(val: Port);

        /**
         * The synth-2 lfo-oscillator.
         * @since 3.15.0
         */
        get synth2LfoOscillator(): Port;
        set synth2LfoOscillator(val: Port);

        /**
         * The synth-2 LFO tuning.
         * @since 3.15.0
         */
        get synth_2_lfo_tuning(): Port;
        set synth_2_lfo_tuning(val: Port);

        /**
         * The synth-2 LFO tuning.
         * @since 3.15.0
         */
        get synth2LfoTuning(): Port;
        set synth2LfoTuning(val: Port);

        /**
         * The synth-2 octave.
         * @since 3.15.0
         */
        get synth_2_octave(): Port;
        set synth_2_octave(val: Port);

        /**
         * The synth-2 octave.
         * @since 3.15.0
         */
        get synth2Octave(): Port;
        set synth2Octave(val: Port);

        /**
         * The synth-2 oscillator.
         * @since 3.15.0
         */
        get synth_2_oscillator(): Port;
        set synth_2_oscillator(val: Port);

        /**
         * The synth-2 oscillator.
         * @since 3.15.0
         */
        get synth2Oscillator(): Port;
        set synth2Oscillator(val: Port);

        /**
         * The synth-2 phase.
         * @since 3.15.0
         */
        get synth_2_phase(): Port;
        set synth_2_phase(val: Port);

        /**
         * The synth-2 phase.
         * @since 3.15.0
         */
        get synth2Phase(): Port;
        set synth2Phase(val: Port);

        /**
         * The synth-2 volume.
         * @since 3.15.0
         */
        get synth_2_volume(): Port;
        set synth_2_volume(val: Port);

        /**
         * The synth-2 volume.
         * @since 3.15.0
         */
        get synth2Volume(): Port;
        set synth2Volume(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxFMSynthAudio.SignalSignatures;

        // Fields
        fx_notation_audio: FxNotationAudio;

        scope_data: FxFMSynthAudioScopeData[];

        // Constructors
        constructor(properties?: Partial<FxFMSynthAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxFMSynthAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxFMSynthAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxFMSynthAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxFMSynthAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxFMSynthAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxFMSynthAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioProcessor.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioProcessor.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxFMSynthAudioProcessor extends FxNotationAudioProcessor implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxFMSynthAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxFMSynthAudioProcessor.SignalSignatures;

        // Fields
        fx_notation_audio_processor: FxNotationAudioProcessor;

        // Constructors
        constructor(properties?: Partial<FxFMSynthAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxFMSynthAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxFMSynthAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxFMSynthAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxFMSynthAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxFMSynthAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxFMSynthAudioSignal {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxFMSynthAudioSignal extends FxNotationAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxFMSynthAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxFMSynthAudioSignal.SignalSignatures;

        // Fields
        fx_notation_audio_signal: FxNotationAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxFMSynthAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxFMSynthAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxFMSynthAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxFMSynthAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxFMSynthAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxFMSynthAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxFMSynthChannel {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxFMSynthChannel extends FxNotationChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxFMSynthChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxFMSynthChannel.SignalSignatures;

        // Fields
        fx_notation_channel: FxNotationChannel;

        // Constructors
        constructor(properties?: Partial<FxFMSynthChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxFMSynthChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxFMSynthChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxFMSynthChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxFMSynthChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxFMSynthChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxFMSynthChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannelProcessor.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannelProcessor.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxFMSynthChannelProcessor extends FxNotationChannelProcessor implements Ags.Connectable {
        static $gtype: GObject.GType<FxFMSynthChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxFMSynthChannelProcessor.SignalSignatures;

        // Fields
        fx_notation_channel_processor: FxNotationChannelProcessor;

        // Constructors
        constructor(properties?: Partial<FxFMSynthChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxFMSynthChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxFMSynthChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxFMSynthChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxFMSynthChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxFMSynthChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxFMSynthRecycling {
        // Signal signatures
        interface SignalSignatures extends FxNotationRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxFMSynthRecycling extends FxNotationRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxFMSynthRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxFMSynthRecycling.SignalSignatures;

        // Fields
        fx_notation_recycling: FxNotationRecycling;

        // Constructors
        constructor(properties?: Partial<FxFMSynthRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxFMSynthRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxFMSynthRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxFMSynthRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxFMSynthRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxFMSynthRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxFactory {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxFactory extends GObject.Object {
        static $gtype: GObject.GType<FxFactory>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxFactory.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<FxFactory.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        // Signals
        /** @signal */
        connect<K extends keyof FxFactory.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxFactory.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof FxFactory.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxFactory.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxFactory.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxFactory.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Instantiate {@link AgsAudio.Recall} by this factory.
         * @param audio an {@link AgsAudio.Audio} that should keep the recalls
         * @param play_container an {@link AgsAudio.RecallContainer} to indetify what recall to use
         * @param recall_container an {@link AgsAudio.RecallContainer} to indetify what recall to use
         * @param plugin_name the plugin identifier to instantiate
         * @param filename the plugin filename
         * @param effect the plugin effect
         * @param start_audio_channel the first audio channel to apply
         * @param stop_audio_channel the last audio channel to apply
         * @param start_pad the first pad to apply
         * @param stop_pad the last pad to apply
         * @param position the position to insert the recall
         * @param create_flags modify the behaviour of this function
         * @param recall_flags flags to be set for {@link AgsAudio.Recall}
         */
        static create(audio: Audio, play_container: RecallContainer, recall_container: RecallContainer, plugin_name: string, filename: string, effect: string, start_audio_channel: number, stop_audio_channel: number, start_pad: number, stop_pad: number, position: number, create_flags: FxFactoryCreateFlags, recall_flags: RecallFlags): Recall[];
    }


    namespace FxHighPassAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxHighPassAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxHighPassAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxHighPassAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxHighPassAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxHighPassAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxHighPassAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxHighPassAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxHighPassAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxHighPassAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxHighPassAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxHighPassAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxHighPassAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxHighPassAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxHighPassAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxHighPassAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxHighPassAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxHighPassAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxHighPassAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxHighPassAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxHighPassAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxHighPassAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxHighPassAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxHighPassAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxHighPassAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxHighPassAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxHighPassAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxHighPassAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxHighPassAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxHighPassAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxHighPassChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::enabled": (pspec: GObject.ParamSpec) => void;
            "notify::filter-gain": (pspec: GObject.ParamSpec) => void;
            "notify::q-lin": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            enabled: Port;
            filter_gain: Port;
            filterGain: Port;
            q_lin: Port;
            qLin: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxHighPassChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxHighPassChannel>;

        // Properties
        /**
         * The enabled port.
         * @since 3.14.0
         */
        get enabled(): Port;
        set enabled(val: Port);

        /**
         * The filter gain port.
         * @since 3.13.0
         */
        get filter_gain(): Port;
        set filter_gain(val: Port);

        /**
         * The filter gain port.
         * @since 3.13.0
         */
        get filterGain(): Port;
        set filterGain(val: Port);

        /**
         * The q-lin port.
         * @since 3.13.0
         */
        get q_lin(): Port;
        set q_lin(val: Port);

        /**
         * The q-lin port.
         * @since 3.13.0
         */
        get qLin(): Port;
        set qLin(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxHighPassChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        // Constructors
        constructor(properties?: Partial<FxHighPassChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxHighPassChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxHighPassChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxHighPassChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxHighPassChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxHighPassChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxHighPassChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxHighPassChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxHighPassChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxHighPassChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxHighPassChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxHighPassChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxHighPassChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxHighPassChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxHighPassChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxHighPassChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxHighPassRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxHighPassRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxHighPassRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxHighPassRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxHighPassRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxHighPassRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxHighPassRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxHighPassRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxHighPassRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxHighPassRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLadspaAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLadspaAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxLadspaAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLadspaAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxLadspaAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxLadspaAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLadspaAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLadspaAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLadspaAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLadspaAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLadspaAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLadspaAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxLadspaAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLadspaAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxLadspaAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxLadspaAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLadspaAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLadspaAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLadspaAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLadspaAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLadspaAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLadspaAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxLadspaAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLadspaAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxLadspaAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxLadspaAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLadspaAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLadspaAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLadspaAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLadspaAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLadspaChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLadspaChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxLadspaChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLadspaChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        output_port_count: number;

        output_port: number;

        input_port_count: number;

        input_port: number;

        input_data: FxLadspaChannelInputData[];

        ladspa_plugin: LadspaPlugin;

        ladspa_port: Port;

        // Constructors
        constructor(properties?: Partial<FxLadspaChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxLadspaChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLadspaChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLadspaChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLadspaChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLadspaChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Load plugin of `fx_ladspa_channel`.
         */
        load_plugin(): void;

        /**
         * Load port of `fx_ladspa_channel`.
         */
        load_port(): void;
    }


    namespace FxLadspaChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLadspaChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxLadspaChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLadspaChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxLadspaChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxLadspaChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLadspaChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLadspaChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLadspaChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLadspaChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLadspaRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLadspaRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxLadspaRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLadspaRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxLadspaRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxLadspaRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLadspaRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLadspaRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLadspaRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLadspaRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLfoAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLfoAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxLfoAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLfoAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxLfoAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxLfoAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLfoAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLfoAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLfoAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLfoAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLfoAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLfoAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxLfoAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLfoAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxLfoAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxLfoAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLfoAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLfoAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLfoAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLfoAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLfoAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLfoAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxLfoAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLfoAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        current_lfo_phase: number;

        // Constructors
        constructor(properties?: Partial<FxLfoAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxLfoAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLfoAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLfoAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLfoAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLfoAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLfoChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::enabled": (pspec: GObject.ParamSpec) => void;
            "notify::lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::lfo-freq": (pspec: GObject.ParamSpec) => void;
            "notify::lfo-phase": (pspec: GObject.ParamSpec) => void;
            "notify::lfo-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::lfo-wave": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            enabled: Port;
            lfo_depth: Port;
            lfoDepth: Port;
            lfo_freq: Port;
            lfoFreq: Port;
            lfo_phase: Port;
            lfoPhase: Port;
            lfo_tuning: Port;
            lfoTuning: Port;
            lfo_wave: Port;
            lfoWave: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxLfoChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxLfoChannel>;

        // Properties
        /**
         * The enable control of LFO.
         * @since 3.8.0
         */
        get enabled(): Port;
        set enabled(val: Port);

        /**
         * The depth of LFO.
         * @since 3.8.0
         */
        get lfo_depth(): Port;
        set lfo_depth(val: Port);

        /**
         * The depth of LFO.
         * @since 3.8.0
         */
        get lfoDepth(): Port;
        set lfoDepth(val: Port);

        /**
         * The freq of LFO.
         * @since 3.8.0
         */
        get lfo_freq(): Port;
        set lfo_freq(val: Port);

        /**
         * The freq of LFO.
         * @since 3.8.0
         */
        get lfoFreq(): Port;
        set lfoFreq(val: Port);

        /**
         * The phase of LFO.
         * @since 3.8.0
         */
        get lfo_phase(): Port;
        set lfo_phase(val: Port);

        /**
         * The phase of LFO.
         * @since 3.8.0
         */
        get lfoPhase(): Port;
        set lfoPhase(val: Port);

        /**
         * The tuning of LFO.
         * @since 3.8.0
         */
        get lfo_tuning(): Port;
        set lfo_tuning(val: Port);

        /**
         * The tuning of LFO.
         * @since 3.8.0
         */
        get lfoTuning(): Port;
        set lfoTuning(val: Port);

        /**
         * The wave of LFO.
         * @since 3.8.0
         */
        get lfo_wave(): Port;
        set lfo_wave(val: Port);

        /**
         * The wave of LFO.
         * @since 3.8.0
         */
        get lfoWave(): Port;
        set lfoWave(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLfoChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        input_data: FxLfoChannelInputData[];

        // Constructors
        constructor(properties?: Partial<FxLfoChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxLfoChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLfoChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLfoChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLfoChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLfoChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLfoChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLfoChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxLfoChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLfoChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxLfoChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxLfoChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLfoChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLfoChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLfoChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLfoChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLfoRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLfoRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxLfoRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLfoRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxLfoRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxLfoRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLfoRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLfoRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLfoRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLfoRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLowPassAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLowPassAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxLowPassAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLowPassAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxLowPassAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxLowPassAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLowPassAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLowPassAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLowPassAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLowPassAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLowPassAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLowPassAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxLowPassAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLowPassAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxLowPassAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxLowPassAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLowPassAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLowPassAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLowPassAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLowPassAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLowPassAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLowPassAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxLowPassAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLowPassAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxLowPassAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxLowPassAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLowPassAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLowPassAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLowPassAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLowPassAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLowPassChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::enabled": (pspec: GObject.ParamSpec) => void;
            "notify::filter-gain": (pspec: GObject.ParamSpec) => void;
            "notify::q-lin": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            enabled: Port;
            filter_gain: Port;
            filterGain: Port;
            q_lin: Port;
            qLin: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxLowPassChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxLowPassChannel>;

        // Properties
        /**
         * The enabled port.
         * @since 3.14.0
         */
        get enabled(): Port;
        set enabled(val: Port);

        /**
         * The filter gain port.
         * @since 3.13.0
         */
        get filter_gain(): Port;
        set filter_gain(val: Port);

        /**
         * The filter gain port.
         * @since 3.13.0
         */
        get filterGain(): Port;
        set filterGain(val: Port);

        /**
         * The q-lin port.
         * @since 3.13.0
         */
        get q_lin(): Port;
        set q_lin(val: Port);

        /**
         * The q-lin port.
         * @since 3.13.0
         */
        get qLin(): Port;
        set qLin(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLowPassChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        // Constructors
        constructor(properties?: Partial<FxLowPassChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxLowPassChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLowPassChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLowPassChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLowPassChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLowPassChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLowPassChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLowPassChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxLowPassChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLowPassChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxLowPassChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxLowPassChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLowPassChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLowPassChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLowPassChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLowPassChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLowPassRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLowPassRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxLowPassRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLowPassRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxLowPassRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxLowPassRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLowPassRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLowPassRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLowPassRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLowPassRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLv2Audio {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudio.SignalSignatures {
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLv2Audio extends FxNotationAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxLv2Audio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLv2Audio.SignalSignatures;

        // Fields
        fx_notation_audio: FxNotationAudio;

        flags: FxLv2AudioFlags;

        output_port_count: number;

        output_port: number;

        input_port_count: number;

        input_port: number;

        has_midiin_event_port: boolean;

        midiin_event_port: number;

        has_midiin_atom_port: boolean;

        midiin_atom_port: number;

        bank: number;

        program: number;

        scope_data: FxLv2AudioScopeData[];

        lv2_plugin: Lv2Plugin;

        lv2_port: Port;

        // Constructors
        constructor(properties?: Partial<FxLv2Audio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxLv2Audio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLv2Audio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLv2Audio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLv2Audio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLv2Audio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Change program of `fx_lv2_audio`.
         * @param bank_index the bank index
         * @param program_index the program index
         */
        change_program(bank_index: number, program_index: number): void;

        /**
         * Load plugin of `fx_lv2_audio`.
         */
        load_plugin(): void;

        /**
         * Load port of `fx_lv2_audio`.
         */
        load_port(): void;

        /**
         * Set `flags` of `fx_lv2_audio`.
         * @param flags the flags
         */
        set_flags(flags: FxLv2AudioFlags): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.FxNotationAudio.set_flags
        set_flags(...args: never[]): any;

        /**
         * Test `flags` of `fx_lv2_audio`.
         * @param flags the flags
         * @returns `true` on success, otherwise `false`
         */
        test_flags(flags: FxLv2AudioFlags): boolean;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.FxNotationAudio.test_flags
        test_flags(...args: never[]): any;

        /**
         * Unset `flags` of `fx_lv2_audio`.
         * @param flags the flags
         */
        unset_flags(flags: FxLv2AudioFlags): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.FxNotationAudio.unset_flags
        unset_flags(...args: never[]): any;
    }


    namespace FxLv2AudioProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioProcessor.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioProcessor.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLv2AudioProcessor extends FxNotationAudioProcessor implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxLv2AudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLv2AudioProcessor.SignalSignatures;

        // Fields
        fx_notation_audio_processor: FxNotationAudioProcessor;

        // Constructors
        constructor(properties?: Partial<FxLv2AudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxLv2AudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLv2AudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLv2AudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLv2AudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLv2AudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxLv2AudioSignal {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLv2AudioSignal extends FxNotationAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxLv2AudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLv2AudioSignal.SignalSignatures;

        // Fields
        fx_notation_audio_signal: FxNotationAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxLv2AudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxLv2AudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLv2AudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLv2AudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLv2AudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLv2AudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLv2Channel {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLv2Channel extends FxNotationChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxLv2Channel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLv2Channel.SignalSignatures;

        // Fields
        fx_notation_channel: FxNotationChannel;

        output_port_count: number;

        output_port: number;

        input_port_count: number;

        input_port: number;

        input_data: FxLv2ChannelInputData[];

        lv2_plugin: Lv2Plugin;

        lv2_port: Port;

        // Constructors
        constructor(properties?: Partial<FxLv2Channel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxLv2Channel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLv2Channel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLv2Channel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLv2Channel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLv2Channel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Load plugin of `fx_lv2_channel`.
         */
        load_plugin(): void;

        /**
         * Load port of `fx_lv2_channel`.
         */
        load_port(): void;
    }


    namespace FxLv2ChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannelProcessor.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannelProcessor.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLv2ChannelProcessor extends FxNotationChannelProcessor implements Ags.Connectable {
        static $gtype: GObject.GType<FxLv2ChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLv2ChannelProcessor.SignalSignatures;

        // Fields
        fx_notation_channel_processor: FxNotationChannelProcessor;

        // Constructors
        constructor(properties?: Partial<FxLv2ChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxLv2ChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLv2ChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLv2ChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLv2ChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLv2ChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxLv2Recycling {
        // Signal signatures
        interface SignalSignatures extends FxNotationRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxLv2Recycling extends FxNotationRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxLv2Recycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxLv2Recycling.SignalSignatures;

        // Fields
        fx_notation_recycling: FxNotationRecycling;

        // Constructors
        constructor(properties?: Partial<FxLv2Recycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxLv2Recycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxLv2Recycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxLv2Recycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxLv2Recycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxLv2Recycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxNotationAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {
            bpm: Port;
            delay: Port;
            duration: Port;
            loop: Port;
            loop_end: Port;
            loopEnd: Port;
            loop_start: Port;
            loopStart: Port;
            tact: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxNotationAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxNotationAudio>;

        // Properties
        /**
         * The beats per minute.
         * @since 3.3.0
         */
        get bpm(): Port;
        set bpm(val: Port);

        /**
         * The notation's delay.
         * @since 3.3.0
         */
        get delay(): Port;
        set delay(val: Port);

        /**
         * The notation's duration.
         * @since 3.3.0
         */
        get duration(): Port;
        set duration(val: Port);

        /**
         * Do loop.
         * @since 3.3.0
         */
        get loop(): Port;
        set loop(val: Port);

        /**
         * The notation's loop-end.
         * @since 3.3.0
         */
        get loop_end(): Port;
        set loop_end(val: Port);

        /**
         * The notation's loop-end.
         * @since 3.3.0
         */
        get loopEnd(): Port;
        set loopEnd(val: Port);

        /**
         * The notation's loop-start.
         * @since 3.3.0
         */
        get loop_start(): Port;
        set loop_start(val: Port);

        /**
         * The notation's loop-start.
         * @since 3.3.0
         */
        get loopStart(): Port;
        set loopStart(val: Port);

        /**
         * The tact segmentation.
         * @since 3.3.0
         */
        get tact(): Port;
        set tact(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxNotationAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        flags: number;

        pattern_mode: boolean;

        note_256th_mode: boolean;

        feed_note: any[];

        // Constructors
        constructor(properties?: Partial<FxNotationAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxNotationAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxNotationAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxNotationAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxNotationAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxNotationAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Add `note` to `fx_notation_audio`.
         * @param note the {@link AgsAudio.Note}
         */
        add_feed_note(note: Note): void;

        /**
         * Get feed note of `fx_notation_audio`.
         * @returns the {@link GLib.List}-struct containing feed note
         */
        get_feed_note(): Note[];

        /**
         * Get note 256th mode of `fx_notation_audio`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get pattern mode of `fx_notation_audio`.
         * @returns `true` if pattern mode, otherwise `false`
         */
        get_pattern_mode(): boolean;

        /**
         * Remove `note` from `fx_notation_audio`.
         * @param note the {@link AgsAudio.Note}
         */
        remove_feed_note(note: Note): void;

        /**
         * Set `flags` of `fx_notation_audio`.
         * @param flags the flags
         */
        set_flags(flags: number): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.set_flags
        set_flags(...args: never[]): any;

        /**
         * Set `note_256th_mode` of `fx_notation_audio`.
         * @param note_256th_mode `true` if note 256th mode, otherwise `false`
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `pattern_mode` of `fx_notation_audio`.
         * @param pattern_mode `true` if pattern mode, otherwise `false`
         */
        set_pattern_mode(pattern_mode: boolean): void;

        /**
         * Test `flags` of `fx_notation_audio`.
         * @param flags the flags
         * @returns `true` on success, otherwise `false`
         */
        test_flags(flags: number): boolean;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.test_flags
        test_flags(...args: never[]): any;

        /**
         * Unset `flags` of `fx_notation_audio`.
         * @param flags the flags
         */
        unset_flags(flags: number): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.unset_flags
        unset_flags(...args: never[]): any;
    }


    namespace FxNotationAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxNotationAudioProcessor extends RecallAudioRun implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxNotationAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxNotationAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        midi_util: MidiUtil;

        delay_completion: number;

        delay_counter: number;

        offset_counter: number;

        current_delay_counter: number;

        current_offset_counter: number;

        timestamp: Ags.Timestamp;

        recording_note: any[];

        recording_audio_signal: any[];

        feeding_note: any[];

        feeding_audio_signal: any[];

        tic_counter: number;

        current_tic_counter: number;

        note_256th_delay: number;

        note_256th_offset_counter: number;

        note_256th_offset_counter_last: number;

        note_256th_current_offset_counter: number;

        note_256th_current_offset_counter_last: number;

        note_256th: any[];

        has_16th_pulse: boolean;

        // Constructors
        constructor(properties?: Partial<FxNotationAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxNotationAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxNotationAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxNotationAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxNotationAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxNotationAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_counter_change(): void;

        /**
         * @virtual
         */
        vfunc_feed(): void;

        /**
         * @param note 
         * @param velocity 
         * @param key_mode 
         * @virtual
         */
        vfunc_key_off(note: Note, velocity: number, key_mode: number): void;

        /**
         * @param note 
         * @param velocity 
         * @param key_mode 
         * @virtual
         */
        vfunc_key_on(note: Note, velocity: number, key_mode: number): void;

        /**
         * @param note 
         * @param velocity 
         * @param key_mode 
         * @virtual
         */
        vfunc_key_pressure(note: Note, velocity: number, key_mode: number): void;

        /**
         * @virtual
         */
        vfunc_play(): void;

        /**
         * @virtual
         */
        vfunc_record(): void;

        // Methods
        counter_change(): void;

        feed(): void;

        /**
         * @param note 
         * @param velocity 
         * @param key_mode 
         */
        key_off(note: Note, velocity: number, key_mode: number): void;

        /**
         * @param note 
         * @param velocity 
         * @param key_mode 
         */
        key_on(note: Note, velocity: number, key_mode: number): void;

        /**
         * @param note 
         * @param velocity 
         * @param key_mode 
         */
        key_pressure(note: Note, velocity: number, key_mode: number): void;

        play(): void;

        record(): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxNotationAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxNotationAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxNotationAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxNotationAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxNotationAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxNotationAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxNotationAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxNotationAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxNotationAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxNotationAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param source 
         * @param note 
         * @param x0 
         * @param x1 
         * @param y 
         * @virtual
         */
        vfunc_notify_remove(source: AudioSignal, note: Note, x0: number, x1: number, y: number): void;

        /**
         * @param source 
         * @param note 
         * @param pattern_mode 
         * @param x0 
         * @param x1 
         * @param y 
         * @param delay_counter 
         * @param offset_counter 
         * @param frame_count 
         * @param delay 
         * @param buffer_size 
         * @virtual
         */
        vfunc_stream_feed(source: AudioSignal, note: Note, pattern_mode: boolean, x0: number, x1: number, y: number, delay_counter: number, offset_counter: number, frame_count: number, delay: number, buffer_size: number): void;

        // Methods
        /**
         * @param source 
         * @param note 
         * @param x0 
         * @param x1 
         * @param y 
         */
        notify_remove(source: AudioSignal, note: Note, x0: number, x1: number, y: number): void;

        /**
         * @param source 
         * @param note 
         * @param pattern_mode 
         * @param x0 
         * @param x1 
         * @param y 
         * @param delay_counter 
         * @param offset_counter 
         * @param frame_count 
         * @param delay 
         * @param buffer_size 
         */
        stream_feed(source: AudioSignal, note: Note, pattern_mode: boolean, x0: number, x1: number, y: number, delay_counter: number, offset_counter: (bigint | number), frame_count: number, delay: number, buffer_size: number): void;
    }


    namespace FxNotationChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxNotationChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxNotationChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxNotationChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        // Constructors
        constructor(properties?: Partial<FxNotationChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxNotationChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxNotationChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxNotationChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxNotationChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxNotationChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxNotationChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxNotationChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxNotationChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxNotationChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxNotationChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxNotationChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxNotationChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxNotationChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxNotationChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxNotationChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxNotationRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxNotationRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxNotationRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxNotationRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxNotationRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxNotationRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxNotationRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxNotationRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxNotationRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxNotationRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPatternAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::bank-index-0": (pspec: GObject.ParamSpec) => void;
            "notify::bank-index0": (pspec: GObject.ParamSpec) => void;
            "notify::bank-index-1": (pspec: GObject.ParamSpec) => void;
            "notify::bank-index1": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {
            bank_index_0: Port;
            bankIndex0: Port;
            bank_index_1: Port;
            bankIndex1: Port;
            bpm: Port;
            delay: Port;
            duration: Port;
            loop: Port;
            loop_end: Port;
            loopEnd: Port;
            loop_start: Port;
            loopStart: Port;
            tact: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxPatternAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxPatternAudio>;

        // Properties
        /**
         * Selected bank index 0.
         * @since 3.3.0
         */
        get bank_index_0(): Port;
        set bank_index_0(val: Port);

        /**
         * Selected bank index 0.
         * @since 3.3.0
         */
        get bankIndex0(): Port;
        set bankIndex0(val: Port);

        /**
         * Selected bank index 1.
         * @since 3.3.0
         */
        get bank_index_1(): Port;
        set bank_index_1(val: Port);

        /**
         * Selected bank index 1.
         * @since 3.3.0
         */
        get bankIndex1(): Port;
        set bankIndex1(val: Port);

        /**
         * The beats per minute.
         * @since 3.3.0
         */
        get bpm(): Port;
        set bpm(val: Port);

        /**
         * The pattern's delay.
         * @since 3.3.0
         */
        get delay(): Port;
        set delay(val: Port);

        /**
         * The pattern's duration.
         * @since 3.3.0
         */
        get duration(): Port;
        set duration(val: Port);

        /**
         * Do loop.
         * @since 3.3.0
         */
        get loop(): Port;
        set loop(val: Port);

        /**
         * The pattern's loop-end.
         * @since 3.3.0
         */
        get loop_end(): Port;
        set loop_end(val: Port);

        /**
         * The pattern's loop-end.
         * @since 3.3.0
         */
        get loopEnd(): Port;
        set loopEnd(val: Port);

        /**
         * The pattern's loop-start.
         * @since 3.3.0
         */
        get loop_start(): Port;
        set loop_start(val: Port);

        /**
         * The pattern's loop-start.
         * @since 3.3.0
         */
        get loopStart(): Port;
        set loopStart(val: Port);

        /**
         * The tact segmentation.
         * @since 3.3.0
         */
        get tact(): Port;
        set tact(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPatternAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        flags: number;

        note: Note;

        // Constructors
        constructor(properties?: Partial<FxPatternAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxPatternAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPatternAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPatternAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPatternAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPatternAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPatternAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPatternAudioProcessor extends RecallAudioRun implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxPatternAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPatternAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        delay_completion: number;

        delay_counter: number;

        offset_counter: number;

        current_delay_counter: number;

        current_offset_counter: number;

        // Constructors
        constructor(properties?: Partial<FxPatternAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxPatternAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPatternAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPatternAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPatternAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPatternAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_counter_change(): void;

        /**
         * @param note 
         * @param velocity 
         * @param key_mode 
         * @virtual
         */
        vfunc_key_on(note: Note, velocity: number, key_mode: number): void;

        /**
         * @virtual
         */
        vfunc_play(): void;

        // Methods
        counter_change(): void;

        /**
         * @param note 
         * @param velocity 
         * @param key_mode 
         */
        key_on(note: Note, velocity: number, key_mode: number): void;

        play(): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxPatternAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPatternAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxPatternAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPatternAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxPatternAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxPatternAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPatternAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPatternAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPatternAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPatternAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param source 
         * @param note 
         * @param x0 
         * @param x1 
         * @param y 
         * @virtual
         */
        vfunc_notify_remove(source: AudioSignal, note: Note, x0: number, x1: number, y: number): void;

        /**
         * @param source 
         * @param note 
         * @param x0 
         * @param x1 
         * @param y 
         * @param delay_counter 
         * @param offset_counter 
         * @param frame_count 
         * @param delay 
         * @param buffer_size 
         * @virtual
         */
        vfunc_stream_feed(source: AudioSignal, note: Note, x0: number, x1: number, y: number, delay_counter: number, offset_counter: number, frame_count: number, delay: number, buffer_size: number): void;

        // Methods
        /**
         * @param source 
         * @param note 
         * @param x0 
         * @param x1 
         * @param y 
         */
        notify_remove(source: AudioSignal, note: Note, x0: number, x1: number, y: number): void;

        /**
         * @param source 
         * @param note 
         * @param x0 
         * @param x1 
         * @param y 
         * @param delay_counter 
         * @param offset_counter 
         * @param frame_count 
         * @param delay 
         * @param buffer_size 
         */
        stream_feed(source: AudioSignal, note: Note, x0: number, x1: number, y: number, delay_counter: number, offset_counter: (bigint | number), frame_count: number, delay: number, buffer_size: number): void;
    }


    namespace FxPatternChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPatternChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxPatternChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPatternChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        // Constructors
        constructor(properties?: Partial<FxPatternChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxPatternChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPatternChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPatternChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPatternChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPatternChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPatternChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPatternChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxPatternChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPatternChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxPatternChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxPatternChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPatternChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPatternChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPatternChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPatternChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPatternRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPatternRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxPatternRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPatternRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxPatternRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxPatternRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPatternRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPatternRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPatternRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPatternRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPeakAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPeakAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxPeakAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPeakAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxPeakAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxPeakAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPeakAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPeakAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPeakAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPeakAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPeakAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPeakAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxPeakAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPeakAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxPeakAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxPeakAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPeakAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPeakAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPeakAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPeakAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPeakAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPeakAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxPeakAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPeakAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxPeakAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxPeakAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPeakAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPeakAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPeakAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPeakAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPeakChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::peak": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            peak: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxPeakChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxPeakChannel>;

        // Properties
        /**
         * The beats per minute.
         * @since 3.3.0
         */
        get peak(): Port;
        set peak(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPeakChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        peak_reseted: boolean;

        input_data: FxPeakChannelInputData[];

        // Constructors
        constructor(properties?: Partial<FxPeakChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxPeakChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPeakChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPeakChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPeakChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPeakChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPeakChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPeakChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxPeakChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPeakChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxPeakChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxPeakChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPeakChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPeakChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPeakChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPeakChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPeakRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPeakRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxPeakRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPeakRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxPeakRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxPeakRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPeakRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPeakRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPeakRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPeakRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPlaybackAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::capture-mode": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::do-export": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::file-audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::file-buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::file-format": (pspec: GObject.ParamSpec) => void;
            "notify::file-samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {
            bpm: Port;
            capture_mode: Port;
            captureMode: Port;
            delay: Port;
            do_export: Port;
            doExport: Port;
            duration: Port;
            file_audio_channels: Port;
            fileAudioChannels: Port;
            file_buffer_size: Port;
            fileBufferSize: Port;
            file_format: Port;
            fileFormat: Port;
            file_samplerate: Port;
            fileSamplerate: Port;
            filename: (Port | any);
            loop: Port;
            loop_end: Port;
            loopEnd: Port;
            loop_start: Port;
            loopStart: Port;
            tact: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxPlaybackAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxPlaybackAudio>;

        // Properties
        /**
         * The beats per minute.
         * @since 3.3.0
         */
        get bpm(): Port;
        set bpm(val: Port);

        /**
         * The capture mode.
         * @since 3.3.0
         */
        get capture_mode(): Port;
        set capture_mode(val: Port);

        /**
         * The capture mode.
         * @since 3.3.0
         */
        get captureMode(): Port;
        set captureMode(val: Port);

        /**
         * The playback's delay.
         * @since 3.3.0
         */
        get delay(): Port;
        set delay(val: Port);

        /**
         * The do-export switch.
         * @since 3.3.0
         */
        get do_export(): Port;
        set do_export(val: Port);

        /**
         * The do-export switch.
         * @since 3.3.0
         */
        get doExport(): Port;
        set doExport(val: Port);

        /**
         * The playback's duration.
         * @since 3.3.0
         */
        get duration(): Port;
        set duration(val: Port);

        /**
         * The file's audio channel count.
         * @since 3.3.0
         */
        get file_audio_channels(): Port;
        set file_audio_channels(val: Port);

        /**
         * The file's audio channel count.
         * @since 3.3.0
         */
        get fileAudioChannels(): Port;
        set fileAudioChannels(val: Port);

        /**
         * The file's buffer size.
         * @since 3.3.0
         */
        get file_buffer_size(): Port;
        set file_buffer_size(val: Port);

        /**
         * The file's buffer size.
         * @since 3.3.0
         */
        get fileBufferSize(): Port;
        set fileBufferSize(val: Port);

        /**
         * The file's format.
         * @since 3.3.0
         */
        get file_format(): Port;
        set file_format(val: Port);

        /**
         * The file's format.
         * @since 3.3.0
         */
        get fileFormat(): Port;
        set fileFormat(val: Port);

        /**
         * The file's samplerate.
         * @since 3.3.0
         */
        get file_samplerate(): Port;
        set file_samplerate(val: Port);

        /**
         * The file's samplerate.
         * @since 3.3.0
         */
        get fileSamplerate(): Port;
        set fileSamplerate(val: Port);

        /**
         * The filename to export.
         * @since 3.3.0
         */
    // This accessor conflicts with another accessor's type in a parent class or interface.
        get filename(): (Port | any);
    // This accessor conflicts with another accessor's type in a parent class or interface.
        set filename(val: (Port | any));

        /**
         * Do loop.
         * @since 3.3.0
         */
        get loop(): Port;
        set loop(val: Port);

        /**
         * The playback's loop-end.
         * @since 3.3.0
         */
        get loop_end(): Port;
        set loop_end(val: Port);

        /**
         * The playback's loop-end.
         * @since 3.3.0
         */
        get loopEnd(): Port;
        set loopEnd(val: Port);

        /**
         * The playback's loop-start.
         * @since 3.3.0
         */
        get loop_start(): Port;
        set loop_start(val: Port);

        /**
         * The playback's loop-start.
         * @since 3.3.0
         */
        get loopStart(): Port;
        set loopStart(val: Port);

        /**
         * The tact segmentation.
         * @since 3.3.0
         */
        get tact(): Port;
        set tact(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPlaybackAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        flags: number;

        feed_audio_signal: any[];

        master_audio_signal: any[];

        audio_file: AudioFile;

        // Constructors
        constructor(properties?: Partial<FxPlaybackAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxPlaybackAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPlaybackAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPlaybackAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPlaybackAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPlaybackAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Add `audio_signal` to `fx_playback_audio`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        add_feed_audio_signal(audio_signal: AudioSignal): void;

        /**
         * Add `audio_signal` to `fx_playback_audio`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        add_master_audio_signal(audio_signal: AudioSignal): void;

        /**
         * Get feed audio_signal of `fx_playback_audio`.
         * @returns the {@link GLib.List}-struct containing feed audio_signal
         */
        get_feed_audio_signal(): AudioSignal[];

        /**
         * Get master audio_signal of `fx_playback_audio`.
         * @returns the {@link GLib.List}-struct containing master audio_signal
         */
        get_master_audio_signal(): AudioSignal[];

        /**
         * Open audio file of `fx_playback_audio`.
         */
        open_audio_file(): void;

        /**
         * Remove `audio_signal` from `fx_playback_audio`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        remove_feed_audio_signal(audio_signal: AudioSignal): void;

        /**
         * Remove `audio_signal` from `fx_playback_audio`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        remove_master_audio_signal(audio_signal: AudioSignal): void;

        /**
         * Set `flags` of `fx_playback_audio`.
         * @param flags the flags
         */
        set_flags(flags: number): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.set_flags
        set_flags(...args: never[]): any;

        /**
         * Test `flags` of `fx_playback_audio`.
         * @param flags the flags
         * @returns `true` on success, otherwise `false`
         */
        test_flags(flags: number): boolean;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.test_flags
        test_flags(...args: never[]): any;

        /**
         * Unset `flags` of `fx_playback_audio`.
         * @param flags the flags
         */
        unset_flags(flags: number): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.Recall.unset_flags
        unset_flags(...args: never[]): any;
    }


    namespace FxPlaybackAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPlaybackAudioProcessor extends RecallAudioRun implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxPlaybackAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPlaybackAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        delay_completion: number;

        delay_counter: number;

        offset_counter: number;

        current_delay_counter: number;

        current_offset_counter: number;

        x_offset: number;

        current_x_offset: number;

        timestamp: Ags.Timestamp;

        playing_buffer: any[];

        playing_audio_signal: any[];

        recording_buffer: any[];

        recording_audio_signal: any[];

        feeding_audio_signal: any[];

        mastering_audio_signal: any[];

        capture_audio_signal: any[];

        // Constructors
        constructor(properties?: Partial<FxPlaybackAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxPlaybackAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPlaybackAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPlaybackAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPlaybackAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPlaybackAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_counter_change(): void;

        /**
         * @param buffer 
         * @param data_mode 
         * @virtual
         */
        vfunc_data_get(buffer: Buffer, data_mode: number): void;

        /**
         * @param buffer 
         * @param data_mode 
         * @virtual
         */
        vfunc_data_put(buffer: Buffer, data_mode: number): void;

        /**
         * @virtual
         */
        vfunc_feed(): void;

        /**
         * @virtual
         */
        vfunc_master(): void;

        /**
         * @virtual
         */
        vfunc_play(): void;

        /**
         * @virtual
         */
        vfunc_record(): void;

        // Methods
        counter_change(): void;

        /**
         * @param buffer 
         * @param data_mode 
         */
        data_get(buffer: Buffer, data_mode: number): void;

        /**
         * @param buffer 
         * @param data_mode 
         */
        data_put(buffer: Buffer, data_mode: number): void;

        feed(): void;

        master(): void;

        play(): void;

        record(): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxPlaybackAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPlaybackAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxPlaybackAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPlaybackAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        sub_block_processed: boolean;

        // Constructors
        constructor(properties?: Partial<FxPlaybackAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxPlaybackAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPlaybackAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPlaybackAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPlaybackAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPlaybackAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPlaybackChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPlaybackChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxPlaybackChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPlaybackChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        // Constructors
        constructor(properties?: Partial<FxPlaybackChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxPlaybackChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPlaybackChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPlaybackChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPlaybackChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPlaybackChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPlaybackChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPlaybackChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxPlaybackChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPlaybackChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxPlaybackChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxPlaybackChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPlaybackChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPlaybackChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPlaybackChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPlaybackChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxPlaybackRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxPlaybackRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxPlaybackRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxPlaybackRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxPlaybackRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxPlaybackRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxPlaybackRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxPlaybackRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxPlaybackRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxPlaybackRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSF2SynthAudio {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudio.SignalSignatures {
            "notify::chorus-delay": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-depth": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-input-volume": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-mix": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-output-volume": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::synth-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth-pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::synth-volume": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-gain": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-lfo-freq": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudio.ConstructorProps, Ags.Connectable.ConstructorProps {
            chorus_delay: Port;
            chorusDelay: Port;
            chorus_depth: Port;
            chorusDepth: Port;
            chorus_enabled: Port;
            chorusEnabled: Port;
            chorus_input_volume: Port;
            chorusInputVolume: Port;
            chorus_lfo_frequency: Port;
            chorusLfoFrequency: Port;
            chorus_lfo_oscillator: Port;
            chorusLfoOscillator: Port;
            chorus_mix: Port;
            chorusMix: Port;
            chorus_output_volume: Port;
            chorusOutputVolume: Port;
            chorus_pitch_type: Port;
            chorusPitchType: Port;
            synth_key: Port;
            synthKey: Port;
            synth_octave: Port;
            synthOctave: Port;
            synth_pitch_type: Port;
            synthPitchType: Port;
            synth_volume: Port;
            synthVolume: Port;
            vibrato_enabled: Port;
            vibratoEnabled: Port;
            vibrato_gain: Port;
            vibratoGain: Port;
            vibrato_lfo_depth: Port;
            vibratoLfoDepth: Port;
            vibrato_lfo_freq: Port;
            vibratoLfoFreq: Port;
            vibrato_tuning: Port;
            vibratoTuning: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxSF2SynthAudio extends FxNotationAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxSF2SynthAudio>;

        // Properties
        /**
         * The chorus delay.
         * @since 3.16.0
         */
        get chorus_delay(): Port;
        set chorus_delay(val: Port);

        /**
         * The chorus delay.
         * @since 3.16.0
         */
        get chorusDelay(): Port;
        set chorusDelay(val: Port);

        /**
         * The chorus depth.
         * @since 3.16.0
         */
        get chorus_depth(): Port;
        set chorus_depth(val: Port);

        /**
         * The chorus depth.
         * @since 3.16.0
         */
        get chorusDepth(): Port;
        set chorusDepth(val: Port);

        /**
         * The chorus enabled.
         * @since 3.16.0
         */
        get chorus_enabled(): Port;
        set chorus_enabled(val: Port);

        /**
         * The chorus enabled.
         * @since 3.16.0
         */
        get chorusEnabled(): Port;
        set chorusEnabled(val: Port);

        /**
         * The chorus input volume.
         * @since 3.16.0
         */
        get chorus_input_volume(): Port;
        set chorus_input_volume(val: Port);

        /**
         * The chorus input volume.
         * @since 3.16.0
         */
        get chorusInputVolume(): Port;
        set chorusInputVolume(val: Port);

        /**
         * The chorus lfo frequency.
         * @since 3.16.0
         */
        get chorus_lfo_frequency(): Port;
        set chorus_lfo_frequency(val: Port);

        /**
         * The chorus lfo frequency.
         * @since 3.16.0
         */
        get chorusLfoFrequency(): Port;
        set chorusLfoFrequency(val: Port);

        /**
         * The chorus lfo oscillator.
         * @since 3.16.0
         */
        get chorus_lfo_oscillator(): Port;
        set chorus_lfo_oscillator(val: Port);

        /**
         * The chorus lfo oscillator.
         * @since 3.16.0
         */
        get chorusLfoOscillator(): Port;
        set chorusLfoOscillator(val: Port);

        /**
         * The chorus mix.
         * @since 3.16.0
         */
        get chorus_mix(): Port;
        set chorus_mix(val: Port);

        /**
         * The chorus mix.
         * @since 3.16.0
         */
        get chorusMix(): Port;
        set chorusMix(val: Port);

        /**
         * The chorus output volume.
         * @since 3.16.0
         */
        get chorus_output_volume(): Port;
        set chorus_output_volume(val: Port);

        /**
         * The chorus output volume.
         * @since 3.16.0
         */
        get chorusOutputVolume(): Port;
        set chorusOutputVolume(val: Port);

        /**
         * The chorus's pitch type.
         * @since 4.0.0
         */
        get chorus_pitch_type(): Port;
        set chorus_pitch_type(val: Port);

        /**
         * The chorus's pitch type.
         * @since 4.0.0
         */
        get chorusPitchType(): Port;
        set chorusPitchType(val: Port);

        /**
         * The synth key.
         * @since 3.16.0
         */
        get synth_key(): Port;
        set synth_key(val: Port);

        /**
         * The synth key.
         * @since 3.16.0
         */
        get synthKey(): Port;
        set synthKey(val: Port);

        /**
         * The synth octave.
         * @since 3.16.0
         */
        get synth_octave(): Port;
        set synth_octave(val: Port);

        /**
         * The synth octave.
         * @since 3.16.0
         */
        get synthOctave(): Port;
        set synthOctave(val: Port);

        /**
         * The synth's pitch type.
         * @since 4.0.0
         */
        get synth_pitch_type(): Port;
        set synth_pitch_type(val: Port);

        /**
         * The synth's pitch type.
         * @since 4.0.0
         */
        get synthPitchType(): Port;
        set synthPitchType(val: Port);

        /**
         * The synth volume.
         * @since 3.16.0
         */
        get synth_volume(): Port;
        set synth_volume(val: Port);

        /**
         * The synth volume.
         * @since 3.16.0
         */
        get synthVolume(): Port;
        set synthVolume(val: Port);

        /**
         * The vibrato enabled.
         * @since 5.2.0
         */
        get vibrato_enabled(): Port;
        set vibrato_enabled(val: Port);

        /**
         * The vibrato enabled.
         * @since 5.2.0
         */
        get vibratoEnabled(): Port;
        set vibratoEnabled(val: Port);

        /**
         * The vibrato gain.
         * @since 5.2.0
         */
        get vibrato_gain(): Port;
        set vibrato_gain(val: Port);

        /**
         * The vibrato gain.
         * @since 5.2.0
         */
        get vibratoGain(): Port;
        set vibratoGain(val: Port);

        /**
         * The vibrato LFO depth.
         * @since 5.2.0
         */
        get vibrato_lfo_depth(): Port;
        set vibrato_lfo_depth(val: Port);

        /**
         * The vibrato LFO depth.
         * @since 5.2.0
         */
        get vibratoLfoDepth(): Port;
        set vibratoLfoDepth(val: Port);

        /**
         * The vibrato LFO freq.
         * @since 5.2.0
         */
        get vibrato_lfo_freq(): Port;
        set vibrato_lfo_freq(val: Port);

        /**
         * The vibrato LFO freq.
         * @since 5.2.0
         */
        get vibratoLfoFreq(): Port;
        set vibratoLfoFreq(val: Port);

        /**
         * The vibrato tuning.
         * @since 5.2.0
         */
        get vibrato_tuning(): Port;
        set vibrato_tuning(val: Port);

        /**
         * The vibrato tuning.
         * @since 5.2.0
         */
        get vibratoTuning(): Port;
        set vibratoTuning(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSF2SynthAudio.SignalSignatures;

        // Fields
        fx_notation_audio: FxNotationAudio;

        scope_data: FxSF2SynthAudioScopeData[];

        // Constructors
        constructor(properties?: Partial<FxSF2SynthAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxSF2SynthAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSF2SynthAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSF2SynthAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSF2SynthAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSF2SynthAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSF2SynthAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioProcessor.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioProcessor.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSF2SynthAudioProcessor extends FxNotationAudioProcessor implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxSF2SynthAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSF2SynthAudioProcessor.SignalSignatures;

        // Fields
        fx_notation_audio_processor: FxNotationAudioProcessor;

        // Constructors
        constructor(properties?: Partial<FxSF2SynthAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxSF2SynthAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSF2SynthAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSF2SynthAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSF2SynthAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSF2SynthAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxSF2SynthAudioSignal {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSF2SynthAudioSignal extends FxNotationAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxSF2SynthAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSF2SynthAudioSignal.SignalSignatures;

        // Fields
        fx_notation_audio_signal: FxNotationAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxSF2SynthAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxSF2SynthAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSF2SynthAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSF2SynthAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSF2SynthAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSF2SynthAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSF2SynthChannel {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSF2SynthChannel extends FxNotationChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxSF2SynthChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSF2SynthChannel.SignalSignatures;

        // Fields
        fx_notation_channel: FxNotationChannel;

        // Constructors
        constructor(properties?: Partial<FxSF2SynthChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxSF2SynthChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSF2SynthChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSF2SynthChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSF2SynthChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSF2SynthChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSF2SynthChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannelProcessor.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannelProcessor.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSF2SynthChannelProcessor extends FxNotationChannelProcessor implements Ags.Connectable {
        static $gtype: GObject.GType<FxSF2SynthChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSF2SynthChannelProcessor.SignalSignatures;

        // Fields
        fx_notation_channel_processor: FxNotationChannelProcessor;

        // Constructors
        constructor(properties?: Partial<FxSF2SynthChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxSF2SynthChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSF2SynthChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSF2SynthChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSF2SynthChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSF2SynthChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSF2SynthRecycling {
        // Signal signatures
        interface SignalSignatures extends FxNotationRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSF2SynthRecycling extends FxNotationRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxSF2SynthRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSF2SynthRecycling.SignalSignatures;

        // Fields
        fx_notation_recycling: FxNotationRecycling;

        // Constructors
        constructor(properties?: Partial<FxSF2SynthRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxSF2SynthRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSF2SynthRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSF2SynthRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSF2SynthRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSF2SynthRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSFZSynthAudio {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudio.SignalSignatures {
            "notify::chorus-delay": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-depth": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-input-volume": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-mix": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-output-volume": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::synth-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth-pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::synth-volume": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-gain": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-lfo-freq": (pspec: GObject.ParamSpec) => void;
            "notify::vibrato-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudio.ConstructorProps, Ags.Connectable.ConstructorProps {
            chorus_delay: Port;
            chorusDelay: Port;
            chorus_depth: Port;
            chorusDepth: Port;
            chorus_enabled: Port;
            chorusEnabled: Port;
            chorus_input_volume: Port;
            chorusInputVolume: Port;
            chorus_lfo_frequency: Port;
            chorusLfoFrequency: Port;
            chorus_lfo_oscillator: Port;
            chorusLfoOscillator: Port;
            chorus_mix: Port;
            chorusMix: Port;
            chorus_output_volume: Port;
            chorusOutputVolume: Port;
            chorus_pitch_type: Port;
            chorusPitchType: Port;
            synth_key: Port;
            synthKey: Port;
            synth_octave: Port;
            synthOctave: Port;
            synth_pitch_type: Port;
            synthPitchType: Port;
            synth_volume: Port;
            synthVolume: Port;
            vibrato_enabled: Port;
            vibratoEnabled: Port;
            vibrato_gain: Port;
            vibratoGain: Port;
            vibrato_lfo_depth: Port;
            vibratoLfoDepth: Port;
            vibrato_lfo_freq: Port;
            vibratoLfoFreq: Port;
            vibrato_tuning: Port;
            vibratoTuning: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxSFZSynthAudio extends FxNotationAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxSFZSynthAudio>;

        // Properties
        /**
         * The chorus delay.
         * @since 3.17.0
         */
        get chorus_delay(): Port;
        set chorus_delay(val: Port);

        /**
         * The chorus delay.
         * @since 3.17.0
         */
        get chorusDelay(): Port;
        set chorusDelay(val: Port);

        /**
         * The chorus depth.
         * @since 3.17.0
         */
        get chorus_depth(): Port;
        set chorus_depth(val: Port);

        /**
         * The chorus depth.
         * @since 3.17.0
         */
        get chorusDepth(): Port;
        set chorusDepth(val: Port);

        /**
         * The chorus enabled.
         * @since 3.17.0
         */
        get chorus_enabled(): Port;
        set chorus_enabled(val: Port);

        /**
         * The chorus enabled.
         * @since 3.17.0
         */
        get chorusEnabled(): Port;
        set chorusEnabled(val: Port);

        /**
         * The chorus input volume.
         * @since 3.17.0
         */
        get chorus_input_volume(): Port;
        set chorus_input_volume(val: Port);

        /**
         * The chorus input volume.
         * @since 3.17.0
         */
        get chorusInputVolume(): Port;
        set chorusInputVolume(val: Port);

        /**
         * The chorus lfo frequency.
         * @since 3.17.0
         */
        get chorus_lfo_frequency(): Port;
        set chorus_lfo_frequency(val: Port);

        /**
         * The chorus lfo frequency.
         * @since 3.17.0
         */
        get chorusLfoFrequency(): Port;
        set chorusLfoFrequency(val: Port);

        /**
         * The chorus lfo oscillator.
         * @since 3.17.0
         */
        get chorus_lfo_oscillator(): Port;
        set chorus_lfo_oscillator(val: Port);

        /**
         * The chorus lfo oscillator.
         * @since 3.17.0
         */
        get chorusLfoOscillator(): Port;
        set chorusLfoOscillator(val: Port);

        /**
         * The chorus mix.
         * @since 3.17.0
         */
        get chorus_mix(): Port;
        set chorus_mix(val: Port);

        /**
         * The chorus mix.
         * @since 3.17.0
         */
        get chorusMix(): Port;
        set chorusMix(val: Port);

        /**
         * The chorus output volume.
         * @since 3.17.0
         */
        get chorus_output_volume(): Port;
        set chorus_output_volume(val: Port);

        /**
         * The chorus output volume.
         * @since 3.17.0
         */
        get chorusOutputVolume(): Port;
        set chorusOutputVolume(val: Port);

        get chorus_pitch_type(): Port;
        set chorus_pitch_type(val: Port);

        get chorusPitchType(): Port;
        set chorusPitchType(val: Port);

        /**
         * The synth key.
         * @since 3.17.0
         */
        get synth_key(): Port;
        set synth_key(val: Port);

        /**
         * The synth key.
         * @since 3.17.0
         */
        get synthKey(): Port;
        set synthKey(val: Port);

        /**
         * The synth octave.
         * @since 3.17.0
         */
        get synth_octave(): Port;
        set synth_octave(val: Port);

        /**
         * The synth octave.
         * @since 3.17.0
         */
        get synthOctave(): Port;
        set synthOctave(val: Port);

        /**
         * The synth's pitch type.
         * @since 4.0.0
         */
        get synth_pitch_type(): Port;
        set synth_pitch_type(val: Port);

        /**
         * The synth's pitch type.
         * @since 4.0.0
         */
        get synthPitchType(): Port;
        set synthPitchType(val: Port);

        /**
         * The synth volume.
         * @since 3.17.0
         */
        get synth_volume(): Port;
        set synth_volume(val: Port);

        /**
         * The synth volume.
         * @since 3.17.0
         */
        get synthVolume(): Port;
        set synthVolume(val: Port);

        /**
         * The vibrato enabled.
         * @since 5.2.0
         */
        get vibrato_enabled(): Port;
        set vibrato_enabled(val: Port);

        /**
         * The vibrato enabled.
         * @since 5.2.0
         */
        get vibratoEnabled(): Port;
        set vibratoEnabled(val: Port);

        /**
         * The vibrato gain.
         * @since 5.2.4
         */
        get vibrato_gain(): Port;
        set vibrato_gain(val: Port);

        /**
         * The vibrato gain.
         * @since 5.2.4
         */
        get vibratoGain(): Port;
        set vibratoGain(val: Port);

        /**
         * The vibrato LFO depth.
         * @since 5.2.0
         */
        get vibrato_lfo_depth(): Port;
        set vibrato_lfo_depth(val: Port);

        /**
         * The vibrato LFO depth.
         * @since 5.2.0
         */
        get vibratoLfoDepth(): Port;
        set vibratoLfoDepth(val: Port);

        /**
         * The vibrato LFO freq.
         * @since 5.2.0
         */
        get vibrato_lfo_freq(): Port;
        set vibrato_lfo_freq(val: Port);

        /**
         * The vibrato LFO freq.
         * @since 5.2.0
         */
        get vibratoLfoFreq(): Port;
        set vibratoLfoFreq(val: Port);

        /**
         * The vibrato tuning.
         * @since 5.2.0
         */
        get vibrato_tuning(): Port;
        set vibrato_tuning(val: Port);

        /**
         * The vibrato tuning.
         * @since 5.2.0
         */
        get vibratoTuning(): Port;
        set vibratoTuning(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSFZSynthAudio.SignalSignatures;

        // Fields
        fx_notation_audio: FxNotationAudio;

        scope_data: FxSFZSynthAudioScopeData[];

        // Constructors
        constructor(properties?: Partial<FxSFZSynthAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxSFZSynthAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSFZSynthAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSFZSynthAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSFZSynthAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSFZSynthAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSFZSynthAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioProcessor.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioProcessor.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSFZSynthAudioProcessor extends FxNotationAudioProcessor implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxSFZSynthAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSFZSynthAudioProcessor.SignalSignatures;

        // Fields
        fx_notation_audio_processor: FxNotationAudioProcessor;

        // Constructors
        constructor(properties?: Partial<FxSFZSynthAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxSFZSynthAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSFZSynthAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSFZSynthAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSFZSynthAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSFZSynthAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxSFZSynthAudioSignal {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSFZSynthAudioSignal extends FxNotationAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxSFZSynthAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSFZSynthAudioSignal.SignalSignatures;

        // Fields
        fx_notation_audio_signal: FxNotationAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxSFZSynthAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxSFZSynthAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSFZSynthAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSFZSynthAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSFZSynthAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSFZSynthAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSFZSynthChannel {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSFZSynthChannel extends FxNotationChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxSFZSynthChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSFZSynthChannel.SignalSignatures;

        // Fields
        fx_notation_channel: FxNotationChannel;

        // Constructors
        constructor(properties?: Partial<FxSFZSynthChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxSFZSynthChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSFZSynthChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSFZSynthChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSFZSynthChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSFZSynthChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSFZSynthChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannelProcessor.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannelProcessor.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSFZSynthChannelProcessor extends FxNotationChannelProcessor implements Ags.Connectable {
        static $gtype: GObject.GType<FxSFZSynthChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSFZSynthChannelProcessor.SignalSignatures;

        // Fields
        fx_notation_channel_processor: FxNotationChannelProcessor;

        // Constructors
        constructor(properties?: Partial<FxSFZSynthChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxSFZSynthChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSFZSynthChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSFZSynthChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSFZSynthChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSFZSynthChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSFZSynthRecycling {
        // Signal signatures
        interface SignalSignatures extends FxNotationRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSFZSynthRecycling extends FxNotationRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxSFZSynthRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSFZSynthRecycling.SignalSignatures;

        // Fields
        fx_notation_recycling: FxNotationRecycling;

        // Constructors
        constructor(properties?: Partial<FxSFZSynthRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxSFZSynthRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSFZSynthRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSFZSynthRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSFZSynthRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSFZSynthRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSynthAudio {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudio.SignalSignatures {
            "notify::chorus-delay": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-depth": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-input-volume": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-mix": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-output-volume": (pspec: GObject.ParamSpec) => void;
            "notify::chorus-pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::high-pass-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::high-pass-filter-gain": (pspec: GObject.ParamSpec) => void;
            "notify::high-pass-q-lin": (pspec: GObject.ParamSpec) => void;
            "notify::high-pass-qlin": (pspec: GObject.ParamSpec) => void;
            "notify::low-pass-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::low-pass-filter-gain": (pspec: GObject.ParamSpec) => void;
            "notify::low-pass-q-lin": (pspec: GObject.ParamSpec) => void;
            "notify::low-pass-qlin": (pspec: GObject.ParamSpec) => void;
            "notify::noise-gain": (pspec: GObject.ParamSpec) => void;
            "notify::pitch-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::sequencer-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::sequencer-sign": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-attack-0": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-attack0": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-attack-1": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-attack1": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-attack-2": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-attack2": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-phase-0": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-phase0": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-phase-1": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-phase1": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-phase-2": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-phase2": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-sync-relative-attack-factor": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-sync-relative-attack-factor": (pspec: GObject.ParamSpec) => void;
            "notify::synth-0-volume": (pspec: GObject.ParamSpec) => void;
            "notify::synth0-volume": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-key": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-octave": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-phase": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-attack-0": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-attack0": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-attack-1": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-attack1": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-attack-2": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-attack2": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-phase-0": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-phase0": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-phase-1": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-phase1": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-phase-2": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-phase2": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-sync-relative-attack-factor": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-sync-relative-attack-factor": (pspec: GObject.ParamSpec) => void;
            "notify::synth-1-volume": (pspec: GObject.ParamSpec) => void;
            "notify::synth1-volume": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::duration": (pspec: GObject.ParamSpec) => void;
            "notify::loop": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::tact": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudio.ConstructorProps, Ags.Connectable.ConstructorProps {
            chorus_delay: Port;
            chorusDelay: Port;
            chorus_depth: Port;
            chorusDepth: Port;
            chorus_enabled: Port;
            chorusEnabled: Port;
            chorus_input_volume: Port;
            chorusInputVolume: Port;
            chorus_lfo_frequency: Port;
            chorusLfoFrequency: Port;
            chorus_lfo_oscillator: Port;
            chorusLfoOscillator: Port;
            chorus_mix: Port;
            chorusMix: Port;
            chorus_output_volume: Port;
            chorusOutputVolume: Port;
            chorus_pitch_type: Port;
            chorusPitchType: Port;
            high_pass_enabled: Port;
            highPassEnabled: Port;
            high_pass_filter_gain: Port;
            highPassFilterGain: Port;
            high_pass_q_lin: Port;
            highPassQLin: Port;
            low_pass_enabled: Port;
            lowPassEnabled: Port;
            low_pass_filter_gain: Port;
            lowPassFilterGain: Port;
            low_pass_q_lin: Port;
            lowPassQLin: Port;
            noise_gain: Port;
            noiseGain: Port;
            pitch_tuning: Port;
            pitchTuning: Port;
            pitch_type: Port;
            pitchType: Port;
            sequencer_enabled: Port;
            sequencerEnabled: Port;
            sequencer_sign: Port;
            sequencerSign: Port;
            synth_0_key: Port;
            synth0Key: Port;
            synth_0_octave: Port;
            synth0Octave: Port;
            synth_0_oscillator: Port;
            synth0Oscillator: Port;
            synth_0_phase: Port;
            synth0Phase: Port;
            synth_0_sync_attack_0: Port;
            synth0SyncAttack0: Port;
            synth_0_sync_attack_1: Port;
            synth0SyncAttack1: Port;
            synth_0_sync_attack_2: Port;
            synth0SyncAttack2: Port;
            synth_0_sync_enabled: Port;
            synth0SyncEnabled: Port;
            synth_0_sync_lfo_frequency: Port;
            synth0SyncLfoFrequency: Port;
            synth_0_sync_lfo_oscillator: Port;
            synth0SyncLfoOscillator: Port;
            synth_0_sync_phase_0: Port;
            synth0SyncPhase0: Port;
            synth_0_sync_phase_1: Port;
            synth0SyncPhase1: Port;
            synth_0_sync_phase_2: Port;
            synth0SyncPhase2: Port;
            synth_0_sync_relative_attack_factor: Port;
            synth0SyncRelativeAttackFactor: Port;
            synth_0_volume: Port;
            synth0Volume: Port;
            synth_1_key: Port;
            synth1Key: Port;
            synth_1_octave: Port;
            synth1Octave: Port;
            synth_1_oscillator: Port;
            synth1Oscillator: Port;
            synth_1_phase: Port;
            synth1Phase: Port;
            synth_1_sync_attack_0: Port;
            synth1SyncAttack0: Port;
            synth_1_sync_attack_1: Port;
            synth1SyncAttack1: Port;
            synth_1_sync_attack_2: Port;
            synth1SyncAttack2: Port;
            synth_1_sync_enabled: Port;
            synth1SyncEnabled: Port;
            synth_1_sync_lfo_frequency: Port;
            synth1SyncLfoFrequency: Port;
            synth_1_sync_lfo_oscillator: Port;
            synth1SyncLfoOscillator: Port;
            synth_1_sync_phase_0: Port;
            synth1SyncPhase0: Port;
            synth_1_sync_phase_1: Port;
            synth1SyncPhase1: Port;
            synth_1_sync_phase_2: Port;
            synth1SyncPhase2: Port;
            synth_1_sync_relative_attack_factor: Port;
            synth1SyncRelativeAttackFactor: Port;
            synth_1_volume: Port;
            synth1Volume: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxSynthAudio extends FxNotationAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxSynthAudio>;

        // Properties
        /**
         * The chorus delay.
         * @since 3.14.0
         */
        get chorus_delay(): Port;
        set chorus_delay(val: Port);

        /**
         * The chorus delay.
         * @since 3.14.0
         */
        get chorusDelay(): Port;
        set chorusDelay(val: Port);

        /**
         * The chorus depth.
         * @since 3.14.0
         */
        get chorus_depth(): Port;
        set chorus_depth(val: Port);

        /**
         * The chorus depth.
         * @since 3.14.0
         */
        get chorusDepth(): Port;
        set chorusDepth(val: Port);

        /**
         * The chorus enabled.
         * @since 3.14.0
         */
        get chorus_enabled(): Port;
        set chorus_enabled(val: Port);

        /**
         * The chorus enabled.
         * @since 3.14.0
         */
        get chorusEnabled(): Port;
        set chorusEnabled(val: Port);

        /**
         * The chorus input volume.
         * @since 3.14.0
         */
        get chorus_input_volume(): Port;
        set chorus_input_volume(val: Port);

        /**
         * The chorus input volume.
         * @since 3.14.0
         */
        get chorusInputVolume(): Port;
        set chorusInputVolume(val: Port);

        /**
         * The chorus lfo frequency.
         * @since 3.14.0
         */
        get chorus_lfo_frequency(): Port;
        set chorus_lfo_frequency(val: Port);

        /**
         * The chorus lfo frequency.
         * @since 3.14.0
         */
        get chorusLfoFrequency(): Port;
        set chorusLfoFrequency(val: Port);

        /**
         * The chorus lfo oscillator.
         * @since 3.14.0
         */
        get chorus_lfo_oscillator(): Port;
        set chorus_lfo_oscillator(val: Port);

        /**
         * The chorus lfo oscillator.
         * @since 3.14.0
         */
        get chorusLfoOscillator(): Port;
        set chorusLfoOscillator(val: Port);

        /**
         * The chorus mix.
         * @since 3.14.0
         */
        get chorus_mix(): Port;
        set chorus_mix(val: Port);

        /**
         * The chorus mix.
         * @since 3.14.0
         */
        get chorusMix(): Port;
        set chorusMix(val: Port);

        /**
         * The chorus output volume.
         * @since 3.14.0
         */
        get chorus_output_volume(): Port;
        set chorus_output_volume(val: Port);

        /**
         * The chorus output volume.
         * @since 3.14.0
         */
        get chorusOutputVolume(): Port;
        set chorusOutputVolume(val: Port);

        /**
         * The chorus pitch type.
         * @since 4.0.0
         */
        get chorus_pitch_type(): Port;
        set chorus_pitch_type(val: Port);

        /**
         * The chorus pitch type.
         * @since 4.0.0
         */
        get chorusPitchType(): Port;
        set chorusPitchType(val: Port);

        /**
         * The high pass enabled.
         * @since 3.14.0
         */
        get high_pass_enabled(): Port;
        set high_pass_enabled(val: Port);

        /**
         * The high pass enabled.
         * @since 3.14.0
         */
        get highPassEnabled(): Port;
        set highPassEnabled(val: Port);

        /**
         * The high pass filter gain.
         * @since 3.14.0
         */
        get high_pass_filter_gain(): Port;
        set high_pass_filter_gain(val: Port);

        /**
         * The high pass filter gain.
         * @since 3.14.0
         */
        get highPassFilterGain(): Port;
        set highPassFilterGain(val: Port);

        /**
         * The high pass q-lin.
         * @since 3.14.0
         */
        get high_pass_q_lin(): Port;
        set high_pass_q_lin(val: Port);

        /**
         * The high pass q-lin.
         * @since 3.14.0
         */
        get highPassQLin(): Port;
        set highPassQLin(val: Port);

        /**
         * The low pass enabled.
         * @since 3.14.0
         */
        get low_pass_enabled(): Port;
        set low_pass_enabled(val: Port);

        /**
         * The low pass enabled.
         * @since 3.14.0
         */
        get lowPassEnabled(): Port;
        set lowPassEnabled(val: Port);

        /**
         * The low pass filter gain.
         * @since 3.14.0
         */
        get low_pass_filter_gain(): Port;
        set low_pass_filter_gain(val: Port);

        /**
         * The low pass filter gain.
         * @since 3.14.0
         */
        get lowPassFilterGain(): Port;
        set lowPassFilterGain(val: Port);

        /**
         * The low pass q-lin.
         * @since 3.14.0
         */
        get low_pass_q_lin(): Port;
        set low_pass_q_lin(val: Port);

        /**
         * The low pass q-lin.
         * @since 3.14.0
         */
        get lowPassQLin(): Port;
        set lowPassQLin(val: Port);

        /**
         * The noise gain.
         * @since 3.14.0
         */
        get noise_gain(): Port;
        set noise_gain(val: Port);

        /**
         * The noise gain.
         * @since 3.14.0
         */
        get noiseGain(): Port;
        set noiseGain(val: Port);

        /**
         * The pitch tuning.
         * @since 3.14.0
         */
        get pitch_tuning(): Port;
        set pitch_tuning(val: Port);

        /**
         * The pitch tuning.
         * @since 3.14.0
         */
        get pitchTuning(): Port;
        set pitchTuning(val: Port);

        /**
         * The pitch type.
         * @since 4.0.0
         */
        get pitch_type(): Port;
        set pitch_type(val: Port);

        /**
         * The pitch type.
         * @since 4.0.0
         */
        get pitchType(): Port;
        set pitchType(val: Port);

        /**
         * The sequencer enabled.
         * @since 3.14.0
         */
        get sequencer_enabled(): Port;
        set sequencer_enabled(val: Port);

        /**
         * The sequencer enabled.
         * @since 3.14.0
         */
        get sequencerEnabled(): Port;
        set sequencerEnabled(val: Port);

        /**
         * The sequencer sign.
         * @since 3.14.0
         */
        get sequencer_sign(): Port;
        set sequencer_sign(val: Port);

        /**
         * The sequencer sign.
         * @since 3.14.0
         */
        get sequencerSign(): Port;
        set sequencerSign(val: Port);

        /**
         * The synth-0 key.
         * @since 3.14.0
         */
        get synth_0_key(): Port;
        set synth_0_key(val: Port);

        /**
         * The synth-0 key.
         * @since 3.14.0
         */
        get synth0Key(): Port;
        set synth0Key(val: Port);

        /**
         * The synth-0 octave.
         * @since 3.14.0
         */
        get synth_0_octave(): Port;
        set synth_0_octave(val: Port);

        /**
         * The synth-0 octave.
         * @since 3.14.0
         */
        get synth0Octave(): Port;
        set synth0Octave(val: Port);

        /**
         * The synth-0 oscillator.
         * @since 3.14.0
         */
        get synth_0_oscillator(): Port;
        set synth_0_oscillator(val: Port);

        /**
         * The synth-0 oscillator.
         * @since 3.14.0
         */
        get synth0Oscillator(): Port;
        set synth0Oscillator(val: Port);

        /**
         * The synth-0 phase.
         * @since 3.14.0
         */
        get synth_0_phase(): Port;
        set synth_0_phase(val: Port);

        /**
         * The synth-0 phase.
         * @since 3.14.0
         */
        get synth0Phase(): Port;
        set synth0Phase(val: Port);

        /**
         * The synth-0 sync attack-0.
         * @since 3.14.0
         */
        get synth_0_sync_attack_0(): Port;
        set synth_0_sync_attack_0(val: Port);

        /**
         * The synth-0 sync attack-0.
         * @since 3.14.0
         */
        get synth0SyncAttack0(): Port;
        set synth0SyncAttack0(val: Port);

        /**
         * The synth-0 sync attack-1.
         * @since 3.14.0
         */
        get synth_0_sync_attack_1(): Port;
        set synth_0_sync_attack_1(val: Port);

        /**
         * The synth-0 sync attack-1.
         * @since 3.14.0
         */
        get synth0SyncAttack1(): Port;
        set synth0SyncAttack1(val: Port);

        /**
         * The synth-0 sync attack-2.
         * @since 3.14.0
         */
        get synth_0_sync_attack_2(): Port;
        set synth_0_sync_attack_2(val: Port);

        /**
         * The synth-0 sync attack-2.
         * @since 3.14.0
         */
        get synth0SyncAttack2(): Port;
        set synth0SyncAttack2(val: Port);

        /**
         * The synth-0 sync enabled.
         * @since 3.14.0
         */
        get synth_0_sync_enabled(): Port;
        set synth_0_sync_enabled(val: Port);

        /**
         * The synth-0 sync enabled.
         * @since 3.14.0
         */
        get synth0SyncEnabled(): Port;
        set synth0SyncEnabled(val: Port);

        /**
         * The synth-0 sync LFO frequency.
         * @since 3.14.0
         */
        get synth_0_sync_lfo_frequency(): Port;
        set synth_0_sync_lfo_frequency(val: Port);

        /**
         * The synth-0 sync LFO frequency.
         * @since 3.14.0
         */
        get synth0SyncLfoFrequency(): Port;
        set synth0SyncLfoFrequency(val: Port);

        /**
         * The synth-0 sync LFO oscillator.
         * @since 3.14.0
         */
        get synth_0_sync_lfo_oscillator(): Port;
        set synth_0_sync_lfo_oscillator(val: Port);

        /**
         * The synth-0 sync LFO oscillator.
         * @since 3.14.0
         */
        get synth0SyncLfoOscillator(): Port;
        set synth0SyncLfoOscillator(val: Port);

        /**
         * The synth-0 sync phase-0.
         * @since 3.14.0
         */
        get synth_0_sync_phase_0(): Port;
        set synth_0_sync_phase_0(val: Port);

        /**
         * The synth-0 sync phase-0.
         * @since 3.14.0
         */
        get synth0SyncPhase0(): Port;
        set synth0SyncPhase0(val: Port);

        /**
         * The synth-0 sync phase-1.
         * @since 3.14.0
         */
        get synth_0_sync_phase_1(): Port;
        set synth_0_sync_phase_1(val: Port);

        /**
         * The synth-0 sync phase-1.
         * @since 3.14.0
         */
        get synth0SyncPhase1(): Port;
        set synth0SyncPhase1(val: Port);

        /**
         * The synth-0 sync phase-2.
         * @since 3.14.0
         */
        get synth_0_sync_phase_2(): Port;
        set synth_0_sync_phase_2(val: Port);

        /**
         * The synth-0 sync phase-2.
         * @since 3.14.0
         */
        get synth0SyncPhase2(): Port;
        set synth0SyncPhase2(val: Port);

        /**
         * The synth-0 sync relative attack factor.
         * @since 3.14.0
         */
        get synth_0_sync_relative_attack_factor(): Port;
        set synth_0_sync_relative_attack_factor(val: Port);

        /**
         * The synth-0 sync relative attack factor.
         * @since 3.14.0
         */
        get synth0SyncRelativeAttackFactor(): Port;
        set synth0SyncRelativeAttackFactor(val: Port);

        /**
         * The synth-0 volume.
         * @since 3.14.0
         */
        get synth_0_volume(): Port;
        set synth_0_volume(val: Port);

        /**
         * The synth-0 volume.
         * @since 3.14.0
         */
        get synth0Volume(): Port;
        set synth0Volume(val: Port);

        /**
         * The synth-1 key.
         * @since 3.14.0
         */
        get synth_1_key(): Port;
        set synth_1_key(val: Port);

        /**
         * The synth-1 key.
         * @since 3.14.0
         */
        get synth1Key(): Port;
        set synth1Key(val: Port);

        /**
         * The synth-1 octave.
         * @since 3.14.0
         */
        get synth_1_octave(): Port;
        set synth_1_octave(val: Port);

        /**
         * The synth-1 octave.
         * @since 3.14.0
         */
        get synth1Octave(): Port;
        set synth1Octave(val: Port);

        /**
         * The synth-1 oscillator.
         * @since 3.14.0
         */
        get synth_1_oscillator(): Port;
        set synth_1_oscillator(val: Port);

        /**
         * The synth-1 oscillator.
         * @since 3.14.0
         */
        get synth1Oscillator(): Port;
        set synth1Oscillator(val: Port);

        /**
         * The synth-1 phase.
         * @since 3.14.0
         */
        get synth_1_phase(): Port;
        set synth_1_phase(val: Port);

        /**
         * The synth-1 phase.
         * @since 3.14.0
         */
        get synth1Phase(): Port;
        set synth1Phase(val: Port);

        /**
         * The synth-1 sync attack-0.
         * @since 3.14.0
         */
        get synth_1_sync_attack_0(): Port;
        set synth_1_sync_attack_0(val: Port);

        /**
         * The synth-1 sync attack-0.
         * @since 3.14.0
         */
        get synth1SyncAttack0(): Port;
        set synth1SyncAttack0(val: Port);

        /**
         * The synth-1 sync attack-1.
         * @since 3.14.0
         */
        get synth_1_sync_attack_1(): Port;
        set synth_1_sync_attack_1(val: Port);

        /**
         * The synth-1 sync attack-1.
         * @since 3.14.0
         */
        get synth1SyncAttack1(): Port;
        set synth1SyncAttack1(val: Port);

        /**
         * The synth-1 sync attack-2.
         * @since 3.14.0
         */
        get synth_1_sync_attack_2(): Port;
        set synth_1_sync_attack_2(val: Port);

        /**
         * The synth-1 sync attack-2.
         * @since 3.14.0
         */
        get synth1SyncAttack2(): Port;
        set synth1SyncAttack2(val: Port);

        /**
         * The synth-1 sync enabled.
         * @since 3.14.0
         */
        get synth_1_sync_enabled(): Port;
        set synth_1_sync_enabled(val: Port);

        /**
         * The synth-1 sync enabled.
         * @since 3.14.0
         */
        get synth1SyncEnabled(): Port;
        set synth1SyncEnabled(val: Port);

        /**
         * The synth-1 sync LFO frequency.
         * @since 3.14.0
         */
        get synth_1_sync_lfo_frequency(): Port;
        set synth_1_sync_lfo_frequency(val: Port);

        /**
         * The synth-1 sync LFO frequency.
         * @since 3.14.0
         */
        get synth1SyncLfoFrequency(): Port;
        set synth1SyncLfoFrequency(val: Port);

        /**
         * The synth-1 sync LFO oscillator.
         * @since 3.14.0
         */
        get synth_1_sync_lfo_oscillator(): Port;
        set synth_1_sync_lfo_oscillator(val: Port);

        /**
         * The synth-1 sync LFO oscillator.
         * @since 3.14.0
         */
        get synth1SyncLfoOscillator(): Port;
        set synth1SyncLfoOscillator(val: Port);

        /**
         * The synth-1 sync phase-0.
         * @since 3.14.0
         */
        get synth_1_sync_phase_0(): Port;
        set synth_1_sync_phase_0(val: Port);

        /**
         * The synth-1 sync phase-0.
         * @since 3.14.0
         */
        get synth1SyncPhase0(): Port;
        set synth1SyncPhase0(val: Port);

        /**
         * The synth-1 sync phase-1.
         * @since 3.14.0
         */
        get synth_1_sync_phase_1(): Port;
        set synth_1_sync_phase_1(val: Port);

        /**
         * The synth-1 sync phase-1.
         * @since 3.14.0
         */
        get synth1SyncPhase1(): Port;
        set synth1SyncPhase1(val: Port);

        /**
         * The synth-1 sync phase-2.
         * @since 3.14.0
         */
        get synth_1_sync_phase_2(): Port;
        set synth_1_sync_phase_2(val: Port);

        /**
         * The synth-1 sync phase-2.
         * @since 3.14.0
         */
        get synth1SyncPhase2(): Port;
        set synth1SyncPhase2(val: Port);

        /**
         * The synth-1 sync relative attack factor.
         * @since 3.14.0
         */
        get synth_1_sync_relative_attack_factor(): Port;
        set synth_1_sync_relative_attack_factor(val: Port);

        /**
         * The synth-1 sync relative attack factor.
         * @since 3.14.0
         */
        get synth1SyncRelativeAttackFactor(): Port;
        set synth1SyncRelativeAttackFactor(val: Port);

        /**
         * The synth-1 volume.
         * @since 3.14.0
         */
        get synth_1_volume(): Port;
        set synth_1_volume(val: Port);

        /**
         * The synth-1 volume.
         * @since 3.14.0
         */
        get synth1Volume(): Port;
        set synth1Volume(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSynthAudio.SignalSignatures;

        // Fields
        fx_notation_audio: FxNotationAudio;

        scope_data: FxSynthAudioScopeData[];

        // Constructors
        constructor(properties?: Partial<FxSynthAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxSynthAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSynthAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSynthAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSynthAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSynthAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSynthAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioProcessor.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioProcessor.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Countable.ConstructorProps, Ags.Seekable.ConstructorProps, Ags.Tactable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSynthAudioProcessor extends FxNotationAudioProcessor implements Ags.Connectable, Ags.Countable, Ags.Seekable, Ags.Tactable {
        static $gtype: GObject.GType<FxSynthAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSynthAudioProcessor.SignalSignatures;

        // Fields
        fx_notation_audio_processor: FxNotationAudioProcessor;

        // Constructors
        constructor(properties?: Partial<FxSynthAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxSynthAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSynthAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSynthAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSynthAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSynthAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Retrieve current position of MIDI.
         * @returns the current position
         */
        get_midi_counter(): number;

        /**
         * Retrieve current position of notation.
         * @returns the current position
         */
        get_notation_counter(): number;

        /**
         * Retrieve current position of sequencer.
         * @returns the current position
         */
        get_sequencer_counter(): number;

        /**
         * Retrieve current position of wave.
         * @returns the current position
         */
        get_wave_counter(): number;

        /**
         * Retrieve current position of MIDI.
         * @virtual
         */
        vfunc_get_midi_counter(): (bigint | number);

        /**
         * Retrieve current position of notation.
         * @virtual
         */
        vfunc_get_notation_counter(): (bigint | number);

        /**
         * Retrieve current position of sequencer.
         * @virtual
         */
        vfunc_get_sequencer_counter(): (bigint | number);

        /**
         * Retrieve current position of wave.
         * @virtual
         */
        vfunc_get_wave_counter(): (bigint | number);

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         */
        seek(offset: (bigint | number), whence: number): void;

        /**
         * Seek.
         * @param offset the offset
         * @param whence the direction, see {@link Ags.SeekType}-enum
         * @virtual
         */
        vfunc_seek(offset: number, whence: number): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace FxSynthAudioSignal {
        // Signal signatures
        interface SignalSignatures extends FxNotationAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSynthAudioSignal extends FxNotationAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxSynthAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSynthAudioSignal.SignalSignatures;

        // Fields
        fx_notation_audio_signal: FxNotationAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxSynthAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxSynthAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSynthAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSynthAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSynthAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSynthAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSynthChannel {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSynthChannel extends FxNotationChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxSynthChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSynthChannel.SignalSignatures;

        // Fields
        fx_notation_channel: FxNotationChannel;

        // Constructors
        constructor(properties?: Partial<FxSynthChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxSynthChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSynthChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSynthChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSynthChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSynthChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSynthChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends FxNotationChannelProcessor.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationChannelProcessor.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSynthChannelProcessor extends FxNotationChannelProcessor implements Ags.Connectable {
        static $gtype: GObject.GType<FxSynthChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSynthChannelProcessor.SignalSignatures;

        // Fields
        fx_notation_channel_processor: FxNotationChannelProcessor;

        // Constructors
        constructor(properties?: Partial<FxSynthChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxSynthChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSynthChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSynthChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSynthChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSynthChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxSynthRecycling {
        // Signal signatures
        interface SignalSignatures extends FxNotationRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends FxNotationRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxSynthRecycling extends FxNotationRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxSynthRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxSynthRecycling.SignalSignatures;

        // Fields
        fx_notation_recycling: FxNotationRecycling;

        // Constructors
        constructor(properties?: Partial<FxSynthRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxSynthRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxSynthRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxSynthRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxSynthRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxSynthRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTremoloAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::tremolo-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::tremolo-gain": (pspec: GObject.ParamSpec) => void;
            "notify::tremolo-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::tremolo-lfo-freq": (pspec: GObject.ParamSpec) => void;
            "notify::tremolo-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {
            tremolo_enabled: Port;
            tremoloEnabled: Port;
            tremolo_gain: Port;
            tremoloGain: Port;
            tremolo_lfo_depth: Port;
            tremoloLfoDepth: Port;
            tremolo_lfo_freq: Port;
            tremoloLfoFreq: Port;
            tremolo_tuning: Port;
            tremoloTuning: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxTremoloAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxTremoloAudio>;

        // Properties
        /**
         * The tremolo's enabled port.
         * @since 5.2.0
         */
        get tremolo_enabled(): Port;
        set tremolo_enabled(val: Port);

        /**
         * The tremolo's enabled port.
         * @since 5.2.0
         */
        get tremoloEnabled(): Port;
        set tremoloEnabled(val: Port);

        /**
         * The tremolo's gain port.
         * @since 5.2.0
         */
        get tremolo_gain(): Port;
        set tremolo_gain(val: Port);

        /**
         * The tremolo's gain port.
         * @since 5.2.0
         */
        get tremoloGain(): Port;
        set tremoloGain(val: Port);

        /**
         * The tremolo's lfo-depth port.
         * @since 5.2.0
         */
        get tremolo_lfo_depth(): Port;
        set tremolo_lfo_depth(val: Port);

        /**
         * The tremolo's lfo-depth port.
         * @since 5.2.0
         */
        get tremoloLfoDepth(): Port;
        set tremoloLfoDepth(val: Port);

        /**
         * The tremolo's lfo-freq port.
         * @since 5.2.0
         */
        get tremolo_lfo_freq(): Port;
        set tremolo_lfo_freq(val: Port);

        /**
         * The tremolo's lfo-freq port.
         * @since 5.2.0
         */
        get tremoloLfoFreq(): Port;
        set tremoloLfoFreq(val: Port);

        /**
         * The tremolo's tuning port.
         * @since 5.2.0
         */
        get tremolo_tuning(): Port;
        set tremolo_tuning(val: Port);

        /**
         * The tremolo's tuning port.
         * @since 5.2.0
         */
        get tremoloTuning(): Port;
        set tremoloTuning(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTremoloAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxTremoloAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxTremoloAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTremoloAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTremoloAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTremoloAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTremoloAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTremoloAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTremoloAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxTremoloAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTremoloAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxTremoloAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxTremoloAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTremoloAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTremoloAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTremoloAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTremoloAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTremoloAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTremoloAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxTremoloAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTremoloAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxTremoloAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxTremoloAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTremoloAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTremoloAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTremoloAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTremoloAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTremoloChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTremoloChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxTremoloChannel>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTremoloChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        // Constructors
        constructor(properties?: Partial<FxTremoloChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxTremoloChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTremoloChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTremoloChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTremoloChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTremoloChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTremoloChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTremoloChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxTremoloChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTremoloChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxTremoloChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxTremoloChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTremoloChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTremoloChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTremoloChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTremoloChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTremoloRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTremoloRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxTremoloRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTremoloRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxTremoloRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxTremoloRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTremoloRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTremoloRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTremoloRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTremoloRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTwoPassAliaseAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTwoPassAliaseAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxTwoPassAliaseAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTwoPassAliaseAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxTwoPassAliaseAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxTwoPassAliaseAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTwoPassAliaseAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTwoPassAliaseAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTwoPassAliaseAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTwoPassAliaseAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTwoPassAliaseAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTwoPassAliaseAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxTwoPassAliaseAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTwoPassAliaseAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxTwoPassAliaseAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxTwoPassAliaseAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTwoPassAliaseAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTwoPassAliaseAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTwoPassAliaseAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTwoPassAliaseAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTwoPassAliaseAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTwoPassAliaseAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxTwoPassAliaseAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTwoPassAliaseAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxTwoPassAliaseAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxTwoPassAliaseAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTwoPassAliaseAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTwoPassAliaseAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTwoPassAliaseAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTwoPassAliaseAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTwoPassAliaseChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::a-amount": (pspec: GObject.ParamSpec) => void;
            "notify::a-phase": (pspec: GObject.ParamSpec) => void;
            "notify::b-amount": (pspec: GObject.ParamSpec) => void;
            "notify::b-phase": (pspec: GObject.ParamSpec) => void;
            "notify::enabled": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            a_amount: Port;
            aAmount: Port;
            a_phase: Port;
            aPhase: Port;
            b_amount: Port;
            bAmount: Port;
            b_phase: Port;
            bPhase: Port;
            enabled: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxTwoPassAliaseChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxTwoPassAliaseChannel>;

        // Properties
        /**
         * The first pass amount.
         * @since 3.8.0
         */
        get a_amount(): Port;
        set a_amount(val: Port);

        /**
         * The first pass amount.
         * @since 3.8.0
         */
        get aAmount(): Port;
        set aAmount(val: Port);

        /**
         * The first pass phase.
         * @since 3.8.0
         */
        get a_phase(): Port;
        set a_phase(val: Port);

        /**
         * The first pass phase.
         * @since 3.8.0
         */
        get aPhase(): Port;
        set aPhase(val: Port);

        /**
         * The second pass amount.
         * @since 3.8.0
         */
        get b_amount(): Port;
        set b_amount(val: Port);

        /**
         * The second pass amount.
         * @since 3.8.0
         */
        get bAmount(): Port;
        set bAmount(val: Port);

        /**
         * The second pass phase.
         * @since 3.8.0
         */
        get b_phase(): Port;
        set b_phase(val: Port);

        /**
         * The second pass phase.
         * @since 3.8.0
         */
        get bPhase(): Port;
        set bPhase(val: Port);

        /**
         * The enable control of two pass aliase.
         * @since 3.8.0
         */
        get enabled(): Port;
        set enabled(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTwoPassAliaseChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        input_data: FxTwoPassAliaseChannelInputData[];

        // Constructors
        constructor(properties?: Partial<FxTwoPassAliaseChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxTwoPassAliaseChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTwoPassAliaseChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTwoPassAliaseChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTwoPassAliaseChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTwoPassAliaseChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get a buffer of `input_data`.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static input_get_a_buffer(input_data: (any | null)): (any | null);

        /**
         * Get a mix of `input_data`.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static input_get_a_mix(input_data: (any | null)): (any | null);

        /**
         * Get b buffer of `input_data`.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static input_get_b_buffer(input_data: (any | null)): (any | null);

        /**
         * Get b mix of `input_data`.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static input_get_b_mix(input_data: (any | null)): (any | null);

        /**
         * Get final mix of `input_data`.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static input_get_final_mix(input_data: (any | null)): (any | null);

        /**
         * Get orig buffer of `input_data`.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static input_get_orig_buffer(input_data: (any | null)): (any | null);

        /**
         * Get parent of `input_data`.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static input_get_parent(input_data: (any | null)): (any | null);
    }


    namespace FxTwoPassAliaseChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTwoPassAliaseChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxTwoPassAliaseChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTwoPassAliaseChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxTwoPassAliaseChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxTwoPassAliaseChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTwoPassAliaseChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTwoPassAliaseChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTwoPassAliaseChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTwoPassAliaseChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxTwoPassAliaseRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxTwoPassAliaseRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxTwoPassAliaseRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxTwoPassAliaseRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxTwoPassAliaseRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxTwoPassAliaseRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxTwoPassAliaseRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxTwoPassAliaseRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxTwoPassAliaseRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxTwoPassAliaseRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxVolumeAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::muted": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Mutable.ConstructorProps {
            muted: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxVolumeAudio extends RecallAudio implements Ags.Connectable, Ags.Mutable {
        static $gtype: GObject.GType<FxVolumeAudio>;

        // Properties
        /**
         * The muted port.
         * @since 3.3.0
         */
        get muted(): Port;
        set muted(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxVolumeAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxVolumeAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxVolumeAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxVolumeAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxVolumeAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxVolumeAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxVolumeAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Mute a class instance.
         * @param muted if `true` then muted, else playing
         */
        set_muted(muted: boolean): void;

        /**
         * Mute a class instance.
         * @param muted if `true` then muted, else playing
         * @virtual
         */
        vfunc_set_muted(muted: boolean): void;
    }


    namespace FxVolumeAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxVolumeAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxVolumeAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxVolumeAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxVolumeAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxVolumeAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxVolumeAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxVolumeAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxVolumeAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxVolumeAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxVolumeAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxVolumeAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxVolumeAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxVolumeAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        // Constructors
        constructor(properties?: Partial<FxVolumeAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxVolumeAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxVolumeAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxVolumeAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxVolumeAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxVolumeAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxVolumeChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::muted": (pspec: GObject.ParamSpec) => void;
            "notify::volume": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Mutable.ConstructorProps {
            muted: Port;
            volume: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxVolumeChannel extends RecallChannel implements Ags.Connectable, Ags.Mutable {
        static $gtype: GObject.GType<FxVolumeChannel>;

        // Properties
        /**
         * The muted port.
         * @since 3.3.0
         */
        get muted(): Port;
        set muted(val: Port);

        /**
         * The volume port.
         * @since 3.3.0
         */
        get volume(): Port;
        set volume(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxVolumeChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        // Constructors
        constructor(properties?: Partial<FxVolumeChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxVolumeChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxVolumeChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxVolumeChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxVolumeChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxVolumeChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Mute a class instance.
         * @param muted if `true` then muted, else playing
         */
        set_muted(muted: boolean): void;

        /**
         * Mute a class instance.
         * @param muted if `true` then muted, else playing
         * @virtual
         */
        vfunc_set_muted(muted: boolean): void;
    }


    namespace FxVolumeChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxVolumeChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxVolumeChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxVolumeChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxVolumeChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxVolumeChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxVolumeChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxVolumeChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxVolumeChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxVolumeChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxVolumeRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxVolumeRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxVolumeRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxVolumeRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxVolumeRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxVolumeRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxVolumeRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxVolumeRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxVolumeRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxVolumeRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxWahWahAudio {
        // Signal signatures
        interface SignalSignatures extends RecallAudio.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudio.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxWahWahAudio extends RecallAudio implements Ags.Connectable {
        static $gtype: GObject.GType<FxWahWahAudio>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxWahWahAudio.SignalSignatures;

        // Fields
        recall_audio: RecallAudio;

        // Constructors
        constructor(properties?: Partial<FxWahWahAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxWahWahAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxWahWahAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxWahWahAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxWahWahAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxWahWahAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxWahWahAudioProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallAudioRun.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxWahWahAudioProcessor extends RecallAudioRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxWahWahAudioProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxWahWahAudioProcessor.SignalSignatures;

        // Fields
        recall_audio_run: RecallAudioRun;

        // Constructors
        constructor(properties?: Partial<FxWahWahAudioProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): FxWahWahAudioProcessor;

        // Conflicted with AgsAudio.RecallAudioRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxWahWahAudioProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxWahWahAudioProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxWahWahAudioProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxWahWahAudioProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxWahWahAudioSignal {
        // Signal signatures
        interface SignalSignatures extends RecallAudioSignal.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallAudioSignal.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxWahWahAudioSignal extends RecallAudioSignal implements Ags.Connectable {
        static $gtype: GObject.GType<FxWahWahAudioSignal>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxWahWahAudioSignal.SignalSignatures;

        // Fields
        recall_audio_signal: RecallAudioSignal;

        wah_wah_util: WahWahUtil;

        // Constructors
        constructor(properties?: Partial<FxWahWahAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio_signal: AudioSignal): FxWahWahAudioSignal;

        // Conflicted with AgsAudio.RecallAudioSignal.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxWahWahAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxWahWahAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxWahWahAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxWahWahAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxWahWahChannel {
        // Signal signatures
        interface SignalSignatures extends RecallChannel.SignalSignatures {
            "notify::wah-wah-attack": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-decay": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-enabled": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-fixed-length": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-length-mode": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-lfo-freq": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-ratio": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-release": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-sustain": (pspec: GObject.ParamSpec) => void;
            "notify::wah-wah-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannel.ConstructorProps, Ags.Connectable.ConstructorProps {
            wah_wah_attack: Port;
            wahWahAttack: Port;
            wah_wah_decay: Port;
            wahWahDecay: Port;
            wah_wah_enabled: Port;
            wahWahEnabled: Port;
            wah_wah_fixed_length: Port;
            wahWahFixedLength: Port;
            wah_wah_length_mode: Port;
            wahWahLengthMode: Port;
            wah_wah_lfo_depth: Port;
            wahWahLfoDepth: Port;
            wah_wah_lfo_freq: Port;
            wahWahLfoFreq: Port;
            wah_wah_ratio: Port;
            wahWahRatio: Port;
            wah_wah_release: Port;
            wahWahRelease: Port;
            wah_wah_sustain: Port;
            wahWahSustain: Port;
            wah_wah_tuning: Port;
            wahWahTuning: Port;
        }
    }

    /**
     * @gir-type Class
     */
    class FxWahWahChannel extends RecallChannel implements Ags.Connectable {
        static $gtype: GObject.GType<FxWahWahChannel>;

        // Properties
        /**
         * The attack.
         * @since 5.2.0
         */
        get wah_wah_attack(): Port;
        set wah_wah_attack(val: Port);

        /**
         * The attack.
         * @since 5.2.0
         */
        get wahWahAttack(): Port;
        set wahWahAttack(val: Port);

        /**
         * The decay.
         * @since 5.2.0
         */
        get wah_wah_decay(): Port;
        set wah_wah_decay(val: Port);

        /**
         * The decay.
         * @since 5.2.0
         */
        get wahWahDecay(): Port;
        set wahWahDecay(val: Port);

        /**
         * The enabled switch.
         * @since 5.2.0
         */
        get wah_wah_enabled(): Port;
        set wah_wah_enabled(val: Port);

        /**
         * The enabled switch.
         * @since 5.2.0
         */
        get wahWahEnabled(): Port;
        set wahWahEnabled(val: Port);

        /**
         * The fixed length.
         * @since 5.2.0
         */
        get wah_wah_fixed_length(): Port;
        set wah_wah_fixed_length(val: Port);

        /**
         * The fixed length.
         * @since 5.2.0
         */
        get wahWahFixedLength(): Port;
        set wahWahFixedLength(val: Port);

        /**
         * The length mode.
         * @since 5.2.0
         */
        get wah_wah_length_mode(): Port;
        set wah_wah_length_mode(val: Port);

        /**
         * The length mode.
         * @since 5.2.0
         */
        get wahWahLengthMode(): Port;
        set wahWahLengthMode(val: Port);

        /**
         * The lfo_depth.
         * @since 5.2.0
         */
        get wah_wah_lfo_depth(): Port;
        set wah_wah_lfo_depth(val: Port);

        /**
         * The lfo_depth.
         * @since 5.2.0
         */
        get wahWahLfoDepth(): Port;
        set wahWahLfoDepth(val: Port);

        /**
         * The lfo_freq.
         * @since 5.2.0
         */
        get wah_wah_lfo_freq(): Port;
        set wah_wah_lfo_freq(val: Port);

        /**
         * The lfo_freq.
         * @since 5.2.0
         */
        get wahWahLfoFreq(): Port;
        set wahWahLfoFreq(val: Port);

        /**
         * The ratio.
         * @since 5.2.0
         */
        get wah_wah_ratio(): Port;
        set wah_wah_ratio(val: Port);

        /**
         * The ratio.
         * @since 5.2.0
         */
        get wahWahRatio(): Port;
        set wahWahRatio(val: Port);

        /**
         * The release.
         * @since 5.2.0
         */
        get wah_wah_release(): Port;
        set wah_wah_release(val: Port);

        /**
         * The release.
         * @since 5.2.0
         */
        get wahWahRelease(): Port;
        set wahWahRelease(val: Port);

        /**
         * The sustain.
         * @since 5.2.0
         */
        get wah_wah_sustain(): Port;
        set wah_wah_sustain(val: Port);

        /**
         * The sustain.
         * @since 5.2.0
         */
        get wahWahSustain(): Port;
        set wahWahSustain(val: Port);

        /**
         * The tuning.
         * @since 5.2.0
         */
        get wah_wah_tuning(): Port;
        set wah_wah_tuning(val: Port);

        /**
         * The tuning.
         * @since 5.2.0
         */
        get wahWahTuning(): Port;
        set wahWahTuning(val: Port);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxWahWahChannel.SignalSignatures;

        // Fields
        recall_channel: RecallChannel;

        wah_wah_mode: number;

        // Constructors
        constructor(properties?: Partial<FxWahWahChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxWahWahChannel;

        // Conflicted with AgsAudio.RecallChannel.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxWahWahChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxWahWahChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxWahWahChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxWahWahChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxWahWahChannelProcessor {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxWahWahChannelProcessor extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<FxWahWahChannelProcessor>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxWahWahChannelProcessor.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<FxWahWahChannelProcessor.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel): FxWahWahChannelProcessor;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxWahWahChannelProcessor.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxWahWahChannelProcessor.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxWahWahChannelProcessor.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxWahWahChannelProcessor.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace FxWahWahRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class FxWahWahRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<FxWahWahRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: FxWahWahRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<FxWahWahRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling): FxWahWahRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof FxWahWahRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, FxWahWahRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof FxWahWahRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<FxWahWahRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GenericRecallChannelRun {
        // Signal signatures
        interface SignalSignatures extends RecallChannelRun.SignalSignatures {
            "notify::generic-recall-recycling-child-type": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallChannelRun.ConstructorProps, Ags.Connectable.ConstructorProps {
            generic_recall_recycling_child_type: GObject.GTypeInput;
            genericRecallRecyclingChildType: GObject.GTypeInput;
        }
    }

    /**
     * @gir-type Class
     */
    class GenericRecallChannelRun extends RecallChannelRun implements Ags.Connectable {
        static $gtype: GObject.GType<GenericRecallChannelRun>;

        // Properties
        /**
         * The type of child {@link AgsAudio.Recall}.
         * @since 3.0.0
         */
        get generic_recall_recycling_child_type(): GObject.GType;
        set generic_recall_recycling_child_type(val: GObject.GType);

        /**
         * The type of child {@link AgsAudio.Recall}.
         * @since 3.0.0
         */
        get genericRecallRecyclingChildType(): GObject.GType;
        set genericRecallRecyclingChildType(val: GObject.GType);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GenericRecallChannelRun.SignalSignatures;

        // Fields
        recall_channel_run: RecallChannelRun;

        // Constructors
        constructor(properties?: Partial<GenericRecallChannelRun.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](source: Channel, child_type: GObject.GType, generic_recall_recycling_child_type: GObject.GType): GenericRecallChannelRun;

        // Conflicted with AgsAudio.RecallChannelRun.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof GenericRecallChannelRun.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GenericRecallChannelRun.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GenericRecallChannelRun.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GenericRecallChannelRun.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GenericRecallRecycling {
        // Signal signatures
        interface SignalSignatures extends RecallRecycling.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends RecallRecycling.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class GenericRecallRecycling extends RecallRecycling implements Ags.Connectable {
        static $gtype: GObject.GType<GenericRecallRecycling>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GenericRecallRecycling.SignalSignatures;

        // Fields
        recall_recycling: RecallRecycling;

        // Constructors
        constructor(properties?: Partial<GenericRecallRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling, child_type: GObject.GType): GenericRecallRecycling;

        // Conflicted with AgsAudio.RecallRecycling.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof GenericRecallRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GenericRecallRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GenericRecallRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GenericRecallRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace GstreamerClient {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::client-name": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::gstreamer-server": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            client_name: string;
            clientName: string;
            device: GObject.Object[];
            gstreamer_server: GstreamerServer;
            gstreamerServer: GstreamerServer;
            port: GstreamerPort[];
        }
    }

    /**
     * @gir-type Class
     */
    class GstreamerClient extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<GstreamerClient>;

        // Properties
        /**
         * The gstreamer client name.
         * @since 3.6.0
         * @default null
         */
        get client_name(): string;
        set client_name(val: string);

        /**
         * The gstreamer client name.
         * @since 3.6.0
         * @default null
         */
        get clientName(): string;
        set clientName(val: string);

        /**
         * The assigned devices.
         * @since 3.6.0
         */
        get device(): GObject.Object[];
        set device(val: GObject.Object[]);

        /**
         * The assigned {@link AgsAudio.GstreamerServer}.
         * @since 3.6.0
         */
        get gstreamer_server(): GstreamerServer;
        set gstreamer_server(val: GstreamerServer);

        /**
         * The assigned {@link AgsAudio.GstreamerServer}.
         * @since 3.6.0
         */
        get gstreamerServer(): GstreamerServer;
        set gstreamerServer(val: GstreamerServer);

        /**
         * The assigned ports.
         * @since 3.6.0
         */
        get port(): GstreamerPort[];
        set port(val: GstreamerPort[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GstreamerClient.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: GstreamerClientFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        pipeline: Gst.Element;

        client_uuid: string;

        // Constructors
        constructor(properties?: Partial<GstreamerClient.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](gstreamer_server: GObject.Object): GstreamerClient;

        // Signals
        /** @signal */
        connect_after<K extends keyof GstreamerClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GstreamerClient.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GstreamerClient.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GstreamerClient.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `client_name` in `gstreamer_client`.
         * @param gstreamer_client the {@link GLib.List}-struct containing {@link AgsAudio.GstreamerClient}
         * @param client_name the client name to find
         */
        static find(gstreamer_client: GstreamerClient[], client_name: string): GstreamerClient[];

        /**
         * Finds next match of `client_uuid` in `gstreamer_client`.
         * @param gstreamer_client the {@link GLib.List}-struct containing {@link AgsAudio.GstreamerClient}
         * @param client_uuid the client uuid to find
         */
        static find_uuid(gstreamer_client: GstreamerClient[], client_uuid: string): GstreamerClient[];

        // Methods
        /**
         * Activate client.
         */
        activate(): void;

        /**
         * Add `gstreamer_device` to `gstreamer_client`.
         * @param gstreamer_device an {@link AgsAudio.GstreamerDevout} or {@link AgsAudio.GstreamerDevin}
         */
        add_device(gstreamer_device: GObject.Object): void;

        /**
         * Add `gstreamer_port` to `gstreamer_client`.
         * @param gstreamer_port an {@link AgsAudio.GstreamerPort}
         */
        add_port(gstreamer_port: GObject.Object): void;

        /**
         * Close the gstreamer client's connection.
         */
        close(): void;

        /**
         * Deactivate client.
         */
        deactivate(): void;

        /**
         * Open the gstreamer client's connection and read uuid.
         * @param client_name the client's name
         */
        open(client_name: string): void;

        /**
         * Remove `gstreamer_device` from `gstreamer_client`.
         * @param gstreamer_device an {@link AgsAudio.GstreamerDevout} or {@link AgsAudio.GstreamerDevin}
         */
        remove_device(gstreamer_device: GObject.Object): void;

        /**
         * Remove `gstreamer_port` from `gstreamer_client`.
         * @param gstreamer_port an {@link AgsAudio.GstreamerPort}
         */
        remove_port(gstreamer_port: GObject.Object): void;

        /**
         * Enable a feature of `gstreamer_client`.
         * @param flags see {@link AgsAudio.GstreamerClientFlags}-enum
         */
        set_flags(flags: GstreamerClientFlags): void;

        /**
         * Test `flags` to be set on `gstreamer_client`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: GstreamerClientFlags): boolean;

        /**
         * Disable a feature of `gstreamer_client`.
         * @param flags see {@link AgsAudio.GstreamerClientFlags}-enum
         */
        unset_flags(flags: GstreamerClientFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace GstreamerDevin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::gstreamer-client": (pspec: GObject.ParamSpec) => void;
            "notify::gstreamer-port": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            gstreamer_client: GstreamerClient;
            gstreamerClient: GstreamerClient;
            gstreamer_port: any;
            gstreamerPort: any;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class GstreamerDevin extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<GstreamerDevin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.6.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.6.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.6.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.6.0
         * @default 940
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.6.0
         * @default 940
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.6.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.6.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The gstreamer soundcard indentifier
         * @since 3.6.0
         * @default ags-gstreamer-devin-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.6.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.6.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.6.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned {@link AgsAudio.GstreamerClient}
         * @since 3.6.0
         */
        get gstreamer_client(): GstreamerClient;
        set gstreamer_client(val: GstreamerClient);

        /**
         * The assigned {@link AgsAudio.GstreamerClient}
         * @since 3.6.0
         */
        get gstreamerClient(): GstreamerClient;
        set gstreamerClient(val: GstreamerClient);

        /**
         * The assigned {@link AgsAudio.GstreamerPort}
         * @since 3.6.0
         */
        get gstreamer_port(): any;
        set gstreamer_port(val: any);

        /**
         * The assigned {@link AgsAudio.GstreamerPort}
         * @since 3.6.0
         */
        get gstreamerPort(): any;
        set gstreamerPort(val: any);

        /**
         * The pcm channel count
         * @since 3.6.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.6.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.6.0
         * @default 44100
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GstreamerDevin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: GstreamerDevinFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: GstreamerDevinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: GstreamerDevinAppBufferMode;

        app_buffer: any;

        sub_block_count: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<GstreamerDevin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): GstreamerDevin;

        // Signals
        /** @signal */
        connect_after<K extends keyof GstreamerDevin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GstreamerDevin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GstreamerDevin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GstreamerDevin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `gstreamer_devin`.
         * @param flags see {@link AgsAudio.GstreamerDevinFlags}-enum
         */
        set_flags(flags: GstreamerDevinFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `gstreamer_devin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: GstreamerDevinFlags): boolean;

        /**
         * Disable a feature of `gstreamer_devin`.
         * @param flags see {@link AgsAudio.GstreamerDevinFlags}-enum
         */
        unset_flags(flags: GstreamerDevinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace GstreamerDevout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::gstreamer-client": (pspec: GObject.ParamSpec) => void;
            "notify::gstreamer-port": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            gstreamer_client: GstreamerClient;
            gstreamerClient: GstreamerClient;
            gstreamer_port: any;
            gstreamerPort: any;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class GstreamerDevout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<GstreamerDevout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.6.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.6.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.6.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.6.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.6.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.6.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.6.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The gstreamer soundcard indentifier
         * @since 3.6.0
         * @default ags-gstreamer-devout-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.6.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.6.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.6.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned {@link AgsAudio.GstreamerClient}
         * @since 3.6.0
         */
        get gstreamer_client(): GstreamerClient;
        set gstreamer_client(val: GstreamerClient);

        /**
         * The assigned {@link AgsAudio.GstreamerClient}
         * @since 3.6.0
         */
        get gstreamerClient(): GstreamerClient;
        set gstreamerClient(val: GstreamerClient);

        /**
         * The assigned {@link AgsAudio.GstreamerPort}
         * @since 3.6.0
         */
        get gstreamer_port(): any;
        set gstreamer_port(val: any);

        /**
         * The assigned {@link AgsAudio.GstreamerPort}
         * @since 3.6.0
         */
        get gstreamerPort(): any;
        set gstreamerPort(val: any);

        /**
         * The pcm channel count
         * @since 3.6.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.6.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.6.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GstreamerDevout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: GstreamerDevoutFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: GstreamerDevoutSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: GstreamerDevoutAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        notify_soundcard: GObject.Object;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<GstreamerDevout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): GstreamerDevout;

        // Signals
        /** @signal */
        connect_after<K extends keyof GstreamerDevout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GstreamerDevout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GstreamerDevout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GstreamerDevout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `gstreamer_devout`.
         * @param flags see {@link AgsAudio.GstreamerDevoutFlags}-enum
         */
        set_flags(flags: GstreamerDevoutFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `gstreamer_devout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: GstreamerDevoutFlags): boolean;

        /**
         * Disable a feature of `gstreamer_devout`.
         * @param flags see {@link AgsAudio.GstreamerDevoutFlags}-enum
         */
        unset_flags(flags: GstreamerDevoutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace GstreamerFile {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, SoundResource.ConstructorProps {
            audio_channels: number;
            audioChannels: number;
            buffer_size: number;
            bufferSize: number;
            filename: string;
            format: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class GstreamerFile extends GObject.Object implements Ags.Connectable, SoundResource {
        static $gtype: GObject.GType<GstreamerFile>;

        // Properties
        /**
         * The audio channels to be used.
         * @since 3.6.0
         * @default 0
         */
        get audio_channels(): number;
        set audio_channels(val: number);

        /**
         * The audio channels to be used.
         * @since 3.6.0
         * @default 0
         */
        get audioChannels(): number;
        set audioChannels(val: number);

        /**
         * The buffer size to be used.
         * @since 3.6.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.6.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The assigned filename.
         * @since 3.6.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The format to be used.
         * @since 3.6.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The samplerate to be used.
         * @since 3.6.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GstreamerFile.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        audio_channel_written: number;

        offset: number;

        buffer_offset: number;

        full_buffer: any;

        buffer: any;

        pointer: number;

        current: number;

        length: number;

        read_pipeline: Gst.Element;

        read_pipeline_running: boolean;

        write_pipeline: Gst.Element;

        write_pipeline_running: boolean;

        write_pipeline_need_data: boolean;

        playbin: Gst.Element;

        video_sink: Gst.Element;

        audio_sink: Gst.Element;

        text_sink: Gst.Element;

        rw_video_app_src: Gst.Element;

        rw_audio_app_src: Gst.Element;

        rw_text_app_src: Gst.Element;

        rw_audio_tee: Gst.Element;

        rw_video_file_sink_queue: Gst.Element;

        rw_audio_file_sink_queue: Gst.Element;

        rw_text_file_sink_queue: Gst.Element;

        rw_audio_convert: Gst.Element;

        rw_audio_resample: Gst.Element;

        rw_audio_mixer: Gst.Element;

        rw_file_encoder: Gst.Element;

        rw_file_sink: Gst.Element;

        rw_audio_app_sink_queue: Gst.Element;

        rw_audio_app_sink: Gst.Element;

        prev_frame_count: number;

        rw_buffer: any[];

        rw_info: any[];

        rw_current_buffer: any[];

        rw_current_info: any[];

        // Constructors
        constructor(properties?: Partial<GstreamerFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): GstreamerFile;

        // Signals
        /** @signal */
        connect_after<K extends keyof GstreamerFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GstreamerFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GstreamerFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GstreamerFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Check suffix.
         * @param filename the filename
         */
        static check_suffix(filename: string): boolean;

        // Methods
        /**
         * Detect rw and create rw pipeline.
         */
        detect_encoding_profile(): boolean;

        /**
         * Enable a feature of `gstreamer_file`.
         * @param flags see `AgsGstreamerFileFlags`-enum
         */
        set_flags(flags: number): void;

        /**
         * Test `flags` to be set on `gstreamer_file`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Disable a feature of `gstreamer_file`.
         * @param flags see `AgsGstreamerFileFlags`-enum
         */
        unset_flags(flags: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Close `sound_resource`.
         */
        close(): void;

        /**
         * Flush `sound_resource`.
         */
        flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         */
        get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         */
        info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         */
        load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @returns `true` if operation was successful, otherwise `false`.
         */
        open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @returns the count of frames actually read
         */
        read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal_at_once(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read wave from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @param x_offset the x offset
         * @param delay the delay
         * @param attack the attack
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.Wave}
         */
        read_wave(soundcard: GObject.Object, audio_channel: number, x_offset: (bigint | number), delay: number, attack: number): Wave[];

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @returns `true` if operation was successful, otherwise `false`.
         */
        rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         */
        seek(frame_count: (bigint | number), whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         */
        set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         */
        write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;

        /**
         * Close `sound_resource`.
         * @virtual
         */
        vfunc_close(): void;

        /**
         * Flush `sound_resource`.
         * @virtual
         */
        vfunc_flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         * @virtual
         */
        vfunc_get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         * @virtual
         */
        vfunc_info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         * @virtual
         */
        vfunc_load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @virtual
         */
        vfunc_open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @virtual
         */
        vfunc_read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @virtual
         */
        vfunc_rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         * @virtual
         */
        vfunc_seek(frame_count: number, whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         * @virtual
         */
        vfunc_set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         * @virtual
         */
        vfunc_write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;
    }


    namespace GstreamerPort {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::gstreamer-client": (pspec: GObject.ParamSpec) => void;
            "notify::gstreamer-devin": (pspec: GObject.ParamSpec) => void;
            "notify::gstreamer-devout": (pspec: GObject.ParamSpec) => void;
            "notify::port-name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            gstreamer_client: GstreamerClient;
            gstreamerClient: GstreamerClient;
            gstreamer_devin: GstreamerDevin;
            gstreamerDevin: GstreamerDevin;
            gstreamer_devout: GstreamerDevout;
            gstreamerDevout: GstreamerDevout;
            port_name: string;
            portName: string;
        }
    }

    /**
     * @gir-type Class
     */
    class GstreamerPort extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<GstreamerPort>;

        // Properties
        /**
         * The assigned {@link AgsAudio.GstreamerClient}.
         * @since 3.6.0
         */
        get gstreamer_client(): GstreamerClient;
        set gstreamer_client(val: GstreamerClient);

        /**
         * The assigned {@link AgsAudio.GstreamerClient}.
         * @since 3.6.0
         */
        get gstreamerClient(): GstreamerClient;
        set gstreamerClient(val: GstreamerClient);

        /**
         * The assigned {@link AgsAudio.GstreamerDevout}.
         * @since 3.6.0
         */
        get gstreamer_devin(): GstreamerDevin;
        set gstreamer_devin(val: GstreamerDevin);

        /**
         * The assigned {@link AgsAudio.GstreamerDevout}.
         * @since 3.6.0
         */
        get gstreamerDevin(): GstreamerDevin;
        set gstreamerDevin(val: GstreamerDevin);

        /**
         * The assigned {@link AgsAudio.GstreamerDevout}.
         * @since 3.6.0
         */
        get gstreamer_devout(): GstreamerDevout;
        set gstreamer_devout(val: GstreamerDevout);

        /**
         * The assigned {@link AgsAudio.GstreamerDevout}.
         * @since 3.6.0
         */
        get gstreamerDevout(): GstreamerDevout;
        set gstreamerDevout(val: GstreamerDevout);

        /**
         * The gstreamer soundcard indentifier
         * @since 3.6.0
         * @default null
         */
        get port_name(): string;
        set port_name(val: string);

        /**
         * The gstreamer soundcard indentifier
         * @since 3.6.0
         * @default null
         */
        get portName(): string;
        set portName(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GstreamerPort.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: GstreamerPortFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        port_uuid: string;

        pcm_channels: number;

        samplerate: number;

        buffer_size: number;

        format: Ags.SoundcardFormat;

        use_cache: boolean;

        cache_buffer_size: number;

        current_cache: number;

        completed_cache: number;

        cache_offset: number;

        cache: any;

        empty_buffer: any;

        is_empty: boolean;

        underflow: number;

        restart: boolean;

        nth_empty_buffer: number;

        queued: number;

        // Constructors
        constructor(properties?: Partial<GstreamerPort.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](gstreamer_client: GObject.Object): GstreamerPort;

        // Signals
        /** @signal */
        connect_after<K extends keyof GstreamerPort.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GstreamerPort.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GstreamerPort.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GstreamerPort.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `port_name` in `gstreamer_port`.
         * @param gstreamer_port the {@link GLib.List}-struct containig {@link AgsAudio.GstreamerPort}
         * @param port_name the port name to find
         */
        static find(gstreamer_port: GstreamerPort[], port_name: string): GstreamerPort[];

        // Methods
        get_fixed_size(): number;

        /**
         * Gets latency.
         */
        get_latency(): number;

        /**
         * Register a new gstreamer port and read uuid. Creates a new AgsSequencer or AgsSoundcard
         * object.
         * @param port_name the name as string
         * @param is_audio if `true` interpreted as audio port
         * @param is_midi if `true` interpreted as midi port
         * @param is_output if `true` port is acting as output, otherwise as input
         */
        register(port_name: string, is_audio: boolean, is_midi: boolean, is_output: boolean): void;

        /**
         * @param buffer_size 
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * @param cache_buffer_size 
         */
        set_cache_buffer_size(cache_buffer_size: number): void;

        /**
         * Enable a feature of `gstreamer_port`.
         * @param flags see {@link AgsAudio.GstreamerPortFlags}-enum
         */
        set_flags(flags: GstreamerPortFlags): void;

        /**
         * @param format 
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * @param pcm_channels 
         */
        set_pcm_channels(pcm_channels: number): void;

        /**
         * @param samplerate 
         */
        set_samplerate(samplerate: number): void;

        /**
         * Test `flags` to be set on `gstreamer_port`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: GstreamerPortFlags): boolean;

        unregister(): void;

        /**
         * Disable a feature of `gstreamer_port`.
         * @param flags see {@link AgsAudio.GstreamerPortFlags}-enum
         */
        unset_flags(flags: GstreamerPortFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace GstreamerServer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::default-gstreamer-client": (pspec: GObject.ParamSpec) => void;
            "notify::default-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::gstreamer-client": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.SoundServer.ConstructorProps {
            default_gstreamer_client: GstreamerClient;
            defaultGstreamerClient: GstreamerClient;
            default_soundcard: GObject.Object;
            defaultSoundcard: GObject.Object;
            gstreamer_client: GstreamerClient[];
            gstreamerClient: GstreamerClient[];
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class GstreamerServer extends GObject.Object implements Ags.Connectable, Ags.SoundServer {
        static $gtype: GObject.GType<GstreamerServer>;

        // Properties
        /**
         * The default gstreamer client.
         * @since 3.6.0
         */
        get default_gstreamer_client(): GstreamerClient;
        set default_gstreamer_client(val: GstreamerClient);

        /**
         * The default gstreamer client.
         * @since 3.6.0
         */
        get defaultGstreamerClient(): GstreamerClient;
        set defaultGstreamerClient(val: GstreamerClient);

        /**
         * The default soundcard.
         * @since 3.6.0
         */
        get default_soundcard(): GObject.Object;
        set default_soundcard(val: GObject.Object);

        /**
         * The default soundcard.
         * @since 3.6.0
         */
        get defaultSoundcard(): GObject.Object;
        set defaultSoundcard(val: GObject.Object);

        /**
         * The gstreamer client list.
         * @since 3.6.0
         */
        get gstreamer_client(): GstreamerClient[];
        set gstreamer_client(val: GstreamerClient[]);

        /**
         * The gstreamer client list.
         * @since 3.6.0
         */
        get gstreamerClient(): GstreamerClient[];
        set gstreamerClient(val: GstreamerClient[]);

        /**
         * The assigned URL.
         * @since 3.6.0
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: GstreamerServer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        running: boolean;

        application_context: Ags.ApplicationContext;

        uuid: Ags.UUID;

        port: number;

        port_count: number;

        n_soundcards: number;

        n_sequencers: number;

        default_client: GObject.Object;

        client: any[];

        // Constructors
        constructor(properties?: Partial<GstreamerServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](url: string): GstreamerServer;

        // Signals
        /** @signal */
        connect_after<K extends keyof GstreamerServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, GstreamerServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof GstreamerServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<GstreamerServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find {@link AgsAudio.GstreamerServer} by url.
         * @param gstreamer_server the {@link GLib.List}-struct containing {@link AgsAudio.GstreamerServer}
         * @param url the url to find
         */
        static find_url(gstreamer_server: GstreamerServer[], url: string): GstreamerServer[];

        // Methods
        /**
         * Add `gstreamer_client` to `gstreamer_server`
         * @param gstreamer_client the {@link AgsAudio.GstreamerClient} to add
         */
        add_client(gstreamer_client: GObject.Object): void;

        /**
         * Connect all clients.
         */
        connect_client(): void;

        /**
         * Disconnect all clients.
         */
        disconnect_client(): void;

        /**
         * Find {@link AgsAudio.GstreamerClient} by uuid.
         * @param client_uuid the uuid to find
         * @returns the {@link AgsAudio.GstreamerClient} found or `null`
         */
        find_client<T = GObject.Object>(client_uuid: string): T;

        /**
         * Find {@link AgsAudio.GstreamerPort} by uuid.
         * @param port_uuid the uuid to find
         * @returns the {@link AgsAudio.GstreamerPort} found or `null`
         */
        find_port<T = GObject.Object>(port_uuid: string): T;

        /**
         * Remove `gstreamer_client` to `gstreamer_server`
         * @param gstreamer_client the {@link AgsAudio.GstreamerClient} to remove
         */
        remove_client(gstreamer_client: GObject.Object): void;

        start_poll(): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @returns the port as string `null`-terminated array
         */
        get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the sequencer as {@link GLib.List}-struct
         */
        get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the soundcard as {@link GLib.List}-struct
         */
        get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @returns the URL as string
         */
        get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Sequencer}
         */
        register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Soundcard}
         */
        register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param port 
         * @param port_count 
         */
        set_ports(port: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         */
        set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         */
        set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         */
        set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         */
        unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         */
        unregister_soundcard(soundcard: GObject.Object): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @virtual
         */
        vfunc_get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @virtual
         */
        vfunc_get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param ports 
         * @param port_count 
         * @virtual
         */
        vfunc_set_ports(ports: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         * @virtual
         */
        vfunc_set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         * @virtual
         */
        vfunc_set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         * @virtual
         */
        vfunc_set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         * @virtual
         */
        vfunc_unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         * @virtual
         */
        vfunc_unregister_soundcard(soundcard: GObject.Object): void;
    }


    namespace Input {
        // Signal signatures
        interface SignalSignatures extends Channel.SignalSignatures {
            "notify::file-link": (pspec: GObject.ParamSpec) => void;
            "notify::synth-generator": (pspec: GObject.ParamSpec) => void;
            "notify::absolute-key": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::first-recycling": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::key": (pspec: GObject.ParamSpec) => void;
            "notify::last-recycling": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::link": (pspec: GObject.ParamSpec) => void;
            "notify::midi-note": (pspec: GObject.ParamSpec) => void;
            "notify::next": (pspec: GObject.ParamSpec) => void;
            "notify::next-pad": (pspec: GObject.ParamSpec) => void;
            "notify::note-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::note-key": (pspec: GObject.ParamSpec) => void;
            "notify::octave": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::play": (pspec: GObject.ParamSpec) => void;
            "notify::playback": (pspec: GObject.ParamSpec) => void;
            "notify::prev": (pspec: GObject.ParamSpec) => void;
            "notify::prev-pad": (pspec: GObject.ParamSpec) => void;
            "notify::recall": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::recycling-context": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Channel.ConstructorProps, Ags.Connectable.ConstructorProps {
            file_link: Ags.FileLink;
            fileLink: Ags.FileLink;
            synth_generator: SynthGenerator[];
            synthGenerator: SynthGenerator[];
        }
    }

    /**
     * @gir-type Class
     */
    class Input extends Channel implements Ags.Connectable {
        static $gtype: GObject.GType<Input>;

        // Properties
        /**
         * The file containing audio data.
         * @since 3.0.0
         */
        get file_link(): Ags.FileLink;
        set file_link(val: Ags.FileLink);

        /**
         * The file containing audio data.
         * @since 3.0.0
         */
        get fileLink(): Ags.FileLink;
        set fileLink(val: Ags.FileLink);

        /**
         * The synth generators assigned with this input.
         * @since 3.0.0
         */
        get synth_generator(): SynthGenerator[];
        set synth_generator(val: SynthGenerator[]);

        /**
         * The synth generators assigned with this input.
         * @since 3.0.0
         */
        get synthGenerator(): SynthGenerator[];
        set synthGenerator(val: SynthGenerator[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Input.SignalSignatures;

        // Fields
        channel: Channel;

        // Constructors
        constructor(properties?: Partial<Input.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: GObject.Object): Input;

        // Signals
        /** @signal */
        connect_after<K extends keyof Input.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Input.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Input.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Input.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Add `synth_generator` to `input`.
         * @param synth_generator the {@link AgsAudio.SynthGenerator}
         */
        add_synth_generator(synth_generator: GObject.Object): void;

        /**
         * Check if `input` is active and needs processing.
         * @param recycling_context the {@link AgsAudio.RecyclingContext} to check
         * @returns `true` if has a need to be processed, else `false`
         */
        is_active(recycling_context: GObject.Object): boolean;

        /**
         * Find next {@link AgsAudio.Input} needed to be processed.
         * @param recycling_context the {@link AgsAudio.RecyclingContext} to check
         * @returns next active {@link AgsAudio.Input}, else `null` if non available
         */
        next_active(recycling_context: GObject.Object): Input;

        /**
         * Open `filename` and assign `audio_channel`'s data as AGS_AUDIO_SIGNAL_TEMPLATE
         * audio signal to `input`'s own recycling.
         * @param filename the filename as string
         * @param preset the preset to open
         * @param instrument the instrument to open
         * @param sample the sample to open
         * @param audio_channel the audio channel to read
         * @returns `true` if open was successful, else `false`
         */
        open_file(filename: string, preset: string, instrument: string, sample: string, audio_channel: number): boolean;

        /**
         * Remove `synth_generator` from `input`.
         * @param synth_generator the {@link AgsAudio.SynthGenerator}
         */
        remove_synth_generator(synth_generator: GObject.Object): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace Ipatch {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::mode": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, SoundContainer.ConstructorProps {
            filename: string;
            mode: string;
            soundcard: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class Ipatch extends GObject.Object implements Ags.Connectable, SoundContainer {
        static $gtype: GObject.GType<Ipatch>;

        // Properties
        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The assigned mode.
         * @since 3.0.0
         * @default null
         */
        get mode(): string;
        set mode(val: string);

        /**
         * The assigned soundcard.
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Ipatch.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: IpatchFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        nesting_level: number;

        level_id: string;

        level_index: number;

        reader: GObject.Object;

        writer: GObject.Object;

        audio_signal: any[];

        // Constructors
        constructor(properties?: Partial<Ipatch.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Ipatch;

        // Signals
        /** @signal */
        connect_after<K extends keyof Ipatch.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Ipatch.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Ipatch.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Ipatch.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Check `filename`'s suffix to be supported.
         * @param filename the filename
         */
        static check_suffix(filename: string): boolean;

        // Methods
        /**
         * Enable a feature of `ipatch`.
         * @param flags see {@link AgsAudio.IpatchFlags}-enum
         */
        set_flags(flags: IpatchFlags): void;

        /**
         * Test `flags` to be set on `ipatch`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: IpatchFlags): boolean;

        /**
         * Disable a feature of `ipatch`.
         * @param flags see {@link AgsAudio.IpatchFlags}-enum
         */
        unset_flags(flags: IpatchFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Close `sound_container`.
         */
        close(): void;

        /**
         * Get level count.
         * @returns the level count
         */
        get_level_count(): number;

        /**
         * Get level id.
         * @returns the level id as string
         */
        get_level_id(): string;

        /**
         * Get level index.
         * @returns the level index unsigned integer
         */
        get_level_index(): number;

        /**
         * Get nesting level.
         * @returns the nesting level
         */
        get_nesting_level(): number;

        /**
         * Get all resources as a {@link GLib.List}-struct.
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_all(): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @param resource_index the index
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_by_index(resource_index: number): GObject.Object[];

        /**
         * Get resources by name as a {@link GLib.List}-struct.
         * @param resource_name the resource name
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_by_name(resource_name: string): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_current(): GObject.Object[];

        /**
         * Get sublevel names.
         * @returns the string vector containing sublevel names
         */
        get_sublevel_name(): string[];

        /**
         * Level up.
         * @param level_count the count of levels to go up
         * @returns the level count went up
         */
        level_up(level_count: number): number;

        /**
         * Open `sound_container`.
         * @param filename the filename
         * @returns `true` if open succeeded, else `false`
         */
        open(filename: string): boolean;

        /**
         * Select level by id.
         * @param level_id the level id as string
         * @returns the index of level as unsigned integer
         */
        select_level_by_id(level_id: string): number;

        /**
         * Select level by index.
         * @param level_index the level index unsigned integer
         * @returns the index of level as unsigned integer
         */
        select_level_by_index(level_index: number): number;

        /**
         * Close `sound_container`.
         * @virtual
         */
        vfunc_close(): void;

        /**
         * Get level count.
         * @virtual
         */
        vfunc_get_level_count(): number;

        /**
         * Get level id.
         * @virtual
         */
        vfunc_get_level_id(): string;

        /**
         * Get level index.
         * @virtual
         */
        vfunc_get_level_index(): number;

        /**
         * Get nesting level.
         * @virtual
         */
        vfunc_get_nesting_level(): number;

        /**
         * Get all resources as a {@link GLib.List}-struct.
         * @virtual
         */
        vfunc_get_resource_all(): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @param resource_index the index
         * @virtual
         */
        vfunc_get_resource_by_index(resource_index: number): GObject.Object[];

        /**
         * Get resources by name as a {@link GLib.List}-struct.
         * @param resource_name the resource name
         * @virtual
         */
        vfunc_get_resource_by_name(resource_name: string): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @virtual
         */
        vfunc_get_resource_current(): GObject.Object[];

        /**
         * Get sublevel names.
         * @virtual
         */
        vfunc_get_sublevel_name(): string[];

        /**
         * Level up.
         * @param level_count the count of levels to go up
         * @virtual
         */
        vfunc_level_up(level_count: number): number;

        /**
         * Open `sound_container`.
         * @param filename the filename
         * @virtual
         */
        vfunc_open(filename: string): boolean;

        /**
         * Select level by id.
         * @param level_id the level id as string
         * @virtual
         */
        vfunc_select_level_by_id(level_id: string): number;

        /**
         * Select level by index.
         * @param level_index the level index unsigned integer
         * @virtual
         */
        vfunc_select_level_by_index(level_index: number): number;
    }


    namespace IpatchDLS2Reader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::ipatch": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            ipatch: Ipatch;
        }
    }

    /**
     * @gir-type Class
     */
    class IpatchDLS2Reader extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<IpatchDLS2Reader>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Ipatch}
         * @since 3.0.0
         */
        get ipatch(): Ipatch;
        set ipatch(val: Ipatch);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: IpatchDLS2Reader.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        level: number;

        index_selected: number;

        name_selected: string;

        error: GLib.Error;

        // Constructors
        constructor(properties?: Partial<IpatchDLS2Reader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](ipatch: Ipatch): IpatchDLS2Reader;

        // Signals
        /** @signal */
        connect_after<K extends keyof IpatchDLS2Reader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, IpatchDLS2Reader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof IpatchDLS2Reader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<IpatchDLS2Reader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get instrument all.
         * @returns the string vector containing instrument
         */
        get_instrument_all(): string[];

        /**
         * Get sample all.
         * @returns the string vector containing sample
         */
        get_sample_all(): string[];

        /**
         * Get sample by instrument index.
         * @param instrument_index the instrument index
         * @returns the string vector containing sample
         */
        get_sample_by_instrument_index(instrument_index: number): string[];

        /**
         * Select instrument.
         * @param instrument_index the instrument index
         * @returns `true` on success, else `false` on failure
         */
        select_instrument(instrument_index: number): boolean;

        /**
         * Select sample.
         * @param sample_index the sample index
         * @returns `true` on success, else `false` on failure
         */
        select_sample(sample_index: number): boolean;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace IpatchGigReader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::ipatch": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            ipatch: Ipatch;
        }
    }

    /**
     * @gir-type Class
     */
    class IpatchGigReader extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<IpatchGigReader>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Ipatch}
         * @since 3.0.0
         */
        get ipatch(): Ipatch;
        set ipatch(val: Ipatch);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: IpatchGigReader.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        level: number;

        index_selected: number;

        name_selected: string;

        error: GLib.Error;

        // Constructors
        constructor(properties?: Partial<IpatchGigReader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](ipatch: Ipatch): IpatchGigReader;

        // Signals
        /** @signal */
        connect_after<K extends keyof IpatchGigReader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, IpatchGigReader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof IpatchGigReader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<IpatchGigReader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get instrument all.
         * @returns the string vector containing instrument
         */
        get_instrument_all(): string[];

        /**
         * Get sample all.
         * @returns the string vector containing sample
         */
        get_sample_all(): string[];

        /**
         * Get sample by instrument index.
         * @param instrument_index the instrument index
         * @returns the string vector containing sample
         */
        get_sample_by_instrument_index(instrument_index: number): string[];

        /**
         * Select instrument.
         * @param instrument_index the instrument index
         * @returns `true` on success, else `false` on failure
         */
        select_instrument(instrument_index: number): boolean;

        /**
         * @param sample_index 
         */
        select_sample(sample_index: number): boolean;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace IpatchSF2Reader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::ipatch": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            ipatch: Ipatch;
        }
    }

    /**
     * @gir-type Class
     */
    class IpatchSF2Reader extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<IpatchSF2Reader>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Ipatch}
         * @since 3.0.0
         */
        get ipatch(): Ipatch;
        set ipatch(val: Ipatch);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: IpatchSF2Reader.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        level: number;

        index_selected: number;

        name_selected: string;

        error: GLib.Error;

        // Constructors
        constructor(properties?: Partial<IpatchSF2Reader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](ipatch: Ipatch): IpatchSF2Reader;

        // Signals
        /** @signal */
        connect_after<K extends keyof IpatchSF2Reader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, IpatchSF2Reader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof IpatchSF2Reader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<IpatchSF2Reader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get all instrument names.
         * @returns the string vector cotaining instrument names or `null` on failure
         */
        get_instrument_all(): string[];

        /**
         * Get instrument by preset index.
         * @param preset_index the preset index
         * @returns the string vector cotaining instrument names or `null` on failure
         */
        get_instrument_by_preset_index(preset_index: number): string[];

        /**
         * Get all preset names.
         * @returns the string vector cotaining preset names or `null` on failure
         */
        get_preset_all(): string[];

        /**
         * Get all sample names.
         * @returns the string vector cotaining sample names or `null` on failure
         */
        get_sample_all(): string[];

        /**
         * Get sample by preset and instrument index.
         * @param preset_index the preset index
         * @param instrument_index the instrument index
         * @returns the string vector cotaining sample names or `null` on failure
         */
        get_sample_by_preset_and_instrument_index(preset_index: number, instrument_index: number): string[];

        /**
         * Get sample by preset index.
         * @param preset_index the preset index
         * @returns the string vector cotaining sample names or `null` on failure
         */
        get_sample_by_preset_index(preset_index: number): string[];

        /**
         * Select instrument.
         * @param instrument_index the instrument index
         * @returns `true` on success, else `false` on failure
         */
        select_instrument(instrument_index: number): boolean;

        /**
         * Select preset.
         * @param preset_index the preset index
         * @returns `true` on success, else `false` on failure
         */
        select_preset(preset_index: number): boolean;

        /**
         * Select sample.
         * @param sample_index the sample index
         * @returns `true` on success, else `false` on failure
         */
        select_sample(sample_index: number): boolean;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace IpatchSample {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::sample": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, SoundResource.ConstructorProps {
            buffer_size: number;
            bufferSize: number;
            format: number;
            sample: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class IpatchSample extends GObject.Object implements Ags.Connectable, SoundResource {
        static $gtype: GObject.GType<IpatchSample>;

        // Properties
        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The format to be used.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned output {@link AgsAudio.IpatchSample}.
         * @since 3.0.0
         */
        get sample(): GObject.Object;
        set sample(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: IpatchSample.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        audio_channels: number;

        audio_channel_written: number;

        offset: number;

        buffer_offset: number;

        full_buffer: any;

        buffer: any;

        pointer: number;

        current: number;

        length: number;

        // Constructors
        constructor(properties?: Partial<IpatchSample.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): IpatchSample;

        // Signals
        /** @signal */
        connect_after<K extends keyof IpatchSample.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, IpatchSample.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof IpatchSample.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<IpatchSample.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Close `sound_resource`.
         */
        close(): void;

        /**
         * Flush `sound_resource`.
         */
        flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         */
        get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         */
        info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         */
        load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @returns `true` if operation was successful, otherwise `false`.
         */
        open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @returns the count of frames actually read
         */
        read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal_at_once(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read wave from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @param x_offset the x offset
         * @param delay the delay
         * @param attack the attack
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.Wave}
         */
        read_wave(soundcard: GObject.Object, audio_channel: number, x_offset: (bigint | number), delay: number, attack: number): Wave[];

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @returns `true` if operation was successful, otherwise `false`.
         */
        rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         */
        seek(frame_count: (bigint | number), whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         */
        set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         */
        write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;

        /**
         * Close `sound_resource`.
         * @virtual
         */
        vfunc_close(): void;

        /**
         * Flush `sound_resource`.
         * @virtual
         */
        vfunc_flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         * @virtual
         */
        vfunc_get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         * @virtual
         */
        vfunc_info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         * @virtual
         */
        vfunc_load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @virtual
         */
        vfunc_open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @virtual
         */
        vfunc_read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @virtual
         */
        vfunc_rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         * @virtual
         */
        vfunc_seek(frame_count: number, whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         * @virtual
         */
        vfunc_set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         * @virtual
         */
        vfunc_write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;
    }


    namespace JackClient {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::client-name": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::jack-server": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            client_name: string;
            clientName: string;
            device: GObject.Object[];
            jack_server: JackServer;
            jackServer: JackServer;
            port: JackPort[];
        }
    }

    /**
     * @gir-type Class
     */
    class JackClient extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<JackClient>;

        // Properties
        /**
         * The JACK client name.
         * @since 3.0.0
         * @default null
         */
        get client_name(): string;
        set client_name(val: string);

        /**
         * The JACK client name.
         * @since 3.0.0
         * @default null
         */
        get clientName(): string;
        set clientName(val: string);

        /**
         * The assigned devices.
         * @since 3.0.0
         */
        get device(): GObject.Object[];
        set device(val: GObject.Object[]);

        /**
         * The assigned {@link AgsAudio.JackServer}.
         * @since 3.0.0
         */
        get jack_server(): JackServer;
        set jack_server(val: JackServer);

        /**
         * The assigned {@link AgsAudio.JackServer}.
         * @since 3.0.0
         */
        get jackServer(): JackServer;
        set jackServer(val: JackServer);

        /**
         * The assigned ports.
         * @since 3.0.0
         */
        get port(): JackPort[];
        set port(val: JackPort[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: JackClient.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: JackClientFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        client_uuid: string;

        queued: number;

        // Constructors
        constructor(properties?: Partial<JackClient.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](jack_server: GObject.Object): JackClient;

        // Signals
        /** @signal */
        connect_after<K extends keyof JackClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JackClient.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof JackClient.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<JackClient.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `client_name` in `jack_client`.
         * @param jack_client the {@link GLib.List}-struct containing {@link AgsAudio.JackClient}
         * @param client_name the client name to find
         */
        static find(jack_client: JackClient[], client_name: string): JackClient[];

        /**
         * Finds next match of `client_uuid` in `jack_client`.
         * @param jack_client the {@link GLib.List}-struct containing {@link AgsAudio.JackClient}
         * @param client_uuid the client uuid to find
         */
        static find_uuid(jack_client: JackClient[], client_uuid: string): JackClient[];

        // Methods
        /**
         * Activate client.
         */
        activate(): void;

        /**
         * Add `jack_device` to `jack_client`.
         * @param jack_device an {@link AgsAudio.JackDevout}, {@link AgsAudio.JackDevin} or {@link AgsAudio.JackMidiin}
         */
        add_device(jack_device: GObject.Object): void;

        /**
         * Add `jack_port` to `jack_client`.
         * @param jack_port an {@link AgsAudio.JackPort}
         */
        add_port(jack_port: GObject.Object): void;

        /**
         * Close the JACK client's connection.
         */
        close(): void;

        /**
         * Deactivate client.
         */
        deactivate(): void;

        /**
         * Open the JACK client's connection and read uuid.
         * @param client_name the client's name
         */
        open(client_name: string): void;

        /**
         * Remove `jack_device` from `jack_client`.
         * @param jack_device an {@link AgsAudio.JackDevout}, {@link AgsAudio.JackDevin} or {@link AgsAudio.JackMidiin}
         */
        remove_device(jack_device: GObject.Object): void;

        /**
         * Remove `jack_port` from `jack_client`.
         * @param jack_port an {@link AgsAudio.JackPort}
         */
        remove_port(jack_port: GObject.Object): void;

        /**
         * Enable a feature of `jack_client`.
         * @param flags see {@link AgsAudio.JackClientFlags}-enum
         */
        set_flags(flags: JackClientFlags): void;

        /**
         * Test `flags` to be set on `jack_client`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: JackClientFlags): boolean;

        /**
         * Disable a feature of `jack_client`.
         * @param flags see {@link AgsAudio.JackClientFlags}-enum
         */
        unset_flags(flags: JackClientFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace JackDevin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::jack-client": (pspec: GObject.ParamSpec) => void;
            "notify::jack-port": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            jack_client: JackClient;
            jackClient: JackClient;
            jack_port: any;
            jackPort: any;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class JackDevin extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<JackDevin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The jack soundcard indentifier
         * @since 3.0.0
         * @default ags-jack-devin-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned {@link AgsAudio.JackClient}
         * @since 3.0.0
         */
        get jack_client(): JackClient;
        set jack_client(val: JackClient);

        /**
         * The assigned {@link AgsAudio.JackClient}
         * @since 3.0.0
         */
        get jackClient(): JackClient;
        set jackClient(val: JackClient);

        /**
         * The assigned {@link AgsAudio.JackPort}
         * @since 3.0.0
         */
        get jack_port(): any;
        set jack_port(val: any);

        /**
         * The assigned {@link AgsAudio.JackPort}
         * @since 3.0.0
         */
        get jackPort(): any;
        set jackPort(val: any);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 44100
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: JackDevin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: JackDevinFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: JackDevinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: JackDevinAppBufferMode;

        app_buffer: any;

        sub_block_count: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<JackDevin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): JackDevin;

        // Signals
        /** @signal */
        connect_after<K extends keyof JackDevin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JackDevin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof JackDevin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<JackDevin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `jack_devin`.
         * @param flags see {@link AgsAudio.JackDevinFlags}-enum
         */
        set_flags(flags: JackDevinFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `jack_devin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: JackDevinFlags): boolean;

        /**
         * Disable a feature of `jack_devin`.
         * @param flags see {@link AgsAudio.JackDevinFlags}-enum
         */
        unset_flags(flags: JackDevinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace JackDevout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::jack-client": (pspec: GObject.ParamSpec) => void;
            "notify::jack-port": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            jack_client: JackClient;
            jackClient: JackClient;
            jack_port: any;
            jackPort: any;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class JackDevout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<JackDevout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The jack soundcard indentifier
         * @since 3.0.0
         * @default ags-jack-devout-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned {@link AgsAudio.JackClient}
         * @since 3.0.0
         */
        get jack_client(): JackClient;
        set jack_client(val: JackClient);

        /**
         * The assigned {@link AgsAudio.JackClient}
         * @since 3.0.0
         */
        get jackClient(): JackClient;
        set jackClient(val: JackClient);

        /**
         * The assigned {@link AgsAudio.JackPort}
         * @since 3.0.0
         */
        get jack_port(): any;
        set jack_port(val: any);

        /**
         * The assigned {@link AgsAudio.JackPort}
         * @since 3.0.0
         */
        get jackPort(): any;
        set jackPort(val: any);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: JackDevout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: JackDevoutFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: JackDevoutSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: JackDevoutAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<JackDevout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): JackDevout;

        // Signals
        /** @signal */
        connect_after<K extends keyof JackDevout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JackDevout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof JackDevout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<JackDevout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `jack_devout`.
         * @param flags see {@link AgsAudio.JackDevoutFlags}-enum
         */
        set_flags(flags: JackDevoutFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `jack_devout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: JackDevoutFlags): boolean;

        /**
         * Disable a feature of `jack_devout`.
         * @param flags see {@link AgsAudio.JackDevoutFlags}-enum
         */
        unset_flags(flags: JackDevoutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace JackMidiin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::jack-client": (pspec: GObject.ParamSpec) => void;
            "notify::jack-port": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Sequencer.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            delay_factor: number;
            delayFactor: number;
            device: string;
            jack_client: JackClient;
            jackClient: JackClient;
            jack_port: JackPort;
            jackPort: JackPort;
        }
    }

    /**
     * @gir-type Class
     */
    class JackMidiin extends GObject.Object implements Ags.Connectable, Ags.Sequencer {
        static $gtype: GObject.GType<JackMidiin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The JACK sequencer indentifier
         * @since 3.0.0
         * @default null
         */
        get device(): string;
        set device(val: string);

        /**
         * The assigned {@link AgsAudio.JackClient}
         * @since 3.0.0
         */
        get jack_client(): JackClient;
        set jack_client(val: JackClient);

        /**
         * The assigned {@link AgsAudio.JackClient}
         * @since 3.0.0
         */
        get jackClient(): JackClient;
        set jackClient(val: JackClient);

        /**
         * The assigned {@link AgsAudio.JackPort}
         * @since 3.0.0
         */
        get jack_port(): JackPort;
        set jack_port(val: JackPort);

        /**
         * The assigned {@link AgsAudio.JackPort}
         * @since 3.0.0
         */
        get jackPort(): JackPort;
        set jackPort(val: JackPort);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: JackMidiin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: JackMidiinFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: JackMidiinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: JackMidiinAppBufferMode;

        app_buffer: string;

        app_buffer_size: number[];

        delay: number;

        latency: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        card_uri: string;

        port_name: string;

        // Constructors
        constructor(properties?: Partial<JackMidiin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): JackMidiin;

        // Signals
        /** @signal */
        connect_after<K extends keyof JackMidiin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JackMidiin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof JackMidiin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<JackMidiin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Enable a feature of `jack_midiin`.
         * @param flags see {@link AgsAudio.JackMidiinFlags}-enum
         */
        set_flags(flags: JackMidiinFlags): void;

        /**
         * The buffer flag indicates the currently recorded buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `jack_midiin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: JackMidiinFlags): boolean;

        /**
         * Disable a feature of `jack_midiin`.
         * @param flags see {@link AgsAudio.JackMidiinFlags}-enum
         */
        unset_flags(flags: JackMidiinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @param buffer_length the buffer's length
         * @returns current playback buffer
         */
        get_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get future playback buffer.
         * @param buffer_length the buffer's length
         * @returns next playback buffer
         */
        get_next_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset(): number;

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Plays the current buffer of sequencer.
         */
        play(): void;

        /**
         * Initializes the sequencer for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of sequencer.
         */
        record(): void;

        /**
         * Initializes the sequencer for recording.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the sequencer from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @param buffer_length the buffer's length
         * @virtual
         */
        vfunc_get_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get future playback buffer.
         * @param buffer_length the buffer's length
         * @virtual
         */
        vfunc_get_next_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Plays the current buffer of sequencer.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the sequencer for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of sequencer.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the sequencer for recording.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the sequencer from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;
    }


    namespace JackPort {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::jack-client": (pspec: GObject.ParamSpec) => void;
            "notify::port-name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            jack_client: JackClient;
            jackClient: JackClient;
            port_name: string;
            portName: string;
        }
    }

    /**
     * @gir-type Class
     */
    class JackPort extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<JackPort>;

        // Properties
        /**
         * The assigned {@link AgsAudio.JackClient}.
         * @since 3.0.0
         */
        get jack_client(): JackClient;
        set jack_client(val: JackClient);

        /**
         * The assigned {@link AgsAudio.JackClient}.
         * @since 3.0.0
         */
        get jackClient(): JackClient;
        set jackClient(val: JackClient);

        /**
         * The JACK port name.
         * @since 3.0.0
         * @default null
         */
        get port_name(): string;
        set port_name(val: string);

        /**
         * The JACK port name.
         * @since 3.0.0
         * @default null
         */
        get portName(): string;
        set portName(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: JackPort.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: JackPortFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        port_uuid: string;

        // Constructors
        constructor(properties?: Partial<JackPort.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](jack_client: GObject.Object): JackPort;

        // Signals
        /** @signal */
        connect_after<K extends keyof JackPort.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JackPort.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof JackPort.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<JackPort.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `port_name` in `jack_port`.
         * @param jack_port the {@link GLib.List}-struct containing {@link AgsAudio.JackPort}
         * @param port_name the port name to find
         */
        static find(jack_port: JackPort[], port_name: string): JackPort[];

        // Methods
        /**
         * Register a new JACK port and read uuid. Creates a new AgsSequencer or AgsSoundcard
         * object.
         * @param port_name the name as string
         * @param is_audio if `true` interpreted as audio port
         * @param is_midi if `true` interpreted as midi port
         * @param is_output if `true` port is acting as output, otherwise as input
         */
        register(port_name: string, is_audio: boolean, is_midi: boolean, is_output: boolean): void;

        /**
         * Enable a feature of `jack_port`.
         * @param flags see {@link AgsAudio.JackPortFlags}-enum
         */
        set_flags(flags: JackPortFlags): void;

        /**
         * Test `flags` to be set on `jack_port`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: JackPortFlags): boolean;

        /**
         * Unregister JACK port.
         */
        unregister(): void;

        /**
         * Disable a feature of `jack_port`.
         * @param flags see {@link AgsAudio.JackPortFlags}-enum
         */
        unset_flags(flags: JackPortFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace JackServer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::default-jack-client": (pspec: GObject.ParamSpec) => void;
            "notify::default-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-jack-client": (pspec: GObject.ParamSpec) => void;
            "notify::jack-client": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.SoundServer.ConstructorProps {
            default_jack_client: JackClient;
            defaultJackClient: JackClient;
            default_soundcard: GObject.Object;
            defaultSoundcard: GObject.Object;
            input_jack_client: JackClient;
            inputJackClient: JackClient;
            jack_client: JackClient[];
            jackClient: JackClient[];
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class JackServer extends GObject.Object implements Ags.Connectable, Ags.SoundServer {
        static $gtype: GObject.GType<JackServer>;

        // Properties
        /**
         * The default jack client.
         * @since 3.0.0
         */
        get default_jack_client(): JackClient;
        set default_jack_client(val: JackClient);

        /**
         * The default jack client.
         * @since 3.0.0
         */
        get defaultJackClient(): JackClient;
        set defaultJackClient(val: JackClient);

        /**
         * The default soundcard.
         * @since 3.0.0
         */
        get default_soundcard(): GObject.Object;
        set default_soundcard(val: GObject.Object);

        /**
         * The default soundcard.
         * @since 3.0.0
         */
        get defaultSoundcard(): GObject.Object;
        set defaultSoundcard(val: GObject.Object);

        /**
         * The input jack client.
         * @since 3.0.0
         */
        get input_jack_client(): JackClient;
        set input_jack_client(val: JackClient);

        /**
         * The input jack client.
         * @since 3.0.0
         */
        get inputJackClient(): JackClient;
        set inputJackClient(val: JackClient);

        /**
         * The jack client list.
         * @since 3.0.0
         */
        get jack_client(): JackClient[];
        set jack_client(val: JackClient[]);

        /**
         * The jack client list.
         * @since 3.0.0
         */
        get jackClient(): JackClient[];
        set jackClient(val: JackClient[]);

        /**
         * The assigned URL.
         * @since 3.0.0
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: JackServer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        port: number;

        port_count: number;

        n_soundcards: number;

        n_sequencers: number;

        default_client: GObject.Object;

        input_client: GObject.Object;

        client: any[];

        // Constructors
        constructor(properties?: Partial<JackServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](url: string): JackServer;

        // Signals
        /** @signal */
        connect_after<K extends keyof JackServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, JackServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof JackServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<JackServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find {@link AgsAudio.JackServer} by url.
         * @param jack_server the {@link GLib.List}-struct containing {@link AgsAudio.JackServer}
         * @param url the url to find
         */
        static find_url(jack_server: JackServer[], url: string): JackServer[];

        // Methods
        /**
         * Add `jack_client` to `jack_server`
         * @param jack_client the {@link AgsAudio.JackClient} to add
         */
        add_client(jack_client: GObject.Object): void;

        /**
         * Connect all clients.
         */
        connect_client(): void;

        /**
         * Connect all clients.
         */
        disconnect_client(): void;

        /**
         * Find {@link AgsAudio.JackClient} by uuid.
         * @param client_uuid the uuid to find
         * @returns the {@link AgsAudio.JackClient} found or `null`
         */
        find_client<T = GObject.Object>(client_uuid: string): T;

        /**
         * Find {@link AgsAudio.JackPort} by uuid.
         * @param port_uuid the uuid to find
         * @returns the {@link AgsAudio.JackPort} found or `null`
         */
        find_port<T = GObject.Object>(port_uuid: string): T;

        /**
         * Remove `jack_client` to `jack_server`
         * @param jack_client the {@link AgsAudio.JackClient} to remove
         */
        remove_client(jack_client: GObject.Object): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @returns the port as string `null`-terminated array
         */
        get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the sequencer as {@link GLib.List}-struct
         */
        get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the soundcard as {@link GLib.List}-struct
         */
        get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @returns the URL as string
         */
        get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Sequencer}
         */
        register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Soundcard}
         */
        register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param port 
         * @param port_count 
         */
        set_ports(port: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         */
        set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         */
        set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         */
        set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         */
        unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         */
        unregister_soundcard(soundcard: GObject.Object): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @virtual
         */
        vfunc_get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @virtual
         */
        vfunc_get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param ports 
         * @param port_count 
         * @virtual
         */
        vfunc_set_ports(ports: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         * @virtual
         */
        vfunc_set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         * @virtual
         */
        vfunc_set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         * @virtual
         */
        vfunc_set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         * @virtual
         */
        vfunc_unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         * @virtual
         */
        vfunc_unregister_soundcard(soundcard: GObject.Object): void;
    }


    namespace LadspaConversion {
        // Signal signatures
        interface SignalSignatures extends Ags.Conversion.SignalSignatures {
            "notify::lower": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::step-count": (pspec: GObject.ParamSpec) => void;
            "notify::upper": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Conversion.ConstructorProps {
            lower: number;
            samplerate: number;
            step_count: number;
            stepCount: number;
            upper: number;
        }
    }

    /**
     * @gir-type Class
     */
    class LadspaConversion extends Ags.Conversion {
        static $gtype: GObject.GType<LadspaConversion>;

        // Properties
        /**
         * The lower to be used.
         * @since 3.0.0
         * @default 0
         */
        get lower(): number;
        set lower(val: number);

        /**
         * The samplerate to be used.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The step count to be used.
         * @since 3.0.0
         * @default 0
         */
        get step_count(): number;
        set step_count(val: number);

        /**
         * The step count to be used.
         * @since 3.0.0
         * @default 0
         */
        get stepCount(): number;
        set stepCount(val: number);

        /**
         * The upper to be used.
         * @since 3.0.0
         * @default 0
         */
        get upper(): number;
        set upper(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LadspaConversion.SignalSignatures;

        // Fields
        conversion: Ags.Conversion;

        flags: number;

        // Constructors
        constructor(properties?: Partial<LadspaConversion.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): LadspaConversion;

        // Signals
        /** @signal */
        connect<K extends keyof LadspaConversion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LadspaConversion.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LadspaConversion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LadspaConversion.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LadspaConversion.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LadspaConversion.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: LadspaConversionFlags): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: LadspaConversionFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: LadspaConversionFlags): void;
    }


    namespace LadspaManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class LadspaManager extends GObject.Object {
        static $gtype: GObject.GType<LadspaManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LadspaManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        ladspa_plugin_blacklist: any[];

        ladspa_plugin: any[];

        // Constructors
        constructor(properties?: Partial<LadspaManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): LadspaManager;

        // Signals
        /** @signal */
        connect<K extends keyof LadspaManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LadspaManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LadspaManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LadspaManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LadspaManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LadspaManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get ladspa manager default plugin path.
         */
        static get_default_path(): string[];

        /**
         * Get instance.
         */
        static get_instance(): LadspaManager;

        /**
         * Set ladspa manager default plugin path.
         * @param default_path the string vector array to use as default path
         */
        static set_default_path(default_path: string[]): void;

        // Methods
        /**
         * Lookup filename in loaded plugins.
         * @param filename the filename of the plugin
         * @param effect the effect's name
         * @returns the {@link AgsAudio.LadspaPlugin}
         */
        find_ladspa_plugin(filename: string, effect: string): LadspaPlugin;

        /**
         * Lookup filename in loaded plugins.
         * @param filename the filename of the plugin
         * @param effect the effect's name
         * @returns the {@link AgsAudio.LadspaPlugin}
         */
        find_ladspa_plugin_with_fallback(filename: string, effect: string): LadspaPlugin;

        /**
         * Retrieve all filenames
         * @returns a `null`-terminated array of filenames
         */
        get_filenames(): string[];

        /**
         * Load blacklisted plugin filenames.
         * @param blacklist_filename the filename as string
         */
        load_blacklist(blacklist_filename: string): void;

        /**
         * Loads all available plugins.
         */
        load_default_directory(): void;

        /**
         * Load `filename` specified plugin.
         * @param ladspa_path the LADSPA path
         * @param filename the filename of the plugin
         */
        load_file(ladspa_path: string, filename: string): void;
    }


    namespace LadspaPlugin {
        // Signal signatures
        interface SignalSignatures extends BasePlugin.SignalSignatures {
            "notify::unique-id": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-port": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::ui-filename": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::uuid": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends BasePlugin.ConstructorProps {
            unique_id: number;
            uniqueId: number;
        }
    }

    /**
     * @gir-type Class
     */
    class LadspaPlugin extends BasePlugin {
        static $gtype: GObject.GType<LadspaPlugin>;

        // Properties
        /**
         * The assigned unique-id.
         * @since 3.0.0
         * @default 0
         */
        get unique_id(): number;
        set unique_id(val: number);

        /**
         * The assigned unique-id.
         * @since 3.0.0
         * @default 0
         */
        get uniqueId(): number;
        set uniqueId(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LadspaPlugin.SignalSignatures;

        // Fields
        base_plugin: BasePlugin;

        // Constructors
        constructor(properties?: Partial<LadspaPlugin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](filename: string, effect: string, effect_index: number): LadspaPlugin;

        // Signals
        /** @signal */
        connect<K extends keyof LadspaPlugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LadspaPlugin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LadspaPlugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LadspaPlugin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LadspaPlugin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LadspaPlugin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace LinkChannel {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::error": (pspec: GObject.ParamSpec) => void;
            "notify::link": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            channel: Channel;
            error: any;
            link: Channel;
        }
    }

    /**
     * @gir-type Class
     */
    class LinkChannel extends Ags.Task {
        static $gtype: GObject.GType<LinkChannel>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.0.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * The assigned {@link GLib.Error}-struct
         * @since 3.0.0
         */
        get error(): any;
        set error(val: any);

        /**
         * The assigned {@link AgsAudio.Channel} link
         * @since 3.0.0
         */
        get link(): Channel;
        set link(val: Channel);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: LinkChannel.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<LinkChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel, link: Channel): LinkChannel;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof LinkChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LinkChannel.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof LinkChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, LinkChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof LinkChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<LinkChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace Lv2Conversion {
        // Signal signatures
        interface SignalSignatures extends Ags.Conversion.SignalSignatures {
            "notify::lower": (pspec: GObject.ParamSpec) => void;
            "notify::step-count": (pspec: GObject.ParamSpec) => void;
            "notify::upper": (pspec: GObject.ParamSpec) => void;
            "notify::description": (pspec: GObject.ParamSpec) => void;
            "notify::name": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Conversion.ConstructorProps {
            lower: number;
            step_count: number;
            stepCount: number;
            upper: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Lv2Conversion extends Ags.Conversion {
        static $gtype: GObject.GType<Lv2Conversion>;

        // Properties
        /**
         * The lower to be used.
         * @since 3.0.0
         * @default 0.00001
         */
        get lower(): number;
        set lower(val: number);

        /**
         * The step count to be used.
         * @since 3.0.0
         * @default 1025
         */
        get step_count(): number;
        set step_count(val: number);

        /**
         * The step count to be used.
         * @since 3.0.0
         * @default 1025
         */
        get stepCount(): number;
        set stepCount(val: number);

        /**
         * The upper to be used.
         * @since 3.0.0
         * @default 1
         */
        get upper(): number;
        set upper(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2Conversion.SignalSignatures;

        // Fields
        conversion: Ags.Conversion;

        flags: number;

        // Constructors
        constructor(properties?: Partial<Lv2Conversion.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2Conversion;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2Conversion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Conversion.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2Conversion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Conversion.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2Conversion.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2Conversion.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: Lv2ConversionFlags): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: Lv2ConversionFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: Lv2ConversionFlags): void;
    }


    namespace Lv2EventManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2EventManager extends GObject.Object {
        static $gtype: GObject.GType<Lv2EventManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2EventManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<Lv2EventManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2EventManager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2EventManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2EventManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2EventManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2EventManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2EventManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2EventManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Singleton function to optain the id manager instance.
         */
        static get_instance(): Lv2EventManager;
    }


    namespace Lv2LogManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2LogManager extends GObject.Object {
        static $gtype: GObject.GType<Lv2LogManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2LogManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<Lv2LogManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2LogManager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2LogManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2LogManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2LogManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2LogManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2LogManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2LogManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Singleton function to optain the id manager instance.
         */
        static get_instance(): Lv2LogManager;
    }


    namespace Lv2Manager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::locale": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            locale: string;
        }
    }

    /**
     * @gir-type Class
     */
    class Lv2Manager extends GObject.Object {
        static $gtype: GObject.GType<Lv2Manager>;

        // Properties
        /**
         * The assigned locale.
         * @since 3.0.0
         * @default null
         */
        get locale(): string;
        set locale(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2Manager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        lv2_plugin_blacklist: any[];

        lv2_plugin: any[];

        quick_scan_plugin_filename: string;

        quick_scan_plugin_effect: string;

        quick_scan_instrument_filename: string;

        quick_scan_instrument_effect: string;

        // Constructors
        constructor(properties?: Partial<Lv2Manager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](locale: string): Lv2Manager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2Manager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Manager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2Manager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Manager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2Manager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2Manager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get lv2 manager default plugin path.
         */
        static get_default_path(): string[];

        /**
         * Get instance.
         */
        static get_instance(): Lv2Manager;

        /**
         * Get global config value parse names.
         */
        static global_get_parse_names(): boolean;

        /**
         * Get global config value preserve turtle.
         */
        static global_get_preserve_turtle(): boolean;

        /**
         * Set lv2 manager default plugin path.
         * @param default_path the string vector array to use as default path
         */
        static set_default_path(default_path: string[]): void;

        // Methods
        /**
         * Lookup filename in loaded plugins.
         * @param filename the filename of the plugin
         * @param effect the effect's name
         * @returns the {@link AgsAudio.Lv2Plugin}
         */
        find_lv2_plugin(filename: string, effect: string): Lv2Plugin;

        /**
         * Lookup filename in loaded plugins.
         * @param filename the filename of the plugin
         * @param effect the effect's name
         * @returns the {@link AgsAudio.Lv2Plugin}
         */
        find_lv2_plugin_with_fallback(filename: string, effect: string): Lv2Plugin;

        /**
         * Retrieve all filenames
         * @returns a `null`-terminated array of filenames
         */
        get_filenames(): string[];

        /**
         * Load blacklisted plugin filenames.
         * @param blacklist_filename the filename as string
         */
        load_blacklist(blacklist_filename: string): void;

        /**
         * Loads all available plugins.
         */
        load_default_directory(): void;

        /**
         * Load `filename` specified plugin.
         * @param manifest the manifest
         * @param turtle the loaded turtle
         * @param lv2_path the lv2 path
         * @param filename the filename of the plugin
         */
        load_file(manifest: Ags.Turtle, turtle: Ags.Turtle, lv2_path: string, filename: string): void;

        /**
         * Load preset.
         * @param lv2_plugin the {@link AgsAudio.Lv2Plugin}
         * @param preset the {@link Ags.Turtle}
         */
        load_preset(lv2_plugin: Lv2Plugin, preset: Ags.Turtle): void;

        /**
         * Quick scan available plugins.
         */
        quick_scan_default_directory(): void;
    }


    namespace Lv2OptionManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::get-option signal gets options of the manager.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "get-option": (arg0: (any | null), arg1: (any | null), arg2: (any | null)) => void;
            /**
             * The ::set-option signal sets options for the manager.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "set-option": (arg0: (any | null), arg1: (any | null), arg2: (any | null)) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2OptionManager extends GObject.Object {
        static $gtype: GObject.GType<Lv2OptionManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2OptionManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<Lv2OptionManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2OptionManager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2OptionManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2OptionManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2OptionManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2OptionManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2OptionManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2OptionManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Singleton function to optain the id manager instance.
         */
        static get_instance(): Lv2OptionManager;

        // Virtual methods
        /**
         * Get option.
         * @param instance the instance
         * @param option the option
         * @param retval return value for #LV2_Options_Status-enum
         * @virtual
         */
        vfunc_get_option(instance: (any | null), option: (any | null), retval: (any | null)): void;

        /**
         * Set option.
         * @param instance the instance
         * @param option the option
         * @param retval return value for #LV2_Options_Status-enum
         * @virtual
         */
        vfunc_set_option(instance: (any | null), option: (any | null), retval: (any | null)): void;

        // Methods
        /**
         * Get option.
         * @param instance the instance
         * @param option the option
         * @param retval return value for #LV2_Options_Status-enum
         */
        get_option(instance: (any | null), option: (any | null), retval: (any | null)): void;

        /**
         * Inserts a data into hash associated with `lv2_option_ressource`.
         * @param lv2_option_ressource the {@link AgsAudio.Lv2OptionRessource}-struct as key
         * @param data the data
         * @returns `true` on success, otherwise `false`
         */
        ressource_insert(lv2_option_ressource: (any | null), data: (any | null)): boolean;

        /**
         * Lookup a ressource associated with `lv2_option_ressource` in
         * `lv2_option_manager`.
         * @param lv2_option_ressource the {@link AgsAudio.Lv2OptionRessource} to lookup
         * @returns the pointer on success, else NULL
         */
        ressource_lookup(lv2_option_ressource: (any | null)): (any | null);

        /**
         * Lookup a ressource associated with `lv2_option_ressource` in
         * `lv2_option_manager`.
         * @param lv2_option_ressource the {@link AgsAudio.Lv2OptionRessource} to lookup
         * @param orig_key the original key found
         * @param value the matched value
         * @returns `true` if ressource found, else `false`
         */
        ressource_lookup_extended(lv2_option_ressource: Lv2OptionRessource, orig_key: (any | null), value: (any | null)): boolean;

        /**
         * Removes an entry associated with `lv2_option_ressource`.
         * @param lv2_option_ressource the struct to remove
         * @returns `true` as successfully removed, otherwise `false`
         */
        ressource_remove(lv2_option_ressource: (any | null)): boolean;

        /**
         * Set option.
         * @param instance the instance
         * @param option the option
         * @param retval return value for #LV2_Options_Status-enum
         */
        set_option(instance: (any | null), option: (any | null), retval: (any | null)): void;
    }


    namespace Lv2Plugin {
        // Signal signatures
        interface SignalSignatures extends BasePlugin.SignalSignatures {
            /**
             * The ::change-program signal creates a new instance of plugin.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "change-program": (arg0: (any | null), arg1: number, arg2: number) => void;
            "notify::doap-name": (pspec: GObject.ParamSpec) => void;
            "notify::foaf-homepage": (pspec: GObject.ParamSpec) => void;
            "notify::foaf-mbox": (pspec: GObject.ParamSpec) => void;
            "notify::foaf-name": (pspec: GObject.ParamSpec) => void;
            "notify::manifest": (pspec: GObject.ParamSpec) => void;
            "notify::pname": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
            "notify::turtle": (pspec: GObject.ParamSpec) => void;
            "notify::ui-uri": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-port": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::ui-filename": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::uuid": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends BasePlugin.ConstructorProps {
            doap_name: string;
            doapName: string;
            foaf_homepage: string;
            foafHomepage: string;
            foaf_mbox: string;
            foafMbox: string;
            foaf_name: string;
            foafName: string;
            manifest: Ags.Turtle;
            pname: string;
            preset: Lv2Preset[];
            turtle: Ags.Turtle;
            ui_uri: string;
            uiUri: string;
            uri: string;
        }
    }

    /**
     * @gir-type Class
     */
    class Lv2Plugin extends BasePlugin {
        static $gtype: GObject.GType<Lv2Plugin>;

        // Properties
        /**
         * The assigned doap name.
         * @since 3.0.0
         * @default null
         */
        get doap_name(): string;
        set doap_name(val: string);

        /**
         * The assigned doap name.
         * @since 3.0.0
         * @default null
         */
        get doapName(): string;
        set doapName(val: string);

        /**
         * The assigned foaf homepage.
         * @since 3.0.0
         * @default null
         */
        get foaf_homepage(): string;
        set foaf_homepage(val: string);

        /**
         * The assigned foaf homepage.
         * @since 3.0.0
         * @default null
         */
        get foafHomepage(): string;
        set foafHomepage(val: string);

        /**
         * The assigned foaf mbox.
         * @since 3.0.0
         * @default null
         */
        get foaf_mbox(): string;
        set foaf_mbox(val: string);

        /**
         * The assigned foaf mbox.
         * @since 3.0.0
         * @default null
         */
        get foafMbox(): string;
        set foafMbox(val: string);

        /**
         * The assigned foaf name.
         * @since 3.0.0
         * @default null
         */
        get foaf_name(): string;
        set foaf_name(val: string);

        /**
         * The assigned foaf name.
         * @since 3.0.0
         * @default null
         */
        get foafName(): string;
        set foafName(val: string);

        /**
         * The assigned manifest.
         * @since 3.0.0
         */
        get manifest(): Ags.Turtle;
        set manifest(val: Ags.Turtle);

        /**
         * The assigned pname.
         * @since 3.0.0
         * @default null
         */
        get pname(): string;
        set pname(val: string);

        /**
         * The assigned preset.
         * @since 3.0.0
         */
        get preset(): Lv2Preset[];
        set preset(val: Lv2Preset[]);

        /**
         * The assigned turtle.
         * @since 3.0.0
         */
        get turtle(): Ags.Turtle;
        set turtle(val: Ags.Turtle);

        /**
         * The assigned ui-uri.
         * @since 3.0.0
         * @default null
         */
        get ui_uri(): string;
        set ui_uri(val: string);

        /**
         * The assigned ui-uri.
         * @since 3.0.0
         * @default null
         */
        get uiUri(): string;
        set uiUri(val: string);

        /**
         * The assigned uri.
         * @since 3.0.0
         * @default null
         */
        get uri(): string;
        set uri(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2Plugin.SignalSignatures;

        // Fields
        base_plugin: BasePlugin;

        flags: number;

        program: string;

        // Constructors
        constructor(properties?: Partial<Lv2Plugin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](turtle: Ags.Turtle, filename: string, effect: string, uri: string, effect_index: number): Lv2Plugin;

        // Conflicted with AgsAudio.BasePlugin.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2Plugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Plugin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2Plugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Plugin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2Plugin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2Plugin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Allocates a LV2_Atom_Sequence
         * @param sequence_size the requested size
         */
        static alloc_atom_sequence(sequence_size: number): (any | null);

        /**
         * Append MIDI data to atom sequence.
         * @param atom_sequence the atom sequence
         * @param sequence_size the atom sequence size
         * @param events the events to write
         * @param event_count the number of events to write
         */
        static atom_sequence_append_midi(atom_sequence: (any | null), sequence_size: number, events: (any | null), event_count: number): boolean;

        /**
         * Free atom sequence.
         * @param atom_sequence the atom sequence
         */
        static atom_sequence_free(atom_sequence: (any | null)): void;

        /**
         * Remove MIDI data from atom sequence.
         * @param atom_sequence the atom sequence
         * @param sequence_size the atom sequence size
         * @param note the key to remove
         */
        static atom_sequence_remove_midi(atom_sequence: (any | null), sequence_size: number, note: number): boolean;

        /**
         * Clear the atom sequence.
         * @param atom_sequence the atom sequence
         * @param sequence_size size of `atom_sequence`
         */
        static clear_atom_sequence(atom_sequence: (any | null), sequence_size: number): void;

        /**
         * Clear the event buffer.
         * @param event_buffer the event buffer
         * @param buffer_size size of `event_buffer`
         */
        static clear_event_buffer(event_buffer: (any | null), buffer_size: number): void;

        /**
         * Allocate LV2_Event_Buffer struct.
         * @param buffer_size the data's buffer size
         */
        static event_buffer_alloc(buffer_size: number): (any | null);

        /**
         * Append MIDI data to event buffer.
         * @param event_buffer the event buffer
         * @param buffer_size the event buffer size
         * @param events the events to write
         * @param event_count the number of events to write
         */
        static event_buffer_append_midi(event_buffer: (any | null), buffer_size: number, events: (any | null), event_count: number): boolean;

        /**
         * Free LV2_Event_Buffer struct.
         * @param event_buffer the LV2_Event_Buffer struct
         */
        static event_buffer_free(event_buffer: (any | null)): void;

        /**
         * Reallocate LV2_Event_Buffer struct's data field.
         * @param event_buffer the LV2_Event_Buffer struct
         * @param buffer_size the data's buffer size
         */
        static event_buffer_realloc_data(event_buffer: (any | null), buffer_size: number): void;

        /**
         * Remove MIDI data from event buffer.
         * @param event_buffer the event buffer
         * @param buffer_size the event buffer size
         * @param note the key to remove
         */
        static event_buffer_remove_midi(event_buffer: (any | null), buffer_size: number, note: number): boolean;

        /**
         * Find pname in `lv2_plugin` {@link GLib.List}-struct
         * @param lv2_plugin a {@link GLib.List}-struct containig {@link AgsAudio.Lv2Plugin}
         * @param pname the pname to find
         */
        static find_pname(lv2_plugin: Lv2Plugin[], pname: string): Lv2Plugin[];

        /**
         * Find uri in `lv2_plugin` {@link GLib.List}-struct
         * @param lv2_plugin a {@link GLib.List}-struct containig {@link AgsAudio.Lv2Plugin}
         * @param uri the uri to find
         */
        static find_uri(lv2_plugin: Lv2Plugin[], uri: string): Lv2Plugin[];

        // Virtual methods
        /**
         * Change program of `lv2_handle`.
         * @param lv2_handle the lv2 handle
         * @param bank_index the bank index
         * @param program_index the program index
         * @virtual
         */
        vfunc_change_program(lv2_handle: (any | null), bank_index: number, program_index: number): void;

        // Methods
        /**
         * Change program of `lv2_handle`.
         * @param lv2_handle the lv2 handle
         * @param bank_index the bank index
         * @param program_index the program index
         */
        change_program(lv2_handle: (any | null), bank_index: number, program_index: number): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: Lv2PluginFlags): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.BasePlugin.set_flags
        set_flags(...args: never[]): any;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: Lv2PluginFlags): boolean;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.BasePlugin.test_flags
        test_flags(...args: never[]): any;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: Lv2PluginFlags): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.BasePlugin.unset_flags
        unset_flags(...args: never[]): any;
    }


    namespace Lv2Preset {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::applies-to": (pspec: GObject.ParamSpec) => void;
            "notify::bank": (pspec: GObject.ParamSpec) => void;
            "notify::lv2-plugin": (pspec: GObject.ParamSpec) => void;
            "notify::manifest": (pspec: GObject.ParamSpec) => void;
            "notify::port-preset": (pspec: GObject.ParamSpec) => void;
            "notify::preset-label": (pspec: GObject.ParamSpec) => void;
            "notify::turtle": (pspec: GObject.ParamSpec) => void;
            "notify::uri": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            applies_to: string;
            appliesTo: string;
            bank: string;
            lv2_plugin: Lv2Plugin;
            lv2Plugin: Lv2Plugin;
            manifest: Ags.Turtle;
            port_preset: any;
            portPreset: any;
            preset_label: string;
            presetLabel: string;
            turtle: Ags.Turtle;
            uri: string;
        }
    }

    /**
     * @gir-type Class
     */
    class Lv2Preset extends GObject.Object {
        static $gtype: GObject.GType<Lv2Preset>;

        // Properties
        /**
         * The assigned applies to.
         * @since 3.0.0
         * @default null
         */
        get applies_to(): string;
        set applies_to(val: string);

        /**
         * The assigned applies to.
         * @since 3.0.0
         * @default null
         */
        get appliesTo(): string;
        set appliesTo(val: string);

        /**
         * The assigned bank.
         * @since 3.0.0
         * @default null
         */
        get bank(): string;
        set bank(val: string);

        /**
         * The assigned lv2 plugin.
         * @since 3.0.0
         */
        get lv2_plugin(): Lv2Plugin;
        set lv2_plugin(val: Lv2Plugin);

        /**
         * The assigned lv2 plugin.
         * @since 3.0.0
         */
        get lv2Plugin(): Lv2Plugin;
        set lv2Plugin(val: Lv2Plugin);

        /**
         * The assigned manifest.
         * @since 3.0.0
         */
        get manifest(): Ags.Turtle;
        set manifest(val: Ags.Turtle);

        /**
         * The port preset.
         * @since 3.0.0
         * @read-only
         */
        get port_preset(): any;

        /**
         * The port preset.
         * @since 3.0.0
         * @read-only
         */
        get portPreset(): any;

        /**
         * The preset label.
         * @since 3.0.0
         * @default null
         */
        get preset_label(): string;
        set preset_label(val: string);

        /**
         * The preset label.
         * @since 3.0.0
         * @default null
         */
        get presetLabel(): string;
        set presetLabel(val: string);

        /**
         * The assigned turtle.
         * @since 3.0.0
         */
        get turtle(): Ags.Turtle;
        set turtle(val: Ags.Turtle);

        /**
         * The assigned uri.
         * @since 3.0.0
         * @default null
         */
        get uri(): string;
        set uri(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2Preset.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        // Constructors
        constructor(properties?: Partial<Lv2Preset.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](lv2_plugin: GObject.Object, turtle: Ags.Turtle, uri: string): Lv2Preset;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2Preset.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Preset.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2Preset.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Preset.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2Preset.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2Preset.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find `preset_label` within `lv2_preset`.
         * @param lv2_preset the {@link GLib.List}-struct containing {@link AgsAudio.Lv2Preset}
         * @param preset_label the preset label
         */
        static find_preset_label(lv2_preset: Lv2Preset[], preset_label: string): Lv2Preset[];

        /**
         * Find `preset_uri` within `lv2_preset`.
         * @param lv2_preset the {@link GLib.List}-struct containing {@link AgsAudio.Lv2Preset}
         * @param preset_uri the preset URI
         */
        static find_preset_uri(lv2_preset: Lv2Preset[], preset_uri: string): Lv2Preset[];

        // Methods
        /**
         * Parse `lv2_preset`.
         */
        parse_turtle(): void;
    }


    namespace Lv2PresetManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2PresetManager extends GObject.Object {
        static $gtype: GObject.GType<Lv2PresetManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2PresetManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        lv2_preset: any[];

        // Constructors
        constructor(properties?: Partial<Lv2PresetManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2PresetManager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2PresetManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2PresetManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2PresetManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2PresetManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2PresetManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2PresetManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get instance.
         */
        static get_instance(): Lv2PresetManager;
    }


    namespace Lv2TurtleParser {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::plugin": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
            "notify::turtle": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            plugin: Lv2Plugin[];
            preset: Lv2Preset[];
            turtle: Ags.Turtle[];
            ui_plugin: any;
            uiPlugin: any;
        }
    }

    /**
     * @gir-type Class
     */
    class Lv2TurtleParser extends GObject.Object {
        static $gtype: GObject.GType<Lv2TurtleParser>;

        // Properties
        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.Lv2Plugin}.
         * @since 3.0.0
         */
        get plugin(): Lv2Plugin[];
        set plugin(val: Lv2Plugin[]);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.Lv2Preset}.
         * @since 3.0.0
         */
        get preset(): Lv2Preset[];
        set preset(val: Lv2Preset[]);

        /**
         * The assigned {@link GLib.List}-struct containing {@link Ags.Turtle}.
         * @since 3.0.0
         */
        get turtle(): Ags.Turtle[];
        set turtle(val: Ags.Turtle[]);

        get ui_plugin(): any;
        set ui_plugin(val: any);

        get uiPlugin(): any;
        set uiPlugin(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2TurtleParser.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        // Constructors
        constructor(properties?: Partial<Lv2TurtleParser.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](manifest: Ags.Turtle): Lv2TurtleParser;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2TurtleParser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2TurtleParser.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2TurtleParser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2TurtleParser.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2TurtleParser.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2TurtleParser.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Parse manifest and referred turtles.
         * @param turtle the `null` terminated array of {@link Ags.Turtle}
         * @param n_turtle the turtle count
         */
        parse(turtle: Ags.Turtle[], n_turtle: number): Ags.Turtle[];

        /**
         * Parse names only from manifest and referred turtles.
         * @param turtle the `null` terminated array of {@link Ags.Turtle}
         * @param n_turtle the turtle count
         */
        parse_names(turtle: Ags.Turtle, n_turtle: number): void;
    }


    namespace Lv2TurtleScanner {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2TurtleScanner extends GObject.Object {
        static $gtype: GObject.GType<Lv2TurtleScanner>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2TurtleScanner.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        status_flags: number;

        cache_turtle: any[];

        current_subject_iriref: string;

        plugin_position: number;

        // Constructors
        constructor(properties?: Partial<Lv2TurtleScanner.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2TurtleScanner;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2TurtleScanner.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2TurtleScanner.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2TurtleScanner.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2TurtleScanner.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2TurtleScanner.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2TurtleScanner.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Quick scan to detect available plugins.
         * @param manifest_filename the manifest filename
         */
        quick_scan(manifest_filename: string): void;

        /**
         * @param parent 
         * @param turtle_filename 
         */
        quick_scan_see_also(parent: Lv2CacheTurtle, turtle_filename: string): void;
    }


    namespace Lv2UriMapManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2UriMapManager extends GObject.Object {
        static $gtype: GObject.GType<Lv2UriMapManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2UriMapManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        id_counter: number;

        // Constructors
        constructor(properties?: Partial<Lv2UriMapManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2UriMapManager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2UriMapManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2UriMapManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2UriMapManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2UriMapManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2UriMapManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2UriMapManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Singleton function to optain the id manager instance.
         */
        static get_instance(): Lv2UriMapManager;

        // Methods
        /**
         * Inserts a id into hash map associated with `uri`.
         * @param uri the object as key
         * @param id the id to insert
         * @returns `true` on success, otherwise `false`
         */
        insert(uri: string, id: (GObject.Value | any)): boolean;

        load_default(): void;

        /**
         * Lookup a id associated with `uri` in `lv2_uri_map_manager`
         * @param uri the object to lookup
         * @returns the id on success, else G_MAXUINT32
         */
        lookup(uri: string): unknown;

        /**
         * Removes a lock associated with `uri`.3
         * @param uri the object to remove
         * @returns `true` as successfully removed, otherwise `false`
         */
        remove(uri: string): boolean;
    }


    namespace Lv2UridManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2UridManager extends GObject.Object {
        static $gtype: GObject.GType<Lv2UridManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2UridManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        id_counter: number;

        // Constructors
        constructor(properties?: Partial<Lv2UridManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2UridManager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2UridManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2UridManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2UridManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2UridManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2UridManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2UridManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Singleton function to optain the id manager instance.
         */
        static get_instance(): Lv2UridManager;

        // Methods
        /**
         * Inserts a id into hash  associated with `urid`.
         * @param uri the object as key
         * @param id the id to insert
         * @returns `true` on success, otherwise `false`
         */
        insert(uri: string, id: (GObject.Value | any)): boolean;

        load_default(): void;

        /**
         * Lookup a id associated with `urid` in `lv2_urid_manager`
         * @param uri the object to lookup
         * @returns the id on success, else the newly created id
         */
        lookup(uri: string): unknown;

        /**
         * Removes an entry associated with `urid`.
         * @param uri the object to remove
         * @returns `true` as successfully removed, otherwise `false`
         */
        remove(uri: string): boolean;
    }


    namespace Lv2Worker {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::handle": (pspec: GObject.ParamSpec) => void;
            "notify::response-data": (pspec: GObject.ParamSpec) => void;
            "notify::work-data": (pspec: GObject.ParamSpec) => void;
            "notify::work-size": (pspec: GObject.ParamSpec) => void;
            "notify::worker-thread": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            handle: any;
            response_data: any;
            responseData: any;
            work_data: any;
            workData: any;
            work_size: number;
            workSize: number;
            worker_thread: Ags.WorkerThread;
            workerThread: Ags.WorkerThread;
        }
    }

    /**
     * @gir-type Class
     */
    class Lv2Worker extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<Lv2Worker>;

        // Properties
        /**
         * The assigned  #LV2_Handle-struct
         * @since 3.0.0
         */
        get handle(): any;
        set handle(val: any);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.Lv2WorkerResponseData}-struct
         * @since 3.0.0
         */
        get response_data(): any;
        set response_data(val: any);

        /**
         * The assigned {@link GLib.List}-struct containing {@link AgsAudio.Lv2WorkerResponseData}-struct
         * @since 3.0.0
         */
        get responseData(): any;
        set responseData(val: any);

        /**
         * The assigned work data
         * @since 3.0.0
         */
        get work_data(): any;
        set work_data(val: any);

        /**
         * The assigned work data
         * @since 3.0.0
         */
        get workData(): any;
        set workData(val: any);

        /**
         * The work size.
         * @since 3.0.0
         * @default 0
         */
        get work_size(): number;
        set work_size(val: number);

        /**
         * The work size.
         * @since 3.0.0
         * @default 0
         */
        get workSize(): number;
        set workSize(val: number);

        /**
         * The assigned worker-thread.
         * @since 3.0.0
         */
        get worker_thread(): Ags.WorkerThread;
        set worker_thread(val: Ags.WorkerThread);

        /**
         * The assigned worker-thread.
         * @since 3.0.0
         */
        get workerThread(): Ags.WorkerThread;
        set workerThread(val: Ags.WorkerThread);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2Worker.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        // Constructors
        constructor(properties?: Partial<Lv2Worker.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2Worker;

        // Signals
        /** @signal */
        connect_after<K extends keyof Lv2Worker.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2Worker.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2Worker.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2Worker.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Safe run callback.
         * @param worker_thread the {@link Ags.WorkerThread}
         * @param data the data
         */
        static do_poll(worker_thread: Ags.WorkerThread, data: (any | null)): void;

        /**
         * Free `response_data`.
         * @param response_data the {@link AgsAudio.Lv2WorkerResponseData}-struct
         */
        static free_response_data(response_data: Lv2WorkerResponseData): void;

        // Methods
        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: number): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace Lv2WorkerManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2WorkerManager extends GObject.Object {
        static $gtype: GObject.GType<Lv2WorkerManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2WorkerManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        thread_pool: Ags.ThreadPool;

        worker: any[];

        // Constructors
        constructor(properties?: Partial<Lv2WorkerManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2WorkerManager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2WorkerManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2WorkerManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2WorkerManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2WorkerManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2WorkerManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2WorkerManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Singleton function to optain the id manager instance.
         */
        static get_instance(): Lv2WorkerManager;

        // Methods
        /**
         * Pull worker.
         * @returns the {@link Ags.WorkerThread}
         */
        pull_worker<T = GObject.Object>(): T;
    }


    namespace Lv2uiManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Lv2uiManager extends GObject.Object {
        static $gtype: GObject.GType<Lv2uiManager>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2uiManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        lv2ui_plugin_blacklist: any[];

        lv2ui_plugin: any[];

        // Constructors
        constructor(properties?: Partial<Lv2uiManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Lv2uiManager;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2uiManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2uiManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2uiManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2uiManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2uiManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2uiManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get lv2ui manager default plugin path.
         */
        static get_default_path(): string[];

        /**
         * Get instance.
         */
        static get_instance(): Lv2uiManager;

        /**
         * Set lv2ui manager default plugin path.
         * @param default_path the string vector array to use as default path
         */
        static set_default_path(default_path: string[]): void;

        // Methods
        /**
         * Lookup UI filename in loaded plugins.
         * @param ui_filename the UI filename of the plugin
         * @param ui_effect the UI effect's name
         * @returns the {@link AgsAudio.Lv2uiPlugin}
         */
        find_lv2ui_plugin(ui_filename: string, ui_effect: string): Lv2uiPlugin;

        /**
         * Lookup `ui_filename` with `ui_effect_index` in loaded plugins.
         * @param ui_filename the UI filename of the plugin
         * @param ui_effect_index the UI index
         * @returns the {@link AgsAudio.Lv2uiPlugin}
         */
        find_lv2ui_plugin_with_index(ui_filename: string, ui_effect_index: number): Lv2uiPlugin;

        /**
         * Retrieve all filenames
         * @returns a `null`-terminated array of filenames
         */
        get_filenames(): string[];

        /**
         * Load blacklisted plugin filenames.
         * @param blacklist_filename the filename as string
         */
        load_blacklist(blacklist_filename: string): void;

        /**
         * Loads all available plugins.
         */
        load_default_directory(): void;

        /**
         * Load `filename` specified plugin.
         * @param manifest the manifest
         * @param turtle the {@link Ags.Turtle}
         * @param lv2ui_path the lv2ui path
         * @param filename the filename of the plugin
         */
        load_file(manifest: Ags.Turtle, turtle: Ags.Turtle, lv2ui_path: string, filename: string): void;
    }


    namespace Lv2uiPlugin {
        // Signal signatures
        interface SignalSignatures extends BasePlugin.SignalSignatures {
            "notify::gui-turtle": (pspec: GObject.ParamSpec) => void;
            "notify::gui-uri": (pspec: GObject.ParamSpec) => void;
            "notify::lv2-plugin": (pspec: GObject.ParamSpec) => void;
            "notify::manifest": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-port": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect": (pspec: GObject.ParamSpec) => void;
            "notify::ui-effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::ui-filename": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin": (pspec: GObject.ParamSpec) => void;
            "notify::ui-plugin-so": (pspec: GObject.ParamSpec) => void;
            "notify::uuid": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends BasePlugin.ConstructorProps {
            gui_turtle: Ags.Turtle;
            guiTurtle: Ags.Turtle;
            gui_uri: string;
            guiUri: string;
            lv2_plugin: Lv2Plugin;
            lv2Plugin: Lv2Plugin;
            manifest: Ags.Turtle;
        }
    }

    /**
     * @gir-type Class
     */
    class Lv2uiPlugin extends BasePlugin {
        static $gtype: GObject.GType<Lv2uiPlugin>;

        // Properties
        /**
         * The assigned GUI turtle.
         * @since 3.0.0
         */
        get gui_turtle(): Ags.Turtle;
        set gui_turtle(val: Ags.Turtle);

        /**
         * The assigned GUI turtle.
         * @since 3.0.0
         */
        get guiTurtle(): Ags.Turtle;
        set guiTurtle(val: Ags.Turtle);

        /**
         * The assigned GUI URI.
         * @since 3.0.0
         * @default null
         */
        get gui_uri(): string;
        set gui_uri(val: string);

        /**
         * The assigned GUI URI.
         * @since 3.0.0
         * @default null
         */
        get guiUri(): string;
        set guiUri(val: string);

        /**
         * The assigned LV2 plugin.
         * @since 3.0.0
         */
        get lv2_plugin(): Lv2Plugin;
        set lv2_plugin(val: Lv2Plugin);

        /**
         * The assigned LV2 plugin.
         * @since 3.0.0
         */
        get lv2Plugin(): Lv2Plugin;
        set lv2Plugin(val: Lv2Plugin);

        /**
         * The assigned manifest.
         * @since 3.0.0
         */
        get manifest(): Ags.Turtle;
        set manifest(val: Ags.Turtle);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Lv2uiPlugin.SignalSignatures;

        // Fields
        base_plugin: BasePlugin;

        flags: number;

        // Constructors
        constructor(properties?: Partial<Lv2uiPlugin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](gui_turtle: Ags.Turtle, filename: string, effect: string, gui_uri: string, effect_index: number): Lv2uiPlugin;

        // Conflicted with AgsAudio.BasePlugin.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof Lv2uiPlugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2uiPlugin.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Lv2uiPlugin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Lv2uiPlugin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Lv2uiPlugin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Lv2uiPlugin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find next matching gui-uri in `lv2ui_plugin`.
         * @param lv2ui_plugin the {@link GLib.List}-struct containing {@link AgsAudio.Lv2uiPlugin}
         * @param gui_uri the gui-uri as string
         */
        static find_gui_uri(lv2ui_plugin: Lv2uiPlugin[], gui_uri: string): Lv2uiPlugin[];

        // Methods
        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: Lv2uiPluginFlags): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.BasePlugin.set_flags
        set_flags(...args: never[]): any;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: Lv2uiPluginFlags): boolean;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.BasePlugin.test_flags
        test_flags(...args: never[]): any;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: Lv2uiPluginFlags): void;

        /**
         * @param args 
         */
    // Conflicted with AgsAudio.BasePlugin.unset_flags
        unset_flags(...args: never[]): any;
    }


    namespace Marker {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::marker-name": (pspec: GObject.ParamSpec) => void;
            "notify::x": (pspec: GObject.ParamSpec) => void;
            "notify::y": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            marker_name: string;
            markerName: string;
            x: number;
            y: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Marker extends GObject.Object {
        static $gtype: GObject.GType<Marker>;

        // Properties
        /**
         * The marker's name.
         * @since 5.1.0
         * @default null
         */
        get marker_name(): string;
        set marker_name(val: string);

        /**
         * The marker's name.
         * @since 5.1.0
         * @default null
         */
        get markerName(): string;
        set markerName(val: string);

        /**
         * Marker offset x.
         * @since 5.1.0
         * @default 0
         */
        get x(): number;
        set x(val: number);

        /**
         * Marker value y.
         * @since 5.1.0
         * @default 0
         */
        get y(): number;
        set y(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Marker.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        // Constructors
        constructor(properties?: Partial<Marker.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Marker;

        // Signals
        /** @signal */
        connect<K extends keyof Marker.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Marker.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Marker.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Marker.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Marker.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Marker.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Sort markers.
         * @param a an {@link AgsAudio.Marker}
         * @param b an other {@link AgsAudio.Marker}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Duplicate a marker.
         * @returns the duplicated {@link AgsAudio.Marker}.
         */
        duplicate(): Marker;

        /**
         * Gets x.
         * @returns the x
         */
        get_x(): number;

        /**
         * Gets y.
         * @returns the y
         */
        get_y(): number;

        /**
         * Set `flags` on `marker`.
         * @param flags the flags
         */
        set_flags(flags: MarkerFlags): void;

        /**
         * Sets x.
         * @param x the x
         */
        set_x(x: number): void;

        /**
         * Sets y.
         * @param y the y
         */
        set_y(y: number): void;

        /**
         * Test `flags` to be set on `marker`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: MarkerFlags): boolean;

        /**
         * Unset `flags` on `marker`.
         * @param flags the flags
         */
        unset_flags(flags: MarkerFlags): void;
    }


    namespace Midi {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::timestamp": (pspec: GObject.ParamSpec) => void;
            "notify::track": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: Audio;
            audio_channel: number;
            audioChannel: number;
            timestamp: Ags.Timestamp;
            track: Track[];
        }
    }

    /**
     * @gir-type Class
     */
    class Midi extends GObject.Object {
        static $gtype: GObject.GType<Midi>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The midi's audio-channel.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The midi's audio-channel.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The midi's timestamp.
         * @since 3.0.0
         */
        get timestamp(): Ags.Timestamp;
        set timestamp(val: Ags.Timestamp);

        /**
         * The assigned {@link AgsAudio.Track}
         * @since 3.0.0
         */
        get track(): Track[];
        set track(val: Track[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Midi.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        selection: any[];

        // Constructors
        constructor(properties?: Partial<Midi.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: GObject.Object, audio_channel: number): Midi;

        // Signals
        /** @signal */
        connect<K extends keyof Midi.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Midi.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Midi.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Midi.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Midi.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Midi.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Add `new_midi` sorted to `midi`
         * @param midi the {@link GLib.List}-struct containing {@link AgsAudio.Midi}
         * @param new_midi the midi to add
         */
        static add(midi: Midi[], new_midi: Midi): Midi[];

        /**
         * Retrieve appropriate midi for timestamp.
         * @param midi the {@link GLib.List}-struct containing {@link AgsAudio.Midi}
         * @param audio_channel the matching audio channel
         * @param timestamp the matching timestamp, or `null` to match any timestamp
         */
        static find_near_timestamp(midi: Midi[], audio_channel: number, timestamp: (Ags.Timestamp | null)): Midi[];

        /**
         * Compare `a` and `b`.
         * @param a the {@link AgsAudio.Midi}
         * @param b another {@link AgsAudio.Midi}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Adds a track to midi.
         * @param track the {@link AgsAudio.Track} to add
         * @param use_selection_list if `true` add to selection, else to default midi
         */
        add_track(track: Track, use_selection_list: boolean): void;

        /**
         * Get audio.
         * @returns the {@link AgsAudio.Audio}
         */
        get_audio<T = GObject.Object>(): T;

        /**
         * Gets audio channel.
         * @returns the audio channel
         */
        get_audio_channel(): number;

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * Get track.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Track}
         */
        get_track(): Track[];

        /**
         * Removes a track from midi.
         * @param track the {@link AgsAudio.Track} to remove
         * @param use_selection_list if `true` remove from selection, else from default midi
         */
        remove_track(track: Track, use_selection_list: boolean): void;

        /**
         * Set audio.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: GObject.Object): void;

        /**
         * Sets audio channel.
         * @param audio_channel the audio channel
         */
        set_audio_channel(audio_channel: number): void;

        /**
         * Set `flags` on `midi`.
         * @param flags the flags
         */
        set_flags(flags: MidiFlags): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Set track by replacing existing.
         * @param track the {@link GLib.List}-struct containing {@link AgsAudio.Track}
         */
        set_track(track: Track[]): void;

        /**
         * Test `flags` to be set on `midi`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: MidiFlags): boolean;

        /**
         * Unset `flags` on `midi`.
         * @param flags the flags
         */
        unset_flags(flags: MidiFlags): void;
    }


    namespace MidiBuilder {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::append-header signal is emited during building of header.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "append-header": (arg0: number, arg1: number, arg2: number, arg3: number, arg4: number, arg5: number, arg6: number) => void;
            /**
             * The ::append-track signal is emited during building of track.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "append-track": (arg0: string) => void;
            /**
             * @signal
             * @run-last
             */
            "change-channel-pressure": (arg0: number, arg1: number, arg2: number) => void;
            /**
             * @signal
             * @run-last
             */
            "change-parameter": (arg0: number, arg1: number, arg2: number, arg3: number) => void;
            /**
             * @signal
             * @run-last
             */
            "change-pitch-bend": (arg0: number, arg1: number, arg2: number, arg3: number) => void;
            /**
             * @signal
             * @run-last
             */
            "change-program": (arg0: number, arg1: number, arg2: number) => void;
            /**
             * @signal
             * @run-last
             */
            "key-off": (arg0: number, arg1: number, arg2: number, arg3: number) => void;
            /**
             * @signal
             * @run-last
             */
            "key-on": (arg0: number, arg1: number, arg2: number, arg3: number) => void;
            /**
             * @signal
             * @run-last
             */
            "key-pressure": (arg0: number, arg1: number, arg2: number, arg3: number) => void;
            /**
             * @signal
             * @run-last
             */
            "key-signature": (arg0: number, arg1: number, arg2: boolean) => void;
            /**
             * The ::midi-putc signal is emited during putting char to file.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "midi-putc": (arg0: number) => void;
            /**
             * The ::on-error signal is emited during building of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "on-error": (arg0: (any | null)) => void;
            /**
             * @signal
             * @run-last
             */
            "quarter-frame": (arg0: number, arg1: number, arg2: number) => void;
            /**
             * @signal
             * @run-last
             */
            "sequence-number": (arg0: number, arg1: number) => void;
            /**
             * @signal
             * @run-last
             */
            "sequencer-meta-event": (arg0: number, arg1: number, arg2: number, arg3: number) => void;
            /**
             * @signal
             * @run-last
             */
            smtpe: (arg0: number, arg1: number, arg2: number, arg3: number, arg4: number, arg5: number) => void;
            /**
             * @signal
             * @run-last
             */
            "song-position": (arg0: number, arg1: number) => void;
            /**
             * @signal
             * @run-last
             */
            "song-select": (arg0: number, arg1: number) => void;
            /**
             * @signal
             * @run-last
             */
            sysex: (arg0: number, arg1: (any | null), arg2: number) => void;
            /**
             * @signal
             * @run-last
             */
            tempo: (arg0: number, arg1: number) => void;
            /**
             * @signal
             * @run-last
             */
            "text-event": (arg0: number, arg1: string, arg2: number) => void;
            /**
             * @signal
             * @run-last
             */
            "time-signature": (arg0: number, arg1: number, arg2: number, arg3: number, arg4: number) => void;
            /**
             * @signal
             * @run-last
             */
            "tune-request": (arg0: number) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            file: any;
        }
    }

    /**
     * @gir-type Class
     */
    class MidiBuilder extends GObject.Object {
        static $gtype: GObject.GType<MidiBuilder>;

        // Properties
        /**
         * The file to parse data from.
         * @since 3.0.0
         */
        get file(): any;
        set file(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: MidiBuilder.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: MidiBuilderFlags;

        midi_smf_util: MidiSmfUtil;

        data: number;

        length: number;

        midi_track: any[];

        // Constructors
        constructor(properties?: Partial<MidiBuilder.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): MidiBuilder;

        static new_from_filename(filename: (string | null)): MidiBuilder;

        // Signals
        /** @signal */
        connect<K extends keyof MidiBuilder.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MidiBuilder.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof MidiBuilder.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MidiBuilder.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof MidiBuilder.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<MidiBuilder.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Appends change parameter.
         * @param delta_time the delta time
         * @param channel the channel
         * @param control the control
         * @param value the value
         * @virtual
         */
        vfunc_append_change_parameter(delta_time: number, channel: number, control: number, value: number): void;

        /**
         * Change pitch bend.
         * @param delta_time the delta time
         * @param channel the channel
         * @param pitch the pitch
         * @param transmitter the transmitter
         * @virtual
         */
        vfunc_append_change_pitch_bend(delta_time: number, channel: number, pitch: number, transmitter: number): void;

        /**
         * Appends change pressure.
         * @param delta_time the delta time
         * @param channel the channel
         * @param pressure the pressure
         * @virtual
         */
        vfunc_append_change_pressure(delta_time: number, channel: number, pressure: number): void;

        /**
         * Appends change program.
         * @param delta_time the delta time
         * @param channel the channel
         * @param program the program
         * @virtual
         */
        vfunc_append_change_program(delta_time: number, channel: number, program: number): void;

        /**
         * Appends MIDI header to `midi_builder`.
         * @param offset start delta-time
         * @param format either 0, 1 or 2.
         * @param track_count the number of tracks
         * @param division timing division
         * @param times pulse per quarter note
         * @param bpm beats per minute
         * @param clicks timing clicks
         * @virtual
         */
        vfunc_append_header(offset: number, format: number, track_count: number, division: number, times: number, bpm: number, clicks: number): void;

        /**
         * Append key-off for `note` to `midi_builder` with key dynamics `velocity`, at `delta_time`.
         * @param delta_time delta-time
         * @param audio_channel the audio channel
         * @param note the note to play from 0 to 128
         * @param velocity key dynamics
         * @virtual
         */
        vfunc_append_key_off(delta_time: number, audio_channel: number, note: number, velocity: number): void;

        /**
         * Append key-on for `note` to `midi_builder` with key dynamics `velocity`, at `delta_time`.
         * @param delta_time delta-time
         * @param audio_channel the audio channel
         * @param note the note to play from 0 to 128
         * @param velocity key dynamics
         * @virtual
         */
        vfunc_append_key_on(delta_time: number, audio_channel: number, note: number, velocity: number): void;

        /**
         * Append key-pressure for `note` to `midi_builder` with key dynamics `pressure`, at `delta_time`.
         * @param delta_time delta-time
         * @param audio_channel the audio channel
         * @param note the note to play from 0 to 128
         * @param pressure key dynamics
         * @virtual
         */
        vfunc_append_key_pressure(delta_time: number, audio_channel: number, note: number, pressure: number): void;

        /**
         * Appends key signature.
         * @param delta_time the delta time
         * @param sf key
         * @param mi mode
         * @virtual
         */
        vfunc_append_key_signature(delta_time: number, sf: number, mi: number): void;

        /**
         * Appends quarter frame.
         * @param delta_time the delta time
         * @param message_type the message type
         * @param values values
         * @virtual
         */
        vfunc_append_quarter_frame(delta_time: number, message_type: number, values: number): void;

        /**
         * Appends sequence number.
         * @param delta_time the delta time
         * @param sequence the sequence number
         * @virtual
         */
        vfunc_append_sequence_number(delta_time: number, sequence: number): void;

        /**
         * Appends sequencer meta event.
         * @param delta_time the delta time
         * @param len the length
         * @param id the id
         * @param data the data
         * @virtual
         */
        vfunc_append_sequencer_meta_event(delta_time: number, len: number, id: number, data: number): void;

        /**
         * Appends smtpe.
         * @param delta_time the delta time
         * @param rr rate
         * @param hr hours
         * @param mn minutes
         * @param se seconds
         * @param fr fraction
         * @virtual
         */
        vfunc_append_smtpe(delta_time: number, rr: number, hr: number, mn: number, se: number, fr: number): void;

        /**
         * Appends song position.
         * @param delta_time the delta time
         * @param song_position the song position
         * @virtual
         */
        vfunc_append_song_position(delta_time: number, song_position: number): void;

        /**
         * Appends song select.
         * @param delta_time the delta time
         * @param song_select the song select
         * @virtual
         */
        vfunc_append_song_select(delta_time: number, song_select: number): void;

        /**
         * Appends sysex data.
         * @param delta_time the delta time
         * @param sysex_data the sysex data
         * @param length length
         * @virtual
         */
        vfunc_append_sysex(delta_time: number, sysex_data: number, length: number): void;

        /**
         * Appends tempo.
         * @param delta_time the delta time
         * @param tempo the tempo
         * @virtual
         */
        vfunc_append_tempo(delta_time: number, tempo: number): void;

        /**
         * Appends text event.
         * @param delta_time the delta time
         * @param text the string
         * @param length the string length
         * @virtual
         */
        vfunc_append_text_event(delta_time: number, text: string, length: number): void;

        /**
         * Appends time signature.
         * @param delta_time the delta time
         * @param nn numerator
         * @param dd denominator
         * @param cc ticks per metronome click
         * @param bb 32nd per quarter note
         * @virtual
         */
        vfunc_append_time_signature(delta_time: number, nn: number, dd: number, cc: number, bb: number): void;

        /**
         * Append a track called `track_name` to `midi_builder`.
         * @param track_name the track name
         * @virtual
         */
        vfunc_append_track(track_name: string): void;

        /**
         * Appends tune request.
         * @param delta_time the delta time
         * @virtual
         */
        vfunc_append_tune_request(delta_time: number): void;

        /**
         * Put char in MIDI file.
         * @param c the character to put
         * @virtual
         */
        vfunc_midi_putc(c: number): void;

        /**
         * Triggered as an error occurs.
         * @virtual
         */
        vfunc_on_error(): void;

        // Methods
        /**
         * Appends change parameter.
         * @param delta_time the delta time
         * @param channel the channel
         * @param control the control
         * @param value the value
         */
        append_change_parameter(delta_time: number, channel: number, control: number, value: number): void;

        /**
         * Change pitch bend.
         * @param delta_time the delta time
         * @param channel the channel
         * @param pitch the pitch
         * @param transmitter the transmitter
         */
        append_change_pitch_bend(delta_time: number, channel: number, pitch: number, transmitter: number): void;

        /**
         * Appends change pressure.
         * @param delta_time the delta time
         * @param channel the channel
         * @param pressure the pressure
         */
        append_change_pressure(delta_time: number, channel: number, pressure: number): void;

        /**
         * Appends change program.
         * @param delta_time the delta time
         * @param channel the channel
         * @param program the program
         */
        append_change_program(delta_time: number, channel: number, program: number): void;

        /**
         * Appends MIDI header to `midi_builder`.
         * @param offset start delta-time
         * @param format either 0, 1 or 2.
         * @param track_count the number of tracks
         * @param division timing division
         * @param times pulse per quarter note
         * @param bpm beats per minute
         * @param clicks timing clicks
         */
        append_header(offset: number, format: number, track_count: number, division: number, times: number, bpm: number, clicks: number): void;

        /**
         * Append key-off for `note` to `midi_builder` with key dynamics `velocity`, at `delta_time`.
         * @param delta_time delta-time
         * @param audio_channel the audio channel
         * @param note the note to play from 0 to 128
         * @param velocity key dynamics
         */
        append_key_off(delta_time: number, audio_channel: number, note: number, velocity: number): void;

        /**
         * Append key-on for `note` to `midi_builder` with key dynamics `velocity`, at `delta_time`.
         * @param delta_time delta-time
         * @param audio_channel the audio channel
         * @param note the note to play from 0 to 128
         * @param velocity key dynamics
         */
        append_key_on(delta_time: number, audio_channel: number, note: number, velocity: number): void;

        /**
         * Append key-pressure for `note` to `midi_builder` with key dynamics `pressure`, at `delta_time`.
         * @param delta_time delta-time
         * @param audio_channel the audio channel
         * @param note the note to play from 0 to 128
         * @param pressure key dynamics
         */
        append_key_pressure(delta_time: number, audio_channel: number, note: number, pressure: number): void;

        /**
         * Appends key signature.
         * @param delta_time the delta time
         * @param sf key
         * @param mi mode
         */
        append_key_signature(delta_time: number, sf: number, mi: number): void;

        /**
         * Appends quarter frame.
         * @param delta_time the delta time
         * @param message_type the message type
         * @param values values
         */
        append_quarter_frame(delta_time: number, message_type: number, values: number): void;

        /**
         * Appends sequence number.
         * @param delta_time the delta time
         * @param sequence the sequence number
         */
        append_sequence_number(delta_time: number, sequence: number): void;

        /**
         * Appends sequencer meta event.
         * @param delta_time the delta time
         * @param len the length
         * @param id the id
         * @param data the data
         */
        append_sequencer_meta_event(delta_time: number, len: number, id: number, data: number): void;

        /**
         * Appends smtpe.
         * @param delta_time the delta time
         * @param rr rate
         * @param hr hours
         * @param mn minutes
         * @param se seconds
         * @param fr fraction
         */
        append_smtpe(delta_time: number, rr: number, hr: number, mn: number, se: number, fr: number): void;

        /**
         * Appends song position.
         * @param delta_time the delta time
         * @param song_position the song position
         */
        append_song_position(delta_time: number, song_position: number): void;

        /**
         * Appends song select.
         * @param delta_time the delta time
         * @param song_select the song select
         */
        append_song_select(delta_time: number, song_select: number): void;

        /**
         * Appends sysex data.
         * @param delta_time the delta time
         * @param sysex_data the sysex data
         * @param length length
         */
        append_sysex(delta_time: number, sysex_data: number, length: number): void;

        /**
         * Appends tempo.
         * @param delta_time the delta time
         * @param tempo the tempo
         */
        append_tempo(delta_time: number, tempo: number): void;

        /**
         * Appends text event.
         * @param delta_time the delta time
         * @param text the string
         * @param length the string length
         */
        append_text_event(delta_time: number, text: string, length: number): void;

        /**
         * Appends time signature.
         * @param delta_time the delta time
         * @param nn numerator
         * @param dd denominator
         * @param cc ticks per metronome click
         * @param bb 32nd per quarter note
         */
        append_time_signature(delta_time: number, nn: number, dd: number, cc: number, bb: number): void;

        /**
         * Append a track called `track_name` to `midi_builder`.
         * @param track_name the track name
         */
        append_track(track_name: string): void;

        /**
         * Appends tune request.
         * @param delta_time the delta time
         */
        append_tune_request(delta_time: number): void;

        /**
         * Append from XML node `node`.
         * @param node the xmlNode
         */
        append_xml_node(node: libxml2.Node): void;

        /**
         * Build the MIDI data.
         */
        build(): void;

        /**
         * Builds from XML document `doc`.
         * @param doc the xmlDocument
         */
        from_xml_doc(doc: libxml2.Doc): void;

        /**
         * Get SMF data of `midi_builder`.
         * @returns the SMF data
         */
        get_data(): number;

        /**
         * @param args 
         */
    // Conflicted with GObject.Object.get_data
        get_data(...args: never[]): any;

        /**
         * Get SMF data of `midi_builder`.
         * @returns the MIDI data as array
         */
        get_data_with_length(): [number, number];

        /**
         * Put char in MIDI file.
         * @param c the character to put
         */
        midi_putc(c: number): void;

        /**
         * Triggered as an error occurs.
         */
        on_error(): void;

        /**
         * Open `filename` and assign FILE stream to `midi_builder`.
         * @param filename the filename
         */
        open_filename(filename: string): void;

        /**
         * Write data of `midi_builder` to file.
         */
        write(): void;
    }


    namespace MidiFile {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::filename": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            filename: string;
        }
    }

    /**
     * @gir-type Class
     */
    class MidiFile extends GObject.Object {
        static $gtype: GObject.GType<MidiFile>;

        // Properties
        /**
         * The assigned filename to perform input/output on.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: MidiFile.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: MidiFileFlags;

        file: any;

        buffer: number;

        buffer_length: number;

        iter: number;

        offset: number;

        format: number;

        count: number;

        division: number;

        times: number;

        beat: number;

        clicks: number;

        track: any[];

        notation: any[];

        midi: any[];

        // Constructors
        constructor(properties?: Partial<MidiFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): MidiFile;

        // Signals
        /** @signal */
        connect<K extends keyof MidiFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MidiFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof MidiFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MidiFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof MidiFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<MidiFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Closes the file stream.
         */
        close(): void;

        /**
         * Flushes file stream's data buffer to disc.
         */
        flush(): void;

        /**
         * Opens a MIDI file read-only.
         * @param filename the filename
         * @returns `true` on success, otherwise `false`
         */
        open(filename: string): boolean;

        /**
         * Opens a virtual MIDI file residing in `data`'s array.
         * @param data the buffer to set
         * @param buffer_length the length of the buffer
         * @returns `true` on success, otherwise `false`
         */
        open_from_data(data: number, buffer_length: number): boolean;

        /**
         * Reads all bytes of the file's stream and stores them in the internal buffer.
         * @returns the data array just read
         */
        read(): number;

        /**
         * Reads a unsigned char quantity.
         * @returns the current value at file's iteration pointer
         */
        read_byte(): number;

        /**
         * Reads a gint16 quantity.
         * @returns the current value at file's iteration pointer
         */
        read_gint16(): number;

        /**
         * Reads a 24-bit quantity.
         * @returns the current value at file's iteration pointer
         */
        read_gint24(): number;

        /**
         * Reads a gint32 quantity.
         * @returns the current value at file's iteration pointer
         */
        read_gint32(): number;

        /**
         * Reads the MIDI file's header and positions internal buffer pointer just behind it.
         * @param buffer_length pointer to return buffer length or `null`
         * @returns the header's bytes
         */
        read_header(buffer_length: number): number;

        read_notation(): void;

        /**
         * Reads a string.
         * @param length the number of bytes to be read, or as long valid string for -1
         * @returns the string at file's iteration pointer
         */
        read_text(length: number): number;

        /**
         * Reads the MIDI file's track data.
         * @param buffer_length pointer to return buffer length or `null`
         * @returns the track's bytes
         */
        read_track_data(buffer_length: number): number;

        /**
         * Reads a variable length quantity.
         * @returns the current value at file's iteration pointer
         */
        read_varlength(): number;

        /**
         * Opens a MIDI file with read-write permission.
         * @param filename the filename
         * @param create `true` create file if not exists, else if `false` return
         * @returns `true` on success, otherwise `false`
         */
        rw_open(filename: string, create: boolean): boolean;

        /**
         * Seeks the file stream's offset.
         * @param position the offset
         * @param whence SEEK_SET, SEEK_END, or SEEK_CUR
         */
        seek(position: number, whence: number): void;

        /**
         * Writes `data` to the file stream and to internal buffer, reallocates it if necessary.
         * @param data the data array to write
         * @param buffer_length n-bytes to write
         */
        write(data: number, buffer_length: number): void;

        /**
         * Writes a unsigned char quantity to internal buffer.
         * @param val the value to write
         */
        write_byte(val: number): void;

        /**
         * Writes a gint16 quantity to internal buffer.
         * @param val the value to write
         */
        write_gint16(val: number): void;

        /**
         * Writes a 24-bit quantity to internal buffer.
         * @param val the value to write
         */
        write_gint24(val: number): void;

        /**
         * Writes a gint32 quantity to internal buffer.
         * @param val the value to write
         */
        write_gint32(val: number): void;

        /**
         * Write header bytes.
         * @param buffer the buffer to write
         * @param buffer_length the length of the buffer
         */
        write_header(buffer: number, buffer_length: number): void;

        /**
         * Writes a string to internal buffer up to length bytes.
         * @param text the text
         * @param length the string's length
         */
        write_text(text: string, length: number): void;

        /**
         * @param buffer 
         * @param buffer_length 
         */
        write_track_data(buffer: number, buffer_length: number): void;

        /**
         * @param val 
         */
        write_varlength(val: (bigint | number)): void;
    }


    namespace MidiParser {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::change-channel-pressure signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "change-channel-pressure": (arg0: number) => (any | null);
            /**
             * The ::change-parameter signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "change-parameter": (arg0: number) => (any | null);
            /**
             * The ::change-pitch-bend signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "change-pitch-bend": (arg0: number) => (any | null);
            /**
             * The ::change-program signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "change-program": (arg0: number) => (any | null);
            /**
             * The ::end-of-track signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "end-of-track": (arg0: number) => (any | null);
            /**
             * The ::key-off signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "key-off": (arg0: number) => (any | null);
            /**
             * The ::key-on signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "key-on": (arg0: number) => (any | null);
            /**
             * The ::key-pressure signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "key-pressure": (arg0: number) => (any | null);
            /**
             * The ::key-signature signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "key-signature": (arg0: number) => (any | null);
            /**
             * The ::meta-event signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "meta-event": (arg0: number) => (any | null);
            /**
             * The ::midi-getc signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "midi-getc": () => number;
            /**
             * The ::not-defined signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "not-defined": (arg0: number) => (any | null);
            /**
             * The ::on-error signal is emited as error occurs.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "on-error": (arg0: (any | null)) => void;
            /**
             * The ::parse-bytes signal is emited during parsing of bytes.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "parse-bytes": (arg0: (any | null), arg1: number) => (any | null);
            /**
             * The ::parse-full signal is emited during parsing of midi file.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "parse-full": () => (any | null);
            /**
             * The ::parse-header signal is emited during parsing of header.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "parse-header": () => (any | null);
            /**
             * The ::parse-track signal is emited during parsing of track.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "parse-track": () => (any | null);
            /**
             * The ::sequence-number signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "sequence-number": (arg0: number) => (any | null);
            /**
             * The ::sequencer-meta-event signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "sequencer-meta-event": (arg0: number) => (any | null);
            /**
             * The ::smtpe signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            smtpe: (arg0: number) => (any | null);
            /**
             * The ::sysex signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            sysex: (arg0: number) => (any | null);
            /**
             * The ::system-common signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "system-common": (arg0: number) => (any | null);
            /**
             * The ::tempo signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            tempo: (arg0: number) => (any | null);
            /**
             * The ::text-event signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "text-event": (arg0: number) => (any | null);
            /**
             * The ::time-signature signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "time-signature": (arg0: number) => (any | null);
            "notify::file": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            file: any;
        }
    }

    /**
     * @gir-type Class
     */
    class MidiParser extends GObject.Object {
        static $gtype: GObject.GType<MidiParser>;

        // Properties
        /**
         * The file to parse data from.
         * @since 3.0.0
         */
        get file(): any;
        set file(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: MidiParser.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: MidiParserFlags;

        nth_chunk: number;

        buffer: number;

        file_length: number;

        offset: number;

        current_time: number;

        current_status: number;

        // Constructors
        constructor(properties?: Partial<MidiParser.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): MidiParser;

        static new_from_filename(filename: (string | null)): MidiParser;

        // Signals
        /** @signal */
        connect<K extends keyof MidiParser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MidiParser.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof MidiParser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MidiParser.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof MidiParser.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<MidiParser.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Parse change channel pressure.
         * @param status the status byte
         * @virtual
         */
        vfunc_change_channel_pressure(status: number): libxml2.Node;

        /**
         * Parse change parameter.
         * @param status the status byte
         * @virtual
         */
        vfunc_change_parameter(status: number): libxml2.Node;

        /**
         * Parse change pitch bend.
         * @param status the status byte
         * @virtual
         */
        vfunc_change_pitch_bend(status: number): libxml2.Node;

        /**
         * Parse change program.
         * @param status the status byte
         * @virtual
         */
        vfunc_change_program(status: number): libxml2.Node;

        /**
         * Parse end of track.
         * @param meta_type the meta type byte
         * @virtual
         */
        vfunc_end_of_track(meta_type: number): libxml2.Node;

        /**
         * Parse key off.
         * @param status the status byte
         * @virtual
         */
        vfunc_key_off(status: number): libxml2.Node;

        /**
         * Parse key on.
         * @param status the status byte
         * @virtual
         */
        vfunc_key_on(status: number): libxml2.Node;

        /**
         * Parse key pressure.
         * @param status the status byte
         * @virtual
         */
        vfunc_key_pressure(status: number): libxml2.Node;

        /**
         * Parse key signature.
         * @param meta_type the meta type byte
         * @virtual
         */
        vfunc_key_signature(meta_type: number): libxml2.Node;

        /**
         * Parse meta event.
         * @param status the status byte
         * @virtual
         */
        vfunc_meta_event(status: number): libxml2.Node;

        /**
         * Read one byte.
         * @virtual
         */
        vfunc_midi_getc(): number;

        /**
         * Parse not defined.
         * @param status the status byte
         * @virtual
         */
        vfunc_not_defined(status: number): libxml2.Node;

        /**
         * Error reporting.
         * @virtual
         */
        vfunc_on_error(): void;

        /**
         * Parse all data of {@link AgsAudio.MidiParser.file}.
         * @virtual
         */
        vfunc_parse_full(): libxml2.Doc;

        /**
         * Parse MIDI header
         * @virtual
         */
        vfunc_parse_header(): libxml2.Node;

        /**
         * Parse MIDI track.
         * @virtual
         */
        vfunc_parse_track(): libxml2.Node;

        /**
         * Parse sequence number.
         * @param meta_type the meta type byte
         * @virtual
         */
        vfunc_sequence_number(meta_type: number): libxml2.Node;

        /**
         * Parse smtpe.
         * @param meta_type the meta type byte
         * @virtual
         */
        vfunc_smtpe(meta_type: number): libxml2.Node;

        /**
         * Parse sysex.
         * @param status the status byte
         * @virtual
         */
        vfunc_sysex(status: number): libxml2.Node;

        /**
         * Parse system common.
         * @param status the status byte
         * @virtual
         */
        vfunc_system_common(status: number): libxml2.Node;

        /**
         * Parse tempo.
         * @param meta_type the meta type byte
         * @virtual
         */
        vfunc_tempo(meta_type: number): libxml2.Node;

        /**
         * Parse text event.
         * @param meta_type the meta type byte
         * @virtual
         */
        vfunc_text_event(meta_type: number): libxml2.Node;

        /**
         * Parse time signature.
         * @param meta_type the meta type byte
         * @virtual
         */
        vfunc_time_signature(meta_type: number): libxml2.Node;

        // Methods
        /**
         * Parse change channel pressure.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        change_channel_pressure(status: number): libxml2.Node;

        /**
         * Parse change parameter.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        change_parameter(status: number): libxml2.Node;

        /**
         * Parse change pitch bend.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        change_pitch_bend(status: number): libxml2.Node;

        /**
         * Parse change program.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        change_program(status: number): libxml2.Node;

        /**
         * Parse channel message.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        channel_message(status: number): libxml2.Node;

        /**
         * Parse end of track.
         * @param meta_type the meta type byte
         * @returns the xmlNode containing parsed data
         */
        end_of_track(meta_type: number): libxml2.Node;

        /**
         * Parse key off.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        key_off(status: number): libxml2.Node;

        /**
         * Parse key on.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        key_on(status: number): libxml2.Node;

        /**
         * Parse key pressure.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        key_pressure(status: number): libxml2.Node;

        /**
         * Parse key signature.
         * @param meta_type the meta type byte
         * @returns the xmlNode containing parsed data
         */
        key_signature(meta_type: number): libxml2.Node;

        /**
         * Parse meta event.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        meta_event(status: number): libxml2.Node;

        /**
         * Read one byte.
         * @returns the byte read
         */
        midi_getc(): number;

        /**
         * Parse not defined.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        not_defined(status: number): libxml2.Node;

        /**
         * Error reporting.
         */
        on_error(): void;

        /**
         * Open `filename` and assign FILE stream to `midi_parser`.
         * @param filename the filename
         */
        open_filename(filename: string): void;

        /**
         * Parse all data of {@link AgsAudio.MidiParser.file}.
         * @returns the xmlDoc representing your MIDI file
         */
        parse_full(): libxml2.Doc;

        /**
         * Parse MIDI header
         * @returns the xmlNode containing parsed data
         */
        parse_header(): libxml2.Node;

        /**
         * Parse MIDI track.
         * @returns the xmlNode containing parsed data
         */
        parse_track(): libxml2.Node;

        /**
         * Read gint16.
         * @returns the read gint16
         */
        read_gint16(): number;

        /**
         * Read gint32.
         * @returns the read gint32
         */
        read_gint24(): number;

        /**
         * Read gint32.
         * @returns the read gint32
         */
        read_gint32(): number;

        /**
         * Read text.
         * @param length the length
         * @returns the text read as string
         */
        read_text(length: number): string;

        /**
         * Read varlength as long.
         * @returns the read varlength
         */
        read_varlength(): number;

        /**
         * Parse sequence number.
         * @param meta_type the meta type byte
         * @returns the xmlNode containing parsed data
         */
        sequence_number(meta_type: number): libxml2.Node;

        /**
         * Set buffer of `midi_parser`
         * @param buffer the buffer
         */
        set_buffer(buffer: number): void;

        /**
         * Set file length of `midi_parser`
         * @param file_length the file length
         */
        set_file_length(file_length: (bigint | number)): void;

        /**
         * Parse smtpe.
         * @param meta_type the meta type byte
         * @returns the xmlNode containing parsed data
         */
        smtpe(meta_type: number): libxml2.Node;

        /**
         * Parse sysex.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        sysex(status: number): libxml2.Node;

        /**
         * Parse system common.
         * @param status the status byte
         * @returns the xmlNode containing parsed data
         */
        system_common(status: number): libxml2.Node;

        /**
         * Parse tempo.
         * @param meta_type the meta type byte
         * @returns the xmlNode containing parsed data
         */
        tempo(meta_type: number): libxml2.Node;

        /**
         * Parse text event.
         * @param meta_type the meta type byte
         * @returns the xmlNode containing parsed data
         */
        text_event(meta_type: number): libxml2.Node;

        /**
         * Convert ticks to seconds.
         * @param ticks the ticks count
         * @param division the division
         * @param tempo the tempo
         * @returns the seconds read from ticks
         */
        ticks_to_sec(ticks: number, division: number, tempo: number): number;

        /**
         * Parse time signature.
         * @param meta_type the meta type byte
         * @returns the xmlNode containing parsed data
         */
        time_signature(meta_type: number): libxml2.Node;
    }


    namespace MoveNote {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::absolute": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::first-x": (pspec: GObject.ParamSpec) => void;
            "notify::first-y": (pspec: GObject.ParamSpec) => void;
            "notify::move-x": (pspec: GObject.ParamSpec) => void;
            "notify::move-y": (pspec: GObject.ParamSpec) => void;
            "notify::notation": (pspec: GObject.ParamSpec) => void;
            "notify::relative": (pspec: GObject.ParamSpec) => void;
            "notify::selection": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            absolute: boolean;
            audio: Audio;
            first_x: number;
            firstX: number;
            first_y: number;
            firstY: number;
            move_x: number;
            moveX: number;
            move_y: number;
            moveY: number;
            notation: Notation;
            relative: boolean;
            selection: Note[];
        }
    }

    /**
     * @gir-type Class
     */
    class MoveNote extends Ags.Task {
        static $gtype: GObject.GType<MoveNote>;

        // Properties
        /**
         * Move notation by absolute position.
         * @since 3.0.0
         * @default false
         */
        get absolute(): boolean;
        set absolute(val: boolean);

        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * Move notation from x offset.
         * @since 3.0.0
         * @default 0
         */
        get first_x(): number;
        set first_x(val: number);

        /**
         * Move notation from x offset.
         * @since 3.0.0
         * @default 0
         */
        get firstX(): number;
        set firstX(val: number);

        /**
         * Move notation with x padding.
         * @since 3.0.0
         * @default 0
         */
        get first_y(): number;
        set first_y(val: number);

        /**
         * Move notation with x padding.
         * @since 3.0.0
         * @default 0
         */
        get firstY(): number;
        set firstY(val: number);

        /**
         * Move notation by move-x amount.
         * @since 3.0.0
         * @default 0
         */
        get move_x(): number;
        set move_x(val: number);

        /**
         * Move notation by move-x amount.
         * @since 3.0.0
         * @default 0
         */
        get moveX(): number;
        set moveX(val: number);

        /**
         * Move notation by move-y amount.
         * @since 3.0.0
         * @default 0
         */
        get move_y(): number;
        set move_y(val: number);

        /**
         * Move notation by move-y amount.
         * @since 3.0.0
         * @default 0
         */
        get moveY(): number;
        set moveY(val: number);

        /**
         * The assigned {@link AgsAudio.Notation}
         * @since 3.0.0
         */
        get notation(): Notation;
        set notation(val: Notation);

        /**
         * Move notation by relative position.
         * @since 3.0.0
         * @default false
         */
        get relative(): boolean;
        set relative(val: boolean);

        /**
         * The assigned {@link AgsAudio.Note}
         * @since 3.0.0
         */
        get selection(): Note[];
        set selection(val: Note[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: MoveNote.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<MoveNote.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, notation: Notation, selection: Note[], first_x: number, first_y: number, move_x: number, move_y: number, relative: boolean, absolute: boolean): MoveNote;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof MoveNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MoveNote.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof MoveNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, MoveNote.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof MoveNote.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<MoveNote.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Set `selection` of `move_note`.
         * @param selection the selection as {@link GLib.List}-struct
         */
        set_selection(selection: Note[]): void;
    }


    namespace Notation {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::note": (pspec: GObject.ParamSpec) => void;
            "notify::timestamp": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: Audio;
            audio_channel: number;
            audioChannel: number;
            note: Note[];
            timestamp: Ags.Timestamp;
        }
    }

    /**
     * @gir-type Class
     */
    class Notation extends GObject.Object {
        static $gtype: GObject.GType<Notation>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The effect's audio-channel.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The effect's audio-channel.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The assigned {@link AgsAudio.Note}
         * @since 3.0.0
         */
        get note(): Note[];
        set note(val: Note[]);

        /**
         * The notation's timestamp.
         * @since 3.0.0
         */
        get timestamp(): Ags.Timestamp;
        set timestamp(val: Ags.Timestamp);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Notation.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: NotationFlags;

        key_format: SoundKeyFormat;

        is_minor: boolean;

        sharp_flats: number;

        maximum_note_length: number;

        selection: any[];

        // Constructors
        constructor(properties?: Partial<Notation.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: GObject.Object, audio_channel: number): Notation;

        // Signals
        /** @signal */
        connect<K extends keyof Notation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Notation.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Notation.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Notation.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Notation.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Notation.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Add `new_notation` sorted to `notation`
         * @param notation the {@link GLib.List}-struct containing {@link AgsAudio.Notation}
         * @param new_notation the {@link AgsAudio.Notation} to add
         */
        static add(notation: Notation[], new_notation: Notation): Notation[];

        /**
         * Retrieve appropriate notation for timestamp.
         * @param notation the {@link GLib.List}-struct containing {@link AgsAudio.Notation}
         * @param audio_channel the matching audio channel
         * @param timestamp the matching timestamp, or `null` to match any timestamp
         */
        static find_near_timestamp(notation: Notation[], audio_channel: number, timestamp: (Ags.Timestamp | null)): Notation[];

        /**
         * Clear all selection of `notation`.
         * @param notation the {@link GLib.List}-struct containing {@link AgsAudio.Notation}
         */
        static free_all_selection(notation: Notation[]): void;

        /**
         * Parse `raw_midi` data and convert to {@link AgsAudio.Notation}.
         * @param raw_midi the data array
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         * @param tempo tempo
         * @param bpm the bpm to use
         * @param delay_factor the segmentation delay factor
         * @param buffer_length the buffer length
         */
        static from_raw_midi(raw_midi: number, nn: (bigint | number), dd: (bigint | number), cc: (bigint | number), bb: (bigint | number), tempo: (bigint | number), bpm: number, delay_factor: number, buffer_length: number): Notation;

        /**
         * Compare `a` and `b`.
         * @param a the {@link AgsAudio.Notation}
         * @param b another {@link AgsAudio.Notation}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Add all note to selection.
         */
        add_all_to_selection(): void;

        /**
         * Adds `note` to `notation`.
         * @param note the {@link AgsAudio.Note} to add
         * @param use_selection_list if `true` add to selection, else to default notation
         */
        add_note(note: Note, use_selection_list: boolean): void;

        /**
         * Select notes at position.
         * @param x x offset
         * @param y y note tone
         * @param replace_current_selection if `true` selection is replaced
         */
        add_point_to_selection(x: number, y: number, replace_current_selection: boolean): void;

        /**
         * Add note within region to selection.
         * @param x0 x start offset
         * @param y0 y start tone
         * @param x1 x end offset
         * @param y1 y end tone
         * @param replace_current_selection if `true` selection is replaced
         */
        add_region_to_selection(x0: number, y0: number, x1: number, y1: number, replace_current_selection: boolean): void;

        /**
         * Copy selection to clipboard.
         * @returns the selection as XML.
         */
        copy_selection(): libxml2.Node;

        /**
         * Cut selection to clipboard.
         * @returns the selection as xmlNode
         */
        cut_selection(): libxml2.Node;

        /**
         * Find all notes by range `x_256th_lower` to `x_256th_upper`.
         * @param x_256th_lower offset lower
         * @param x_256th_upper offset upper
         * @param use_selection_list if `true` selection is searched
         * @returns the {@link GLib.List}-struct containing matching {@link AgsAudio.Note}
         */
        find_note_256th_range(x_256th_lower: number, x_256th_upper: number, use_selection_list: boolean): Note[];

        /**
         * Find all notes by offset `x`.
         * @param x offset
         * @param use_selection_list if `true` selection is searched
         * @returns the {@link GLib.List}-struct containing matching {@link AgsAudio.Note}
         */
        find_offset(x: number, use_selection_list: boolean): Note[];

        /**
         * Find note by offset and tone.
         * @param x offset
         * @param y note
         * @param use_selection_list if `true` selection is searched
         * @returns the matching note.
         */
        find_point(x: number, y: number, use_selection_list: boolean): Note;

        /**
         * Find note by offset and tone region.
         * @param x0 start offset
         * @param y0 start tone
         * @param x1 end offset
         * @param y1 end tone
         * @param use_selection_list if `true` selection is searched
         * @returns the matching notes as {@link GLib.List}-struct
         */
        find_region(x0: number, y0: number, x1: number, y1: number, use_selection_list: boolean): Note[];

        /**
         * Clear selection.
         */
        free_selection(): void;

        /**
         * Get audio.
         * @returns the {@link AgsAudio.Audio}
         */
        get_audio<T = GObject.Object>(): T;

        /**
         * Gets audio channel.
         * @returns the audio channel
         */
        get_audio_channel(): number;

        /**
         * Gets is minor.
         * @returns is minor
         */
        get_is_minor(): boolean;

        /**
         * Get note.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Note}
         */
        get_note(): Note[];

        /**
         * Retrieve selection.
         * @returns the selection.
         */
        get_selection(): Note[];

        /**
         * Gets sharp flats.
         * @returns the sharp flats
         */
        get_sharp_flats(): number;

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * Paste previously copied notes.
         * @param notation_node the clipboard XML data
         * @param reset_x_offset if `true` `x_offset` used as cursor
         * @param x_offset region start cursor offset
         * @param reset_y_offset if `true` `y_offset` used as cursor
         * @param y_offset region start cursor tone
         */
        insert_from_clipboard(notation_node: libxml2.Node, reset_x_offset: boolean, x_offset: number, reset_y_offset: boolean, y_offset: number): void;

        /**
         * Paste previously copied notes.
         * @param notation_node the clipboard XML data
         * @param reset_x_offset if `true` `x_offset` used as cursor
         * @param x_offset region start cursor offset
         * @param reset_y_offset if `true` `y_offset` used as cursor
         * @param y_offset region start cursor tone
         * @param match_channel only paste if channel matches
         * @param no_duplicates only paste if current note doesn't exist
         */
        insert_from_clipboard_extended(notation_node: libxml2.Node, reset_x_offset: boolean, x_offset: number, reset_y_offset: boolean, y_offset: number, match_channel: boolean, no_duplicates: boolean): void;

        /**
         * Check selection for note.
         * @param note the {@link AgsAudio.Note} to check for
         * @returns `true` if selected otherwise `false`
         */
        is_note_selected(note: Note): boolean;

        /**
         * Removes `note` from `notation`.
         * @param note the {@link AgsAudio.Note} to remove
         * @param use_selection_list if `true` remove from selection, else from default notation
         */
        remove_note(note: Note, use_selection_list: boolean): void;

        /**
         * Removes one {@link AgsAudio.Note} of notation.
         * @param x offset
         * @param y note
         * @returns `true` if successfully removed note.
         */
        remove_note_at_position(x: number, y: number): boolean;

        /**
         * Remove notes at position of selection.
         * @param x x offset
         * @param y y note tone
         */
        remove_point_from_selection(x: number, y: number): void;

        /**
         * Remove note within region of selection.
         * @param x0 x start offset
         * @param y0 y start tone
         * @param x1 x end offset
         * @param y1 y end tone
         */
        remove_region_from_selection(x0: number, y0: number, x1: number, y1: number): void;

        /**
         * Set audio.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: GObject.Object): void;

        /**
         * Sets audio channel.
         * @param audio_channel the audio channel
         */
        set_audio_channel(audio_channel: number): void;

        /**
         * Set `flags` on `notation`.
         * @param flags the flags
         */
        set_flags(flags: NotationFlags): void;

        /**
         * Sets is minor.
         * @param is_minor is minor
         */
        set_is_minor(is_minor: boolean): void;

        /**
         * Set note by replacing existing.
         * @param note the {@link GLib.List}-struct containing {@link AgsAudio.Note}
         */
        set_note(note: Note[]): void;

        /**
         * Sets sharp flats.
         * @param sharp_flats the sharp flats
         */
        set_sharp_flats(sharp_flats: number): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Test `flags` to be set on `notation`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: NotationFlags): boolean;

        /**
         * Convert `notation` to raw-midi.
         * @param bpm the source bpm
         * @param delay_factor the source delay factor
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         * @param tempo tempo
         * @param buffer_length the return location of buffer length
         * @returns the raw-midi buffer
         */
        to_raw_midi(bpm: number, delay_factor: number, nn: (bigint | number), dd: (bigint | number), cc: (bigint | number), bb: (bigint | number), tempo: (bigint | number), buffer_length: number): number;

        /**
         * Unset `flags` on `notation`.
         * @param flags the flags
         */
        unset_flags(flags: NotationFlags): void;
    }


    namespace Note {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::decay": (pspec: GObject.ParamSpec) => void;
            "notify::feed-x-256th": (pspec: GObject.ParamSpec) => void;
            "notify::feed-x256th": (pspec: GObject.ParamSpec) => void;
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::note-name": (pspec: GObject.ParamSpec) => void;
            "notify::ratio": (pspec: GObject.ParamSpec) => void;
            "notify::release": (pspec: GObject.ParamSpec) => void;
            "notify::rt-attack": (pspec: GObject.ParamSpec) => void;
            "notify::rt-offset": (pspec: GObject.ParamSpec) => void;
            "notify::stream-attack": (pspec: GObject.ParamSpec) => void;
            "notify::stream-delay": (pspec: GObject.ParamSpec) => void;
            "notify::stream-frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::sustain": (pspec: GObject.ParamSpec) => void;
            "notify::x0": (pspec: GObject.ParamSpec) => void;
            "notify::x0-256th": (pspec: GObject.ParamSpec) => void;
            "notify::x0256th": (pspec: GObject.ParamSpec) => void;
            "notify::x1": (pspec: GObject.ParamSpec) => void;
            "notify::x1-256th": (pspec: GObject.ParamSpec) => void;
            "notify::x1256th": (pspec: GObject.ParamSpec) => void;
            "notify::y": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            attack: Ags.Complex;
            decay: Ags.Complex;
            feed_x_256th: boolean;
            feedX256th: boolean;
            frequency: number;
            note_name: string;
            noteName: string;
            ratio: Ags.Complex;
            release: Ags.Complex;
            rt_attack: number;
            rtAttack: number;
            rt_offset: (bigint | number);
            rtOffset: (bigint | number);
            stream_attack: number;
            streamAttack: number;
            stream_delay: number;
            streamDelay: number;
            stream_frame_count: (bigint | number);
            streamFrameCount: (bigint | number);
            sustain: Ags.Complex;
            x0: number;
            x0_256th: number;
            x0256th: number;
            x1: number;
            x1_256th: number;
            x1256th: number;
            y: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Note extends GObject.Object {
        static $gtype: GObject.GType<Note>;

        // Properties
        /**
         * Envelope attack.
         * @since 3.0.0
         */
        get attack(): Ags.Complex;
        set attack(val: Ags.Complex);

        /**
         * Envelope decay.
         * @since 3.0.0
         */
        get decay(): Ags.Complex;
        set decay(val: Ags.Complex);

        /**
         * Feed note x as 256th.
         * @since 6.1.0
         * @default false
         */
        get feed_x_256th(): boolean;
        set feed_x_256th(val: boolean);

        /**
         * Feed note x as 256th.
         * @since 6.1.0
         * @default false
         */
        get feedX256th(): boolean;
        set feedX256th(val: boolean);

        /**
         * The note's frequency.
         * @since 3.0.0
         * @default 0
         */
        get frequency(): number;
        set frequency(val: number);

        /**
         * The note's name.
         * @since 3.0.0
         * @default null
         */
        get note_name(): string;
        set note_name(val: string);

        /**
         * The note's name.
         * @since 3.0.0
         * @default null
         */
        get noteName(): string;
        set noteName(val: string);

        /**
         * Envelope ratio.
         * @since 3.0.0
         */
        get ratio(): Ags.Complex;
        set ratio(val: Ags.Complex);

        /**
         * Envelope release.
         * @since 3.0.0
         */
        get release(): Ags.Complex;
        set release(val: Ags.Complex);

        /**
         * Note realtime attack.
         * @since 3.0.0
         * @default 0
         */
        get rt_attack(): number;
        set rt_attack(val: number);

        /**
         * Note realtime attack.
         * @since 3.0.0
         * @default 0
         */
        get rtAttack(): number;
        set rtAttack(val: number);

        /**
         * Note realtime offset.
         * @since 3.0.0
         * @default 0
         */
        get rt_offset(): number;
        set rt_offset(val: (bigint | number));

        /**
         * Note realtime offset.
         * @since 3.0.0
         * @default 0
         */
        get rtOffset(): number;
        set rtOffset(val: (bigint | number));

        /**
         * The stream's attack.
         * @since 3.0.0
         * @default 0
         */
        get stream_attack(): number;
        set stream_attack(val: number);

        /**
         * The stream's attack.
         * @since 3.0.0
         * @default 0
         */
        get streamAttack(): number;
        set streamAttack(val: number);

        /**
         * The stream's delay.
         * @since 3.0.0
         * @default 0
         */
        get stream_delay(): number;
        set stream_delay(val: number);

        /**
         * The stream's delay.
         * @since 3.0.0
         * @default 0
         */
        get streamDelay(): number;
        set streamDelay(val: number);

        /**
         * The stream's frame count.
         * @since 3.0.0
         * @default 0
         */
        get stream_frame_count(): number;
        set stream_frame_count(val: (bigint | number));

        /**
         * The stream's frame count.
         * @since 3.0.0
         * @default 0
         */
        get streamFrameCount(): number;
        set streamFrameCount(val: (bigint | number));

        /**
         * Envelope sustain.
         * @since 3.0.0
         */
        get sustain(): Ags.Complex;
        set sustain(val: Ags.Complex);

        /**
         * Note offset x0.
         * @since 3.0.0
         * @default 0
         */
        get x0(): number;
        set x0(val: number);

        /**
         * Note offset x0 as 256th.
         * @since 6.1.0
         * @default 0
         */
        get x0_256th(): number;
        set x0_256th(val: number);

        /**
         * Note offset x0 as 256th.
         * @since 6.1.0
         * @default 0
         */
        get x0256th(): number;
        set x0256th(val: number);

        /**
         * Note offset x1.
         * @since 3.0.0
         * @default 0
         */
        get x1(): number;
        set x1(val: number);

        /**
         * Note offset x1 as 256th.
         * @since 6.1.0
         * @default 0
         */
        get x1_256th(): number;
        set x1_256th(val: number);

        /**
         * Note offset x1 as 256th.
         * @since 6.1.0
         * @default 0
         */
        get x1256th(): number;
        set x1256th(val: number);

        /**
         * Note offset y.
         * @since 3.0.0
         * @default 0
         */
        get y(): number;
        set y(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Note.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: NoteFlags;

        key_format: SoundKeyFormat;

        is_minor: boolean;

        sharp_flats: number;

        x: number[];

        x_256th: number[];

        // Constructors
        constructor(properties?: Partial<Note.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Note;

        static new_with_offset(x0: number, x1: number, y: number, stream_delay: number, stream_attack: number): Note;

        // Signals
        /** @signal */
        connect<K extends keyof Note.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Note.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Note.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Note.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Note.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Note.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find next note having the same y offset.
         * @param note the {@link GLib.List}-struct containing {@link AgsAudio.Note}
         * @param x0 x offset
         * @param y y offset
         */
        static find_next(note: Note[], x0: number, y: number): Note[];

        /**
         * Find prev note having the same y offset.
         * @param note the {@link GLib.List}-struct containing {@link AgsAudio.Note}
         * @param x0 x offset
         * @param y y offset
         */
        static find_prev(note: Note[], x0: number, y: number): Note[];

        /**
         * Parse `raw_midi` data and convert to {@link AgsAudio.Note}.
         * @param raw_midi the data array
         * @param bpm the bpm to use
         * @param delay_factor the segmentation delay factor
         * @param length the length of the array
         */
        static from_raw_midi(raw_midi: number, bpm: number, delay_factor: number, length: number): Note[];

        /**
         * Parse `raw_midi` data and convert to {@link AgsAudio.Note}.
         * @param raw_midi the data array
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         * @param tempo tempo
         * @param bpm the bpm to use
         * @param delay_factor the segmentation delay factor
         * @param length the length of the array
         */
        static from_raw_midi_extended(raw_midi: number, nn: (bigint | number), dd: (bigint | number), cc: (bigint | number), bb: (bigint | number), tempo: (bigint | number), bpm: number, delay_factor: number, length: number): Note[];

        /**
         * Convert ALSA sequencer data `event` to {@link AgsAudio.Note}.
         * @param event ALSA sequencer events as array
         * @param bpm the bpm to use
         * @param delay_factor the segmentation delay factor
         * @param n_events the arrays length
         */
        static from_seq_event(event: (any | null), bpm: number, delay_factor: number, n_events: number): Note[];

        /**
         * Parse `raw_midi` data and convert to {@link AgsAudio.Note}.
         * @param event the snd_seq_event_t struct array
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         * @param tempo tempo
         * @param bpm the bpm to use
         * @param delay_factor the segmentation delay factor
         * @param n_events the count snd_seq_event_t structs
         */
        static from_seq_event_extended(event: (any | null), nn: (bigint | number), dd: (bigint | number), cc: (bigint | number), bb: (bigint | number), tempo: (bigint | number), bpm: number, delay_factor: number, n_events: number): Note[];

        /**
         * Convert note length to SMF delta-time.
         * @param note_length the note length to convert
         * @param bpm the source bpm
         * @param delay_factor the source delay factor
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         * @param tempo tempo
         */
        static length_to_smf_delta_time(note_length: number, bpm: number, delay_factor: number, nn: (bigint | number), dd: (bigint | number), cc: (bigint | number), bb: (bigint | number), tempo: (bigint | number)): number;

        /**
         * Convert SMF delta-time to note length.
         * @param delta_time delta-time
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         * @param tempo tempo
         * @param bpm the target bpm
         * @param delay_factor the target delay factor
         */
        static smf_delta_time_to_length(delta_time: (bigint | number), nn: (bigint | number), dd: (bigint | number), cc: (bigint | number), bb: (bigint | number), tempo: (bigint | number), bpm: number, delay_factor: number): number;

        /**
         * Sort notes.
         * @param a the {@link AgsAudio.Note}
         * @param b another {@link AgsAudio.Note}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Duplicate a note.
         * @returns the duplicated {@link AgsAudio.Note}.
         */
        duplicate(): Note;

        /**
         * Gets attack.
         * @returns the attack
         */
        get_attack(): Ags.Complex;

        /**
         * Gets decay.
         * @returns the decay
         */
        get_decay(): Ags.Complex;

        /**
         * Gets feed x as 256th.
         * @returns the feed x as 256th
         */
        get_feed_x_256th(): boolean;

        /**
         * Gets is minor.
         * @returns is minor
         */
        get_is_minor(): boolean;

        /**
         * Gets ratio.
         * @returns the ratio
         */
        get_ratio(): Ags.Complex;

        /**
         * Gets release.
         * @returns the release
         */
        get_release(): Ags.Complex;

        /**
         * Gets rt-attack.
         * @returns the rt-attack
         */
        get_rt_attack(): number;

        /**
         * Gets rt-offset.
         * @returns the rt-offset
         */
        get_rt_offset(): number;

        /**
         * Gets sharp flats.
         * @returns the sharp flats
         */
        get_sharp_flats(): number;

        /**
         * Gets sustain.
         * @returns the sustain
         */
        get_sustain(): Ags.Complex;

        /**
         * Gets x0.
         * @returns the x0
         */
        get_x0(): number;

        /**
         * Gets x0 as 256th.
         * @returns the x0 as 256th
         */
        get_x0_256th(): number;

        /**
         * Gets x1.
         * @returns the x1
         */
        get_x1(): number;

        /**
         * Gets x1 as 256th.
         * @returns the x1 as 256th
         */
        get_x1_256th(): number;

        /**
         * Gets y.
         * @returns the y
         */
        get_y(): number;

        /**
         * Sets attack.
         * @param attack the attack
         */
        set_attack(attack: Ags.Complex): void;

        /**
         * Sets decay.
         * @param decay the decay
         */
        set_decay(decay: Ags.Complex): void;

        /**
         * @param feed_x_256th 
         */
        set_feed_x_256th(feed_x_256th: boolean): void;

        /**
         * Set `flags` on `note`.
         * @param flags the flags
         */
        set_flags(flags: NoteFlags): void;

        /**
         * Sets is minor.
         * @param is_minor is minor
         */
        set_is_minor(is_minor: boolean): void;

        /**
         * Set `key_format` on `note`.
         * @param key_format the key format
         */
        set_key_format(key_format: SoundKeyFormat): void;

        /**
         * Sets ratio.
         * @param ratio the ratio
         */
        set_ratio(ratio: Ags.Complex): void;

        /**
         * Sets release.
         * @param release the release
         */
        set_release(release: Ags.Complex): void;

        /**
         * Sets rt-attack.
         * @param rt_attack the rt-attack
         */
        set_rt_attack(rt_attack: number): void;

        /**
         * Sets rt-offset.
         * @param rt_offset the rt-offset
         */
        set_rt_offset(rt_offset: (bigint | number)): void;

        /**
         * Sets sharp flats.
         * @param sharp_flats the sharp flats
         */
        set_sharp_flats(sharp_flats: number): void;

        /**
         * Sets sustain.
         * @param sustain the sustain
         */
        set_sustain(sustain: Ags.Complex): void;

        /**
         * Sets x0.
         * @param x0 the x0
         */
        set_x0(x0: number): void;

        /**
         * Sets x0 as 256th.
         * @param x0_256th the x0 as 256th
         */
        set_x0_256th(x0_256th: number): void;

        /**
         * Sets x1.
         * @param x1 the x1
         */
        set_x1(x1: number): void;

        /**
         * Sets x1 as 256th.
         * @param x1_256th the x1 as 256th
         */
        set_x1_256th(x1_256th: number): void;

        /**
         * Sets y.
         * @param y the y
         */
        set_y(y: number): void;

        /**
         * Test `flags` to be set on `note`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: NoteFlags): boolean;

        /**
         * Test `key_format` to be set on `note`.
         * @param key_format the key format
         * @returns `true` if key format is set, else `false`
         */
        test_key_format(key_format: SoundKeyFormat): boolean;

        /**
         * Convert `note` to raw MIDI and set the buffer length of returned bytes
         * in the array as `buffer_length`.
         * @param bpm the bpm to use
         * @param delay_factor the segmentation delay factor
         * @param buffer_length the length of the returned buffer
         * @returns The sequencer raw midi as array.
         */
        to_raw_midi(bpm: number, delay_factor: number, buffer_length: number): number;

        /**
         * Convert `note` to raw-midi.
         * @param bpm the source bpm
         * @param delay_factor the source delay factor
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         * @param tempo tempo
         * @param buffer_length the return location of buffer length
         * @returns the raw-midi buffer
         */
        to_raw_midi_extended(bpm: number, delay_factor: number, nn: (bigint | number), dd: (bigint | number), cc: (bigint | number), bb: (bigint | number), tempo: (bigint | number), buffer_length: number): number;

        /**
         * Convert `note` to ALSA sequencer events and set the number of events
         * in the array as `n_events`.
         * @param bpm the bpm to use
         * @param delay_factor the segmentation delay factor
         * @param n_events the count of events
         * @returns The sequencer events as array.
         */
        to_seq_event(bpm: number, delay_factor: number, n_events: number): (any | null);

        /**
         * Convert `note` to raw-midi.
         * @param bpm the source bpm
         * @param delay_factor the source delay factor
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         * @param tempo tempo
         * @param n_events the return location of event count
         * @returns an array of snd_seq_event_t structs
         */
        to_seq_event_extended(bpm: number, delay_factor: number, nn: (bigint | number), dd: (bigint | number), cc: (bigint | number), bb: (bigint | number), tempo: (bigint | number), n_events: number): (any | null);

        /**
         * Unset `flags` on `note`.
         * @param flags the flags
         */
        unset_flags(flags: NoteFlags): void;
    }


    namespace OpenFile {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::create-channels": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::overwrite-channels": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            create_channels: boolean;
            createChannels: boolean;
            filename: string[];
            overwrite_channels: boolean;
            overwriteChannels: boolean;
        }
    }

    /**
     * @gir-type Class
     */
    class OpenFile extends Ags.Task {
        static $gtype: GObject.GType<OpenFile>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * As open files create {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @default false
         */
        get create_channels(): boolean;
        set create_channels(val: boolean);

        /**
         * As open files create {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @default false
         */
        get createChannels(): boolean;
        set createChannels(val: boolean);

        /**
         * The assigned {@link GLib.SList}-struct providing filename as string
         * @since 3.0.0
         */
        get filename(): string[];
        set filename(val: string[]);

        /**
         * As open files overwrite {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @default false
         */
        get overwrite_channels(): boolean;
        set overwrite_channels(val: boolean);

        /**
         * As open files overwrite {@link AgsAudio.Channel}.
         * @since 3.0.0
         * @default false
         */
        get overwriteChannels(): boolean;
        set overwriteChannels(val: boolean);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OpenFile.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<OpenFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, filename: string[], overwrite_channels: boolean, create_channels: boolean): OpenFile;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof OpenFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OpenFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OpenFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OpenFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OpenSFZFile {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::sfz-file": (pspec: GObject.ParamSpec) => void;
            "notify::start-pad": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            filename: string;
            sfz_file: SFZFile;
            sfzFile: SFZFile;
            start_pad: number;
            startPad: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OpenSFZFile extends Ags.Task {
        static $gtype: GObject.GType<OpenSFZFile>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.4.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The assigned filename.
         * @since 3.4.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The assigned {@link AgsAudio.SFZFile}
         * @since 3.4.0
         */
        get sfz_file(): SFZFile;
        set sfz_file(val: SFZFile);

        /**
         * The assigned {@link AgsAudio.SFZFile}
         * @since 3.4.0
         */
        get sfzFile(): SFZFile;
        set sfzFile(val: SFZFile);

        /**
         * The assigned start-pad.
         * @since 3.4.0
         * @default 0
         */
        get start_pad(): number;
        set start_pad(val: number);

        /**
         * The assigned start-pad.
         * @since 3.4.0
         * @default 0
         */
        get startPad(): number;
        set startPad(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OpenSFZFile.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<OpenSFZFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, sfz_file: SFZFile, filename: string, start_pad: number): OpenSFZFile;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof OpenSFZFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenSFZFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OpenSFZFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenSFZFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OpenSFZFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OpenSFZFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OpenSf2Instrument {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::instrument": (pspec: GObject.ParamSpec) => void;
            "notify::ipatch": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
            "notify::start-pad": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            filename: string;
            instrument: string;
            ipatch: Ipatch;
            preset: string;
            start_pad: number;
            startPad: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OpenSf2Instrument extends Ags.Task {
        static $gtype: GObject.GType<OpenSf2Instrument>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The assigned instrument.
         * @since 3.0.0
         * @default null
         */
        get instrument(): string;
        set instrument(val: string);

        /**
         * The assigned {@link AgsAudio.Ipatch}
         * @since 3.0.0
         */
        get ipatch(): Ipatch;
        set ipatch(val: Ipatch);

        /**
         * The assigned preset.
         * @since 3.0.0
         * @default null
         */
        get preset(): string;
        set preset(val: string);

        /**
         * The assigned start-pad.
         * @since 3.0.0
         * @default 0
         */
        get start_pad(): number;
        set start_pad(val: number);

        /**
         * The assigned start-pad.
         * @since 3.0.0
         * @default 0
         */
        get startPad(): number;
        set startPad(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OpenSf2Instrument.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<OpenSf2Instrument.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, ipatch: Ipatch, filename: string, preset: string, instrument: string, start_pad: number): OpenSf2Instrument;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof OpenSf2Instrument.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenSf2Instrument.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OpenSf2Instrument.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenSf2Instrument.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OpenSf2Instrument.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OpenSf2Instrument.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OpenSf2Sample {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::instrument": (pspec: GObject.ParamSpec) => void;
            "notify::ipatch-sample": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
            "notify::sample": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio_channel: number;
            audioChannel: number;
            channel: Channel;
            filename: string;
            instrument: string;
            ipatch_sample: IpatchSample;
            ipatchSample: IpatchSample;
            preset: string;
            sample: string;
        }
    }

    /**
     * @gir-type Class
     */
    class OpenSf2Sample extends Ags.Task {
        static $gtype: GObject.GType<OpenSf2Sample>;

        // Properties
        /**
         * The nth audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The nth audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.0.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The assigned instrument.
         * @since 3.0.0
         * @default null
         */
        get instrument(): string;
        set instrument(val: string);

        /**
         * The assigned {@link AgsAudio.IpatchSample}
         * @since 3.0.0
         */
        get ipatch_sample(): IpatchSample;
        set ipatch_sample(val: IpatchSample);

        /**
         * The assigned {@link AgsAudio.IpatchSample}
         * @since 3.0.0
         */
        get ipatchSample(): IpatchSample;
        set ipatchSample(val: IpatchSample);

        /**
         * The assigned preset.
         * @since 3.0.0
         * @default null
         */
        get preset(): string;
        set preset(val: string);

        /**
         * The assigned sample.
         * @since 3.0.0
         * @default null
         */
        get sample(): string;
        set sample(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OpenSf2Sample.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<OpenSf2Sample.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel, ipatch_sample: IpatchSample, filename: string, preset: string, instrument: string, sample: string, audio_channel: number): OpenSf2Sample;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof OpenSf2Sample.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenSf2Sample.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OpenSf2Sample.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenSf2Sample.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OpenSf2Sample.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OpenSf2Sample.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OpenSingleFile {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio_channel: number;
            audioChannel: number;
            channel: Channel;
            filename: string;
        }
    }

    /**
     * @gir-type Class
     */
    class OpenSingleFile extends Ags.Task {
        static $gtype: GObject.GType<OpenSingleFile>;

        // Properties
        /**
         * The audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.0.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OpenSingleFile.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<OpenSingleFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel, filename: string, audio_channel: number): OpenSingleFile;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof OpenSingleFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenSingleFile.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OpenSingleFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenSingleFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OpenSingleFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OpenSingleFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OpenWave {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-file": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::start-pad": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            audio_file: AudioFile;
            audioFile: AudioFile;
            filename: string;
            start_pad: number;
            startPad: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OpenWave extends Ags.Task {
        static $gtype: GObject.GType<OpenWave>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The assigned {@link AgsAudio.AudioFile}
         * @since 3.0.0
         */
        get audio_file(): AudioFile;
        set audio_file(val: AudioFile);

        /**
         * The assigned {@link AgsAudio.AudioFile}
         * @since 3.0.0
         */
        get audioFile(): AudioFile;
        set audioFile(val: AudioFile);

        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The assigned start-pad.
         * @since 3.0.0
         * @default 0
         */
        get start_pad(): number;
        set start_pad(val: number);

        /**
         * The assigned start-pad.
         * @since 3.0.0
         * @default 0
         */
        get startPad(): number;
        set startPad(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OpenWave.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<OpenWave.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, audio_file: AudioFile, filename: string, start_pad: number): OpenWave;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof OpenWave.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenWave.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OpenWave.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OpenWave.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OpenWave.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OpenWave.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OscActionController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::run-action signal is emited during get data of action controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "run-action": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscActionController extends OscController {
        static $gtype: GObject.GType<OscActionController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscActionController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        // Constructors
        constructor(properties?: Partial<OscActionController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscActionController;

        // Signals
        /** @signal */
        connect<K extends keyof OscActionController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscActionController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscActionController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscActionController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscActionController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscActionController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Run action.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @virtual
         */
        vfunc_run_action(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        // Methods
        /**
         * Run action.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        run_action(osc_connection: OscConnection, message: number, message_size: number): (any | null);
    }


    namespace OscBuilder {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::append-bundle signal is emited during building bundle.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "append-bundle": (arg0: (any | null), arg1: number, arg2: number, arg3: boolean) => void;
            /**
             * The ::append-message signal is emited during building message.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "append-message": (arg0: (any | null), arg1: (any | null), arg2: (any | null)) => void;
            /**
             * The ::append-packet signal is emited during building packet.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "append-packet": () => void;
            /**
             * The ::append-value signal is emited during building value.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "append-value": (arg0: number, arg1: (any | null)) => void;
            /**
             * The ::on-error signal is emited during building of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "on-error": (arg0: (any | null)) => void;
            /**
             * The ::osc-putc signal is emited during putting char to file.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "osc-putc": (arg0: number) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscBuilder extends GObject.Object {
        static $gtype: GObject.GType<OscBuilder>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscBuilder.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OscBuilderFlags;

        data: number;

        length: number;

        offset: number;

        packet: any[];

        current_type_tag: string;

        offset_type_tag: string;

        // Constructors
        constructor(properties?: Partial<OscBuilder.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscBuilder;

        // Signals
        /** @signal */
        connect<K extends keyof OscBuilder.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscBuilder.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscBuilder.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscBuilder.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscBuilder.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscBuilder.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Append bundle.
         * @param parent_bundle the parent {@link AgsAudio.OscBuilderBundle}-struct or `null`
         * @param tv_secs number of seconds since midnight on January 1, 1900
         * @param tv_fraction fraction of seconds to a precision of about 200 picoseconds
         * @param immediately if `true` apply immediately, otherwise `false` not immediately
         * @virtual
         */
        vfunc_append_bundle(parent_bundle: OscBuilderBundle, tv_secs: number, tv_fraction: number, immediately: boolean): void;

        /**
         * Append message.
         * @param parent_bundle the parent {@link AgsAudio.OscBuilderBundle}-struct or `null`
         * @param address_pattern the address pattern
         * @param type_tag the type tag
         * @virtual
         */
        vfunc_append_message(parent_bundle: OscBuilderBundle, address_pattern: string, type_tag: string): void;

        /**
         * @virtual
         */
        vfunc_append_packet(): void;

        /**
         * Append value.
         * @param message the message
         * @param v_type the type as char
         * @param value the {@link GObject.Value}-struct containinig value
         * @virtual
         */
        vfunc_append_value(message: OscBuilderMessage, v_type: number, value: unknown): void;

        /**
         * Report error.
         * @virtual
         */
        vfunc_on_error(): void;

        /**
         * Put character
         * @param c the character
         * @virtual
         */
        vfunc_osc_putc(c: number): void;

        // Methods
        /**
         * Append bundle.
         * @param parent_bundle the parent {@link AgsAudio.OscBuilderBundle}-struct or `null`
         * @param tv_secs number of seconds since midnight on January 1, 1900
         * @param tv_fraction fraction of seconds to a precision of about 200 picoseconds
         * @param immediately if `true` apply immediately, otherwise `false` not immediately
         */
        append_bundle(parent_bundle: OscBuilderBundle, tv_secs: number, tv_fraction: number, immediately: boolean): void;

        /**
         * Append message.
         * @param parent_bundle the parent {@link AgsAudio.OscBuilderBundle}-struct or `null`
         * @param address_pattern the address pattern
         * @param type_tag the type tag
         */
        append_message(parent_bundle: OscBuilderBundle, address_pattern: string, type_tag: string): void;

        append_packet(): void;

        /**
         * Append value.
         * @param message the message
         * @param v_type the type as char
         * @param value the {@link GObject.Value}-struct containinig value
         */
        append_value(message: OscBuilderMessage, v_type: number, value: (GObject.Value | any)): void;

        /**
         * Build the OSC data.
         */
        build(): void;

        /**
         * Get OSC data of `osc_builder`.
         * @returns the OSC data
         */
        get_data(): number;

        /**
         * @param args 
         */
    // Conflicted with GObject.Object.get_data
        get_data(...args: never[]): any;

        /**
         * Get OSC data of `osc_builder`.
         * @returns the OSC data as array
         */
        get_data_with_length(): [number, number];

        /**
         * Report error.
         */
        on_error(): void;

        /**
         * Put character
         * @param c the character
         */
        osc_putc(c: number): void;
    }


    namespace OscClient {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::connect signal is emited as connection is establishing.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            connect: () => void;
            /**
             * The ::read-bytes signal is emited while read bytes.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "read-bytes": (arg0: (any | null)) => (any | null);
            /**
             * The ::resolve signal is emited during resolve of domain.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            resolve: () => void;
            /**
             * The ::write-bytes signal is emited while write bytes.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "write-bytes": (arg0: (any | null), arg1: number) => (boolean | void);
            "notify::domain": (pspec: GObject.ParamSpec) => void;
            "notify::ip4": (pspec: GObject.ParamSpec) => void;
            "notify::ip6": (pspec: GObject.ParamSpec) => void;
            "notify::server-port": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            domain: string;
            ip4: string;
            ip6: string;
            server_port: number;
            serverPort: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OscClient extends GObject.Object {
        static $gtype: GObject.GType<OscClient>;

        // Properties
        /**
         * The domain to lookup server.
         * @since 3.0.0
         * @default null
         */
        get domain(): string;
        set domain(val: string);

        /**
         * The IPv4 address as string of the server.
         * @since 3.0.0
         * @default null
         */
        get ip4(): string;
        set ip4(val: string);

        /**
         * The IPv6 address as string of the server.
         * @since 3.0.0
         * @default null
         */
        get ip6(): string;
        set ip6(val: string);

        /**
         * The server port to connect.
         * @since 3.0.0
         * @default 9000
         */
        get server_port(): number;
        set server_port(val: number);

        /**
         * The server port to connect.
         * @since 3.0.0
         * @default 9000
         */
        get serverPort(): number;
        set serverPort(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscClient.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OscClientFlags;

        connectable_flags: Ags.ConnectableFlags;

        ip4_fd: number;

        ip6_fd: number;

        ip4_socket: Gio.Socket;

        ip6_socket: Gio.Socket;

        ip4_address: Gio.SocketAddress;

        ip6_address: Gio.SocketAddress;

        max_retry_count: number;

        retry_delay: any;

        start_time: any;

        cache_data: number;

        cache_data_length: number;

        buffer: number;

        allocated_buffer_size: number;

        read_count: number;

        has_valid_data: boolean;

        timeout_delay: any;

        // Constructors
        constructor(properties?: Partial<OscClient.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscClient;

        // Signals
        /** @signal */
        connect<K extends keyof OscClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscClient.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscClient.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscClient.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscClient.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Connect OSC client.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Read bytes.
         * @param data_length the return location of byte array's length
         * @virtual
         */
        vfunc_read_bytes(data_length: number): number;

        /**
         * Resolve OSC client.
         * @virtual
         */
        vfunc_resolve(): void;

        /**
         * Write `data` using OSC client.
         * @param data the byte array
         * @param data_length the length of byte array
         * @virtual
         */
        vfunc_write_bytes(data: number, data_length: number): boolean;

        // Methods
        /**
         * Connect OSC client.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Read bytes.
         * @param data_length the return location of byte array's length
         * @returns byte array read or `null` if no data available
         */
        read_bytes(data_length: number): number;

        /**
         * Resolve OSC client.
         */
        resolve(): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: OscClientFlags): void;

        /**
         * Test `flags` to be set on `osc_client`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OscClientFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: OscClientFlags): void;

        /**
         * Write `data` using OSC client.
         * @param data the byte array
         * @param data_length the length of byte array
         * @returns `true` if all bytes written, otherwise `false`
         */
        write_bytes(data: number, data_length: number): boolean;
    }


    namespace OscConfigController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::apply-config signal is emited during get data of config controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "apply-config": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscConfigController extends OscController {
        static $gtype: GObject.GType<OscConfigController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscConfigController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        // Constructors
        constructor(properties?: Partial<OscConfigController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscConfigController;

        // Signals
        /** @signal */
        connect<K extends keyof OscConfigController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscConfigController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscConfigController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscConfigController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscConfigController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscConfigController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Apply config.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @virtual
         */
        vfunc_apply_config(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        // Methods
        /**
         * Apply config.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        apply_config(osc_connection: OscConnection, message: number, message_size: number): (any | null);
    }


    namespace OscConnection {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::close signal is emited as closing the file descriptor.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            close: () => void;
            /**
             * The ::read-bytes signal is emited while read bytes.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "read-bytes": (arg0: (any | null)) => (any | null);
            /**
             * The ::write-response signal is emited while write response.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "write-response": (arg0: GObject.Object) => (bigint | number);
            "notify::ip4": (pspec: GObject.ParamSpec) => void;
            "notify::ip6": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            ip4: string;
            ip6: string;
            osc_server: OscServer;
            oscServer: OscServer;
        }
    }

    /**
     * @gir-type Class
     */
    class OscConnection extends GObject.Object {
        static $gtype: GObject.GType<OscConnection>;

        // Properties
        /**
         * The IPv4 address as string of the server connection.
         * @since 3.0.0
         * @default null
         */
        get ip4(): string;
        set ip4(val: string);

        /**
         * The IPv6 address as string of the server connection.
         * @since 3.0.0
         * @default null
         */
        get ip6(): string;
        set ip6(val: string);

        /**
         * The assigned {@link AgsAudio.OscServer}.
         * @since 3.0.0
         */
        get osc_server(): OscServer;
        set osc_server(val: OscServer);

        /**
         * The assigned {@link AgsAudio.OscServer}.
         * @since 3.0.0
         */
        get oscServer(): OscServer;
        set oscServer(val: OscServer);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscConnection.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OscConnectionFlags;

        connectable_flags: Ags.ConnectableFlags;

        fd: number;

        socket: Gio.Socket;

        start_time: any;

        cache_data: number;

        cache_data_length: number;

        buffer: number;

        allocated_buffer_size: number;

        read_count: number;

        has_valid_data: boolean;

        timeout_delay: any;

        timestamp: any;

        // Constructors
        constructor(properties?: Partial<OscConnection.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](osc_server: GObject.Object): OscConnection;

        // Signals
        /** @signal */
        connect<K extends keyof OscConnection.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscConnection.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscConnection.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscConnection.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscConnection.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscConnection.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Check `start_time` plus `timeout_delay` against current time.
         * @param start_time the start time #timespec-struct
         * @param timeout_delay the delay #timespec-struct
         */
        static timeout_expired(start_time: (any | null), timeout_delay: (any | null)): boolean;

        // Virtual methods
        /**
         * Close `osc_connection`.
         * @virtual
         */
        vfunc_close(): void;

        /**
         * Read bytes.
         * @param data_length the return location of byte array's length
         * @virtual
         */
        vfunc_read_bytes(data_length: number): number;

        /**
         * Write response.
         * @param osc_response the {@link AgsAudio.OscResponse}
         * @virtual
         */
        vfunc_write_response(osc_response: GObject.Object): (bigint | number);

        // Methods
        /**
         * Close `osc_connection`.
         */
        close(): void;

        /**
         * Read bytes.
         * @param data_length the return location of byte array's length
         * @returns byte array read or `null` if no data available
         */
        read_bytes(data_length: number): number;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: OscConnectionFlags): void;

        /**
         * Test `flags` to be set on `osc_connection`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OscConnectionFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: OscConnectionFlags): void;

        /**
         * Write response.
         * @param osc_response the {@link AgsAudio.OscResponse}
         * @returns the count of bytes written
         */
        write_response(osc_response: GObject.Object): number;
    }


    namespace OscController {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            context_path: string;
            contextPath: string;
            osc_server: OscServer;
            oscServer: OscServer;
        }
    }

    /**
     * @gir-type Class
     */
    class OscController extends GObject.Object {
        static $gtype: GObject.GType<OscController>;

        // Properties
        /**
         * The context path provided.
         * @since 3.0.0
         * @default null
         */
        get context_path(): string;
        set context_path(val: string);

        /**
         * The context path provided.
         * @since 3.0.0
         * @default null
         */
        get contextPath(): string;
        set contextPath(val: string);

        /**
         * The assigned {@link AgsAudio.OscServer}
         * @since 3.0.0
         */
        get osc_server(): OscServer;
        set osc_server(val: OscServer);

        /**
         * The assigned {@link AgsAudio.OscServer}
         * @since 3.0.0
         */
        get oscServer(): OscServer;
        set oscServer(val: OscServer);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscController.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<OscController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscController;

        // Signals
        /** @signal */
        connect<K extends keyof OscController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OscExportController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::do-export signal is emited during get data of export controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "do-export": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscExportController extends OscController {
        static $gtype: GObject.GType<OscExportController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscExportController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        // Constructors
        constructor(properties?: Partial<OscExportController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscExportController;

        // Signals
        /** @signal */
        connect<K extends keyof OscExportController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscExportController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscExportController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscExportController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscExportController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscExportController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Run export.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @virtual
         */
        vfunc_do_export(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        // Methods
        /**
         * Run export.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        do_export(osc_connection: OscConnection, message: number, message_size: number): (any | null);
    }


    namespace OscFrontController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::do-request signal is emited during do request of front controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "do-request": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            /**
             * The ::start-delegate signal is emited during start of delegating messages.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "start-delegate": () => void;
            /**
             * The ::stop-delegate signal is emited during stop of delegating messages.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "stop-delegate": () => void;
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscFrontController extends OscController {
        static $gtype: GObject.GType<OscFrontController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscFrontController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        flags: OscFrontControllerFlags;

        delegate_timeout: number;

        do_reset: boolean;

        message: any[];

        // Constructors
        constructor(properties?: Partial<OscFrontController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscFrontController;

        // Signals
        /** @signal */
        connect<K extends keyof OscFrontController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscFrontController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscFrontController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscFrontController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscFrontController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscFrontController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @param osc_connection 
         * @param packet 
         * @param packet_size 
         * @virtual
         */
        vfunc_do_request(osc_connection: OscConnection, packet: number, packet_size: number): (any | null);

        /**
         * Start delegating.
         * @virtual
         */
        vfunc_start_delegate(): void;

        /**
         * Stop delegating.
         * @virtual
         */
        vfunc_stop_delegate(): void;

        // Methods
        /**
         * Add `message` to `osc_front_controller`.
         * @param message the {@link AgsAudio.OscMessage}
         */
        add_message(message: GObject.Object): void;

        /**
         * @param osc_connection 
         * @param packet 
         * @param packet_size 
         */
        do_request(osc_connection: OscConnection, packet: number, packet_size: (bigint | number)): (any | null);

        /**
         * Remove `message` from `osc_front_controller`.
         * @param message the {@link AgsAudio.OscMessage}
         */
        remove_message(message: GObject.Object): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: OscFrontControllerFlags): void;

        /**
         * Start delegating.
         */
        start_delegate(): void;

        /**
         * Stop delegating.
         */
        stop_delegate(): void;

        /**
         * Test `flags` to be set on `osc_front_controller`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OscFrontControllerFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: OscFrontControllerFlags): void;
    }


    namespace OscInfoController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::get-info signal is emited during get info of info controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "get-info": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscInfoController extends OscController {
        static $gtype: GObject.GType<OscInfoController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscInfoController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        // Constructors
        constructor(properties?: Partial<OscInfoController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscInfoController;

        // Signals
        /** @signal */
        connect<K extends keyof OscInfoController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscInfoController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscInfoController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscInfoController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscInfoController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscInfoController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Get info.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @virtual
         */
        vfunc_get_info(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        // Methods
        /**
         * Get info.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        get_info(osc_connection: OscConnection, message: number, message_size: number): (any | null);
    }


    namespace OscMessage {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::immediately": (pspec: GObject.ParamSpec) => void;
            "notify::message": (pspec: GObject.ParamSpec) => void;
            "notify::message-size": (pspec: GObject.ParamSpec) => void;
            "notify::osc-connection": (pspec: GObject.ParamSpec) => void;
            "notify::tv-fraction": (pspec: GObject.ParamSpec) => void;
            "notify::tv-sec": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            immediately: boolean;
            message: any;
            message_size: number;
            messageSize: number;
            osc_connection: OscConnection;
            oscConnection: OscConnection;
            tv_fraction: number;
            tvFraction: number;
            tv_sec: number;
            tvSec: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OscMessage extends GObject.Object {
        static $gtype: GObject.GType<OscMessage>;

        // Properties
        /**
         * The immediately switch.
         * @since 3.0.0
         * @default false
         */
        get immediately(): boolean;
        set immediately(val: boolean);

        /**
         * The message data.
         * @since 3.0.0
         */
        get message(): any;
        set message(val: any);

        /**
         * The size of message in bytes.
         * @since 3.0.0
         * @default 0
         */
        get message_size(): number;
        set message_size(val: number);

        /**
         * The size of message in bytes.
         * @since 3.0.0
         * @default 0
         */
        get messageSize(): number;
        set messageSize(val: number);

        /**
         * The assigned {@link AgsAudio.OscConnection}.
         * @since 3.0.0
         */
        get osc_connection(): OscConnection;
        set osc_connection(val: OscConnection);

        /**
         * The assigned {@link AgsAudio.OscConnection}.
         * @since 3.0.0
         */
        get oscConnection(): OscConnection;
        set oscConnection(val: OscConnection);

        /**
         * The time value's fraction.
         * @since 3.0.0
         * @default 0
         */
        get tv_fraction(): number;
        set tv_fraction(val: number);

        /**
         * The time value's fraction.
         * @since 3.0.0
         * @default 0
         */
        get tvFraction(): number;
        set tvFraction(val: number);

        /**
         * The time value in seconds.
         * @since 3.0.0
         * @default 0
         */
        get tv_sec(): number;
        set tv_sec(val: number);

        /**
         * The time value in seconds.
         * @since 3.0.0
         * @default 0
         */
        get tvSec(): number;
        set tvSec(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscMessage.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        // Constructors
        constructor(properties?: Partial<OscMessage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscMessage;

        // Signals
        /** @signal */
        connect<K extends keyof OscMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscMessage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscMessage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscMessage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscMessage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * @param a 
         * @param b 
         */
        static sort_func(a: (any | null), b: (any | null)): number;
    }


    namespace OscMeterController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::monitor-meter signal is emited during get data of meter controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "monitor-meter": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            /**
             * The ::start-monitor signal is emited during start of monitoring meter.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "start-monitor": () => void;
            /**
             * The ::stop-monitor signal is emited during stop of monitoring meter.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "stop-monitor": () => void;
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscMeterController extends OscController {
        static $gtype: GObject.GType<OscMeterController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscMeterController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        flags: OscMeterControllerFlags;

        monitor: any[];

        // Constructors
        constructor(properties?: Partial<OscMeterController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscMeterController;

        // Signals
        /** @signal */
        connect<K extends keyof OscMeterController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscMeterController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscMeterController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscMeterController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscMeterController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscMeterController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Get meter.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @virtual
         */
        vfunc_monitor_meter(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        /**
         * Start monitoring.
         * @virtual
         */
        vfunc_start_monitor(): void;

        /**
         * Stop monitoring.
         * @virtual
         */
        vfunc_stop_monitor(): void;

        // Methods
        /**
         * @param monitor 
         */
        add_monitor(monitor: OscMeterControllerMonitor): void;

        /**
         * @param osc_connection 
         * @param port 
         */
        contains_monitor(osc_connection: OscConnection, port: Port): boolean;

        /**
         * Get meter.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        monitor_meter(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        /**
         * @param monitor 
         */
        remove_monitor(monitor: OscMeterControllerMonitor): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: OscMeterControllerFlags): void;

        /**
         * Start monitoring.
         */
        start_monitor(): void;

        /**
         * Stop monitoring.
         */
        stop_monitor(): void;

        /**
         * Test `flags` to be set on `osc_meter_controller`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OscMeterControllerFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: OscMeterControllerFlags): void;
    }


    namespace OscNodeController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::get-data signal is emited during get data of node controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "get-data": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscNodeController extends OscController {
        static $gtype: GObject.GType<OscNodeController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscNodeController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        // Constructors
        constructor(properties?: Partial<OscNodeController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscNodeController;

        // Signals
        /** @signal */
        connect<K extends keyof OscNodeController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscNodeController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscNodeController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscNodeController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscNodeController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscNodeController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Get node.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @virtual
         */
        vfunc_get_data(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        // Methods
        /**
         * Get node.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        get_data(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        /**
         * @param args 
         */
    // Conflicted with GObject.Object.get_data
        get_data(...args: never[]): any;
    }


    namespace OscParser {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::bundle signal is emited during parsing.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            bundle: () => (any | null);
            /**
             * The ::message signal is emited during parsing.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            message: () => (any | null);
            /**
             * The ::on-error signal is emited as error occurs.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "on-error": (arg0: (any | null)) => void;
            /**
             * The ::osc-getc signal is emited during parsing of event.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "osc-getc": () => number;
            /**
             * The ::packet signal is emited during parsing.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            packet: () => (any | null);
            /**
             * The ::parse-bytes signal is emited during parsing of bytes.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "parse-bytes": (arg0: (any | null), arg1: number) => (any | null);
            /**
             * The ::parse-full signal is emited during parsing of osc file.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "parse-full": () => (any | null);
            /**
             * The ::value signal is emited during parsing.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            value: (arg0: number) => (any | null);
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscParser extends GObject.Object {
        static $gtype: GObject.GType<OscParser>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscParser.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OscParserFlags;

        buffer: number;

        file_length: number;

        offset: number;

        start_offset: number;

        packet_size: number;

        // Constructors
        constructor(properties?: Partial<OscParser.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscParser;

        // Signals
        /** @signal */
        connect<K extends keyof OscParser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscParser.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscParser.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscParser.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscParser.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscParser.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Parse OSC bundle.
         * @virtual
         */
        vfunc_bundle(): libxml2.Node;

        /**
         * Parse OSC message.
         * @virtual
         */
        vfunc_message(): libxml2.Node;

        /**
         * On error event.
         * @virtual
         */
        vfunc_on_error(): void;

        /**
         * Read byte.
         * @virtual
         */
        vfunc_osc_getc(): number;

        /**
         * Parse OSC packet.
         * @virtual
         */
        vfunc_packet(): libxml2.Node;

        /**
         * Parse bytes.
         * @param osc_buffer the data buffer
         * @param buffer_length the length of data buffer
         * @virtual
         */
        vfunc_parse_bytes(osc_buffer: number, buffer_length: number): libxml2.Node;

        /**
         * Parse full document.
         * @virtual
         */
        vfunc_parse_full(): libxml2.Doc;

        /**
         * Parse OSC value.
         * @param v_type value type
         * @virtual
         */
        vfunc_value(v_type: number): libxml2.Node;

        // Methods
        /**
         * Parse OSC bundle.
         * @returns the parsed XML node
         */
        bundle(): libxml2.Node;

        /**
         * Parse OSC message.
         * @returns the parsed XML node
         */
        message(): libxml2.Node;

        /**
         * On error event.
         */
        on_error(): void;

        /**
         * Read byte.
         * @returns the byte read
         */
        osc_getc(): number;

        /**
         * Parse OSC packet.
         * @returns the parsed XML node
         */
        packet(): libxml2.Node;

        /**
         * Parse bytes.
         * @param osc_buffer the data buffer
         * @param buffer_length the length of data buffer
         * @returns the parsed XML node
         */
        parse_bytes(osc_buffer: number, buffer_length: number): libxml2.Node;

        /**
         * Parse full document.
         * @returns the parsed XML doc
         */
        parse_full(): libxml2.Doc;

        /**
         * Read double precision floating point value.
         * @returns the gdouble read
         */
        read_gdouble(): number;

        /**
         * Read floating point value.
         * @returns the gfloat read
         */
        read_gfloat(): number;

        /**
         * Read 32 bit integer.
         * @returns the gint32 read
         */
        read_gint32(): number;

        /**
         * Read 64 bit integer.
         * @returns the gint64 read
         */
        read_gint64(): number;

        /**
         * Read text.
         * @param length the length
         * @returns the text read as string
         */
        read_text(length: number): string;

        /**
         * Parse OSC value.
         * @param v_type value type
         * @returns the parsed XML node
         */
        value(v_type: number): libxml2.Node;
    }


    namespace OscRenewController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::set-data signal is emited during get data of renew controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "set-data": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscRenewController extends OscController {
        static $gtype: GObject.GType<OscRenewController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscRenewController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        // Constructors
        constructor(properties?: Partial<OscRenewController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscRenewController;

        // Signals
        /** @signal */
        connect<K extends keyof OscRenewController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscRenewController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscRenewController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscRenewController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscRenewController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscRenewController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Renew data.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @virtual
         */
        vfunc_set_data(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        // Methods
        /**
         * Renew data.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        set_data(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        /**
         * @param args 
         */
    // Conflicted with GObject.Object.set_data
        set_data(...args: never[]): any;
    }


    namespace OscResponse {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::error-message": (pspec: GObject.ParamSpec) => void;
            "notify::osc-message": (pspec: GObject.ParamSpec) => void;
            "notify::packet": (pspec: GObject.ParamSpec) => void;
            "notify::packet-size": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            error_message: string;
            errorMessage: string;
            osc_message: OscMessage;
            oscMessage: OscMessage;
            packet: any;
            packet_size: number;
            packetSize: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OscResponse extends GObject.Object {
        static $gtype: GObject.GType<OscResponse>;

        // Properties
        /**
         * The error message.
         * @since 3.0.0
         * @default null
         */
        get error_message(): string;
        set error_message(val: string);

        /**
         * The error message.
         * @since 3.0.0
         * @default null
         */
        get errorMessage(): string;
        set errorMessage(val: string);

        /**
         * The assigned {@link AgsAudio.OscMessage}
         * @since 3.0.0
         */
        get osc_message(): OscMessage;
        set osc_message(val: OscMessage);

        /**
         * The assigned {@link AgsAudio.OscMessage}
         * @since 3.0.0
         */
        get oscMessage(): OscMessage;
        set oscMessage(val: OscMessage);

        /**
         * The response packet.
         * @since 3.0.0
         */
        get packet(): any;
        set packet(val: any);

        /**
         * The response packet's size.
         * @since 3.0.0
         * @default 0
         */
        get packet_size(): number;
        set packet_size(val: number);

        /**
         * The response packet's size.
         * @since 3.0.0
         * @default 0
         */
        get packetSize(): number;
        set packetSize(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscResponse.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OscResponseFlags;

        connectable_flags: Ags.ConnectableFlags;

        creation_time: any;

        // Constructors
        constructor(properties?: Partial<OscResponse.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscResponse;

        // Signals
        /** @signal */
        connect<K extends keyof OscResponse.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscResponse.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscResponse.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscResponse.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscResponse.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscResponse.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: OscResponseFlags): void;

        /**
         * Test `flags` to be set on `osc_response`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OscResponseFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: OscResponseFlags): void;
    }


    namespace OscServer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::dispatch signal is emited during dispatch of server.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            dispatch: () => void;
            /**
             * The ::listen signal is emited during listen of server.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            listen: () => (boolean | void);
            /**
             * The ::start signal is emited during start of server.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            start: () => void;
            /**
             * The ::stop signal is emited during stop of server.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            stop: () => void;
            "notify::connection": (pspec: GObject.ParamSpec) => void;
            "notify::controller": (pspec: GObject.ParamSpec) => void;
            "notify::domain": (pspec: GObject.ParamSpec) => void;
            "notify::front-controller": (pspec: GObject.ParamSpec) => void;
            "notify::ip4": (pspec: GObject.ParamSpec) => void;
            "notify::ip6": (pspec: GObject.ParamSpec) => void;
            "notify::server-port": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            connection: OscConnection[];
            controller: OscController[];
            domain: string;
            front_controller: OscFrontController;
            frontController: OscFrontController;
            ip4: string;
            ip6: string;
            server_port: number;
            serverPort: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OscServer extends GObject.Object {
        static $gtype: GObject.GType<OscServer>;

        // Properties
        /**
         * The assigned {@link AgsAudio.OscConnection} providing default settings.
         * @since 3.0.0
         */
        get connection(): OscConnection[];
        set connection(val: OscConnection[]);

        /**
         * The assigned {@link AgsAudio.OscController} providing default settings.
         * @since 3.0.0
         */
        get controller(): OscController[];
        set controller(val: OscController[]);

        /**
         * The domain to use.
         * @since 3.0.0
         * @default null
         */
        get domain(): string;
        set domain(val: string);

        /**
         * The assigned {@link AgsAudio.OscFrontController}.
         * @since 3.0.0
         */
        get front_controller(): OscFrontController;
        set front_controller(val: OscFrontController);

        /**
         * The assigned {@link AgsAudio.OscFrontController}.
         * @since 3.0.0
         */
        get frontController(): OscFrontController;
        set frontController(val: OscFrontController);

        /**
         * The IPv4 address as string of the server.
         * @since 3.0.0
         * @default null
         */
        get ip4(): string;
        set ip4(val: string);

        /**
         * The IPv6 address as string of the server.
         * @since 3.0.0
         * @default null
         */
        get ip6(): string;
        set ip6(val: string);

        /**
         * The server port to use.
         * @since 3.0.0
         * @default 9000
         */
        get server_port(): number;
        set server_port(val: number);

        /**
         * The server port to use.
         * @since 3.0.0
         * @default 9000
         */
        get serverPort(): number;
        set serverPort(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscServer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OscServerFlags;

        connectable_flags: Ags.ConnectableFlags;

        ip4_fd: number;

        ip6_fd: number;

        ip4_socket: Gio.Socket;

        ip6_socket: Gio.Socket;

        ip4_address: Gio.SocketAddress;

        ip6_address: Gio.SocketAddress;

        accept_delay: any;

        dispatch_delay: any;

        // Constructors
        constructor(properties?: Partial<OscServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscServer;

        // Signals
        /** @signal */
        connect<K extends keyof OscServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscServer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Dispatch the OSC server.
         * @virtual
         */
        vfunc_dispatch(): void;

        /**
         * Listen as OSC server.
         * @virtual
         */
        vfunc_listen(): boolean;

        /**
         * Start OSC server.
         * @virtual
         */
        vfunc_start(): void;

        /**
         * Stop OSC server.
         * @virtual
         */
        vfunc_stop(): void;

        // Methods
        /**
         * Add `osc_connection` to `osc_server`.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         */
        add_connection(osc_connection: GObject.Object): void;

        /**
         * Add `osc_controller` to `osc_server`.
         * @param osc_controller the {@link AgsAudio.OscController}
         */
        add_controller(osc_controller: GObject.Object): void;

        /**
         * Add default controlers to `osc_server`.
         */
        add_default_controller(): void;

        /**
         * Dispatch the OSC server.
         */
        dispatch(): void;

        /**
         * Listen as OSC server.
         * @returns `true` as a new connection was initiated, otherwise `false`
         */
        listen(): boolean;

        /**
         * Remove `osc_connection` from `osc_server`.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         */
        remove_connection(osc_connection: GObject.Object): void;

        /**
         * Remove `osc_controller` from `osc_server`.
         * @param osc_controller the {@link AgsAudio.OscController}
         */
        remove_controller(osc_controller: GObject.Object): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: OscServerFlags): void;

        /**
         * Start OSC server.
         */
        start(): void;

        /**
         * Stop OSC server.
         */
        stop(): void;

        /**
         * Test `flags` to be set on `osc_server`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OscServerFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: OscServerFlags): void;
    }


    namespace OscStatusController {
        // Signal signatures
        interface SignalSignatures extends OscController.SignalSignatures {
            /**
             * The ::get-status signal is emited during get status of status controller.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "get-status": (arg0: GObject.Object, arg1: (any | null), arg2: number) => (any | null);
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscController.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class OscStatusController extends OscController {
        static $gtype: GObject.GType<OscStatusController>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscStatusController.SignalSignatures;

        // Fields
        osc_controller: OscController;

        // Constructors
        constructor(properties?: Partial<OscStatusController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscStatusController;

        // Signals
        /** @signal */
        connect<K extends keyof OscStatusController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscStatusController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscStatusController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscStatusController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscStatusController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscStatusController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Get status.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @virtual
         */
        vfunc_get_status(osc_connection: OscConnection, message: number, message_size: number): (any | null);

        // Methods
        /**
         * Get status.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the message received
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        get_status(osc_connection: OscConnection, message: number, message_size: number): (any | null);
    }


    namespace OscWebsocketConnection {
        // Signal signatures
        interface SignalSignatures extends OscConnection.SignalSignatures {
            "notify::login": (pspec: GObject.ParamSpec) => void;
            "notify::resource-id": (pspec: GObject.ParamSpec) => void;
            "notify::security-context": (pspec: GObject.ParamSpec) => void;
            "notify::security-token": (pspec: GObject.ParamSpec) => void;
            "notify::websocket-connection": (pspec: GObject.ParamSpec) => void;
            "notify::ip4": (pspec: GObject.ParamSpec) => void;
            "notify::ip6": (pspec: GObject.ParamSpec) => void;
            "notify::osc-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscConnection.ConstructorProps {
            login: string;
            resource_id: string;
            resourceId: string;
            security_context: Ags.SecurityContext;
            securityContext: Ags.SecurityContext;
            security_token: string;
            securityToken: string;
            websocket_connection: Soup.WebsocketConnection;
            websocketConnection: Soup.WebsocketConnection;
        }
    }

    /**
     * @gir-type Class
     */
    class OscWebsocketConnection extends OscConnection {
        static $gtype: GObject.GType<OscWebsocketConnection>;

        // Properties
        /**
         * The current login.
         * @since 3.0.0
         * @default null
         */
        get login(): string;
        set login(val: string);

        /**
         * The resource ID from a redirect.
         * @since 3.0.0
         * @default null
         */
        get resource_id(): string;
        set resource_id(val: string);

        /**
         * The resource ID from a redirect.
         * @since 3.0.0
         * @default null
         */
        get resourceId(): string;
        set resourceId(val: string);

        /**
         * The assigned {@link Ags.SecurityContext}.
         * @since 3.0.0
         */
        get security_context(): Ags.SecurityContext;
        set security_context(val: Ags.SecurityContext);

        /**
         * The assigned {@link Ags.SecurityContext}.
         * @since 3.0.0
         */
        get securityContext(): Ags.SecurityContext;
        set securityContext(val: Ags.SecurityContext);

        /**
         * The current security token.
         * @since 3.0.0
         * @default null
         */
        get security_token(): string;
        set security_token(val: string);

        /**
         * The current security token.
         * @since 3.0.0
         * @default null
         */
        get securityToken(): string;
        set securityToken(val: string);

        /**
         * The assigned {@link Soup.Message}.
         * @since 3.0.0
         */
        get websocket_connection(): Soup.WebsocketConnection;
        set websocket_connection(val: Soup.WebsocketConnection);

        /**
         * The assigned {@link Soup.Message}.
         * @since 3.0.0
         */
        get websocketConnection(): Soup.WebsocketConnection;
        set websocketConnection(val: Soup.WebsocketConnection);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscWebsocketConnection.SignalSignatures;

        // Fields
        connection: OscConnection;

        // Constructors
        constructor(properties?: Partial<OscWebsocketConnection.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](osc_server: GObject.Object): OscWebsocketConnection;

        // Signals
        /** @signal */
        connect<K extends keyof OscWebsocketConnection.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscWebsocketConnection.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscWebsocketConnection.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscWebsocketConnection.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscWebsocketConnection.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscWebsocketConnection.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find `resource_id` in `osc_websocket_connection`.
         * @param osc_websocket_connection the {@link GLib.List}-struct containing {@link AgsAudio.OscWebsocketConnection}
         * @param resource_id the resource id
         */
        static find_resource_id(osc_websocket_connection: OscWebsocketConnection[], resource_id: string): OscWebsocketConnection[];
    }


    namespace OscXmlrpcController {
        // Signal signatures
        interface SignalSignatures extends Ags.Controller.SignalSignatures {
            /**
             * The ::start-delegate signal is emited during start of delegating messages.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "start-delegate": () => void;
            /**
             * The ::stop-delegate signal is emited during stop of delegating messages.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "stop-delegate": () => void;
            "notify::osc-xmlrpc-server": (pspec: GObject.ParamSpec) => void;
            "notify::context-path": (pspec: GObject.ParamSpec) => void;
            "notify::server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Controller.ConstructorProps, Ags.PluginController.ConstructorProps {
            osc_xmlrpc_server: OscXmlrpcServer;
            oscXmlrpcServer: OscXmlrpcServer;
        }
    }

    /**
     * @gir-type Class
     */
    class OscXmlrpcController extends Ags.Controller implements Ags.PluginController {
        static $gtype: GObject.GType<OscXmlrpcController>;

        // Properties
        /**
         * The assigned {@link AgsAudio.OscXmlrpcServer}
         * @since 3.0.0
         */
        get osc_xmlrpc_server(): OscXmlrpcServer;
        set osc_xmlrpc_server(val: OscXmlrpcServer);

        /**
         * The assigned {@link AgsAudio.OscXmlrpcServer}
         * @since 3.0.0
         */
        get oscXmlrpcServer(): OscXmlrpcServer;
        set oscXmlrpcServer(val: OscXmlrpcServer);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscXmlrpcController.SignalSignatures;

        // Fields
        controller: Ags.Controller;

        flags: OscXmlrpcControllerFlags;

        do_reset: boolean;

        message: any[];

        queued_response: any[];

        // Constructors
        constructor(properties?: Partial<OscXmlrpcController.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscXmlrpcController;

        // Signals
        /** @signal */
        connect<K extends keyof OscXmlrpcController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscXmlrpcController.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscXmlrpcController.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscXmlrpcController.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscXmlrpcController.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscXmlrpcController.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Start delegating.
         * @virtual
         */
        vfunc_start_delegate(): void;

        /**
         * Stop delegating.
         * @virtual
         */
        vfunc_stop_delegate(): void;

        // Methods
        /**
         * Add `message` to `osc_xmlrpc_controller`.
         * @param message the {@link AgsAudio.OscMessage}
         */
        add_message(message: GObject.Object): void;

        /**
         * Remove `message` from `osc_xmlrpc_controller`.
         * @param message the {@link AgsAudio.OscMessage}
         */
        remove_message(message: GObject.Object): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: OscXmlrpcControllerFlags): void;

        /**
         * Start delegating.
         */
        start_delegate(): void;

        /**
         * Stop delegating.
         */
        stop_delegate(): void;

        /**
         * Test `flags` to be set on `osc_xmlrpc_controller`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OscXmlrpcControllerFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: OscXmlrpcControllerFlags): void;

        /**
         * Do request.
         * @param msg the {@link Soup.ServerMessage}
         * @param query the {@link GLib.HashTable}
         * @param security_context the {@link Ags.SecurityContext}
         * @param path the context path to access
         * @param login the login
         * @param security_token the security token
         * @returns the {@link GLib.List}-struct containing `AgsResponse`
         */
        do_request(msg: Soup.ServerMessage, query: ({ [key: string]: any } | GLib.HashTable<any, any>), security_context: GObject.Object, path: string, login: string, security_token: string): (any | null);

        /**
         * Do request.
         * @param msg the {@link Soup.ServerMessage}
         * @param query the {@link GLib.HashTable}
         * @param security_context the {@link Ags.SecurityContext}
         * @param path the context path to access
         * @param login the login
         * @param security_token the security token
         * @virtual
         */
        vfunc_do_request(msg: Soup.ServerMessage, query: GLib.HashTable<any, any>, security_context: GObject.Object, path: string, login: string, security_token: string): (any | null);
    }


    namespace OscXmlrpcMessage {
        // Signal signatures
        interface SignalSignatures extends OscMessage.SignalSignatures {
            "notify::query": (pspec: GObject.ParamSpec) => void;
            "notify::server-msg": (pspec: GObject.ParamSpec) => void;
            "notify::immediately": (pspec: GObject.ParamSpec) => void;
            "notify::message": (pspec: GObject.ParamSpec) => void;
            "notify::message-size": (pspec: GObject.ParamSpec) => void;
            "notify::osc-connection": (pspec: GObject.ParamSpec) => void;
            "notify::tv-fraction": (pspec: GObject.ParamSpec) => void;
            "notify::tv-sec": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscMessage.ConstructorProps {
            query: any;
            server_msg: Soup.ServerMessage;
            serverMsg: Soup.ServerMessage;
        }
    }

    /**
     * @gir-type Class
     */
    class OscXmlrpcMessage extends OscMessage {
        static $gtype: GObject.GType<OscXmlrpcMessage>;

        // Properties
        /**
         * The current query.
         * @since 3.0.0
         */
        get query(): any;
        set query(val: any);

        /**
         * The assigned {@link Soup.Message}.
         * @since 4.0.0
         */
        get server_msg(): Soup.ServerMessage;
        set server_msg(val: Soup.ServerMessage);

        /**
         * The assigned {@link Soup.Message}.
         * @since 4.0.0
         */
        get serverMsg(): Soup.ServerMessage;
        set serverMsg(val: Soup.ServerMessage);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscXmlrpcMessage.SignalSignatures;

        // Fields
        osc_message: OscMessage;

        // Constructors
        constructor(properties?: Partial<OscXmlrpcMessage.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscXmlrpcMessage;

        // Signals
        /** @signal */
        connect<K extends keyof OscXmlrpcMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscXmlrpcMessage.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscXmlrpcMessage.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscXmlrpcMessage.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscXmlrpcMessage.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscXmlrpcMessage.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace OscXmlrpcServer {
        // Signal signatures
        interface SignalSignatures extends OscServer.SignalSignatures {
            "notify::osc-xmlrpc-controller": (pspec: GObject.ParamSpec) => void;
            "notify::xmlrpc-server": (pspec: GObject.ParamSpec) => void;
            "notify::connection": (pspec: GObject.ParamSpec) => void;
            "notify::controller": (pspec: GObject.ParamSpec) => void;
            "notify::domain": (pspec: GObject.ParamSpec) => void;
            "notify::front-controller": (pspec: GObject.ParamSpec) => void;
            "notify::ip4": (pspec: GObject.ParamSpec) => void;
            "notify::ip6": (pspec: GObject.ParamSpec) => void;
            "notify::server-port": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends OscServer.ConstructorProps {
            osc_xmlrpc_controller: Ags.Controller;
            oscXmlrpcController: Ags.Controller;
            xmlrpc_server: Ags.Server;
            xmlrpcServer: Ags.Server;
        }
    }

    /**
     * @gir-type Class
     */
    class OscXmlrpcServer extends OscServer {
        static $gtype: GObject.GType<OscXmlrpcServer>;

        // Properties
        /**
         * The assigned {@link AgsAudio.OscXmlrpcController}.
         * @since 3.0.0
         */
        get osc_xmlrpc_controller(): Ags.Controller;
        set osc_xmlrpc_controller(val: Ags.Controller);

        /**
         * The assigned {@link AgsAudio.OscXmlrpcController}.
         * @since 3.0.0
         */
        get oscXmlrpcController(): Ags.Controller;
        set oscXmlrpcController(val: Ags.Controller);

        /**
         * The assigned {@link Ags.Server}.
         * @since 3.0.0
         */
        get xmlrpc_server(): Ags.Server;
        set xmlrpc_server(val: Ags.Server);

        /**
         * The assigned {@link Ags.Server}.
         * @since 3.0.0
         */
        get xmlrpcServer(): Ags.Server;
        set xmlrpcServer(val: Ags.Server);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OscXmlrpcServer.SignalSignatures;

        // Fields
        osc_server: OscServer;

        // Constructors
        constructor(properties?: Partial<OscXmlrpcServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OscXmlrpcServer;

        // Signals
        /** @signal */
        connect<K extends keyof OscXmlrpcServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscXmlrpcServer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof OscXmlrpcServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OscXmlrpcServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OscXmlrpcServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OscXmlrpcServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        add_default_controller(): void;

        /**
         * Add websocket handler to soup server.
         * @param path the path
         * @param origin the origin
         * @param protocols the protocls as string vector
         * @param callback the callback
         */
        add_websocket_handler(path: string, origin: string, protocols: string, callback: Soup.ServerWebsocketCallback): void;
    }


    namespace OssDevin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OssDevin extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<OssDevin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.13.2
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.13.2
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.13.2
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.13.2
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.13.2
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The soundcard indentifier
         * @since 3.13.2
         * @default /dev/dsp
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.13.2
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.13.2
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.13.2
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.13.2
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.13.2
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.13.2
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OssDevin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OssDevinFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        app_buffer_mode: OssDevinAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        available: boolean;

        backend_buffer_mode: OssDevinBackendBufferMode;

        backend_buffer: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        device_fd: number;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<OssDevin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OssDevin;

        // Signals
        /** @signal */
        connect_after<K extends keyof OssDevin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OssDevin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OssDevin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OssDevin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `oss_devin`.
         * @param flags see {@link AgsAudio.OssDevinFlags}-enum
         */
        set_flags(flags: OssDevinFlags): void;

        /**
         * The buffer flag indicates the currently recorded buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `oss_devin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OssDevinFlags): boolean;

        /**
         * Disable a feature of `oss_devin`.
         * @param flags see {@link AgsAudio.OssDevinFlags}-enum
         */
        unset_flags(flags: OssDevinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace OssDevout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class OssDevout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<OssDevout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.13.2
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.13.2
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.13.2
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.13.2
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.13.2
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The OSS soundcard indentifier
         * @since 3.13.2
         * @default /dev/dsp
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.13.2
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.13.2
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.13.2
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.13.2
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.13.2
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.13.2
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OssDevout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OssDevoutFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        app_buffer_mode: OssDevoutBackendBufferMode;

        sub_block_count: number;

        app_buffer: any;

        available: boolean;

        backend_buffer_mode: number;

        backend_buffer: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        device_fd: number;

        io_channel: any[];

        tag: any[];

        poll_timeout: number;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<OssDevout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OssDevout;

        // Signals
        /** @signal */
        connect_after<K extends keyof OssDevout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OssDevout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OssDevout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OssDevout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `oss_devout`.
         * @param flags see {@link AgsAudio.OssDevoutFlags}-enum
         */
        set_flags(flags: OssDevoutFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `oss_devout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OssDevoutFlags): boolean;

        /**
         * Disable a feature of `oss_devout`.
         * @param flags see {@link AgsAudio.OssDevoutFlags}-enum
         */
        unset_flags(flags: OssDevoutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace OssMidiin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Sequencer.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            delay_factor: number;
            delayFactor: number;
            device: string;
        }
    }

    /**
     * @gir-type Class
     */
    class OssMidiin extends GObject.Object implements Ags.Connectable, Ags.Sequencer {
        static $gtype: GObject.GType<OssMidiin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.13.2
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.13.2
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.13.2
         * @read-only
         */
        get buffer(): any;

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.13.2
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The OSS sequencer indentifier
         * @since 3.13.2
         * @default /dev/midi00
         */
        get device(): string;
        set device(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: OssMidiin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: OssMidiinFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: OssMidiinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: OssMidiinAppBufferMode;

        app_buffer: string;

        app_buffer_size: number[];

        backend_buffer_mode: OssMidiinBackendBufferMode;

        backend_buffer: string;

        backend_buffer_size: number[];

        delay: number;

        latency: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        device_fd: number;

        // Constructors
        constructor(properties?: Partial<OssMidiin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): OssMidiin;

        // Signals
        /** @signal */
        connect_after<K extends keyof OssMidiin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, OssMidiin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof OssMidiin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<OssMidiin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Enable a feature of `oss_midiin`.
         * @param flags see {@link AgsAudio.OssMidiinFlags}-enum
         */
        set_flags(flags: OssMidiinFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `oss_midiin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: OssMidiinFlags): boolean;

        /**
         * Disable a feature of `oss_midiin`.
         * @param flags see {@link AgsAudio.OssMidiinFlags}-enum
         */
        unset_flags(flags: OssMidiinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @param buffer_length the buffer's length
         * @returns current playback buffer
         */
        get_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get future playback buffer.
         * @param buffer_length the buffer's length
         * @returns next playback buffer
         */
        get_next_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset(): number;

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Plays the current buffer of sequencer.
         */
        play(): void;

        /**
         * Initializes the sequencer for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of sequencer.
         */
        record(): void;

        /**
         * Initializes the sequencer for recording.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the sequencer from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @param buffer_length the buffer's length
         * @virtual
         */
        vfunc_get_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get future playback buffer.
         * @param buffer_length the buffer's length
         * @virtual
         */
        vfunc_get_next_buffer(buffer_length: number): (any | null);

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Plays the current buffer of sequencer.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the sequencer for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of sequencer.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the sequencer for recording.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the sequencer from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;
    }


    namespace Output {
        // Signal signatures
        interface SignalSignatures extends Channel.SignalSignatures {
            "notify::absolute-key": (pspec: GObject.ParamSpec) => void;
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::first-recycling": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::key": (pspec: GObject.ParamSpec) => void;
            "notify::last-recycling": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::link": (pspec: GObject.ParamSpec) => void;
            "notify::midi-note": (pspec: GObject.ParamSpec) => void;
            "notify::next": (pspec: GObject.ParamSpec) => void;
            "notify::next-pad": (pspec: GObject.ParamSpec) => void;
            "notify::note-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::note-key": (pspec: GObject.ParamSpec) => void;
            "notify::octave": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::play": (pspec: GObject.ParamSpec) => void;
            "notify::playback": (pspec: GObject.ParamSpec) => void;
            "notify::prev": (pspec: GObject.ParamSpec) => void;
            "notify::prev-pad": (pspec: GObject.ParamSpec) => void;
            "notify::recall": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::recycling-context": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Channel.ConstructorProps, Ags.Connectable.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class Output extends Channel implements Ags.Connectable {
        static $gtype: GObject.GType<Output>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Output.SignalSignatures;

        // Fields
        channel: Channel;

        // Constructors
        constructor(properties?: Partial<Output.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: GObject.Object): Output;

        // Signals
        /** @signal */
        connect_after<K extends keyof Output.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Output.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Output.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Output.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Retrieve first input recycling.
         * @returns the first {@link AgsAudio.Recycling} of {@link AgsAudio.Audio}
         */
        find_first_input_recycling(): Recycling;

        /**
         * Retrieve last input recycling.
         * @returns the last {@link AgsAudio.Recycling} of {@link AgsAudio.Audio}
         */
        find_last_input_recycling(): Recycling;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace Pattern {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::current-bit": (pspec: GObject.ParamSpec) => void;
            "notify::first-index": (pspec: GObject.ParamSpec) => void;
            "notify::offset": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::second-index": (pspec: GObject.ParamSpec) => void;
            "notify::timestamp": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Tactable.ConstructorProps {
            channel: Channel;
            current_bit: boolean;
            currentBit: boolean;
            first_index: number;
            firstIndex: number;
            offset: number;
            port: Port;
            second_index: number;
            secondIndex: number;
            timestamp: Ags.Timestamp;
        }
    }

    /**
     * @gir-type Class
     */
    class Pattern extends GObject.Object implements Ags.Connectable, Ags.Tactable {
        static $gtype: GObject.GType<Pattern>;

        // Properties
        /**
         * The pattern's channel.
         * @since 3.3.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * Offset of current position.
         * @since 3.0.0
         * @read-only
         * @default false
         */
        get current_bit(): boolean;

        /**
         * Offset of current position.
         * @since 3.0.0
         * @read-only
         * @default false
         */
        get currentBit(): boolean;

        /**
         * Selected bank 0.
         * @since 3.0.0
         * @default 0
         */
        get first_index(): number;
        set first_index(val: number);

        /**
         * Selected bank 0.
         * @since 3.0.0
         * @default 0
         */
        get firstIndex(): number;
        set firstIndex(val: number);

        /**
         * Position of pattern.
         * @since 3.0.0
         * @default 0
         */
        get offset(): number;
        set offset(val: number);

        /**
         * The pattern's port.
         * @since 3.0.0
         */
        get port(): Port;
        set port(val: Port);

        /**
         * Selected bank 1.
         * @since 3.0.0
         * @default 0
         */
        get second_index(): number;
        set second_index(val: number);

        /**
         * Selected bank 1.
         * @since 3.0.0
         * @default 0
         */
        get secondIndex(): number;
        set secondIndex(val: number);

        /**
         * The pattern's timestamp.
         * @since 3.0.0
         */
        get timestamp(): Ags.Timestamp;
        set timestamp(val: Ags.Timestamp);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Pattern.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        dim: number[];

        pattern: number;

        i: number;

        j: number;

        bit: number;

        note: Note;

        // Constructors
        constructor(properties?: Partial<Pattern.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Pattern;

        // Signals
        /** @signal */
        connect_after<K extends keyof Pattern.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Pattern.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Pattern.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Pattern.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Retrieve appropriate pattern for timestamp.
         * @param pattern the {@link GLib.List}-struct containing {@link AgsAudio.Pattern}
         * @param timestamp the matching {@link Ags.Timestamp}
         */
        static find_near_timestamp(pattern: Pattern[], timestamp: Ags.Timestamp): Pattern[];

        // Methods
        /**
         * Check for tic to be played.
         * @param i bank index 0
         * @param j bank index 1
         * @param bit the tic to check
         * @returns `true` if tone is enabled.
         */
        get_bit(i: number, j: number, bit: number): boolean;

        /**
         * Get channel.
         * @returns the {@link AgsAudio.Channel}
         */
        get_channel<T = GObject.Object>(): T;

        /**
         * Get the pattern's dimensions.
         */
        get_dim(): [number, number, number];

        /**
         * Get note of `pattern`.
         * @param bit the tic
         * @returns the {@link AgsAudio.Note} at given position or `null`
         */
        get_note(bit: number): Note;

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * @param i 
         * @param j 
         */
        is_empty(i: number, j: number): boolean;

        /**
         * Set channel.
         * @param channel the {@link AgsAudio.Channel}
         */
        set_channel(channel: GObject.Object): void;

        /**
         * Reallocates the pattern's dimensions.
         * @param dim0 bank 0 size
         * @param dim1 bank 1 size
         * @param length amount of beats
         */
        set_dim(dim0: number, dim1: number, length: number): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: number): void;

        /**
         * @param port 
         */
        set_port(port: GObject.Object): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Test `flags` to be set on `pattern`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Toggle tone.
         * @param i bank index 0
         * @param j bank index 1
         * @param bit the tic to toggle
         */
        toggle_bit(i: number, j: number, bit: number): void;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         */
        change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         */
        change_midi_duration(midi_duration: (bigint | number)): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         */
        change_notation_duration(notation_duration: (bigint | number)): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         */
        change_sequencer_duration(sequencer_duration: (bigint | number)): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         */
        change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         */
        change_wave_duration(wave_duration: (bigint | number)): void;

        /**
         * Get bpm.
         * @returns the bpm
         */
        get_bpm(): number;

        /**
         * Get midi duration.
         * @returns the midi duration
         */
        get_midi_duration(): number;

        /**
         * Get notation duration.
         * @returns the notation duration
         */
        get_notation_duration(): number;

        /**
         * Get sequencer duration.
         * @returns the sequencer duration
         */
        get_sequencer_duration(): number;

        /**
         * Get tact.
         * @returns the tact
         */
        get_tact(): number;

        /**
         * Get wave duration.
         * @returns the wave duration
         */
        get_wave_duration(): number;

        /**
         * Change bpm.
         * @param new_bpm the new bpm
         * @param old_bpm the old bpm
         * @virtual
         */
        vfunc_change_bpm(new_bpm: number, old_bpm: number): void;

        /**
         * Change midi duration.
         * @param midi_duration the duration
         * @virtual
         */
        vfunc_change_midi_duration(midi_duration: number): void;

        /**
         * Change notation duration.
         * @param notation_duration the duration
         * @virtual
         */
        vfunc_change_notation_duration(notation_duration: number): void;

        /**
         * Change sequencer duration.
         * @param sequencer_duration the duration
         * @virtual
         */
        vfunc_change_sequencer_duration(sequencer_duration: number): void;

        /**
         * Change tact.
         * @param new_tact the new tact
         * @param old_tact the old tact
         * @virtual
         */
        vfunc_change_tact(new_tact: number, old_tact: number): void;

        /**
         * Change wave duration.
         * @param wave_duration the duration
         * @virtual
         */
        vfunc_change_wave_duration(wave_duration: number): void;

        /**
         * Get bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get midi duration.
         * @virtual
         */
        vfunc_get_midi_duration(): (bigint | number);

        /**
         * Get notation duration.
         * @virtual
         */
        vfunc_get_notation_duration(): (bigint | number);

        /**
         * Get sequencer duration.
         * @virtual
         */
        vfunc_get_sequencer_duration(): (bigint | number);

        /**
         * Get tact.
         * @virtual
         */
        vfunc_get_tact(): number;

        /**
         * Get wave duration.
         * @virtual
         */
        vfunc_get_wave_duration(): (bigint | number);
    }


    namespace Playback {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::play-note": (pspec: GObject.ParamSpec) => void;
            "notify::playback-domain": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio_channel: number;
            audioChannel: number;
            channel: Channel;
            play_note: Note;
            playNote: Note;
            playback_domain: PlaybackDomain;
            playbackDomain: PlaybackDomain;
        }
    }

    /**
     * @gir-type Class
     */
    class Playback extends GObject.Object {
        static $gtype: GObject.GType<Playback>;

        // Properties
        /**
         * The assigned audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The assigned audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The assigned channel.
         * @since 3.0.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * The assigned note.
         * @since 3.0.0
         */
        get play_note(): Note;
        set play_note(val: Note);

        /**
         * The assigned note.
         * @since 3.0.0
         */
        get playNote(): Note;
        set playNote(val: Note);

        /**
         * The parent playback domain.
         * @since 3.0.0
         */
        get playback_domain(): PlaybackDomain;
        set playback_domain(val: PlaybackDomain);

        /**
         * The parent playback domain.
         * @since 3.0.0
         */
        get playbackDomain(): PlaybackDomain;
        set playbackDomain(val: PlaybackDomain);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Playback.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: PlaybackFlags;

        connectable_flags: Ags.ConnectableFlags;

        channel_thread: Ags.Thread;

        recall_id: RecallID;

        // Constructors
        constructor(properties?: Partial<Playback.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: GObject.Object): Playback;

        // Signals
        /** @signal */
        connect<K extends keyof Playback.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Playback.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Playback.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Playback.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Playback.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Playback.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find `channel` in `playback`.
         * @param playback the {@link GLib.List}-struct containing {@link AgsAudio.Playback}
         * @param channel the {@link AgsAudio.Channel}
         */
        static find_channel(playback: Playback[], channel: GObject.Object): Playback;

        // Methods
        /**
         * Get channel thread of appropriate scope.
         * @param sound_scope the scope of the thread to get
         * @returns the matching {@link Ags.Thread} or `null`
         */
        get_channel_thread(sound_scope: number): Ags.Thread;

        /**
         * Get recall id of appropriate scope.
         * @param sound_scope the scope of the recall id to get
         * @returns the matching {@link AgsAudio.RecallID} or `null`
         */
        get_recall_id(sound_scope: number): RecallID;

        /**
         * Set channel thread of appropriate scope.
         * @param thread the {@link AgsAudio.ChannelThread}
         * @param sound_scope the scope of the thread to set
         */
        set_channel_thread(thread: Ags.Thread, sound_scope: number): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: PlaybackFlags): void;

        /**
         * Set recall id of appropriate scope.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @param sound_scope the scope of the recall id to set
         */
        set_recall_id(recall_id: RecallID, sound_scope: number): void;

        /**
         * Test `flags` to be set on `playback`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: PlaybackFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: PlaybackFlags): void;
    }


    namespace PlaybackDomain {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::input-playback": (pspec: GObject.ParamSpec) => void;
            "notify::output-playback": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: GObject.Object;
            input_playback: Playback[];
            inputPlayback: Playback[];
            output_playback: Playback[];
            outputPlayback: Playback[];
        }
    }

    /**
     * @gir-type Class
     */
    class PlaybackDomain extends GObject.Object {
        static $gtype: GObject.GType<PlaybackDomain>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}.
         * @since 3.0.0
         */
        get audio(): GObject.Object;
        set audio(val: GObject.Object);

        /**
         * The assigned input playback.
         * @since 3.0.0
         */
        get input_playback(): Playback[];
        set input_playback(val: Playback[]);

        /**
         * The assigned input playback.
         * @since 3.0.0
         */
        get inputPlayback(): Playback[];
        set inputPlayback(val: Playback[]);

        /**
         * The assigned output playback.
         * @since 3.0.0
         */
        get output_playback(): Playback[];
        set output_playback(val: Playback[]);

        /**
         * The assigned output playback.
         * @since 3.0.0
         */
        get outputPlayback(): Playback[];
        set outputPlayback(val: Playback[]);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PlaybackDomain.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: PlaybackDomainFlags;

        connectable_flags: Ags.ConnectableFlags;

        audio_thread: Ags.Thread;

        // Constructors
        constructor(properties?: Partial<PlaybackDomain.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: GObject.Object): PlaybackDomain;

        // Signals
        /** @signal */
        connect<K extends keyof PlaybackDomain.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PlaybackDomain.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof PlaybackDomain.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PlaybackDomain.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PlaybackDomain.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PlaybackDomain.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Add `playback` for `channel_type` to `playback_domain`.
         * @param playback the {@link AgsAudio.Playback}
         * @param channel_type either an AGS_TYPE_OUTPUT or AGS_TYPE_INPUT
         */
        add_playback(playback: GObject.Object, channel_type: GObject.GType): void;

        /**
         * Get audio thread of specified scope.
         * @param sound_scope the thread's scope
         * @returns the matching {@link Ags.Thread} or `null`
         */
        get_audio_thread(sound_scope: number): Ags.Thread;

        /**
         * Add `playback` for `channel_type` to `playback_domain` at `position`.
         * @param playback the {@link AgsAudio.Playback}
         * @param channel_type either an AGS_TYPE_OUTPUT or AGS_TYPE_INPUT
         * @param position the position
         */
        insert_playback(playback: GObject.Object, channel_type: GObject.GType, position: number): void;

        /**
         * Remove `playback` for `channel_type` of `playback_domain`.
         * @param playback the {@link AgsAudio.Playback}
         * @param channel_type either an AGS_TYPE_OUTPUT or AGS_TYPE_INPUT
         */
        remove_playback(playback: GObject.Object, channel_type: GObject.GType): void;

        /**
         * Set audio thread to specified scope.
         * @param thread the {@link Ags.Thread}
         * @param sound_scope the thread's scope
         */
        set_audio_thread(thread: Ags.Thread, sound_scope: number): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: PlaybackDomainFlags): void;

        /**
         * Test `flags` to be set on `playback_domain`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: PlaybackDomainFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: PlaybackDomainFlags): void;
    }


    namespace PluginPort {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::default-value": (pspec: GObject.ParamSpec) => void;
            "notify::lower-value": (pspec: GObject.ParamSpec) => void;
            "notify::port-index": (pspec: GObject.ParamSpec) => void;
            "notify::port-name": (pspec: GObject.ParamSpec) => void;
            "notify::port-symbol": (pspec: GObject.ParamSpec) => void;
            "notify::scale-point": (pspec: GObject.ParamSpec) => void;
            "notify::scale-steps": (pspec: GObject.ParamSpec) => void;
            "notify::scale-value": (pspec: GObject.ParamSpec) => void;
            "notify::upper-value": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            default_value: any;
            defaultValue: any;
            lower_value: any;
            lowerValue: any;
            port_index: number;
            portIndex: number;
            port_name: string;
            portName: string;
            port_symbol: string;
            portSymbol: string;
            scale_point: any;
            scalePoint: any;
            scale_steps: number;
            scaleSteps: number;
            scale_value: any;
            scaleValue: any;
            upper_value: any;
            upperValue: any;
        }
    }

    /**
     * @gir-type Class
     */
    class PluginPort extends GObject.Object {
        static $gtype: GObject.GType<PluginPort>;

        // Properties
        /**
         * The default value.
         * @since 3.0.0
         */
        get default_value(): any;
        set default_value(val: any);

        /**
         * The default value.
         * @since 3.0.0
         */
        get defaultValue(): any;
        set defaultValue(val: any);

        /**
         * The lower value.
         * @since 3.0.0
         */
        get lower_value(): any;
        set lower_value(val: any);

        /**
         * The lower value.
         * @since 3.0.0
         */
        get lowerValue(): any;
        set lowerValue(val: any);

        /**
         * The assigned port-index.
         * @since 3.0.0
         * @default 0
         */
        get port_index(): number;
        set port_index(val: number);

        /**
         * The assigned port-index.
         * @since 3.0.0
         * @default 0
         */
        get portIndex(): number;
        set portIndex(val: number);

        /**
         * The port's name.
         * @since 3.0.0
         * @default null
         */
        get port_name(): string;
        set port_name(val: string);

        /**
         * The port's name.
         * @since 3.0.0
         * @default null
         */
        get portName(): string;
        set portName(val: string);

        /**
         * The port's symbol.
         * @since 3.0.0
         * @default null
         */
        get port_symbol(): string;
        set port_symbol(val: string);

        /**
         * The port's symbol.
         * @since 3.0.0
         * @default null
         */
        get portSymbol(): string;
        set portSymbol(val: string);

        /**
         * The scale point string vector.
         * @since 3.0.0
         * @read-only
         */
        get scale_point(): any;

        /**
         * The scale point string vector.
         * @since 3.0.0
         * @read-only
         */
        get scalePoint(): any;

        /**
         * The number of scale steps.
         * @since 3.0.0
         * @default 0
         */
        get scale_steps(): number;
        set scale_steps(val: number);

        /**
         * The number of scale steps.
         * @since 3.0.0
         * @default 0
         */
        get scaleSteps(): number;
        set scaleSteps(val: number);

        /**
         * The scale value array.
         * @since 3.0.0
         * @read-only
         */
        get scale_value(): any;

        /**
         * The scale value array.
         * @since 3.0.0
         * @read-only
         */
        get scaleValue(): any;

        /**
         * The upper value.
         * @since 3.0.0
         */
        get upper_value(): any;
        set upper_value(val: any);

        /**
         * The upper value.
         * @since 3.0.0
         */
        get upperValue(): any;
        set upperValue(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PluginPort.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        user_data: any;

        // Constructors
        constructor(properties?: Partial<PluginPort.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): PluginPort;

        // Signals
        /** @signal */
        connect<K extends keyof PluginPort.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PluginPort.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof PluginPort.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PluginPort.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PluginPort.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PluginPort.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find `port_index` within `plugin_port`.
         * @param plugin_port the {@link GLib.List}-struct containing {@link AgsAudio.PluginPort}
         * @param port_index the port index
         */
        static find_port_index(plugin_port: PluginPort[], port_index: number): PluginPort[];

        /**
         * Find `port_symbol` within `plugin_port`.
         * @param plugin_port the {@link GLib.List}-struct containing {@link AgsAudio.PluginPort}
         * @param port_symbol the port symbol
         */
        static find_symbol(plugin_port: PluginPort[], port_symbol: string): PluginPort[];

        // Methods
        /**
         * Get default value.
         * @returns the default value
         */
        get_default_value(): unknown;

        /**
         * Get lower value.
         * @returns the lower value
         */
        get_lower_value(): unknown;

        /**
         * Get port index.
         * @returns the port index
         */
        get_port_index(): number;

        /**
         * Get port name.
         * @returns the port name
         */
        get_port_name(): string;

        /**
         * Get port symbol.
         * @returns the port symbol
         */
        get_port_symbol(): string;

        /**
         * Get scale steps.
         * @returns the scale steps
         */
        get_scale_steps(): number;

        /**
         * Get scale value.
         * @returns the scale value
         */
        get_scale_value(): number;

        /**
         * Get upper value.
         * @returns the upper value
         */
        get_upper_value(): unknown;

        /**
         * Set default value.
         * @param default_value the default value
         */
        set_default_value(default_value: (GObject.Value | any)): void;

        /**
         * Set `flags` on `plugin_port`.
         * @param flags the flags
         */
        set_flags(flags: PluginPortFlags): void;

        /**
         * Set lower value.
         * @param lower_value the lower value
         */
        set_lower_value(lower_value: (GObject.Value | any)): void;

        /**
         * Set port index.
         * @param port_index the port index
         */
        set_port_index(port_index: number): void;

        /**
         * Set port name.
         * @param port_name the port name
         */
        set_port_name(port_name: string): void;

        /**
         * Set port symbol.
         * @param port_symbol the port symbol
         */
        set_port_symbol(port_symbol: string): void;

        /**
         * Set scale point.
         * @param scale_point the scale point
         */
        set_scale_point(scale_point: string): void;

        /**
         * Set scale steps.
         * @param scale_steps the scale steps
         */
        set_scale_steps(scale_steps: number): void;

        /**
         * Set scale value.
         * @param scale_value the scale value
         */
        set_scale_value(scale_value: number): void;

        /**
         * Set upper value.
         * @param upper_value the upper value
         */
        set_upper_value(upper_value: (GObject.Value | any)): void;

        /**
         * Test `flags` to be set on `plugin_port`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: PluginPortFlags): boolean;

        /**
         * Unset `flags` on `plugin_port`.
         * @param flags the flags
         */
        unset_flags(flags: PluginPortFlags): void;
    }


    namespace Port {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::safe-get-property signal is emited while safe get property.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "safe-get-property": (arg0: string, arg1: (any | null)) => void;
            /**
             * The ::safe-read signal is emited while doing safe read operation.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "safe-read": (arg0: (any | null)) => void;
            /**
             * The ::safe-set-property signal is emited while safe set property.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "safe-set-property": (arg0: string, arg1: (any | null)) => void;
            /**
             * The ::safe-write signal is emited while doing safe write operation.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "safe-write": (arg0: (any | null)) => void;
            "notify::automation": (pspec: GObject.ParamSpec) => void;
            "notify::channel-type": (pspec: GObject.ParamSpec) => void;
            "notify::control-port": (pspec: GObject.ParamSpec) => void;
            "notify::conversion": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-name": (pspec: GObject.ParamSpec) => void;
            "notify::plugin-port": (pspec: GObject.ParamSpec) => void;
            "notify::port-value-is-pointer": (pspec: GObject.ParamSpec) => void;
            "notify::port-value-length": (pspec: GObject.ParamSpec) => void;
            "notify::port-value-size": (pspec: GObject.ParamSpec) => void;
            "notify::port-value-type": (pspec: GObject.ParamSpec) => void;
            "notify::specifier": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            automation: Automation[];
            channel_type: GObject.GTypeInput;
            channelType: GObject.GTypeInput;
            control_port: string;
            controlPort: string;
            conversion: Ags.Conversion;
            line: number;
            plugin_name: string;
            pluginName: string;
            plugin_port: PluginPort;
            pluginPort: PluginPort;
            port_value_is_pointer: boolean;
            portValueIsPointer: boolean;
            port_value_length: number;
            portValueLength: number;
            port_value_size: number;
            portValueSize: number;
            port_value_type: GObject.GTypeInput;
            portValueType: GObject.GTypeInput;
            specifier: string;
        }
    }

    /**
     * @gir-type Class
     */
    class Port extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<Port>;

        // Properties
        /**
         * The port's automation.
         * @since 3.0.0
         */
        get automation(): Automation[];
        set automation(val: Automation[]);

        /**
         * The port's channel type.
         * @since 5.4.0
         */
        get channel_type(): GObject.GType;
        set channel_type(val: GObject.GType);

        /**
         * The port's channel type.
         * @since 5.4.0
         */
        get channelType(): GObject.GType;
        set channelType(val: GObject.GType);

        /**
         * The assigned plugin control port.
         * @since 3.0.0
         * @default null
         */
        get control_port(): string;
        set control_port(val: string);

        /**
         * The assigned plugin control port.
         * @since 3.0.0
         * @default null
         */
        get controlPort(): string;
        set controlPort(val: string);

        /**
         * The port's conversion object.
         * @since 3.0.0
         */
        get conversion(): Ags.Conversion;
        set conversion(val: Ags.Conversion);

        /**
         * The port's line.
         * @since 5.4.0
         * @default 0
         */
        get line(): number;
        set line(val: number);

        /**
         * The assigned plugin.
         * @since 3.0.0
         * @default null
         */
        get plugin_name(): string;
        set plugin_name(val: string);

        /**
         * The assigned plugin.
         * @since 3.0.0
         * @default null
         */
        get pluginName(): string;
        set pluginName(val: string);

        /**
         * The plugin-port.
         * @since 3.0.0
         */
        get plugin_port(): PluginPort;
        set plugin_port(val: PluginPort);

        /**
         * The plugin-port.
         * @since 3.0.0
         */
        get pluginPort(): PluginPort;
        set pluginPort(val: PluginPort);

        /**
         * Specify port data as pointer.
         * @since 3.0.0
         * @default false
         */
        get port_value_is_pointer(): boolean;
        set port_value_is_pointer(val: boolean);

        /**
         * Specify port data as pointer.
         * @since 3.0.0
         * @default false
         */
        get portValueIsPointer(): boolean;
        set portValueIsPointer(val: boolean);

        /**
         * The port's data array length.
         * @since 3.0.0
         * @default 1
         */
        get port_value_length(): number;
        set port_value_length(val: number);

        /**
         * The port's data array length.
         * @since 3.0.0
         * @default 1
         */
        get portValueLength(): number;
        set portValueLength(val: number);

        /**
         * The port's data type size.
         * @since 3.0.0
         * @default 8
         */
        get port_value_size(): number;
        set port_value_size(val: number);

        /**
         * The port's data type size.
         * @since 3.0.0
         * @default 8
         */
        get portValueSize(): number;
        set portValueSize(val: number);

        /**
         * The port's data type.
         * @since 3.0.0
         */
        get port_value_type(): GObject.GType;
        set port_value_type(val: GObject.GType);

        /**
         * The port's data type.
         * @since 3.0.0
         */
        get portValueType(): GObject.GType;
        set portValueType(val: GObject.GType);

        /**
         * The assigned plugin identifier.
         * @since 3.0.0
         * @default null
         */
        get specifier(): string;
        set specifier(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Port.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: PortFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        port_value: any;

        // Constructors
        constructor(properties?: Partial<Port.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Port;

        // Signals
        /** @signal */
        connect_after<K extends keyof Port.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Port.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Port.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Port.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Retrieve port by plugin port.
         * @param port the {@link GLib.List}-struct containing {@link AgsAudio.Port}
         * @param plugin_port the {@link AgsAudio.PluginPort}
         */
        static find_plugin_port(port: Port[], plugin_port: GObject.Object): Port[];

        /**
         * Retrieve port by specifier.
         * @param port the {@link GLib.List}-struct containing {@link AgsAudio.Port}
         * @param specifier the recall specifier to match
         */
        static find_specifier(port: Port[], specifier: string): Port[];

        // Virtual methods
        /**
         * Perform safe get property.
         * @param property_name the property's name
         * @param value the {@link GObject.Value} to store the result
         * @virtual
         */
        vfunc_safe_get_property(property_name: string, value: unknown): void;

        /**
         * Perform safe read.
         * @param value the {@link GObject.Value} to store result
         * @virtual
         */
        vfunc_safe_read(value: unknown): void;

        /**
         * Perform safe set property.
         * @param property_name the property's name
         * @param value the {@link GObject.Value} containing data
         * @virtual
         */
        vfunc_safe_set_property(property_name: string, value: unknown): void;

        /**
         * Perform safe write.
         * @param value the {@link GObject.Value} containing data
         * @virtual
         */
        vfunc_safe_write(value: unknown): void;

        // Methods
        /**
         * Adds an automation.
         * @param automation the {@link AgsAudio.Automation}
         */
        add_automation(automation: GObject.Object): void;

        /**
         * Get automation.
         * @returns get automation {@link GLib.List}-struct
         */
        get_automation(): Port[];

        /**
         * Get channel type.
         * @returns get channel type
         */
        get_channel_type(): GObject.GType;

        /**
         * Get line.
         * @returns get line
         */
        get_line(): number;

        /**
         * Get the plugin port of `port`.
         * @returns the plugin port
         */
        get_plugin_port<T = GObject.Object>(): T;

        /**
         * Removes an automation.
         * @param automation the {@link AgsAudio.Automation}
         */
        remove_automation(automation: GObject.Object): void;

        /**
         * Perform safe get property.
         * @param property_name the property's name
         * @param value the {@link GObject.Value} to store the result
         */
        safe_get_property(property_name: string, value: (GObject.Value | any)): void;

        /**
         * Perform safe read.
         * @param value the {@link GObject.Value} to store result
         */
        safe_read(value: (GObject.Value | any)): void;

        /**
         * Perform safe read.
         * @param value the {@link GObject.Value} to store result
         */
        safe_read_raw(value: (GObject.Value | any)): void;

        /**
         * Perform safe set property.
         * @param property_name the property's name
         * @param value the {@link GObject.Value} containing data
         */
        safe_set_property(property_name: string, value: (GObject.Value | any)): void;

        /**
         * Perform safe write.
         * @param value the {@link GObject.Value} containing data
         */
        safe_write(value: (GObject.Value | any)): void;

        /**
         * @param value 
         */
        safe_write_raw(value: (GObject.Value | any)): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: PortFlags): void;

        /**
         * Set the plugin port of `port`.
         * @param plugin_port the {@link AgsAudio.PluginPort}
         */
        set_plugin_port(plugin_port: GObject.Object): void;

        /**
         * Test `flags` to be set on `port`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: PortFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: PortFlags): void;

        /**
         * Loads conversion object by using `plugin_port` and sets in on `port`.
         * @param plugin_port the {@link AgsAudio.PluginPort}
         */
        util_load_ladspa_conversion(plugin_port: PluginPort): void;

        /**
         * Loads conversion object by using `plugin_port` and sets in on `port`.
         * @param plugin_port the {@link AgsAudio.PluginPort}
         */
        util_load_lv2_conversion(plugin_port: PluginPort): void;

        /**
         * Loads conversion object by using `plugin_port` and sets in on `port`.
         * @param plugin_port the {@link AgsAudio.PluginPort}
         */
        util_load_vst3_conversion(plugin_port: PluginPort): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace Preset {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel-end": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel-start": (pspec: GObject.ParamSpec) => void;
            "notify::pad-end": (pspec: GObject.ParamSpec) => void;
            "notify::pad-start": (pspec: GObject.ParamSpec) => void;
            "notify::preset-name": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::x-end": (pspec: GObject.ParamSpec) => void;
            "notify::x-start": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: GObject.Object;
            audio_channel_end: number;
            audioChannelEnd: number;
            audio_channel_start: number;
            audioChannelStart: number;
            pad_end: number;
            padEnd: number;
            pad_start: number;
            padStart: number;
            preset_name: string;
            presetName: string;
            scope: string;
            x_end: number;
            xEnd: number;
            x_start: number;
            xStart: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Preset extends GObject.Object {
        static $gtype: GObject.GType<Preset>;

        // Properties
        /**
         * The {@link AgsAudio.Audio} belonging to.
         * @since 3.0.0
         */
        get audio(): GObject.Object;
        set audio(val: GObject.Object);

        /**
         * The end audio channel to apply.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel_end(): number;
        set audio_channel_end(val: number);

        /**
         * The end audio channel to apply.
         * @since 3.0.0
         * @default 0
         */
        get audioChannelEnd(): number;
        set audioChannelEnd(val: number);

        /**
         * The start audio channel to apply.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel_start(): number;
        set audio_channel_start(val: number);

        /**
         * The start audio channel to apply.
         * @since 3.0.0
         * @default 0
         */
        get audioChannelStart(): number;
        set audioChannelStart(val: number);

        /**
         * The end pad to apply.
         * @since 3.0.0
         * @default 0
         */
        get pad_end(): number;
        set pad_end(val: number);

        /**
         * The end pad to apply.
         * @since 3.0.0
         * @default 0
         */
        get padEnd(): number;
        set padEnd(val: number);

        /**
         * The start pad to apply.
         * @since 3.0.0
         * @default 0
         */
        get pad_start(): number;
        set pad_start(val: number);

        /**
         * The start pad to apply.
         * @since 3.0.0
         * @default 0
         */
        get padStart(): number;
        set padStart(val: number);

        /**
         * The preset name.
         * @since 3.0.0
         * @default null
         */
        get preset_name(): string;
        set preset_name(val: string);

        /**
         * The preset name.
         * @since 3.0.0
         * @default null
         */
        get presetName(): string;
        set presetName(val: string);

        /**
         * The preset's scope.
         * @since 3.0.0
         * @default null
         */
        get scope(): string;
        set scope(val: string);

        /**
         * The end x to apply.
         * @since 3.0.0
         * @default 0
         */
        get x_end(): number;
        set x_end(val: number);

        /**
         * The end x to apply.
         * @since 3.0.0
         * @default 0
         */
        get xEnd(): number;
        set xEnd(val: number);

        /**
         * The start x to apply.
         * @since 3.0.0
         * @default 0
         */
        get x_start(): number;
        set x_start(val: number);

        /**
         * The start x to apply.
         * @since 3.0.0
         * @default 0
         */
        get xStart(): number;
        set xStart(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Preset.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        n_params: number;

        parameter_name: string;

        // Constructors
        constructor(properties?: Partial<Preset.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Preset;

        // Signals
        /** @signal */
        connect<K extends keyof Preset.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Preset.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Preset.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Preset.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Preset.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Preset.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        /**
         * Find preset name in `preset`.
         * @param preset the {@link GLib.List}-struct containing {@link AgsAudio.Preset}
         * @param preset_name the preset's name
         */
        static find_name(preset: Preset[], preset_name: string): Preset[];

        /**
         * Find preset's scope in `preset`.
         * @param preset the {@link GLib.List}-struct containing {@link AgsAudio.Preset}
         * @param scope the preset's scope
         */
        static find_scope(preset: Preset[], scope: string): Preset[];

        // Methods
        /**
         * Add parameter to `preset`.
         * @param param_name the parameter name
         * @param value the value to add
         */
        add_parameter(param_name: string, value: (GObject.Value | any)): boolean;

        /**
         * Get audio.
         * @returns the {@link AgsAudio.Audio}
         */
        get_audio<T = GObject.Object>(): T;

        /**
         * Gets audio channel end.
         * @returns the audio channel end
         */
        get_audio_channel_end(): number;

        /**
         * Gets audio channel start.
         * @returns the audio channel start
         */
        get_audio_channel_start(): number;

        /**
         * Gets pad end.
         * @returns the pad end
         */
        get_pad_end(): number;

        /**
         * Gets pad start.
         * @returns the pad start
         */
        get_pad_start(): number;

        /**
         * Get parameter specified by `param_name`. If parameter not available
         * the `error` is set to indicate the failure.
         * @param param_name the parameter name
         * @param value the return location of value
         */
        get_parameter(param_name: string, value: (GObject.Value | any)): void;

        /**
         * Get preset name.
         * @returns the preset name
         */
        get_preset_name(): string;

        /**
         * Get scope.
         * @returns the scope
         */
        get_scope(): string;

        /**
         * Gets x end.
         * @returns the x end
         */
        get_x_end(): number;

        /**
         * Gets x start.
         * @returns the x start
         */
        get_x_start(): number;

        /**
         * Remove parameter of `preset`.
         * @param nth the nth parameter to remove
         */
        remove_parameter(nth: number): void;

        /**
         * Set audio.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: GObject.Object): void;

        /**
         * Sets audio channel end.
         * @param audio_channel_end the audio channel end
         */
        set_audio_channel_end(audio_channel_end: number): void;

        /**
         * Sets audio channel start.
         * @param audio_channel_start the audio channel start
         */
        set_audio_channel_start(audio_channel_start: number): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: number): void;

        /**
         * Sets pad end.
         * @param pad_end the pad end
         */
        set_pad_end(pad_end: number): void;

        /**
         * Sets pad start.
         * @param pad_start the pad start
         */
        set_pad_start(pad_start: number): void;

        /**
         * Set preset name.
         * @param preset_name the preset name
         */
        set_preset_name(preset_name: string): void;

        /**
         * Set scope.
         * @param scope the scope
         */
        set_scope(scope: string): void;

        /**
         * Sets x end.
         * @param x_end the x end
         */
        set_x_end(x_end: number): void;

        /**
         * Sets x start.
         * @param x_start the x start
         */
        set_x_start(x_start: number): void;

        /**
         * Test `flags` to be set on `preset`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: number): void;
    }


    namespace Program {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::control-key": (pspec: GObject.ParamSpec) => void;
            "notify::control-name": (pspec: GObject.ParamSpec) => void;
            "notify::marker": (pspec: GObject.ParamSpec) => void;
            "notify::timestamp": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            control_key: (any | null);
            controlKey: (any | null);
            control_name: string;
            controlName: string;
            marker: Marker[];
            timestamp: Ags.Timestamp;
        }
    }

    /**
     * @gir-type Class
     */
    class Program extends GObject.Object {
        static $gtype: GObject.GType<Program>;

        // Properties
        /**
         * The effect's assigned control key.
         * @since 5.4.0
         */
        get control_key(): (any | null);
        set control_key(val: (any | null));

        /**
         * The effect's assigned control key.
         * @since 5.4.0
         */
        get controlKey(): (any | null);
        set controlKey(val: (any | null));

        /**
         * The program's control-name.
         * @since 5.1.0
         * @default null
         */
        get control_name(): string;
        set control_name(val: string);

        /**
         * The program's control-name.
         * @since 5.1.0
         * @default null
         */
        get controlName(): string;
        set controlName(val: string);

        /**
         * The assigned {@link AgsAudio.Marker}
         * @since 5.1.0
         */
        get marker(): Marker[];
        set marker(val: Marker[]);

        /**
         * The program's timestamp.
         * @since 5.1.0
         */
        get timestamp(): Ags.Timestamp;
        set timestamp(val: Ags.Timestamp);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Program.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: ProgramFlags;

        port: GObject.Object;

        selection: any[];

        // Constructors
        constructor(properties?: Partial<Program.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](control_name: string): Program;

        // Signals
        /** @signal */
        connect<K extends keyof Program.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Program.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Program.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Program.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Program.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Program.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Add `new_program` sorted to `program`
         * @param program the {@link GLib.List}-struct containing {@link AgsAudio.Program}
         * @param new_program the {@link AgsAudio.Program} to add
         */
        static add(program: Program[], new_program: Program): Program[];

        /**
         * Filter `program` by `specifier`.
         * @param program the {@link GLib.List}-struct containing {@link AgsAudio.Program}
         * @param specifier the string specifier to find
         */
        static filter(program: Program[], specifier: string): Program[];

        /**
         * Find port control_name.
         * @param program the {@link GLib.List}-struct containing {@link AgsAudio.Program}
         * @param control_name the string control_name to find
         */
        static find_control_name(program: Program[], control_name: string): Program[];

        /**
         * Retrieve appropriate program for timestamp.
         * @param program the {@link GLib.List}-struct containing {@link AgsAudio.Program}
         * @param timestamp the matching {@link Ags.Timestamp}, or `null` to match any timestamp
         */
        static find_near_timestamp(program: Program[], timestamp: Ags.Timestamp): Program[];

        /**
         * Retrieve appropriate program for control name and timestamp.
         * @param program the {@link GLib.List}-struct containing {@link AgsAudio.Program}
         * @param control_name the control name
         * @param timestamp the matching {@link Ags.Timestamp}, or `null` to match any timestamp
         */
        static find_near_timestamp_extended(program: Program[], control_name: string, timestamp: Ags.Timestamp): Program[];

        /**
         * Clear all selection of `program`.
         * @param program the {@link GLib.List}-struct containing {@link AgsAudio.Program}
         */
        static free_all_selection(program: Program[]): void;

        /**
         * Retrieve program port control_name.
         * @param program the {@link GLib.List}-struct containing {@link AgsAudio.Program}
         */
        static get_control_name_unique(program: Program[]): string[];

        /**
         * Remove all empty `program`.
         * @param program the {@link GLib.List}-struct containing {@link AgsAudio.Program}
         */
        static remove_all_empty(program: Program[]): Program[];

        /**
         * Compare `a` and `b`.
         * @param a the {@link AgsAudio.Program}
         * @param b another {@link AgsAudio.Program}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Select all marker to selection.
         */
        add_all_to_selection(): void;

        /**
         * Add `marker` to `program`.
         * @param marker the {@link AgsAudio.Marker} to add
         * @param use_selection_list if `true` add to selection, else to default program
         */
        add_marker(marker: Marker, use_selection_list: boolean): void;

        /**
         * @param x 
         * @param replace_current_selection 
         */
        add_point_to_selection(x: number, replace_current_selection: boolean): void;

        /**
         * Add marker within region to selection.
         * @param x0 x start offset
         * @param x1 x end offset
         * @param replace_current_selection if `true` selection is replaced
         */
        add_region_to_selection(x0: number, x1: number, replace_current_selection: boolean): void;

        /**
         * Copy selection to clipboard.
         * @returns the selection as XML.
         */
        copy_selection(): libxml2.Node;

        /**
         * Cut selection to clipboard.
         * @returns the selection as xmlNode
         */
        cut_selection(): libxml2.Node;

        /**
         * Find markers by offset.
         * @param x offset
         * @param use_selection_list if `true` selection is searched
         * @returns the matching marker as {@link AgsAudio.Marker}.
         */
        find_point(x: number, use_selection_list: boolean): Marker;

        /**
         * Find markers by offset and region.
         * @param x0 x start offset
         * @param x1 x end offset
         * @param use_selection_list if `true` selection is searched
         * @returns the matching markers as {@link GLib.List}.
         */
        find_region(x0: number, x1: number, use_selection_list: boolean): Marker[];

        /**
         * Clear selection.
         */
        free_selection(): void;

        /**
         * Get control key.
         * @returns the control key
         */
        get_control_key(): (any | null);

        /**
         * Get control name.
         * @returns the control name
         */
        get_control_name(): string;

        /**
         * Get marker.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Marker}
         */
        get_marker(): Marker[];

        /**
         * Get object mutex.
         * @returns the {@link GLib.RecMutex} to lock `program`
         */
        get_obj_mutex(): GLib.RecMutex;

        /**
         * Retrieve selection.
         * @returns the selection.
         */
        get_selection(): Marker[];

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * @param x 
         * @param x_end 
         * @param use_prev_on_failure 
         * @param value 
         */
        get_value(x: number, x_end: number, use_prev_on_failure: boolean, value: (GObject.Value | any)): number;

        /**
         * Paste previously copied markers.
         * @param program_node the clipboard XML data
         * @param reset_x_offset if `true` `x_offset` used as cursor
         * @param x_offset region start cursor offset
         */
        insert_from_clipboard(program_node: libxml2.Node, reset_x_offset: boolean, x_offset: number): void;

        /**
         * Paste previously copied markers.
         * @param program_node the clipboard XML data
         * @param reset_x_offset if `true` `x_offset` used as cursor
         * @param x_offset region start cursor offset
         * @param do_replace if `true` current data is replaced, otherwise combined
         */
        insert_from_clipboard_extended(program_node: libxml2.Node, reset_x_offset: boolean, x_offset: number, do_replace: boolean): void;

        /**
         * Check selection for marker.
         * @param marker the {@link AgsAudio.Marker} to check for
         * @returns `true` if selected otherwise `false`
         */
        is_marker_selected(marker: Marker): boolean;

        /**
         * Removes `marker` from `program`.
         * @param marker the {@link AgsAudio.Marker} to remove
         * @param use_selection_list if `true` remove from selection, else from default program
         */
        remove_marker(marker: Marker, use_selection_list: boolean): void;

        /**
         * Removes one {@link AgsAudio.Marker} of program.
         * @param x x offset
         * @returns `true` if successfully removed marker, else `false`
         */
        remove_marker_at_position(x: number): boolean;

        /**
         * @param x 
         */
        remove_point_from_selection(x: number): void;

        /**
         * Remove markers within region of selection.
         * @param x0 x start offset
         * @param x1 x end offset
         */
        remove_region_from_selection(x0: number, x1: number): void;

        /**
         * @param audio 
         */
        set_audio(audio: GObject.Object): void;

        /**
         * Set control key.
         * @param control_key the control key
         */
        set_control_key(control_key: (any | null)): void;

        /**
         * Set control name.
         * @param control_name the control name
         */
        set_control_name(control_name: string): void;

        /**
         * Set `flags` on `program`.
         * @param flags the flags
         */
        set_flags(flags: ProgramFlags): void;

        /**
         * Set marker by replacing existing.
         * @param marker the {@link GLib.List}-struct containing {@link AgsAudio.Marker}
         */
        set_marker(marker: Marker[]): void;

        /**
         * @param port 
         */
        set_port(port: GObject.Object): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Test `flags` to be set on `program`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: ProgramFlags): boolean;

        /**
         * Unset `flags` on `program`.
         * @param flags the flags
         */
        unset_flags(flags: ProgramFlags): void;
    }


    namespace ProgramControlNameKeyManager {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::control-name-key": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            control_name_key: any;
            controlNameKey: any;
        }
    }

    /**
     * @gir-type Class
     */
    class ProgramControlNameKeyManager extends GObject.Object {
        static $gtype: GObject.GType<ProgramControlNameKeyManager>;

        // Properties
        /**
         * The {@link GLib.HashTable}-struct containing gpointer of key.
         * @since 5.4.0
         */
        get control_name_key(): any;
        set control_name_key(val: any);

        /**
         * The {@link GLib.HashTable}-struct containing gpointer of key.
         * @since 5.4.0
         */
        get controlNameKey(): any;
        set controlNameKey(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ProgramControlNameKeyManager.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        // Constructors
        constructor(properties?: Partial<ProgramControlNameKeyManager.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): ProgramControlNameKeyManager;

        // Signals
        /** @signal */
        connect<K extends keyof ProgramControlNameKeyManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ProgramControlNameKeyManager.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ProgramControlNameKeyManager.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ProgramControlNameKeyManager.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ProgramControlNameKeyManager.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ProgramControlNameKeyManager.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get ags audio file manager instance.
         */
        static get_instance(): ProgramControlNameKeyManager;

        // Methods
        /**
         * @param control_name 
         */
        find_program(control_name: string): (any | null);
    }


    namespace PulseClient {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::client-name": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-server": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            client_name: string;
            clientName: string;
            device: GObject.Object[];
            port: PulsePort[];
            pulse_server: PulseServer;
            pulseServer: PulseServer;
        }
    }

    /**
     * @gir-type Class
     */
    class PulseClient extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<PulseClient>;

        // Properties
        /**
         * The pulseaudio client name.
         * @since 3.0.0
         * @default null
         */
        get client_name(): string;
        set client_name(val: string);

        /**
         * The pulseaudio client name.
         * @since 3.0.0
         * @default null
         */
        get clientName(): string;
        set clientName(val: string);

        /**
         * The assigned devices.
         * @since 3.0.0
         */
        get device(): GObject.Object[];
        set device(val: GObject.Object[]);

        /**
         * The assigned ports.
         * @since 3.0.0
         */
        get port(): PulsePort[];
        set port(val: PulsePort[]);

        /**
         * The assigned {@link AgsAudio.PulseServer}.
         * @since 3.0.0
         */
        get pulse_server(): PulseServer;
        set pulse_server(val: PulseServer);

        /**
         * The assigned {@link AgsAudio.PulseServer}.
         * @since 3.0.0
         */
        get pulseServer(): PulseServer;
        set pulseServer(val: PulseServer);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PulseClient.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: PulseClientFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        client_uuid: string;

        // Constructors
        constructor(properties?: Partial<PulseClient.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](pulse_server: GObject.Object): PulseClient;

        // Signals
        /** @signal */
        connect_after<K extends keyof PulseClient.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PulseClient.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PulseClient.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PulseClient.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `client_name` in `pulse_client`.
         * @param pulse_client the {@link GLib.List}-struct containing {@link AgsAudio.PulseClient}
         * @param client_name the client name to find
         */
        static find(pulse_client: PulseClient[], client_name: string): PulseClient[];

        /**
         * Finds next match of `client_uuid` in `pulse_client`.
         * @param pulse_client the {@link GLib.List}-struct containing {@link AgsAudio.PulseClient}
         * @param client_uuid the client uuid to find
         */
        static find_uuid(pulse_client: PulseClient[], client_uuid: string): PulseClient[];

        // Methods
        /**
         * Activate client.
         */
        activate(): void;

        /**
         * Add `pulse_device` to `pulse_client`.
         * @param pulse_device an {@link AgsAudio.PulseDevout} or {@link AgsAudio.PulseDevin}
         */
        add_device(pulse_device: GObject.Object): void;

        /**
         * Add `pulse_port` to `pulse_client`.
         * @param pulse_port an {@link AgsAudio.PulsePort}
         */
        add_port(pulse_port: GObject.Object): void;

        /**
         * Close the pulseaudio client's connection.
         */
        close(): void;

        /**
         * Deactivate client.
         */
        deactivate(): void;

        /**
         * Open the pulseaudio client's connection and read uuid.
         * @param client_name the client's name
         */
        open(client_name: string): void;

        /**
         * Remove `pulse_device` from `pulse_client`.
         * @param pulse_device an {@link AgsAudio.PulseDevout} or {@link AgsAudio.PulseDevin}
         */
        remove_device(pulse_device: GObject.Object): void;

        /**
         * Remove `pulse_port` from `pulse_client`.
         * @param pulse_port an {@link AgsAudio.PulsePort}
         */
        remove_port(pulse_port: GObject.Object): void;

        /**
         * Enable a feature of `pulse_client`.
         * @param flags see {@link AgsAudio.PulseClientFlags}-enum
         */
        set_flags(flags: PulseClientFlags): void;

        /**
         * Test `flags` to be set on `pulse_client`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: PulseClientFlags): boolean;

        /**
         * Disable a feature of `pulse_client`.
         * @param flags see {@link AgsAudio.PulseClientFlags}-enum
         */
        unset_flags(flags: PulseClientFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace PulseDevin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-client": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-port": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            pulse_client: PulseClient;
            pulseClient: PulseClient;
            pulse_port: any;
            pulsePort: any;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class PulseDevin extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<PulseDevin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The pulse soundcard indentifier
         * @since 3.0.0
         * @default ags-pulse-devin-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The assigned {@link AgsAudio.PulseClient}
         * @since 3.0.0
         */
        get pulse_client(): PulseClient;
        set pulse_client(val: PulseClient);

        /**
         * The assigned {@link AgsAudio.PulseClient}
         * @since 3.0.0
         */
        get pulseClient(): PulseClient;
        set pulseClient(val: PulseClient);

        /**
         * The assigned {@link AgsAudio.PulsePort}
         * @since 3.0.0
         */
        get pulse_port(): any;
        set pulse_port(val: any);

        /**
         * The assigned {@link AgsAudio.PulsePort}
         * @since 3.0.0
         */
        get pulsePort(): any;
        set pulsePort(val: any);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PulseDevin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: PulseDevinFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: PulseDevinSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: PulseDevinAppBufferMode;

        app_buffer: any;

        sub_block_count: number;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        notify_soundcard: GObject.Object;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<PulseDevin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): PulseDevin;

        // Signals
        /** @signal */
        connect_after<K extends keyof PulseDevin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PulseDevin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PulseDevin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PulseDevin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `pulse_devin`.
         * @param flags see {@link AgsAudio.PulseDevinFlags}-enum
         */
        set_flags(flags: PulseDevinFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `pulse_devin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: PulseDevinFlags): boolean;

        /**
         * Disable a feature of `pulse_devin`.
         * @param flags see {@link AgsAudio.PulseDevinFlags}-enum
         */
        unset_flags(flags: PulseDevinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace PulseDevout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-client": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-port": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            pulse_client: PulseClient;
            pulseClient: PulseClient;
            pulse_port: any;
            pulsePort: any;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class PulseDevout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<PulseDevout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The pulse soundcard indentifier
         * @since 3.0.0
         * @default ags-pulse-devout-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The assigned {@link AgsAudio.PulseClient}
         * @since 3.0.0
         */
        get pulse_client(): PulseClient;
        set pulse_client(val: PulseClient);

        /**
         * The assigned {@link AgsAudio.PulseClient}
         * @since 3.0.0
         */
        get pulseClient(): PulseClient;
        set pulseClient(val: PulseClient);

        /**
         * The assigned {@link AgsAudio.PulsePort}
         * @since 3.0.0
         */
        get pulse_port(): any;
        set pulse_port(val: any);

        /**
         * The assigned {@link AgsAudio.PulsePort}
         * @since 3.0.0
         */
        get pulsePort(): any;
        set pulsePort(val: any);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PulseDevout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: PulseDevoutFlags;

        connectable_flags: Ags.ConnectableFlags;

        sync_flags: PulseDevoutSyncFlags;

        uuid: Ags.UUID;

        app_buffer_mode: PulseDevoutAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        card_uri: string;

        port_name: string;

        notify_soundcard: GObject.Object;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<PulseDevout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): PulseDevout;

        // Signals
        /** @signal */
        connect_after<K extends keyof PulseDevout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PulseDevout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PulseDevout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PulseDevout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `pulse_devout`.
         * @param flags see {@link AgsAudio.PulseDevoutFlags}-enum
         */
        set_flags(flags: PulseDevoutFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `pulse_devout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: PulseDevoutFlags): boolean;

        /**
         * Disable a feature of `pulse_devout`.
         * @param flags see {@link AgsAudio.PulseDevoutFlags}-enum
         */
        unset_flags(flags: PulseDevoutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace PulsePort {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::port-name": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-client": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-devin": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-devout": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            port_name: string;
            portName: string;
            pulse_client: PulseClient;
            pulseClient: PulseClient;
            pulse_devin: PulseDevin;
            pulseDevin: PulseDevin;
            pulse_devout: PulseDevout;
            pulseDevout: PulseDevout;
        }
    }

    /**
     * @gir-type Class
     */
    class PulsePort extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<PulsePort>;

        // Properties
        /**
         * The pulse soundcard indentifier
         * @since 3.0.0
         * @default null
         */
        get port_name(): string;
        set port_name(val: string);

        /**
         * The pulse soundcard indentifier
         * @since 3.0.0
         * @default null
         */
        get portName(): string;
        set portName(val: string);

        /**
         * The assigned {@link AgsAudio.PulseClient}.
         * @since 3.0.0
         */
        get pulse_client(): PulseClient;
        set pulse_client(val: PulseClient);

        /**
         * The assigned {@link AgsAudio.PulseClient}.
         * @since 3.0.0
         */
        get pulseClient(): PulseClient;
        set pulseClient(val: PulseClient);

        /**
         * The assigned {@link AgsAudio.PulseDevout}.
         * @since 3.0.0
         */
        get pulse_devin(): PulseDevin;
        set pulse_devin(val: PulseDevin);

        /**
         * The assigned {@link AgsAudio.PulseDevout}.
         * @since 3.0.0
         */
        get pulseDevin(): PulseDevin;
        set pulseDevin(val: PulseDevin);

        /**
         * The assigned {@link AgsAudio.PulseDevout}.
         * @since 3.0.0
         */
        get pulse_devout(): PulseDevout;
        set pulse_devout(val: PulseDevout);

        /**
         * The assigned {@link AgsAudio.PulseDevout}.
         * @since 3.0.0
         */
        get pulseDevout(): PulseDevout;
        set pulseDevout(val: PulseDevout);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PulsePort.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: PulsePortFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        port_uuid: string;

        pcm_channels: number;

        samplerate: number;

        buffer_size: number;

        format: Ags.SoundcardFormat;

        use_cache: boolean;

        cache_buffer_size: number;

        current_cache: number;

        completed_cache: number;

        cache_offset: number;

        cache: any;

        empty_buffer: any;

        is_empty: boolean;

        underflow: number;

        restart: boolean;

        nth_empty_buffer: number;

        queued: number;

        // Constructors
        constructor(properties?: Partial<PulsePort.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](pulse_client: GObject.Object): PulsePort;

        // Signals
        /** @signal */
        connect_after<K extends keyof PulsePort.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PulsePort.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PulsePort.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PulsePort.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next match of `port_name` in `pulse_port`.
         * @param pulse_port the {@link GLib.List}-struct containig {@link AgsAudio.PulsePort}
         * @param port_name the port name to find
         */
        static find(pulse_port: PulsePort[], port_name: string): PulsePort[];

        // Methods
        get_fixed_size(): number;

        /**
         * Gets latency.
         */
        get_latency(): number;

        /**
         * Register a new pulseaudio port and read uuid. Creates a new AgsSequencer or AgsSoundcard
         * object.
         * @param port_name the name as string
         * @param is_audio if `true` interpreted as audio port
         * @param is_midi if `true` interpreted as midi port
         * @param is_output if `true` port is acting as output, otherwise as input
         */
        register(port_name: string, is_audio: boolean, is_midi: boolean, is_output: boolean): void;

        /**
         * @param buffer_size 
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * @param cache_buffer_size 
         */
        set_cache_buffer_size(cache_buffer_size: number): void;

        /**
         * Enable a feature of `pulse_port`.
         * @param flags see {@link AgsAudio.PulsePortFlags}-enum
         */
        set_flags(flags: number): void;

        /**
         * @param format 
         */
        set_format(format: number): void;

        /**
         * @param pcm_channels 
         */
        set_pcm_channels(pcm_channels: number): void;

        /**
         * @param samplerate 
         */
        set_samplerate(samplerate: number): void;

        /**
         * Test `flags` to be set on `pulse_port`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        unregister(): void;

        /**
         * Disable a feature of `pulse_port`.
         * @param flags see {@link AgsAudio.PulsePortFlags}-enum
         */
        unset_flags(flags: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace PulseServer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::default-pulse-client": (pspec: GObject.ParamSpec) => void;
            "notify::default-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::pulse-client": (pspec: GObject.ParamSpec) => void;
            "notify::url": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.SoundServer.ConstructorProps {
            default_pulse_client: PulseClient;
            defaultPulseClient: PulseClient;
            default_soundcard: GObject.Object;
            defaultSoundcard: GObject.Object;
            pulse_client: PulseClient[];
            pulseClient: PulseClient[];
            url: string;
        }
    }

    /**
     * @gir-type Class
     */
    class PulseServer extends GObject.Object implements Ags.Connectable, Ags.SoundServer {
        static $gtype: GObject.GType<PulseServer>;

        // Properties
        /**
         * The default pulse client.
         * @since 3.0.0
         */
        get default_pulse_client(): PulseClient;
        set default_pulse_client(val: PulseClient);

        /**
         * The default pulse client.
         * @since 3.0.0
         */
        get defaultPulseClient(): PulseClient;
        set defaultPulseClient(val: PulseClient);

        /**
         * The default soundcard.
         * @since 3.0.0
         */
        get default_soundcard(): GObject.Object;
        set default_soundcard(val: GObject.Object);

        /**
         * The default soundcard.
         * @since 3.0.0
         */
        get defaultSoundcard(): GObject.Object;
        set defaultSoundcard(val: GObject.Object);

        /**
         * The pulse client list.
         * @since 3.0.0
         */
        get pulse_client(): PulseClient[];
        set pulse_client(val: PulseClient[]);

        /**
         * The pulse client list.
         * @since 3.0.0
         */
        get pulseClient(): PulseClient[];
        set pulseClient(val: PulseClient[]);

        /**
         * The assigned URL.
         * @since 3.0.0
         * @default null
         */
        get url(): string;
        set url(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: PulseServer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        running: boolean;

        application_context: Ags.ApplicationContext;

        uuid: Ags.UUID;

        port: number;

        port_count: number;

        n_soundcards: number;

        n_sequencers: number;

        default_client: GObject.Object;

        client: any[];

        // Constructors
        constructor(properties?: Partial<PulseServer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](url: string): PulseServer;

        // Signals
        /** @signal */
        connect_after<K extends keyof PulseServer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, PulseServer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof PulseServer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<PulseServer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find {@link AgsAudio.PulseServer} by url.
         * @param pulse_server the {@link GLib.List}-struct containing {@link AgsAudio.PulseServer}
         * @param url the url to find
         */
        static find_url(pulse_server: PulseServer[], url: string): PulseServer[];

        // Methods
        /**
         * Add `pulse_client` to `pulse_server`
         * @param pulse_client the {@link AgsAudio.PulseClient} to add
         */
        add_client(pulse_client: GObject.Object): void;

        /**
         * Connect all clients.
         */
        connect_client(): void;

        /**
         * Disconnect all clients.
         */
        disconnect_client(): void;

        /**
         * Find {@link AgsAudio.PulseClient} by uuid.
         * @param client_uuid the uuid to find
         * @returns the {@link AgsAudio.PulseClient} found or `null`
         */
        find_client<T = GObject.Object>(client_uuid: string): T;

        /**
         * Find {@link AgsAudio.PulsePort} by uuid.
         * @param port_uuid the uuid to find
         * @returns the {@link AgsAudio.PulsePort} found or `null`
         */
        find_port<T = GObject.Object>(port_uuid: string): T;

        /**
         * Remove `pulse_client` to `pulse_server`
         * @param pulse_client the {@link AgsAudio.PulseClient} to remove
         */
        remove_client(pulse_client: GObject.Object): void;

        start_poll(): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @returns the port as string `null`-terminated array
         */
        get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the sequencer as {@link GLib.List}-struct
         */
        get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @returns the soundcard as {@link GLib.List}-struct
         */
        get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @returns the URL as string
         */
        get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Sequencer}
         */
        register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @returns a new {@link Ags.Soundcard}
         */
        register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param port 
         * @param port_count 
         */
        set_ports(port: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         */
        set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         */
        set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         */
        set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         */
        unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         */
        unregister_soundcard(soundcard: GObject.Object): void;

        /**
         * Gets the ports of `sound_server`.
         * @param port_count the number of ports returned
         * @virtual
         */
        vfunc_get_ports(port_count: number): number;

        /**
         * Gets the sequencer of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_sequencer(client_uuid: string): GObject.Object[];

        /**
         * Gets the soundcard of `sound_server` associated with `client_uuid`.
         * @param client_uuid the client uuid
         * @virtual
         */
        vfunc_get_soundcard(client_uuid: string): GObject.Object[];

        /**
         * Gets the URL of `sound_server`.
         * @virtual
         */
        vfunc_get_url(): string;

        /**
         * Fetches `sequencer` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_sequencer<T = GObject.Object>(is_output: boolean): T;

        /**
         * Fetches `soundcard` of `sound_server`.
         * @param is_output if `true` the used as sink, else as source
         * @virtual
         */
        vfunc_register_soundcard<T = GObject.Object>(is_output: boolean): T;

        /**
         * @param ports 
         * @param port_count 
         * @virtual
         */
        vfunc_set_ports(ports: number, port_count: number): void;

        /**
         * Sets the sequencer at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param sequencer the sequencer to set
         * @virtual
         */
        vfunc_set_sequencer(client_uuid: string, sequencer: GObject.Object[]): void;

        /**
         * Sets the soundcard at `client_uuid`.
         * @param client_uuid the location to fetch from
         * @param soundcard the soundcard to set
         * @virtual
         */
        vfunc_set_soundcard(client_uuid: string, soundcard: GObject.Object[]): void;

        /**
         * Sets the url of `sound_server`.
         * @param url the url to set
         * @virtual
         */
        vfunc_set_url(url: string): void;

        /**
         * Releases `sequencer` in `sound_server`.
         * @param sequencer the {@link Ags.Sequencer}
         * @virtual
         */
        vfunc_unregister_sequencer(sequencer: GObject.Object): void;

        /**
         * Releases `soundcard` in `sound_server`.
         * @param soundcard the {@link Ags.Soundcard}
         * @virtual
         */
        vfunc_unregister_soundcard(soundcard: GObject.Object): void;
    }


    namespace Recall {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::automate signal notifies about running
             * automation and is normally called during ::run-pre.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            automate: () => void;
            /**
             * The ::cancel signal notifies about cancelling playback.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            cancel: () => void;
            /**
             * The ::check-rt-data signal notifies about initializing
             * stage 0.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "check-rt-data": () => void;
            /**
             * The ::child-added signal notifies about children
             * added.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "child-added": (arg0: GObject.Object) => void;
            /**
             * The ::do-feedback signal notifies about running
             * stage 2.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "do-feedback": () => void;
            /**
             * The ::done signal notifies about stopping playback.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            done: () => void;
            /**
             * The ::duplicate signal notifies about instantiating.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            duplicate: (arg0: GObject.Object, arg1: (any | null), arg2: (any | null), arg3: (any | null)) => GObject.Object;
            /**
             * The ::feed-input-queue signal notifies about running
             * feed input queue.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "feed-input-queue": () => void;
            /**
             * The ::feed-output-queue signal notifies about running
             * feed output queue.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "feed-output-queue": () => void;
            /**
             * The ::jack-metadata signal notifies about checking JACK metadata property change.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "jack-metadata": () => void;
            /**
             * The ::midi1-control change event notifies about parsing MIDI version 1.0 control change.
             * @signal
             * @since 7.0.0
             * @run-last
             */
            "midi1-control-change": () => void;
            /**
             * The ::midi2-control change event notifies about parsing MIDI version 2.0 control change.
             * @signal
             * @since 7.0.0
             * @run-last
             */
            "midi2-control-change": () => void;
            /**
             * The ::notify-dependency signal notifies about dependency
             * added.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "notify-dependency": (arg0: number, arg1: boolean) => void;
            /**
             * The ::resolve-dependency signal notifies about resolving
             * dependency.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "resolve-dependency": () => void;
            /**
             * The ::run-init-inter signal notifies about initializing
             * stage 1.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "run-init-inter": () => void;
            /**
             * The ::run-init-post signal notifies about initializing
             * stage 2.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "run-init-post": () => void;
            /**
             * The ::run-init-pre signal notifies about initializing
             * stage 0.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "run-init-pre": () => void;
            /**
             * The ::run-inter signal notifies about running
             * stage 1.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "run-inter": () => void;
            /**
             * The ::run-post signal notifies about running
             * stage 2.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "run-post": () => void;
            /**
             * The ::run-pre signal notifies about running
             * stage 0.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "run-pre": () => void;
            /**
             * The ::stop-persistent signal notifies about definitively
             * stopping playback.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "stop-persistent": () => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio_channel: number;
            audioChannel: number;
            automation_port: Port[];
            automationPort: Port[];
            buffer_size: number;
            bufferSize: number;
            child: Recall[];
            child_type: GObject.GTypeInput;
            childType: GObject.GTypeInput;
            effect: string;
            effect_index: number;
            effectIndex: number;
            filename: string;
            format: number;
            input_soundcard: GObject.Object;
            inputSoundcard: GObject.Object;
            input_soundcard_channel: number;
            inputSoundcardChannel: number;
            line: number;
            output_soundcard: GObject.Object;
            outputSoundcard: GObject.Object;
            output_soundcard_channel: number;
            outputSoundcardChannel: number;
            pad: number;
            parent: Recall;
            port: Port[];
            recall_container: RecallContainer;
            recallContainer: RecallContainer;
            recall_dependency: RecallDependency[];
            recallDependency: RecallDependency[];
            recall_id: RecallID;
            recallId: RecallID;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Recall extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<Recall>;

        // Properties
        /**
         * The nth audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The nth audio channel.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The {@link AgsAudio.Port} doing automation.
         * @since 3.0.0
         */
        get automation_port(): Port[];
        set automation_port(val: Port[]);

        /**
         * The {@link AgsAudio.Port} doing automation.
         * @since 3.0.0
         */
        get automationPort(): Port[];
        set automationPort(val: Port[]);

        /**
         * The buffer size.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The child {@link AgsAudio.Recall}.
         * @since 3.0.0
         */
        get child(): Recall[];
        set child(val: Recall[]);

        /**
         * The type of child {@link AgsAudio.Recall}.
         * @since 3.0.0
         */
        get child_type(): GObject.GType;
        set child_type(val: GObject.GType);

        /**
         * The type of child {@link AgsAudio.Recall}.
         * @since 3.0.0
         */
        get childType(): GObject.GType;
        set childType(val: GObject.GType);

        /**
         * The plugin's effect.
         * @since 3.0.0
         * @default null
         */
        get effect(): string;
        set effect(val: string);

        /**
         * The effect's index.
         * @since 3.0.0
         * @default 0
         */
        get effect_index(): number;
        set effect_index(val: number);

        /**
         * The effect's index.
         * @since 3.0.0
         * @default 0
         */
        get effectIndex(): number;
        set effectIndex(val: number);

        /**
         * The plugin's filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The format.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned soundcard.
         * @since 3.0.0
         */
        get input_soundcard(): GObject.Object;
        set input_soundcard(val: GObject.Object);

        /**
         * The assigned soundcard.
         * @since 3.0.0
         */
        get inputSoundcard(): GObject.Object;
        set inputSoundcard(val: GObject.Object);

        /**
         * The input soundcard channel.
         * @since 3.0.0
         * @default -1
         */
        get input_soundcard_channel(): number;
        set input_soundcard_channel(val: number);

        /**
         * The input soundcard channel.
         * @since 3.0.0
         * @default -1
         */
        get inputSoundcardChannel(): number;
        set inputSoundcardChannel(val: number);

        /**
         * The nth line.
         * @since 3.0.0
         * @default 0
         */
        get line(): number;
        set line(val: number);

        /**
         * The assigned soundcard.
         * @since 3.0.0
         */
        get output_soundcard(): GObject.Object;
        set output_soundcard(val: GObject.Object);

        /**
         * The assigned soundcard.
         * @since 3.0.0
         */
        get outputSoundcard(): GObject.Object;
        set outputSoundcard(val: GObject.Object);

        /**
         * The output soundcard channel.
         * @since 3.0.0
         * @default -1
         */
        get output_soundcard_channel(): number;
        set output_soundcard_channel(val: number);

        /**
         * The output soundcard channel.
         * @since 3.0.0
         * @default -1
         */
        get outputSoundcardChannel(): number;
        set outputSoundcardChannel(val: number);

        /**
         * The nth pad.
         * @since 3.0.0
         * @default 0
         */
        get pad(): number;
        set pad(val: number);

        /**
         * The parent {@link AgsAudio.Recall}.
         * @since 3.0.0
         */
        get parent(): Recall;
        set parent(val: Recall);

        /**
         * The assigned {@link AgsAudio.Port}
         * @since 3.0.0
         */
        get port(): Port[];
        set port(val: Port[]);

        /**
         * The {@link AgsAudio.RecallContainer} packed into.
         * @since 3.0.0
         */
        get recall_container(): RecallContainer;
        set recall_container(val: RecallContainer);

        /**
         * The {@link AgsAudio.RecallContainer} packed into.
         * @since 3.0.0
         */
        get recallContainer(): RecallContainer;
        set recallContainer(val: RecallContainer);

        /**
         * The {@link AgsAudio.RecallDependency}.
         * @since 3.0.0
         */
        get recall_dependency(): RecallDependency[];
        set recall_dependency(val: RecallDependency[]);

        /**
         * The {@link AgsAudio.RecallDependency}.
         * @since 3.0.0
         */
        get recallDependency(): RecallDependency[];
        set recallDependency(val: RecallDependency[]);

        /**
         * The {@link AgsAudio.RecallID} running in.
         * @since 3.0.0
         */
        get recall_id(): RecallID;
        set recall_id(val: RecallID);

        /**
         * The {@link AgsAudio.RecallID} running in.
         * @since 3.0.0
         */
        get recallId(): RecallID;
        set recallId(val: RecallID);

        /**
         * The samplerate.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Recall.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: RecallFlags;

        connectable_flags: Ags.ConnectableFlags;

        ability_flags: SoundAbilityFlags;

        behaviour_flags: SoundBehaviourFlags;

        sound_scope: number;

        staging_flags: SoundStagingFlags;

        state_flags: SoundStateFlags;

        uuid: Ags.UUID;

        version: string;

        build_id: string;

        name: string;

        xml_type: string;

        recall_handler: any[];

        n_child_params: number;

        child_parameter_name: string;

        children: any[];

        midi_util: MidiUtil;

        midi_ump_util: MidiUmpUtil;

        // Constructors
        constructor(properties?: Partial<Recall.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Recall;

        // Signals
        /** @signal */
        connect_after<K extends keyof Recall.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Recall.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Recall.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Recall.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds next matching recall for type which has `provider`. The `provider` may be either an {@link AgsAudio.Channel}
         * or an {@link AgsAudio.Audio} object. This function tries to find the corresponding {@link AgsAudio.RecallChannel} and {@link AgsAudio.RecallAudio}
         * objects of a {@link AgsAudio.Recall} to find. If these recalls contains the `provider`, the function will return.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param provider the {@link GObject.Object}, either {@link AgsAudio.Audio}, {@link AgsAudio.Channel}, {@link AgsAudio.Recycling} or {@link AgsAudio.AudioSignal}
         */
        static find_provider(recall: Recall[], provider: GObject.Object): Recall[];

        /**
         * Like `ags_recall_template_find_provider()` but given additionally `recycling_context` as search parameter.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param provider the {@link GObject.Object} as provider
         * @param recycling_context the {@link AgsAudio.RecyclingContext}
         */
        static find_provider_with_recycling_context(recall: Recall[], provider: GObject.Object, recycling_context: GObject.Object): Recall[];

        /**
         * Finds next matching effect name. Intended to be used as
         * iteration function.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param recall_id the {@link AgsAudio.RecallID}, may be `null`
         * @param filename the filename or `null`
         * @param effect the effect name
         */
        static find_recall_id_with_effect(recall: Recall[], recall_id: RecallID, filename: string, effect: string): Recall[];

        /**
         * Finds next matching recall which has `recycling_context`, see `AgsRecallId` for further
         * details about {@link AgsAudio.RecyclingContext}. Intended to be used as iteration function.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param recycling_context the {@link AgsAudio.RecyclingContext}
         */
        static find_recycling_context(recall: Recall[], recycling_context: GObject.Object): Recall[];

        /**
         * Finds next template, see #AGS_RECALL_TEMPLATE flag. Intended to be used as
         * iteration function.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         */
        static find_template(recall: Recall[]): Recall[];

        /**
         * Finds next matching recall for type. Intended to be used as
         * iteration function.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param type the {@link GObject.GType}
         */
        static find_type(recall: Recall[], type: GObject.GType): Recall[];

        /**
         * Finds next matching recall for type which has `recycling_context`, see `AgsRecallId` for further
         * details about {@link AgsAudio.RecyclingContext}. Intended to be used as iteration function.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param type the {@link GObject.GType}
         * @param recycling_context the {@link AgsAudio.RecyclingContext}
         */
        static find_type_with_recycling_context(recall: Recall[], type: GObject.GType, recycling_context: GObject.Object): Recall[];

        /**
         * Finds all recalls matching `filename` and `effect`.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param filename the filename containing `effect` or `null`
         * @param effect the effect name
         */
        static get_by_effect(recall: Recall[], filename: string, effect: string): Recall[];

        /**
         * Get global config value lock free children.
         */
        static global_get_children_lock_free(): boolean;

        /**
         * Get global config value omit event.
         */
        static global_get_omit_event(): boolean;

        /**
         * Get global config value performance mode.
         */
        static global_get_performance_mode(): boolean;

        /**
         * Get global config value rt-safe.
         */
        static global_get_rt_safe(): boolean;

        /**
         * Set global config value omit event.
         * @param omit_event `true` if omit event, otherwise `false`
         */
        static global_set_omit_event(omit_event: boolean): void;

        /**
         * Check if recall is over.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param recycling_context the {@link AgsAudio.RecyclingContext}
         */
        static is_done(recall: Recall[], recycling_context: GObject.Object): boolean;

        /**
         * Finds provider eg. {@link AgsAudio.Audio} or {@link AgsAudio.Channel} within `recall` containig {@link AgsAudio.Recall}.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param provider the {@link GObject.Object} as provider
         */
        static template_find_provider(recall: Recall[], provider: GObject.Object): Recall[];

        /**
         * Finds next matching recall for type which is a template, see #AGS_RECALL_TEMPLATE flag.
         * Intended to be used as iteration function.
         * @param recall the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         * @param type a {@link GObject.GType}
         */
        static template_find_type(recall: Recall[], type: GObject.GType): Recall[];

        // Virtual methods
        /**
         * Automate port of `recall`.
         * @virtual
         */
        vfunc_automate(): void;

        /**
         * The {@link AgsAudio.Recall} doesn't want to run anymore, it aborts further execution.
         * @virtual
         */
        vfunc_cancel(): void;

        /**
         * Prepare for run, this is the pre stage within the preparation.
         * @virtual
         */
        vfunc_check_rt_data(): void;

        /**
         * A signal indicating that the a child has been added.
         * @param child the child {@link AgsAudio.Recall}
         * @virtual
         */
        vfunc_child_added(child: Recall): void;

        /**
         * Do feedback of `recall`.
         * @virtual
         */
        vfunc_do_feedback(): void;

        /**
         * The {@link AgsAudio.Recall} doesn't want to run anymore, it has been done its
         * work.
         * @virtual
         */
        vfunc_done(): void;

        /**
         * Should duplicate an {@link AgsAudio.Recall}, so it can pass the run stages. Mainly used for
         * creating duplicates of templates, see {@link AgsAudio.RecallFlags.RECALL_TEMPLATE}.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @param n_params guint pointer to parameter count
         * @param parameter_name string vector containing parameter names
         * @param value the {@link GObject.Value}-struct array
         * @virtual
         */
        vfunc_duplicate(recall_id: RecallID, n_params: number, parameter_name: string, value: unknown): Recall;

        /**
         * Feed input queue of `recall`.
         * @virtual
         */
        vfunc_feed_input_queue(): void;

        /**
         * Feed output queue of `recall`.
         * @virtual
         */
        vfunc_feed_output_queue(): void;

        /**
         * The {@link AgsAudio.Recall} checks JACK metadata property change from CV port.
         * @virtual
         */
        vfunc_jack_metadata(): void;

        /**
         * The {@link AgsAudio.Recall} checks MIDI version 1 control change from input sequencer.
         * @virtual
         */
        vfunc_midi1_control_change(): void;

        /**
         * The {@link AgsAudio.Recall} checks MIDI version 2 control change from input sequencer.
         * @virtual
         */
        vfunc_midi2_control_change(): void;

        /**
         * Notifies a recall that an other depends on it.
         * @param dependency the dependency to notify for, see {@link AgsAudio.RecallNotifyDependencyMode}-enum
         * @param increase if `true` increase, else if `false` decrease
         * @virtual
         */
        vfunc_notify_dependency(dependency: number, increase: boolean): void;

        /**
         * A signal indicating that the inheriting object should resolve
         * it's dependency.
         * @virtual
         */
        vfunc_resolve_dependency(): void;

        /**
         * Prepare for run, this is the inter stage within the preparation.
         * @virtual
         */
        vfunc_run_init_inter(): void;

        /**
         * Prepare for run, this is the post stage within the preparation.
         * @virtual
         */
        vfunc_run_init_post(): void;

        /**
         * Prepare for run, this is the pre stage within the preparation.
         * @virtual
         */
        vfunc_run_init_pre(): void;

        /**
         * This is the inter stage within a run.
         * @virtual
         */
        vfunc_run_inter(): void;

        /**
         * This is the post stage within a run.
         * @virtual
         */
        vfunc_run_post(): void;

        /**
         * This is the pre stage within a run.
         * @virtual
         */
        vfunc_run_pre(): void;

        /**
         * Unsets the {@link AgsAudio.SoundBehaviourFlags.SOUND_BEHAVIOUR_PERSISTENT} and related behaviour flags and
         * invokes `ags_recall_done()`.
         * @virtual
         */
        vfunc_stop_persistent(): void;

        // Methods
        /**
         * Add `child` to `recall`.
         * @param child the child {@link AgsAudio.Recall}
         */
        add_child(child: Recall): void;

        /**
         * Add `port` to `recall`.
         * @param port the {@link AgsAudio.Port}
         */
        add_port(port: Port): void;

        /**
         * Associate a new dependency for this recall.
         * @param recall_dependency the {@link AgsAudio.RecallDependency}
         */
        add_recall_dependency(recall_dependency: RecallDependency): void;

        /**
         * Connect callback to `recall` specified by `recall_handler`.
         * @param recall_handler the signal specs
         */
        add_recall_handler(recall_handler: (any | null)): void;

        /**
         * Automate port of `recall`.
         */
        automate(): void;

        /**
         * The {@link AgsAudio.Recall} doesn't want to run anymore, it aborts further execution.
         */
        cancel(): void;

        /**
         * Check if `ability_flags` is set for `recall`.
         * @param ability_flags the ability flags
         * @returns `true` flags are set, otherwise `false`
         */
        check_ability_flags(ability_flags: SoundAbilityFlags): boolean;

        /**
         * Check if `behaviour_flags` is set for `recall`.
         * @param behaviour_flags the behaviour flags
         * @returns `true` flags are set, otherwise `false`
         */
        check_behaviour_flags(behaviour_flags: SoundBehaviourFlags): boolean;

        /**
         * Prepare for run, this is the pre stage within the preparation.
         */
        check_rt_data(): void;

        /**
         * Check if `sound_scope` is set for `recall`.
         * @param sound_scope the sound scope to check or -1 to check all
         * @returns `true` if sound scope matches, otherwise  `false`
         */
        check_sound_scope(sound_scope: number): boolean;

        /**
         * Check the occurence of `staging_flags` in `recall`.
         * @param staging_flags staging flags to check
         * @returns `true` if all flags matched, otherwise `false`
         */
        check_staging_flags(staging_flags: SoundStagingFlags): boolean;

        /**
         * Check the occurence of `state_flags` in `recall`.
         * @param state_flags state flags to check
         * @returns `true` if all flags matched, otherwise `false`
         */
        check_state_flags(state_flags: SoundStateFlags): boolean;

        /**
         * A signal indicating that the a child has been added.
         * @param child the child {@link AgsAudio.Recall}
         */
        child_added(child: Recall): void;

        /**
         * Do feedback of `recall`.
         */
        do_feedback(): void;

        /**
         * The {@link AgsAudio.Recall} doesn't want to run anymore, it has been done its
         * work.
         */
        done(): void;

        /**
         * Should duplicate an {@link AgsAudio.Recall}, so it can pass the run stages. Mainly used for
         * creating duplicates of templates, see {@link AgsAudio.RecallFlags.RECALL_TEMPLATE}.
         * @param recall_id the {@link AgsAudio.RecallID}
         * @param n_params guint pointer to parameter count
         * @param parameter_name string vector containing parameter names
         * @param value the {@link GObject.Value}-struct array
         * @returns the duplicated {@link AgsAudio.Recall}
         */
        duplicate(recall_id: RecallID, n_params: number, parameter_name: string, value: (GObject.Value | any)): Recall;

        /**
         * Feed input queue of `recall`.
         */
        feed_input_queue(): void;

        /**
         * Feed output queue of `recall`.
         */
        feed_output_queue(): void;

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Get recall children.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Recall}
         */
        get_children(): Recall[];

        /**
         * Get effect.
         * @returns the effect
         */
        get_effect(): string;

        /**
         * Get effect index.
         * @returns the effect index
         */
        get_effect_index(): number;

        /**
         * Get filename.
         * @returns the filename
         */
        get_filename(): string;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get the input soundcard object of `recall`.
         * @returns the input soundcard
         */
        get_input_soundcard<T = GObject.Object>(): T;

        /**
         * Get object mutex.
         * @returns the {@link GLib.RecMutex} to lock `recall`
         */
        get_obj_mutex(): (any | null);

        /**
         * Get the output soundcard object of `recall`.
         * @returns the output soundcard
         */
        get_output_soundcard<T = GObject.Object>(): T;

        /**
         * Get port.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Port}
         */
        get_port(): Port[];

        /**
         * Get recall container of `recall`.
         * @returns the {@link AgsAudio.RecallContainer}
         */
        get_recall_container<T = GObject.Object>(): T;

        /**
         * Get recall dependency.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.RecallDependency}
         */
        get_recall_dependency(): RecallDependency[];

        /**
         * Get recall id of `recall`.
         * @returns the {@link AgsAudio.RecallID}
         */
        get_recall_id(): RecallID;

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get sound scope for `recall`.
         * @returns the used sound scope
         */
        get_sound_scope(): number;

        /**
         * The {@link AgsAudio.Recall} checks JACK metadata property change from CV port.
         */
        jack_metadata(): void;

        /**
         * Locks the ports.
         */
        lock_port(): void;

        /**
         * Check if `sound_scope` related ability flag is set.
         * @param sound_scope the sound scope
         * @returns `true` if sound scope is available, otherwise `false`
         */
        match_ability_flags_to_scope(sound_scope: number): boolean;

        /**
         * The {@link AgsAudio.Recall} checks MIDI version 1 control change from input sequencer.
         */
        midi1_control_change(): void;

        /**
         * The {@link AgsAudio.Recall} checks MIDI version 2 control change from input sequencer.
         */
        midi2_control_change(): void;

        /**
         * Notifies a recall that an other depends on it.
         * @param dependency the dependency to notify for, see {@link AgsAudio.RecallNotifyDependencyMode}-enum
         * @param increase if `true` increase, else if `false` decrease
         */
        notify_dependency(dependency: number, increase: boolean): void;

        /**
         * Remove `child` from `recall`.
         * @param child the child {@link AgsAudio.Recall}
         */
        remove_child(child: Recall): void;

        /**
         * Remove `port` from `recall`.
         * @param port the {@link AgsAudio.Port}
         */
        remove_port(port: Port): void;

        /**
         * Remove a prior associated dependency.
         * @param recall_dependency the {@link AgsAudio.RecallDependency}
         */
        remove_recall_dependency(recall_dependency: RecallDependency): void;

        /**
         * Remove a {@link AgsAudio.RecallHandler}-struct from `recall`.
         * @param recall_handler the signal specs
         */
        remove_recall_handler(recall_handler: (any | null)): void;

        /**
         * A signal indicating that the inheriting object should resolve
         * it's dependency.
         */
        resolve_dependency(): void;

        /**
         * Prepare for run, this is the inter stage within the preparation.
         */
        run_init_inter(): void;

        /**
         * Prepare for run, this is the post stage within the preparation.
         */
        run_init_post(): void;

        /**
         * Prepare for run, this is the pre stage within the preparation.
         */
        run_init_pre(): void;

        /**
         * This is the inter stage within a run.
         */
        run_inter(): void;

        /**
         * This is the post stage within a run.
         */
        run_post(): void;

        /**
         * This is the pre stage within a run.
         */
        run_pre(): void;

        /**
         * Set ability flags recursively.
         * @param ability_flags ability flags
         */
        set_ability_flags(ability_flags: SoundAbilityFlags): void;

        /**
         * Set behaviour flags of `recall`.
         * @param behaviour_flags the behaviour flags
         */
        set_behaviour_flags(behaviour_flags: SoundBehaviourFlags): void;

        /**
         * Set buffer size of `recall`.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Set recall children by replacing existing.
         * @param children the {@link GLib.List}-struct containing {@link AgsAudio.Recall}
         */
        set_children(children: Recall[]): void;

        /**
         * Set effect.
         * @param effect the effect
         */
        set_effect(effect: string): void;

        /**
         * Set effect index.
         * @param effect_index the effect index
         */
        set_effect_index(effect_index: number): void;

        /**
         * Set filename.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: RecallFlags): void;

        /**
         * Set format of `recall`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set the input soundcard object of `recall`.
         * @param input_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_input_soundcard(input_soundcard: GObject.Object): void;

        /**
         * Set the output soundcard object of `recall`.
         * @param output_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_output_soundcard(output_soundcard: GObject.Object): void;

        /**
         * Set port by replacing existing.
         * @param port the {@link GLib.List}-struct containing {@link AgsAudio.Port}
         */
        set_port(port: Port[]): void;

        /**
         * Set `recall_container` of `recall`.
         * @param recall_container the {@link AgsAudio.RecallContainer}
         */
        set_recall_container(recall_container: GObject.Object): void;

        /**
         * Set recall dependency by replacing existing.
         * @param recall_dependency the {@link GLib.List}-struct containing {@link AgsAudio.RecallDependency}
         */
        set_recall_dependency(recall_dependency: RecallDependency[]): void;

        /**
         * Set `recall_id` of `recall` and all its children.
         * @param recall_id the {@link AgsAudio.RecallID} to set
         */
        set_recall_id(recall_id: RecallID): void;

        /**
         * Set samplerate of `recall`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `sound_scope` for `recall`.
         * @param sound_scope the sound scope
         */
        set_sound_scope(sound_scope: number): void;

        /**
         * Set staging flags.
         * @param staging_flags staging flags to set
         */
        set_staging_flags(staging_flags: SoundStagingFlags): void;

        /**
         * Set state flags.
         * @param state_flags state flags to set
         */
        set_state_flags(state_flags: SoundStateFlags): void;

        /**
         * Unsets the {@link AgsAudio.SoundBehaviourFlags.SOUND_BEHAVIOUR_PERSISTENT} and related behaviour flags and
         * invokes `ags_recall_done()`.
         */
        stop_persistent(): void;

        /**
         * Test `ability_flags` to be set on `recall`.
         * @param ability_flags the ability flags
         * @returns `true` if flags are set, else `false`
         */
        test_ability_flags(ability_flags: SoundAbilityFlags): boolean;

        /**
         * Test `behaviour_flags` to be set on `recall`.
         * @param behaviour_flags the behaviour flags
         * @returns `true` if flags are set, else `false`
         */
        test_behaviour_flags(behaviour_flags: SoundBehaviourFlags): boolean;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: RecallFlags): boolean;

        /**
         * Test `staging_flags` to be set on `recall`.
         * @param staging_flags the staging flags
         * @returns `true` if flags are set, else `false`
         */
        test_staging_flags(staging_flags: SoundStagingFlags): boolean;

        /**
         * Test `state_flags` to be set on `recall`.
         * @param state_flags the state flags
         * @returns `true` if flags are set, else `false`
         */
        test_state_flags(state_flags: SoundStateFlags): boolean;

        /**
         * Unlocks the ports.
         */
        unlock_port(): void;

        /**
         * Unset ability flags recursively.
         * @param ability_flags ability flags
         */
        unset_ability_flags(ability_flags: SoundAbilityFlags): void;

        /**
         * Unset behaviour flags of `recall`.
         * @param behaviour_flags the behaviour flags
         */
        unset_behaviour_flags(behaviour_flags: SoundBehaviourFlags): void;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: RecallFlags): void;

        /**
         * Unset staging flags.
         * @param staging_flags staging flags to unset
         */
        unset_staging_flags(staging_flags: SoundStagingFlags): void;

        /**
         * Unset state flags.
         * @param state_flags state flags to unset
         */
        unset_state_flags(state_flags: SoundStateFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace RecallAudio {
        // Signal signatures
        interface SignalSignatures extends Recall.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Recall.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio: Audio;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallAudio extends Recall implements Ags.Connectable {
        static $gtype: GObject.GType<RecallAudio>;

        // Properties
        /**
         * The assigned audio.
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallAudio.SignalSignatures;

        // Fields
        recall: Recall;

        flags: number;

        // Constructors
        constructor(properties?: Partial<RecallAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): RecallAudio;

        // Conflicted with AgsAudio.Recall.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof RecallAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get audio.
         * @returns the {@link AgsAudio.Audio}
         */
        get_audio(): Audio;

        /**
         * Set audio.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace RecallAudioRun {
        // Signal signatures
        interface SignalSignatures extends Recall.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Recall.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio: Audio;
            recall_audio: RecallAudio;
            recallAudio: RecallAudio;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallAudioRun extends Recall implements Ags.Connectable {
        static $gtype: GObject.GType<RecallAudioRun>;

        // Properties
        /**
         * The assigned audio.
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The recall audio belonging to.
         * @since 3.0.0
         */
        get recall_audio(): RecallAudio;
        set recall_audio(val: RecallAudio);

        /**
         * The recall audio belonging to.
         * @since 3.0.0
         */
        get recallAudio(): RecallAudio;
        set recallAudio(val: RecallAudio);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallAudioRun.SignalSignatures;

        // Fields
        recall: Recall;

        // Constructors
        constructor(properties?: Partial<RecallAudioRun.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): RecallAudioRun;

        // Signals
        /** @signal */
        connect_after<K extends keyof RecallAudioRun.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallAudioRun.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallAudioRun.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallAudioRun.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get audio.
         * @returns the {@link AgsAudio.Audio}
         */
        get_audio(): Audio;

        /**
         * Get recall audio.
         * @returns the {@link AgsAudio.RecallAudio}
         */
        get_recall_audio(): RecallAudio;

        /**
         * Set audio.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio): void;

        /**
         * Set recall audio.
         * @param recall_audio the {@link AgsAudio.RecallAudio}
         */
        set_recall_audio(recall_audio: RecallAudio): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace RecallAudioSignal {
        // Signal signatures
        interface SignalSignatures extends Recall.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Recall.ConstructorProps, Ags.Connectable.ConstructorProps {
            destination: AudioSignal;
            source: AudioSignal;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallAudioSignal extends Recall implements Ags.Connectable {
        static $gtype: GObject.GType<RecallAudioSignal>;

        // Properties
        /**
         * The destination audio signal
         * @since 3.0.0
         */
        get destination(): AudioSignal;
        set destination(val: AudioSignal);

        /**
         * The source audio signal
         * @since 3.0.0
         */
        get source(): AudioSignal;
        set source(val: AudioSignal);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallAudioSignal.SignalSignatures;

        // Fields
        recall: Recall;

        // Constructors
        constructor(properties?: Partial<RecallAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): RecallAudioSignal;

        // Signals
        /** @signal */
        connect_after<K extends keyof RecallAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace RecallChannel {
        // Signal signatures
        interface SignalSignatures extends Recall.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Recall.ConstructorProps, Ags.Connectable.ConstructorProps {
            destination: Channel;
            recall_audio: RecallAudio;
            recallAudio: RecallAudio;
            source: Channel;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallChannel extends Recall implements Ags.Connectable {
        static $gtype: GObject.GType<RecallChannel>;

        // Properties
        /**
         * The assigned destination channel.
         * @since 3.0.0
         */
        get destination(): Channel;
        set destination(val: Channel);

        /**
         * The associated recall within audio context.
         * @since 3.0.0
         */
        get recall_audio(): RecallAudio;
        set recall_audio(val: RecallAudio);

        /**
         * The associated recall within audio context.
         * @since 3.0.0
         */
        get recallAudio(): RecallAudio;
        set recallAudio(val: RecallAudio);

        /**
         * The assigned source channel.
         * @since 3.0.0
         */
        get source(): Channel;
        set source(val: Channel);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallChannel.SignalSignatures;

        // Fields
        recall: Recall;

        // Constructors
        constructor(properties?: Partial<RecallChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): RecallChannel;

        // Signals
        /** @signal */
        connect_after<K extends keyof RecallChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Retrieve next recall assigned to channel.
         * @param recall_channel the {@link GLib.List}-struct containing {@link AgsAudio.RecallChannel}
         * @param source the {@link AgsAudio.Channel} to find
         */
        static find_channel(recall_channel: RecallChannel[], source: Channel): RecallChannel[];

        // Methods
        /**
         * Get destination.
         * @returns the `AgsDestination`
         */
        get_destination(): Channel;

        /**
         * Get source.
         * @returns the `AgsSource`
         */
        get_source(): Channel;

        /**
         * Set destination.
         * @param destination the {@link AgsAudio.Channel}
         */
        set_destination(destination: Channel): void;

        /**
         * Set source.
         * @param source the {@link AgsAudio.Channel}
         */
        set_source(source: Channel): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace RecallChannelRun {
        // Signal signatures
        interface SignalSignatures extends Recall.SignalSignatures {
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Recall.ConstructorProps, Ags.Connectable.ConstructorProps {
            destination: Channel;
            recall_audio: RecallAudio;
            recallAudio: RecallAudio;
            recall_audio_run: RecallAudioRun;
            recallAudioRun: RecallAudioRun;
            recall_channel: RecallChannel;
            recallChannel: RecallChannel;
            source: Channel;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallChannelRun extends Recall implements Ags.Connectable {
        static $gtype: GObject.GType<RecallChannelRun>;

        // Properties
        /**
         * The channel to do output to.
         * @since 3.0.0
         */
        get destination(): Channel;
        set destination(val: Channel);

        /**
         * The audio context of this recall.
         * @since 3.0.0
         */
        get recall_audio(): RecallAudio;
        set recall_audio(val: RecallAudio);

        /**
         * The audio context of this recall.
         * @since 3.0.0
         */
        get recallAudio(): RecallAudio;
        set recallAudio(val: RecallAudio);

        /**
         * The audio run context of this recall.
         * @since 3.0.0
         */
        get recall_audio_run(): RecallAudioRun;
        set recall_audio_run(val: RecallAudioRun);

        /**
         * The audio run context of this recall.
         * @since 3.0.0
         */
        get recallAudioRun(): RecallAudioRun;
        set recallAudioRun(val: RecallAudioRun);

        /**
         * The channel context of this recall.
         * @since 3.0.0
         */
        get recall_channel(): RecallChannel;
        set recall_channel(val: RecallChannel);

        /**
         * The channel context of this recall.
         * @since 3.0.0
         */
        get recallChannel(): RecallChannel;
        set recallChannel(val: RecallChannel);

        /**
         * The channel to do input from.
         * @since 3.0.0
         */
        get source(): Channel;
        set source(val: Channel);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallChannelRun.SignalSignatures;

        // Fields
        recall: Recall;

        // Constructors
        constructor(properties?: Partial<RecallChannelRun.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): RecallChannelRun;

        // Signals
        /** @signal */
        connect_after<K extends keyof RecallChannelRun.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallChannelRun.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallChannelRun.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallChannelRun.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get destination.
         * @returns the `AgsDestination`
         */
        get_destination(): Channel;

        /**
         * Get recall audio.
         * @returns the {@link AgsAudio.RecallAudio}
         */
        get_recall_audio(): RecallAudio;

        /**
         * Get recall audio.
         * @returns the {@link AgsAudio.RecallAudioRun}
         */
        get_recall_audio_run(): RecallAudioRun;

        /**
         * Get recall audio.
         * @returns the {@link AgsAudio.RecallChannel}
         */
        get_recall_channel(): RecallChannel;

        /**
         * Get source.
         * @returns the `AgsSource`
         */
        get_source(): Channel;

        /**
         * Set destination.
         * @param destination the {@link AgsAudio.Channel}
         */
        set_destination(destination: Channel): void;

        /**
         * Set recall audio.
         * @param recall_audio the {@link AgsAudio.RecallAudio}
         */
        set_recall_audio(recall_audio: RecallAudio): void;

        /**
         * Set recall audio.
         * @param recall_audio_run the {@link AgsAudio.RecallAudioRun}
         */
        set_recall_audio_run(recall_audio_run: RecallAudioRun): void;

        /**
         * Set recall audio.
         * @param recall_channel the {@link AgsAudio.RecallChannel}
         */
        set_recall_channel(recall_channel: RecallChannel): void;

        /**
         * Set source.
         * @param source the {@link AgsAudio.Channel}
         */
        set_source(source: Channel): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace RecallContainer {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::recall-audio": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-run-type": (pspec: GObject.ParamSpec) => void;
            "notify::recall-audio-type": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel-run": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel-run-type": (pspec: GObject.ParamSpec) => void;
            "notify::recall-channel-type": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            recall_audio: RecallAudio;
            recallAudio: RecallAudio;
            recall_audio_run: Recall[];
            recallAudioRun: Recall[];
            recall_audio_run_type: GObject.GTypeInput;
            recallAudioRunType: GObject.GTypeInput;
            recall_audio_type: GObject.GTypeInput;
            recallAudioType: GObject.GTypeInput;
            recall_channel: Recall[];
            recallChannel: Recall[];
            recall_channel_run: Recall[];
            recallChannelRun: Recall[];
            recall_channel_run_type: GObject.GTypeInput;
            recallChannelRunType: GObject.GTypeInput;
            recall_channel_type: GObject.GTypeInput;
            recallChannelType: GObject.GTypeInput;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallContainer extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<RecallContainer>;

        // Properties
        /**
         * The associated recall within audio context.
         * @since 3.0.0
         */
        get recall_audio(): RecallAudio;
        set recall_audio(val: RecallAudio);

        /**
         * The associated recall within audio context.
         * @since 3.0.0
         */
        get recallAudio(): RecallAudio;
        set recallAudio(val: RecallAudio);

        /**
         * The associated recall within dynamic audio context.
         * @since 3.0.0
         */
        get recall_audio_run(): Recall[];
        set recall_audio_run(val: Recall[]);

        /**
         * The associated recall within dynamic audio context.
         * @since 3.0.0
         */
        get recallAudioRun(): Recall[];
        set recallAudioRun(val: Recall[]);

        /**
         * The associated recall type within dynamic audio context.
         * @since 3.0.0
         */
        get recall_audio_run_type(): GObject.GType;
        set recall_audio_run_type(val: GObject.GType);

        /**
         * The associated recall type within dynamic audio context.
         * @since 3.0.0
         */
        get recallAudioRunType(): GObject.GType;
        set recallAudioRunType(val: GObject.GType);

        /**
         * The associated recall type within audio context.
         * @since 3.0.0
         */
        get recall_audio_type(): GObject.GType;
        set recall_audio_type(val: GObject.GType);

        /**
         * The associated recall type within audio context.
         * @since 3.0.0
         */
        get recallAudioType(): GObject.GType;
        set recallAudioType(val: GObject.GType);

        /**
         * The associated recall within channel context.
         * @since 3.0.0
         */
        get recall_channel(): Recall[];
        set recall_channel(val: Recall[]);

        /**
         * The associated recall within channel context.
         * @since 3.0.0
         */
        get recallChannel(): Recall[];
        set recallChannel(val: Recall[]);

        /**
         * The associated recall within dynamic channel context.
         * @since 3.0.0
         */
        get recall_channel_run(): Recall[];
        set recall_channel_run(val: Recall[]);

        /**
         * The associated recall within dynamic channel context.
         * @since 3.0.0
         */
        get recallChannelRun(): Recall[];
        set recallChannelRun(val: Recall[]);

        /**
         * The associated recall type within dynamic channel context.
         * @since 3.0.0
         */
        get recall_channel_run_type(): GObject.GType;
        set recall_channel_run_type(val: GObject.GType);

        /**
         * The associated recall type within dynamic channel context.
         * @since 3.0.0
         */
        get recallChannelRunType(): GObject.GType;
        set recallChannelRunType(val: GObject.GType);

        /**
         * The associated recall type within channel context.
         * @since 3.0.0
         */
        get recall_channel_type(): GObject.GType;
        set recall_channel_type(val: GObject.GType);

        /**
         * The associated recall type within channel context.
         * @since 3.0.0
         */
        get recallChannelType(): GObject.GType;
        set recallChannelType(val: GObject.GType);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallContainer.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: RecallContainerFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        // Constructors
        constructor(properties?: Partial<RecallContainer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): RecallContainer;

        // Signals
        /** @signal */
        connect_after<K extends keyof RecallContainer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallContainer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallContainer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallContainer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Finds {@link AgsAudio.Recall} for appropriate search criteria.
         * @param recall_container the {@link GLib.List}-struct containing {@link AgsAudio.RecallContainer}
         * @param type recall type
         * @param find_flags search mask
         * @param recall_id an {@link AgsAudio.RecallID}
         */
        static find(recall_container: RecallContainer[], type: GObject.GType, find_flags: RecallContainerFindFlags, recall_id: RecallID): Recall[];

        // Methods
        /**
         * Add `recall` to `recall_container`.
         * @param recall the {@link AgsAudio.Recall}
         */
        add(recall: Recall): void;

        /**
         * Retrieve recall audio of container.
         * @returns the {@link AgsAudio.RecallAudio}
         */
        get_recall_audio(): Recall;

        /**
         * Retrieve recall audio run of container.
         * @returns the {@link AgsAudio.RecallAudioRun} as list
         */
        get_recall_audio_run(): RecallAudioRun[];

        /**
         * Retrieve the recall channel of container.
         * @returns the {@link AgsAudio.RecallChannel}
         */
        get_recall_channel(): RecallChannel[];

        /**
         * Retrieve the recall channel run of container.
         * @returns the {@link AgsAudio.Recall}
         */
        get_recall_channel_run(): RecallChannelRun[];

        /**
         * Remove `recall` from `recall_container`.
         * @param recall the {@link AgsAudio.Recall}
         */
        remove(recall: Recall): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: number): void;

        /**
         * Test `flags` to be set on `recall_container`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace RecallDependency {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::dependency": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            dependency: Recall;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallDependency extends GObject.Object {
        static $gtype: GObject.GType<RecallDependency>;

        // Properties
        /**
         * The dependency.
         * @since 3.0.0
         */
        get dependency(): Recall;
        set dependency(val: Recall);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallDependency.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        // Constructors
        constructor(properties?: Partial<RecallDependency.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](dependency: GObject.Object): RecallDependency;

        // Signals
        /** @signal */
        connect<K extends keyof RecallDependency.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallDependency.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RecallDependency.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallDependency.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallDependency.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallDependency.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Retrieve dependency.
         * @param recall_dependency the {@link GLib.List}-struct containing  {@link AgsAudio.RecallDependency}
         * @param dependency the {@link AgsAudio.Recall} depending on
         */
        static find_dependency(recall_dependency: RecallDependency[], dependency: GObject.Object): RecallDependency[];

        /**
         * Retrieve dependency by provider.
         * @param recall_dependency the {@link GLib.List}-struct containing  {@link AgsAudio.RecallDependency}
         * @param provider the object providing recall, like {@link AgsAudio.Audio} or {@link AgsAudio.Channel}
         */
        static find_dependency_by_provider(recall_dependency: RecallDependency[], provider: GObject.Object): RecallDependency[];

        // Methods
        /**
         * Resolve dependency.
         * @param recall_id the {@link AgsAudio.RecallID} refering to
         * @returns the {@link AgsAudio.Recall} dependency.
         */
        resolve<T = GObject.Object>(recall_id: RecallID): T;
    }


    namespace RecallID {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::recycling-context": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            recycling_context: GObject.Object;
            recyclingContext: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallID extends GObject.Object {
        static $gtype: GObject.GType<RecallID>;

        // Properties
        /**
         * The dynamic run context belonging to.
         * @since 3.0.0
         */
        get recycling_context(): GObject.Object;
        set recycling_context(val: GObject.Object);

        /**
         * The dynamic run context belonging to.
         * @since 3.0.0
         */
        get recyclingContext(): GObject.Object;
        set recyclingContext(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallID.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        sound_scope: number;

        staging_flags: number;

        state_flags: number;

        // Constructors
        constructor(properties?: Partial<RecallID.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): RecallID;

        // Signals
        /** @signal */
        connect<K extends keyof RecallID.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallID.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RecallID.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallID.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallID.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallID.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Retrieve recall id by recycling context.
         * @param recall_id the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         * @param parent_recycling_context the {@link AgsAudio.RecyclingContext} to match
         */
        static find_parent_recycling_context(recall_id: RecallID[], parent_recycling_context: RecyclingContext): RecallID;

        /**
         * Retrieve recall id by recycling context.
         * @param recall_id the {@link GLib.List}-struct containing {@link AgsAudio.RecallID}
         * @param recycling_context the {@link AgsAudio.RecyclingContext} to match
         */
        static find_recycling_context(recall_id: RecallID[], recycling_context: RecyclingContext): RecallID;

        // Methods
        /**
         * Check if `sound_scope` is set for `recall_id`.
         * @param sound_scope the sound scope to check or -1 to check all
         * @returns `true` if sound scope matches, otherwise  `false`
         */
        check_sound_scope(sound_scope: number): boolean;

        /**
         * Check the occurence of `staging_flags` in `recall_id`.
         * @param staging_flags staging flags to check
         * @returns `true` if all flags matched, otherwise `false`
         */
        check_staging_flags(staging_flags: number): boolean;

        /**
         * Check the occurence of `state_flags` in `recall_id`.
         * @param state_flags state flags to check
         * @returns `true` if all flags matched, otherwise `false`
         */
        check_state_flags(state_flags: number): boolean;

        /**
         * Get recycling context.
         * @returns the {@link AgsAudio.RecyclingContext}
         */
        get_recycling_context(): RecyclingContext;

        /**
         * Set recycling context.
         * @param recycling_context the {@link AgsAudio.RecyclingContext}
         */
        set_recycling_context(recycling_context: RecyclingContext): void;

        /**
         * @param sound_scope 
         */
        set_sound_scope(sound_scope: number): void;

        /**
         * Set staging flags.
         * @param staging_flags staging flags to set
         */
        set_staging_flags(staging_flags: number): void;

        /**
         * Set state flags.
         * @param state_flags state flags to set
         */
        set_state_flags(state_flags: number): void;

        /**
         * Test `staging_flags` to be set on `recall_id`.
         * @param staging_flags the staging flags
         * @returns `true` if flags are set, else `false`
         */
        test_staging_flags(staging_flags: number): boolean;

        /**
         * Test `state_flags` to be set on `recall_id`.
         * @param state_flags the state flags
         * @returns `true` if flags are set, else `false`
         */
        test_state_flags(state_flags: number): boolean;

        /**
         * Unset staging flags.
         * @param staging_flags staging flags to unset
         */
        unset_staging_flags(staging_flags: number): void;

        /**
         * Unset state flags.
         * @param state_flags state flags to unset
         */
        unset_state_flags(state_flags: number): void;
    }


    namespace RecallRecycling {
        // Signal signatures
        interface SignalSignatures extends Recall.SignalSignatures {
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::child-destination": (pspec: GObject.ParamSpec) => void;
            "notify::child-source": (pspec: GObject.ParamSpec) => void;
            "notify::destination": (pspec: GObject.ParamSpec) => void;
            "notify::source": (pspec: GObject.ParamSpec) => void;
            "notify::automation-port": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::child-type": (pspec: GObject.ParamSpec) => void;
            "notify::effect": (pspec: GObject.ParamSpec) => void;
            "notify::effect-index": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::pad": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::port": (pspec: GObject.ParamSpec) => void;
            "notify::recall-container": (pspec: GObject.ParamSpec) => void;
            "notify::recall-dependency": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Recall.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio_channel: number;
            audioChannel: number;
            child_destination: AudioSignal;
            childDestination: AudioSignal;
            child_source: AudioSignal[];
            childSource: AudioSignal[];
            destination: Recycling;
            source: Recycling;
        }
    }

    /**
     * @gir-type Class
     */
    class RecallRecycling extends Recall implements Ags.Connectable {
        static $gtype: GObject.GType<RecallRecycling>;

        // Properties
        /**
         * The assigned destination recycling.
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The assigned destination recycling.
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The assigned destination audio signal.
         * @since 3.0.0
         */
        get child_destination(): AudioSignal;
        set child_destination(val: AudioSignal);

        /**
         * The assigned destination audio signal.
         * @since 3.0.0
         */
        get childDestination(): AudioSignal;
        set childDestination(val: AudioSignal);

        /**
         * The assigned source audio signal.
         * @since 3.0.0
         * @write-only
         */
        set child_source(val: AudioSignal[]);

        /**
         * The assigned source audio signal.
         * @since 3.0.0
         * @write-only
         */
        set childSource(val: AudioSignal[]);

        /**
         * The assigned destination recycling.
         * @since 3.0.0
         */
        get destination(): Recycling;
        set destination(val: Recycling);

        /**
         * The assigned source recycling.
         * @since 3.0.0
         */
        get source(): Recycling;
        set source(val: Recycling);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecallRecycling.SignalSignatures;

        // Fields
        recall: Recall;

        flags: number;

        // Constructors
        constructor(properties?: Partial<RecallRecycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): RecallRecycling;

        // Signals
        /** @signal */
        connect_after<K extends keyof RecallRecycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecallRecycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecallRecycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecallRecycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace Recycling {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            /**
             * The ::add-audio-signal signal is emited as adding {@link AgsAudio.AudioSignal}.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "add-audio-signal": (arg0: GObject.Object) => void;
            /**
             * The ::data-request signal is emited as requesting data for `audio_signal`.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "data-request": (arg0: GObject.Object) => void;
            /**
             * The ::remove-audio-signal signal is emited as removing {@link AgsAudio.AudioSignal}.
             * @signal
             * @since 3.0.0
             * @run-last
             */
            "remove-audio-signal": (arg0: GObject.Object) => void;
            "notify::audio-signal": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::input-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::next": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::output-soundcard-channel": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::prev": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            audio_signal: AudioSignal[];
            audioSignal: AudioSignal[];
            buffer_size: number;
            bufferSize: number;
            channel: Channel;
            format: number;
            input_soundcard: GObject.Object;
            inputSoundcard: GObject.Object;
            input_soundcard_channel: number;
            inputSoundcardChannel: number;
            next: Recycling;
            output_soundcard: GObject.Object;
            outputSoundcard: GObject.Object;
            output_soundcard_channel: number;
            outputSoundcardChannel: number;
            parent: Recycling;
            prev: Recycling;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Recycling extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<Recycling>;

        // Properties
        /**
         * The containing  {@link AgsAudio.AudioSignal}.
         * @since 3.0.0
         */
        get audio_signal(): AudioSignal[];
        set audio_signal(val: AudioSignal[]);

        /**
         * The containing  {@link AgsAudio.AudioSignal}.
         * @since 3.0.0
         */
        get audioSignal(): AudioSignal[];
        set audioSignal(val: AudioSignal[]);

        /**
         * The buffer size.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The assigned {@link AgsAudio.Channel}.
         * @since 3.0.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * The format.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned input soundcard.
         * @since 3.0.0
         */
        get input_soundcard(): GObject.Object;
        set input_soundcard(val: GObject.Object);

        /**
         * The assigned input soundcard.
         * @since 3.0.0
         */
        get inputSoundcard(): GObject.Object;
        set inputSoundcard(val: GObject.Object);

        /**
         * The input soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get input_soundcard_channel(): number;
        set input_soundcard_channel(val: number);

        /**
         * The input soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get inputSoundcardChannel(): number;
        set inputSoundcardChannel(val: number);

        /**
         * The assigned next {@link AgsAudio.Recycling}.
         * @since 3.0.0
         */
        get next(): Recycling;
        set next(val: Recycling);

        /**
         * The assigned output soundcard acting as default sink.
         * @since 3.0.0
         */
        get output_soundcard(): GObject.Object;
        set output_soundcard(val: GObject.Object);

        /**
         * The assigned output soundcard acting as default sink.
         * @since 3.0.0
         */
        get outputSoundcard(): GObject.Object;
        set outputSoundcard(val: GObject.Object);

        /**
         * The output soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get output_soundcard_channel(): number;
        set output_soundcard_channel(val: number);

        /**
         * The output soundcard channel.
         * @since 3.0.0
         * @default 0
         */
        get outputSoundcardChannel(): number;
        set outputSoundcardChannel(val: number);

        /**
         * The assigned parent {@link AgsAudio.Recycling}.
         * @since 3.0.0
         */
        get parent(): Recycling;
        set parent(val: Recycling);

        /**
         * The assigned prev {@link AgsAudio.Recycling}.
         * @since 3.0.0
         */
        get prev(): Recycling;
        set prev(val: Recycling);

        /**
         * The samplerate.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Recycling.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: number;

        uuid: Ags.UUID;

        // Constructors
        constructor(properties?: Partial<Recycling.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](output_soundcard: GObject.Object): Recycling;

        // Signals
        /** @signal */
        connect_after<K extends keyof Recycling.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Recycling.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Recycling.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Recycling.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * Add `audio_signal` to `recycling`.
         * @param audio_signal the {@link AgsAudio.AudioSignal} to add
         * @virtual
         */
        vfunc_add_audio_signal(audio_signal: AudioSignal): void;

        /**
         * Request data of `audio_signal`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         * @virtual
         */
        vfunc_data_request(audio_signal: AudioSignal): void;

        /**
         * Remove `audio_signal` of `recycling`.
         * @param audio_signal the {@link AgsAudio.AudioSignal} to remove
         * @virtual
         */
        vfunc_remove_audio_signal(audio_signal: AudioSignal): void;

        // Methods
        /**
         * Add `audio_signal` to `recycling`.
         * @param audio_signal the {@link AgsAudio.AudioSignal} to add
         */
        add_audio_signal(audio_signal: AudioSignal): void;

        /**
         * Create audio signal with defaults.
         * @param audio_signal the {@link AgsAudio.AudioSignal} to apply defaults
         * @param delay the delay
         * @param attack the attack
         */
        create_audio_signal_with_defaults(audio_signal: AudioSignal, delay: number, attack: number): void;

        /**
         * Create audio signal with frame count.
         * @param audio_signal the {@link AgsAudio.AudioSignal} to apply defaults
         * @param frame_count the audio data size
         * @param delay the delay
         * @param attack the attack
         */
        create_audio_signal_with_frame_count(audio_signal: AudioSignal, frame_count: number, delay: number, attack: number): void;

        /**
         * Request data of `audio_signal`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         */
        data_request(audio_signal: AudioSignal): void;

        /**
         * Retrieve next recycling with different channel.
         * @param end_region boundary end
         * @param prev_channel previous channel
         * @returns Matching recycling.
         */
        find_next_channel(end_region: Recycling, prev_channel: GObject.Object): Recycling;

        /**
         * Get recall id.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.AudioSignal}
         */
        get_audio_signal(): AudioSignal[];

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Get channel.
         * @returns the {@link AgsAudio.Channel}
         */
        get_channel<T = GObject.Object>(): T;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): number;

        /**
         * Get the input soundcard object of `recycling`.
         * @returns the input soundcard
         */
        get_input_soundcard<T = GObject.Object>(): T;

        /**
         * Get the output soundcard object of `recycling`.
         * @returns the output soundcard
         */
        get_output_soundcard<T = GObject.Object>(): T;

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Check if is active.
         * @param end_region boundary end
         * @param recall_id the {@link AgsAudio.RecallID}
         * @returns `true` if related audio signal to recall id is available, otherwise `false`
         */
        is_active(end_region: Recycling, recall_id: GObject.Object): boolean;

        /**
         * Retrieve position of recycling.
         * @param end_region boundary end
         * @param recycling matching recycling
         * @returns position within boundary.
         */
        position(end_region: Recycling, recycling: Recycling): number;

        /**
         * Remove `audio_signal` of `recycling`.
         * @param audio_signal the {@link AgsAudio.AudioSignal} to remove
         */
        remove_audio_signal(audio_signal: AudioSignal): void;

        /**
         * Set recall id by replacing existing.
         * @param audio_signal the {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        set_audio_signal(audio_signal: AudioSignal[]): void;

        /**
         * Set buffer size.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Set channel.
         * @param channel the {@link AgsAudio.Channel}
         */
        set_channel(channel: GObject.Object): void;

        /**
         * Enable a feature of `recycling`.
         * @param flags see {@link AgsAudio.RecyclingFlags}-enum
         */
        set_flags(flags: number): void;

        /**
         * Set format.
         * @param format the format
         */
        set_format(format: number): void;

        /**
         * Set the input soundcard object of `recycling`.
         * @param input_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_input_soundcard(input_soundcard: GObject.Object): void;

        /**
         * Set the output soundcard object of `recycling`.
         * @param output_soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_output_soundcard(output_soundcard: GObject.Object): void;

        /**
         * Sets samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Test `flags` to be set on `recycling`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Disable a feature of `recycling`.
         * @param flags see {@link AgsAudio.RecyclingFlags}-enum
         */
        unset_flags(flags: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace RecyclingContext {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::child": (pspec: GObject.ParamSpec) => void;
            "notify::length": (pspec: GObject.ParamSpec) => void;
            "notify::parent": (pspec: GObject.ParamSpec) => void;
            "notify::recall-id": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            child: RecyclingContext[];
            length: (bigint | number);
            parent: RecyclingContext;
            recall_id: RecallID;
            recallId: RecallID;
        }
    }

    /**
     * @gir-type Class
     */
    class RecyclingContext extends GObject.Object {
        static $gtype: GObject.GType<RecyclingContext>;

        // Properties
        /**
         * The child recycling contexts.
         * @since 3.0.0
         */
        get child(): RecyclingContext[];
        set child(val: RecyclingContext[]);

        /**
         * Boundary length.
         * @since 3.0.0
         * @default 0
         */
        get length(): number;
        set length(val: (bigint | number));

        /**
         * The parent recycling context within tree.
         * @since 3.0.0
         */
        get parent(): RecyclingContext;
        set parent(val: RecyclingContext);

        /**
         * The assigned {@link AgsAudio.RecallID}.
         * @since 3.0.0
         */
        get recall_id(): RecallID;
        set recall_id(val: RecallID);

        /**
         * The assigned {@link AgsAudio.RecallID}.
         * @since 3.0.0
         */
        get recallId(): RecallID;
        set recallId(val: RecallID);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RecyclingContext.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: RecyclingContextFlags;

        connectable_flags: Ags.ConnectableFlags;

        sound_scope: number;

        children: any[];

        recycling: Recycling;

        // Constructors
        constructor(properties?: Partial<RecyclingContext.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](length: (bigint | number)): RecyclingContext;

        // Signals
        /** @signal */
        connect<K extends keyof RecyclingContext.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecyclingContext.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RecyclingContext.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RecyclingContext.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RecyclingContext.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RecyclingContext.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Find matching `sound_scope` in `recycling_context`.
         * @param recycling_context the {@link GLib.List}-struct containing {@link AgsAudio.RecyclingContext}
         * @param sound_scope the sound scope
         */
        static find_scope(recycling_context: RecyclingContext[], sound_scope: number): RecyclingContext[];

        // Methods
        /**
         * Adds a recycling to a context.
         * @param recycling the {@link AgsAudio.Recycling} to add
         */
        add(recycling: Recycling): void;

        /**
         * Adds a recycling context as child.
         * @param child the child
         */
        add_child(child: RecyclingContext): void;

        /**
         * Find position of recycling within array.
         * @param recycling the {@link AgsAudio.Recycling} to look up
         * @returns recycling array index
         */
        find(recycling: Recycling): number;

        /**
         * Find position of recycling within arrays.
         * @param recycling the {@link AgsAudio.Recycling} to look up
         * @returns recycling array index
         */
        find_child(recycling: Recycling): number;

        /**
         * Find position of recycling within array.
         * @param recycling the {@link AgsAudio.Recycling} to look up
         * @returns recycling array index
         */
        find_parent(recycling: Recycling): number;

        /**
         * Retrieve all child recall ids.
         * @returns the {@link AgsAudio.RecallID} as {@link GLib.List}-struct
         */
        get_child_recall_id(): RecallID[];

        /**
         * Iterates the tree up to highest level.
         * @returns the topmost recycling context
         */
        get_toplevel(): RecyclingContext;

        /**
         * Inserts a recycling to a context.
         * @param recycling the {@link AgsAudio.Recycling} to insert
         * @param position the index to insert at
         */
        insert(recycling: Recycling, position: number): void;

        /**
         * Removes a recycling in a context.
         * @param recycling the {@link AgsAudio.Recycling} to remove
         */
        remove(recycling: Recycling): void;

        /**
         * Removes a recycling context of its parent.
         * @param child the child to remove
         */
        remove_child(child: RecyclingContext): void;

        /**
         * Replaces one recycling entry in a context.
         * @param recycling the {@link AgsAudio.Recycling} to add
         * @param position the index of `recycling`
         */
        replace(recycling: Recycling, position: number): void;

        /**
         * Modify recycling of context.
         * @param old_first_recycling the first recycling to replace
         * @param old_last_recycling the last recycling to replace
         * @param new_first_recycling the first recycling to insert
         * @param new_last_recycling the last recycling to insert
         * @returns the new {@link AgsAudio.RecyclingContext}
         */
        reset_recycling(old_first_recycling: Recycling, old_last_recycling: Recycling, new_first_recycling: Recycling, new_last_recycling: Recycling): RecyclingContext;
    }


    namespace RemoveAudio {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
        }
    }

    /**
     * @gir-type Class
     */
    class RemoveAudio extends Ags.Task {
        static $gtype: GObject.GType<RemoveAudio>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RemoveAudio.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<RemoveAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio): RemoveAudio;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof RemoveAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RemoveAudio.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RemoveAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RemoveAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RemoveAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RemoveAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace RemoveAudioSignal {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio-signal": (pspec: GObject.ParamSpec) => void;
            "notify::recycling": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio_signal: AudioSignal;
            audioSignal: AudioSignal;
            recycling: Recycling;
        }
    }

    /**
     * @gir-type Class
     */
    class RemoveAudioSignal extends Ags.Task {
        static $gtype: GObject.GType<RemoveAudioSignal>;

        // Properties
        /**
         * The assigned {@link AgsAudio.AudioSignal}
         * @since 3.0.0
         */
        get audio_signal(): AudioSignal;
        set audio_signal(val: AudioSignal);

        /**
         * The assigned {@link AgsAudio.AudioSignal}
         * @since 3.0.0
         */
        get audioSignal(): AudioSignal;
        set audioSignal(val: AudioSignal);

        /**
         * The assigned {@link AgsAudio.Recycling}
         * @since 3.0.0
         */
        get recycling(): Recycling;
        set recycling(val: Recycling);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RemoveAudioSignal.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<RemoveAudioSignal.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](recycling: Recycling, audio_signal: AudioSignal): RemoveAudioSignal;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof RemoveAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RemoveAudioSignal.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RemoveAudioSignal.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RemoveAudioSignal.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RemoveAudioSignal.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RemoveAudioSignal.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace RemoveNote {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channel": (pspec: GObject.ParamSpec) => void;
            "notify::note": (pspec: GObject.ParamSpec) => void;
            "notify::use-selection-list": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            audio_channel: number;
            audioChannel: number;
            note: Note;
            use_selection_list: boolean;
            useSelectionList: boolean;
        }
    }

    /**
     * @gir-type Class
     */
    class RemoveNote extends Ags.Task {
        static $gtype: GObject.GType<RemoveNote>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The assigned audio channel
         * @since 3.0.0
         * @default 0
         */
        get audio_channel(): number;
        set audio_channel(val: number);

        /**
         * The assigned audio channel
         * @since 3.0.0
         * @default 0
         */
        get audioChannel(): number;
        set audioChannel(val: number);

        /**
         * The assigned {@link AgsAudio.Note}
         * @since 3.0.0
         */
        get note(): Note;
        set note(val: Note);

        /**
         * The notation's use-selection-list.
         * @since 3.0.0
         * @default false
         */
        get use_selection_list(): boolean;
        set use_selection_list(val: boolean);

        /**
         * The notation's use-selection-list.
         * @since 3.0.0
         * @default false
         */
        get useSelectionList(): boolean;
        set useSelectionList(val: boolean);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RemoveNote.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<RemoveNote.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, note: Note, audio_channel: number, use_selection_list: boolean): RemoveNote;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof RemoveNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RemoveNote.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RemoveNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RemoveNote.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RemoveNote.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RemoveNote.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace RemoveSoundcard {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            soundcard: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class RemoveSoundcard extends Ags.Task {
        static $gtype: GObject.GType<RemoveSoundcard>;

        // Properties
        /**
         * The assigned {@link Ags.Soundcard}
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: RemoveSoundcard.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<RemoveSoundcard.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](soundcard: GObject.Object): RemoveSoundcard;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof RemoveSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RemoveSoundcard.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof RemoveSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, RemoveSoundcard.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof RemoveSoundcard.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<RemoveSoundcard.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace ResetFxAnalyse {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class ResetFxAnalyse extends Ags.Task {
        static $gtype: GObject.GType<ResetFxAnalyse>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ResetFxAnalyse.SignalSignatures;

        // Fields
        task: Ags.Task;

        fx_analyse_channel: any[];

        // Constructors
        constructor(properties?: Partial<ResetFxAnalyse.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): ResetFxAnalyse;

        // Signals
        /** @signal */
        connect<K extends keyof ResetFxAnalyse.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResetFxAnalyse.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ResetFxAnalyse.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResetFxAnalyse.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ResetFxAnalyse.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ResetFxAnalyse.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get instance.
         */
        static get_instance(): ResetFxAnalyse;

        // Methods
        /**
         * Add `fx_analyse_channel`.
         * @param fx_analyse_channel the {@link AgsAudio.FxAnalyseChannel}
         */
        add(fx_analyse_channel: FxAnalyseChannel): void;

        /**
         * Remove `fx_analyse_channel`.
         * @param fx_analyse_channel the {@link AgsAudio.FxAnalyseChannel}
         */
        remove(fx_analyse_channel: FxAnalyseChannel): void;
    }


    namespace ResetFxPeak {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class ResetFxPeak extends Ags.Task {
        static $gtype: GObject.GType<ResetFxPeak>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ResetFxPeak.SignalSignatures;

        // Fields
        task: Ags.Task;

        fx_peak_channel: any[];

        // Constructors
        constructor(properties?: Partial<ResetFxPeak.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): ResetFxPeak;

        // Signals
        /** @signal */
        connect<K extends keyof ResetFxPeak.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResetFxPeak.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ResetFxPeak.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResetFxPeak.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ResetFxPeak.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ResetFxPeak.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get instance.
         */
        static get_instance(): ResetFxPeak;

        // Methods
        /**
         * Add `fx_peak_channel`.
         * @param fx_peak_channel the {@link AgsAudio.FxPeakChannel}
         */
        add(fx_peak_channel: FxPeakChannel): void;

        /**
         * Remove `fx_peak_channel`.
         * @param fx_peak_channel the {@link AgsAudio.FxPeakChannel}
         */
        remove(fx_peak_channel: FxPeakChannel): void;
    }


    namespace ResetNote {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class ResetNote extends Ags.Task {
        static $gtype: GObject.GType<ResetNote>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ResetNote.SignalSignatures;

        // Fields
        task: Ags.Task;

        note_offset: number;

        note: any[];

        // Constructors
        constructor(properties?: Partial<ResetNote.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): ResetNote;

        // Signals
        /** @signal */
        connect<K extends keyof ResetNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResetNote.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ResetNote.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResetNote.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ResetNote.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ResetNote.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get instance.
         */
        static get_instance(): ResetNote;

        // Methods
        /**
         * Add `note`.
         * @param note the {@link AgsAudio.Note}
         */
        add(note: Note): void;

        /**
         * Remove `note`.
         * @param note the {@link AgsAudio.Note}
         */
        remove(note: Note): void;
    }


    namespace ResetRecallStaging {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class ResetRecallStaging extends Ags.Task {
        static $gtype: GObject.GType<ResetRecallStaging>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ResetRecallStaging.SignalSignatures;

        // Fields
        task: Ags.Task;

        staging_flags: SoundStagingFlags;

        recall: any[];

        // Constructors
        constructor(properties?: Partial<ResetRecallStaging.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): ResetRecallStaging;

        // Signals
        /** @signal */
        connect<K extends keyof ResetRecallStaging.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResetRecallStaging.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ResetRecallStaging.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResetRecallStaging.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ResetRecallStaging.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ResetRecallStaging.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Get instance.
         */
        static get_instance(): ResetRecallStaging;

        // Methods
        /**
         * Add `recall`.
         * @param recall the {@link AgsAudio.Recall}
         */
        add(recall: Recall): void;

        /**
         * Remove `recall`.
         * @param recall the {@link AgsAudio.Recall}
         */
        remove(recall: Recall): void;
    }


    namespace ResizeAudio {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::input-pads": (pspec: GObject.ParamSpec) => void;
            "notify::output-pads": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            audio_channels: number;
            audioChannels: number;
            input_pads: number;
            inputPads: number;
            output_pads: number;
            outputPads: number;
        }
    }

    /**
     * @gir-type Class
     */
    class ResizeAudio extends Ags.Task {
        static $gtype: GObject.GType<ResizeAudio>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The count of audio channels to apply to audio.
         * @since 3.0.0
         * @default 0
         */
        get audio_channels(): number;
        set audio_channels(val: number);

        /**
         * The count of audio channels to apply to audio.
         * @since 3.0.0
         * @default 0
         */
        get audioChannels(): number;
        set audioChannels(val: number);

        /**
         * The count of input pads to apply to audio.
         * @since 3.0.0
         * @default 0
         */
        get input_pads(): number;
        set input_pads(val: number);

        /**
         * The count of input pads to apply to audio.
         * @since 3.0.0
         * @default 0
         */
        get inputPads(): number;
        set inputPads(val: number);

        /**
         * The count of output pads to apply to audio.
         * @since 3.0.0
         * @default 0
         */
        get output_pads(): number;
        set output_pads(val: number);

        /**
         * The count of output pads to apply to audio.
         * @since 3.0.0
         * @default 0
         */
        get outputPads(): number;
        set outputPads(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: ResizeAudio.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<ResizeAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, output_pads: number, input_pads: number, audio_channels: number): ResizeAudio;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof ResizeAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResizeAudio.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof ResizeAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, ResizeAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof ResizeAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<ResizeAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SF2Loader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-container": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::instrument": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: Audio;
            audio_container: AudioContainer;
            audioContainer: AudioContainer;
            filename: string;
            instrument: string;
            preset: string;
        }
    }

    /**
     * @gir-type Class
     */
    class SF2Loader extends GObject.Object {
        static $gtype: GObject.GType<SF2Loader>;

        // Properties
        /**
         * The assigned audio.
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The audio container opened.
         * @since 3.0.0
         */
        get audio_container(): AudioContainer;
        set audio_container(val: AudioContainer);

        /**
         * The audio container opened.
         * @since 3.0.0
         */
        get audioContainer(): AudioContainer;
        set audioContainer(val: AudioContainer);

        /**
         * The filename to open.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The instrument to open.
         * @since 3.0.0
         * @default null
         */
        get instrument(): string;
        set instrument(val: string);

        /**
         * The preset to open.
         * @since 3.0.0
         * @default null
         */
        get preset(): string;
        set preset(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SF2Loader.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: SF2LoaderFlags;

        connectable_flags: Ags.ConnectableFlags;

        base_note: number;

        count: number;

        // Constructors
        constructor(properties?: Partial<SF2Loader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, filename: string, preset: string, instrument: string): SF2Loader;

        // Signals
        /** @signal */
        connect<K extends keyof SF2Loader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SF2Loader.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SF2Loader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SF2Loader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SF2Loader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SF2Loader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get audio of `sf2_loader`.
         * @returns the assigned {@link AgsAudio.Audio}
         */
        get_audio(): Audio;

        /**
         * Get audio container of `sf2_loader`.
         * @returns the assigned {@link AgsAudio.AudioContainer}
         */
        get_audio_container(): AudioContainer;

        /**
         * Get filename of `sf2_loader`.
         * @returns the assigned filename
         */
        get_filename(): string;

        /**
         * Get instrument of `sf2_loader`.
         * @returns the assigned instrument
         */
        get_instrument(): string;

        /**
         * Get preset of `sf2_loader`.
         * @returns the assigned preset
         */
        get_preset(): string;

        /**
         * Set audio of `sf2_loader`.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio): void;

        /**
         * Set audio container of `sf2_loader`.
         * @param audio_container the {@link AgsAudio.AudioContainer}
         */
        set_audio_container(audio_container: AudioContainer): void;

        /**
         * Set filename of `sf2_loader`.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: SF2LoaderFlags): void;

        /**
         * Set instrument of `sf2_loader`.
         * @param instrument the instrument
         */
        set_instrument(instrument: string): void;

        /**
         * Set preset of `sf2_loader`.
         * @param preset the preset
         */
        set_preset(preset: string): void;

        /**
         * Start `sf2_loader`.
         */
        start(): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: SF2LoaderFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: SF2LoaderFlags): void;
    }


    namespace SF2MidiLocaleLoader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-container": (pspec: GObject.ParamSpec) => void;
            "notify::bank": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::program": (pspec: GObject.ParamSpec) => void;
            "notify::synth": (pspec: GObject.ParamSpec) => void;
            "notify::synth-template": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: Audio;
            audio_container: AudioContainer;
            audioContainer: AudioContainer;
            bank: number;
            filename: string;
            program: number;
            synth: any;
            synth_template: any;
            synthTemplate: any;
        }
    }

    /**
     * @gir-type Class
     */
    class SF2MidiLocaleLoader extends GObject.Object {
        static $gtype: GObject.GType<SF2MidiLocaleLoader>;

        // Properties
        /**
         * The assigned audio.
         * @since 3.16.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The audio container opened.
         * @since 3.16.0
         */
        get audio_container(): AudioContainer;
        set audio_container(val: AudioContainer);

        /**
         * The audio container opened.
         * @since 3.16.0
         */
        get audioContainer(): AudioContainer;
        set audioContainer(val: AudioContainer);

        /**
         * The bank to open.
         * @since 3.16.0
         * @default -1
         */
        get bank(): number;
        set bank(val: number);

        /**
         * The filename to open.
         * @since 3.16.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The program to open.
         * @since 3.16.0
         * @default -1
         */
        get program(): number;
        set program(val: number);

        /**
         * The assigned synth.
         * @since 3.16.0
         */
        get synth(): any;
        set synth(val: any);

        /**
         * The assigned synth template.
         * @since 3.16.0
         */
        get synth_template(): any;
        set synth_template(val: any);

        /**
         * The assigned synth template.
         * @since 3.16.0
         */
        get synthTemplate(): any;
        set synthTemplate(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SF2MidiLocaleLoader.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: SF2MidiLocaleLoaderFlags;

        connectable_flags: Ags.ConnectableFlags;

        base_note: number;

        count: number;

        // Constructors
        constructor(properties?: Partial<SF2MidiLocaleLoader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, filename: string, bank: number, program: number): SF2MidiLocaleLoader;

        // Signals
        /** @signal */
        connect<K extends keyof SF2MidiLocaleLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SF2MidiLocaleLoader.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SF2MidiLocaleLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SF2MidiLocaleLoader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SF2MidiLocaleLoader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SF2MidiLocaleLoader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get audio of `sf2_midi_locale_loader`.
         * @returns the assigned {@link AgsAudio.Audio}
         */
        get_audio(): Audio;

        /**
         * Get audio container of `sf2_midi_locale_loader`.
         * @returns the assigned {@link AgsAudio.AudioContainer}
         */
        get_audio_container(): AudioContainer;

        /**
         * Get bank of `sf2_midi_locale_loader`.
         * @returns the assigned bank
         */
        get_bank(): number;

        /**
         * Get filename of `sf2_midi_locale_loader`.
         * @returns the assigned filename
         */
        get_filename(): string;

        /**
         * Get program of `sf2_midi_locale_loader`.
         * @returns the assigned program
         */
        get_program(): number;

        /**
         * Get {@link AgsAudio.SF2SynthUtil}-struct synth of `sf2_midi_locale_loader`.
         * @returns the assigned {@link AgsAudio.SF2SynthUtil}-struct
         */
        get_synth(): SF2SynthUtil;

        /**
         * Get {@link AgsAudio.SF2SynthUtil}-struct synth template of `sf2_midi_locale_loader`.
         * @returns the assigned {@link AgsAudio.SF2SynthUtil}-struct
         */
        get_synth_template(): SF2SynthUtil;

        /**
         * Set audio of `sf2_midi_locale_loader`.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio): void;

        /**
         * Set audio container of `sf2_midi_locale_loader`.
         * @param audio_container the {@link AgsAudio.AudioContainer}
         */
        set_audio_container(audio_container: AudioContainer): void;

        /**
         * Set bank of `sf2_midi_locale_loader`.
         * @param bank the bank
         */
        set_bank(bank: number): void;

        /**
         * Set filename of `sf2_midi_locale_loader`.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: SF2MidiLocaleLoaderFlags): void;

        /**
         * Set program of `sf2_midi_locale_loader`.
         * @param program the program
         */
        set_program(program: number): void;

        /**
         * Set {@link AgsAudio.SF2SynthUtil}-struct synth of `sf2_midi_locale_loader`.
         * @param synth the {@link AgsAudio.SF2SynthUtil}-struct
         */
        set_synth(synth: SF2SynthUtil): void;

        /**
         * Set {@link AgsAudio.SF2SynthUtil}-struct synth template of `sf2_midi_locale_loader`.
         * @param synth_template the {@link AgsAudio.SF2SynthUtil}-struct
         */
        set_synth_template(synth_template: SF2SynthUtil): void;

        /**
         * Start `sf2_midi_locale_loader`.
         */
        start(): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: SF2MidiLocaleLoaderFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: SF2MidiLocaleLoaderFlags): void;
    }


    namespace SF2SynthGenerator {
        // Signal signatures
        interface SignalSignatures extends Ags.Function.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bank": (pspec: GObject.ParamSpec) => void;
            "notify::base-key": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::instrument": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::preset": (pspec: GObject.ParamSpec) => void;
            "notify::program": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::tuning": (pspec: GObject.ParamSpec) => void;
            "notify::volume": (pspec: GObject.ParamSpec) => void;
            "notify::normalized-function": (pspec: GObject.ParamSpec) => void;
            "notify::pivot-table": (pspec: GObject.ParamSpec) => void;
            "notify::source-function": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Function.ConstructorProps {
            attack: number;
            bank: number;
            base_key: number;
            baseKey: number;
            buffer_size: number;
            bufferSize: number;
            delay: number;
            filename: string;
            format: number;
            frame_count: number;
            frameCount: number;
            instrument: string;
            loop_end: number;
            loopEnd: number;
            loop_start: number;
            loopStart: number;
            pitch_type: string;
            pitchType: string;
            preset: string;
            program: number;
            samplerate: number;
            tuning: number;
            volume: number;
        }
    }

    /**
     * @gir-type Class
     */
    class SF2SynthGenerator extends Ags.Function {
        static $gtype: GObject.GType<SF2SynthGenerator>;

        // Properties
        /**
         * The attack to be used.
         * @since 3.4.11
         * @default 0
         */
        get attack(): number;
        set attack(val: number);

        /**
         * The bank to be used.
         * @since 3.4.0
         * @default 0
         */
        get bank(): number;
        set bank(val: number);

        /**
         * The base key to be used.
         * @since 3.4.0
         * @default -48
         */
        get base_key(): number;
        set base_key(val: number);

        /**
         * The base key to be used.
         * @since 3.4.0
         * @default -48
         */
        get baseKey(): number;
        set baseKey(val: number);

        /**
         * The buffer size to be used.
         * @since 3.4.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.4.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The delay to be used.
         * @since 3.4.11
         * @default 0
         */
        get delay(): number;
        set delay(val: number);

        /**
         * The assigned filename.
         * @since 3.4.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The format to be used.
         * @since 3.4.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The frame count to be used.
         * @since 3.4.0
         * @default 0
         */
        get frame_count(): number;
        set frame_count(val: number);

        /**
         * The frame count to be used.
         * @since 3.4.0
         * @default 0
         */
        get frameCount(): number;
        set frameCount(val: number);

        /**
         * The assigned instrument.
         * @since 3.4.0
         * @default null
         */
        get instrument(): string;
        set instrument(val: string);

        /**
         * The loop end to be used.
         * @since 3.4.0
         * @default 0
         */
        get loop_end(): number;
        set loop_end(val: number);

        /**
         * The loop end to be used.
         * @since 3.4.0
         * @default 0
         */
        get loopEnd(): number;
        set loopEnd(val: number);

        /**
         * The loop start to be used.
         * @since 3.4.0
         * @default 0
         */
        get loop_start(): number;
        set loop_start(val: number);

        /**
         * The loop start to be used.
         * @since 3.4.0
         * @default 0
         */
        get loopStart(): number;
        set loopStart(val: number);

        /**
         * The pitch type to be used.
         * @since 3.9.0
         * @default ags-fluid-4th-order
         */
        get pitch_type(): string;
        set pitch_type(val: string);

        /**
         * The pitch type to be used.
         * @since 3.9.0
         * @default ags-fluid-4th-order
         */
        get pitchType(): string;
        set pitchType(val: string);

        /**
         * The assigned preset.
         * @since 3.4.0
         * @default null
         */
        get preset(): string;
        set preset(val: string);

        /**
         * The program to be used.
         * @since 3.4.0
         * @default 0
         */
        get program(): number;
        set program(val: number);

        /**
         * The samplerate to be used.
         * @since 3.4.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The tuning to be used.
         * @since 3.4.0
         * @default 0
         */
        get tuning(): number;
        set tuning(val: number);

        /**
         * The volume to be used.
         * @since 3.4.11
         * @default 1
         */
        get volume(): number;
        set volume(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SF2SynthGenerator.SignalSignatures;

        // Fields
        "function": Ags.Function;

        flags: SF2SynthGeneratorFlags;

        timestamp: GObject.Object;

        // Constructors
        constructor(properties?: Partial<SF2SynthGenerator.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): SF2SynthGenerator;

        // Signals
        /** @signal */
        connect<K extends keyof SF2SynthGenerator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SF2SynthGenerator.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SF2SynthGenerator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SF2SynthGenerator.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SF2SynthGenerator.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SF2SynthGenerator.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Compute SF2 synth for `note`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         * @param note the note to compute
         */
        compute(audio_signal: GObject.Object, note: number): void;

        /**
         * Compute instrument for `note`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         * @param preset the preset
         * @param instrument the instrument
         * @param note the note to compute
         */
        compute_instrument(audio_signal: GObject.Object, preset: string, instrument: string, note: number): void;

        /**
         * Compute synth for `note`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         * @param bank the bank
         * @param program the program
         * @param note the note to compute
         */
        compute_midi_locale(audio_signal: GObject.Object, bank: number, program: number, note: number): void;

        /**
         * Gets bank.
         * @returns the bank
         */
        get_bank(): number;

        /**
         * Gets base key.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Gets delay.
         * @returns the delay
         */
        get_delay(): number;

        /**
         * Gets filename.
         * @returns the filename
         */
        get_filename(): string;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Gets frame count.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Gets instrument.
         * @returns the instrument
         */
        get_instrument(): string;

        /**
         * Gets loop end.
         * @returns the loop end
         */
        get_loop_end(): number;

        /**
         * Gets loop start.
         * @returns the loop start
         */
        get_loop_start(): number;

        /**
         * Gets pitch type.
         * @returns the pitch type
         */
        get_pitch_type(): string;

        /**
         * Gets preset.
         * @returns the preset
         */
        get_preset(): string;

        /**
         * Gets program.
         * @returns the program
         */
        get_program(): number;

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * Gets tuning.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Set buffer size.
         * @param bank the buffer size
         */
        set_bank(bank: number): void;

        /**
         * Sets base key.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set buffer size.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Sets delay.
         * @param delay the delay
         */
        set_delay(delay: number): void;

        /**
         * Set filename.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set `flags` on `sf2_synth_generator`.
         * @param flags the flags
         */
        set_flags(flags: SF2SynthGeneratorFlags): void;

        /**
         * Set format.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Sets frame count.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set instrument.
         * @param instrument the instrument
         */
        set_instrument(instrument: string): void;

        /**
         * Sets loop end.
         * @param loop_end the loop end
         */
        set_loop_end(loop_end: number): void;

        /**
         * Sets loop start.
         * @param loop_start the loop start
         */
        set_loop_start(loop_start: number): void;

        /**
         * Set pitch type.
         * @param pitch_type the pitch type
         */
        set_pitch_type(pitch_type: string): void;

        /**
         * Set preset.
         * @param preset the preset
         */
        set_preset(preset: string): void;

        /**
         * Set buffer size.
         * @param program the buffer size
         */
        set_program(program: number): void;

        /**
         * Set samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Sets tuning.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Test `flags` to be set on `sf2_synth_generator`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: SF2SynthGeneratorFlags): boolean;

        /**
         * Unset `flags` on `sf2_synth_generator`.
         * @param flags the flags
         */
        unset_flags(flags: SF2SynthGeneratorFlags): void;
    }


    namespace SFZFile {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::group": (pspec: GObject.ParamSpec) => void;
            "notify::mode": (pspec: GObject.ParamSpec) => void;
            "notify::region": (pspec: GObject.ParamSpec) => void;
            "notify::sample": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, SoundContainer.ConstructorProps {
            filename: string;
            group: SFZGroup[];
            mode: string;
            region: SFZRegion[];
            sample: SFZSample[];
            soundcard: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SFZFile extends GObject.Object implements Ags.Connectable, SoundContainer {
        static $gtype: GObject.GType<SFZFile>;

        // Properties
        /**
         * The assigned filename.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The containing groups.
         * @since 3.0.0
         */
        get group(): SFZGroup[];
        set group(val: SFZGroup[]);

        /**
         * The assigned mode.
         * @since 3.0.0
         * @default null
         */
        get mode(): string;
        set mode(val: string);

        /**
         * The containing regions.
         * @since 3.0.0
         */
        get region(): SFZRegion[];
        set region(val: SFZRegion[]);

        /**
         * The containing samples.
         * @since 3.0.0
         */
        get sample(): SFZSample[];
        set sample(val: SFZSample[]);

        /**
         * The assigned soundcard.
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SFZFile.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        file: any;

        nesting_level: number;

        level_id: string;

        level_index: number;

        reader: GObject.Object;

        writer: GObject.Object;

        index_selected: number;

        name_selected: string;

        current_sample: any;

        audio_signal: any[];

        // Constructors
        constructor(properties?: Partial<SFZFile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): SFZFile;

        // Signals
        /** @signal */
        connect_after<K extends keyof SFZFile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZFile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SFZFile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SFZFile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Check `filename`'s suffix to be supported.
         * @param filename the filename
         */
        static check_suffix(filename: string): boolean;

        // Methods
        /**
         * Get the groups of `sfz_file`.
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.SFZGroup}
         */
        get_group(): SFZGroup[];

        /**
         * Get range of `sfz_file`, set return location `hikey` and `lokey`.
         */
        get_range(): [number, number];

        /**
         * Get the regions of `sfz_file`.
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.SFZRegion}
         */
        get_region(): SFZRegion[];

        /**
         * Get the samples of `sfz_file`.
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.SFZSample}
         */
        get_sample(): SFZSample[];

        /**
         * Parse `sfz_file`.
         */
        parse(): void;

        /**
         * Select sample.
         * @param sample_index the sample index
         * @returns `true` on success, else `false` on failure
         */
        select_sample(sample_index: number): boolean;

        /**
         * Set the group field of `sfz_file`
         * @param group the {@link GLib.List}-struct containing {@link AgsAudio.SFZGroup}
         */
        set_group(group: SFZGroup[]): void;

        /**
         * Set the region field of `sfz_file`
         * @param region the {@link GLib.List}-struct containing {@link AgsAudio.SFZRegion}
         */
        set_region(region: SFZRegion[]): void;

        /**
         * Set the sample field of `sfz_file`
         * @param sample the {@link GLib.List}-struct containing {@link AgsAudio.SFZSample}
         */
        set_sample(sample: SFZSample[]): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Close `sound_container`.
         */
        close(): void;

        /**
         * Get level count.
         * @returns the level count
         */
        get_level_count(): number;

        /**
         * Get level id.
         * @returns the level id as string
         */
        get_level_id(): string;

        /**
         * Get level index.
         * @returns the level index unsigned integer
         */
        get_level_index(): number;

        /**
         * Get nesting level.
         * @returns the nesting level
         */
        get_nesting_level(): number;

        /**
         * Get all resources as a {@link GLib.List}-struct.
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_all(): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @param resource_index the index
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_by_index(resource_index: number): GObject.Object[];

        /**
         * Get resources by name as a {@link GLib.List}-struct.
         * @param resource_name the resource name
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_by_name(resource_name: string): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_current(): GObject.Object[];

        /**
         * Get sublevel names.
         * @returns the string vector containing sublevel names
         */
        get_sublevel_name(): string[];

        /**
         * Level up.
         * @param level_count the count of levels to go up
         * @returns the level count went up
         */
        level_up(level_count: number): number;

        /**
         * Open `sound_container`.
         * @param filename the filename
         * @returns `true` if open succeeded, else `false`
         */
        open(filename: string): boolean;

        /**
         * Select level by id.
         * @param level_id the level id as string
         * @returns the index of level as unsigned integer
         */
        select_level_by_id(level_id: string): number;

        /**
         * Select level by index.
         * @param level_index the level index unsigned integer
         * @returns the index of level as unsigned integer
         */
        select_level_by_index(level_index: number): number;

        /**
         * Close `sound_container`.
         * @virtual
         */
        vfunc_close(): void;

        /**
         * Get level count.
         * @virtual
         */
        vfunc_get_level_count(): number;

        /**
         * Get level id.
         * @virtual
         */
        vfunc_get_level_id(): string;

        /**
         * Get level index.
         * @virtual
         */
        vfunc_get_level_index(): number;

        /**
         * Get nesting level.
         * @virtual
         */
        vfunc_get_nesting_level(): number;

        /**
         * Get all resources as a {@link GLib.List}-struct.
         * @virtual
         */
        vfunc_get_resource_all(): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @param resource_index the index
         * @virtual
         */
        vfunc_get_resource_by_index(resource_index: number): GObject.Object[];

        /**
         * Get resources by name as a {@link GLib.List}-struct.
         * @param resource_name the resource name
         * @virtual
         */
        vfunc_get_resource_by_name(resource_name: string): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @virtual
         */
        vfunc_get_resource_current(): GObject.Object[];

        /**
         * Get sublevel names.
         * @virtual
         */
        vfunc_get_sublevel_name(): string[];

        /**
         * Level up.
         * @param level_count the count of levels to go up
         * @virtual
         */
        vfunc_level_up(level_count: number): number;

        /**
         * Open `sound_container`.
         * @param filename the filename
         * @virtual
         */
        vfunc_open(filename: string): boolean;

        /**
         * Select level by id.
         * @param level_id the level id as string
         * @virtual
         */
        vfunc_select_level_by_id(level_id: string): number;

        /**
         * Select level by index.
         * @param level_index the level index unsigned integer
         * @virtual
         */
        vfunc_select_level_by_index(level_index: number): number;
    }


    namespace SFZGroup {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::nth-group": (pspec: GObject.ParamSpec) => void;
            "notify::region": (pspec: GObject.ParamSpec) => void;
            "notify::sample": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            nth_group: number;
            nthGroup: number;
            region: SFZRegion[];
            sample: SFZSample;
        }
    }

    /**
     * @gir-type Class
     */
    class SFZGroup extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<SFZGroup>;

        // Properties
        /**
         * The nth group.
         * @since 3.0.0
         * @default -1
         */
        get nth_group(): number;
        set nth_group(val: number);

        /**
         * The nth group.
         * @since 3.0.0
         * @default -1
         */
        get nthGroup(): number;
        set nthGroup(val: number);

        /**
         * The region assigned with.
         * @since 3.0.0
         */
        get region(): SFZRegion[];
        set region(val: SFZRegion[]);

        /**
         * The sample assigned with.
         * @since 3.0.0
         */
        get sample(): SFZSample;
        set sample(val: SFZSample);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SFZGroup.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        // Constructors
        constructor(properties?: Partial<SFZGroup.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): SFZGroup;

        // Signals
        /** @signal */
        connect_after<K extends keyof SFZGroup.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZGroup.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SFZGroup.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SFZGroup.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get all control of `sfz_group`.
         * @returns the {@link GLib.List}-struct containing controls as string
         */
        get_control(): string[];

        /**
         * Get the regions of `sfz_group`.
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.SFZRegion}
         */
        get_region(): SFZRegion[];

        /**
         * Get the samples of `sfz_group`.
         * @returns the {@link AgsAudio.SFZSample}
         */
        get_sample<T = GObject.Object>(): T;

        /**
         * Insert control specified by `key` and `value` to `sfz_group`.
         * @param key the key
         * @param value the value
         */
        insert_control(key: string, value: string): void;

        /**
         * Lookup control specified by `key` of `sfz_group`.
         * @param key the key
         * @returns the matching value of key
         */
        lookup_control(key: string): string;

        /**
         * Set the region field of `sfz_group`
         * @param region the {@link GLib.List}-struct containing {@link AgsAudio.SFZRegion}
         */
        set_region(region: SFZRegion[]): void;

        /**
         * Set the sample field of `sfz_group`
         * @param sample the {@link AgsAudio.SFZSample}
         */
        set_sample(sample: GObject.Object): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace SFZInstrumentLoader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-container": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::synth": (pspec: GObject.ParamSpec) => void;
            "notify::synth-template": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: Audio;
            audio_container: AudioContainer;
            audioContainer: AudioContainer;
            filename: string;
            synth: any;
            synth_template: any;
            synthTemplate: any;
        }
    }

    /**
     * @gir-type Class
     */
    class SFZInstrumentLoader extends GObject.Object {
        static $gtype: GObject.GType<SFZInstrumentLoader>;

        // Properties
        /**
         * The assigned audio.
         * @since 3.17.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The audio container opened.
         * @since 3.17.0
         */
        get audio_container(): AudioContainer;
        set audio_container(val: AudioContainer);

        /**
         * The audio container opened.
         * @since 3.17.0
         */
        get audioContainer(): AudioContainer;
        set audioContainer(val: AudioContainer);

        /**
         * The filename to open.
         * @since 3.17.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The assigned synth.
         * @since 3.17.0
         */
        get synth(): any;
        set synth(val: any);

        /**
         * The assigned synth template.
         * @since 3.17.0
         */
        get synth_template(): any;
        set synth_template(val: any);

        /**
         * The assigned synth template.
         * @since 3.17.0
         */
        get synthTemplate(): any;
        set synthTemplate(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SFZInstrumentLoader.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: SFZInstrumentLoaderFlags;

        connectable_flags: Ags.ConnectableFlags;

        base_note: number;

        count: number;

        // Constructors
        constructor(properties?: Partial<SFZInstrumentLoader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, filename: string): SFZInstrumentLoader;

        // Signals
        /** @signal */
        connect<K extends keyof SFZInstrumentLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZInstrumentLoader.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SFZInstrumentLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZInstrumentLoader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SFZInstrumentLoader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SFZInstrumentLoader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get audio of `sfz_instrument_loader`.
         * @returns the assigned {@link AgsAudio.Audio}
         */
        get_audio(): Audio;

        /**
         * Get audio container of `sfz_instrument_loader`.
         * @returns the assigned {@link AgsAudio.AudioContainer}
         */
        get_audio_container(): AudioContainer;

        /**
         * Get filename of `sfz_instrument_loader`.
         * @returns the assigned filename
         */
        get_filename(): string;

        /**
         * Get {@link AgsAudio.SFZSynthUtil}-struct synth of `sfz_instrument_loader`.
         * @returns the assigned {@link AgsAudio.SFZSynthUtil}-struct
         */
        get_synth(): SFZSynthUtil;

        /**
         * Get {@link AgsAudio.SFZSynthUtil}-struct synth template of `sfz_instrument_loader`.
         * @returns the assigned {@link AgsAudio.SFZSynthUtil}-struct
         */
        get_synth_template(): SFZSynthUtil;

        /**
         * Set audio of `sfz_instrument_loader`.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio): void;

        /**
         * Set audio container of `sfz_instrument_loader`.
         * @param audio_container the {@link AgsAudio.AudioContainer}
         */
        set_audio_container(audio_container: AudioContainer): void;

        /**
         * Set filename of `sfz_instrument_loader`.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: SFZInstrumentLoaderFlags): void;

        /**
         * Set {@link AgsAudio.SFZSynthUtil}-struct synth of `sfz_instrument_loader`.
         * @param synth the {@link AgsAudio.SFZSynthUtil}-struct
         */
        set_synth(synth: SFZSynthUtil): void;

        /**
         * Set {@link AgsAudio.SFZSynthUtil}-struct synth template of `sfz_instrument_loader`.
         * @param synth_template the {@link AgsAudio.SFZSynthUtil}-struct
         */
        set_synth_template(synth_template: SFZSynthUtil): void;

        /**
         * Start `sfz_instrument_loader`.
         */
        start(): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: SFZInstrumentLoaderFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: SFZInstrumentLoaderFlags): void;
    }


    namespace SFZLoader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-container": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: Audio;
            audio_container: AudioContainer;
            audioContainer: AudioContainer;
            filename: string;
        }
    }

    /**
     * @gir-type Class
     */
    class SFZLoader extends GObject.Object {
        static $gtype: GObject.GType<SFZLoader>;

        // Properties
        /**
         * The assigned audio.
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The audio container opened.
         * @since 3.0.0
         */
        get audio_container(): AudioContainer;
        set audio_container(val: AudioContainer);

        /**
         * The audio container opened.
         * @since 3.0.0
         */
        get audioContainer(): AudioContainer;
        set audioContainer(val: AudioContainer);

        /**
         * The filename to open.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SFZLoader.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: SFZLoaderFlags;

        connectable_flags: Ags.ConnectableFlags;

        base_note: number;

        count: number;

        // Constructors
        constructor(properties?: Partial<SFZLoader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, filename: string, do_replace: boolean): SFZLoader;

        // Signals
        /** @signal */
        connect<K extends keyof SFZLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZLoader.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SFZLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZLoader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SFZLoader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SFZLoader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get audio of `sfz_loader`.
         * @returns the assigned {@link AgsAudio.Audio}
         */
        get_audio(): Audio;

        /**
         * Get audio container of `sfz_loader`.
         * @returns the assigned {@link AgsAudio.AudioContainer}
         */
        get_audio_container(): AudioContainer;

        /**
         * Get filename of `sfz_loader`.
         * @returns the assigned filename
         */
        get_filename(): string;

        /**
         * Set audio of `sfz_loader`.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio): void;

        /**
         * Set audio container of `sfz_loader`.
         * @param audio_container the {@link AgsAudio.AudioContainer}
         */
        set_audio_container(audio_container: AudioContainer): void;

        /**
         * Set filename of `sfz_loader`.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: SFZLoaderFlags): void;

        /**
         * Start `sfz_loader`.
         */
        start(): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: SFZLoaderFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: SFZLoaderFlags): void;
    }


    namespace SFZRegion {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::group": (pspec: GObject.ParamSpec) => void;
            "notify::nth-region": (pspec: GObject.ParamSpec) => void;
            "notify::sample": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps {
            group: SFZGroup;
            nth_region: number;
            nthRegion: number;
            sample: SFZSample;
        }
    }

    /**
     * @gir-type Class
     */
    class SFZRegion extends GObject.Object implements Ags.Connectable {
        static $gtype: GObject.GType<SFZRegion>;

        // Properties
        /**
         * The group assigned with.
         * @since 3.0.0
         */
        get group(): SFZGroup;
        set group(val: SFZGroup);

        /**
         * The nth region.
         * @since 3.0.0
         * @default -1
         */
        get nth_region(): number;
        set nth_region(val: number);

        /**
         * The nth region.
         * @since 3.0.0
         * @default -1
         */
        get nthRegion(): number;
        set nthRegion(val: number);

        /**
         * The sample assigned with.
         * @since 3.0.0
         */
        get sample(): SFZSample;
        set sample(val: SFZSample);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SFZRegion.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        // Constructors
        constructor(properties?: Partial<SFZRegion.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): SFZRegion;

        // Signals
        /** @signal */
        connect_after<K extends keyof SFZRegion.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZRegion.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SFZRegion.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SFZRegion.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get all control of `sfz_region`.
         * @returns the {@link GLib.List}-struct containing controls as string
         */
        get_control(): string[];

        /**
         * Get the groups of `sfz_region`.
         * @returns the {@link AgsAudio.SFZGroup}
         */
        get_group<T = GObject.Object>(): T;

        /**
         * Get the samples of `sfz_region`.
         * @returns the {@link AgsAudio.SFZSample}
         */
        get_sample<T = GObject.Object>(): T;

        /**
         * Insert control specified by `key` and `value` to `sfz_region`.
         * @param key the key
         * @param value the value
         */
        insert_control(key: string, value: string): void;

        /**
         * Lookup control specified by `key` of `sfz_region`.
         * @param key the key
         * @returns the matching value of key
         */
        lookup_control(key: string): string;

        /**
         * Set the group field of `sfz_region`
         * @param group the {@link AgsAudio.SFZGroup}
         */
        set_group(group: GObject.Object): void;

        /**
         * Set the sample field of `sfz_region`
         * @param sample the {@link AgsAudio.SFZSample}
         */
        set_sample(sample: GObject.Object): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace SFZSample {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::group": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::region": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, SoundResource.ConstructorProps {
            audio_channels: number;
            audioChannels: number;
            buffer_size: number;
            bufferSize: number;
            filename: string;
            format: number;
            group: SFZGroup;
            loop_end: number;
            loopEnd: number;
            loop_start: number;
            loopStart: number;
            region: SFZRegion;
        }
    }

    /**
     * @gir-type Class
     */
    class SFZSample extends GObject.Object implements Ags.Connectable, SoundResource {
        static $gtype: GObject.GType<SFZSample>;

        // Properties
        /**
         * The audio channels to be used.
         * @since 3.0.0
         * @default 0
         */
        get audio_channels(): number;
        set audio_channels(val: number);

        /**
         * The audio channels to be used.
         * @since 3.0.0
         * @default 0
         */
        get audioChannels(): number;
        set audioChannels(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The filename to be used.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The format to be used.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The group assigned with.
         * @since 3.0.0
         */
        get group(): SFZGroup;
        set group(val: SFZGroup);

        /**
         * The loop end to be used.
         * @since 3.0.0
         * @default 0
         */
        get loop_end(): number;
        set loop_end(val: number);

        /**
         * The loop end to be used.
         * @since 3.0.0
         * @default 0
         */
        get loopEnd(): number;
        set loopEnd(val: number);

        /**
         * The loop start to be used.
         * @since 3.0.0
         * @default 0
         */
        get loop_start(): number;
        set loop_start(val: number);

        /**
         * The loop start to be used.
         * @since 3.0.0
         * @default 0
         */
        get loopStart(): number;
        set loopStart(val: number);

        /**
         * The region assigned with.
         * @since 3.0.0
         */
        get region(): SFZRegion;
        set region(val: SFZRegion);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SFZSample.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: number;

        uuid: Ags.UUID;

        audio_channel_written: number;

        offset: number;

        buffer_offset: number;

        full_buffer: any;

        buffer: any;

        pointer: number;

        current: number;

        length: number;

        // Constructors
        constructor(properties?: Partial<SFZSample.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): SFZSample;

        // Signals
        /** @signal */
        connect_after<K extends keyof SFZSample.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZSample.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SFZSample.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SFZSample.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get the groups of `sfz_sample`.
         * @returns the {@link AgsAudio.SFZGroup}
         */
        get_group<T = GObject.Object>(): T;

        /**
         * Get high key of `sfz_sample`.
         * @returns the key
         */
        get_hikey(): number;

        /**
         * Get key of `sfz_sample`.
         * @returns the key
         */
        get_key(): number;

        /**
         * Get low key of `sfz_sample`.
         * @returns the key
         */
        get_lokey(): number;

        /**
         * Get key of `sfz_sample`.
         * @returns the key
         */
        get_loop_end(): number;

        /**
         * Get key of `sfz_sample`.
         * @returns the key
         */
        get_loop_mode(): number;

        /**
         * Get key of `sfz_sample`.
         * @returns the key
         */
        get_loop_start(): number;

        /**
         * Get pitch key-center of `sfz_sample`.
         * @returns the key
         */
        get_pitch_keycenter(): number;

        /**
         * Get the regions of `sfz_sample`.
         * @returns the {@link AgsAudio.SFZRegion}
         */
        get_region<T = GObject.Object>(): T;

        /**
         * Enable a feature of `sfz_sample`.
         * @param flags see {@link AgsAudio.SFZSampleFlags}-enum
         */
        set_flags(flags: number): void;

        /**
         * Set the group field of `sfz_sample`
         * @param group the {@link AgsAudio.SFZGroup}
         */
        set_group(group: GObject.Object): void;

        /**
         * Set the region field of `sfz_sample`
         * @param region the {@link AgsAudio.SFZRegion}
         */
        set_region(region: GObject.Object): void;

        /**
         * Test `flags` to be set on `sfz_sample`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: number): boolean;

        /**
         * Disable a feature of `sfz_sample`.
         * @param flags see {@link AgsAudio.SFZSampleFlags}-enum
         */
        unset_flags(flags: number): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Close `sound_resource`.
         */
        close(): void;

        /**
         * Flush `sound_resource`.
         */
        flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         */
        get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         */
        info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         */
        load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @returns `true` if operation was successful, otherwise `false`.
         */
        open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @returns the count of frames actually read
         */
        read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal_at_once(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read wave from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @param x_offset the x offset
         * @param delay the delay
         * @param attack the attack
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.Wave}
         */
        read_wave(soundcard: GObject.Object, audio_channel: number, x_offset: (bigint | number), delay: number, attack: number): Wave[];

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @returns `true` if operation was successful, otherwise `false`.
         */
        rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         */
        seek(frame_count: (bigint | number), whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         */
        set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         */
        write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;

        /**
         * Close `sound_resource`.
         * @virtual
         */
        vfunc_close(): void;

        /**
         * Flush `sound_resource`.
         * @virtual
         */
        vfunc_flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         * @virtual
         */
        vfunc_get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         * @virtual
         */
        vfunc_info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         * @virtual
         */
        vfunc_load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @virtual
         */
        vfunc_open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @virtual
         */
        vfunc_read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @virtual
         */
        vfunc_rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         * @virtual
         */
        vfunc_seek(frame_count: number, whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         * @virtual
         */
        vfunc_set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         * @virtual
         */
        vfunc_write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;
    }


    namespace SFZSynthGenerator {
        // Signal signatures
        interface SignalSignatures extends Ags.Function.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::base-key": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::pitch-type": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::tuning": (pspec: GObject.ParamSpec) => void;
            "notify::volume": (pspec: GObject.ParamSpec) => void;
            "notify::normalized-function": (pspec: GObject.ParamSpec) => void;
            "notify::pivot-table": (pspec: GObject.ParamSpec) => void;
            "notify::source-function": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Function.ConstructorProps {
            attack: number;
            base_key: number;
            baseKey: number;
            buffer_size: number;
            bufferSize: number;
            delay: number;
            filename: string;
            format: number;
            frame_count: number;
            frameCount: number;
            loop_end: number;
            loopEnd: number;
            loop_start: number;
            loopStart: number;
            pitch_type: string;
            pitchType: string;
            samplerate: number;
            tuning: number;
            volume: number;
        }
    }

    /**
     * @gir-type Class
     */
    class SFZSynthGenerator extends Ags.Function {
        static $gtype: GObject.GType<SFZSynthGenerator>;

        // Properties
        /**
         * The attack to be used.
         * @since 3.4.11
         * @default 0
         */
        get attack(): number;
        set attack(val: number);

        /**
         * The base key to be used.
         * @since 3.4.0
         * @default -48
         */
        get base_key(): number;
        set base_key(val: number);

        /**
         * The base key to be used.
         * @since 3.4.0
         * @default -48
         */
        get baseKey(): number;
        set baseKey(val: number);

        /**
         * The buffer size to be used.
         * @since 3.4.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.4.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The delay to be used.
         * @since 3.4.11
         * @default 0
         */
        get delay(): number;
        set delay(val: number);

        /**
         * The assigned filename.
         * @since 3.4.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * The format to be used.
         * @since 3.4.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The frame count to be used.
         * @since 3.4.0
         * @default 0
         */
        get frame_count(): number;
        set frame_count(val: number);

        /**
         * The frame count to be used.
         * @since 3.4.0
         * @default 0
         */
        get frameCount(): number;
        set frameCount(val: number);

        /**
         * The loop end to be used.
         * @since 3.4.0
         * @default 0
         */
        get loop_end(): number;
        set loop_end(val: number);

        /**
         * The loop end to be used.
         * @since 3.4.0
         * @default 0
         */
        get loopEnd(): number;
        set loopEnd(val: number);

        /**
         * The loop start to be used.
         * @since 3.4.0
         * @default 0
         */
        get loop_start(): number;
        set loop_start(val: number);

        /**
         * The loop start to be used.
         * @since 3.4.0
         * @default 0
         */
        get loopStart(): number;
        set loopStart(val: number);

        /**
         * The pitch type to be used.
         * @since 3.9.0
         * @default ags-fluid-4th-order
         */
        get pitch_type(): string;
        set pitch_type(val: string);

        /**
         * The pitch type to be used.
         * @since 3.9.0
         * @default ags-fluid-4th-order
         */
        get pitchType(): string;
        set pitchType(val: string);

        /**
         * The samplerate to be used.
         * @since 3.4.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The tuning to be used.
         * @since 3.4.0
         * @default 0
         */
        get tuning(): number;
        set tuning(val: number);

        /**
         * The volume to be used.
         * @since 3.4.11
         * @default 1
         */
        get volume(): number;
        set volume(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SFZSynthGenerator.SignalSignatures;

        // Fields
        "function": Ags.Function;

        flags: number;

        timestamp: GObject.Object;

        // Constructors
        constructor(properties?: Partial<SFZSynthGenerator.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): SFZSynthGenerator;

        // Signals
        /** @signal */
        connect<K extends keyof SFZSynthGenerator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZSynthGenerator.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SFZSynthGenerator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SFZSynthGenerator.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SFZSynthGenerator.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SFZSynthGenerator.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Compute instrument for `note`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         * @param note the note to compute
         */
        compute(audio_signal: GObject.Object, note: number): void;

        /**
         * Gets base key.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Gets delay.
         * @returns the delay
         */
        get_delay(): number;

        /**
         * Gets filename.
         * @returns the filename
         */
        get_filename(): string;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Gets frame count.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Gets loop end.
         * @returns the loop end
         */
        get_loop_end(): number;

        /**
         * Gets loop start.
         * @returns the loop start
         */
        get_loop_start(): number;

        /**
         * Gets pitch type.
         * @returns the pitch type
         */
        get_pitch_type(): string;

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * Gets tuning.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Sets base key.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set buffer size.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Sets delay.
         * @param delay the delay
         */
        set_delay(delay: number): void;

        /**
         * Set filename.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set format.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Sets frame count.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Sets loop end.
         * @param loop_end the loop end
         */
        set_loop_end(loop_end: number): void;

        /**
         * Sets loop start.
         * @param loop_start the loop start
         */
        set_loop_start(loop_start: number): void;

        /**
         * Set pitch type.
         * @param pitch_type the pitch type
         */
        set_pitch_type(pitch_type: string): void;

        /**
         * Set samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Sets tuning.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;
    }


    namespace SeekSoundcard {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::offset": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::whence": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            offset: (bigint | number);
            soundcard: GObject.Object;
            whence: number;
        }
    }

    /**
     * @gir-type Class
     */
    class SeekSoundcard extends Ags.Task {
        static $gtype: GObject.GType<SeekSoundcard>;

        // Properties
        /**
         * The offset to seek.
         * @since 3.0.0
         * @default 0
         */
        get offset(): number;
        set offset(val: (bigint | number));

        /**
         * The assigned {@link Ags.Soundcard}
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * Whence either AGS_SEEK_SET, AGS_SEEK_CUR or AGS_SEEK_END
         * @since 3.0.0
         * @default 1
         */
        get whence(): number;
        set whence(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SeekSoundcard.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SeekSoundcard.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](soundcard: GObject.Object, offset: (bigint | number), whence: number): SeekSoundcard;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SeekSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SeekSoundcard.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SeekSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SeekSoundcard.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SeekSoundcard.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SeekSoundcard.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SequencerThread {
        // Signal signatures
        interface SignalSignatures extends Ags.Thread.SignalSignatures {
            /**
             * @signal
             * @run-last
             */
            "interval-timeout": () => void;
            "notify::sequencer": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::max-precision": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Thread.ConstructorProps, Ags.Connectable.ConstructorProps {
            sequencer: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SequencerThread extends Ags.Thread implements Ags.Connectable {
        static $gtype: GObject.GType<SequencerThread>;

        // Properties
        /**
         * The assigned {@link Ags.Sequencer}.
         * @since 3.0.0
         * @write-only
         */
        set sequencer(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SequencerThread.SignalSignatures;

        // Fields
        thread: Ags.Thread;

        time_val: number;

        timestamp_thread: Ags.Thread;

        error: GLib.Error;

        // Constructors
        constructor(properties?: Partial<SequencerThread.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](sequencer: GObject.Object): SequencerThread;

        // Conflicted with Ags.Thread.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof SequencerThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SequencerThread.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SequencerThread.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SequencerThread.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_interval_timeout(): void;

        // Methods
        /**
         * Find `sequencer` as sibling of `sequencer_thread`, if it was found on the returned
         * thread should be called `g_object_unref()`.
         * @param sequencer the {@link Ags.Sequencer} to find
         * @returns the matching {@link AgsAudio.SequencerThread}, if not found `null`.
         */
        find_sequencer(sequencer: GObject.Object): SequencerThread;

        interval_timeout(): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace SetAudioChannels {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio_channels: number;
            audioChannels: number;
            soundcard: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SetAudioChannels extends Ags.Task {
        static $gtype: GObject.GType<SetAudioChannels>;

        // Properties
        /**
         * The count of audio channels to apply to soundcard.
         * @since 3.0.0
         * @default 0
         */
        get audio_channels(): number;
        set audio_channels(val: number);

        /**
         * The count of audio channels to apply to soundcard.
         * @since 3.0.0
         * @default 0
         */
        get audioChannels(): number;
        set audioChannels(val: number);

        /**
         * The assigned {@link Ags.Soundcard} instance.
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SetAudioChannels.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SetAudioChannels.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](soundcard: GObject.Object, audio_channels: number): SetAudioChannels;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SetAudioChannels.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetAudioChannels.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SetAudioChannels.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetAudioChannels.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SetAudioChannels.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SetAudioChannels.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SetBacklog {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::has-backlog": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            has_backlog: boolean;
            hasBacklog: boolean;
            scope: any;
        }
    }

    /**
     * @gir-type Class
     */
    class SetBacklog extends Ags.Task {
        static $gtype: GObject.GType<SetBacklog>;

        // Properties
        /**
         * The backlog to apply to scope.
         * @since 6.2.0
         * @default true
         */
        get has_backlog(): boolean;
        set has_backlog(val: boolean);

        /**
         * The backlog to apply to scope.
         * @since 6.2.0
         * @default true
         */
        get hasBacklog(): boolean;
        set hasBacklog(val: boolean);

        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 6.2.0
         */
        get scope(): any;
        set scope(val: any);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SetBacklog.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SetBacklog.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): SetBacklog;

        // Signals
        /** @signal */
        connect<K extends keyof SetBacklog.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetBacklog.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SetBacklog.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetBacklog.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SetBacklog.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SetBacklog.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Add `scope` to `set_backlog`.
         * @param scope the {@link AgsAudio.AudioSignal}
         */
        add(scope: AudioSignal): void;

        /**
         * Remove `scope` from `set_backlog`.
         * @param scope the {@link AgsAudio.AudioSignal}
         */
        remove(scope: AudioSignal): void;
    }


    namespace SetBufferSize {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            buffer_size: number;
            bufferSize: number;
            scope: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SetBufferSize extends Ags.Task {
        static $gtype: GObject.GType<SetBufferSize>;

        // Properties
        /**
         * The buffer size to apply to scope.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to apply to scope.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SetBufferSize.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SetBufferSize.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, buffer_size: number): SetBufferSize;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SetBufferSize.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetBufferSize.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SetBufferSize.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetBufferSize.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SetBufferSize.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SetBufferSize.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SetDevice {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            device: string;
            scope: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SetDevice extends Ags.Task {
        static $gtype: GObject.GType<SetDevice>;

        // Properties
        /**
         * The device to set.
         * @since 3.0.0
         * @default null
         */
        get device(): string;
        set device(val: string);

        /**
         * The assigned {@link Ags.Soundcard} or {@link Ags.Sequencer} instance.
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SetDevice.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SetDevice.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, device: string): SetDevice;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SetDevice.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetDevice.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SetDevice.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetDevice.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SetDevice.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SetDevice.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SetFormat {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            format: number;
            scope: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SetFormat extends Ags.Task {
        static $gtype: GObject.GType<SetFormat>;

        // Properties
        /**
         * The format to apply to scope.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SetFormat.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SetFormat.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, format: Ags.SoundcardFormat): SetFormat;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SetFormat.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetFormat.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SetFormat.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetFormat.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SetFormat.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SetFormat.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SetMuted {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::muted": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            muted: boolean;
            scope: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SetMuted extends Ags.Task {
        static $gtype: GObject.GType<SetMuted>;

        // Properties
        /**
         * The muted to set to scope.
         * @since 3.0.0
         * @default false
         */
        get muted(): boolean;
        set muted(val: boolean);

        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SetMuted.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SetMuted.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, muted: boolean): SetMuted;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SetMuted.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetMuted.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SetMuted.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetMuted.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SetMuted.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SetMuted.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SetPresets {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            buffer_size: number;
            bufferSize: number;
            format: number;
            samplerate: number;
            scope: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SetPresets extends Ags.Task {
        static $gtype: GObject.GType<SetPresets>;

        // Properties
        /**
         * The buffer size to apply to scope.
         * @since 5.3.1
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to apply to scope.
         * @since 5.3.1
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The format to apply to scope.
         * @since 5.3.1
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * The samplerate to apply to scope.
         * @since 5.3.1
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 5.3.1
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SetPresets.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SetPresets.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, buffer_size: number, format: Ags.SoundcardFormat, samplerate: number): SetPresets;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SetPresets.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetPresets.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SetPresets.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetPresets.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SetPresets.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SetPresets.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SetSamplerate {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            samplerate: number;
            scope: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SetSamplerate extends Ags.Task {
        static $gtype: GObject.GType<SetSamplerate>;

        // Properties
        /**
         * The samplerate to apply to scope.
         * @since 3.0.0
         * @default 0
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The assigned {@link GObject.Object} as scope.
         * @since 3.0.0
         */
        get scope(): GObject.Object;
        set scope(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SetSamplerate.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SetSamplerate.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](scope: GObject.Object, samplerate: number): SetSamplerate;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SetSamplerate.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetSamplerate.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SetSamplerate.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SetSamplerate.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SetSamplerate.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SetSamplerate.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace Sndfile {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio-channels": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::file": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, SoundResource.ConstructorProps {
            audio_channels: number;
            audioChannels: number;
            buffer_size: number;
            bufferSize: number;
            file: any;
            format: number;
        }
    }

    /**
     * @gir-type Class
     */
    class Sndfile extends GObject.Object implements Ags.Connectable, SoundResource {
        static $gtype: GObject.GType<Sndfile>;

        // Properties
        /**
         * The audio channels to be used.
         * @since 3.0.0
         * @default 0
         */
        get audio_channels(): number;
        set audio_channels(val: number);

        /**
         * The audio channels to be used.
         * @since 3.0.0
         * @default 0
         */
        get audioChannels(): number;
        set audioChannels(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 0
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The assigned output `SNDFILE`-struct.
         * @since 3.0.0
         */
        get file(): any;
        set file(val: any);

        /**
         * The format to be used.
         * @since 3.0.0
         * @default 0
         */
        get format(): number;
        set format(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Sndfile.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: SndfileFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        audio_channel_written: number;

        offset: number;

        buffer_offset: number;

        full_buffer: any;

        buffer: any;

        pointer: number;

        current: number;

        length: number;

        // Constructors
        constructor(properties?: Partial<Sndfile.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Sndfile;

        // Signals
        /** @signal */
        connect_after<K extends keyof Sndfile.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Sndfile.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Sndfile.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Sndfile.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Check suffix.
         * @param filename the filename
         */
        static check_suffix(filename: string): boolean;

        // Methods
        /**
         * Enable a feature of `sndfile`.
         * @param flags see {@link AgsAudio.SndfileFlags}-enum
         */
        set_flags(flags: SndfileFlags): void;

        /**
         * Test `flags` to be set on `sndfile`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: SndfileFlags): boolean;

        /**
         * Disable a feature of `sndfile`.
         * @param flags see {@link AgsAudio.SndfileFlags}-enum
         */
        unset_flags(flags: SndfileFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Close `sound_resource`.
         */
        close(): void;

        /**
         * Flush `sound_resource`.
         */
        flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         */
        get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         */
        info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         */
        load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @returns `true` if operation was successful, otherwise `false`.
         */
        open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @returns the count of frames actually read
         */
        read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal_at_once(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read wave from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @param x_offset the x offset
         * @param delay the delay
         * @param attack the attack
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.Wave}
         */
        read_wave(soundcard: GObject.Object, audio_channel: number, x_offset: (bigint | number), delay: number, attack: number): Wave[];

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @returns `true` if operation was successful, otherwise `false`.
         */
        rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         */
        seek(frame_count: (bigint | number), whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         */
        set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         */
        write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;

        /**
         * Close `sound_resource`.
         * @virtual
         */
        vfunc_close(): void;

        /**
         * Flush `sound_resource`.
         * @virtual
         */
        vfunc_flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         * @virtual
         */
        vfunc_get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         * @virtual
         */
        vfunc_info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         * @virtual
         */
        vfunc_load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @virtual
         */
        vfunc_open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @virtual
         */
        vfunc_read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @virtual
         */
        vfunc_rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         * @virtual
         */
        vfunc_seek(frame_count: number, whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         * @virtual
         */
        vfunc_set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         * @virtual
         */
        vfunc_write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;
    }


    namespace SoundcardThread {
        // Signal signatures
        interface SignalSignatures extends Ags.Thread.SignalSignatures {
            "notify::soundcard": (pspec: GObject.ParamSpec) => void;
            "notify::soundcard-capability": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::max-precision": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Thread.ConstructorProps, Ags.Connectable.ConstructorProps {
            soundcard: GObject.Object;
            soundcard_capability: number;
            soundcardCapability: number;
        }
    }

    /**
     * @gir-type Class
     */
    class SoundcardThread extends Ags.Thread implements Ags.Connectable {
        static $gtype: GObject.GType<SoundcardThread>;

        // Properties
        /**
         * The assigned {@link Ags.Soundcard}.
         * @since 3.0.0
         */
        get soundcard(): GObject.Object;
        set soundcard(val: GObject.Object);

        /**
         * The soundcard capability.
         * @since 3.0.0
         * @default 0
         */
        get soundcard_capability(): number;
        set soundcard_capability(val: number);

        /**
         * The soundcard capability.
         * @since 3.0.0
         * @default 0
         */
        get soundcardCapability(): number;
        set soundcardCapability(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SoundcardThread.SignalSignatures;

        // Fields
        thread: Ags.Thread;

        flags: number;

        error: GLib.Error;

        // Constructors
        constructor(properties?: Partial<SoundcardThread.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](soundcard: GObject.Object, soundcard_capability: number): SoundcardThread;

        // Conflicted with Ags.Thread.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect_after<K extends keyof SoundcardThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SoundcardThread.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SoundcardThread.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SoundcardThread.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Find `soundcard` as sibling of `soundcard_thread`, if it was found on the returned
         * thread should be called `g_object_unref()`.
         * @param soundcard the {@link Ags.Soundcard} to find
         * @returns the matching {@link AgsAudio.SoundcardThread}, if not found `null`.
         */
        find_soundcard(soundcard: GObject.Object): SoundcardThread;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;
    }


    namespace StartAudio {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::sound-scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            audio: Audio;
            sound_scope: number;
            soundScope: number;
        }
    }

    /**
     * @gir-type Class
     */
    class StartAudio extends Ags.Task {
        static $gtype: GObject.GType<StartAudio>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The effects sound-scope.
         * @since 3.0.0
         * @default -1
         */
        get sound_scope(): number;
        set sound_scope(val: number);

        /**
         * The effects sound-scope.
         * @since 3.0.0
         * @default -1
         */
        get soundScope(): number;
        set soundScope(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: StartAudio.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<StartAudio.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, sound_scope: number): StartAudio;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof StartAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StartAudio.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof StartAudio.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StartAudio.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof StartAudio.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<StartAudio.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace StartChannel {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::channel": (pspec: GObject.ParamSpec) => void;
            "notify::sound-scope": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            channel: Channel;
            sound_scope: number;
            soundScope: number;
        }
    }

    /**
     * @gir-type Class
     */
    class StartChannel extends Ags.Task {
        static $gtype: GObject.GType<StartChannel>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Channel}
         * @since 3.0.0
         */
        get channel(): Channel;
        set channel(val: Channel);

        /**
         * The effects sound-scope.
         * @since 3.0.0
         * @default -1
         */
        get sound_scope(): number;
        set sound_scope(val: number);

        /**
         * The effects sound-scope.
         * @since 3.0.0
         * @default -1
         */
        get soundScope(): number;
        set soundScope(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: StartChannel.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<StartChannel.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](channel: Channel, sound_scope: number): StartChannel;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof StartChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StartChannel.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof StartChannel.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StartChannel.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof StartChannel.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<StartChannel.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace StartSequencer {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class StartSequencer extends Ags.Task {
        static $gtype: GObject.GType<StartSequencer>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: StartSequencer.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<StartSequencer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): StartSequencer;

        // Signals
        /** @signal */
        connect<K extends keyof StartSequencer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StartSequencer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof StartSequencer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StartSequencer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof StartSequencer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<StartSequencer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace StartSoundcard {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class StartSoundcard extends Ags.Task {
        static $gtype: GObject.GType<StartSoundcard>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: StartSoundcard.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<StartSoundcard.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): StartSoundcard;

        // Signals
        /** @signal */
        connect<K extends keyof StartSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StartSoundcard.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof StartSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StartSoundcard.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof StartSoundcard.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<StartSoundcard.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace StopSequencer {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class StopSequencer extends Ags.Task {
        static $gtype: GObject.GType<StopSequencer>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: StopSequencer.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<StopSequencer.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): StopSequencer;

        // Signals
        /** @signal */
        connect<K extends keyof StopSequencer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StopSequencer.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof StopSequencer.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StopSequencer.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof StopSequencer.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<StopSequencer.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace StopSoundcard {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class StopSoundcard extends Ags.Task {
        static $gtype: GObject.GType<StopSoundcard>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: StopSoundcard.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<StopSoundcard.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): StopSoundcard;

        // Signals
        /** @signal */
        connect<K extends keyof StopSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StopSoundcard.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof StopSoundcard.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StopSoundcard.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof StopSoundcard.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<StopSoundcard.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace StopThread {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {

        }
    }

    /**
     * @gir-type Class
     */
    class StopThread extends Ags.Task {
        static $gtype: GObject.GType<StopThread>;

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: StopThread.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<StopThread.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): StopThread;

        // Signals
        /** @signal */
        connect<K extends keyof StopThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StopThread.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof StopThread.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, StopThread.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof StopThread.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<StopThread.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SwitchBufferFlag {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            device: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class SwitchBufferFlag extends Ags.Task {
        static $gtype: GObject.GType<SwitchBufferFlag>;

        // Properties
        /**
         * The assigned {@link Ags.Soundcard} or {@link Ags.Sequencer}
         * @since 3.0.0
         */
        get device(): GObject.Object;
        set device(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SwitchBufferFlag.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<SwitchBufferFlag.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](device: GObject.Object): SwitchBufferFlag;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof SwitchBufferFlag.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SwitchBufferFlag.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SwitchBufferFlag.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SwitchBufferFlag.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SwitchBufferFlag.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SwitchBufferFlag.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace SynthGenerator {
        // Signal signatures
        interface SignalSignatures extends Ags.Function.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay": (pspec: GObject.ParamSpec) => void;
            "notify::do-fm-synth": (pspec: GObject.ParamSpec) => void;
            "notify::do-lfo": (pspec: GObject.ParamSpec) => void;
            "notify::fm-lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::fm-lfo-frequency": (pspec: GObject.ParamSpec) => void;
            "notify::fm-lfo-oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::fm-tuning": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::frame-count": (pspec: GObject.ParamSpec) => void;
            "notify::frequency": (pspec: GObject.ParamSpec) => void;
            "notify::lfo-depth": (pspec: GObject.ParamSpec) => void;
            "notify::loop-end": (pspec: GObject.ParamSpec) => void;
            "notify::loop-start": (pspec: GObject.ParamSpec) => void;
            "notify::oscillator": (pspec: GObject.ParamSpec) => void;
            "notify::phase": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::timestamp": (pspec: GObject.ParamSpec) => void;
            "notify::tuning": (pspec: GObject.ParamSpec) => void;
            "notify::volume": (pspec: GObject.ParamSpec) => void;
            "notify::normalized-function": (pspec: GObject.ParamSpec) => void;
            "notify::pivot-table": (pspec: GObject.ParamSpec) => void;
            "notify::source-function": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Function.ConstructorProps {
            attack: number;
            buffer_size: number;
            bufferSize: number;
            delay: number;
            do_fm_synth: boolean;
            doFmSynth: boolean;
            do_lfo: boolean;
            doLfo: boolean;
            fm_lfo_depth: number;
            fmLfoDepth: number;
            fm_lfo_frequency: number;
            fmLfoFrequency: number;
            fm_lfo_oscillator: number;
            fmLfoOscillator: number;
            fm_tuning: number;
            fmTuning: number;
            format: number;
            frame_count: number;
            frameCount: number;
            frequency: number;
            lfo_depth: number;
            lfoDepth: number;
            loop_end: number;
            loopEnd: number;
            loop_start: number;
            loopStart: number;
            oscillator: number;
            phase: number;
            samplerate: number;
            timestamp: GObject.Object;
            tuning: number;
            volume: number;
        }
    }

    /**
     * @gir-type Class
     */
    class SynthGenerator extends Ags.Function {
        static $gtype: GObject.GType<SynthGenerator>;

        // Properties
        /**
         * The attack to be used.
         * @since 3.0.0
         * @default 0
         */
        get attack(): number;
        set attack(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size to be used.
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The delay to be used.
         * @since 3.0.0
         * @default 0
         */
        get delay(): number;
        set delay(val: number);

        /**
         * If `true` compute FM synth, otherwise not.
         * @since 3.0.0
         * @default false
         */
        get do_fm_synth(): boolean;
        set do_fm_synth(val: boolean);

        /**
         * If `true` compute FM synth, otherwise not.
         * @since 3.0.0
         * @default false
         */
        get doFmSynth(): boolean;
        set doFmSynth(val: boolean);

        /**
         * If `true` compute LFO amplification, otherwise not.
         * @since 3.0.0
         * @default false
         */
        get do_lfo(): boolean;
        set do_lfo(val: boolean);

        /**
         * If `true` compute LFO amplification, otherwise not.
         * @since 3.0.0
         * @default false
         */
        get doLfo(): boolean;
        set doLfo(val: boolean);

        /**
         * The fm LFO depth to be used.
         * @since 3.0.0
         * @default 1
         */
        get fm_lfo_depth(): number;
        set fm_lfo_depth(val: number);

        /**
         * The fm LFO depth to be used.
         * @since 3.0.0
         * @default 1
         */
        get fmLfoDepth(): number;
        set fmLfoDepth(val: number);

        /**
         * The fm LFO frequency to be used.
         * @since 3.0.0
         * @default 6
         */
        get fm_lfo_frequency(): number;
        set fm_lfo_frequency(val: number);

        /**
         * The fm LFO frequency to be used.
         * @since 3.0.0
         * @default 6
         */
        get fmLfoFrequency(): number;
        set fmLfoFrequency(val: number);

        /**
         * The frame count to be used.
         * @since 3.0.0
         * @default 0
         */
        get fm_lfo_oscillator(): number;
        set fm_lfo_oscillator(val: number);

        /**
         * The frame count to be used.
         * @since 3.0.0
         * @default 0
         */
        get fmLfoOscillator(): number;
        set fmLfoOscillator(val: number);

        /**
         * The fm tuning to be used.
         * @since 3.0.0
         * @default 0
         */
        get fm_tuning(): number;
        set fm_tuning(val: number);

        /**
         * The fm tuning to be used.
         * @since 3.0.0
         * @default 0
         */
        get fmTuning(): number;
        set fmTuning(val: number);

        /**
         * The format to be used.
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The frame count to be used.
         * @since 3.0.0
         * @default 0
         */
        get frame_count(): number;
        set frame_count(val: number);

        /**
         * The frame count to be used.
         * @since 3.0.0
         * @default 0
         */
        get frameCount(): number;
        set frameCount(val: number);

        /**
         * The frequency to be used.
         * @since 3.0.0
         * @default 440
         */
        get frequency(): number;
        set frequency(val: number);

        /**
         * The LFO depth to be used.
         * @since 3.0.0
         * @default 1
         */
        get lfo_depth(): number;
        set lfo_depth(val: number);

        /**
         * The LFO depth to be used.
         * @since 3.0.0
         * @default 1
         */
        get lfoDepth(): number;
        set lfoDepth(val: number);

        /**
         * The loop end to be used.
         * @since 3.0.0
         * @default 0
         */
        get loop_end(): number;
        set loop_end(val: number);

        /**
         * The loop end to be used.
         * @since 3.0.0
         * @default 0
         */
        get loopEnd(): number;
        set loopEnd(val: number);

        /**
         * The loop start to be used.
         * @since 3.0.0
         * @default 0
         */
        get loop_start(): number;
        set loop_start(val: number);

        /**
         * The loop start to be used.
         * @since 3.0.0
         * @default 0
         */
        get loopStart(): number;
        set loopStart(val: number);

        /**
         * The oscillator to be used.
         * @since 3.0.0
         * @default 0
         */
        get oscillator(): number;
        set oscillator(val: number);

        /**
         * The phase to be used.
         * @since 3.0.0
         * @default 0
         */
        get phase(): number;
        set phase(val: number);

        /**
         * The samplerate to be used.
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The assigned timestamp.
         * @since 3.0.0
         */
        get timestamp(): GObject.Object;
        set timestamp(val: GObject.Object);

        /**
         * The tuning to be used.
         * @since 3.0.0
         * @default 0
         */
        get tuning(): number;
        set tuning(val: number);

        /**
         * The volume to be used.
         * @since 3.0.0
         * @default 1
         */
        get volume(): number;
        set volume(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: SynthGenerator.SignalSignatures;

        // Fields
        "function": Ags.Function;

        flags: number;

        sync_relative: boolean;

        sync_point: Ags.Complex;

        sync_point_count: number;

        damping: Ags.Complex;

        vibration: Ags.Complex;

        // Constructors
        constructor(properties?: Partial<SynthGenerator.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): SynthGenerator;

        // Signals
        /** @signal */
        connect<K extends keyof SynthGenerator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SynthGenerator.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof SynthGenerator.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, SynthGenerator.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof SynthGenerator.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<SynthGenerator.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Compute synth for `note`.
         * @param audio_signal the {@link AgsAudio.AudioSignal}
         * @param note the note to compute
         */
        compute(audio_signal: GObject.Object, note: number): void;

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Gets delay.
         * @returns the delay
         */
        get_delay(): number;

        /**
         * Gets do FM synth.
         * @returns the do FM synth
         */
        get_do_fm_synth(): boolean;

        /**
         * Gets do lfo.
         * @returns the do lfo
         */
        get_do_lfo(): boolean;

        /**
         * Gets FM LFO depth.
         * @returns the FM LFO depth
         */
        get_fm_lfo_depth(): number;

        /**
         * Gets FM LFO frequency.
         * @returns the FM LFO frequency
         */
        get_fm_lfo_frequency(): number;

        /**
         * Gets FM LFO oscillator.
         * @returns the FM LFO oscillator
         */
        get_fm_lfo_oscillator(): number;

        /**
         * Gets FM tuning.
         * @returns the FM tuning
         */
        get_fm_tuning(): number;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Gets frame count.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Gets frequency.
         * @returns the frequency
         */
        get_frequency(): number;

        /**
         * Gets LFO depth.
         * @returns the LFO depth
         */
        get_lfo_depth(): number;

        /**
         * Gets loop end.
         * @returns the loop end
         */
        get_loop_end(): number;

        /**
         * Gets loop start.
         * @returns the loop start
         */
        get_loop_start(): number;

        /**
         * Gets oscillator.
         * @returns the oscillator
         */
        get_oscillator(): number;

        /**
         * Gets phase.
         * @returns the phase
         */
        get_phase(): number;

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get sync points of `synth_generator`.
         * @returns the sync point as a {@link Ags.Complex} array
         */
        get_sync_point(): Ags.Complex;

        /**
         * Get sync point count of `synth_generator`.
         */
        get_sync_point_count(): number;

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * Gets tuning.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Gets volume.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Set buffer size.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Sets delay.
         * @param delay the delay
         */
        set_delay(delay: number): void;

        /**
         * Sets do FM synth.
         * @param do_fm_synth the do FM synth
         */
        set_do_fm_synth(do_fm_synth: boolean): void;

        /**
         * Sets do lfo.
         * @param do_lfo the do lfo
         */
        set_do_lfo(do_lfo: boolean): void;

        /**
         * Sets FM LFO depth.
         * @param fm_lfo_depth the FM LFO depth
         */
        set_fm_lfo_depth(fm_lfo_depth: number): void;

        /**
         * Sets FM LFO frequency.
         * @param fm_lfo_frequency the FM LFO frequency
         */
        set_fm_lfo_frequency(fm_lfo_frequency: number): void;

        /**
         * Sets FM LFO oscillator.
         * @param fm_lfo_oscillator the FM LFO oscillator
         */
        set_fm_lfo_oscillator(fm_lfo_oscillator: number): void;

        /**
         * Sets FM tuning.
         * @param fm_tuning the FM tuning
         */
        set_fm_tuning(fm_tuning: number): void;

        /**
         * Set format.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Sets frame count.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Sets frequency.
         * @param frequency the frequency
         */
        set_frequency(frequency: number): void;

        /**
         * Sets LFO depth.
         * @param lfo_depth the LFO depth
         */
        set_lfo_depth(lfo_depth: number): void;

        /**
         * Sets loop end.
         * @param loop_end the loop end
         */
        set_loop_end(loop_end: number): void;

        /**
         * Sets loop start.
         * @param loop_start the loop start
         */
        set_loop_start(loop_start: number): void;

        /**
         * Sets oscillator.
         * @param oscillator the oscillator
         */
        set_oscillator(oscillator: number): void;

        /**
         * Sets phase.
         * @param phase the phase
         */
        set_phase(phase: number): void;

        /**
         * Set samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `value` as sync point at `position` of `synth_generator`.
         * @param position the position to set
         * @param value the {@link Ags.Complex} value to set
         */
        set_sync_point(position: number, value: Ags.Complex): void;

        /**
         * Set sync point count and realloc sync points of `synth_generator`.
         * @param sync_point_count the sync point count
         */
        set_sync_point_count(sync_point_count: number): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Sets tuning.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Sets volume.
         * @param volume the volume
         */
        set_volume(volume: number): void;
    }


    namespace TicDevice {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            device: GObject.Object;
        }
    }

    /**
     * @gir-type Class
     */
    class TicDevice extends Ags.Task {
        static $gtype: GObject.GType<TicDevice>;

        // Properties
        /**
         * The assigned {@link GObject.Object} as device.
         * @since 3.0.0
         */
        get device(): GObject.Object;
        set device(val: GObject.Object);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TicDevice.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<TicDevice.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](device: GObject.Object): TicDevice;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof TicDevice.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TicDevice.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TicDevice.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TicDevice.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TicDevice.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TicDevice.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;
    }


    namespace TogglePatternBit {
        // Signal signatures
        interface SignalSignatures extends Ags.Task.SignalSignatures {
            "notify::bit": (pspec: GObject.ParamSpec) => void;
            "notify::index-i": (pspec: GObject.ParamSpec) => void;
            "notify::index-j": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::pattern": (pspec: GObject.ParamSpec) => void;
            "notify::task-launcher": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends Ags.Task.ConstructorProps {
            bit: number;
            index_i: number;
            indexI: number;
            index_j: number;
            indexJ: number;
            line: number;
            pattern: Pattern;
        }
    }

    /**
     * @gir-type Class
     */
    class TogglePatternBit extends Ags.Task {
        static $gtype: GObject.GType<TogglePatternBit>;

        // Properties
        /**
         * The bit.
         * @since 3.0.0
         * @default 0
         */
        get bit(): number;
        set bit(val: number);

        /**
         * The index-i.
         * @since 3.0.0
         * @default 0
         */
        get index_i(): number;
        set index_i(val: number);

        /**
         * The index-i.
         * @since 3.0.0
         * @default 0
         */
        get indexI(): number;
        set indexI(val: number);

        /**
         * The index-j.
         * @since 3.0.0
         * @default 0
         */
        get index_j(): number;
        set index_j(val: number);

        /**
         * The index-j.
         * @since 3.0.0
         * @default 0
         */
        get indexJ(): number;
        set indexJ(val: number);

        /**
         * The line.
         * @since 3.0.0
         * @default 0
         */
        get line(): number;
        set line(val: number);

        /**
         * The assigned {@link AgsAudio.Pattern}
         * @since 3.0.0
         */
        get pattern(): Pattern;
        set pattern(val: Pattern);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: TogglePatternBit.SignalSignatures;

        // Fields
        task: Ags.Task;

        // Constructors
        constructor(properties?: Partial<TogglePatternBit.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](pattern: Pattern, line: number, index_i: number, index_j: number, bit: number): TogglePatternBit;

        // Conflicted with Ags.Task.new
        static ["new"](...args: never[]): any;

        // Signals
        /** @signal */
        connect<K extends keyof TogglePatternBit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TogglePatternBit.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof TogglePatternBit.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, TogglePatternBit.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof TogglePatternBit.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<TogglePatternBit.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Virtual methods
        /**
         * @virtual
         */
        vfunc_refresh_gui(): void;

        // Methods
        refresh_gui(): void;
    }


    namespace Track {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::smf-buffer": (pspec: GObject.ParamSpec) => void;
            "notify::x": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            smf_buffer: (any | null);
            smfBuffer: (any | null);
            x: (bigint | number);
        }
    }

    /**
     * @gir-type Class
     */
    class Track extends GObject.Object {
        static $gtype: GObject.GType<Track>;

        // Properties
        /**
         * Track's SMF buffer.
         * @since 3.0.0
         */
        get smf_buffer(): (any | null);
        set smf_buffer(val: (any | null));

        /**
         * Track's SMF buffer.
         * @since 3.0.0
         */
        get smfBuffer(): (any | null);
        set smfBuffer(val: (any | null));

        /**
         * Track's x offset.
         * @since 3.0.0
         * @default 0
         */
        get x(): number;
        set x(val: (bigint | number));

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Track.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: TrackFlags;

        allocated_smf_buffer_length: number;

        smf_buffer_length: number;

        // Constructors
        constructor(properties?: Partial<Track.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): Track;

        // Signals
        /** @signal */
        connect<K extends keyof Track.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Track.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Track.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Track.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Track.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Track.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Sort tracks.
         * @param a an {@link AgsAudio.Track}
         * @param b an {@link AgsAudio.Track}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Allocate SMF buffer of `track`.
         * @param smf_buffer_length SMF buffer length
         * @returns the newly allocated SMF buffer
         */
        alloc_smf_buffer(smf_buffer_length: number): (any | null);

        /**
         * Duplicate a track.
         * @returns the duplicated {@link AgsAudio.Track}.
         */
        duplicate(): Track;

        /**
         * @param smf_buffer_length 
         */
        get_smf_buffer(smf_buffer_length: number): (any | null);

        /**
         * Gets x.
         * @returns the x
         */
        get_x(): number;

        /**
         * Lock object mutex.
         */
        lock(): void;

        /**
         * Reallocate SMF buffer of `track`.
         * @param smf_buffer_length SMF buffer length
         * @returns the reallocated SMF buffer
         */
        realloc_smf_buffer(smf_buffer_length: number): (any | null);

        /**
         * Set `flags` on `track`.
         * @param flags the flags
         */
        set_flags(flags: TrackFlags): void;

        /**
         * Sets x.
         * @param x the x
         */
        set_x(x: (bigint | number)): void;

        /**
         * Test `flags` to be set on `track`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: TrackFlags): boolean;

        /**
         * Unlock object mutex.
         */
        unlock(): void;

        /**
         * Unset `flags` on `track`.
         * @param flags the flags
         */
        unset_flags(flags: TrackFlags): void;
    }


    namespace WasapiDevin {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class WasapiDevin extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<WasapiDevin>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The core audio soundcard indentifier
         * @since 3.0.0
         * @default ags-wasapi-devin-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 44100
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: WasapiDevin.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: WasapiDevinFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        wasapi_buffer_size: number;

        app_buffer_mode: WasapiDevinAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        mm_device: any;

        audio_client: any;

        notify_soundcard: GObject.Object;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<WasapiDevin.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): WasapiDevin;

        // Signals
        /** @signal */
        connect_after<K extends keyof WasapiDevin.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, WasapiDevin.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof WasapiDevin.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<WasapiDevin.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `wasapi_devin`.
         * @param flags see {@link AgsAudio.WasapiDevinFlags}-enum
         */
        set_flags(flags: WasapiDevinFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `wasapi_devin`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: WasapiDevinFlags): boolean;

        /**
         * Disable a feature of `wasapi_devin`.
         * @param flags see {@link AgsAudio.WasapiDevinFlags}-enum
         */
        unset_flags(flags: WasapiDevinFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace WasapiDevout {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::attack": (pspec: GObject.ParamSpec) => void;
            "notify::bpm": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::delay-factor": (pspec: GObject.ParamSpec) => void;
            "notify::device": (pspec: GObject.ParamSpec) => void;
            "notify::dsp-channels": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::pcm-channels": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps, Ags.Connectable.ConstructorProps, Ags.Soundcard.ConstructorProps {
            attack: any;
            bpm: number;
            buffer: any;
            buffer_size: number;
            bufferSize: number;
            delay_factor: number;
            delayFactor: number;
            device: string;
            dsp_channels: number;
            dspChannels: number;
            format: number;
            pcm_channels: number;
            pcmChannels: number;
            samplerate: number;
        }
    }

    /**
     * @gir-type Class
     */
    class WasapiDevout extends GObject.Object implements Ags.Connectable, Ags.Soundcard {
        static $gtype: GObject.GType<WasapiDevout>;

        // Properties
        /**
         * Attack of the buffer
         * @since 3.0.0
         * @read-only
         */
        get attack(): any;

        /**
         * Beats per minute
         * @since 3.0.0
         * @default 120
         */
        get bpm(): number;
        set bpm(val: number);

        /**
         * The buffer
         * @since 3.0.0
         * @read-only
         */
        get buffer(): any;

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The buffer size
         * @since 3.0.0
         * @default 940
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delay_factor(): number;
        set delay_factor(val: number);

        /**
         * tact
         * @since 3.0.0
         * @default 1
         */
        get delayFactor(): number;
        set delayFactor(val: number);

        /**
         * The core audio soundcard indentifier
         * @since 3.0.0
         * @default ags-wasapi-devout-0
         */
        get device(): string;
        set device(val: string);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dsp_channels(): number;
        set dsp_channels(val: number);

        /**
         * The dsp channel count
         * @since 3.0.0
         * @default 2
         */
        get dspChannels(): number;
        set dspChannels(val: number);

        /**
         * The precision of the buffer
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcm_channels(): number;
        set pcm_channels(val: number);

        /**
         * The pcm channel count
         * @since 3.0.0
         * @default 2
         */
        get pcmChannels(): number;
        set pcmChannels(val: number);

        /**
         * The samplerate
         * @since 3.0.0
         * @default 44100
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: WasapiDevout.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: WasapiDevoutFlags;

        connectable_flags: Ags.ConnectableFlags;

        uuid: Ags.UUID;

        wasapi_buffer_size: number;

        app_buffer_mode: WasapiDevoutAppBufferMode;

        sub_block_count: number;

        app_buffer: any;

        use_cache: boolean;

        cache_buffer_size: number;

        current_cache: number;

        completed_cache: number;

        cache_offset: number;

        cache: any;

        delay: number;

        tact_counter: number;

        delay_counter: number;

        tic_counter: number;

        start_note_offset: number;

        note_offset: number;

        note_offset_absolute: number;

        loop_left: number;

        loop_right: number;

        do_loop: boolean;

        loop_offset: number;

        mm_device: any;

        audio_client: any;

        note_256th_delay: number;

        note_256th_attack: any[];

        note_256th_offset: number;

        note_256th_offset_last: number;

        note_256th_attack_of_16th_pulse: number;

        note_256th_attack_of_16th_pulse_position: number;

        // Constructors
        constructor(properties?: Partial<WasapiDevout.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](): WasapiDevout;

        // Signals
        /** @signal */
        connect_after<K extends keyof WasapiDevout.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, WasapiDevout.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof WasapiDevout.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<WasapiDevout.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        static error_quark(): GLib.Quark;

        // Methods
        /**
         * Calculate delay and attack and reset it.
         */
        adjust_delay_and_attack(): void;

        /**
         * Reallocate the internal audio buffer.
         */
        realloc_buffer(): void;

        /**
         * Enable a feature of `wasapi_devout`.
         * @param flags see {@link AgsAudio.WasapiDevoutFlags}-enum
         */
        set_flags(flags: WasapiDevoutFlags): void;

        /**
         * The buffer flag indicates the currently played buffer.
         */
        switch_buffer_flag(): void;

        /**
         * Test `flags` to be set on `wasapi_devout`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: WasapiDevoutFlags): boolean;

        /**
         * Disable a feature of `wasapi_devout`.
         * @param flags see {@link AgsAudio.WasapiDevoutFlags}-enum
         */
        unset_flags(flags: WasapiDevoutFlags): void;

        /**
         * Add connectable to registry.
         */
        add_to_registry(): void;

        /**
         * Connect the connectable.
         */
        connect(): void;

        /**
         * @param args 
         */
        connect(...args: never[]): any;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         */
        disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         */
        disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @returns the assigned {@link Ags.UUID}
         */
        get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @returns `true` if `connectable` can be added to registry, otherwise `false`.
         */
        has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @returns `true` if is connected, otherwise `false`.
         */
        is_connected(): boolean;

        /**
         * Connect the connectable.
         * @returns `true` if is added to registry, otherwise `false`.
         */
        is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @returns the #xmlNode-struct
         */
        list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         */
        remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @returns the #xmlNode-struct
         */
        xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         */
        xml_parse(node: libxml2.Node): void;

        /**
         * Add connectable to registry.
         * @virtual
         */
        vfunc_add_to_registry(): void;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_connect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_connect_connection(connection: GObject.Object): void;

        /**
         * Disconnect the connectable.
         * @virtual
         */
        vfunc_disconnect(): void;

        /**
         * Disconnect the connectable.
         * @param connection the connection
         * @virtual
         */
        vfunc_disconnect_connection(connection: GObject.Object): void;

        /**
         * Get UUID of `connectable`.
         * @virtual
         */
        vfunc_get_uuid(): Ags.UUID;

        /**
         * Check the connectable to have resources.
         * @virtual
         */
        vfunc_has_resource(): boolean;

        /**
         * Check if the `connectable` was connected.
         * @virtual
         */
        vfunc_is_connected(): boolean;

        /**
         * Connect the connectable.
         * @virtual
         */
        vfunc_is_ready(): boolean;

        /**
         * List resources as an XML element and return it.
         * @virtual
         */
        vfunc_list_resource(): libxml2.Node;

        /**
         * Remove connectable from registry.
         * @virtual
         */
        vfunc_remove_from_registry(): void;

        /**
         * Compose an XML element and return it.
         * @virtual
         */
        vfunc_xml_compose(): libxml2.Node;

        /**
         * Parse `node` as XML element and apply it.
         * @param node the #xmlNode-struct
         * @virtual
         */
        vfunc_xml_parse(node: libxml2.Node): void;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @returns attack
         */
        get_attack(): number;

        /**
         * Get current playback bpm.
         * @returns bpm
         */
        get_bpm(): number;

        /**
         * Get current playback buffer.
         * @returns current playback buffer
         */
        get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @returns the capablities flags
         */
        get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @returns delay
         */
        get_delay(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @returns delay factor
         */
        get_delay_factor(): number;

        /**
         * Get device.
         * @returns the device's identifier
         */
        get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         */
        get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @returns offset
         */
        get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @returns next playback buffer
         */
        get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         */
        get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         */
        get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @returns the note 256th attack of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @returns the note 256th attack position of 16th pulse
         */
        get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         */
        get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         */
        get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @returns the current note offset
         */
        get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @returns offset
         */
        get_note_offset_absolute(): number;

        /**
         * Get presets.
         */
        get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @returns prev playback buffer
         */
        get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @returns the start note offset
         */
        get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @returns the sub block count
         */
        get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @returns playback time as string
         */
        get_uptime(): string;

        /**
         * Get available.
         * @returns `true` if available, else `false`
         */
        is_available(): boolean;

        /**
         * Get playing.
         * @returns `true` if playing, else `false`
         */
        is_playing(): boolean;

        /**
         * Get recording.
         * @returns `true` if recording, else `false`
         */
        is_recording(): boolean;

        /**
         * Get starting.
         * @returns `true` if starting, else `false`
         */
        is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         */
        list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         */
        lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         */
        offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         */
        pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         */
        play(): void;

        /**
         * Initializes the soundcard for playback.
         */
        play_init(): void;

        /**
         * Records the current buffer of soundcard.
         */
        record(): void;

        /**
         * Initializes the soundcard for recordback.
         */
        record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         */
        set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         */
        set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         */
        set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         */
        set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         */
        set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         */
        set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         */
        set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         */
        stop(): void;

        /**
         * Every call to play may generate a tic.
         */
        tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @returns `true` on success, otherwise `false`
         */
        trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         */
        unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         */
        unlock_sub_block(buffer: (any | null), sub_block: number): void;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_absolute_delay(): number;

        /**
         * Get current playback attack.
         * @virtual
         */
        vfunc_get_attack(): number;

        /**
         * Get current playback bpm.
         * @virtual
         */
        vfunc_get_bpm(): number;

        /**
         * Get current playback buffer.
         * @virtual
         */
        vfunc_get_buffer(): (any | null);

        /**
         * Retrieve {@link Ags.SoundcardCapability}-enum information.
         * @virtual
         */
        vfunc_get_capability(): Ags.SoundcardCapability;

        /**
         * Get current playback delay.
         * @virtual
         */
        vfunc_get_delay(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_delay_counter(): number;

        /**
         * Get current playback delay factor.
         * @virtual
         */
        vfunc_get_delay_factor(): number;

        /**
         * Get device.
         * @virtual
         */
        vfunc_get_device(): string;

        /**
         * Get loop parameters of `soundcard`.
         * @virtual
         */
        vfunc_get_loop(): [number, number, boolean];

        /**
         * Get current playback loop offset.
         * @virtual
         */
        vfunc_get_loop_offset(): number;

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_next_buffer(): (any | null);

        /**
         * Get current playback note 256th attack.
         * @param note_256th_attack_lower the return location of attack lower range
         * @param note_256th_attack_upper the return location of attack upper range
         * @virtual
         */
        vfunc_get_note_256th_attack(note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position the note 256th attack position
         * @virtual
         */
        vfunc_get_note_256th_attack_at_position(note_256th_attack_position: number): number;

        /**
         * Get note 256th attack of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse(): number;

        /**
         * Get note 256th attack position of current 16th pulse.
         * @virtual
         */
        vfunc_get_note_256th_attack_of_16th_pulse_position(): number;

        /**
         * Get current playback note 256th attack position within 16 times `AGS_SOUNDCARD_DEFAULT_PERIOD`.
         * @param note_256th_attack_position_lower the return location of attack position lower range
         * @param note_256th_attack_position_upper the return location of attack position upper range
         * @virtual
         */
        vfunc_get_note_256th_attack_position(note_256th_attack_position_lower: number, note_256th_attack_position_upper: number): void;

        /**
         * Get current playback note 256th offset.
         * @param note_256th_offset_lower the return location of offset lower range
         * @param note_256th_offset_upper the return location of offset upper range
         * @virtual
         */
        vfunc_get_note_256th_offset(note_256th_offset_lower: number, note_256th_offset_upper: number): void;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset(): number;

        /**
         * Get current playback note offset.
         * @virtual
         */
        vfunc_get_note_offset_absolute(): number;

        /**
         * Get presets.
         * @virtual
         */
        vfunc_get_presets(): [number, number, number, Ags.SoundcardFormat];

        /**
         * Get future playback buffer.
         * @virtual
         */
        vfunc_get_prev_buffer(): (any | null);

        /**
         * Get start playback note offset.
         * @virtual
         */
        vfunc_get_start_note_offset(): number;

        /**
         * Get sub block count.
         * @virtual
         */
        vfunc_get_sub_block_count(): number;

        /**
         * Get playback time as string.
         * @virtual
         */
        vfunc_get_uptime(): string;

        /**
         * Get available.
         * @virtual
         */
        vfunc_is_available(): boolean;

        /**
         * Get playing.
         * @virtual
         */
        vfunc_is_playing(): boolean;

        /**
         * Get recording.
         * @virtual
         */
        vfunc_is_recording(): boolean;

        /**
         * Get starting.
         * @virtual
         */
        vfunc_is_starting(): boolean;

        /**
         * Retrieve `card_id` and `card_name` as a list of strings.
         * @virtual
         */
        vfunc_list_cards(): [string[], string[]];

        /**
         * Lock `buffer`.
         * @param buffer the buffer to lock
         * @virtual
         */
        vfunc_lock_buffer(buffer: (any | null)): void;

        /**
         * Callback when counter expires minor note offset.
         * @param note_offset the current note offset
         * @virtual
         */
        vfunc_offset_changed(note_offset: number): void;

        /**
         * Retrieve detailed information of `card_id` soundcard.
         * @param card_id the selected soundcard by its string identifier
         * @virtual
         */
        vfunc_pcm_info(card_id: string): [number, number, number, number, number, number];

        /**
         * Plays the current buffer of soundcard.
         * @virtual
         */
        vfunc_play(): void;

        /**
         * Initializes the soundcard for playback.
         * @virtual
         */
        vfunc_play_init(): void;

        /**
         * Records the current buffer of soundcard.
         * @virtual
         */
        vfunc_record(): void;

        /**
         * Initializes the soundcard for recordback.
         * @virtual
         */
        vfunc_record_init(): void;

        /**
         * Set current playback bpm.
         * @param bpm the bpm to set
         * @virtual
         */
        vfunc_set_bpm(bpm: number): void;

        /**
         * Set current playback delay factor.
         * @param delay_factor the delay factor to set
         * @virtual
         */
        vfunc_set_delay_factor(delay_factor: number): void;

        /**
         * Set device.
         * @param card_id the device to set
         * @virtual
         */
        vfunc_set_device(card_id: string): void;

        /**
         * Set loop parameters of `soundcard`.
         * @param loop_left loop position of region
         * @param loop_right loop position of region
         * @param do_loop if `true` do loop, else don't loop
         * @virtual
         */
        vfunc_set_loop(loop_left: number, loop_right: number, do_loop: boolean): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset(note_offset: number): void;

        /**
         * Set current playback note offset.
         * @param note_offset the note offset to set
         * @virtual
         */
        vfunc_set_note_offset_absolute(note_offset: number): void;

        /**
         * Set presets.
         * @param channels the audio channels
         * @param rate the samplerate
         * @param buffer_size the buffer size
         * @param format the format
         * @virtual
         */
        vfunc_set_presets(channels: number, rate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Set start playback note offset.
         * @param start_note_offset the start note offset to set
         * @virtual
         */
        vfunc_set_start_note_offset(start_note_offset: number): void;

        /**
         * Stops the soundcard from playing to it.
         * @virtual
         */
        vfunc_stop(): void;

        /**
         * Every call to play may generate a tic.
         * @virtual
         */
        vfunc_tic(): void;

        /**
         * Trylock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_trylock_sub_block(buffer: (any | null), sub_block: number): boolean;

        /**
         * Unlock `buffer`.
         * @param buffer the buffer to unlock
         * @virtual
         */
        vfunc_unlock_buffer(buffer: (any | null)): void;

        /**
         * Unlock sub block.
         * @param buffer the buffer to lock
         * @param sub_block and its sub block
         * @virtual
         */
        vfunc_unlock_sub_block(buffer: (any | null), sub_block: number): void;
    }


    namespace Wave {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::buffer": (pspec: GObject.ParamSpec) => void;
            "notify::buffer-size": (pspec: GObject.ParamSpec) => void;
            "notify::format": (pspec: GObject.ParamSpec) => void;
            "notify::line": (pspec: GObject.ParamSpec) => void;
            "notify::samplerate": (pspec: GObject.ParamSpec) => void;
            "notify::timestamp": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: Audio;
            buffer: Buffer[];
            buffer_size: number;
            bufferSize: number;
            format: number;
            line: number;
            samplerate: number;
            timestamp: Ags.Timestamp;
        }
    }

    /**
     * @gir-type Class
     */
    class Wave extends GObject.Object {
        static $gtype: GObject.GType<Wave>;

        // Properties
        /**
         * The assigned {@link AgsAudio.Audio}
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The assigned {@link AgsAudio.Buffer}
         * @since 3.0.0
         */
        get buffer(): Buffer[];
        set buffer(val: Buffer[]);

        /**
         * The audio buffer's buffer size.
         * @since 3.0.0
         * @default 1024
         */
        get buffer_size(): number;
        set buffer_size(val: number);

        /**
         * The audio buffer's buffer size.
         * @since 3.0.0
         * @default 1024
         */
        get bufferSize(): number;
        set bufferSize(val: number);

        /**
         * The audio buffer's format.
         * @since 3.0.0
         * @default 16
         */
        get format(): number;
        set format(val: number);

        /**
         * The wave's line.
         * @since 3.0.0
         * @default 0
         */
        get line(): number;
        set line(val: number);

        /**
         * The audio buffer's samplerate.
         * @since 3.0.0
         * @default 48000
         */
        get samplerate(): number;
        set samplerate(val: number);

        /**
         * The wave's timestamp.
         * @since 3.0.0
         */
        get timestamp(): Ags.Timestamp;
        set timestamp(val: Ags.Timestamp);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: Wave.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: WaveFlags;

        duration: number;

        selection: any[];

        // Constructors
        constructor(properties?: Partial<Wave.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: GObject.Object, line: number): Wave;

        // Signals
        /** @signal */
        connect<K extends keyof Wave.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Wave.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof Wave.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, Wave.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof Wave.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<Wave.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Static methods
        /**
         * Add `new_wave` sorted to `wave`
         * @param wave the {@link GLib.List}-struct containing {@link AgsAudio.Wave}
         * @param new_wave the {@link AgsAudio.Wave} to add
         */
        static add(wave: Wave[], new_wave: Wave): Wave[];

        /**
         * Retrieve appropriate wave for timestamp.
         * @param wave the {@link GLib.List}-struct containing {@link AgsAudio.Wave}
         * @param line the matching audio channel
         * @param timestamp the matching {@link Ags.Timestamp}, or `null` to match any timestamp
         */
        static find_near_timestamp(wave: Wave[], line: number, timestamp: Ags.Timestamp): Wave[];

        /**
         * Compare `a` and `b`.
         * @param a the {@link AgsAudio.Wave}
         * @param b another {@link AgsAudio.Wave}
         */
        static sort_func(a: (any | null), b: (any | null)): number;

        // Methods
        /**
         * Select all buffer to selection.
         */
        add_all_to_selection(): void;

        /**
         * Add `buffer` to `wave`.
         * @param buffer the {@link AgsAudio.Buffer} to add
         * @param use_selection_list if `true` add to selection, else to default wave
         */
        add_buffer(buffer: Buffer, use_selection_list: boolean): void;

        /**
         * Add buffer within region to selection.
         * @param x0 x start offset
         * @param x1 x end offset
         * @param replace_current_selection if `true` selection is replaced
         */
        add_region_to_selection(x0: (bigint | number), x1: (bigint | number), replace_current_selection: boolean): void;

        /**
         * Copy selection to clipboard.
         * @returns the selection as XML.
         */
        copy_selection(): libxml2.Node;

        /**
         * Cut selection to clipboard.
         * @returns the selection as xmlNode
         */
        cut_selection(): libxml2.Node;

        /**
         * Find buffers by offset.
         * @param x offset
         * @param use_selection_list if `true` selection is searched
         * @returns the matching buffer as {@link AgsAudio.Buffer}.
         */
        find_point(x: (bigint | number), use_selection_list: boolean): Buffer;

        /**
         * Find buffers by offset and region.
         * @param x0 x start offset
         * @param x1 x end offset
         * @param use_selection_list if `true` selection is searched
         * @returns the matching buffers as {@link GLib.List}.
         */
        find_region(x0: (bigint | number), x1: (bigint | number), use_selection_list: boolean): Buffer[];

        /**
         * Clear selection.
         */
        free_selection(): void;

        /**
         * Get audio.
         * @returns the {@link AgsAudio.Audio}
         */
        get_audio<T = GObject.Object>(): T;

        /**
         * Get buffer.
         * @returns the {@link GLib.List}-struct containig {@link AgsAudio.Buffer}
         */
        get_buffer(): Buffer[];

        /**
         * Gets buffer size.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Gets format.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Gets line.
         * @returns the line
         */
        get_line(): number;

        /**
         * Gets samplerate.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Retrieve selection.
         * @returns the selection.
         */
        get_selection(): Buffer[];

        /**
         * Get timestamp.
         * @returns the {@link Ags.Timestamp}
         */
        get_timestamp(): Ags.Timestamp;

        /**
         * Paste previously copied buffers.
         * @param wave_node the clipboard XML data
         * @param reset_x_offset if `true` `x_offset` used as cursor
         * @param x_offset region start cursor offset
         * @param delay the delay to be used
         * @param attack the attack to be used
         */
        insert_from_clipboard(wave_node: libxml2.Node, reset_x_offset: boolean, x_offset: (bigint | number), delay: number, attack: number): void;

        /**
         * Paste previously copied buffers.
         * @param wave_node the clipboard XML data
         * @param reset_x_offset if `true` `x_offset` used as cursor
         * @param x_offset region start cursor offset
         * @param delay the delay to be used
         * @param attack the attack to be used
         * @param match_line only paste if channel matches
         * @param do_replace if `true` current data is replaced, otherwise additive mixing is performed
         */
        insert_from_clipboard_extended(wave_node: libxml2.Node, reset_x_offset: boolean, x_offset: (bigint | number), delay: number, attack: number, match_line: boolean, do_replace: boolean): void;

        /**
         * Check selection for buffer.
         * @param buffer the {@link AgsAudio.Buffer} to check for
         * @returns `true` if selected otherwise `false`
         */
        is_buffer_selected(buffer: Buffer): boolean;

        /**
         * Removes `buffer` from `wave`.
         * @param buffer the {@link AgsAudio.Buffer} to remove
         * @param use_selection_list if `true` remove from selection, else from default wave
         */
        remove_buffer(buffer: Buffer, use_selection_list: boolean): void;

        /**
         * Remove buffers within region of selection.
         * @param x0 x start offset
         * @param x1 x end offset
         */
        remove_region_from_selection(x0: (bigint | number), x1: (bigint | number)): void;

        /**
         * Set audio.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: GObject.Object): void;

        /**
         * Set buffer by replacing existing.
         * @param buffer the {@link GLib.List}-struct containing {@link AgsAudio.Buffer}
         */
        set_buffer(buffer: Buffer[]): void;

        /**
         * Set buffer size.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Set `flags` on `wave`.
         * @param flags the flags
         */
        set_flags(flags: WaveFlags): void;

        /**
         * Set format.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Sets line.
         * @param line the line
         */
        set_line(line: number): void;

        /**
         * Set samplerate.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set timestamp.
         * @param timestamp the {@link Ags.Timestamp}
         */
        set_timestamp(timestamp: Ags.Timestamp): void;

        /**
         * Test `flags` to be set on `wave`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: WaveFlags): boolean;

        /**
         * Unset `flags` on `wave`.
         * @param flags the flags
         */
        unset_flags(flags: WaveFlags): void;
    }


    namespace WaveLoader {
        // Signal signatures
        interface SignalSignatures extends GObject.Object.SignalSignatures {
            "notify::audio": (pspec: GObject.ParamSpec) => void;
            "notify::audio-file": (pspec: GObject.ParamSpec) => void;
            "notify::filename": (pspec: GObject.ParamSpec) => void;
        }

        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {
            audio: Audio;
            audio_file: AudioFile;
            audioFile: AudioFile;
            filename: string;
        }
    }

    /**
     * @gir-type Class
     */
    class WaveLoader extends GObject.Object {
        static $gtype: GObject.GType<WaveLoader>;

        // Properties
        /**
         * The assigned audio.
         * @since 3.0.0
         */
        get audio(): Audio;
        set audio(val: Audio);

        /**
         * The audio file opened.
         * @since 3.0.0
         */
        get audio_file(): AudioFile;
        set audio_file(val: AudioFile);

        /**
         * The audio file opened.
         * @since 3.0.0
         */
        get audioFile(): AudioFile;
        set audioFile(val: AudioFile);

        /**
         * The filename to open.
         * @since 3.0.0
         * @default null
         */
        get filename(): string;
        set filename(val: string);

        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         * @internal
         */
        $signals: WaveLoader.SignalSignatures;

        // Fields
        gobject: GObject.Object;

        flags: WaveLoaderFlags;

        connectable_flags: Ags.ConnectableFlags;

        // Constructors
        constructor(properties?: Partial<WaveLoader.ConstructorProps>, ...args: any[]);

        _init(...args: any[]): void;

        static ["new"](audio: Audio, filename: string, do_replace: boolean): WaveLoader;

        // Signals
        /** @signal */
        connect<K extends keyof WaveLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, WaveLoader.SignalSignatures[K]>): number;
        connect(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        connect_after<K extends keyof WaveLoader.SignalSignatures>(signal: K, callback: GObject.SignalCallback<this, WaveLoader.SignalSignatures[K]>): number;
        connect_after(signal: string, callback: (...args: any[]) => any): number;

        /** @signal */
        emit<K extends keyof WaveLoader.SignalSignatures>(signal: K, ...args: GObject.GjsParameters<WaveLoader.SignalSignatures[K]> extends [any, ...infer Q] ? Q : never): void;
        emit(signal: string, ...args: any[]): void;

        // Methods
        /**
         * Get audio of `wave_loader`.
         * @returns the assigned {@link AgsAudio.Audio}
         */
        get_audio(): Audio;

        /**
         * Get audio file of `wave_loader`.
         * @returns the assigned {@link AgsAudio.AudioFile}
         */
        get_audio_file(): AudioFile;

        /**
         * Get filename of `wave_loader`.
         * @returns the assigned filename
         */
        get_filename(): string;

        /**
         * Set audio of `wave_loader`.
         * @param audio the {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio): void;

        /**
         * Set audio file of `wave_loader`.
         * @param audio_file the {@link AgsAudio.AudioFile}
         */
        set_audio_file(audio_file: AudioFile): void;

        /**
         * Set filename of `wave_loader`.
         * @param filename the filename
         */
        set_filename(filename: string): void;

        /**
         * Set flags.
         * @param flags the flags
         */
        set_flags(flags: WaveLoaderFlags): void;

        /**
         * Start `wave_loader`.
         */
        start(): void;

        /**
         * Test `flags` to be set on `recall`.
         * @param flags the flags
         * @returns `true` if flags are set, else `false`
         */
        test_flags(flags: WaveLoaderFlags): boolean;

        /**
         * Unset flags.
         * @param flags the flags
         */
        unset_flags(flags: WaveLoaderFlags): void;
    }


    /**
     * @gir-type Alias
     */
    type AccelerationClass = typeof Acceleration;

    /**
     * @gir-type Alias
     */
    type AddAudioClass = typeof AddAudio;

    /**
     * @gir-type Alias
     */
    type AddAudioSignalClass = typeof AddAudioSignal;

    /**
     * @gir-type Alias
     */
    type AddNoteClass = typeof AddNote;

    /**
     * @gir-type Alias
     */
    type AddSoundcardClass = typeof AddSoundcard;

    /**
     * @gir-type Alias
     */
    type AlsaDevinClass = typeof AlsaDevin;

    /**
     * @gir-type Alias
     */
    type AlsaDevoutClass = typeof AlsaDevout;

    /**
     * @gir-type Alias
     */
    type AlsaMidiinClass = typeof AlsaMidiin;

    /**
     * @gir-type Alias
     */
    type ApplyBpmClass = typeof ApplyBpm;

    /**
     * @gir-type Alias
     */
    type ApplyPresetsClass = typeof ApplyPresets;

    /**
     * @gir-type Alias
     */
    type ApplySF2MidiLocaleClass = typeof ApplySF2MidiLocale;

    /**
     * @gir-type Alias
     */
    type ApplySF2SynthClass = typeof ApplySF2Synth;

    /**
     * @gir-type Alias
     */
    type ApplySFZInstrumentClass = typeof ApplySFZInstrument;

    /**
     * @gir-type Alias
     */
    type ApplySFZSynthClass = typeof ApplySFZSynth;

    /**
     * @gir-type Alias
     */
    type ApplySequencerLengthClass = typeof ApplySequencerLength;

    /**
     * @gir-type Alias
     */
    type ApplySoundConfigClass = typeof ApplySoundConfig;

    /**
     * @gir-type Alias
     */
    type ApplySynthClass = typeof ApplySynth;

    /**
     * @gir-type Alias
     */
    type ApplyTactClass = typeof ApplyTact;

    /**
     * @gir-type Alias
     */
    type AudioApplicationContextClass = typeof AudioApplicationContext;

    /**
     * @gir-type Struct
     */
    abstract class AudioBufferUtil {
        static $gtype: GObject.GType<AudioBufferUtil>;

        // Static methods
        /**
         * Clears the audio data.
         * @param buffer the audio data
         * @param channels the number of channels
         * @param count number of frames
         * @param format the format
         */
        static clear_buffer(buffer: (any | null), channels: number, count: number, format: number): void;

        /**
         * Clears a buffer.
         * @param buffer the buffer to clear
         * @param channels number of channels
         * @param count count frames
         */
        static clear_complex(buffer: Ags.Complex, channels: number, count: number): void;

        /**
         * Clears a buffer.
         * @param buffer the buffer to clear
         * @param channels number of channels
         * @param count count frames
         */
        static clear_double(buffer: number, channels: number, count: number): void;

        /**
         * Clears a buffer.
         * @param buffer the buffer to clear
         * @param channels number of channels
         * @param count count frames
         */
        static clear_float(buffer: number, channels: number, count: number): void;

        /**
         * Wrapper function to copy functions. Doing type conversion.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param doffset start frame of destination
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param soffset start frame of source
         * @param count number of frames to copy
         * @param mode specified type conversion as described
         */
        static copy_buffer_to_buffer(destination: (any | null), dchannels: number, doffset: number, source: (any | null), schannels: number, soffset: number, count: number, mode: AudioBufferUtilCopyMode): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_complex_to_complex(destination: Ags.Complex, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_complex_to_double(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_complex_to_float(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_complex_to_s16(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_complex_to_s24(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_complex_to_s32(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_complex_to_s64(destination: (bigint | number), dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_complex_to_s8(destination: number, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_double_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_double_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_double_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_double_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_double_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_double_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_double_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_double_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_float_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_float_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_float_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_float_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_float_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_float_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_float_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_float_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s16_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s16_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s16_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s16_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s16_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s16_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s16_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s16_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s24_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s24_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s24_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s24_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s24_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s24_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s24_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s24_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s32_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s32_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s32_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s32_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s32_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s32_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s32_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s32_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s64_to_complex(destination: Ags.Complex, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s64_to_double(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s64_to_float(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s64_to_s16(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s64_to_s24(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s64_to_s32(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s64_to_s64(destination: (bigint | number), dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s64_to_s8(destination: number, dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s8_to_complex(destination: Ags.Complex, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s8_to_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s8_to_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s8_to_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s8_to_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s8_to_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s8_to_s64(destination: (bigint | number), dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Copy audio data using additive strategy.
         * @param destination destination buffer
         * @param dchannels destination buffer's count of channels
         * @param source source buffer
         * @param schannels source buffer's count of channels
         * @param count number of frames to copy
         */
        static copy_s8_to_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Translate soundcard format to audio buffer util format.
         * @param soundcard_format the soundcard bit mode
         */
        static format_from_soundcard(soundcard_format: Ags.SoundcardFormat): AudioBufferUtilFormat;

        /**
         * Get copy mode for given destination and source format.
         * @param destination_format the destination buffer format
         * @param source_format the source buffer format
         */
        static get_copy_mode(destination_format: AudioBufferUtilFormat, source_format: AudioBufferUtilFormat): AudioBufferUtilCopyMode;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         * @param format the format
         */
        static pong(destination: (any | null), dchannels: number, source: (any | null), schannels: number, count: number, format: number): void;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         */
        static pong_complex(destination: Ags.Complex, dchannels: number, source: Ags.Complex, schannels: number, count: number): void;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         */
        static pong_double(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         */
        static pong_float(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         */
        static pong_s16(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         */
        static pong_s24(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         */
        static pong_s32(destination: number, dchannels: number, source: number, schannels: number, count: number): void;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         */
        static pong_s64(destination: (bigint | number), dchannels: number, source: (bigint | number), schannels: number, count: number): void;

        /**
         * Pingpong copy `source` to `destination`.
         * @param destination the destination
         * @param dchannels the destination channels
         * @param source the source
         * @param schannels the source channels
         * @param count the count of frames
         */
        static pong_s8(destination: number, dchannels: number, source: number, schannels: number, count: number): void;
    }


    /**
     * @gir-type Alias
     */
    type AudioClass = typeof Audio;

    /**
     * @gir-type Alias
     */
    type AudioContainerClass = typeof AudioContainer;

    /**
     * @gir-type Alias
     */
    type AudioContainerManagerClass = typeof AudioContainerManager;

    /**
     * @gir-type Alias
     */
    type AudioFileClass = typeof AudioFile;

    /**
     * @gir-type Alias
     */
    type AudioFileLinkClass = typeof AudioFileLink;

    /**
     * @gir-type Alias
     */
    type AudioFileManagerClass = typeof AudioFileManager;

    /**
     * @gir-type Alias
     */
    type AudioLoopClass = typeof AudioLoop;

    /**
     * @gir-type Alias
     */
    type AudioSignalClass = typeof AudioSignal;

    /**
     * @gir-type Alias
     */
    type AudioThreadClass = typeof AudioThread;

    /**
     * @gir-type Struct
     */
    class AudioThreadScopeData {
        static $gtype: GObject.GType<AudioThreadScopeData>;

        // Fields
        fx_done: boolean;

        fx_wait: number;

        // Methods
        /**
         * Free `scope_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type AudioUnitClientClass = typeof AudioUnitClient;

    /**
     * @gir-type Alias
     */
    type AudioUnitDevinClass = typeof AudioUnitDevin;

    /**
     * @gir-type Alias
     */
    type AudioUnitDevoutClass = typeof AudioUnitDevout;

    /**
     * @gir-type Alias
     */
    type AudioUnitPortClass = typeof AudioUnitPort;

    /**
     * @gir-type Alias
     */
    type AudioUnitServerClass = typeof AudioUnitServer;

    /**
     * @gir-type Alias
     */
    type AutomationClass = typeof Automation;

    /**
     * @gir-type Alias
     */
    type AutomationControlNameKeyManagerClass = typeof AutomationControlNameKeyManager;

    /**
     * @gir-type Alias
     */
    type BasePluginClass = typeof BasePlugin;

    /**
     * @gir-type Alias
     */
    type BufferClass = typeof Buffer;

    /**
     * @gir-type Alias
     */
    type CancelAudioClass = typeof CancelAudio;

    /**
     * @gir-type Alias
     */
    type CancelChannelClass = typeof CancelChannel;

    /**
     * @gir-type Alias
     */
    type ChannelClass = typeof Channel;

    /**
     * @gir-type Alias
     */
    type ChannelThreadClass = typeof ChannelThread;

    /**
     * @gir-type Struct
     */
    abstract class CharBufferUtil {
        static $gtype: GObject.GType<CharBufferUtil>;

        // Static methods
        /**
         * @param destination 
         * @param dchannels 
         * @param doffset 
         * @param source 
         * @param schannels 
         * @param soffset 
         * @param frame_count 
         * @param byte_order 
         * @param word_size 
         * @param mode 
         */
        static copy_buffer_to_buffer(destination: (any | null), dchannels: number, doffset: number, source: (any | null), schannels: number, soffset: number, frame_count: number, byte_order: number, word_size: number, mode: CharBufferUtilCopyMode): void;

        /**
         * Copy char to double buffer.
         * @param destination destination buffer
         * @param dchannels destination channels
         * @param source source buffer
         * @param word_size word size
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_cbuffer_to_double(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy char to float buffer.
         * @param destination destination buffer
         * @param dchannels destination channels
         * @param source source buffer
         * @param word_size word size
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_cbuffer_to_float(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy char to gint16 buffer.
         * @param destination destination buffer
         * @param dchannels destination channels
         * @param source source buffer
         * @param word_size word size
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_cbuffer_to_s16(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy char to gint32 buffer.
         * @param destination destination buffer
         * @param dchannels destination channels
         * @param source source buffer
         * @param word_size word size
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_cbuffer_to_s24(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy char to gint32 buffer.
         * @param destination destination buffer
         * @param dchannels destination channels
         * @param source source buffer
         * @param word_size word size
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_cbuffer_to_s32(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy char to gint64 buffer.
         * @param destination destination buffer
         * @param dchannels destination channels
         * @param source source buffer
         * @param word_size word size
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_cbuffer_to_s64(destination: (bigint | number), dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy char to gint8 buffer.
         * @param destination destination buffer
         * @param dchannels destination channels
         * @param source source buffer
         * @param word_size word size
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_cbuffer_to_s8(destination: number, dchannels: number, source: number, word_size: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy double to char buffer.
         * @param destination destination buffer
         * @param word_size word size
         * @param dchannels destination channels
         * @param source source buffer
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_double_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy float to char buffer.
         * @param destination destination buffer
         * @param word_size word size
         * @param dchannels destination channels
         * @param source source buffer
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_float_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy gint16 to char buffer.
         * @param destination destination buffer
         * @param word_size word size
         * @param dchannels destination channels
         * @param source source buffer
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_s16_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy gint32 to char buffer.
         * @param destination destination buffer
         * @param word_size word size
         * @param dchannels destination channels
         * @param source source buffer
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_s24_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy gint32 to char buffer.
         * @param destination destination buffer
         * @param word_size word size
         * @param dchannels destination channels
         * @param source source buffer
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_s32_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy gint64 to char buffer.
         * @param destination destination buffer
         * @param word_size word size
         * @param dchannels destination channels
         * @param source source buffer
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_s64_to_cbuffer(destination: number, word_size: number, dchannels: number, source: (bigint | number), schannels: number, frame_count: number, byte_order: number): void;

        /**
         * Copy gint8 to char buffer.
         * @param destination destination buffer
         * @param word_size word size
         * @param dchannels destination channels
         * @param source source buffer
         * @param schannels source channels
         * @param frame_count frame count
         * @param byte_order the byte-order
         */
        static copy_s8_to_cbuffer(destination: number, word_size: number, dchannels: number, source: number, schannels: number, frame_count: number, byte_order: number): void;
    }


    /**
     * @gir-type Struct
     */
    class ChorusUtil {
        static $gtype: GObject.GType<ChorusUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        pitch_mix_buffer: any;

        pitch_mix_max_buffer_length: number;

        pitch_mix_buffer_history: any;

        history_buffer_length: number;

        buffer_length: number;

        format: number;

        samplerate: number;

        offset: number;

        base_key: number;

        input_volume: number;

        output_volume: number;

        lfo_oscillator: number;

        lfo_frequency: number;

        depth: number;

        mix: number;

        delay: number;

        pitch_type: GObject.GType;

        pitch_util: any;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.ChorusUtil}-struct.
         */
        static alloc(): ChorusUtil;

        // Methods
        /**
         * Choralize `chorus_util`.
         */
        compute(): void;

        /**
         * Choralize `chorus_util` of complex data.
         */
        compute_complex(): void;

        /**
         * Choralize `chorus_util` of double precision floating point data.
         */
        compute_double(): void;

        /**
         * Choralize `chorus_util` of floating point data.
         */
        compute_float(): void;

        /**
         * Choralize `chorus_util` of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Choralize `chorus_util` of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Choralize `chorus_util` of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Choralize `chorus_util` of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Choralize `chorus_util` of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.ChorusUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get base key of `chorus_util`.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Get buffer length of `chorus_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get delay of `chorus_util`.
         * @returns the delay
         */
        get_delay(): number;

        /**
         * Get depth of `chorus_util`.
         * @returns the depth
         */
        get_depth(): number;

        /**
         * Get destination buffer of `chorus_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `chorus_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `chorus_util`.
         * @returns the format
         */
        get_format(): number;

        /**
         * Get input volume of `chorus_util`.
         * @returns the input volume
         */
        get_input_volume(): number;

        /**
         * Get LFO frequency of `chorus_util`.
         * @returns the LFO frequency
         */
        get_lfo_frequency(): number;

        /**
         * Get LFO oscillator of `chorus_util`.
         * @returns the LFO oscillator of {@link AgsAudio.SynthOscillatorMode}-enum
         */
        get_lfo_oscillator(): number;

        /**
         * Get mix of `chorus_util`.
         * @returns the mix
         */
        get_mix(): number;

        /**
         * Get note 256th mode of `chorus_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `chorus_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `chorus_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get output volume of `chorus_util`.
         * @returns the output volume
         */
        get_output_volume(): number;

        /**
         * Get pitch type of `chorus_util`.
         * @returns the pitch type
         */
        get_pitch_type(): GObject.GType;

        /**
         * Get pitch util of `chorus_util`.
         * @returns the pitch util
         */
        get_pitch_util(): (any | null);

        /**
         * Get samplerate of `chorus_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `chorus_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `chorus_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Set `base_key` of `chorus_util`.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set `buffer_length` of `chorus_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `delay` of `chorus_util`.
         * @param delay the delay
         */
        set_delay(delay: number): void;

        /**
         * Set `depth` of `chorus_util`.
         * @param depth the depth
         */
        set_depth(depth: number): void;

        /**
         * Set `destination` buffer of `chorus_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `chorus_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `chorus_util`.
         * @param format the format
         */
        set_format(format: number): void;

        /**
         * Set `input_volume` of `chorus_util`.
         * @param input_volume the input volume
         */
        set_input_volume(input_volume: number): void;

        /**
         * Set `lfo_frequency` of `chorus_util`.
         * @param lfo_frequency the LFO frequency
         */
        set_lfo_frequency(lfo_frequency: number): void;

        /**
         * Set `lfo_oscillator` of `chorus_util`.
         * @param lfo_oscillator the LFO oscillator of {@link AgsAudio.SynthOscillatorMode}-enum
         */
        set_lfo_oscillator(lfo_oscillator: number): void;

        /**
         * Set `mix` of `chorus_util`.
         * @param mix the mix
         */
        set_mix(mix: number): void;

        /**
         * Set `note_256th_mode` of `chorus_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `chorus_util`.
         * @param offset the offset
         */
        set_offset(offset: (bigint | number)): void;

        /**
         * Set `offset_256th` of `chorus_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `output_volume` of `chorus_util`.
         * @param output_volume the output volume
         */
        set_output_volume(output_volume: number): void;

        /**
         * Set `pitch_type` of `chorus_util`.
         * @param pitch_type the pitch type
         */
        set_pitch_type(pitch_type: GObject.GType): void;

        /**
         * Set `pitch_util` of `chorus_util`.
         * @param pitch_util the pitch util
         */
        set_pitch_util(pitch_util: (any | null)): void;

        /**
         * Set `samplerate` of `chorus_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `chorus_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `chorus_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;
    }


    /**
     * @gir-type Alias
     */
    type ClearAudioSignalClass = typeof ClearAudioSignal;

    /**
     * @gir-type Alias
     */
    type ClearBufferClass = typeof ClearBuffer;

    /**
     * @gir-type Struct
     */
    abstract class CommonPitchUtil {
        static $gtype: GObject.GType<CommonPitchUtil>;

        // Static methods
        /**
         * Get base key of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_base_key(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get buffer length of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_buffer_length(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get destination of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_destination(pitch_util: (any | null), pitch_type: GObject.GType): (any | null);

        /**
         * Get destination stride of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_destination_stride(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get format of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_format(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get frame count of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_frame_count(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get note 256th mode of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_note_256th_mode(pitch_util: (any | null), pitch_type: GObject.GType): boolean;

        /**
         * Get offset of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_offset(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get offset note 256th of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_offset_256th(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get samplerate of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_samplerate(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get source of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_source(pitch_util: (any | null), pitch_type: GObject.GType): (any | null);

        /**
         * Get source stride of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_source_stride(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get tuning of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_tuning(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get vibrato enabled of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_vibrato_enabled(pitch_util: (any | null), pitch_type: GObject.GType): boolean;

        /**
         * Get vibrato_gain of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_vibrato_gain(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get vibrato LFO depth of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_vibrato_lfo_depth(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get vibrato LFO frequency of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_vibrato_lfo_freq(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get vibrato LFO offset of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_vibrato_lfo_offset(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Get vibrato tuning of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static get_vibrato_tuning(pitch_util: (any | null), pitch_type: GObject.GType): number;

        /**
         * Pitch `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         */
        static pitch(pitch_util: (any | null), pitch_type: GObject.GType): void;

        /**
         * Set `base_key` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param base_key the base key
         */
        static set_base_key(pitch_util: (any | null), pitch_type: GObject.GType, base_key: number): void;

        /**
         * Set `buffer_length` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param buffer_length the buffer length
         */
        static set_buffer_length(pitch_util: (any | null), pitch_type: GObject.GType, buffer_length: number): void;

        /**
         * Set `destination` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param destination the destination
         */
        static set_destination(pitch_util: (any | null), pitch_type: GObject.GType, destination: (any | null)): void;

        /**
         * Set `destination_stride` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param destination_stride the destination stride
         */
        static set_destination_stride(pitch_util: (any | null), pitch_type: GObject.GType, destination_stride: number): void;

        /**
         * Set `format` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param format the format
         */
        static set_format(pitch_util: (any | null), pitch_type: GObject.GType, format: number): void;

        /**
         * Set `frame_count` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param frame_count the frame count
         */
        static set_frame_count(pitch_util: (any | null), pitch_type: GObject.GType, frame_count: number): void;

        /**
         * Set `note_256th_mode` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param note_256th_mode `true` if note 256th mode, otherwise `false`
         */
        static set_note_256th_mode(pitch_util: (any | null), pitch_type: GObject.GType, note_256th_mode: boolean): void;

        /**
         * Set `offset` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param offset the offset
         */
        static set_offset(pitch_util: (any | null), pitch_type: GObject.GType, offset: number): void;

        /**
         * Set `offset_256th` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param offset_256th the offset note 256th
         */
        static set_offset_256th(pitch_util: (any | null), pitch_type: GObject.GType, offset_256th: number): void;

        /**
         * Set `samplerate` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param samplerate the samplerate
         */
        static set_samplerate(pitch_util: (any | null), pitch_type: GObject.GType, samplerate: number): void;

        /**
         * Set `source` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param source the source
         */
        static set_source(pitch_util: (any | null), pitch_type: GObject.GType, source: (any | null)): void;

        /**
         * Set `source_stride` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param source_stride the source stride
         */
        static set_source_stride(pitch_util: (any | null), pitch_type: GObject.GType, source_stride: number): void;

        /**
         * Set `tuning` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param tuning the tuning
         */
        static set_tuning(pitch_util: (any | null), pitch_type: GObject.GType, tuning: number): void;

        /**
         * Set `vibrato_enabled` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param vibrato_enabled the vibrato enabled
         */
        static set_vibrato_enabled(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_enabled: boolean): void;

        /**
         * Set `vibrato_gain` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param vibrato_gain the vibrato_gain
         */
        static set_vibrato_gain(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_gain: number): void;

        /**
         * Set `vibrato_lfo_depth` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param vibrato_lfo_depth the vibrato LFO depth
         */
        static set_vibrato_lfo_depth(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_lfo_depth: number): void;

        /**
         * Set `vibrato_lfo_freq` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param vibrato_lfo_freq the vibrato LFO frequency
         */
        static set_vibrato_lfo_freq(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_lfo_freq: number): void;

        /**
         * Set `vibrato_lfo_offset` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param vibrato_lfo_offset the vibrato LFO offset
         */
        static set_vibrato_lfo_offset(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_lfo_offset: number): void;

        /**
         * Set `vibrato_tuning` of `pitch_util`.
         * @param pitch_util the pitch util
         * @param pitch_type the pitch type
         * @param vibrato_tuning the vibrato tuning
         */
        static set_vibrato_tuning(pitch_util: (any | null), pitch_type: GObject.GType, vibrato_tuning: number): void;
    }


    /**
     * @gir-type Alias
     */
    type CoreAudioClientClass = typeof CoreAudioClient;

    /**
     * @gir-type Alias
     */
    type CoreAudioDevinClass = typeof CoreAudioDevin;

    /**
     * @gir-type Alias
     */
    type CoreAudioDevoutClass = typeof CoreAudioDevout;

    /**
     * @gir-type Alias
     */
    type CoreAudioMidiinClass = typeof CoreAudioMidiin;

    /**
     * @gir-type Alias
     */
    type CoreAudioPortClass = typeof CoreAudioPort;

    /**
     * @gir-type Alias
     */
    type CoreAudioServerClass = typeof CoreAudioServer;

    /**
     * @gir-type Alias
     */
    type CropNoteClass = typeof CropNote;

    /**
     * @gir-type Struct
     */
    abstract class DiatonicScale {
        static $gtype: GObject.GType<DiatonicScale>;

        // Static methods
        /**
         * Convert MIDI `key` to note name and store in `note`.
         * @param key MIDI key
         * @param note return location of note name
         */
        static midi_key_to_note(key: (bigint | number), note: string): number;

        /**
         * Convert `note` to MIDI key and store in `key`.
         * @param note the note name
         * @param key return location of MIDI key
         */
        static note_to_midi_key(note: string, key: (bigint | number)): number;
    }


    /**
     * @gir-type Alias
     */
    type DssiManagerClass = typeof DssiManager;

    /**
     * @gir-type Alias
     */
    type DssiPluginClass = typeof DssiPlugin;

    /**
     * @gir-type Struct
     */
    class EnvelopeUtil {
        static $gtype: GObject.GType<EnvelopeUtil>;

        // Fields
        destination: any;

        destination_stride: number;

        source: any;

        source_stride: number;

        buffer_length: number;

        format: number;

        samplerate: number;

        volume: number;

        amount: number;

        wah_wah_enabled: boolean;

        wah_wah_delay: number;

        wah_wah_lfo_depth: number;

        wah_wah_lfo_freq: number;

        wah_wah_tuning: number;

        wah_wah_lfo_frame_count: number;

        wah_wah_lfo_offset: number;

        // Constructors

        constructor(properties?: Partial<{
            destination: any;
            destination_stride: number;
            source: any;
            source_stride: number;
            buffer_length: number;
            format: number;
            samplerate: number;
            volume: number;
            amount: number;
            wah_wah_enabled: boolean;
            wah_wah_delay: number;
            wah_wah_lfo_depth: number;
            wah_wah_lfo_freq: number;
            wah_wah_tuning: number;
            wah_wah_lfo_frame_count: number;
            wah_wah_lfo_offset: number;
        }>);

        // Static methods
        /**
         * Allocate {@link AgsAudio.EnvelopeUtil}-struct
         */
        static alloc(): EnvelopeUtil;

        // Methods
        /**
         * Compute envelope.
         */
        compute(): void;

        /**
         * Compute envelope of complex floating point data.
         */
        compute_complex(): void;

        /**
         * Compute envelope of double floating point data.
         */
        compute_double(): void;

        /**
         * Compute envelope of floating point data.
         */
        compute_float(): void;

        /**
         * Compute envelope of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute envelope of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute envelope of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute envelope of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute envelope of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.EnvelopeUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get amount of `envelope_util`.
         * @returns the amount
         */
        get_amount(): number;

        /**
         * Get buffer length of `envelope_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `envelope_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `envelope_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `envelope_util`.
         * @returns the format
         */
        get_format(): number;

        /**
         * Get source buffer of `envelope_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `envelope_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get volume of `envelope_util`.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Get wah-wah LFO depth of `envelope_util`.
         * @returns the wah-wah LFO depth
         */
        get_wah_wah_lfo_depth(): number;

        /**
         * Get wah-wah LFO freq of `envelope_util`.
         * @returns the wah-wah LFO freq
         */
        get_wah_wah_lfo_freq(): number;

        /**
         * Get wah-wah LFO offset of `envelope_util`.
         * @returns the wah-wah LFO offset
         */
        get_wah_wah_lfo_offset(): number;

        /**
         * Get wah-wah tuning of `envelope_util`.
         * @returns the wah-wah tuning
         */
        get_wah_wah_tuning(): number;

        /**
         * Set `amount` of `envelope_util`.
         * @param amount the amount
         */
        set_amount(amount: number): void;

        /**
         * Set `buffer_length` of `envelope_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `envelope_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `envelope_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `envelope_util`.
         * @param format the format
         */
        set_format(format: number): void;

        /**
         * Set `source` buffer of `envelope_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `envelope_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `volume` of `envelope_util`.
         * @param volume the volume
         */
        set_volume(volume: number): void;

        /**
         * Set `wah_wah_lfo_depth` of `envelope_util`.
         * @param wah_wah_lfo_depth the wah-wah LFO depth
         */
        set_wah_wah_lfo_depth(wah_wah_lfo_depth: number): void;

        /**
         * Set `wah_wah_lfo_freq` of `envelope_util`.
         * @param wah_wah_lfo_freq the wah-wah LFO freq
         */
        set_wah_wah_lfo_freq(wah_wah_lfo_freq: number): void;

        /**
         * Set `wah_wah_lfo_offset` of `envelope_util`.
         * @param wah_wah_lfo_offset the wah-wah LFO offset
         */
        set_wah_wah_lfo_offset(wah_wah_lfo_offset: number): void;

        /**
         * Set `wah_wah_tuning` of `envelope_util`.
         * @param wah_wah_tuning the wah-wah tuning
         */
        set_wah_wah_tuning(wah_wah_tuning: number): void;
    }


    /**
     * @gir-type Alias
     */
    type ExportOutputClass = typeof ExportOutput;

    /**
     * @gir-type Alias
     */
    type ExportThreadClass = typeof ExportThread;

    /**
     * @gir-type Struct
     */
    class FMSynthUtil {
        static $gtype: GObject.GType<FMSynthUtil>;

        // Fields
        source: any;

        source_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        synth_oscillator_mode: number;

        frequency: number;

        phase: number;

        volume: number;

        lfo_oscillator_mode: number;

        lfo_frequency: number;

        lfo_depth: number;

        tuning: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FMSynthUtil}-struct
         */
        static alloc(): FMSynthUtil;

        // Methods
        /**
         * Compute FM impulse synth.
         */
        compute_impulse(): void;

        /**
         * Compute FM impulse synth of complex data.
         */
        compute_impulse_complex(): void;

        /**
         * Compute FM impulse synth of double precision floating point data.
         */
        compute_impulse_double(): void;

        /**
         * Compute FM impulse synth of floating point data.
         */
        compute_impulse_float(): void;

        /**
         * Compute FM impulse synth of signed 16 bit data.
         */
        compute_impulse_s16(): void;

        /**
         * Compute FM impulse synth of signed 24 bit data.
         */
        compute_impulse_s24(): void;

        /**
         * Compute FM impulse synth of signed 32 bit data.
         */
        compute_impulse_s32(): void;

        /**
         * Compute FM impulse synth of signed 64 bit data.
         */
        compute_impulse_s64(): void;

        /**
         * Compute FM impulse synth of signed 8 bit data.
         */
        compute_impulse_s8(): void;

        /**
         * Compute FM sawtooth synth.
         */
        compute_sawtooth(): void;

        /**
         * Compute FM sawtooth synth of complex data.
         */
        compute_sawtooth_complex(): void;

        /**
         * Compute FM sawtooth synth of double precision floating point data.
         */
        compute_sawtooth_double(): void;

        /**
         * Compute FM sawtooth synth of floating point data.
         */
        compute_sawtooth_float(): void;

        /**
         * Compute FM sawtooth synth of signed 16 bit data.
         */
        compute_sawtooth_s16(): void;

        /**
         * Compute FM sawtooth synth of signed 24 bit data.
         */
        compute_sawtooth_s24(): void;

        /**
         * Compute FM sawtooth synth of signed 32 bit data.
         */
        compute_sawtooth_s32(): void;

        /**
         * Compute FM sawtooth synth of signed 64 bit data.
         */
        compute_sawtooth_s64(): void;

        /**
         * Compute FM sawtooth synth of signed 8 bit data.
         */
        compute_sawtooth_s8(): void;

        /**
         * Compute FM sine synth.
         */
        compute_sin(): void;

        /**
         * Compute FM sine synth of complex data.
         */
        compute_sin_complex(): void;

        /**
         * Compute FM sine synth of double precision floating point data.
         */
        compute_sin_double(): void;

        /**
         * Compute FM sine synth of floating point data.
         */
        compute_sin_float(): void;

        /**
         * Compute FM sine synth of signed 16 bit data.
         */
        compute_sin_s16(): void;

        /**
         * Compute FM sine synth of signed 24 bit data.
         */
        compute_sin_s24(): void;

        /**
         * Compute FM sine synth of signed 32 bit data.
         */
        compute_sin_s32(): void;

        /**
         * Compute FM sine synth of signed 64 bit data.
         */
        compute_sin_s64(): void;

        /**
         * Compute FM sine synth of signed 8 bit data.
         */
        compute_sin_s8(): void;

        /**
         * Compute FM square synth.
         */
        compute_square(): void;

        /**
         * Compute FM square synth of complex data.
         */
        compute_square_complex(): void;

        /**
         * Compute FM square synth of double precision floating point data.
         */
        compute_square_double(): void;

        /**
         * Compute FM square synth of floating point data.
         */
        compute_square_float(): void;

        /**
         * Compute FM square synth of signed 16 bit data.
         */
        compute_square_s16(): void;

        /**
         * Compute FM square synth of signed 24 bit data.
         */
        compute_square_s24(): void;

        /**
         * Compute FM square synth of signed 32 bit data.
         */
        compute_square_s32(): void;

        /**
         * Compute FM square synth of signed 64 bit data.
         */
        compute_square_s64(): void;

        /**
         * Compute FM square synth of signed 8 bit data.
         */
        compute_square_s8(): void;

        /**
         * Compute FM triangle synth.
         */
        compute_triangle(): void;

        /**
         * Compute FM triangle synth of complex data.
         */
        compute_triangle_complex(): void;

        /**
         * Compute FM triangle synth of double precision floating point data.
         */
        compute_triangle_double(): void;

        /**
         * Compute FM triangle synth of floating point data.
         */
        compute_triangle_float(): void;

        /**
         * Compute FM triangle synth of signed 16 bit data.
         */
        compute_triangle_s16(): void;

        /**
         * Compute FM triangle synth of signed 24 bit data.
         */
        compute_triangle_s24(): void;

        /**
         * Compute FM triangle synth of signed 32 bit data.
         */
        compute_triangle_s32(): void;

        /**
         * Compute FM triangle synth of signed 64 bit data.
         */
        compute_triangle_s64(): void;

        /**
         * Compute FM triangle synth of signed 8 bit data.
         */
        compute_triangle_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.FMSynthUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `fm_synth_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get audio buffer util format of `fm_synth_util`.
         * @returns the audio buffer util format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `fm_synth_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get frequency of `fm_synth_util`.
         * @returns the frequency
         */
        get_frequency(): number;

        /**
         * Get LFO depth of `fm_synth_util`.
         * @returns the LFO depth
         */
        get_lfo_depth(): number;

        /**
         * Get LFO frequency of `fm_synth_util`.
         * @returns the LFO frequency
         */
        get_lfo_frequency(): number;

        /**
         * Get LFO oscillator mode of `fm_synth_util`.
         * @returns the LFO oscillator mode
         */
        get_lfo_oscillator_mode(): number;

        /**
         * Get note 256th mode of `fm_synth_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `fm_synth_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `fm_synth_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get phase of `fm_synth_util`.
         * @returns the phase
         */
        get_phase(): number;

        /**
         * Get samplerate of `fm_synth_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `fm_synth_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `fm_synth_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get synth oscillator mode of `fm_synth_util`.
         * @returns the synth oscillator mode
         */
        get_synth_oscillator_mode(): number;

        /**
         * Get tuning of `fm_synth_util`.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Get volume of `fm_synth_util`.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Set `buffer_length` of `fm_synth_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `format` of `fm_synth_util`.
         * @param format the audio buffer util format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `fm_synth_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `frequency` of `fm_synth_util`.
         * @param frequency the frequency
         */
        set_frequency(frequency: number): void;

        /**
         * Set `lfo_depth` of `fm_synth_util`.
         * @param lfo_depth the LFO depth
         */
        set_lfo_depth(lfo_depth: number): void;

        /**
         * Set `lfo_frequency` of `fm_synth_util`.
         * @param lfo_frequency the LFO frequency
         */
        set_lfo_frequency(lfo_frequency: number): void;

        /**
         * Set `lfo_oscillator_mode` of `fm_synth_util`.
         * @param lfo_oscillator_mode the LFO oscillator mode
         */
        set_lfo_oscillator_mode(lfo_oscillator_mode: number): void;

        /**
         * Set `note_256th_mode` of `fm_synth_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `fm_synth_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `fm_synth_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `phase` of `fm_synth_util`.
         * @param phase the phase
         */
        set_phase(phase: number): void;

        /**
         * Set `samplerate` of `fm_synth_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `fm_synth_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `fm_synth_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `synth_oscillator_mode` of `fm_synth_util`.
         * @param synth_oscillator_mode the synth oscillator mode
         */
        set_synth_oscillator_mode(synth_oscillator_mode: number): void;

        /**
         * Set `tuning` of `fm_synth_util`.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Set `volume` of `fm_synth_util`.
         * @param volume the volume
         */
        set_volume(volume: number): void;
    }


    /**
     * @gir-type Struct
     */
    class FastPitchUtil {
        static $gtype: GObject.GType<FastPitchUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        mix_buffer: any;

        mix_buffer_max_buffer_length: number;

        im_mix_buffer: any;

        im_mix_buffer_max_buffer_length: number;

        low_mix_buffer: any;

        low_mix_buffer_max_buffer_length: number;

        new_mix_buffer: any;

        new_mix_buffer_max_buffer_length: number;

        buffer_length: number;

        format: number;

        samplerate: number;

        base_key: number;

        tuning: number;

        vibrato_enabled: number;

        vibrato_gain: number;

        vibrato_lfo_depth: number;

        vibrato_lfo_freq: number;

        vibrato_tuning: number;

        vibrato_lfo_frame_count: number;

        vibrato_lfo_offset: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Constructors

        constructor(properties?: Partial<{
            source: any;
            source_stride: number;
            destination: any;
            destination_stride: number;
            mix_buffer: any;
            mix_buffer_max_buffer_length: number;
            im_mix_buffer: any;
            im_mix_buffer_max_buffer_length: number;
            low_mix_buffer: any;
            low_mix_buffer_max_buffer_length: number;
            new_mix_buffer: any;
            new_mix_buffer_max_buffer_length: number;
            buffer_length: number;
            format: number;
            samplerate: number;
            base_key: number;
            tuning: number;
            vibrato_enabled: number;
            vibrato_gain: number;
            vibrato_lfo_depth: number;
            vibrato_lfo_freq: number;
            vibrato_tuning: number;
            vibrato_lfo_frame_count: number;
            vibrato_lfo_offset: number;
            frame_count: number;
            offset: number;
            note_256th_mode: boolean;
            offset_256th: number;
        }>);

        // Static methods
        /**
         * Allocate {@link AgsAudio.FastPitchUtil}-struct.
         */
        static alloc(): FastPitchUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.FastPitchUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get base key of `fast_pitch_util`.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Get buffer length of `fast_pitch_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `fast_pitch_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `fast_pitch_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `fast_pitch_util`.
         * @returns the format
         */
        get_format(): number;

        /**
         * Get frame count of `fast_pitch_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get note 256th mode of `fast_pitch_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `fast_pitch_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `fast_pitch_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get samplerate of `fast_pitch_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `fast_pitch_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `fast_pitch_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get tuning of `fast_pitch_util`.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Get vibrato enabled of `fast_pitch_util`.
         * @returns `true` if the vibrato is enabled, otherwise `false`
         */
        get_vibrato_enabled(): boolean;

        /**
         * Get vibrato gain of `fast_pitch_util`.
         * @returns the vibrato gain
         */
        get_vibrato_gain(): number;

        /**
         * Get vibrato lfo_depth of `fast_pitch_util`.
         * @returns the vibrato lfo_depth
         */
        get_vibrato_lfo_depth(): number;

        /**
         * Get vibrato lfo_freq of `fast_pitch_util`.
         * @returns the vibrato lfo_freq
         */
        get_vibrato_lfo_freq(): number;

        /**
         * Get vibrato lfo_offset of `fast_pitch_util`.
         * @returns the vibrato lfo_offset
         */
        get_vibrato_lfo_offset(): number;

        /**
         * Get vibrato tuning of `fast_pitch_util`.
         * @returns the vibrato tuning
         */
        get_vibrato_tuning(): number;

        /**
         * Pitch `fast_pitch_util`.
         */
        pitch(): void;

        /**
         * Pitch `fast_pitch_util` of complex data.
         */
        pitch_complex(): void;

        /**
         * Pitch `fast_pitch_util` of double precision floating point data.
         */
        pitch_double(): void;

        /**
         * Pitch `fast_pitch_util` of floating point data.
         */
        pitch_float(): void;

        /**
         * Pitch `fast_pitch_util` of signed 16 bit data.
         */
        pitch_s16(): void;

        /**
         * Pitch `fast_pitch_util` of signed 24 bit data.
         */
        pitch_s24(): void;

        /**
         * Pitch `fast_pitch_util` of signed 32 bit data.
         */
        pitch_s32(): void;

        /**
         * Pitch `fast_pitch_util` of signed 64 bit data.
         */
        pitch_s64(): void;

        /**
         * Pitch `fast_pitch_util` of signed 8 bit data.
         */
        pitch_s8(): void;

        /**
         * Set `base_key` of `fast_pitch_util`.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set `buffer_length` of `fast_pitch_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `fast_pitch_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `fast_pitch_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `fast_pitch_util`.
         * @param format the format
         */
        set_format(format: number): void;

        /**
         * Set `frame_count` of `fast_pitch_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `note_256th_mode` of `fast_pitch_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `fast_pitch_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `fast_pitch_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `samplerate` of `fast_pitch_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `fast_pitch_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `fast_pitch_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `tuning` of `fast_pitch_util`.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Set `vibrato_enabled` of `fast_pitch_util`.
         * @param vibrato_enabled the vibrato enabled
         */
        set_vibrato_enabled(vibrato_enabled: boolean): void;

        /**
         * Set `vibrato_gain` of `fast_pitch_util`.
         * @param vibrato_gain the vibrato gain
         */
        set_vibrato_gain(vibrato_gain: number): void;

        /**
         * Set `vibrato_lfo_depth` of `fast_pitch_util`.
         * @param vibrato_lfo_depth the vibrato lfo_depth
         */
        set_vibrato_lfo_depth(vibrato_lfo_depth: number): void;

        /**
         * Set `vibrato_lfo_freq` of `fast_pitch_util`.
         * @param vibrato_lfo_freq the vibrato lfo_freq
         */
        set_vibrato_lfo_freq(vibrato_lfo_freq: number): void;

        /**
         * Set `vibrato_lfo_offset` of `fast_pitch_util`.
         * @param vibrato_lfo_offset the vibrato lfo_offset
         */
        set_vibrato_lfo_offset(vibrato_lfo_offset: number): void;

        /**
         * Set `vibrato_tuning` of `fast_pitch_util`.
         * @param vibrato_tuning the vibrato tuning
         */
        set_vibrato_tuning(vibrato_tuning: number): void;
    }


    /**
     * @gir-type Alias
     */
    type FifooutClass = typeof Fifoout;

    /**
     * @gir-type Struct
     */
    class FluidIIRFilterUtil {
        static $gtype: GObject.GType<FluidIIRFilterUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        filter_type: FluidIIRFilterType;

        flags: number;

        b02: number;

        b1: number;

        a1: number;

        a2: number;

        b02_incr: number;

        b1_incr: number;

        a1_incr: number;

        a2_incr: number;

        filter_coeff_incr_count: number;

        compensate_incr: number;

        hist1: number;

        hist2: number;

        filter_startup: boolean;

        fres: number;

        last_fres: number;

        q_lin: number;

        filter_gain: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FluidIIRFilterUtil}-struct.
         */
        static alloc(): FluidIIRFilterUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.FluidIIRFilterUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `fluid_iir_filter_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `fluid_iir_filter_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `fluid_iir_filter_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get filter gain of `fluid_iir_filter_util`.
         * @returns the filter gain
         */
        get_filter_gain(): number;

        /**
         * Get filter startup of `fluid_iir_filter_util`.
         * @returns `true` if the filter does startup, otherwise `false`
         */
        get_filter_startup(): boolean;

        /**
         * Get filter type of `fluid_iir_filter_util`.
         * @returns the filter type
         */
        get_filter_type(): FluidIIRFilterType;

        /**
         * Get flags of `fluid_iir_filter_util`.
         * @returns the flags
         */
        get_flags(): number;

        /**
         * Get format of `fluid_iir_filter_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get q-lin of `fluid_iir_filter_util`.
         * @returns the q-lin
         */
        get_q_lin(): number;

        /**
         * Get samplerate of `fluid_iir_filter_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `fluid_iir_filter_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `fluid_iir_filter_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Process `fluid_iir_filter_util`.
         */
        process(): void;

        /**
         * Process `fluid_iir_filter_util` of complex data.
         */
        process_complex(): void;

        /**
         * Process `fluid_iir_filter_util` of double precision floating point data.
         */
        process_double(): void;

        /**
         * Process `fluid_iir_filter_util` of floating point data.
         */
        process_float(): void;

        /**
         * Process `fluid_iir_filter_util` of signed 16 bit data.
         */
        process_s16(): void;

        /**
         * Process `fluid_iir_filter_util` of signed 24 bit data.
         */
        process_s24(): void;

        /**
         * Process `fluid_iir_filter_util` of signed 32 bit data.
         */
        process_s32(): void;

        /**
         * Process `fluid_iir_filter_util` of signed 64 bit data.
         */
        process_s64(): void;

        /**
         * Process `fluid_iir_filter_util` of signed 8 bit data.
         */
        process_s8(): void;

        /**
         * Set `buffer_length` of `fluid_iir_filter_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `fluid_iir_filter_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `fluid_iir_filter_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `filter_gain` of `fluid_iir_filter_util`.
         * @param filter_gain the filter gain
         */
        set_filter_gain(filter_gain: number): void;

        /**
         * Set `filter_startup` of `fluid_iir_filter_util`.
         * @param filter_startup the filter startup
         */
        set_filter_startup(filter_startup: boolean): void;

        /**
         * Set `filter_type` of `fluid_iir_filter_util`.
         * @param filter_type the filter type
         */
        set_filter_type(filter_type: FluidIIRFilterType): void;

        /**
         * Set `flags` of `fluid_iir_filter_util`.
         * @param flags the flags
         */
        set_flags(flags: number): void;

        /**
         * Set `format` of `fluid_iir_filter_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `q_lin` of `fluid_iir_filter_util`.
         * @param q_lin the q-lin
         */
        set_q_lin(q_lin: number): void;

        /**
         * Set `samplerate` of `fluid_iir_filter_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `fluid_iir_filter_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `fluid_iir_filter_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;
    }


    /**
     * @gir-type Struct
     */
    class FluidInterpolate4thOrderUtil {
        static $gtype: GObject.GType<FluidInterpolate4thOrderUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        base_key: number;

        tuning: number;

        phase_increment: number;

        vibrato_enabled: boolean;

        vibrato_gain: number;

        vibrato_lfo_depth: number;

        vibrato_lfo_freq: number;

        vibrato_tuning: number;

        vibrato_lfo_frame_count: number;

        vibrato_lfo_offset: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FluidInterpolate4thOrderUtil}-struct.
         */
        static alloc(): FluidInterpolate4thOrderUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.FluidInterpolate4thOrderUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get base key of `fluid_interpolate_4th_order_util`.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Get buffer length of `fluid_interpolate_4th_order_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `fluid_interpolate_4th_order_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `fluid_interpolate_4th_order_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `fluid_interpolate_4th_order_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `fluid_interpolate_4th_order_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get note 256th mode of `fluid_interpolate_4th_order_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `fluid_interpolate_4th_order_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `fluid_interpolate_4th_order_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get phase increment of `fluid_interpolate_4th_order_util`.
         * @returns the phase increment
         */
        get_phase_increment(): number;

        /**
         * Get samplerate of `fluid_interpolate_4th_order_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `fluid_interpolate_4th_order_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `fluid_interpolate_4th_order_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get tuning of `fluid_interpolate_4th_order_util`.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Get vibrato enabled of `fluid_interpolate_4th_order_util`.
         * @returns `true` if the vibrato is enabled, otherwise `false`
         */
        get_vibrato_enabled(): boolean;

        /**
         * Get vibrato gain of `fluid_interpolate_4th_order_util`.
         * @returns the vibrato gain
         */
        get_vibrato_gain(): number;

        /**
         * Get vibrato LFO depth of `fluid_interpolate_4th_order_util`.
         * @returns the vibrato LFO depth
         */
        get_vibrato_lfo_depth(): number;

        /**
         * Get vibrato LFO frequency of `fluid_interpolate_4th_order_util`.
         * @returns the vibrato LFO frequency
         */
        get_vibrato_lfo_freq(): number;

        /**
         * Get vibrato lfo_offset of `fluid_interpolate_4th_order_util`.
         * @returns the vibrato lfo_offset
         */
        get_vibrato_lfo_offset(): number;

        /**
         * Get vibrato tuning of `fluid_interpolate_4th_order_util`.
         * @returns the vibrato tuning
         */
        get_vibrato_tuning(): number;

        /**
         * Pitch `fluid_interpolate_4th_order_util`.
         */
        pitch(): void;

        /**
         * Pitch `fluid_interpolate_4th_order_util` of complex data.
         */
        pitch_complex(): void;

        /**
         * Pitch `fluid_interpolate_4th_order_util` of double precision floating point data.
         */
        pitch_double(): void;

        /**
         * Pitch `fluid_interpolate_4th_order_util` of floating point data.
         */
        pitch_float(): void;

        /**
         * Pitch `fluid_interpolate_4th_order_util` of signed 16 bit data.
         */
        pitch_s16(): void;

        /**
         * Pitch `fluid_interpolate_4th_order_util` of signed 24 bit data.
         */
        pitch_s24(): void;

        /**
         * Pitch `fluid_interpolate_4th_order_util` of signed 32 bit data.
         */
        pitch_s32(): void;

        /**
         * Pitch `fluid_interpolate_4th_order_util` of signed 64 bit data.
         */
        pitch_s64(): void;

        /**
         * Pitch `fluid_interpolate_4th_order_util` of signed 8 bit data.
         */
        pitch_s8(): void;

        /**
         * Set `base_key` of `fluid_interpolate_4th_order_util`.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set `buffer_length` of `fluid_interpolate_4th_order_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `fluid_interpolate_4th_order_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `fluid_interpolate_4th_order_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `fluid_interpolate_4th_order_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `fluid_interpolate_4th_order_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `note_256th_mode` of `fluid_interpolate_4th_order_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `fluid_interpolate_4th_order_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `fluid_interpolate_4th_order_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `phase_increment` of `fluid_interpolate_4th_order_util`.
         * @param phase_increment the phase increment
         */
        set_phase_increment(phase_increment: number): void;

        /**
         * Set `samplerate` of `fluid_interpolate_4th_order_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `fluid_interpolate_4th_order_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `fluid_interpolate_4th_order_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `tuning` of `fluid_interpolate_4th_order_util`.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Set `vibrato_enabled` of `fluid_interpolate_4th_order_util`.
         * @param vibrato_enabled the vibrato enabled
         */
        set_vibrato_enabled(vibrato_enabled: boolean): void;

        /**
         * Set `vibrato_gain` of `fluid_interpolate_4th_order_util`.
         * @param vibrato_gain the vibrato gain
         */
        set_vibrato_gain(vibrato_gain: number): void;

        /**
         * Set `vibrato_lfo_depth` of `fluid_interpolate_4th_order_util`.
         * @param vibrato_lfo_depth the vibrato LFO depth
         */
        set_vibrato_lfo_depth(vibrato_lfo_depth: number): void;

        /**
         * Set `vibrato_lfo_freq` of `fluid_interpolate_4th_order_util`.
         * @param vibrato_lfo_freq the vibrato LFO freq
         */
        set_vibrato_lfo_freq(vibrato_lfo_freq: number): void;

        /**
         * Set `vibrato_lfo_offset` of `fluid_interpolate_4th_order_util`.
         * @param vibrato_lfo_offset the vibrato lfo_offset
         */
        set_vibrato_lfo_offset(vibrato_lfo_offset: number): void;

        /**
         * Set `vibrato_tuning` of `fluid_interpolate_4th_order_util`.
         * @param vibrato_tuning the vibrato tuning
         */
        set_vibrato_tuning(vibrato_tuning: number): void;
    }


    /**
     * @gir-type Struct
     */
    class FluidInterpolate7thOrderUtil {
        static $gtype: GObject.GType<FluidInterpolate7thOrderUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        base_key: number;

        tuning: number;

        phase_increment: number;

        vibrato_enabled: boolean;

        vibrato_gain: number;

        vibrato_lfo_depth: number;

        vibrato_lfo_freq: number;

        vibrato_tuning: number;

        vibrato_lfo_frame_count: number;

        vibrato_lfo_offset: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FluidInterpolate7thOrderUtil}-struct.
         */
        static alloc(): FluidInterpolate7thOrderUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.FluidInterpolate7thOrderUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get base key of `fluid_interpolate_7th_order_util`.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Get buffer length of `fluid_interpolate_7th_order_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `fluid_interpolate_7th_order_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `fluid_interpolate_7th_order_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `fluid_interpolate_7th_order_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `fluid_interpolate_7th_order_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get note 256th mode of `fluid_interpolate_7th_order_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `fluid_interpolate_7th_order_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `fluid_interpolate_7th_order_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get phase increment of `fluid_interpolate_7th_order_util`.
         * @returns the phase increment
         */
        get_phase_increment(): number;

        /**
         * Get samplerate of `fluid_interpolate_7th_order_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `fluid_interpolate_7th_order_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `fluid_interpolate_7th_order_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get tuning of `fluid_interpolate_7th_order_util`.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Get vibrato enabled of `fluid_interpolate_7th_order_util`.
         * @returns `true` if the vibrato is enabled, otherwise `false`
         */
        get_vibrato_enabled(): boolean;

        /**
         * Get vibrato_gain of `fluid_interpolate_7th_order_util`.
         * @returns the vibrato_gain
         */
        get_vibrato_gain(): number;

        /**
         * Get vibrato LFO depth of `fluid_interpolate_7th_order_util`.
         * @returns the vibrato LFO depth
         */
        get_vibrato_lfo_depth(): number;

        /**
         * Get vibrato LFO frequency of `fluid_interpolate_7th_order_util`.
         * @returns the vibrato LFO frequency
         */
        get_vibrato_lfo_freq(): number;

        /**
         * Get vibrato lfo_offset of `fluid_interpolate_7th_order_util`.
         * @returns the vibrato lfo_offset
         */
        get_vibrato_lfo_offset(): number;

        /**
         * Get vibrato tuning of `fluid_interpolate_7th_order_util`.
         * @returns the vibrato tuning
         */
        get_vibrato_tuning(): number;

        /**
         * Pitch `fluid_interpolate_7th_order_util`.
         */
        pitch(): void;

        /**
         * Pitch `fluid_interpolate_7th_order_util` of complex data.
         */
        pitch_complex(): void;

        /**
         * Pitch `fluid_interpolate_7th_order_util` of double precision floating point data.
         */
        pitch_double(): void;

        /**
         * Pitch `fluid_interpolate_7th_order_util` of floating point data.
         */
        pitch_float(): void;

        /**
         * Pitch `fluid_interpolate_7th_order_util` of signed 16 bit data.
         */
        pitch_s16(): void;

        /**
         * Pitch `fluid_interpolate_7th_order_util` of signed 24 bit data.
         */
        pitch_s24(): void;

        /**
         * Pitch `fluid_interpolate_7th_order_util` of signed 32 bit data.
         */
        pitch_s32(): void;

        /**
         * Pitch `fluid_interpolate_7th_order_util` of signed 64 bit data.
         */
        pitch_s64(): void;

        /**
         * Pitch `fluid_interpolate_7th_order_util` of signed 8 bit data.
         */
        pitch_s8(): void;

        /**
         * Set `base_key` of `fluid_interpolate_7th_order_util`.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set `buffer_length` of `fluid_interpolate_7th_order_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `fluid_interpolate_7th_order_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `fluid_interpolate_7th_order_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `fluid_interpolate_7th_order_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `fluid_interpolate_7th_order_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `note_256th_mode` of `fluid_interpolate_7th_order_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `fluid_interpolate_7th_order_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `fluid_interpolate_7th_order_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `phase_increment` of `fluid_interpolate_7th_order_util`.
         * @param phase_increment the phase increment
         */
        set_phase_increment(phase_increment: number): void;

        /**
         * Set `samplerate` of `fluid_interpolate_7th_order_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `fluid_interpolate_7th_order_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `fluid_interpolate_7th_order_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `tuning` of `fluid_interpolate_7th_order_util`.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Set `vibrato_enabled` of `fluid_interpolate_7th_order_util`.
         * @param vibrato_enabled the vibrato enabled
         */
        set_vibrato_enabled(vibrato_enabled: boolean): void;

        /**
         * Set `vibrato_gain` of `fluid_interpolate_7th_order_util`.
         * @param vibrato_gain the vibrato_gain
         */
        set_vibrato_gain(vibrato_gain: number): void;

        /**
         * Set `vibrato_lfo_depth` of `fluid_interpolate_7th_order_util`.
         * @param vibrato_lfo_depth the vibrato LFO depth
         */
        set_vibrato_lfo_depth(vibrato_lfo_depth: number): void;

        /**
         * Set `vibrato_lfo_freq` of `fluid_interpolate_7th_order_util`.
         * @param vibrato_lfo_freq the vibrato LFO frequency
         */
        set_vibrato_lfo_freq(vibrato_lfo_freq: number): void;

        /**
         * Set `vibrato_lfo_offset` of `fluid_interpolate_7th_order_util`.
         * @param vibrato_lfo_offset the vibrato lfo_offset
         */
        set_vibrato_lfo_offset(vibrato_lfo_offset: number): void;

        /**
         * Set `vibrato_tuning` of `fluid_interpolate_7th_order_util`.
         * @param vibrato_tuning the vibrato_tuning
         */
        set_vibrato_tuning(vibrato_tuning: number): void;
    }


    /**
     * @gir-type Struct
     */
    class FluidInterpolateLinearUtil {
        static $gtype: GObject.GType<FluidInterpolateLinearUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        base_key: number;

        tuning: number;

        phase_increment: number;

        vibrato_enabled: boolean;

        vibrato_gain: number;

        vibrato_lfo_depth: number;

        vibrato_lfo_freq: number;

        vibrato_tuning: number;

        vibrato_lfo_frame_count: number;

        vibrato_lfo_offset: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FluidInterpolateLinearUtil}-struct.
         */
        static alloc(): FluidInterpolateLinearUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.FluidInterpolateLinearUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get base key of `fluid_interpolate_linear_util`.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Get buffer length of `fluid_interpolate_linear_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `fluid_interpolate_linear_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `fluid_interpolate_linear_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `fluid_interpolate_linear_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `fluid_interpolate_linear_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get note 256th mode of `fluid_interpolate_linear_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `fluid_interpolate_linear_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `fluid_interpolate_linear_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get phase increment of `fluid_interpolate_linear_util`.
         * @returns the phase increment
         */
        get_phase_increment(): number;

        /**
         * Get samplerate of `fluid_interpolate_linear_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `fluid_interpolate_linear_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `fluid_interpolate_linear_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get tuning of `fluid_interpolate_linear_util`.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Get vibrato enabled of `fluid_interpolate_linear_util`.
         * @returns `true` if the vibrato is enabled, otherwise `false`
         */
        get_vibrato_enabled(): boolean;

        /**
         * Get vibrato_gain of `fluid_interpolate_linear_util`.
         * @returns the vibrato_gain
         */
        get_vibrato_gain(): number;

        /**
         * Get vibrato LFO depth of `fluid_interpolate_linear_util`.
         * @returns the vibrato LFO depth
         */
        get_vibrato_lfo_depth(): number;

        /**
         * Get vibrato LFO frequency of `fluid_interpolate_linear_util`.
         * @returns the vibrato LFO frequency
         */
        get_vibrato_lfo_freq(): number;

        /**
         * Get vibrato LFO offset of `fluid_interpolate_linear_util`.
         * @returns the vibrato LFO offset
         */
        get_vibrato_lfo_offset(): number;

        /**
         * Get vibrato tuning of `fluid_interpolate_linear_util`.
         * @returns the vibrato tuning
         */
        get_vibrato_tuning(): number;

        /**
         * Pitch `fluid_interpolate_linear_util`.
         */
        pitch(): void;

        /**
         * Pitch `fluid_interpolate_linear_util` of complex data.
         */
        pitch_complex(): void;

        /**
         * Pitch `fluid_interpolate_linear_util` of double precision floating point data.
         */
        pitch_double(): void;

        /**
         * Pitch `fluid_interpolate_linear_util` of floating point data.
         */
        pitch_float(): void;

        /**
         * Pitch `fluid_interpolate_linear_util` of signed 16 bit data.
         */
        pitch_s16(): void;

        /**
         * Pitch `fluid_interpolate_linear_util` of signed 24 bit data.
         */
        pitch_s24(): void;

        /**
         * Pitch `fluid_interpolate_linear_util` of signed 32 bit data.
         */
        pitch_s32(): void;

        /**
         * Pitch `fluid_interpolate_linear_util` of signed 64 bit data.
         */
        pitch_s64(): void;

        /**
         * Pitch `fluid_interpolate_linear_util` of signed 8 bit data.
         */
        pitch_s8(): void;

        /**
         * Set `base_key` of `fluid_interpolate_linear_util`.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set `buffer_length` of `fluid_interpolate_linear_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `fluid_interpolate_linear_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `fluid_interpolate_linear_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `fluid_interpolate_linear_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `fluid_interpolate_linear_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `note_256th_mode` of `fluid_interpolate_linear_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `fluid_interpolate_linear_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `fluid_interpolate_linear_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `phase_increment` of `fluid_interpolate_linear_util`.
         * @param phase_increment the phase increment
         */
        set_phase_increment(phase_increment: number): void;

        /**
         * Set `samplerate` of `fluid_interpolate_linear_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `fluid_interpolate_linear_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `fluid_interpolate_linear_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `tuning` of `fluid_interpolate_linear_util`.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Set `vibrato_enabled` of `fluid_interpolate_linear_util`.
         * @param vibrato_enabled the vibrato enabled
         */
        set_vibrato_enabled(vibrato_enabled: boolean): void;

        /**
         * Set `vibrato_gain` of `fluid_interpolate_linear_util`.
         * @param vibrato_gain the vibrato_gain
         */
        set_vibrato_gain(vibrato_gain: number): void;

        /**
         * Set `vibrato_lfo_depth` of `fluid_interpolate_linear_util`.
         * @param vibrato_lfo_depth the vibrato LFO depth
         */
        set_vibrato_lfo_depth(vibrato_lfo_depth: number): void;

        /**
         * Set `vibrato_lfo_freq` of `fluid_interpolate_linear_util`.
         * @param vibrato_lfo_freq the vibrato LFO frequency
         */
        set_vibrato_lfo_freq(vibrato_lfo_freq: number): void;

        /**
         * Set `vibrato_lfo_offset` of `fluid_interpolate_linear_util`.
         * @param vibrato_lfo_offset the vibrato LFO offset
         */
        set_vibrato_lfo_offset(vibrato_lfo_offset: number): void;

        /**
         * Set `vibrato_tuning` of `fluid_interpolate_linear_util`.
         * @param vibrato_tuning the vibrato tuning
         */
        set_vibrato_tuning(vibrato_tuning: number): void;
    }


    /**
     * @gir-type Struct
     */
    class FluidInterpolateNoneUtil {
        static $gtype: GObject.GType<FluidInterpolateNoneUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        base_key: number;

        tuning: number;

        phase_increment: number;

        vibrato_enabled: boolean;

        vibrato_gain: number;

        vibrato_lfo_depth: number;

        vibrato_lfo_freq: number;

        vibrato_tuning: number;

        vibrato_lfo_frame_count: number;

        vibrato_lfo_offset: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FluidInterpolateNoneUtil}-struct.
         */
        static alloc(): FluidInterpolateNoneUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.FluidInterpolateNoneUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get base key of `fluid_interpolate_none_util`.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Get buffer length of `fluid_interpolate_none_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `fluid_interpolate_none_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `fluid_interpolate_none_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `fluid_interpolate_none_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `fluid_interpolate_none_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get note 256th mode of `fluid_interpolate_none_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `fluid_interpolate_none_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `fluid_interpolate_none_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get phase increment of `fluid_interpolate_none_util`.
         * @returns the phase increment
         */
        get_phase_increment(): number;

        /**
         * Get samplerate of `fluid_interpolate_none_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `fluid_interpolate_none_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `fluid_interpolate_none_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get tuning of `fluid_interpolate_none_util`.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Get vibrato enabled of `fluid_interpolate_none_util`.
         * @returns `true` if the vibrato is enabled, otherwise `false`
         */
        get_vibrato_enabled(): boolean;

        /**
         * Get vibrato_gain of `fluid_interpolate_none_util`.
         * @returns the vibrato_gain
         */
        get_vibrato_gain(): number;

        /**
         * Get vibrato LFO depth of `fluid_interpolate_none_util`.
         * @returns the vibrato LFO depth
         */
        get_vibrato_lfo_depth(): number;

        /**
         * Get vibrato LFO frequency of `fluid_interpolate_none_util`.
         * @returns the vibrato LFO frequency
         */
        get_vibrato_lfo_freq(): number;

        /**
         * Get vibrato LFO offset of `fluid_interpolate_none_util`.
         * @returns the vibrato LFO offset
         */
        get_vibrato_lfo_offset(): number;

        /**
         * Get vibrato tuning of `fluid_interpolate_none_util`.
         * @returns the vibrato tuning
         */
        get_vibrato_tuning(): number;

        /**
         * Pitch `fluid_interpolate_none_util`.
         */
        pitch(): void;

        /**
         * Pitch `fluid_interpolate_none_util` of complex data.
         */
        pitch_complex(): void;

        /**
         * Pitch `fluid_interpolate_none_util` of double precision floating point data.
         */
        pitch_double(): void;

        /**
         * Pitch `fluid_interpolate_none_util` of floating point data.
         */
        pitch_float(): void;

        /**
         * Pitch `fluid_interpolate_none_util` of signed 16 bit data.
         */
        pitch_s16(): void;

        /**
         * Pitch `fluid_interpolate_none_util` of signed 24 bit data.
         */
        pitch_s24(): void;

        /**
         * Pitch `fluid_interpolate_none_util` of signed 32 bit data.
         */
        pitch_s32(): void;

        /**
         * Pitch `fluid_interpolate_none_util` of signed 64 bit data.
         */
        pitch_s64(): void;

        /**
         * Pitch `fluid_interpolate_none_util` of signed 8 bit data.
         */
        pitch_s8(): void;

        /**
         * Set `base_key` of `fluid_interpolate_none_util`.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set `buffer_length` of `fluid_interpolate_none_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `fluid_interpolate_none_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `fluid_interpolate_none_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `fluid_interpolate_none_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `fluid_interpolate_none_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `note_256th_mode` of `fluid_interpolate_none_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `fluid_interpolate_none_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `fluid_interpolate_none_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `phase_increment` of `fluid_interpolate_none_util`.
         * @param phase_increment the phase increment
         */
        set_phase_increment(phase_increment: number): void;

        /**
         * Set `samplerate` of `fluid_interpolate_none_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `fluid_interpolate_none_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `fluid_interpolate_none_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `tuning` of `fluid_interpolate_none_util`.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Set `vibrato_enabled` of `fluid_interpolate_none_util`.
         * @param vibrato_enabled the vibrato enabled
         */
        set_vibrato_enabled(vibrato_enabled: boolean): void;

        /**
         * Set `vibrato_gain` of `fluid_interpolate_none_util`.
         * @param vibrato_gain the vibrato_gain
         */
        set_vibrato_gain(vibrato_gain: number): void;

        /**
         * Set `vibrato_lfo_depth` of `fluid_interpolate_none_util`.
         * @param vibrato_lfo_depth the vibrato LFO depth
         */
        set_vibrato_lfo_depth(vibrato_lfo_depth: number): void;

        /**
         * Set `vibrato_lfo_freq` of `fluid_interpolate_none_util`.
         * @param vibrato_lfo_freq the vibrato LFO freq
         */
        set_vibrato_lfo_freq(vibrato_lfo_freq: number): void;

        /**
         * Set `vibrato_lfo_offset` of `fluid_interpolate_none_util`.
         * @param vibrato_lfo_offset the vibrato LFO offset
         */
        set_vibrato_lfo_offset(vibrato_lfo_offset: number): void;

        /**
         * Set `vibrato_tuning` of `fluid_interpolate_none_util`.
         * @param vibrato_tuning the vibrato tuning
         */
        set_vibrato_tuning(vibrato_tuning: number): void;
    }


    /**
     * @gir-type Struct
     */
    abstract class FluidUtil {
        static $gtype: GObject.GType<FluidUtil>;
    }


    /**
     * @gir-type Struct
     */
    class FourierTransformUtil {
        static $gtype: GObject.GType<FourierTransformUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: Ags.Complex;

        destination_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        // Static methods
        /**
         * Compute fourier transform of `buffer`.
         * @param buffer the audio buffer
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static compute_stft_complex(buffer: Ags.Complex, channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute fourier transform of `buffer`.
         * @param buffer the audio buffer
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static compute_stft_double(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute fourier transform of `buffer`.
         * @param buffer the audio buffer
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static compute_stft_float(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute fourier transform of `buffer`.
         * @param buffer the audio buffer
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static compute_stft_s16(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute fourier transform of `buffer`.
         * @param buffer the audio buffer
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static compute_stft_s24(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute fourier transform of `buffer`.
         * @param buffer the audio buffer
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static compute_stft_s32(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute fourier transform of `buffer`.
         * @param buffer the audio buffer
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static compute_stft_s64(buffer: (bigint | number), channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute fourier transform of `buffer`.
         * @param buffer the audio buffer
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static compute_stft_s8(buffer: number, channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute inverse fourier transform of `buffer`.
         * @param buffer the complex data
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static inverse_stft_complex(buffer: Ags.Complex, channels: number, buffer_length: number, retval: Ags.Complex): void;

        /**
         * Compute inverse fourier transform of `buffer`.
         * @param buffer the complex data
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static inverse_stft_double(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

        /**
         * Compute inverse fourier transform of `buffer`.
         * @param buffer the complex data
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static inverse_stft_float(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

        /**
         * Compute inverse fourier transform of `buffer`.
         * @param buffer the complex data
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static inverse_stft_s16(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

        /**
         * Compute inverse fourier transform of `buffer`.
         * @param buffer the complex data
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static inverse_stft_s24(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

        /**
         * Compute inverse fourier transform of `buffer`.
         * @param buffer the complex data
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static inverse_stft_s32(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;

        /**
         * Compute inverse fourier transform of `buffer`.
         * @param buffer the complex data
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static inverse_stft_s64(buffer: Ags.Complex, channels: number, buffer_length: number, retval: (bigint | number)): void;

        /**
         * Compute inverse fourier transform of `buffer`.
         * @param buffer the complex data
         * @param channels number of audio channels
         * @param buffer_length the buffer's length
         * @param retval the return location of result
         */
        static inverse_stft_s8(buffer: Ags.Complex, channels: number, buffer_length: number, retval: number): void;
    }


    /**
     * @gir-type Alias
     */
    type FreeSelectionClass = typeof FreeSelection;

    /**
     * @gir-type Struct
     */
    class FrequencyAliaseUtil {
        static $gtype: GObject.GType<FrequencyAliaseUtil>;

        // Fields
        source: any;

        source_stride: number;

        phase_shifted_source: any;

        phase_shifted_source_stride: number;

        destination: any;

        destination_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FrequencyAliaseUtil}-struct
         */
        static alloc(): FrequencyAliaseUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.FrequencyAliaseUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `frequency_aliase_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `frequency_aliase_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `frequency_aliase_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `frequency_aliase_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get phase shifted source buffer of `frequency_aliase_util`.
         * @returns the phase shifted source buffer
         */
        get_phase_shifted_source(): (any | null);

        get_phase_shifted_source_stride(): number;

        /**
         * Get source buffer of `frequency_aliase_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `frequency_aliase_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Process phase shift of `frequency_aliase_util`.
         */
        process(): void;

        /**
         * Process phase shift of `frequency_aliase_util` of complex floating point data.
         */
        process_complex(): void;

        /**
         * Process phase shift of `frequency_aliase_util` of double precision floating point data.
         */
        process_double(): void;

        /**
         * Process phase shift of `frequency_aliase_util` of single precision floating point data.
         */
        process_float(): void;

        /**
         * Process phase shift of `frequency_aliase_util` of signed 16 bit data.
         */
        process_s16(): void;

        /**
         * Process phase shift of `frequency_aliase_util` of signed 24 bit data.
         */
        process_s24(): void;

        /**
         * Process phase shift of `frequency_aliase_util` of signed 32 bit data.
         */
        process_s32(): void;

        /**
         * Process phase shift of `frequency_aliase_util` of signed 64 bit data.
         */
        process_s64(): void;

        /**
         * Process phase shift of `frequency_aliase_util` of signed 8 bit data.
         */
        process_s8(): void;

        /**
         * Set `buffer_length` of `frequency_aliase_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `frequency_aliase_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `frequency_aliase_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `frequency_aliase_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `phase_shifted_source` buffer of `frequency_aliase_util`.
         * @param phase_shifted_source the phase shifted source buffer
         */
        set_phase_shifted_source(phase_shifted_source: (any | null)): void;

        /**
         * @param phase_shifted_source_stride 
         */
        set_phase_shifted_source_stride(phase_shifted_source_stride: number): void;

        /**
         * Set `source` buffer of `frequency_aliase_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `frequency_aliase_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;
    }


    /**
     * @gir-type Alias
     */
    type FrequencyMapClass = typeof FrequencyMap;

    /**
     * @gir-type Alias
     */
    type FrequencyMapManagerClass = typeof FrequencyMapManager;

    /**
     * @gir-type Alias
     */
    type FxAnalyseAudioClass = typeof FxAnalyseAudio;

    /**
     * @gir-type Alias
     */
    type FxAnalyseAudioProcessorClass = typeof FxAnalyseAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxAnalyseAudioSignalClass = typeof FxAnalyseAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxAnalyseChannelClass = typeof FxAnalyseChannel;

    /**
     * @gir-type Struct
     */
    abstract class FxAnalyseChannelInputData {
        static $gtype: GObject.GType<FxAnalyseChannelInputData>;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FxAnalyseChannelInputData}-struct
         */
        static alloc(): (any | null);

        /**
         * Free `input_data`.
         * @param input_data the {@link AgsAudio.FxAnalyseChannelInputData}-struct
         */
        static free(input_data: (any | null)): void;

        /**
         * Get structure mutex.
         * @param input_data the {@link AgsAudio.FxAnalyseChannelInputData}
         */
        static get_strct_mutex(input_data: (any | null)): (any | null);
    }


    /**
     * @gir-type Alias
     */
    type FxAnalyseChannelProcessorClass = typeof FxAnalyseChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxAnalyseRecyclingClass = typeof FxAnalyseRecycling;

    /**
     * @gir-type Alias
     */
    type FxBufferAudioClass = typeof FxBufferAudio;

    /**
     * @gir-type Alias
     */
    type FxBufferAudioProcessorClass = typeof FxBufferAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxBufferAudioSignalClass = typeof FxBufferAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxBufferChannelClass = typeof FxBufferChannel;

    /**
     * @gir-type Struct
     */
    class FxBufferChannelInputData {
        static $gtype: GObject.GType<FxBufferChannelInputData>;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FxBufferChannelInputData}-struct
         */
        static alloc(): (any | null);

        /**
         * Free `input_data`.
         * @param input_data the {@link AgsAudio.FxBufferChannelInputData}-struct
         */
        static free(input_data: (any | null)): void;

        /**
         * Get structure mutex.
         * @param input_data the {@link AgsAudio.FxBufferChannelInputData}
         */
        static get_strct_mutex(input_data: (any | null)): (any | null);
    }


    /**
     * @gir-type Alias
     */
    type FxBufferChannelProcessorClass = typeof FxBufferChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxBufferRecyclingClass = typeof FxBufferRecycling;

    /**
     * @gir-type Struct
     */
    class FxDssiAudioChannelData {
        static $gtype: GObject.GType<FxDssiAudioChannelData>;

        // Fields
        event_count: number;

        input_data: FxDssiAudioInputData[];

        // Methods
        /**
         * Free `channel_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxDssiAudioClass = typeof FxDssiAudio;

    /**
     * @gir-type Struct
     */
    class FxDssiAudioInputData {
        static $gtype: GObject.GType<FxDssiAudioInputData>;

        // Fields
        key_on: number;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxDssiAudioProcessorClass = typeof FxDssiAudioProcessor;

    /**
     * @gir-type Struct
     */
    class FxDssiAudioScopeData {
        static $gtype: GObject.GType<FxDssiAudioScopeData>;

        // Fields
        audio_channels: number;

        // Methods
        /**
         * Free `scope_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxDssiAudioSignalClass = typeof FxDssiAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxDssiChannelClass = typeof FxDssiChannel;

    /**
     * @gir-type Alias
     */
    type FxDssiChannelProcessorClass = typeof FxDssiChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxDssiRecyclingClass = typeof FxDssiRecycling;

    /**
     * @gir-type Alias
     */
    type FxEnvelopeAudioClass = typeof FxEnvelopeAudio;

    /**
     * @gir-type Alias
     */
    type FxEnvelopeAudioProcessorClass = typeof FxEnvelopeAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxEnvelopeAudioSignalClass = typeof FxEnvelopeAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxEnvelopeChannelClass = typeof FxEnvelopeChannel;

    /**
     * @gir-type Alias
     */
    type FxEnvelopeChannelProcessorClass = typeof FxEnvelopeChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxEnvelopeRecyclingClass = typeof FxEnvelopeRecycling;

    /**
     * @gir-type Alias
     */
    type FxEq10AudioClass = typeof FxEq10Audio;

    /**
     * @gir-type Alias
     */
    type FxEq10AudioProcessorClass = typeof FxEq10AudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxEq10AudioSignalClass = typeof FxEq10AudioSignal;

    /**
     * @gir-type Alias
     */
    type FxEq10ChannelClass = typeof FxEq10Channel;

    /**
     * @gir-type Struct
     */
    class FxEq10ChannelInputData {
        static $gtype: GObject.GType<FxEq10ChannelInputData>;

        // Fields
        cache_28hz: number[];

        cache_56hz: number[];

        cache_112hz: number[];

        cache_224hz: number[];

        cache_448hz: number[];

        cache_896hz: number[];

        cache_1792hz: number[];

        cache_3584hz: number[];

        cache_7168hz: number[];

        cache_14336hz: number[];

        output: number;

        input: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FxEq10ChannelInputData}-struct
         */
        static alloc(): (any | null);

        /**
         * Free `input_data`.
         * @param input_data the {@link AgsAudio.FxEq10ChannelInputData}-struct
         */
        static free(input_data: (any | null)): void;
    }


    /**
     * @gir-type Alias
     */
    type FxEq10ChannelProcessorClass = typeof FxEq10ChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxEq10RecyclingClass = typeof FxEq10Recycling;

    /**
     * @gir-type Struct
     */
    class FxFMSynthAudioChannelData {
        static $gtype: GObject.GType<FxFMSynthAudioChannelData>;

        // Fields
        pitch_type: GObject.GType;

        pitch_util: any;

        input_data: FxFMSynthAudioInputData[];

        // Methods
        /**
         * Free `channel_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxFMSynthAudioClass = typeof FxFMSynthAudio;

    /**
     * @gir-type Struct
     */
    class FxFMSynthAudioInputData {
        static $gtype: GObject.GType<FxFMSynthAudioInputData>;

        // Fields
        key_on: number;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxFMSynthAudioProcessorClass = typeof FxFMSynthAudioProcessor;

    /**
     * @gir-type Struct
     */
    class FxFMSynthAudioScopeData {
        static $gtype: GObject.GType<FxFMSynthAudioScopeData>;

        // Fields
        audio_channels: number;

        // Methods
        /**
         * Free `scope_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxFMSynthAudioSignalClass = typeof FxFMSynthAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxFMSynthChannelClass = typeof FxFMSynthChannel;

    /**
     * @gir-type Alias
     */
    type FxFMSynthChannelProcessorClass = typeof FxFMSynthChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxFMSynthRecyclingClass = typeof FxFMSynthRecycling;

    /**
     * @gir-type Alias
     */
    type FxFactoryClass = typeof FxFactory;

    /**
     * @gir-type Alias
     */
    type FxHighPassAudioClass = typeof FxHighPassAudio;

    /**
     * @gir-type Alias
     */
    type FxHighPassAudioProcessorClass = typeof FxHighPassAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxHighPassAudioSignalClass = typeof FxHighPassAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxHighPassChannelClass = typeof FxHighPassChannel;

    /**
     * @gir-type Alias
     */
    type FxHighPassChannelProcessorClass = typeof FxHighPassChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxHighPassRecyclingClass = typeof FxHighPassRecycling;

    /**
     * @gir-type Alias
     */
    type FxLadspaAudioClass = typeof FxLadspaAudio;

    /**
     * @gir-type Alias
     */
    type FxLadspaAudioProcessorClass = typeof FxLadspaAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxLadspaAudioSignalClass = typeof FxLadspaAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxLadspaChannelClass = typeof FxLadspaChannel;

    /**
     * @gir-type Struct
     */
    class FxLadspaChannelInputData {
        static $gtype: GObject.GType<FxLadspaChannelInputData>;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxLadspaChannelProcessorClass = typeof FxLadspaChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxLadspaRecyclingClass = typeof FxLadspaRecycling;

    /**
     * @gir-type Alias
     */
    type FxLfoAudioClass = typeof FxLfoAudio;

    /**
     * @gir-type Alias
     */
    type FxLfoAudioProcessorClass = typeof FxLfoAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxLfoAudioSignalClass = typeof FxLfoAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxLfoChannelClass = typeof FxLfoChannel;

    /**
     * @gir-type Struct
     */
    class FxLfoChannelInputData {
        static $gtype: GObject.GType<FxLfoChannelInputData>;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxLfoChannelProcessorClass = typeof FxLfoChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxLfoRecyclingClass = typeof FxLfoRecycling;

    /**
     * @gir-type Alias
     */
    type FxLowPassAudioClass = typeof FxLowPassAudio;

    /**
     * @gir-type Alias
     */
    type FxLowPassAudioProcessorClass = typeof FxLowPassAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxLowPassAudioSignalClass = typeof FxLowPassAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxLowPassChannelClass = typeof FxLowPassChannel;

    /**
     * @gir-type Alias
     */
    type FxLowPassChannelProcessorClass = typeof FxLowPassChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxLowPassRecyclingClass = typeof FxLowPassRecycling;

    /**
     * @gir-type Struct
     */
    class FxLv2AudioChannelData {
        static $gtype: GObject.GType<FxLv2AudioChannelData>;

        // Fields
        event_count: number;

        output: number;

        input: number;

        midiin_event_port: any;

        midiout_event_port: any;

        midiin_atom_port: any;

        midiout_atom_port: any;

        input_data: FxLv2AudioInputData[];

        // Methods
        /**
         * Free `channel_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxLv2AudioClass = typeof FxLv2Audio;

    /**
     * @gir-type Struct
     */
    class FxLv2AudioInputData {
        static $gtype: GObject.GType<FxLv2AudioInputData>;

        // Fields
        output: number;

        input: number;

        midiin_event_port: any;

        midiout_event_port: any;

        midiin_atom_port: any;

        midiout_atom_port: any;

        key_on: number;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxLv2AudioProcessorClass = typeof FxLv2AudioProcessor;

    /**
     * @gir-type Struct
     */
    class FxLv2AudioScopeData {
        static $gtype: GObject.GType<FxLv2AudioScopeData>;

        // Fields
        audio_channels: number;

        // Methods
        /**
         * Free `scope_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxLv2AudioSignalClass = typeof FxLv2AudioSignal;

    /**
     * @gir-type Alias
     */
    type FxLv2ChannelClass = typeof FxLv2Channel;

    /**
     * @gir-type Struct
     */
    class FxLv2ChannelInputData {
        static $gtype: GObject.GType<FxLv2ChannelInputData>;

        // Fields
        output: number;

        input: number;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxLv2ChannelProcessorClass = typeof FxLv2ChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxLv2RecyclingClass = typeof FxLv2Recycling;

    /**
     * @gir-type Alias
     */
    type FxNotationAudioClass = typeof FxNotationAudio;

    /**
     * @gir-type Alias
     */
    type FxNotationAudioProcessorClass = typeof FxNotationAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxNotationAudioSignalClass = typeof FxNotationAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxNotationChannelClass = typeof FxNotationChannel;

    /**
     * @gir-type Alias
     */
    type FxNotationChannelProcessorClass = typeof FxNotationChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxNotationRecyclingClass = typeof FxNotationRecycling;

    /**
     * @gir-type Alias
     */
    type FxPatternAudioClass = typeof FxPatternAudio;

    /**
     * @gir-type Alias
     */
    type FxPatternAudioProcessorClass = typeof FxPatternAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxPatternAudioSignalClass = typeof FxPatternAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxPatternChannelClass = typeof FxPatternChannel;

    /**
     * @gir-type Alias
     */
    type FxPatternChannelProcessorClass = typeof FxPatternChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxPatternRecyclingClass = typeof FxPatternRecycling;

    /**
     * @gir-type Alias
     */
    type FxPeakAudioClass = typeof FxPeakAudio;

    /**
     * @gir-type Alias
     */
    type FxPeakAudioProcessorClass = typeof FxPeakAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxPeakAudioSignalClass = typeof FxPeakAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxPeakChannelClass = typeof FxPeakChannel;

    /**
     * @gir-type Struct
     */
    class FxPeakChannelInputData {
        static $gtype: GObject.GType<FxPeakChannelInputData>;

        // Fields
        buffer: any;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxPeakChannelProcessorClass = typeof FxPeakChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxPeakRecyclingClass = typeof FxPeakRecycling;

    /**
     * @gir-type Alias
     */
    type FxPlaybackAudioClass = typeof FxPlaybackAudio;

    /**
     * @gir-type Alias
     */
    type FxPlaybackAudioProcessorClass = typeof FxPlaybackAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxPlaybackAudioSignalClass = typeof FxPlaybackAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxPlaybackChannelClass = typeof FxPlaybackChannel;

    /**
     * @gir-type Alias
     */
    type FxPlaybackChannelProcessorClass = typeof FxPlaybackChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxPlaybackRecyclingClass = typeof FxPlaybackRecycling;

    /**
     * @gir-type Struct
     */
    class FxSF2SynthAudioChannelData {
        static $gtype: GObject.GType<FxSF2SynthAudioChannelData>;

        // Fields
        input_data: FxSF2SynthAudioInputData[];

        // Methods
        /**
         * Free `channel_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSF2SynthAudioClass = typeof FxSF2SynthAudio;

    /**
     * @gir-type Struct
     */
    class FxSF2SynthAudioInputData {
        static $gtype: GObject.GType<FxSF2SynthAudioInputData>;

        // Fields
        key_on: number;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSF2SynthAudioProcessorClass = typeof FxSF2SynthAudioProcessor;

    /**
     * @gir-type Struct
     */
    class FxSF2SynthAudioScopeData {
        static $gtype: GObject.GType<FxSF2SynthAudioScopeData>;

        // Fields
        audio_channels: number;

        // Methods
        /**
         * Free `scope_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSF2SynthAudioSignalClass = typeof FxSF2SynthAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxSF2SynthChannelClass = typeof FxSF2SynthChannel;

    /**
     * @gir-type Alias
     */
    type FxSF2SynthChannelProcessorClass = typeof FxSF2SynthChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxSF2SynthRecyclingClass = typeof FxSF2SynthRecycling;

    /**
     * @gir-type Struct
     */
    class FxSFZSynthAudioChannelData {
        static $gtype: GObject.GType<FxSFZSynthAudioChannelData>;

        // Fields
        input_data: FxSFZSynthAudioInputData[];

        // Methods
        /**
         * Free `channel_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSFZSynthAudioClass = typeof FxSFZSynthAudio;

    /**
     * @gir-type Struct
     */
    class FxSFZSynthAudioInputData {
        static $gtype: GObject.GType<FxSFZSynthAudioInputData>;

        // Fields
        key_on: number;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSFZSynthAudioProcessorClass = typeof FxSFZSynthAudioProcessor;

    /**
     * @gir-type Struct
     */
    class FxSFZSynthAudioScopeData {
        static $gtype: GObject.GType<FxSFZSynthAudioScopeData>;

        // Fields
        audio_channels: number;

        // Methods
        /**
         * Free `scope_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSFZSynthAudioSignalClass = typeof FxSFZSynthAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxSFZSynthChannelClass = typeof FxSFZSynthChannel;

    /**
     * @gir-type Alias
     */
    type FxSFZSynthChannelProcessorClass = typeof FxSFZSynthChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxSFZSynthRecyclingClass = typeof FxSFZSynthRecycling;

    /**
     * @gir-type Struct
     */
    class FxSynthAudioChannelData {
        static $gtype: GObject.GType<FxSynthAudioChannelData>;

        // Fields
        pitch_type: GObject.GType;

        pitch_util: any;

        input_data: FxSynthAudioInputData[];

        // Methods
        /**
         * Free `channel_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSynthAudioClass = typeof FxSynthAudio;

    /**
     * @gir-type Struct
     */
    class FxSynthAudioInputData {
        static $gtype: GObject.GType<FxSynthAudioInputData>;

        // Fields
        key_on: number;

        // Methods
        /**
         * Free `input_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSynthAudioProcessorClass = typeof FxSynthAudioProcessor;

    /**
     * @gir-type Struct
     */
    class FxSynthAudioScopeData {
        static $gtype: GObject.GType<FxSynthAudioScopeData>;

        // Fields
        audio_channels: number;

        // Methods
        /**
         * Free `scope_data`.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type FxSynthAudioSignalClass = typeof FxSynthAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxSynthChannelClass = typeof FxSynthChannel;

    /**
     * @gir-type Alias
     */
    type FxSynthChannelProcessorClass = typeof FxSynthChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxSynthRecyclingClass = typeof FxSynthRecycling;

    /**
     * @gir-type Alias
     */
    type FxTremoloAudioClass = typeof FxTremoloAudio;

    /**
     * @gir-type Alias
     */
    type FxTremoloAudioProcessorClass = typeof FxTremoloAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxTremoloAudioSignalClass = typeof FxTremoloAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxTremoloChannelClass = typeof FxTremoloChannel;

    /**
     * @gir-type Alias
     */
    type FxTremoloChannelProcessorClass = typeof FxTremoloChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxTremoloRecyclingClass = typeof FxTremoloRecycling;

    /**
     * @gir-type Alias
     */
    type FxTwoPassAliaseAudioClass = typeof FxTwoPassAliaseAudio;

    /**
     * @gir-type Alias
     */
    type FxTwoPassAliaseAudioProcessorClass = typeof FxTwoPassAliaseAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxTwoPassAliaseAudioSignalClass = typeof FxTwoPassAliaseAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxTwoPassAliaseChannelClass = typeof FxTwoPassAliaseChannel;

    /**
     * @gir-type Struct
     */
    class FxTwoPassAliaseChannelInputData {
        static $gtype: GObject.GType<FxTwoPassAliaseChannelInputData>;

        // Fields
        orig_buffer: number;

        a_buffer: number;

        b_buffer: number;

        a_mix: number;

        b_mix: number;

        final_mix: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static alloc(): (any | null);

        /**
         * Free `input_data`.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}-struct
         */
        static free(input_data: (any | null)): void;

        /**
         * Get structure mutex.
         * @param input_data the {@link AgsAudio.FxTwoPassAliaseChannelInputData}
         */
        static get_strct_mutex(input_data: (any | null)): (any | null);
    }


    /**
     * @gir-type Alias
     */
    type FxTwoPassAliaseChannelProcessorClass = typeof FxTwoPassAliaseChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxTwoPassAliaseRecyclingClass = typeof FxTwoPassAliaseRecycling;

    /**
     * @gir-type Alias
     */
    type FxVolumeAudioClass = typeof FxVolumeAudio;

    /**
     * @gir-type Alias
     */
    type FxVolumeAudioProcessorClass = typeof FxVolumeAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxVolumeAudioSignalClass = typeof FxVolumeAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxVolumeChannelClass = typeof FxVolumeChannel;

    /**
     * @gir-type Alias
     */
    type FxVolumeChannelProcessorClass = typeof FxVolumeChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxVolumeRecyclingClass = typeof FxVolumeRecycling;

    /**
     * @gir-type Alias
     */
    type FxWahWahAudioClass = typeof FxWahWahAudio;

    /**
     * @gir-type Alias
     */
    type FxWahWahAudioProcessorClass = typeof FxWahWahAudioProcessor;

    /**
     * @gir-type Alias
     */
    type FxWahWahAudioSignalClass = typeof FxWahWahAudioSignal;

    /**
     * @gir-type Alias
     */
    type FxWahWahChannelClass = typeof FxWahWahChannel;

    /**
     * @gir-type Alias
     */
    type FxWahWahChannelProcessorClass = typeof FxWahWahChannelProcessor;

    /**
     * @gir-type Alias
     */
    type FxWahWahRecyclingClass = typeof FxWahWahRecycling;

    /**
     * @gir-type Alias
     */
    type GenericRecallChannelRunClass = typeof GenericRecallChannelRun;

    /**
     * @gir-type Alias
     */
    type GenericRecallRecyclingClass = typeof GenericRecallRecycling;

    /**
     * @gir-type Alias
     */
    type GstreamerClientClass = typeof GstreamerClient;

    /**
     * @gir-type Alias
     */
    type GstreamerDevinClass = typeof GstreamerDevin;

    /**
     * @gir-type Alias
     */
    type GstreamerDevoutClass = typeof GstreamerDevout;

    /**
     * @gir-type Alias
     */
    type GstreamerFileClass = typeof GstreamerFile;

    /**
     * @gir-type Alias
     */
    type GstreamerPortClass = typeof GstreamerPort;

    /**
     * @gir-type Alias
     */
    type GstreamerServerClass = typeof GstreamerServer;

    /**
     * @gir-type Struct
     */
    class HQPitchUtil {
        static $gtype: GObject.GType<HQPitchUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        low_mix_buffer: any;

        low_mix_buffer_max_buffer_length: number;

        new_mix_buffer: any;

        new_mix_buffer_max_buffer_length: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        base_key: number;

        tuning: number;

        linear_interpolate_util: LinearInterpolateUtil;

        vibrato_enabled: number;

        vibrato_gain: number;

        vibrato_lfo_depth: number;

        vibrato_lfo_freq: number;

        vibrato_tuning: number;

        vibrato_lfo_frame_count: number;

        vibrato_lfo_offset: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.HQPitchUtil}-struct.
         */
        static alloc(): HQPitchUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.HQPitchUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get base key of `hq_pitch_util`.
         * @returns the base key
         */
        get_base_key(): number;

        /**
         * Get buffer length of `hq_pitch_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `hq_pitch_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `hq_pitch_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `hq_pitch_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `hq_pitch_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get note 256th mode of `hq_pitch_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `hq_pitch_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `hq_pitch_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get samplerate of `hq_pitch_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `hq_pitch_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `hq_pitch_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get tuning of `hq_pitch_util`.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Get vibrato enabled of `hq_pitch_util`.
         * @returns `true` if the vibrato is enabled, otherwise `false`
         */
        get_vibrato_enabled(): boolean;

        /**
         * Get vibrato gain of `hq_pitch_util`.
         * @returns the vibrato gain
         */
        get_vibrato_gain(): number;

        /**
         * Get vibrato lfo_depth of `hq_pitch_util`.
         * @returns the vibrato lfo_depth
         */
        get_vibrato_lfo_depth(): number;

        /**
         * Get vibrato lfo_freq of `hq_pitch_util`.
         * @returns the vibrato lfo_freq
         */
        get_vibrato_lfo_freq(): number;

        /**
         * Get vibrato lfo_offset of `hq_pitch_util`.
         * @returns the vibrato lfo_offset
         */
        get_vibrato_lfo_offset(): number;

        /**
         * Get vibrato tuning of `hq_pitch_util`.
         * @returns the vibrato tuning
         */
        get_vibrato_tuning(): number;

        /**
         * Pitch `hq_pitch_util`.
         */
        pitch(): void;

        /**
         * Pitch `hq_pitch_util` of complex data.
         */
        pitch_complex(): void;

        /**
         * Pitch `hq_pitch_util` of double precision floating point data.
         */
        pitch_double(): void;

        /**
         * Pitch `hq_pitch_util` of floating point data.
         */
        pitch_float(): void;

        /**
         * Pitch `hq_pitch_util` of signed 16 bit data.
         */
        pitch_s16(): void;

        /**
         * Pitch `hq_pitch_util` of signed 24 bit data.
         */
        pitch_s24(): void;

        /**
         * Pitch `hq_pitch_util` of signed 32 bit data.
         */
        pitch_s32(): void;

        /**
         * Pitch `hq_pitch_util` of signed 64 bit data.
         */
        pitch_s64(): void;

        /**
         * Pitch `hq_pitch_util` of signed 8 bit data.
         */
        pitch_s8(): void;

        /**
         * Set `base_key` of `hq_pitch_util`.
         * @param base_key the base key
         */
        set_base_key(base_key: number): void;

        /**
         * Set `buffer_length` of `hq_pitch_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `hq_pitch_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `hq_pitch_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `hq_pitch_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `hq_pitch_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `note_256th_mode` of `hq_pitch_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `hq_pitch_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `hq_pitch_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `samplerate` of `hq_pitch_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `hq_pitch_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `hq_pitch_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `tuning` of `hq_pitch_util`.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;

        /**
         * Set `vibrato_enabled` of `hq_pitch_util`.
         * @param vibrato_enabled the vibrato enabled
         */
        set_vibrato_enabled(vibrato_enabled: boolean): void;

        /**
         * Set `vibrato_gain` of `hq_pitch_util`.
         * @param vibrato_gain the vibrato gain
         */
        set_vibrato_gain(vibrato_gain: number): void;

        /**
         * Set `vibrato_lfo_depth` of `hq_pitch_util`.
         * @param vibrato_lfo_depth the vibrato lfo_depth
         */
        set_vibrato_lfo_depth(vibrato_lfo_depth: number): void;

        /**
         * Set `vibrato_lfo_freq` of `hq_pitch_util`.
         * @param vibrato_lfo_freq the vibrato lfo_freq
         */
        set_vibrato_lfo_freq(vibrato_lfo_freq: number): void;

        /**
         * Set `vibrato_lfo_offset` of `hq_pitch_util`.
         * @param vibrato_lfo_offset the vibrato lfo_offset
         */
        set_vibrato_lfo_offset(vibrato_lfo_offset: number): void;

        /**
         * Set `vibrato_tuning` of `hq_pitch_util`.
         * @param vibrato_tuning the vibrato tuning
         */
        set_vibrato_tuning(vibrato_tuning: number): void;
    }


    /**
     * @gir-type Alias
     */
    type InputClass = typeof Input;

    /**
     * @gir-type Alias
     */
    type IpatchClass = typeof Ipatch;

    /**
     * @gir-type Alias
     */
    type IpatchDLS2ReaderClass = typeof IpatchDLS2Reader;

    /**
     * @gir-type Alias
     */
    type IpatchGigReaderClass = typeof IpatchGigReader;

    /**
     * @gir-type Alias
     */
    type IpatchSF2ReaderClass = typeof IpatchSF2Reader;

    /**
     * @gir-type Alias
     */
    type IpatchSampleClass = typeof IpatchSample;

    /**
     * @gir-type Alias
     */
    type JackClientClass = typeof JackClient;

    /**
     * @gir-type Alias
     */
    type JackDevinClass = typeof JackDevin;

    /**
     * @gir-type Alias
     */
    type JackDevoutClass = typeof JackDevout;

    /**
     * @gir-type Alias
     */
    type JackMidiinClass = typeof JackMidiin;

    /**
     * @gir-type Alias
     */
    type JackPortClass = typeof JackPort;

    /**
     * @gir-type Alias
     */
    type JackServerClass = typeof JackServer;

    /**
     * @gir-type Struct
     */
    class LFOSynthUtil {
        static $gtype: GObject.GType<LFOSynthUtil>;

        // Fields
        source: any;

        source_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        lfo_synth_oscillator_mode: number;

        frequency: number;

        phase: number;

        lfo_depth: number;

        tuning: number;

        offset: number;

        frame_count: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.LFOSynthUtil}-struct
         */
        static alloc(): LFOSynthUtil;

        // Methods
        /**
         * Compute LFO impulse synth.
         */
        compute_impulse(): void;

        /**
         * Compute LFO impulse synth of complex data.
         */
        compute_impulse_complex(): void;

        /**
         * Compute LFO impulse synth of double precision floating point data.
         */
        compute_impulse_double(): void;

        /**
         * Compute LFO impulse synth of single precision floating point data.
         */
        compute_impulse_float(): void;

        /**
         * Compute LFO impulse synth of signed 16 bit data.
         */
        compute_impulse_s16(): void;

        /**
         * Compute LFO impulse synth of signed 24 bit data.
         */
        compute_impulse_s24(): void;

        /**
         * Compute LFO impulse synth of signed 32 bit data.
         */
        compute_impulse_s32(): void;

        /**
         * Compute LFO impulse synth of signed 64 bit data.
         */
        compute_impulse_s64(): void;

        /**
         * Compute LFO impulse synth of signed 8 bit data.
         */
        compute_impulse_s8(): void;

        /**
         * Compute LFO sawtooth synth.
         */
        compute_sawtooth(): void;

        /**
         * Compute LFO sawtooth synth of complex data.
         */
        compute_sawtooth_complex(): void;

        /**
         * Compute LFO sawtooth synth of double precision floating point data.
         */
        compute_sawtooth_double(): void;

        /**
         * Compute LFO sawtooth synth of single precision floating point data.
         */
        compute_sawtooth_float(): void;

        /**
         * Compute LFO sawtooth synth of signed 16 bit data.
         */
        compute_sawtooth_s16(): void;

        /**
         * Compute LFO sawtooth synth of signed 24 bit data.
         */
        compute_sawtooth_s24(): void;

        /**
         * Compute LFO sawtooth synth of signed 32 bit data.
         */
        compute_sawtooth_s32(): void;

        /**
         * Compute LFO sawtooth synth of signed 64 bit data.
         */
        compute_sawtooth_s64(): void;

        /**
         * Compute LFO sawtooth synth of signed 8 bit data.
         */
        compute_sawtooth_s8(): void;

        /**
         * Compute LFO sine synth.
         */
        compute_sin(): void;

        /**
         * Compute LFO sine synth of complex data.
         */
        compute_sin_complex(): void;

        /**
         * Compute LFO sine synth of double precision floating point data.
         */
        compute_sin_double(): void;

        /**
         * Compute LFO sine synth of single precision floating point data.
         */
        compute_sin_float(): void;

        /**
         * Compute LFO sine synth of signed 16 bit data.
         */
        compute_sin_s16(): void;

        /**
         * Compute LFO sine synth of signed 24 bit data.
         */
        compute_sin_s24(): void;

        /**
         * Compute LFO sine synth of signed 32 bit data.
         */
        compute_sin_s32(): void;

        /**
         * Compute LFO sine synth of signed 64 bit data.
         */
        compute_sin_s64(): void;

        /**
         * Compute LFO sine synth of signed 8 bit data.
         */
        compute_sin_s8(): void;

        /**
         * Compute LFO square synth.
         */
        compute_square(): void;

        /**
         * Compute LFO square synth of complex data.
         */
        compute_square_complex(): void;

        /**
         * Compute LFO square synth of double precision floating point data.
         */
        compute_square_double(): void;

        /**
         * Compute LFO square synth of single precision floating point data.
         */
        compute_square_float(): void;

        /**
         * Compute LFO square synth of signed 16 bit data.
         */
        compute_square_s16(): void;

        /**
         * Compute LFO square synth of signed 24 bit data.
         */
        compute_square_s24(): void;

        /**
         * Compute LFO square synth of signed 32 bit data.
         */
        compute_square_s32(): void;

        /**
         * Compute LFO square synth of signed 64 bit data.
         */
        compute_square_s64(): void;

        /**
         * Compute LFO square synth of signed 8 bit data.
         */
        compute_square_s8(): void;

        /**
         * Compute LFO triangle synth.
         */
        compute_triangle(): void;

        /**
         * Compute LFO triangle synth of complex data.
         */
        compute_triangle_complex(): void;

        /**
         * Compute LFO triangle synth of double precision floating point data.
         */
        compute_triangle_double(): void;

        /**
         * Compute LFO triangle synth of single precision floating point data.
         */
        compute_triangle_float(): void;

        /**
         * Compute LFO triangle synth of signed 16 bit data.
         */
        compute_triangle_s16(): void;

        /**
         * Compute LFO triangle synth of signed 24 bit data.
         */
        compute_triangle_s24(): void;

        /**
         * Compute LFO triangle synth of signed 32 bit data.
         */
        compute_triangle_s32(): void;

        /**
         * Compute LFO triangle synth of signed 64 bit data.
         */
        compute_triangle_s64(): void;

        /**
         * Compute LFO triangle synth of signed 8 bit data.
         */
        compute_triangle_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.LFOSynthUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `lfo_synth_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get audio buffer util format of `lfo_synth_util`.
         * @returns the audio buffer util format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `lfo_synth_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get frequency of `lfo_synth_util`.
         * @returns the frequency
         */
        get_frequency(): number;

        /**
         * Get LFO depth of `lfo_synth_util`.
         * @returns the LFO depth
         */
        get_lfo_depth(): number;

        /**
         * Get LFO synth oscillator mode of `lfo_synth_util`.
         * @returns the LFO synth oscillator mode
         */
        get_lfo_synth_oscillator_mode(): SynthOscillatorMode;

        /**
         * Get offset of `lfo_synth_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get phase of `lfo_synth_util`.
         * @returns the phase
         */
        get_phase(): number;

        /**
         * Get samplerate of `lfo_synth_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `lfo_synth_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `lfo_synth_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get tuning of `lfo_synth_util`.
         * @returns the tuning
         */
        get_tuning(): number;

        /**
         * Set `buffer_length` of `lfo_synth_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `format` of `lfo_synth_util`.
         * @param format the audio buffer util format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `lfo_synth_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `frequency` of `lfo_synth_util`.
         * @param frequency the frequency
         */
        set_frequency(frequency: number): void;

        /**
         * Set `lfo_depth` of `lfo_synth_util`.
         * @param lfo_depth the LFO depth
         */
        set_lfo_depth(lfo_depth: number): void;

        /**
         * Set `lfo_synth_oscillator_mode` of `lfo_synth_util`.
         * @param lfo_synth_oscillator_mode the LFO synth oscillator mode
         */
        set_lfo_synth_oscillator_mode(lfo_synth_oscillator_mode: SynthOscillatorMode): void;

        /**
         * Set `offset` of `lfo_synth_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `phase` of `lfo_synth_util`.
         * @param phase the phase
         */
        set_phase(phase: number): void;

        /**
         * Set `samplerate` of `lfo_synth_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `lfo_synth_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `lfo_synth_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `tuning` of `lfo_synth_util`.
         * @param tuning the tuning
         */
        set_tuning(tuning: number): void;
    }


    /**
     * @gir-type Alias
     */
    type LadspaConversionClass = typeof LadspaConversion;

    /**
     * @gir-type Alias
     */
    type LadspaManagerClass = typeof LadspaManager;

    /**
     * @gir-type Alias
     */
    type LadspaPluginClass = typeof LadspaPlugin;

    /**
     * @gir-type Struct
     */
    class LinearInterpolateUtil {
        static $gtype: GObject.GType<LinearInterpolateUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        buffer_length: number;

        format: number;

        samplerate: number;

        factor: number;

        // Constructors

        constructor(properties?: Partial<{
            source: any;
            source_stride: number;
            destination: any;
            destination_stride: number;
            buffer_length: number;
            format: number;
            samplerate: number;
            factor: number;
        }>);

        // Static methods
        /**
         * Allocate {@link AgsAudio.LinearInterpolateUtil}-struct.
         */
        static alloc(): LinearInterpolateUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.LinearInterpolateUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `linear_interpolate_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `linear_interpolate_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `linear_interpolate_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get factor of `linear_interpolate_util`.
         * @returns the factor
         */
        get_factor(): number;

        /**
         * Get format of `linear_interpolate_util`.
         * @returns the format
         */
        get_format(): number;

        /**
         * Get samplerate of `linear_interpolate_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `linear_interpolate_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `linear_interpolate_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Pitch `linear_interpolate_util`.
         */
        pitch(): void;

        /**
         * Pitch `linear_interpolate_util` of complex data.
         */
        pitch_complex(): void;

        /**
         * Pitch `linear_interpolate_util` of double precision floating point data.
         */
        pitch_double(): void;

        /**
         * Pitch `linear_interpolate_util` of floating point data.
         */
        pitch_float(): void;

        /**
         * Pitch `linear_interpolate_util` of signed 16 bit data.
         */
        pitch_s16(): void;

        /**
         * Pitch `linear_interpolate_util` of signed 24 bit data.
         */
        pitch_s24(): void;

        /**
         * Pitch `linear_interpolate_util` of signed 32 bit data.
         */
        pitch_s32(): void;

        /**
         * Pitch `linear_interpolate_util` of signed 64 bit data.
         */
        pitch_s64(): void;

        /**
         * Pitch `linear_interpolate_util` of signed 8 bit data.
         */
        pitch_s8(): void;

        /**
         * Set `buffer_length` of `linear_interpolate_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `linear_interpolate_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `linear_interpolate_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `factor` of `linear_interpolate_util`.
         * @param factor the factor
         */
        set_factor(factor: number): void;

        /**
         * Set `format` of `linear_interpolate_util`.
         * @param format the format
         */
        set_format(format: number): void;

        /**
         * Set `samplerate` of `linear_interpolate_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `linear_interpolate_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `linear_interpolate_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;
    }


    /**
     * @gir-type Alias
     */
    type LinkChannelClass = typeof LinkChannel;

    /**
     * @gir-type Struct
     */
    class Lv2CacheTurtle {
        static $gtype: GObject.GType<Lv2CacheTurtle>;

        // Fields
        turtle_filename: string;

        plugin_count: number;

        see_also: string;

        // Methods
        /**
         * Free an {@link AgsAudio.Lv2CacheTurtle}-struct
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type Lv2ConversionClass = typeof Lv2Conversion;

    /**
     * @gir-type Alias
     */
    type Lv2EventManagerClass = typeof Lv2EventManager;

    /**
     * @gir-type Alias
     */
    type Lv2LogManagerClass = typeof Lv2LogManager;

    /**
     * @gir-type Alias
     */
    type Lv2ManagerClass = typeof Lv2Manager;

    /**
     * @gir-type Alias
     */
    type Lv2OptionManagerClass = typeof Lv2OptionManager;

    /**
     * @gir-type Struct
     */
    class Lv2OptionRessource {
        static $gtype: GObject.GType<Lv2OptionRessource>;

        // Constructors

        constructor(properties?: Partial<{
            instance: unknown;
            option: unknown;
        }>);

        // Static methods
        /**
         * Allocate an {@link AgsAudio.Lv2OptionRessource}.
         */
        static alloc(): (any | null);
    }


    /**
     * @gir-type Alias
     */
    type Lv2PluginClass = typeof Lv2Plugin;

    /**
     * @gir-type Struct
     */
    class Lv2PortPreset {
        static $gtype: GObject.GType<Lv2PortPreset>;

        // Fields
        port_symbol: string;

        // Static methods
        /**
         * Allocated {@link AgsAudio.Lv2PortPreset}-struct.
         * @param port_symbol the port symbol
         * @param port_type the port type
         */
        static alloc(port_symbol: string, port_type: GObject.GType): (any | null);

        /**
         * Free `lv2_port_preset`.
         * @param lv2_port_preset the {@link AgsAudio.Lv2PortPreset}-struct
         */
        static free(lv2_port_preset: (any | null)): void;
    }


    /**
     * @gir-type Alias
     */
    type Lv2PresetClass = typeof Lv2Preset;

    /**
     * @gir-type Alias
     */
    type Lv2PresetManagerClass = typeof Lv2PresetManager;

    /**
     * @gir-type Alias
     */
    type Lv2TurtleParserClass = typeof Lv2TurtleParser;

    /**
     * @gir-type Alias
     */
    type Lv2TurtleScannerClass = typeof Lv2TurtleScanner;

    /**
     * @gir-type Alias
     */
    type Lv2UriMapManagerClass = typeof Lv2UriMapManager;

    /**
     * @gir-type Alias
     */
    type Lv2UridManagerClass = typeof Lv2UridManager;

    /**
     * @gir-type Alias
     */
    type Lv2WorkerClass = typeof Lv2Worker;

    /**
     * @gir-type Alias
     */
    type Lv2WorkerManagerClass = typeof Lv2WorkerManager;

    /**
     * @gir-type Struct
     */
    class Lv2WorkerResponseData {
        static $gtype: GObject.GType<Lv2WorkerResponseData>;

        // Fields
        data_size: number;

        data: any;

        // Constructors

        constructor(properties?: Partial<{
            data_size: number;
            data: any;
        }>);
    }


    /**
     * @gir-type Alias
     */
    type Lv2uiManagerClass = typeof Lv2uiManager;

    /**
     * @gir-type Alias
     */
    type Lv2uiPluginClass = typeof Lv2uiPlugin;

    /**
     * @gir-type Alias
     */
    type MarkerClass = typeof Marker;

    /**
     * @gir-type Alias
     */
    type MidiBuilderClass = typeof MidiBuilder;

    /**
     * @gir-type Struct
     */
    class MidiBuilderHeader {
        static $gtype: GObject.GType<MidiBuilderHeader>;

        // Fields
        midi_builder: MidiBuilder;

        offset: number;

        format: number;

        count: number;

        division: number;

        times: number;

        beat: number;

        clicks: number;

        data: number;

        length: number;

        // Static methods
        /**
         * Allocate MIDI builder header.
         */
        static alloc(): (any | null);

        /**
         * Free MIDI builder header.
         * @param midi_builder_header the {@link AgsAudio.MidiBuilderHeader}-struct
         */
        static free(midi_builder_header: (any | null)): void;
    }


    /**
     * @gir-type Struct
     */
    class MidiBuilderTrack {
        static $gtype: GObject.GType<MidiBuilderTrack>;

        // Fields
        midi_builder: MidiBuilder;

        offset: number;

        track_name: string;

        absolute_time: number;

        data: number;

        length: number;

        // Static methods
        /**
         * Allocate MIDI builder track.
         */
        static alloc(): (any | null);

        /**
         * Free MIDI builder track.
         * @param midi_builder_track the {@link AgsAudio.MidiBuilderTrack}-struct
         */
        static free(midi_builder_track: (any | null)): void;

        // Methods
        /**
         * Get offset by delta time.
         * @param absolute_time the absolute time
         * @returns the buffer position before `delta_time`
         */
        get_delta_time_offset(absolute_time: (bigint | number)): number;

        /**
         * Insert MIDI message.
         * @param buffer the MIDI message
         * @param length the buffer length
         */
        insert_midi_message(buffer: number, length: number): void;
    }


    /**
     * @gir-type Struct
     */
    class MidiCIUtil {
        static $gtype: GObject.GType<MidiCIUtil>;

        // Fields
        major: number;

        minor: number;

        // Static methods
        /**
         * Allocate MIDI CI util.
         */
        static alloc(): MidiCIUtil;

        // Methods
        /**
         * Copy MIDI CI util.
         * @returns the newly allocated {@link AgsAudio.MidiCIUtil}-struct
         */
        copy(): MidiCIUtil;

        /**
         * Free MIDI CI util.
         */
        free(): void;

        /**
         * Generate MUID.
         * @returns the generated MUID
         */
        generate_muid(): MUID;

        /**
         * Get ACK MIDI CI message.
         * @param buffer the buffer
         * @param orig_transaction the original transaction
         * @returns the number of bytes read
         */
        get_ack(buffer: number, orig_transaction: number): [number, number, number, MUID, MUID, number, number, number, number, number];

        /**
         * Get confirm protocol type message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_confirm_protocol_type(buffer: number): [number, number, number, MUID, MUID, MidiCIAuthorityLevel];

        /**
         * Get confirm protocol type established message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_confirm_protocol_type_established(buffer: number): [number, number, number, MUID, MUID, MidiCIAuthorityLevel];

        /**
         * Get confirm protocol type reply message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_confirm_protocol_type_reply(buffer: number): [number, number, number, MUID, MUID, MidiCIAuthorityLevel];

        /**
         * Get discovery message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_discovery(buffer: number): [number, number, number, MUID, number, number, number, number, number, number];

        /**
         * Get discovery message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_discovery_reply(buffer: number): [number, number, number, MUID, MUID, number, number, number, number, number, number];

        /**
         * Get message report.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_end_of_message_report(buffer: number): [number, number, number, MUID, MUID];

        /**
         * Get get property data.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_get_property_data(buffer: number): [number, number, number, MUID, MUID, number, number, number, number, number, number, number];

        /**
         * Get get property data.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_get_property_data_reply(buffer: number): [number, number, number, MUID, MUID, number, number, number, number, number, number, number];

        /**
         * Get initiate protocol negotiation message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_initiate_protocol_negotiation(buffer: number): [number, number, number, MUID, MUID, MidiCIAuthorityLevel, number, number];

        /**
         * Get initiate protocol negotiation reply message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_initiate_protocol_negotiation_reply(buffer: number): [number, number, number, MUID, MUID, MidiCIAuthorityLevel, number, number];

        /**
         * Get invalidate MUID.
         * @param buffer the buffer
         * @param device_id the return location of device ID
         * @param version the return location of version
         * @param source the return location of source
         * @param target_muid the return location of MUID
         * @returns the number of bytes read
         */
        get_invalidate_muid(buffer: number, device_id: number, version: number, source: MUID, target_muid: MUID): number;

        /**
         * Get message report.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_message_report(buffer: number): [number, number, number, MUID, MUID, number, number, number, number, number];

        /**
         * Get message report reply.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_message_report_reply(buffer: number): [number, number, number, MUID, MUID, number, number, number, number];

        /**
         * Get MUID.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_muid(buffer: number): [number, MUID];

        /**
         * Get NAK MIDI CI message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_nak(buffer: number): [number, number, number, MUID, MUID];

        /**
         * Get process capabilities.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_process_capabilities(buffer: number): [number, number, number, MUID, MUID];

        /**
         * Get process capabilities reply.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_process_capabilities_reply(buffer: number): [number, number, number, MUID, MUID, number];

        /**
         * Profile message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_profile(buffer: number): [number, number, number, MUID, MUID];

        /**
         * Get profile added message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_profile_added(buffer: number): [number, number, number, MUID, number];

        /**
         * Get profile disabled report message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_profile_disabled_report(buffer: number): [number, number, number, MUID, number, number];

        /**
         * Get profile enabled report message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_profile_enabled_report(buffer: number): [number, number, number, MUID, number, number];

        /**
         * Get profile remove message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_profile_removed(buffer: number): [number, number, number, MUID, number];

        /**
         * Profile message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_profile_reply(buffer: number): [number, number, number, MUID, MUID, number, number, number, number];

        /**
         * Get profile specific data message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_profile_specific_data(buffer: number): [number, number, number, MUID, MUID, number, number, number];

        /**
         * Get number of supported property exchange count data message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_property_exchange_capabilities(buffer: number): [number, number, number, MUID, MUID, number, number, number];

        /**
         * Get number of supported property exchange count data message.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_property_exchange_capabilities_reply(buffer: number): [number, number, number, MUID, MUID, number, number, number];

        /**
         * Get set property data.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_set_property_data(buffer: number): [number, number, number, MUID, MUID, number, number, number, number, number, number, number];

        /**
         * Get set property data reply.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_set_property_data_reply(buffer: number): [number, number, number, MUID, MUID, number, number, number, number, number, number, number];

        /**
         * Get set protocol type message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_set_protocol_type(buffer: number): [number, number, number, MUID, MUID, MidiCIAuthorityLevel, number];

        /**
         * Get subscription.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_subscription(buffer: number): [number, number, number, MUID, MUID, number, number, number, number, number, number, number];

        /**
         * Get subscription reply.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_subscription_reply(buffer: number): [number, number, number, MUID, MUID, number, number, number, number, number, number, number];

        /**
         * Put ACK MIDI CI message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param orig_transaction the original transaction
         * @param status_code the status code
         * @param status_data the status data
         * @param details the details
         * @param message_length the message length
         * @param message the message
         */
        put_ack(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, orig_transaction: number, status_code: number, status_data: number, details: number, message_length: number, message: number): void;

        /**
         * Put confirm protocol type message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param authority_level the authority level
         */
        put_confirm_protocol_type(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, authority_level: MidiCIAuthorityLevel): void;

        /**
         * Put confirm protocol type established message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param authority_level the authority level
         */
        put_confirm_protocol_type_established(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, authority_level: MidiCIAuthorityLevel): void;

        /**
         * Put confirm protocol type reply message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param authority_level the authority level
         */
        put_confirm_protocol_type_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, authority_level: MidiCIAuthorityLevel): void;

        /**
         * Put discovery message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param manufacturer_id the manufacturer ID
         * @param device_family the device family
         * @param device_family_model_number the device family number
         * @param software_revision_level the software revision level
         * @param capability the capability
         * @param max_sysex_message_size the maximum SYSEX message size
         */
        put_discovery(buffer: number, device_id: number, version: number, source: MUID, manufacturer_id: number, device_family: number, device_family_model_number: number, software_revision_level: number, capability: number, max_sysex_message_size: number): void;

        /**
         * Put discovery message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param manufacturer_id the manufacturer ID
         * @param device_family the device family
         * @param device_family_model_number the device family number
         * @param software_revision_level the software revision level
         * @param capability the capability
         * @param max_sysex_message_size the maximum SYSEX message size
         */
        put_discovery_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, manufacturer_id: string, device_family: number, device_family_model_number: number, software_revision_level: string, capability: number, max_sysex_message_size: number): void;

        /**
         * Put message report.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         */
        put_end_of_message_report(buffer: number, device_id: number, version: number, source: MUID, destination: MUID): void;

        /**
         * Put get property data.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param request_id the request ID
         * @param header_data_length the header data length
         * @param header_data the header data
         * @param chunk_count the chunk count
         * @param nth_chunk the nth chunk
         * @param property_data_length the property data length
         * @param property_data the property data
         */
        put_get_property_data(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, request_id: number, header_data_length: number, header_data: number, chunk_count: number, nth_chunk: number, property_data_length: number, property_data: number): void;

        /**
         * Put get property data.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param request_id the request ID
         * @param header_data_length the header data length
         * @param header_data the header data
         * @param chunk_count the chunk count
         * @param nth_chunk the nth chunk
         * @param property_data_length the property data length
         * @param property_data the property data
         */
        put_get_property_data_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, request_id: number, header_data_length: number, header_data: number, chunk_count: number, nth_chunk: number, property_data_length: number, property_data: number): void;

        /**
         * Put initiate protocol negotiation message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param authority_level the authority level
         * @param number_of_supported_protocols the number of supported protocols
         * @param preferred_protocol_type the preferred protocol type
         */
        put_initiate_protocol_negotiation(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, authority_level: MidiCIAuthorityLevel, number_of_supported_protocols: number, preferred_protocol_type: number): void;

        /**
         * Put initiate protocol negotiation reply message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param authority_level the authority level
         * @param number_of_supported_protocols the number of supported protocols
         * @param preferred_protocol_type the preferred protocol type
         */
        put_initiate_protocol_negotiation_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, authority_level: MidiCIAuthorityLevel, number_of_supported_protocols: number, preferred_protocol_type: number): void;

        /**
         * Put invalidate MUID.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param target_muid the MUID
         */
        put_invalidate_muid(buffer: number, device_id: number, version: number, source: MUID, target_muid: MUID): void;

        /**
         * Put message report.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param data_control the data control
         * @param system_messages the system messages
         * @param other_messages the other messages
         * @param channel_controller_messages the channel controller messages
         * @param note_data_messages the note data messages
         */
        put_message_report(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, data_control: number, system_messages: number, other_messages: number, channel_controller_messages: number, note_data_messages: number): void;

        /**
         * Put message report reply.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param system_messages the system messages
         * @param other_messages the other messages
         * @param channel_controller_messages the channel controller messages
         * @param note_data_messages the note data messages
         */
        put_message_report_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, system_messages: number, other_messages: number, channel_controller_messages: number, note_data_messages: number): void;

        /**
         * Put MUID.
         * @param buffer the buffer
         * @param muid the {@link AgsAudio.MUID}
         */
        put_muid(buffer: number, muid: MUID): void;

        /**
         * Put NAK MIDI CI message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         */
        put_nak(buffer: number, device_id: number, version: number, source: MUID, destination: MUID): void;

        /**
         * Put process capabilities.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         */
        put_process_capabilities(buffer: number, device_id: number, version: number, source: MUID, destination: MUID): void;

        /**
         * Put process capabilities reply.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param supported_features the supported features
         */
        put_process_capabilities_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, supported_features: number): void;

        /**
         * Put profile message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         */
        put_profile(buffer: number, device_id: number, version: number, source: MUID, destination: MUID): void;

        /**
         * Put profile added message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param add_profile the profile to add
         */
        put_profile_added(buffer: number, device_id: number, version: number, source: MUID, add_profile: number): void;

        /**
         * Put profile disabled report message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param disabled_profile the disabled profile
         * @param disabled_channel_count the disabled channel count
         */
        put_profile_disabled_report(buffer: number, device_id: number, version: number, source: MUID, disabled_profile: number, disabled_channel_count: number): void;

        /**
         * Put profile enabled report message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param enabled_profile the enabled profile
         * @param enabled_channel_count the enabled channel count
         */
        put_profile_enabled_report(buffer: number, device_id: number, version: number, source: MUID, enabled_profile: number, enabled_channel_count: number): void;

        /**
         * Put profile remove message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param remove_profile the profile to remove
         */
        put_profile_removed(buffer: number, device_id: number, version: number, source: MUID, remove_profile: number): void;

        /**
         * Put profile message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param enabled_profile_count enabled profile count
         * @param enabled_profile enabled profile
         * @param disabled_profile_count disabled profile count
         * @param disabled_profile disabled profile
         */
        put_profile_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, enabled_profile_count: number, enabled_profile: number, disabled_profile_count: number, disabled_profile: number): void;

        /**
         * Put profile specific data message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param profile_id the profile ID
         * @param profile_specific_data_length profile specific data length
         * @param profile_specific_data profile specific data
         */
        put_profile_specific_data(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, profile_id: number, profile_specific_data_length: number, profile_specific_data: number): void;

        /**
         * Put number of supported property exchange message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param supported_property_exchange_count the supported property exchange count
         * @param property_exchange_major the major version
         * @param property_exchange_minor the minor version
         */
        put_property_exchange_capabilities(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, supported_property_exchange_count: number, property_exchange_major: number, property_exchange_minor: number): void;

        /**
         * Put number of supported property exchange message.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param supported_property_exchange_count the supported property exchange count
         * @param property_exchange_major the major version
         * @param property_exchange_minor the minor version
         */
        put_property_exchange_capabilities_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, supported_property_exchange_count: number, property_exchange_major: number, property_exchange_minor: number): void;

        /**
         * Put set property data.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param request_id the request ID
         * @param header_data_length the header data length
         * @param header_data the header data
         * @param chunk_count the chunk count
         * @param nth_chunk the nth chunk
         * @param property_data_length the property data length
         * @param property_data the property data
         */
        put_set_property_data(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, request_id: number, header_data_length: number, header_data: number, chunk_count: number, nth_chunk: number, property_data_length: number, property_data: number): void;

        /**
         * Put set property data reply.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param request_id the request ID
         * @param header_data_length the header data length
         * @param header_data the header data
         * @param chunk_count the chunk count
         * @param nth_chunk the nth chunk
         * @param property_data_length the property data length
         * @param property_data the property data
         */
        put_set_property_data_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, request_id: number, header_data_length: number, header_data: number, chunk_count: number, nth_chunk: number, property_data_length: number, property_data: number): void;

        /**
         * Put set protocol type message. Deprecated since MIDI CI version 1.2.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param authority_level the authority level
         * @param protocol_type the new protocol type
         */
        put_set_protocol_type(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, authority_level: MidiCIAuthorityLevel, protocol_type: number): void;

        /**
         * Put subscription.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param request_id the request ID
         * @param header_data_length the header data length
         * @param header_data the header data
         * @param chunk_count the chunk count
         * @param nth_chunk the nth chunk
         * @param property_data_length the property data length
         * @param property_data the property data
         */
        put_subscription(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, request_id: number, header_data_length: number, header_data: number, chunk_count: number, nth_chunk: number, property_data_length: number, property_data: number): void;

        /**
         * Put subscription reply.
         * @param buffer the buffer
         * @param device_id the device ID
         * @param version the version
         * @param source the source
         * @param destination the destination
         * @param request_id the request ID
         * @param header_data_length the header data length
         * @param header_data the header data
         * @param chunk_count the chunk count
         * @param nth_chunk the nth chunk
         * @param property_data_length the property data length
         * @param property_data the property data
         */
        put_subscription_reply(buffer: number, device_id: number, version: number, source: MUID, destination: MUID, request_id: number, header_data_length: number, header_data: number, chunk_count: number, nth_chunk: number, property_data_length: number, property_data: number): void;
    }


    /**
     * @gir-type Alias
     */
    type MidiClass = typeof Midi;

    /**
     * @gir-type Alias
     */
    type MidiFileClass = typeof MidiFile;

    /**
     * @gir-type Struct
     */
    class MidiFileTrack {
        static $gtype: GObject.GType<MidiFileTrack>;

        // Fields
        sequencer: GObject.Object;

        track_name: string;

        buffer: number;
    }


    /**
     * @gir-type Alias
     */
    type MidiParserClass = typeof MidiParser;

    /**
     * @gir-type Struct
     */
    class MidiSmfUtil {
        static $gtype: GObject.GType<MidiSmfUtil>;

        // Fields
        major: number;

        minor: number;

        // Constructors

        constructor(properties?: Partial<{
            major: number;
            minor: number;
        }>);

        // Static methods
        /**
         * Allocate MIDI util.
         */
        static alloc(): MidiSmfUtil;

        // Methods
        /**
         * Copy MIDI util.
         * @returns the newly allocated {@link AgsAudio.MidiSmfUtil}-struct
         */
        copy(): MidiSmfUtil;

        /**
         * Free MIDI util.
         */
        free(): void;

        /**
         * Get change parameter.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param channel the return location of channel
         * @param control the return location of the control
         * @param value the return location the value
         * @returns the number of bytes read.
         */
        get_change_parameter(buffer: number, delta_time: number, channel: number, control: number, value: number): number;

        /**
         * Get change pressure.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param channel the return location of channel
         * @param pressure the return location of the pressure
         * @returns the number of bytes read.
         */
        get_change_pressure(buffer: number, delta_time: number, channel: number, pressure: number): number;

        /**
         * Get change program.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param channel the return location of channel
         * @param program the return location of the program
         * @returns the number of bytes read.
         */
        get_change_program(buffer: number, delta_time: number, channel: number, program: number): number;

        /**
         * Get end of track.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @returns the number of bytes read.
         */
        get_end_of_track(buffer: number, delta_time: number): number;

        /**
         * Gets the midi header
         * @param buffer the MIDI buffer
         * @param offset start delta-time
         * @param format either 0, 1 or 2.
         * @param track_count the number of tracks
         * @param division timing division
         * @returns the number of bytes read.
         */
        get_header(buffer: number, offset: number, format: number, track_count: number, division: number): number;

        /**
         * Get signed 32 bit integer.
         * @param buffer the MIDI buffer
         * @param val return location of the integer
         */
        get_int16(buffer: number, val: number): void;

        /**
         * Get signed 24 bit integer.
         * @param buffer the MIDI buffer
         * @param val return location of the integer
         */
        get_int24(buffer: number, val: number): void;

        /**
         * Get signed 32 bit integer.
         * @param buffer the MIDI buffer
         * @param val return location of the integer
         */
        get_int32(buffer: number, val: number): void;

        /**
         * Get the key-off message from `buffer`.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param channel the return location of channel
         * @param key the return location of key
         * @param velocity the return location of velocity
         * @returns the number of bytes read.
         */
        get_key_off(buffer: number, delta_time: number, channel: number, key: number, velocity: number): number;

        /**
         * Get the key-on message from `buffer`.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param channel the return location of channel
         * @param key the return location of key
         * @param velocity the return location of velocity
         * @returns the number of bytes read.
         */
        get_key_on(buffer: number, delta_time: number, channel: number, key: number, velocity: number): number;

        /**
         * Get the key-pressure message from `buffer`.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param channel the return location of channel
         * @param key the return location of key
         * @param pressure the return location of pressure
         * @returns the number of bytes read.
         */
        get_key_pressure(buffer: number, delta_time: number, channel: number, key: number, pressure: number): number;

        /**
         * Get key signature.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param sf the return location of flats or sharps
         * @param mi the return location of minor or major
         * @returns the number of bytes read.
         */
        get_key_signature(buffer: number, delta_time: number, sf: number, mi: number): number;

        /**
         * Get pitch bend.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param channel the return location of channel
         * @param pitch the return location of the pitch
         * @param transmitter the return location the transmitter
         * @returns the number of bytes read.
         */
        get_pitch_bend(buffer: number, delta_time: number, channel: number, pitch: number, transmitter: number): number;

        /**
         * Get quarter frame.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param message_type the return location of the message type
         * @param values the return location of the values
         * @returns the number of bytes read.
         */
        get_quarter_frame(buffer: number, delta_time: number, message_type: number, values: number): number;

        /**
         * Get sequence number.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param sequence the return location of the sequence
         * @returns the number of bytes read.
         */
        get_sequence_number(buffer: number, delta_time: number, sequence: number): number;

        /**
         * Get sequencer meta event.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param len the return location of the length of data
         * @param id the return location of the manufacturer id
         * @param data the return location of the data
         * @returns the number of bytes read.
         */
        get_sequencer_meta_event(buffer: number, delta_time: number, len: number, id: number, data: number): number;

        /**
         * Get smtpe timestamp.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param rr the return location of frame rate
         * @param hr the return location of hour
         * @param mn the return location of minute
         * @param se the return location of second
         * @param fr the return location of frame number
         * @returns the number of bytes read.
         */
        get_smtpe(buffer: number, delta_time: number, rr: number, hr: number, mn: number, se: number, fr: number): number;

        /**
         * Get song position.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param song_position the return location of the song position
         * @returns the number of bytes read.
         */
        get_song_position(buffer: number, delta_time: number, song_position: number): number;

        /**
         * Get song select.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param song_select the return location of the song select
         * @returns the number of bytes read.
         */
        get_song_select(buffer: number, delta_time: number, song_select: number): number;

        /**
         * Get sysex.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param data the return location of data
         * @param length the return location of length
         * @returns the number of bytes read.
         */
        get_sysex(buffer: number, delta_time: number, data: number, length: number): number;

        /**
         * Get tempo.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param tempo the tempo
         * @returns the number of bytes read.
         */
        get_tempo(buffer: number, delta_time: number, tempo: number): number;

        /**
         * Get text event.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param text the return location of the text
         * @param length the return location of the length
         * @returns the number of bytes read.
         */
        get_text_event(buffer: number, delta_time: number, text: string, length: number): number;

        /**
         * Get time signature.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @param nn the return location of numerator
         * @param dd the return location of denominator
         * @param cc the return location of clocks
         * @param bb the return location of beats
         * @returns the number of bytes read.
         */
        get_time_signature(buffer: number, delta_time: number, nn: number, dd: number, cc: number, bb: number): number;

        /**
         * Get track
         * @param buffer the MIDI buffer
         * @param offset start delta-time
         * @returns the number of bytes read.
         */
        get_track(buffer: number, offset: number): number;

        /**
         * Get tune request.
         * @param buffer the MIDI buffer
         * @param delta_time the return location of timing information
         * @returns the number of bytes read.
         */
        get_tune_request(buffer: number, delta_time: number): number;

        /**
         * Get the variable lenght value from `buffer`.
         * @param buffer the MIDI buffer
         * @param varlength the return location
         * @returns the number of bytes read.
         */
        get_varlength(buffer: number, varlength: number): number;

        /**
         * Retrieve the size needed to store the variable length.
         * @param varlength the variable length
         * @returns the size needed to be allocated
         */
        get_varlength_size(varlength: number): number;

        /**
         * Put change parameter.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param channel channel
         * @param control the control
         * @param value the value
         */
        put_change_parameter(buffer: number, delta_time: number, channel: number, control: number, value: number): void;

        /**
         * Put change pressure.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param channel channel
         * @param pressure the pressure
         */
        put_change_pressure(buffer: number, delta_time: number, channel: number, pressure: number): void;

        /**
         * Put change program.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param channel channel
         * @param program the program
         */
        put_change_program(buffer: number, delta_time: number, channel: number, program: number): void;

        /**
         * Put end of track.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         */
        put_end_of_track(buffer: number, delta_time: number): void;

        /**
         * Puts the midi header.
         * @param buffer the MIDI buffer
         * @param offset start delta-time
         * @param format either 0, 1 or 2.
         * @param track_count the number of tracks
         * @param division timing division
         */
        put_header(buffer: number, offset: number, format: number, track_count: number, division: number): void;

        /**
         * Put signed 16 bit integer.
         * @param buffer the MIDI buffer
         * @param val the integer
         */
        put_int16(buffer: number, val: number): void;

        /**
         * Put signed 24 bit integer.
         * @param buffer the MIDI buffer
         * @param val the integer
         */
        put_int24(buffer: number, val: number): void;

        /**
         * Put signed 32 bit integer.
         * @param buffer the MIDI buffer
         * @param val the integer
         */
        put_int32(buffer: number, val: number): void;

        /**
         * Puts the given values to `buffer` with appropriate channel message.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param channel valid channels from 0-15
         * @param key valid keys to play 0-128
         * @param velocity the key dynamics
         */
        put_key_off(buffer: number, delta_time: number, channel: number, key: number, velocity: number): void;

        /**
         * Puts the given values to `buffer` with appropriate channel message.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param channel valid channels from 0-15
         * @param key valid keys to play 0-128
         * @param velocity the key dynamics
         */
        put_key_on(buffer: number, delta_time: number, channel: number, key: number, velocity: number): void;

        /**
         * Puts the given values to `buffer` with appropriate channel message.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param channel valid channels from 0-15
         * @param key valid keys to play 0-128
         * @param pressure the key dynamics
         */
        put_key_pressure(buffer: number, delta_time: number, channel: number, key: number, pressure: number): void;

        /**
         * Put key signature.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param sf flats or sharps
         * @param mi 1 equals minor or 0 means major
         */
        put_key_signature(buffer: number, delta_time: number, sf: number, mi: number): void;

        /**
         * Put pitch bend.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param channel channel
         * @param pitch the pitch
         * @param transmitter the transmitter
         */
        put_pitch_bend(buffer: number, delta_time: number, channel: number, pitch: number, transmitter: number): void;

        /**
         * Put quarter frame.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param message_type the message type
         * @param values the values
         */
        put_quarter_frame(buffer: number, delta_time: number, message_type: number, values: number): void;

        /**
         * Put sequence number.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param sequence the sequence
         */
        put_sequence_number(buffer: number, delta_time: number, sequence: number): void;

        /**
         * Put sequencer meta event.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param len the length of data
         * @param id the manufacturer id
         * @param data the data
         */
        put_sequencer_meta_event(buffer: number, delta_time: number, len: number, id: number, data: number): void;

        /**
         * Put smtpe timestamp.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param rr frame rate
         * @param hr hour
         * @param mn minute
         * @param se second
         * @param fr frame number
         */
        put_smtpe(buffer: number, delta_time: number, rr: number, hr: number, mn: number, se: number, fr: number): void;

        /**
         * Put song position.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param song_position the song position
         */
        put_song_position(buffer: number, delta_time: number, song_position: number): void;

        /**
         * Put song select.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param song_select the song select
         */
        put_song_select(buffer: number, delta_time: number, song_select: number): void;

        /**
         * Put sysex.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param data the data
         * @param length the data's length
         */
        put_sysex(buffer: number, delta_time: number, data: number, length: number): void;

        /**
         * Put tempo.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param tempo the tempo
         */
        put_tempo(buffer: number, delta_time: number, tempo: number): void;

        /**
         * Put text event.
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param text the text
         * @param length the length
         */
        put_text_event(buffer: number, delta_time: number, text: string, length: number): void;

        /**
         * Put time signature
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         * @param nn numerator
         * @param dd denominator
         * @param cc clocks
         * @param bb beats
         */
        put_time_signature(buffer: number, delta_time: number, nn: number, dd: number, cc: number, bb: number): void;

        /**
         * Put track.
         * @param buffer the MIDI buffer
         * @param offset start delta-time
         */
        put_track(buffer: number, offset: number): void;

        /**
         * Put tune request
         * @param buffer the MIDI buffer
         * @param delta_time timing information
         */
        put_tune_request(buffer: number, delta_time: number): void;

        /**
         * Put the variable lenght value to `buffer`.
         * @param buffer the MIDI buffer
         * @param varlength the value to put
         */
        put_varlength(buffer: number, varlength: number): void;

        /**
         * Seek MIDI messages from `buffer`
         * @param buffer the buffer to seek
         * @param message_count seek count messages
         * @param delta_time the return location of current delta time
         * @returns the buffer at offset at `message_count` ahead
         */
        seek_message(buffer: number, message_count: number, delta_time: number): number;
    }


    /**
     * @gir-type Struct
     */
    class MidiUmpUtil {
        static $gtype: GObject.GType<MidiUmpUtil>;

        // Fields
        major: number;

        minor: number;

        // Constructors

        constructor(properties?: Partial<{
            major: number;
            minor: number;
        }>);

        // Static methods
        /**
         * Allocate MIDI UMP util.
         */
        static alloc(): MidiUmpUtil;

        // Methods
        /**
         * Copy MIDI UMP util.
         * @returns the newly allocated {@link AgsAudio.MidiUmpUtil}-struct
         */
        copy(): MidiUmpUtil;

        /**
         * Free MIDI UMP util.
         */
        free(): void;

        /**
         * Get product instance ID notification.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_delta_clock_ticks_per_quarter_note(buffer: number): [number, number, string, unknown, number];

        /**
         * Get product instance ID notification.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_delta_clock_ticks_since_last_event(buffer: number): [number, number, string, unknown, number];

        /**
         * Get device identity notification.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_device_identity_notification(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * @param buffer 
         * @param extension_name 
         * @param extension_value 
         * @param extension_count 
         */
        get_end_of_clip(buffer: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): number;

        /**
         * Get endpoint discovery.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_endpoint_discovery(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * Get endpoint discovery.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_endpoint_info_notification(buffer: number): [number, number, number, boolean, number, boolean, boolean, boolean, boolean, string, unknown, number];

        /**
         * Get endpoint name notification.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_endpoint_name_notification(buffer: number): [number, string, string, unknown, number];

        /**
         * Get flex set chord name.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_flex_set_chord_name(buffer: number): [number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, string, unknown, number];

        /**
         * Get flex set time signature.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_flex_set_key_signature(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get flex set metronome.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_flex_set_metronome(buffer: number): [number, number, number, number, number, number, number, number, number, string, unknown, number];

        /**
         * Get flex set tempo.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_flex_set_tempo(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * Get flex set time signature.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_flex_set_time_signature(buffer: number): [number, number, number, number, number, number, string, unknown, number];

        /**
         * Get function block discovery.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_function_block_discovery(buffer: number): [number, number, number, string, unknown, number];

        /**
         * Get function block discovery.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_function_block_info_notification(buffer: number): [number, boolean, number, number, number, number, number, number, number, number, string, unknown, number];

        /**
         * Get function block name notification.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_function_block_name_notification(buffer: number): [number, number, string, string, unknown, number];

        /**
         * Get jitter reduction clock.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_jr_clock(buffer: number): [number, number, string, unknown, number];

        /**
         * Get jitter reduction timestamp.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_jr_timestamp(buffer: number): [number, number, string, unknown, number];

        /**
         * Get MIDI version 1.0 channel pressure.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi1_channel_pressure(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 1.0 control change.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi1_control_change(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 1.0 note off.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi1_note_off(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 1.0 note on.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi1_note_on(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 1.0 channel pitch.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi1_pitch_bend(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 1.0 polyphonic aftertouch.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi1_polyphonic_aftertouch(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 1.0 program change.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi1_program_change(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 assignable per note controller.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_assignable_per_note_controller(buffer: number): [number, number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 channel pressure.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_channel_pressure(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 control change.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_control_change(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 note off.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_note_off(buffer: number): [number, number, number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 note on.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_note_on(buffer: number): [number, number, number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 assignable per note controller.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_per_note_management(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 per note pitch bend.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_per_note_pitch_bend(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 pitch bend.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_pitch_bend(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 polyphonic aftertouch.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_polyphonic_aftertouch(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 program change.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_program_change(buffer: number): [number, number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 registered per note controller.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_registered_per_note_controller(buffer: number): [number, number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 RPN coarse tuning.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_rpn_coarse_tuning(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 RPN MPE MCM.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_rpn_mpe_mcm(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 RPN pitch bend range.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_rpn_pitch_bend_range(buffer: number): [number, number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 RPN tuning bank select.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_rpn_tuning_bank_select(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * Get MIDI version 2.0 RPN tuning program change.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_midi2_rpn_tuning_program_change(buffer: number): [number, number, number, number, string, unknown, number];

        /**
         * @param buffer 
         * @param extension_name 
         * @param extension_value 
         * @param extension_count 
         */
        get_noop(buffer: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): number;

        /**
         * Get product instance ID notification.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_product_instance_id_notification(buffer: number): [number, string, string, unknown, number];

        /**
         * @param buffer 
         * @param extension_name 
         * @param extension_value 
         * @param extension_count 
         */
        get_start_of_clip(buffer: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): number;

        /**
         * Get stream configuration notification.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_stream_configuration_notification(buffer: number): [number, number, boolean, boolean, string, unknown, number];

        /**
         * Get stream configuration request.
         * @param buffer the buffer
         * @returns the number of bytes read
         */
        get_stream_configuration_request(buffer: number): [number, number, boolean, boolean, string, unknown, number];

        /**
         * Test if is data message.
         * @param buffer the buffer
         * @returns `true` if is data message, otherwise `false`
         */
        is_data_message(buffer: number): boolean;

        /**
         * Test if is data message with SYSEX.
         * @param buffer the buffer
         * @returns `true` if is data message with SYSEX, otherwise `false`
         */
        is_data_message_with_sysex(buffer: number): boolean;

        /**
         * Test if is delta clock ticks per quarter note.
         * @param buffer the buffer
         * @returns `true` if is delta clock ticks per quarter note, otherwise `false`
         */
        is_delta_clock_ticks_per_quarter_note(buffer: number): boolean;

        /**
         * Test if is delta clock ticks since last event.
         * @param buffer the buffer
         * @returns `true` if is delta clock ticks since last event, otherwise `false`
         */
        is_delta_clock_ticks_since_last_event(buffer: number): boolean;

        /**
         * Test if is device identity notification.
         * @param buffer the buffer
         * @returns `true` if is device identity notification, otherwise `false`
         */
        is_device_identity_notification(buffer: number): boolean;

        /**
         * Test if is end of clip.
         * @param buffer the buffer
         * @returns `true` if is end of clip, otherwise `false`
         */
        is_end_of_clip(buffer: number): boolean;

        /**
         * Test if is endpoint discovery.
         * @param buffer the buffer
         * @returns `true` if is endpoint discovery, otherwise `false`
         */
        is_endpoint_discovery(buffer: number): boolean;

        /**
         * Test if is endpoint info notification.
         * @param buffer the buffer
         * @returns `true` if is endpoint info notification, otherwise `false`
         */
        is_endpoint_info_notification(buffer: number): boolean;

        /**
         * Test if is endpoint name notification.
         * @param buffer the buffer
         * @returns `true` if is endpoint name notification, otherwise `false`
         */
        is_endpoint_name_notification(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 flex set chord name.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 flex set chord name, otherwise `false`
         */
        is_flex_set_chord_name(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 flex set key signature.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 flex set key signature, otherwise `false`
         */
        is_flex_set_key_signature(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 flex set metronome.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 flex set metronome, otherwise `false`
         */
        is_flex_set_metronome(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 flex set tempo.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 flex set tempo, otherwise `false`
         */
        is_flex_set_tempo(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 flex set text.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 flex set text, otherwise `false`
         */
        is_flex_set_text(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 flex set time_signature.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 flex set time_signature, otherwise `false`
         */
        is_flex_set_time_signature(buffer: number): boolean;

        /**
         * Test if is function block discovery.
         * @param buffer the buffer
         * @returns `true` if is function block discovery, otherwise `false`
         */
        is_function_block_discovery(buffer: number): boolean;

        /**
         * Test if is function block info notification.
         * @param buffer the buffer
         * @returns `true` if is function block info notification, otherwise `false`
         */
        is_function_block_info_notification(buffer: number): boolean;

        /**
         * Test if is function block name notification.
         * @param buffer the buffer
         * @returns `true` if is function block name notification, otherwise `false`
         */
        is_function_block_name_notification(buffer: number): boolean;

        /**
         * Test if is JR clock.
         * @param buffer the buffer
         * @returns `true` if is JR clock, otherwise `false`
         */
        is_jr_clock(buffer: number): boolean;

        /**
         * Test if is JR timestamp.
         * @param buffer the buffer
         * @returns `true` if is JR timestamp, otherwise `false`
         */
        is_jr_timestamp(buffer: number): boolean;

        /**
         * Test if is MIDI version 1.0 channel pressure.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 1.0 channel pressure, otherwise `false`
         */
        is_midi1_channel_pressure(buffer: number): boolean;

        /**
         * Test if is MIDI version 1.0 channel voice.
         * @param buffer the buffer
         * @returns `true` if is delta clock ticks since last event, otherwise `false`
         */
        is_midi1_channel_voice(buffer: number): boolean;

        /**
         * Test if is MIDI version 1.0 control change.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 1.0 control change, otherwise `false`
         */
        is_midi1_control_change(buffer: number): boolean;

        /**
         * Test if is MIDI version 1.0 note off.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 1.0 note off, otherwise `false`
         */
        is_midi1_note_off(buffer: number): boolean;

        /**
         * Test if is MIDI version 1.0 note on.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 1.0 note on, otherwise `false`
         */
        is_midi1_note_on(buffer: number): boolean;

        /**
         * Test if is MIDI version 1.0 pitch bend.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 1.0 pitch bend, otherwise `false`
         */
        is_midi1_pitch_bend(buffer: number): boolean;

        /**
         * Test if is MIDI version 1.0 polyphonic aftertouch.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 1.0 polyphonic aftertouch, otherwise `false`
         */
        is_midi1_polyphonic_aftertouch(buffer: number): boolean;

        /**
         * Test if is MIDI version 1.0 program change.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 1.0 program change, otherwise `false`
         */
        is_midi1_program_change(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 assignable per note controller.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 assignable per note controller, otherwise `false`
         */
        is_midi2_assignable_per_note_controller(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 channel pressure.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 channel pressure, otherwise `false`
         */
        is_midi2_channel_pressure(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 channel voice.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 channel voice, otherwise `false`
         */
        is_midi2_channel_voice(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 control change.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 control change, otherwise `false`
         */
        is_midi2_control_change(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 note off.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 note off, otherwise `false`
         */
        is_midi2_note_off(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 note on.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 note on, otherwise `false`
         */
        is_midi2_note_on(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 per note management.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 per note management, otherwise `false`
         */
        is_midi2_per_note_management(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 pitch bend.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 pitch bend, otherwise `false`
         */
        is_midi2_per_note_pitch_bend(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 pitch bend.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 pitch bend, otherwise `false`
         */
        is_midi2_pitch_bend(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 polyphonic aftertouch.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 polyphonic aftertouch, otherwise `false`
         */
        is_midi2_polyphonic_aftertouch(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 program change.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 program change, otherwise `false`
         */
        is_midi2_program_change(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 registered per note controller.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 registered per note controller, otherwise `false`
         */
        is_midi2_registered_per_note_controller(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 RPN coarse tuning.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 RPN coarse tuning, otherwise `false`
         */
        is_midi2_rpn_coarse_tuning(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 RPN MPE MCM.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 RPN MPE MCM, otherwise `false`
         */
        is_midi2_rpn_mpe_mcm(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 RPN pitch bend range.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 RPN pitch bend range, otherwise `false`
         */
        is_midi2_rpn_pitch_bend_range(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 RPN tuning bank select.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 RPN tuning bank select, otherwise `false`
         */
        is_midi2_rpn_tuning_bank_select(buffer: number): boolean;

        /**
         * Test if is MIDI version 2.0 RPN tuning program change.
         * @param buffer the buffer
         * @returns `true` if is MIDI version 2.0 RPN tuning program change, otherwise `false`
         */
        is_midi2_rpn_tuning_program_change(buffer: number): boolean;

        /**
         * Test if is NOOP.
         * @param buffer the buffer
         * @returns `true` if is NOOP, otherwise `false`
         */
        is_noop(buffer: number): boolean;

        /**
         * @param buffer 
         */
        is_product_instance_id_notification(buffer: number): boolean;

        /**
         * Test if is start of clip.
         * @param buffer the buffer
         * @returns `true` if is start of clip, otherwise `false`
         */
        is_start_of_clip(buffer: number): boolean;

        /**
         * Test if is stream configuration notification.
         * @param buffer the buffer
         * @returns `true` if is stream configuration notification, otherwise `false`
         */
        is_stream_configuration_notification(buffer: number): boolean;

        /**
         * Test if is stream configuration request.
         * @param buffer the buffer
         * @returns `true` if is stream configuration request, otherwise `false`
         */
        is_stream_configuration_request(buffer: number): boolean;

        /**
         * Test if is stream message.
         * @param buffer the buffer
         * @returns `true` if is stream message, otherwise `false`
         */
        is_stream_message(buffer: number): boolean;

        /**
         * Test if is system common.
         * @param buffer the buffer
         * @returns `true` if is system common, otherwise `false`
         */
        is_system_common(buffer: number): boolean;

        /**
         * Put delta clock ticks per quarter note.
         * @param buffer the buffer
         * @param ticks_per_quarter_note_count ticks per quarter note count as a unsigned 16 bit integer
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_delta_clock_ticks_per_quarter_note(buffer: number, ticks_per_quarter_note_count: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put delta clock ticks per quarter note.
         * @param buffer the buffer
         * @param ticks_since_last_event_count ticks since last event count as a unsigned 16 bit integer
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_delta_clock_ticks_since_last_event(buffer: number, ticks_since_last_event_count: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put device identity notification.
         * @param buffer the buffer
         * @param device_manufacturer the device manufacturer
         * @param device_family the device family
         * @param device_family_model the device family model
         * @param software_revision the software revision
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_device_identity_notification(buffer: number, device_manufacturer: number, device_family: number, device_family_model: number, software_revision: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put end of clip.
         * @param buffer the buffer
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_end_of_clip(buffer: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put endpoint discovery.
         * @param buffer the buffer
         * @param major the major
         * @param minor the minor
         * @param filter the filter
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_endpoint_discovery(buffer: number, major: number, minor: number, filter: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * @param buffer 
         * @param major 
         * @param minor 
         * @param static_function_blocks 
         * @param function_block_count 
         * @param midi_v2_0_support 
         * @param midi_v1_0_support 
         * @param rx_jitter_reduction 
         * @param tx_jitter_reduction 
         * @param extension_name 
         * @param extension_value 
         * @param extension_count 
         */
        put_endpoint_info_notification(buffer: number, major: number, minor: number, static_function_blocks: boolean, function_block_count: number, midi_v2_0_support: boolean, midi_v1_0_support: boolean, rx_jitter_reduction: boolean, tx_jitter_reduction: boolean, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put endpoint name notification.
         * @param buffer the buffer
         * @param endpoint_name the endpoint name
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_endpoint_name_notification(buffer: number, endpoint_name: string, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 flex set chord name.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param t_sharp_flats the t sharp flats count
         * @param chord_tonic the chord tonic
         * @param chord_type the chord type
         * @param alter_1_type the alter 1 type
         * @param alter_1_degree the alter 1 degree
         * @param alter_2_type the alter 2 type
         * @param alter_2_degree the alter 2 degree
         * @param alter_3_type the alter 3 type
         * @param alter_3_degree the alter 3 degree
         * @param alter_4_type the alter 4 type
         * @param alter_4_degree the alter 4 degree
         * @param b_sharp_flats the b sharp flats count
         * @param bass_note the bass note
         * @param bass_chord_type the bass chord type
         * @param b_alter_1_type the alter 1 type
         * @param b_alter_1_degree the alter 1 degree
         * @param b_alter_2_type the alter 2 type
         * @param b_alter_2_degree the alter 2 degree
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_flex_set_chord_name(buffer: number, group: number, channel: number, t_sharp_flats: number, chord_tonic: number, chord_type: number, alter_1_type: number, alter_1_degree: number, alter_2_type: number, alter_2_degree: number, alter_3_type: number, alter_3_degree: number, alter_4_type: number, alter_4_degree: number, b_sharp_flats: number, bass_note: number, bass_chord_type: number, b_alter_1_type: number, b_alter_1_degree: number, b_alter_2_type: number, b_alter_2_degree: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 flex set key signature.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param sharp_flats the sharp flats count
         * @param tonic_note the tonic note
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_flex_set_key_signature(buffer: number, group: number, channel: number, sharp_flats: number, tonic_note: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 flex set metronome.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param clocks_per_primary_click the clocks per primary click
         * @param bar_accent_part_1 the bar accent part 1
         * @param bar_accent_part_2 the bar accent part 2
         * @param bar_accent_part_3 the bar accent part 3
         * @param subdivision_clicks_1 the subdivision clicks 1
         * @param subdivision_clicks_2 the subdivision clicks 2
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_flex_set_metronome(buffer: number, group: number, channel: number, clocks_per_primary_click: number, bar_accent_part_1: number, bar_accent_part_2: number, bar_accent_part_3: number, subdivision_clicks_1: number, subdivision_clicks_2: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 flex set tempo.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param ten_ns_per_quarter_note the 10 ns per quarter note
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_flex_set_tempo(buffer: number, group: number, channel: number, ten_ns_per_quarter_note: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 flex set time_signature.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param numerator the numerator
         * @param denominator the denominator
         * @param thirty_two_ticks the 1/32 ticks
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_flex_set_time_signature(buffer: number, group: number, channel: number, numerator: number, denominator: number, thirty_two_ticks: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put function block discovery.
         * @param buffer the buffer
         * @param function_block the function block through 0x00 - 0x1f or 0xff to request all
         * @param filter the filter
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_function_block_discovery(buffer: number, function_block: number, filter: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put function block info notification.
         * @param buffer the buffer
         * @param function_block_active the function block active
         * @param function_block the function block through 0x00 - 0x1f or 0xff to request all
         * @param direction the direction
         * @param midi1_port the MIDI v1.0 port
         * @param ui_hint the UI hint
         * @param first_group the first group
         * @param group_count the group count
         * @param message_version the message version
         * @param max_sysex8_stream_count the max SYSEX8 stream count
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_function_block_info_notification(buffer: number, function_block_active: boolean, function_block: number, direction: number, midi1_port: number, ui_hint: number, first_group: number, group_count: number, message_version: number, max_sysex8_stream_count: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put function block name notification.
         * @param buffer the buffer
         * @param function_block the function block
         * @param function_block_name the function block name
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_function_block_name_notification(buffer: number, function_block: number, function_block_name: string, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put JR clock.
         * @param buffer the buffer
         * @param sender_clock_time sender clock time as a unsigned 16 bit integer
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_jr_clock(buffer: number, sender_clock_time: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put JR timestamp.
         * @param buffer the buffer
         * @param sender_clock_timestamp sender clock timestamp as a unsigned 16 bit integer
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_jr_timestamp(buffer: number, sender_clock_timestamp: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 1.0 channel pressure.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param pressure the pressure
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi1_channel_pressure(buffer: number, group: number, channel: number, pressure: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 1.0 control change.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param index_key the index key
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi1_control_change(buffer: number, group: number, channel: number, index_key: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 1.0 note off.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param velocity the velocity
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi1_note_off(buffer: number, group: number, channel: number, key: number, velocity: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 1.0 note on.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param velocity the velocity
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi1_note_on(buffer: number, group: number, channel: number, key: number, velocity: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 1.0 pitch bend.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param pitch the pitch
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi1_pitch_bend(buffer: number, group: number, channel: number, pitch: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 1.0 polyphonic aftertouch.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi1_polyphonic_aftertouch(buffer: number, group: number, channel: number, key: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 1.0 program change.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param program the program
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi1_program_change(buffer: number, group: number, channel: number, program: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 assignable per note controller.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param data_index the data index
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_assignable_per_note_controller(buffer: number, group: number, channel: number, key: number, data_index: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 channel pressure.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_channel_pressure(buffer: number, group: number, channel: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 control change.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param index_key the index key
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_control_change(buffer: number, group: number, channel: number, index_key: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 note off.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param attribute_type the attribute type
         * @param velocity the velocity
         * @param attribute the attribute
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_note_off(buffer: number, group: number, channel: number, key: number, attribute_type: number, velocity: number, attribute: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 note on.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param attribute_type the attribute type
         * @param velocity the velocity
         * @param attribute the attribute
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_note_on(buffer: number, group: number, channel: number, key: number, attribute_type: number, velocity: number, attribute: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 registered per note management.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param options_flags the options flags
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_per_note_management(buffer: number, group: number, channel: number, key: number, options_flags: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 pitch bend.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the key
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_per_note_pitch_bend(buffer: number, group: number, channel: number, key: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 pitch bend.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_pitch_bend(buffer: number, group: number, channel: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 polyphonic aftertouch.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_polyphonic_aftertouch(buffer: number, group: number, channel: number, key: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 program change.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param option_flags the option flags
         * @param program the program
         * @param bank the bank
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_program_change(buffer: number, group: number, channel: number, option_flags: number, program: number, bank: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 registered per note controller.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param key the index key
         * @param data_index the data index
         * @param data the data
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_registered_per_note_controller(buffer: number, group: number, channel: number, key: number, data_index: number, data: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 RPN coarse tuning.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param coarse_tuning the coarse tuning
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_rpn_coarse_tuning(buffer: number, group: number, channel: number, coarse_tuning: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 RPN MPE MCM.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param channel_count the tuning bank number
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_rpn_mpe_mcm(buffer: number, group: number, channel: number, channel_count: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 RPN pitch bend range.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param semitones the semi tones
         * @param cents the cents
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_rpn_pitch_bend_range(buffer: number, group: number, channel: number, semitones: number, cents: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 RPN tuning bank select.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param tuning_bank_number the tuning bank number
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_rpn_tuning_bank_select(buffer: number, group: number, channel: number, tuning_bank_number: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put MIDI version 2.0 RPN tuning program change.
         * @param buffer the buffer
         * @param group the group
         * @param channel the channel number
         * @param tuning_program_number the tuning program number
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_midi2_rpn_tuning_program_change(buffer: number, group: number, channel: number, tuning_program_number: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put NOOP.
         * @param buffer the buffer
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_noop(buffer: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put product instance ID notification.
         * @param buffer the buffer
         * @param product_instance_id the product instance ID
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_product_instance_id_notification(buffer: number, product_instance_id: string, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put start of clip.
         * @param buffer the buffer
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_start_of_clip(buffer: number, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put product instance ID notification.
         * @param buffer the buffer
         * @param protocol the protocol
         * @param rx_jitter_reduction `true` if receiving jitter reduction, otherwise `false`
         * @param tx_jitter_reduction `true` if transfer jitter reduction, otherwise `false`
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_stream_configuration_notification(buffer: number, protocol: number, rx_jitter_reduction: boolean, tx_jitter_reduction: boolean, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;

        /**
         * Put product instance ID request.
         * @param buffer the buffer
         * @param protocol the protocol
         * @param rx_jitter_reduction `true` if receiving jitter reduction, otherwise `false`
         * @param tx_jitter_reduction `true` if transfer jitter reduction, otherwise `false`
         * @param extension_name the extension name string vector
         * @param extension_value the extension value array
         * @param extension_count the extension count
         */
        put_stream_configuration_request(buffer: number, protocol: number, rx_jitter_reduction: boolean, tx_jitter_reduction: boolean, extension_name: string, extension_value: (GObject.Value | any), extension_count: number): void;
    }


    /**
     * @gir-type Struct
     */
    class MidiUtil {
        static $gtype: GObject.GType<MidiUtil>;

        // Fields
        major: number;

        minor: number;

        // Constructors

        constructor(properties?: Partial<{
            major: number;
            minor: number;
        }>);

        // Static methods
        /**
         * Allocate MIDI util.
         */
        static alloc(): MidiUtil;

        // Methods
        /**
         * Copy MIDI util.
         * @returns the newly allocated {@link AgsAudio.MidiUtil}-struct
         */
        copy(): MidiUtil;

        /**
         * Delta time to note 256th offset.
         * @param delay_factor delay factor
         * @param division division
         * @param tempo tempo
         * @param bpm bpm
         * @param delta_time delta time
         * @returns the note 256th offset
         */
        delta_time_to_note_256th_offset(delay_factor: number, division: number, tempo: number, bpm: number, delta_time: number): number;

        /**
         * Delta time to offset.
         * @param delay_factor delay factor
         * @param division division
         * @param tempo tempo
         * @param bpm bpm
         * @param delta_time delta time
         * @returns the offset
         */
        delta_time_to_offset(delay_factor: number, division: number, tempo: number, bpm: number, delta_time: number): number;

        /**
         * Free MIDI util.
         */
        free(): void;

        /**
         * Get change parameter fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_change_parameter(buffer: number): [boolean, number, number, number];

        /**
         * Get change pressure fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_change_pressure(buffer: number): [boolean, number, number];

        /**
         * Get change program fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_change_program(buffer: number): [boolean, number, number];

        /**
         * Get key off fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_key_off(buffer: number): [boolean, number, number, number];

        /**
         * Get key on fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_key_on(buffer: number): [boolean, number, number, number];

        /**
         * Get key pressure fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_key_pressure(buffer: number): [boolean, number, number, number];

        /**
         * Get pitch bend fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_pitch_bend(buffer: number): [boolean, number, number, number];

        /**
         * Get quarter frame fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_quarter_frame(buffer: number): [boolean, number, number];

        /**
         * Get song position fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_song_position(buffer: number): [boolean, number];

        /**
         * Get song select fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_song_select(buffer: number): [boolean, number];

        /**
         * Get sysex fields of `buffer`.
         * @param buffer the MIDI buffer
         * @returns `true` if successful, otherwise `false`
         */
        get_sysex(buffer: number): [boolean, number, number];

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_change_parameter(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_change_pressure(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_change_program(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_key_off(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_key_on(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_key_pressure(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_meta_event(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_pitch_bend(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_quarter_frame(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_song_position(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_song_select(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_sysex(buffer: number): boolean;

        /**
         * @param buffer the MIDI buffer
         * @returns `true` on success, otherwise `false`
         */
        is_tune_request(buffer: number): boolean;

        /**
         * Note 256th offset to delta time.
         * @param delay_factor delay factor
         * @param division division
         * @param tempo tempo
         * @param bpm bpm
         * @param x_256th note 256th offset
         * @returns the delta time
         */
        note_256th_offset_to_delta_time(delay_factor: number, division: number, tempo: number, bpm: number, x_256th: number): number;

        /**
         * Offset to delta time.
         * @param delay_factor delay factor
         * @param division division
         * @param tempo tempo
         * @param bpm bpm
         * @param x note offset
         * @returns the delta time
         */
        offset_to_delta_time(delay_factor: number, division: number, tempo: number, bpm: number, x: number): number;

        /**
         * Convert real-time MIDI to SMF.
         * @param midi_buffer the MIDI buffer
         * @param buffer_length the buffer length
         * @param delta_time the delta time
         * @param smf_buffer_length the return location of resulting length
         * @returns the SMF buffer
         */
        to_smf(midi_buffer: number, buffer_length: number, delta_time: number, smf_buffer_length: number): number;
    }


    /**
     * @gir-type Alias
     */
    type MoveNoteClass = typeof MoveNote;

    /**
     * @gir-type Struct
     */
    class NoiseUtil {
        static $gtype: GObject.GType<NoiseUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        noise: any;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        mode: NoiseuUtilMode;

        volume: number;

        frequency: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.NoiseUtil}-struct.
         */
        static alloc(): NoiseUtil;

        // Methods
        /**
         * Compute noise of `noise_util`.
         */
        compute(): void;

        /**
         * Compute noise of `noise_util` of complex data.
         */
        compute_complex(): void;

        /**
         * Compute noise of `noise_util` of double precision floating point data.
         */
        compute_double(): void;

        /**
         * Compute noise of `noise_util` of floating point data.
         */
        compute_float(): void;

        /**
         * Compute noise of `noise_util` of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute noise of `noise_util` of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute noise of `noise_util` of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute noise of `noise_util` of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute noise of `noise_util` of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.NoiseUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `noise_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `noise_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `noise_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `noise_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `noise_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get frequency of `noise_util`.
         * @returns the frequency
         */
        get_frequency(): number;

        /**
         * Get note 256th mode of `noise_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `noise_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `noise_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get samplerate of `noise_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `noise_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `noise_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get volume of `noise_util`.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Set `buffer_length` of `noise_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `noise_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `noise_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `noise_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `noise_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `frequency` of `noise_util`.
         * @param frequency the frequency
         */
        set_frequency(frequency: number): void;

        /**
         * Set `note_256th_mode` of `noise_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `noise_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `noise_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `samplerate` of `noise_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `noise_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `noise_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `volume` of `noise_util`.
         * @param volume the volume
         */
        set_volume(volume: number): void;
    }


    /**
     * @gir-type Alias
     */
    type NotationClass = typeof Notation;

    /**
     * @gir-type Alias
     */
    type NoteClass = typeof Note;

    /**
     * @gir-type Alias
     */
    type OpenFileClass = typeof OpenFile;

    /**
     * @gir-type Alias
     */
    type OpenSFZFileClass = typeof OpenSFZFile;

    /**
     * @gir-type Alias
     */
    type OpenSf2InstrumentClass = typeof OpenSf2Instrument;

    /**
     * @gir-type Alias
     */
    type OpenSf2SampleClass = typeof OpenSf2Sample;

    /**
     * @gir-type Alias
     */
    type OpenSingleFileClass = typeof OpenSingleFile;

    /**
     * @gir-type Alias
     */
    type OpenWaveClass = typeof OpenWave;

    /**
     * @gir-type Alias
     */
    type OscActionControllerClass = typeof OscActionController;

    /**
     * @gir-type Struct
     */
    class OscBufferUtil {
        static $gtype: GObject.GType<OscBufferUtil>;

        // Fields
        major: number;

        minor: number;

        // Constructors

        constructor(properties?: Partial<{
            major: number;
            minor: number;
        }>);

        // Static methods
        /**
         * Allocate OSC util.
         */
        static alloc(): OscBufferUtil;

        // Methods
        /**
         * Copy OSC util.
         * @returns the newly allocated {@link AgsAudio.OscBufferUtil}-struct
         */
        copy(): OscBufferUtil;

        /**
         * Free OSC util.
         */
        free(): void;

        /**
         * Get `data` from `buffer`.
         * @param buffer the guchar buffer
         * @param data_size the return location of blob's data size
         * @param data the return location of blob's data
         */
        get_blob(buffer: number, data_size: number, data: number): void;

        /**
         * Get bundle from `buffer`.
         * @param buffer the guchar buffer
         * @param tv_secs return location of number of seconds since midnight on January 1, 1900
         * @param tv_fraction return location of fraction of seconds to a precision of about 200 picoseconds
         * @param immediately return location of if `true` apply immediately, otherwise `false` not immediately
         */
        get_bundle(buffer: number, tv_secs: number, tv_fraction: number, immediately: boolean): void;

        /**
         * Get `val` from `buffer`.
         * @param buffer the guchar buffer
         * @param val the return location of ASCII char
         */
        get_char(buffer: number, val: string): void;

        /**
         * Get `val` from `buffer`.
         * @param buffer the guchar buffer
         * @param val the return location of double floating point value
         */
        get_double(buffer: number, val: number): void;

        /**
         * Get `val` from `buffer`.
         * @param buffer the guchar buffer
         * @param val the float value
         */
        get_float(buffer: number, val: number): void;

        /**
         * Get `val` from `buffer`.
         * @param buffer the guchar buffer
         * @param val return location of the 32 bit integer value
         */
        get_int32(buffer: number, val: number): void;

        /**
         * Get `val` from `buffer`.
         * @param buffer the guchar buffer
         * @param val the return location of 64 bit integer
         */
        get_int64(buffer: number, val: (bigint | number)): void;

        /**
         * Get message heading from `buffer`. Note you have to get following arguments
         * yourself.
         * @param buffer the guchar buffer
         * @param address_pattern return location of the address pattern
         * @param type_tag return location of the type tag
         */
        get_message(buffer: number, address_pattern: string, type_tag: string): void;

        /**
         * Get MIDI from `buffer`.
         * @param buffer the guchar buffer
         * @param port the return location of port
         * @param status_byte the return location of status byte
         * @param data0 the return location of first piece of data
         * @param data1 the return location of second piece of data
         */
        get_midi(buffer: number, port: number, status_byte: number, data0: number, data1: number): void;

        /**
         * Get `packet` from `buffer`.
         * @param buffer the guchar buffer
         * @param packet_size the return location of packet's size
         * @param packet the return location of packet
         */
        get_packet(buffer: number, packet_size: number, packet: number): void;

        /**
         * Get RGBA color specified by `r`, `g`, `b` and `a` from `buffer`.
         * @param buffer the guchar buffer
         * @param r the return location of red value
         * @param g the return location of green value
         * @param b the return location of blue value
         * @param a the return location of alpha value
         */
        get_rgba(buffer: number, r: number, g: number, b: number, a: number): void;

        /**
         * Get `str` from `buffer`.
         * @param buffer the guchar buffer
         * @param str the return location of ASCII string
         * @param length the return location of bytes read
         */
        get_string(buffer: number, str: string, length: (bigint | number)): void;

        /**
         * Get timetag specified by `tv_secs`, `tv_fraction` and `immediately` from `buffer`.
         * @param buffer the guchar buffer
         * @param tv_secs return location of number of seconds since midnight on January 1, 1900
         * @param tv_fraction return location of fraction of seconds to a precision of about 200 picoseconds
         * @param immediately return location of if `true` apply immediately, otherwise `false` not immediately
         */
        get_timetag(buffer: number, tv_secs: number, tv_fraction: number, immediately: boolean): void;

        /**
         * Put `data` to `buffer`.
         * @param buffer the guchar buffer
         * @param data_size the blob's data size
         * @param data the blob's data
         */
        put_blob(buffer: number, data_size: number, data: number): void;

        /**
         * Put bundle to `buffer`.
         * @param buffer the guchar buffer
         * @param tv_secs number of seconds since midnight on January 1, 1900
         * @param tv_fraction fraction of seconds to a precision of about 200 picoseconds
         * @param immediately if `true` apply immediately, otherwise `false` not immediately
         */
        put_bundle(buffer: number, tv_secs: number, tv_fraction: number, immediately: boolean): void;

        /**
         * Put `val` to `buffer`.
         * @param buffer the guchar buffer
         * @param val the ASCII char
         */
        put_char(buffer: number, val: number): void;

        /**
         * Put `val` to `buffer`.
         * @param buffer the guchar buffer
         * @param val the double floating point value
         */
        put_double(buffer: number, val: number): void;

        /**
         * Put `val` to `buffer`.
         * @param buffer the guchar buffer
         * @param val the float value
         */
        put_float(buffer: number, val: number): void;

        /**
         * Put `val` in `buffer`.
         * @param buffer the guchar buffer
         * @param val the 32 bit integer value
         */
        put_int32(buffer: number, val: number): void;

        /**
         * Put `val` to `buffer`.
         * @param buffer the guchar buffer
         * @param val the 64 bit integer
         */
        put_int64(buffer: number, val: (bigint | number)): void;

        /**
         * Put message heading to `buffer`. Note you have to put following arguments
         * yourself.
         * @param buffer the guchar buffer
         * @param address_pattern the address pattern
         * @param type_tag the type tag
         */
        put_message(buffer: number, address_pattern: string, type_tag: string): void;

        /**
         * Put MIDI to `buffer`.
         * @param buffer the guchar buffer
         * @param port the port
         * @param status_byte the status byte
         * @param data0 the first piece of data
         * @param data1 the second piece of data
         */
        put_midi(buffer: number, port: number, status_byte: number, data0: number, data1: number): void;

        /**
         * Put `packet` to `buffer`.
         * @param buffer the guchar buffer
         * @param packet_size the packet's size
         * @param packet the packet
         */
        put_packet(buffer: number, packet_size: number, packet: number): void;

        /**
         * Put RGBA color specified by `r`, `g`, `b` and `a` to `buffer`.
         * @param buffer the guchar buffer
         * @param r the red value
         * @param g the green value
         * @param b the blue value
         * @param a the alpha value
         */
        put_rgba(buffer: number, r: number, g: number, b: number, a: number): void;

        /**
         * Put `str` to `buffer`.
         * @param buffer the guchar buffer
         * @param str the ASCII string
         * @param length the length to write or -1 to write until null byte
         */
        put_string(buffer: number, str: string, length: (bigint | number)): void;

        /**
         * Put timetag specified by `tv_secs`, `tv_fraction` and `immediately` to `buffer`.
         * @param buffer the guchar buffer
         * @param tv_secs number of seconds since midnight on January 1, 1900
         * @param tv_fraction fraction of seconds to a precision of about 200 picoseconds
         * @param immediately if `true` apply immediately, otherwise `false` not immediately
         */
        put_timetag(buffer: number, tv_secs: number, tv_fraction: number, immediately: boolean): void;
    }


    /**
     * @gir-type Struct
     */
    class OscBuilderBundle {
        static $gtype: GObject.GType<OscBuilderBundle>;

        // Fields
        offset: number;

        bundle_size: number;

        tv_secs: number;

        tv_fraction: number;

        immediately: boolean;

        message: any[];

        bundle: any[];

        // Static methods
        /**
         * Allocate {@link AgsAudio.OscBuilderBundle}-struct.
         * @param offset the current offset
         */
        static alloc(offset: (bigint | number)): (any | null);

        /**
         * Free `bundle`.
         * @param bundle the {@link AgsAudio.OscBuilderBundle}-struct
         */
        static free(bundle: (any | null)): void;
    }


    /**
     * @gir-type Alias
     */
    type OscBuilderClass = typeof OscBuilder;

    /**
     * @gir-type Struct
     */
    class OscBuilderMessage {
        static $gtype: GObject.GType<OscBuilderMessage>;

        // Fields
        offset: number;

        address_pattern: string;

        type_tag: string;

        data_allocated_length: number;

        data_length: number;

        data: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.OscBuilderMessage}-struct.
         * @param offset the current offset
         */
        static alloc(offset: (bigint | number)): (any | null);

        /**
         * Free `message`.
         * @param message the {@link AgsAudio.OscBuilderMessage}-struct
         */
        static free(message: (any | null)): void;
    }


    /**
     * @gir-type Struct
     */
    class OscBuilderPacket {
        static $gtype: GObject.GType<OscBuilderPacket>;

        // Fields
        offset: number;

        packet_size: number;

        builder: OscBuilder;

        message: any[];

        bundle: any[];

        // Static methods
        /**
         * Allocate {@link AgsAudio.OscBuilderPacket}-struct.
         * @param offset the current offset
         */
        static alloc(offset: (bigint | number)): (any | null);

        /**
         * Free `packet`.
         * @param packet the {@link AgsAudio.OscBuilderPacket}-struct
         */
        static free(packet: (any | null)): void;
    }


    /**
     * @gir-type Alias
     */
    type OscClientClass = typeof OscClient;

    /**
     * @gir-type Alias
     */
    type OscConfigControllerClass = typeof OscConfigController;

    /**
     * @gir-type Alias
     */
    type OscConnectionClass = typeof OscConnection;

    /**
     * @gir-type Alias
     */
    type OscControllerClass = typeof OscController;

    /**
     * @gir-type Alias
     */
    type OscExportControllerClass = typeof OscExportController;

    /**
     * @gir-type Alias
     */
    type OscFrontControllerClass = typeof OscFrontController;

    /**
     * @gir-type Alias
     */
    type OscInfoControllerClass = typeof OscInfoController;

    /**
     * @gir-type Alias
     */
    type OscMessageClass = typeof OscMessage;

    /**
     * @gir-type Alias
     */
    type OscMeterControllerClass = typeof OscMeterController;

    /**
     * @gir-type Struct
     */
    class OscMeterControllerMonitor {
        static $gtype: GObject.GType<OscMeterControllerMonitor>;

        // Fields
        ref_count: number;

        osc_connection: OscConnection;

        path: string;

        port: Port;

        // Static methods
        /**
         * Allocate {@link AgsAudio.OscMeterControllerMonitor}-struct.
         */
        static alloc(): (any | null);

        /**
         * Free `monitor`.
         * @param monitor the {@link AgsAudio.OscMeterControllerMonitor}-struct
         */
        static free(monitor: (any | null)): void;

        /**
         * Increase reference count of `monitor`.
         * @param monitor the {@link AgsAudio.OscMeterControllerMonitor}-struct
         */
        static ref(monitor: (any | null)): void;

        /**
         * Decrease reference count of `monitor`. If ref count is less or equal 0
         * `monitor` is freed.
         * @param monitor the {@link AgsAudio.OscMeterControllerMonitor}-struct
         */
        static unref(monitor: (any | null)): void;
    }


    /**
     * @gir-type Alias
     */
    type OscNodeControllerClass = typeof OscNodeController;

    /**
     * @gir-type Alias
     */
    type OscParserClass = typeof OscParser;

    /**
     * @gir-type Alias
     */
    type OscPluginControllerInterface = typeof OscPluginController;

    /**
     * @gir-type Alias
     */
    type OscRenewControllerClass = typeof OscRenewController;

    /**
     * @gir-type Alias
     */
    type OscResponseClass = typeof OscResponse;

    /**
     * @gir-type Alias
     */
    type OscServerClass = typeof OscServer;

    /**
     * @gir-type Alias
     */
    type OscStatusControllerClass = typeof OscStatusController;

    /**
     * @gir-type Struct
     */
    class OscUtil {
        static $gtype: GObject.GType<OscUtil>;

        // Fields
        major: number;

        minor: number;

        // Constructors

        constructor(properties?: Partial<{
            major: number;
            minor: number;
        }>);

        // Static methods
        /**
         * Allocate OSC util.
         */
        static alloc(): OscUtil;

        /**
         * Get meta data.
         * @param uri the URI
         * @param meta_data_length the return location of meta data's length
         */
        static meta_data(uri: string, meta_data_length: number): number;

        /**
         * Get MIME header.
         * @param uri the URI
         * @param mime_header_length the return location of MIME header's length
         */
        static mime_header(uri: string, mime_header_length: number): number;

        /**
         * Decode `slip_buffer` from SLIP encoded format.
         * See SLIP (RFC1055).
         * @param slip_buffer the SLIP encoded OSC buffer
         * @param slip_buffer_length the buffer length of SLIP encoded OSC buffer
         * @param returned_buffer_length the returned buffer's length
         */
        static slip_decode(slip_buffer: number, slip_buffer_length: number, returned_buffer_length: number): number;

        /**
         * Encode `osc_buffer` to be suitable for network transmission.
         * See SLIP (RFC1055).
         * @param osc_buffer the OSC buffer
         * @param buffer_length the OSC buffer's length
         * @param returned_buffer_length the returned buffer's length
         */
        static slip_encode(osc_buffer: number, buffer_length: number, returned_buffer_length: number): number;

        /**
         * Get current time.
         * @param tv_sec the return location of number of seconds since midnight on January 1, 1900
         * @param tv_fraction the return location of fraction of seconds to a precision of about 200 picoseconds
         */
        static timetag_now(tv_sec: number, tv_fraction: number): void;

        /**
         * Count types in `type_tag`.
         * @param type_tag the type tag string
         */
        static type_tag_string_count_type(type_tag: string): number;

        // Methods
        /**
         * Copy OSC util.
         * @returns the newly allocated {@link AgsAudio.OscUtil}-struct
         */
        copy(): OscUtil;

        /**
         * Free OSC util.
         */
        free(): void;
    }


    /**
     * @gir-type Alias
     */
    type OscWebsocketConnectionClass = typeof OscWebsocketConnection;

    /**
     * @gir-type Alias
     */
    type OscXmlrpcControllerClass = typeof OscXmlrpcController;

    /**
     * @gir-type Alias
     */
    type OscXmlrpcMessageClass = typeof OscXmlrpcMessage;

    /**
     * @gir-type Alias
     */
    type OscXmlrpcServerClass = typeof OscXmlrpcServer;

    /**
     * @gir-type Alias
     */
    type OssDevinClass = typeof OssDevin;

    /**
     * @gir-type Alias
     */
    type OssDevoutClass = typeof OssDevout;

    /**
     * @gir-type Alias
     */
    type OssMidiinClass = typeof OssMidiin;

    /**
     * @gir-type Alias
     */
    type OutputClass = typeof Output;

    /**
     * @gir-type Alias
     */
    type PatternClass = typeof Pattern;

    /**
     * @gir-type Struct
     */
    class PeakUtil {
        static $gtype: GObject.GType<PeakUtil>;

        // Fields
        source: any;

        source_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        harmonic_rate: number;

        pressure_factor: number;

        peak: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.PeakUtil}-struct
         */
        static alloc(): PeakUtil;

        // Methods
        /**
         * Compute peak.
         */
        compute(): void;

        /**
         * Compute peak of complex floating point data.
         */
        compute_complex(): void;

        /**
         * Compute peak of double floating point data.
         */
        compute_double(): void;

        /**
         * Compute peak of floating point data.
         */
        compute_float(): void;

        /**
         * Compute peak of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute peak of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute peak of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute peak of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute peak of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.PeakUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `peak_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get format of `peak_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get harmonic rate of `peak_util`.
         * @returns the harmonic rate
         */
        get_harmonic_rate(): number;

        /**
         * Get peak of `peak_util`.
         * @returns the peak
         */
        get_peak(): number;

        /**
         * Get pressure factor of `peak_util`.
         * @returns the pressure factor
         */
        get_pressure_factor(): number;

        /**
         * Get samplerate of `peak_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `peak_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `peak_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Set `buffer_length` of `peak_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `format` of `peak_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `harmonic_rate` of `peak_util`.
         * @param harmonic_rate the harmonic rate
         */
        set_harmonic_rate(harmonic_rate: number): void;

        /**
         * Set `peak` of `peak_util`.
         * @param peak the peak
         */
        set_peak(peak: number): void;

        /**
         * Set `pressure_factor` of `peak_util`.
         * @param pressure_factor the pressure factor
         */
        set_pressure_factor(pressure_factor: number): void;

        /**
         * Set `samplerate` of `peak_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `peak_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `peak_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;
    }


    /**
     * @gir-type Struct
     */
    class PhaseShiftUtil {
        static $gtype: GObject.GType<PhaseShiftUtil>;

        // Fields
        source: any;

        source_stride: number;

        destination: any;

        destination_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        frequency: number;

        amount: number;

        phase: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.PhaseShiftUtil}-struct
         */
        static alloc(): PhaseShiftUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.PhaseShiftUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get amount of `phase_shift_util`.
         * @returns the amount
         */
        get_amount(): number;

        /**
         * Get buffer length of `phase_shift_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `phase_shift_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `phase_shift_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `phase_shift_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frequency of `phase_shift_util`.
         * @returns the frequency
         */
        get_frequency(): number;

        /**
         * Get phase of `phase_shift_util`.
         * @returns the phase
         */
        get_phase(): number;

        /**
         * Get source buffer of `phase_shift_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `phase_shift_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Process phase shift of `phase_shift_util`.
         */
        process(): void;

        /**
         * Process phase shift of `phase_shift_util` of complex floating point data.
         */
        process_complex(): void;

        /**
         * Process phase shift of `phase_shift_util` of double precision floating point data.
         */
        process_double(): void;

        /**
         * Process phase shift of `phase_shift_util` of single precision floating point data.
         */
        process_float(): void;

        /**
         * Process phase shift of `phase_shift_util` of signed 16 bit data.
         */
        process_s16(): void;

        /**
         * Process phase shift of `phase_shift_util` of signed 24 bit data.
         */
        process_s24(): void;

        /**
         * Process phase shift of `phase_shift_util` of signed 32 bit data.
         */
        process_s32(): void;

        /**
         * Process phase shift of `phase_shift_util` of signed 64 bit data.
         */
        process_s64(): void;

        /**
         * Process phase shift of `phase_shift_util` of signed 8 bit data.
         */
        process_s8(): void;

        /**
         * Set `amount` of `phase_shift_util`.
         * @param amount the amount
         */
        set_amount(amount: number): void;

        /**
         * Set `buffer_length` of `phase_shift_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `phase_shift_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `phase_shift_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `phase_shift_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frequency` of `phase_shift_util`.
         * @param frequency the frequency
         */
        set_frequency(frequency: number): void;

        /**
         * Set `phase` of `phase_shift_util`.
         * @param phase the phase
         */
        set_phase(phase: number): void;

        /**
         * Set `source` buffer of `phase_shift_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `phase_shift_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;
    }


    /**
     * @gir-type Alias
     */
    type PlaybackClass = typeof Playback;

    /**
     * @gir-type Alias
     */
    type PlaybackDomainClass = typeof PlaybackDomain;

    /**
     * @gir-type Alias
     */
    type PluginPortClass = typeof PluginPort;

    /**
     * @gir-type Alias
     */
    type PortClass = typeof Port;

    /**
     * @gir-type Struct
     */
    abstract class PortUtil {
        static $gtype: GObject.GType<PortUtil>;
    }


    /**
     * @gir-type Alias
     */
    type PresetClass = typeof Preset;

    /**
     * @gir-type Alias
     */
    type ProgramClass = typeof Program;

    /**
     * @gir-type Alias
     */
    type ProgramControlNameKeyManagerClass = typeof ProgramControlNameKeyManager;

    /**
     * @gir-type Alias
     */
    type PulseClientClass = typeof PulseClient;

    /**
     * @gir-type Alias
     */
    type PulseDevinClass = typeof PulseDevin;

    /**
     * @gir-type Alias
     */
    type PulseDevoutClass = typeof PulseDevout;

    /**
     * @gir-type Alias
     */
    type PulsePortClass = typeof PulsePort;

    /**
     * @gir-type Alias
     */
    type PulseServerClass = typeof PulseServer;

    /**
     * @gir-type Alias
     */
    type RecallAudioClass = typeof RecallAudio;

    /**
     * @gir-type Alias
     */
    type RecallAudioRunClass = typeof RecallAudioRun;

    /**
     * @gir-type Alias
     */
    type RecallAudioSignalClass = typeof RecallAudioSignal;

    /**
     * @gir-type Alias
     */
    type RecallChannelClass = typeof RecallChannel;

    /**
     * @gir-type Alias
     */
    type RecallChannelRunClass = typeof RecallChannelRun;

    /**
     * @gir-type Alias
     */
    type RecallClass = typeof Recall;

    /**
     * @gir-type Alias
     */
    type RecallContainerClass = typeof RecallContainer;

    /**
     * @gir-type Alias
     */
    type RecallDependencyClass = typeof RecallDependency;

    /**
     * A {@link AgsAudio.RecallHandler}-struct acts as a callback definition
     * @gir-type Struct
     */
    class RecallHandler {
        static $gtype: GObject.GType<RecallHandler>;

        // Fields
        signal_name: string;

        callback: GObject.Callback;

        data: GObject.Object;

        // Static methods
        /**
         * Allocates {@link AgsAudio.RecallHandler}-struct.
         * @param signal_name signal's name to connect
         * @param callback the {@link GObject.Callback} function
         * @param data the data to pass the callback
         */
        static alloc(signal_name: string, callback: GObject.Callback, data: GObject.Object): (any | null);

        /**
         * Free `recall_hanlder`.
         * @param recall_handler the {@link AgsAudio.RecallHandler}-struct
         */
        static free(recall_handler: (any | null)): void;
    }


    /**
     * @gir-type Alias
     */
    type RecallIDClass = typeof RecallID;

    /**
     * @gir-type Alias
     */
    type RecallRecyclingClass = typeof RecallRecycling;

    /**
     * @gir-type Alias
     */
    type RecyclingClass = typeof Recycling;

    /**
     * @gir-type Alias
     */
    type RecyclingContextClass = typeof RecyclingContext;

    /**
     * @gir-type Alias
     */
    type RemoveAudioClass = typeof RemoveAudio;

    /**
     * @gir-type Alias
     */
    type RemoveAudioSignalClass = typeof RemoveAudioSignal;

    /**
     * @gir-type Alias
     */
    type RemoveNoteClass = typeof RemoveNote;

    /**
     * @gir-type Alias
     */
    type RemoveSoundcardClass = typeof RemoveSoundcard;

    /**
     * @gir-type Struct
     */
    class ResampleUtil {
        static $gtype: GObject.GType<ResampleUtil>;

        // Fields
        destination: any;

        destination_stride: number;

        source: any;

        source_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        target_samplerate: number;

        src_ratio: number;

        saved_frames: number;

        input_frames_used: number;

        output_frames_gen: number;

        end_of_input: number;

        input_frames: number;

        data_in: any;

        output_frames: number;

        data_out: any;

        increment: number;

        bypass_cache: boolean;

        in_count: number;

        in_used: number;

        out_count: number;

        out_gen: number;

        coeff_half_len: number;

        index_inc: number;

        input_index: number;

        b_current: number;

        b_end: number;

        b_real_end: number;

        b_len: number;

        coeffs: number;

        left_calc: number;

        right_calc: number;

        last_ratio: number;

        last_position: number;

        buffer: any;

        // Static methods
        /**
         * Allocate {@link AgsAudio.ResampleUtil}-struct
         */
        static alloc(): ResampleUtil;

        // Methods
        /**
         * Compute resample.
         */
        compute(): void;

        /**
         * Compute resample of complex floating point data.
         */
        compute_complex(): void;

        /**
         * Compute resample of double floating point data.
         */
        compute_double(): void;

        /**
         * Compute resample of floating point data.
         */
        compute_float(): void;

        /**
         * Compute resample of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute resample of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute resample of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute resample of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute resample of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.ResampleUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `resample_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `resample_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `resample_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `resample_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get samplerate of `resample_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `resample_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `resample_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get target samplerate of `resample_util`.
         * @returns the  samplerate
         */
        get_target_samplerate(): number;

        /**
         * Init {@link AgsAudio.ResampleUtil}-struct
         */
        init(): void;

        /**
         * Set `buffer_length` of `resample_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `resample_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `resample_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `resample_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `samplerate` of `resample_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `resample_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `resample_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `target_samplerate` of `resample_util`.
         * @param target_samplerate the target samplerate
         */
        set_target_samplerate(target_samplerate: number): void;
    }


    /**
     * @gir-type Alias
     */
    type ResetFxAnalyseClass = typeof ResetFxAnalyse;

    /**
     * @gir-type Alias
     */
    type ResetFxPeakClass = typeof ResetFxPeak;

    /**
     * @gir-type Alias
     */
    type ResetNoteClass = typeof ResetNote;

    /**
     * @gir-type Alias
     */
    type ResetRecallStagingClass = typeof ResetRecallStaging;

    /**
     * @gir-type Alias
     */
    type ResizeAudioClass = typeof ResizeAudio;

    /**
     * @gir-type Alias
     */
    type SF2LoaderClass = typeof SF2Loader;

    /**
     * @gir-type Alias
     */
    type SF2MidiLocaleLoaderClass = typeof SF2MidiLocaleLoader;

    /**
     * @gir-type Alias
     */
    type SF2SynthGeneratorClass = typeof SF2SynthGenerator;

    /**
     * @gir-type Struct
     */
    class SF2SynthUtil {
        static $gtype: GObject.GType<SF2SynthUtil>;

        // Fields
        flags: number;

        sf2_file: AudioContainer;

        sf2_sample_count: number;

        sf2_note_range: number;

        sf2_loop_mode: number[];

        sf2_loop_start: number[];

        sf2_loop_end: number[];

        sf2_orig_buffer_length: number[];

        sf2_orig_buffer: any;

        sf2_resampled_buffer_length: number[];

        sf2_resampled_buffer: any;

        source: any;

        source_stride: number;

        sample_buffer: any;

        im_buffer: any;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        orig_samplerate: number;

        preset: string;

        instrument: string;

        sample: string;

        bank: number;

        program: number;

        midi_key: number;

        note: number;

        volume: number;

        frame_count: number;

        offset: number;

        loop_mode: SF2SynthUtilLoopMode;

        loop_start: number;

        loop_end: number;

        pitch_type: GObject.GType;

        pitch_util: any;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.SF2SynthUtil}-struct.
         */
        static alloc(): SF2SynthUtil;

        /**
         * Find sample near MIDI key.
         * @param ipatch the {@link AgsAudio.Ipatch}
         * @param bank the bank
         * @param program the program
         * @param midi_key the MIDI key
         */
        static midi_locale_find_sample_near_midi_key(ipatch: Ipatch, bank: number, program: number, midi_key: number): [IpatchSample, string, string, string];

        // Methods
        /**
         * Copy {@link AgsAudio.SF2SynthUtil}-struct.
         * @returns the newly allocated {@link AgsAudio.SF2SynthUtil}-struct
         */
        boxed_copy(): (any | null);

        /**
         * Compute Soundfont2 synth.
         */
        compute(): void;

        /**
         * Compute Soundfont2 synth of complex data.
         */
        compute_complex(): void;

        /**
         * Compute Soundfont2 synth of double precision floating point data.
         */
        compute_double(): void;

        /**
         * Compute Soundfont2 synth of floating point data.
         */
        compute_float(): void;

        /**
         * Compute Soundfont2 synth of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute Soundfont2 synth of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute Soundfont2 synth of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute Soundfont2 synth of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute Soundfont2 synth of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Free {@link AgsAudio.SF2SynthUtil}-struct.
         */
        free(): void;

        /**
         * Get bank of `sf2_synth_util`.
         * @returns the bank
         */
        get_bank(): number;

        /**
         * Get buffer length of `sf2_synth_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get format of `sf2_synth_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `sf2_synth_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get instrument of `sf2_synth_util`.
         * @returns the instrument
         */
        get_instrument(): string;

        /**
         * Get loop end of `sf2_synth_util`.
         * @returns the loop end
         */
        get_loop_end(): number;

        /**
         * Get loop mode of `sf2_synth_util`.
         * @returns the loop mode
         */
        get_loop_mode(): SF2SynthUtilLoopMode;

        /**
         * Get loop start of `sf2_synth_util`.
         * @returns the loop start
         */
        get_loop_start(): number;

        /**
         * Get midi_key of `sf2_synth_util`.
         * @returns the midi key
         */
        get_midi_key(): number;

        /**
         * Get note of `sf2_synth_util`.
         * @returns the note
         */
        get_note(): number;

        /**
         * Get note 256th mode of `sf2_synth_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `sf2_synth_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `sf2_synth_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get pitch type of `sf2_synth_util`.
         * @returns the pitch type
         */
        get_pitch_type(): GObject.GType;

        /**
         * Get pitch util of `sf2_synth_util`.
         * @returns the pitch util
         */
        get_pitch_util(): (any | null);

        /**
         * Get preset of `sf2_synth_util`.
         * @returns the preset
         */
        get_preset(): string;

        /**
         * Get program of `sf2_synth_util`.
         * @returns the program
         */
        get_program(): number;

        /**
         * Get resample util of `sf2_synth_util`.
         * @returns the resample util
         */
        get_resample_util(): ResampleUtil;

        /**
         * Get sample of `sf2_synth_util`.
         * @returns the sample
         */
        get_sample(): string;

        /**
         * Get samplerate of `sf2_synth_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get Soundfont2 file of `sf2_synth_util`.
         * @returns the Soundfont2 file
         */
        get_sf2_file(): AudioContainer;

        /**
         * Get source buffer of `sf2_synth_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `sf2_synth_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get volume of `sf2_synth_util`.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Load instrument of `sf2_synth_util`.
         * @param preset the preset
         * @param instrument the instrument
         */
        load_instrument(preset: string, instrument: string): void;

        /**
         * Load midi locale of `sf2_synth_util`.
         * @param bank the bank
         * @param program the program
         */
        load_midi_locale(bank: number, program: number): void;

        /**
         * Set `bank` of `sf2_synth_util`.
         * @param bank the bank
         */
        set_bank(bank: number): void;

        /**
         * Set `buffer_length` of `sf2_synth_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `format` of `sf2_synth_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `sf2_synth_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `instrument` of `sf2_synth_util`.
         * @param instrument the instrument
         */
        set_instrument(instrument: string): void;

        /**
         * Set `loop_end` of `sf2_synth_util`.
         * @param loop_end the loop end
         */
        set_loop_end(loop_end: number): void;

        /**
         * Set `loop_mode` of `sf2_synth_util`.
         * @param loop_mode the loop mode
         */
        set_loop_mode(loop_mode: SF2SynthUtilLoopMode): void;

        /**
         * Set `loop_start` of `sf2_synth_util`.
         * @param loop_start the loop start
         */
        set_loop_start(loop_start: number): void;

        /**
         * Set `midi_key` of `sf2_synth_util`.
         * @param midi_key the midi key
         */
        set_midi_key(midi_key: number): void;

        /**
         * Set `note` of `sf2_synth_util`.
         * @param note the note
         */
        set_note(note: number): void;

        /**
         * Set `note_256th_mode` of `sf2_synth_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `sf2_synth_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `sf2_synth_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `pitch_type` of `sf2_synth_util`.
         * @param pitch_type the pitch type
         */
        set_pitch_type(pitch_type: GObject.GType): void;

        /**
         * Set `pitch_util` of `sf2_synth_util`.
         * @param pitch_util the pitch util
         */
        set_pitch_util(pitch_util: (any | null)): void;

        /**
         * Set `preset` of `sf2_synth_util`.
         * @param preset the preset
         */
        set_preset(preset: string): void;

        /**
         * Set `program` of `sf2_synth_util`.
         * @param program the program
         */
        set_program(program: number): void;

        /**
         * Set `resample_util` of `sf2_synth_util`.
         * @param resample_util the resample util
         */
        set_resample_util(resample_util: ResampleUtil): void;

        /**
         * Set `sample` of `sf2_synth_util`.
         * @param sample the sample
         */
        set_sample(sample: string): void;

        /**
         * Set `samplerate` of `sf2_synth_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set Soundfont2 file of `sf2_synth_util`.
         * @param sf2_file the Soundfont2 file
         */
        set_sf2_file(sf2_file: AudioContainer): void;

        /**
         * Set `source` buffer of `sf2_synth_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `sf2_synth_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `volume` of `sf2_synth_util`.
         * @param volume the volume
         */
        set_volume(volume: number): void;
    }


    /**
     * @gir-type Alias
     */
    type SFZFileClass = typeof SFZFile;

    /**
     * @gir-type Alias
     */
    type SFZGroupClass = typeof SFZGroup;

    /**
     * @gir-type Alias
     */
    type SFZInstrumentLoaderClass = typeof SFZInstrumentLoader;

    /**
     * @gir-type Alias
     */
    type SFZLoaderClass = typeof SFZLoader;

    /**
     * @gir-type Alias
     */
    type SFZRegionClass = typeof SFZRegion;

    /**
     * @gir-type Alias
     */
    type SFZSampleClass = typeof SFZSample;

    /**
     * @gir-type Alias
     */
    type SFZSynthGeneratorClass = typeof SFZSynthGenerator;

    /**
     * @gir-type Struct
     */
    class SFZSynthUtil {
        static $gtype: GObject.GType<SFZSynthUtil>;

        // Fields
        flags: number;

        sfz_file: AudioContainer;

        sfz_sample_count: number;

        sfz_sample_arr: SFZSample;

        sfz_note_range: number;

        sfz_loop_mode: number[];

        sfz_loop_start: number[];

        sfz_loop_end: number[];

        sfz_orig_buffer_length: number[];

        sfz_orig_buffer: any;

        sfz_resampled_buffer_length: number[];

        sfz_resampled_buffer: any;

        source: any;

        source_stride: number;

        sample_buffer: any;

        im_buffer: any;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        orig_samplerate: number;

        midi_key: number;

        note: number;

        volume: number;

        frame_count: number;

        offset: number;

        loop_mode: number;

        loop_start: number;

        loop_end: number;

        pitch_type: GObject.GType;

        pitch_util: any;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.SFZSynthUtil}-struct.
         */
        static alloc(): SFZSynthUtil;

        // Methods
        /**
         * Copy {@link AgsAudio.SFZSynthUtil}-struct.
         * @returns the newly allocated {@link AgsAudio.SFZSynthUtil}-struct
         */
        boxed_copy(): (any | null);

        /**
         * Compute SFZ synth.
         */
        compute(): void;

        /**
         * Compute SFZ synth of complex data.
         */
        compute_complex(): void;

        /**
         * Compute SFZ synth of double precision floating point data.
         */
        compute_double(): void;

        /**
         * Compute SFZ synth of floating point data.
         */
        compute_float(): void;

        /**
         * Compute SFZ synth of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute SFZ synth of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute SFZ synth of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute SFZ synth of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute SFZ synth of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Free {@link AgsAudio.SFZSynthUtil}-struct.
         */
        free(): void;

        /**
         * Get buffer length of `sfz_synth_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get format of `sfz_synth_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `sfz_synth_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get loop end of `sfz_synth_util`.
         * @returns the loop end
         */
        get_loop_end(): number;

        /**
         * Get loop mode of `sfz_synth_util`.
         * @returns the loop mode
         */
        get_loop_mode(): number;

        /**
         * Get loop start of `sfz_synth_util`.
         * @returns the loop start
         */
        get_loop_start(): number;

        /**
         * Get midi_key of `sfz_synth_util`.
         * @returns the midi key
         */
        get_midi_key(): number;

        /**
         * Get note of `sfz_synth_util`.
         * @returns the note
         */
        get_note(): number;

        /**
         * Get note 256th mode of `sfz_synth_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `sfz_synth_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `sfz_synth_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get pitch type of `sfz_synth_util`.
         * @returns the pitch type
         */
        get_pitch_type(): GObject.GType;

        /**
         * Get pitch util of `sfz_synth_util`.
         * @returns the pitch util
         */
        get_pitch_util(): (any | null);

        /**
         * Get samplerate of `sfz_synth_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `sfz_synth_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `sfz_synth_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get volume of `sfz_synth_util`.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Load instrument of `sfz_synth_util`.
         */
        load_instrument(): void;

        /**
         * Set `buffer_length` of `sfz_synth_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `format` of `sfz_synth_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `sfz_synth_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `loop_end` of `sfz_synth_util`.
         * @param loop_end the loop end
         */
        set_loop_end(loop_end: number): void;

        /**
         * Set `loop_mode` of `sfz_synth_util`.
         * @param loop_mode the loop mode
         */
        set_loop_mode(loop_mode: number): void;

        /**
         * Set `loop_start` of `sfz_synth_util`.
         * @param loop_start the loop start
         */
        set_loop_start(loop_start: number): void;

        /**
         * Set `midi_key` of `sfz_synth_util`.
         * @param midi_key the midi key
         */
        set_midi_key(midi_key: number): void;

        /**
         * Set `note` of `sfz_synth_util`.
         * @param note the note
         */
        set_note(note: number): void;

        /**
         * Set `note_256th_mode` of `sfz_synth_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `sfz_synth_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `sfz_synth_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `pitch_type` of `sfz_synth_util`.
         * @param pitch_type the pitch type
         */
        set_pitch_type(pitch_type: GObject.GType): void;

        /**
         * Set `pitch_util` of `sfz_synth_util`.
         * @param pitch_util the pitch util
         */
        set_pitch_util(pitch_util: (any | null)): void;

        /**
         * Set `samplerate` of `sfz_synth_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set SFZ file of `sfz_synth_util`.
         * @param sfz_file the SFZ file
         */
        set_sfz_file(sfz_file: AudioContainer): void;

        /**
         * Set `source` buffer of `sfz_synth_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `sfz_synth_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `volume` of `sfz_synth_util`.
         * @param volume the volume
         */
        set_volume(volume: number): void;
    }


    /**
     * @gir-type Alias
     */
    type SeekSoundcardClass = typeof SeekSoundcard;

    /**
     * @gir-type Alias
     */
    type SequencerThreadClass = typeof SequencerThread;

    /**
     * @gir-type Struct
     */
    abstract class SequencerUtil {
        static $gtype: GObject.GType<SequencerUtil>;

        // Static methods
        /**
         * Get object mutex of `sequencer`.
         * @param sequencer the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
         */
        static get_obj_mutex(sequencer: GObject.Object): GLib.RecMutex;
    }


    /**
     * @gir-type Alias
     */
    type SetAudioChannelsClass = typeof SetAudioChannels;

    /**
     * @gir-type Alias
     */
    type SetBacklogClass = typeof SetBacklog;

    /**
     * @gir-type Alias
     */
    type SetBufferSizeClass = typeof SetBufferSize;

    /**
     * @gir-type Alias
     */
    type SetDeviceClass = typeof SetDevice;

    /**
     * @gir-type Alias
     */
    type SetFormatClass = typeof SetFormat;

    /**
     * @gir-type Alias
     */
    type SetMutedClass = typeof SetMuted;

    /**
     * @gir-type Alias
     */
    type SetPresetsClass = typeof SetPresets;

    /**
     * @gir-type Alias
     */
    type SetSamplerateClass = typeof SetSamplerate;

    /**
     * @gir-type Alias
     */
    type SndfileClass = typeof Sndfile;

    /**
     * @gir-type Alias
     */
    type SoundContainerInterface = typeof SoundContainer;

    /**
     * @gir-type Alias
     */
    type SoundProviderInterface = typeof SoundProvider;

    /**
     * @gir-type Alias
     */
    type SoundResourceInterface = typeof SoundResource;

    /**
     * @gir-type Alias
     */
    type SoundcardThreadClass = typeof SoundcardThread;

    /**
     * @gir-type Struct
     */
    abstract class SoundcardUtil {
        static $gtype: GObject.GType<SoundcardUtil>;

        // Static methods
        /**
         * Adjust delay and attack of `soundcard`.
         * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
         */
        static adjust_delay_and_attack(soundcard: GObject.Object): void;

        /**
         * @param soundcard 
         * @param note_256th_attack_lower 
         * @param note_256th_attack_upper 
         */
        static calc_next_note_256th_attack(soundcard: GObject.Object, note_256th_attack_lower: number, note_256th_attack_upper: number): void;

        /**
         * Calc next note 256th offset of `soundcard`.
         * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
         */
        static calc_next_note_256th_offset(soundcard: GObject.Object): [number, number];

        /**
         * Offset of `soundcard` to system time.
         * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
         */
        static calc_system_time(soundcard: GObject.Object): number;

        /**
         * Offset of `soundcard` to system time.
         * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
         */
        static calc_time_samples(soundcard: GObject.Object): number;

        /**
         * Offset of `soundcard` to system time.
         * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
         */
        static calc_time_samples_absolute(soundcard: GObject.Object): number;

        /**
         * Get object mutex of `soundcard`.
         * @param soundcard the {@link GObject.Object} sub-type implementing {@link Ags.Soundcard}
         */
        static get_obj_mutex(soundcard: GObject.Object): GLib.RecMutex;
    }


    /**
     * @gir-type Alias
     */
    type StartAudioClass = typeof StartAudio;

    /**
     * @gir-type Alias
     */
    type StartChannelClass = typeof StartChannel;

    /**
     * @gir-type Alias
     */
    type StartSequencerClass = typeof StartSequencer;

    /**
     * @gir-type Alias
     */
    type StartSoundcardClass = typeof StartSoundcard;

    /**
     * @gir-type Alias
     */
    type StopSequencerClass = typeof StopSequencer;

    /**
     * @gir-type Alias
     */
    type StopSoundcardClass = typeof StopSoundcard;

    /**
     * @gir-type Alias
     */
    type StopThreadClass = typeof StopThread;

    /**
     * @gir-type Alias
     */
    type SwitchBufferFlagClass = typeof SwitchBufferFlag;

    /**
     * @gir-type Alias
     */
    type SynthGeneratorClass = typeof SynthGenerator;

    /**
     * @gir-type Struct
     */
    class SynthUtil {
        static $gtype: GObject.GType<SynthUtil>;

        // Fields
        source: any;

        source_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        synth_oscillator_mode: number;

        frequency: number;

        phase: number;

        volume: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.SynthUtil}-struct
         */
        static alloc(): SynthUtil;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param format the format
         * @param buffer_size the buffer size
         */
        static get_xcross_count(buffer: (any | null), format: number, buffer_size: number): number;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param buffer_size the buffer size
         */
        static get_xcross_count_complex(buffer: Ags.Complex, buffer_size: number): number;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param buffer_size the buffer size
         */
        static get_xcross_count_double(buffer: number, buffer_size: number): number;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param buffer_size the buffer size
         */
        static get_xcross_count_float(buffer: number, buffer_size: number): number;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param buffer_size the buffer size
         */
        static get_xcross_count_s16(buffer: number, buffer_size: number): number;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param buffer_size the buffer size
         */
        static get_xcross_count_s24(buffer: number, buffer_size: number): number;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param buffer_size the buffer size
         */
        static get_xcross_count_s32(buffer: number, buffer_size: number): number;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param buffer_size the buffer size
         */
        static get_xcross_count_s64(buffer: (bigint | number), buffer_size: number): number;

        /**
         * Get zero-cross count.
         * @param buffer the buffer containing audio data
         * @param buffer_size the buffer size
         */
        static get_xcross_count_s8(buffer: number, buffer_size: number): number;

        // Methods
        /**
         * Compute impulse synth.
         */
        compute_impulse(): void;

        /**
         * Compute impulse synth of complex data.
         */
        compute_impulse_complex(): void;

        /**
         * Compute impulse synth of double precision floating point data.
         */
        compute_impulse_double(): void;

        /**
         * Compute impulse synth of floating point data.
         */
        compute_impulse_float(): void;

        /**
         * Compute impulse synth of signed 16 bit data.
         */
        compute_impulse_s16(): void;

        /**
         * Compute impulse synth of signed 24 bit data.
         */
        compute_impulse_s24(): void;

        /**
         * Compute impulse synth of signed 32 bit data.
         */
        compute_impulse_s32(): void;

        /**
         * Compute impulse synth of signed 64 bit data.
         */
        compute_impulse_s64(): void;

        /**
         * Compute impulse synth of signed 8 bit data.
         */
        compute_impulse_s8(): void;

        /**
         * Compute sawtooth synth.
         */
        compute_sawtooth(): void;

        /**
         * Compute sawtooth synth of complex data.
         */
        compute_sawtooth_complex(): void;

        /**
         * Compute sawtooth synth of double precision floating point data.
         */
        compute_sawtooth_double(): void;

        /**
         * Compute sawtooth synth of floating point data.
         */
        compute_sawtooth_float(): void;

        /**
         * Compute sawtooth synth of signed 16 bit data.
         */
        compute_sawtooth_s16(): void;

        /**
         * Compute sawtooth synth of signed 24 bit data.
         */
        compute_sawtooth_s24(): void;

        /**
         * Compute sawtooth synth of signed 32 bit data.
         */
        compute_sawtooth_s32(): void;

        /**
         * Compute sawtooth synth of signed 64 bit data.
         */
        compute_sawtooth_s64(): void;

        /**
         * Compute sawtooth synth of signed 8 bit data.
         */
        compute_sawtooth_s8(): void;

        /**
         * Compute sine synth.
         */
        compute_sin(): void;

        /**
         * Compute sine synth of complex data.
         */
        compute_sin_complex(): void;

        /**
         * Compute sine synth of double precision floating point data.
         */
        compute_sin_double(): void;

        /**
         * Compute sine synth of floating point data.
         */
        compute_sin_float(): void;

        /**
         * Compute sine synth of signed 16 bit data.
         */
        compute_sin_s16(): void;

        /**
         * Compute sine synth of signed 24 bit data.
         */
        compute_sin_s24(): void;

        /**
         * Compute sine synth of signed 32 bit data.
         */
        compute_sin_s32(): void;

        /**
         * Compute sine synth of signed 64 bit data.
         */
        compute_sin_s64(): void;

        /**
         * Compute sine synth of signed 8 bit data.
         */
        compute_sin_s8(): void;

        /**
         * Compute square synth.
         */
        compute_square(): void;

        /**
         * Compute square synth of complex data.
         */
        compute_square_complex(): void;

        /**
         * Compute square synth of double precision floating point data.
         */
        compute_square_double(): void;

        /**
         * Compute square synth of floating point data.
         */
        compute_square_float(): void;

        /**
         * Compute square synth of signed 16 bit data.
         */
        compute_square_s16(): void;

        /**
         * Compute square synth of signed 24 bit data.
         */
        compute_square_s24(): void;

        /**
         * Compute square synth of signed 32 bit data.
         */
        compute_square_s32(): void;

        /**
         * Compute square synth of signed 64 bit data.
         */
        compute_square_s64(): void;

        /**
         * Compute square synth of signed 8 bit data.
         */
        compute_square_s8(): void;

        /**
         * Compute triangle synth.
         */
        compute_triangle(): void;

        /**
         * Compute triangle synth of complex data.
         */
        compute_triangle_complex(): void;

        /**
         * Compute triangle synth of double precision floating point data.
         */
        compute_triangle_double(): void;

        /**
         * Compute triangle synth of floating point data.
         */
        compute_triangle_float(): void;

        /**
         * Compute triangle synth of signed 16 bit data.
         */
        compute_triangle_s16(): void;

        /**
         * Compute triangle synth of signed 24 bit data.
         */
        compute_triangle_s24(): void;

        /**
         * Compute triangle synth of signed 32 bit data.
         */
        compute_triangle_s32(): void;

        /**
         * Compute triangle synth of signed 64 bit data.
         */
        compute_triangle_s64(): void;

        /**
         * Compute triangle synth of signed 8 bit data.
         */
        compute_triangle_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.SynthUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `synth_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get audio buffer util format of `synth_util`.
         * @returns the audio buffer util format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `synth_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get frequency of `synth_util`.
         * @returns the frequency
         */
        get_frequency(): number;

        /**
         * Get note 256th mode of `synth_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `synth_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `synth_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get phase of `synth_util`.
         * @returns the phase
         */
        get_phase(): number;

        /**
         * Get samplerate of `synth_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `synth_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `synth_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get synth oscillator mode of `synth_util`.
         * @returns the synth oscillator mode
         */
        get_synth_oscillator_mode(): number;

        /**
         * Get volume of `synth_util`.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Set `buffer_length` of `synth_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `format` of `synth_util`.
         * @param format the audio buffer util format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `synth_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `frequency` of `synth_util`.
         * @param frequency the frequency
         */
        set_frequency(frequency: number): void;

        /**
         * Set `note_256th_mode` of `synth_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `synth_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `synth_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `phase` of `synth_util`.
         * @param phase the phase
         */
        set_phase(phase: number): void;

        /**
         * Set `samplerate` of `synth_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `synth_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `synth_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `synth_oscillator_mode` of `synth_util`.
         * @param synth_oscillator_mode the synth oscillator mode
         */
        set_synth_oscillator_mode(synth_oscillator_mode: number): void;

        /**
         * Set `volume` of `synth_util`.
         * @param volume the volume
         */
        set_volume(volume: number): void;
    }


    /**
     * @gir-type Alias
     */
    type TicDeviceClass = typeof TicDevice;

    /**
     * @gir-type Struct
     */
    class TimeStretchUtil {
        static $gtype: GObject.GType<TimeStretchUtil>;

        // Fields
        source: any;

        source_stride: number;

        source_buffer_length: number;

        destination: any;

        destination_stride: number;

        destination_buffer_length: number;

        buffer_size: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        frequency: number;

        orig_bpm: number;

        new_bpm: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.TimeStretchUtil}-struct.
         */
        static alloc(): TimeStretchUtil;

        // Methods
        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.TimeStretchUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer size of `time_stretch_util`.
         * @returns the buffer size
         */
        get_buffer_size(): number;

        /**
         * Get destination buffer of `time_stretch_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination buffer length of `time_stretch_util`.
         * @returns the destination buffer length
         */
        get_destination_buffer_length(): number;

        /**
         * Get destination stride of `time_stretch_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `time_stretch_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frequency of `time_stretch_util`.
         * @returns the frequency
         */
        get_frequency(): number;

        /**
         * Get new BPM of `time_stretch_util`.
         * @returns the new BPM
         */
        get_new_bpm(): number;

        /**
         * Get original BPM of `time_stretch_util`.
         * @returns the original BPM
         */
        get_orig_bpm(): number;

        /**
         * Get samplerate of `time_stretch_util`.
         * @returns the samplerate
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `time_stretch_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source buffer length of `time_stretch_util`.
         * @returns the source buffer length
         */
        get_source_buffer_length(): number;

        /**
         * Get source stride of `time_stretch_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Set `buffer_size` of `time_stretch_util`.
         * @param buffer_size the buffer size
         */
        set_buffer_size(buffer_size: number): void;

        /**
         * Set `destination` buffer of `time_stretch_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination_buffer_length` of `time_stretch_util`.
         * @param destination_buffer_length the buffer length
         */
        set_destination_buffer_length(destination_buffer_length: number): void;

        /**
         * Set `destination` stride of `time_stretch_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `time_stretch_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frequency` of `time_stretch_util`.
         * @param frequency the frequency
         */
        set_frequency(frequency: number): void;

        /**
         * Set `new_bpm` of `time_stretch_util`.
         * @param new_bpm the new_bpm
         */
        set_new_bpm(new_bpm: number): void;

        /**
         * Set `orig_bpm` of `time_stretch_util`.
         * @param orig_bpm the original BPM
         */
        set_orig_bpm(orig_bpm: number): void;

        /**
         * Set `samplerate` of `time_stretch_util`.
         * @param samplerate the samplerate
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `time_stretch_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source_buffer_length` of `time_stretch_util`.
         * @param source_buffer_length the buffer length
         */
        set_source_buffer_length(source_buffer_length: number): void;

        /**
         * Set `source` stride of `time_stretch_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Stretch `time_stretch_util`.
         */
        stretch(): void;

        /**
         * Stretch `time_stretch_util` of complex data.
         */
        stretch_complex(): void;

        /**
         * Stretch `time_stretch_util` of double precision floating point data.
         */
        stretch_double(): void;

        /**
         * Stretch `time_stretch_util` of floating point data.
         */
        stretch_float(): void;

        /**
         * Stretch `time_stretch_util` of signed 16 bit data.
         */
        stretch_s16(): void;

        /**
         * Stretch `time_stretch_util` of signed 24 bit data.
         */
        stretch_s24(): void;

        /**
         * Stretch `time_stretch_util` of signed 32 bit data.
         */
        stretch_s32(): void;

        /**
         * Stretch `time_stretch_util` of signed 64 bit data.
         */
        stretch_s64(): void;

        /**
         * Stretch `time_stretch_util` of signed 8 bit data.
         */
        stretch_s8(): void;
    }


    /**
     * @gir-type Alias
     */
    type TogglePatternBitClass = typeof TogglePatternBit;

    /**
     * @gir-type Alias
     */
    type TrackClass = typeof Track;

    /**
     * @gir-type Struct
     */
    class TremoloUtil {
        static $gtype: GObject.GType<TremoloUtil>;

        // Fields
        destination: any;

        destination_stride: number;

        source: any;

        source_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        samplerate: number;

        tremolo_gain: number;

        tremolo_lfo_depth: number;

        tremolo_lfo_freq: number;

        tremolo_tuning: number;

        tremolo_lfo_frame_count: number;

        tremolo_lfo_offset: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.TremoloUtil}-struct
         */
        static alloc(): TremoloUtil;

        // Methods
        /**
         * Compute tremolo.
         */
        compute(): void;

        /**
         * Compute tremolo of complex floating point data.
         */
        compute_complex(): void;

        /**
         * Compute tremolo of double floating point data.
         */
        compute_double(): void;

        /**
         * Compute tremolo of floating point data.
         */
        compute_float(): void;

        /**
         * Compute tremolo of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute tremolo of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute tremolo of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute tremolo of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute tremolo of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.TremoloUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `tremolo_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `tremolo_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `tremolo_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `tremolo_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get frame count of `tremolo_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get note 256th mode of `tremolo_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `tremolo_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `tremolo_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get buffer length of `tremolo_util`.
         * @returns the buffer length
         */
        get_samplerate(): number;

        /**
         * Get source buffer of `tremolo_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `tremolo_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get tremolo gain of `tremolo_util`.
         * @returns the tremolo gain
         */
        get_tremolo_gain(): number;

        /**
         * Get tremolo LFO depth of `tremolo_util`.
         * @returns the tremolo LFO depth
         */
        get_tremolo_lfo_depth(): number;

        /**
         * Get tremolo LFO freq of `tremolo_util`.
         * @returns the tremolo LFO freq
         */
        get_tremolo_lfo_freq(): number;

        /**
         * Get tremolo LFO offset of `tremolo_util`.
         * @returns the tremolo LFO offset
         */
        get_tremolo_lfo_offset(): number;

        /**
         * Get tremolo tuning of `tremolo_util`.
         * @returns the tremolo tuning
         */
        get_tremolo_tuning(): number;

        /**
         * Set `buffer_length` of `tremolo_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `tremolo_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `tremolo_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `tremolo_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `frame_count` of `tremolo_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `note_256th_mode` of `tremolo_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `tremolo_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `tremolo_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `samplerate` of `tremolo_util`.
         * @param samplerate the buffer length
         */
        set_samplerate(samplerate: number): void;

        /**
         * Set `source` buffer of `tremolo_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `tremolo_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `tremolo_gain` of `tremolo_util`.
         * @param tremolo_gain the tremolo gain
         */
        set_tremolo_gain(tremolo_gain: number): void;

        /**
         * Set `tremolo_lfo_depth` of `tremolo_util`.
         * @param tremolo_lfo_depth the tremolo LFO depth
         */
        set_tremolo_lfo_depth(tremolo_lfo_depth: number): void;

        /**
         * Set `tremolo_lfo_freq` of `tremolo_util`.
         * @param tremolo_lfo_freq the tremolo LFO freq
         */
        set_tremolo_lfo_freq(tremolo_lfo_freq: number): void;

        /**
         * Set `tremolo_lfo_offset` of `tremolo_util`.
         * @param tremolo_lfo_offset the tremolo LFO offset
         */
        set_tremolo_lfo_offset(tremolo_lfo_offset: number): void;

        /**
         * Set `tremolo_tuning` of `tremolo_util`.
         * @param tremolo_tuning the tremolo tuning
         */
        set_tremolo_tuning(tremolo_tuning: number): void;
    }


    /**
     * @gir-type Struct
     */
    class VolumeUtil {
        static $gtype: GObject.GType<VolumeUtil>;

        // Fields
        destination: any;

        destination_stride: number;

        source: any;

        source_stride: number;

        buffer_length: number;

        format: Ags.SoundcardFormat;

        volume: number;

        // Static methods
        /**
         * Allocate {@link AgsAudio.VolumeUtil}-struct
         */
        static alloc(): VolumeUtil;

        // Methods
        /**
         * Compute volume.
         */
        compute(): void;

        /**
         * Compute volume of complex floating point data.
         */
        compute_complex(): void;

        /**
         * Compute volume of double floating point data.
         */
        compute_double(): void;

        /**
         * Compute volume of floating point data.
         */
        compute_float(): void;

        /**
         * Compute volume of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute volume of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute volume of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute volume of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute volume of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.VolumeUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get buffer length of `volume_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `volume_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `volume_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `volume_util`.
         * @returns the format
         */
        get_format(): Ags.SoundcardFormat;

        /**
         * Get source buffer of `volume_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `volume_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get volume of `volume_util`.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Set `buffer_length` of `volume_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `volume_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `volume_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `volume_util`.
         * @param format the format
         */
        set_format(format: Ags.SoundcardFormat): void;

        /**
         * Set `source` buffer of `volume_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `volume_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `volume` of `volume_util`.
         * @param volume the volume
         */
        set_volume(volume: number): void;
    }


    /**
     * @gir-type Struct
     */
    class WahWahUtil {
        static $gtype: GObject.GType<WahWahUtil>;

        // Fields
        destination: any;

        destination_stride: number;

        source: any;

        source_stride: number;

        buffer_length: number;

        format: number;

        samplerate: number;

        volume: number;

        amount: number;

        wah_wah_enabled: boolean;

        wah_wah_delay: number;

        wah_wah_lfo_depth: number;

        wah_wah_lfo_freq: number;

        wah_wah_tuning: number;

        wah_wah_lfo_frame_count: number;

        wah_wah_lfo_offset: number;

        frame_count: number;

        offset: number;

        note_256th_mode: boolean;

        offset_256th: number;

        // Constructors

        constructor(properties?: Partial<{
            destination: any;
            destination_stride: number;
            source: any;
            source_stride: number;
            buffer_length: number;
            format: number;
            samplerate: number;
            volume: number;
            amount: number;
            wah_wah_enabled: boolean;
            wah_wah_delay: number;
            wah_wah_lfo_depth: number;
            wah_wah_lfo_freq: number;
            wah_wah_tuning: number;
            wah_wah_lfo_frame_count: number;
            wah_wah_lfo_offset: number;
            frame_count: number;
            offset: number;
            note_256th_mode: boolean;
            offset_256th: number;
        }>);

        // Static methods
        /**
         * Allocate {@link AgsAudio.WahWahUtil}-struct
         */
        static alloc(): WahWahUtil;

        // Methods
        /**
         * Compute wah_wah.
         */
        compute(): void;

        /**
         * Compute wah_wah of complex floating point data.
         */
        compute_complex(): void;

        /**
         * Compute wah_wah of double floating point data.
         */
        compute_double(): void;

        /**
         * Compute wah_wah of floating point data.
         */
        compute_float(): void;

        /**
         * Compute wah_wah of signed 16 bit data.
         */
        compute_s16(): void;

        /**
         * Compute wah_wah of signed 24 bit data.
         */
        compute_s24(): void;

        /**
         * Compute wah_wah of signed 32 bit data.
         */
        compute_s32(): void;

        /**
         * Compute wah_wah of signed 64 bit data.
         */
        compute_s64(): void;

        /**
         * Compute wah_wah of signed 8 bit data.
         */
        compute_s8(): void;

        /**
         * Create a copy of `ptr`.
         * @returns a pointer of the new {@link AgsAudio.WahWahUtil}-struct
         */
        copy(): (any | null);

        /**
         * Free the memory of `ptr`.
         */
        free(): void;

        /**
         * Get amount of `wah_wah_util`.
         * @returns the amount
         */
        get_amount(): number;

        /**
         * Get buffer length of `wah_wah_util`.
         * @returns the buffer length
         */
        get_buffer_length(): number;

        /**
         * Get destination buffer of `wah_wah_util`.
         * @returns the destination buffer
         */
        get_destination(): (any | null);

        /**
         * Get destination stride of `wah_wah_util`.
         * @returns the destination buffer stride
         */
        get_destination_stride(): number;

        /**
         * Get format of `wah_wah_util`.
         * @returns the format
         */
        get_format(): number;

        /**
         * Get frame count of `wah_wah_util`.
         * @returns the frame count
         */
        get_frame_count(): number;

        /**
         * Get note 256th mode of `wah_wah_util`.
         * @returns `true` if note 256th mode, otherwise `false`
         */
        get_note_256th_mode(): boolean;

        /**
         * Get offset of `wah_wah_util`.
         * @returns the offset
         */
        get_offset(): number;

        /**
         * Get offset as note 256th of `wah_wah_util`.
         * @returns the offset as note 256th
         */
        get_offset_256th(): number;

        /**
         * Get source buffer of `wah_wah_util`.
         * @returns the source buffer
         */
        get_source(): (any | null);

        /**
         * Get source stride of `wah_wah_util`.
         * @returns the source buffer stride
         */
        get_source_stride(): number;

        /**
         * Get volume of `wah_wah_util`.
         * @returns the volume
         */
        get_volume(): number;

        /**
         * Get wah-wah LFO depth of `wah_wah_util`.
         * @returns the wah-wah LFO depth
         */
        get_wah_wah_lfo_depth(): number;

        /**
         * Get wah-wah LFO freq of `wah_wah_util`.
         * @returns the wah-wah LFO freq
         */
        get_wah_wah_lfo_freq(): number;

        /**
         * Get wah-wah LFO offset of `wah_wah_util`.
         * @returns the wah-wah LFO offset
         */
        get_wah_wah_lfo_offset(): number;

        /**
         * Get wah-wah tuning of `wah_wah_util`.
         * @returns the wah-wah tuning
         */
        get_wah_wah_tuning(): number;

        /**
         * Set `amount` of `wah_wah_util`.
         * @param amount the amount
         */
        set_amount(amount: number): void;

        /**
         * Set `buffer_length` of `wah_wah_util`.
         * @param buffer_length the buffer length
         */
        set_buffer_length(buffer_length: number): void;

        /**
         * Set `destination` buffer of `wah_wah_util`.
         * @param destination the destination buffer
         */
        set_destination(destination: (any | null)): void;

        /**
         * Set `destination` stride of `wah_wah_util`.
         * @param destination_stride the destination buffer stride
         */
        set_destination_stride(destination_stride: number): void;

        /**
         * Set `format` of `wah_wah_util`.
         * @param format the format
         */
        set_format(format: number): void;

        /**
         * Set `frame_count` of `wah_wah_util`.
         * @param frame_count the frame count
         */
        set_frame_count(frame_count: number): void;

        /**
         * Set `note_256th_mode` of `wah_wah_util`.
         * @param note_256th_mode the note 256th mode
         */
        set_note_256th_mode(note_256th_mode: boolean): void;

        /**
         * Set `offset` of `wah_wah_util`.
         * @param offset the offset
         */
        set_offset(offset: number): void;

        /**
         * Set `offset_256th` of `wah_wah_util`.
         * @param offset_256th the offset as note 256th
         */
        set_offset_256th(offset_256th: number): void;

        /**
         * Set `source` buffer of `wah_wah_util`.
         * @param source the source buffer
         */
        set_source(source: (any | null)): void;

        /**
         * Set `source` stride of `wah_wah_util`.
         * @param source_stride the source buffer stride
         */
        set_source_stride(source_stride: number): void;

        /**
         * Set `volume` of `wah_wah_util`.
         * @param volume the volume
         */
        set_volume(volume: number): void;

        /**
         * Set `wah_wah_lfo_depth` of `wah_wah_util`.
         * @param wah_wah_lfo_depth the wah-wah LFO depth
         */
        set_wah_wah_lfo_depth(wah_wah_lfo_depth: number): void;

        /**
         * Set `wah_wah_lfo_freq` of `wah_wah_util`.
         * @param wah_wah_lfo_freq the wah-wah LFO freq
         */
        set_wah_wah_lfo_freq(wah_wah_lfo_freq: number): void;

        /**
         * Set `wah_wah_lfo_offset` of `wah_wah_util`.
         * @param wah_wah_lfo_offset the wah-wah LFO offset
         */
        set_wah_wah_lfo_offset(wah_wah_lfo_offset: number): void;

        /**
         * Set `wah_wah_tuning` of `wah_wah_util`.
         * @param wah_wah_tuning the wah-wah tuning
         */
        set_wah_wah_tuning(wah_wah_tuning: number): void;
    }


    /**
     * @gir-type Alias
     */
    type WasapiDevinClass = typeof WasapiDevin;

    /**
     * @gir-type Alias
     */
    type WasapiDevoutClass = typeof WasapiDevout;

    /**
     * @gir-type Alias
     */
    type WaveClass = typeof Wave;

    /**
     * @gir-type Alias
     */
    type WaveLoaderClass = typeof WaveLoader;

    /**
     * @gir-type Struct
     */
    abstract class _PortValue {
        static $gtype: GObject.GType<_PortValue>;

        // Fields
        ags_port_boolean: boolean;

        ags_port_int: number;

        ags_port_uint: number;

        ags_port_float: number;

        ags_port_double: number;

        ags_port_complex: Ags.Complex;

        ags_port_string: string;

        ags_port_boolean_ptr: boolean;

        ags_port_int_ptr: number;

        ags_port_uint_ptr: number;

        ags_port_float_ptr: number;

        ags_port_double_ptr: number;

        ags_port_complex_ptr: Ags.Complex;

        ags_port_pointer: any;

        ags_port_object: GObject.Object;
    }


    namespace OscPluginController {
        /**
         * Interface for implementing OscPluginController.
         * Contains only the virtual methods that need to be implemented.
         */
        interface Interface {

            // Virtual methods
            /**
             * Do request.
             * @param osc_connection the {@link AgsAudio.OscConnection}
             * @param message the OSC message
             * @param message_size the message size
             * @virtual
             */
            vfunc_do_request(osc_connection: OscConnection, message: number, message_size: number): (any | null);
        }


        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    export interface OscPluginControllerNamespace {
        $gtype: GObject.GType<OscPluginController>;
        prototype: OscPluginController;
    }
    /**
     * @gir-type Interface
     */
    interface OscPluginController extends GObject.Object, OscPluginController.Interface {

        // Methods
        /**
         * Do request.
         * @param osc_connection the {@link AgsAudio.OscConnection}
         * @param message the OSC message
         * @param message_size the message size
         * @returns the {@link GLib.List}-struct containing {@link AgsAudio.OscResponse}
         */
        do_request(osc_connection: OscConnection, message: number, message_size: number): (any | null);
    }


    export const OscPluginController: OscPluginControllerNamespace & {
        new (): OscPluginController; // This allows `obj instanceof OscPluginController`
    };

    namespace SoundContainer {
        /**
         * Interface for implementing SoundContainer.
         * Contains only the virtual methods that need to be implemented.
         */
        interface Interface {

            // Virtual methods
            /**
             * Close `sound_container`.
             * @virtual
             */
            vfunc_close(): void;

            /**
             * Get level count.
             * @virtual
             */
            vfunc_get_level_count(): number;

            /**
             * Get level id.
             * @virtual
             */
            vfunc_get_level_id(): string;

            /**
             * Get level index.
             * @virtual
             */
            vfunc_get_level_index(): number;

            /**
             * Get nesting level.
             * @virtual
             */
            vfunc_get_nesting_level(): number;

            /**
             * Get all resources as a {@link GLib.List}-struct.
             * @virtual
             */
            vfunc_get_resource_all(): GObject.Object[];

            /**
             * Get resources by index as a {@link GLib.List}-struct.
             * @param resource_index the index
             * @virtual
             */
            vfunc_get_resource_by_index(resource_index: number): GObject.Object[];

            /**
             * Get resources by name as a {@link GLib.List}-struct.
             * @param resource_name the resource name
             * @virtual
             */
            vfunc_get_resource_by_name(resource_name: string): GObject.Object[];

            /**
             * Get resources by index as a {@link GLib.List}-struct.
             * @virtual
             */
            vfunc_get_resource_current(): GObject.Object[];

            /**
             * Get sublevel names.
             * @virtual
             */
            vfunc_get_sublevel_name(): string[];

            /**
             * Level up.
             * @param level_count the count of levels to go up
             * @virtual
             */
            vfunc_level_up(level_count: number): number;

            /**
             * Open `sound_container`.
             * @param filename the filename
             * @virtual
             */
            vfunc_open(filename: string): boolean;

            /**
             * Select level by id.
             * @param level_id the level id as string
             * @virtual
             */
            vfunc_select_level_by_id(level_id: string): number;

            /**
             * Select level by index.
             * @param level_index the level index unsigned integer
             * @virtual
             */
            vfunc_select_level_by_index(level_index: number): number;
        }


        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    export interface SoundContainerNamespace {
        $gtype: GObject.GType<SoundContainer>;
        prototype: SoundContainer;
    }
    /**
     * @gir-type Interface
     */
    interface SoundContainer extends GObject.Object, SoundContainer.Interface {

        // Methods
        /**
         * Close `sound_container`.
         */
        close(): void;

        /**
         * Get level count.
         * @returns the level count
         */
        get_level_count(): number;

        /**
         * Get level id.
         * @returns the level id as string
         */
        get_level_id(): string;

        /**
         * Get level index.
         * @returns the level index unsigned integer
         */
        get_level_index(): number;

        /**
         * Get nesting level.
         * @returns the nesting level
         */
        get_nesting_level(): number;

        /**
         * Get all resources as a {@link GLib.List}-struct.
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_all(): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @param resource_index the index
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_by_index(resource_index: number): GObject.Object[];

        /**
         * Get resources by name as a {@link GLib.List}-struct.
         * @param resource_name the resource name
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_by_name(resource_name: string): GObject.Object[];

        /**
         * Get resources by index as a {@link GLib.List}-struct.
         * @returns the {@link GLib.List}-struct containing `AgsResource`
         */
        get_resource_current(): GObject.Object[];

        /**
         * Get sublevel names.
         * @returns the string vector containing sublevel names
         */
        get_sublevel_name(): string[];

        /**
         * Level up.
         * @param level_count the count of levels to go up
         * @returns the level count went up
         */
        level_up(level_count: number): number;

        /**
         * Open `sound_container`.
         * @param filename the filename
         * @returns `true` if open succeeded, else `false`
         */
        open(filename: string): boolean;

        /**
         * Select level by id.
         * @param level_id the level id as string
         * @returns the index of level as unsigned integer
         */
        select_level_by_id(level_id: string): number;

        /**
         * Select level by index.
         * @param level_index the level index unsigned integer
         * @returns the index of level as unsigned integer
         */
        select_level_by_index(level_index: number): number;
    }


    export const SoundContainer: SoundContainerNamespace & {
        new (): SoundContainer; // This allows `obj instanceof SoundContainer`
    };

    namespace SoundProvider {
        /**
         * Interface for implementing SoundProvider.
         * Contains only the virtual methods that need to be implemented.
         */
        interface Interface {

            // Virtual methods
            /**
             * Get audio objects.
             * @virtual
             */
            vfunc_get_audio(): Audio[];

            /**
             * Get default soundcard thread.
             * @virtual
             */
            vfunc_get_default_soundcard<T = GObject.Object>(): T;

            /**
             * Get default soundcard thread.
             * @virtual
             */
            vfunc_get_default_soundcard_thread<T = GObject.Object>(): T;

            /**
             * Set distributed manager.
             * @virtual
             */
            vfunc_get_osc_server(): GObject.Object[];

            /**
             * Get sequencers.
             * @virtual
             */
            vfunc_get_sequencer(): GObject.Object[];

            /**
             * Set sound server.
             * @virtual
             */
            vfunc_get_sound_server(): GObject.Object[];

            /**
             * Get soundcards.
             * @virtual
             */
            vfunc_get_soundcard(): GObject.Object[];

            /**
             * Set audio objects.
             * @param audio a {@link GLib.List}-struct containing {@link AgsAudio.Audio}
             * @virtual
             */
            vfunc_set_audio(audio: Audio[]): void;

            /**
             * Set default soundcard.
             * @param soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
             * @virtual
             */
            vfunc_set_default_soundcard(soundcard: GObject.Object): void;

            /**
             * Set default soundcard thread.
             * @param soundcard_thread the default {@link Ags.Thread}
             * @virtual
             */
            vfunc_set_default_soundcard_thread(soundcard_thread: GObject.Object): void;

            /**
             * Set OSC server.
             * @param osc_server a {@link GLib.List}-struct containing {@link AgsAudio.OscServer}
             * @virtual
             */
            vfunc_set_osc_server(osc_server: GObject.Object[]): void;

            /**
             * Set sequencers.
             * @param sequencer a {@link GLib.List}-struct containing {@link Ags.Sequencer}
             * @virtual
             */
            vfunc_set_sequencer(sequencer: GObject.Object[]): void;

            /**
             * Get sound server.
             * @param sound_server a {@link GLib.List}-struct containing {@link Ags.SoundServer}
             * @virtual
             */
            vfunc_set_sound_server(sound_server: GObject.Object[]): void;

            /**
             * Set soundcards.
             * @param soundcard a {@link GLib.List}-struct containing {@link Ags.Soundcard}
             * @virtual
             */
            vfunc_set_soundcard(soundcard: GObject.Object[]): void;
        }


        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    export interface SoundProviderNamespace {
        $gtype: GObject.GType<SoundProvider>;
        prototype: SoundProvider;
    }
    /**
     * @gir-type Interface
     */
    interface SoundProvider extends GObject.Object, SoundProvider.Interface {

        // Methods
        /**
         * Get audio objects.
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.Audio}
         */
        get_audio(): Audio[];

        /**
         * Get default soundcard thread.
         * @returns the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        get_default_soundcard<T = GObject.Object>(): T;

        /**
         * Get default soundcard thread.
         * @returns the {@link Ags.Thread}
         */
        get_default_soundcard_thread<T = GObject.Object>(): T;

        /**
         * Set distributed manager.
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.OscServer}
         */
        get_osc_server(): GObject.Object[];

        /**
         * Get sequencers.
         * @returns a {@link GLib.List}-struct containing {@link Ags.Sequencer}
         */
        get_sequencer(): GObject.Object[];

        /**
         * Set sound server.
         * @returns a {@link GLib.List}-struct containing {@link Ags.SoundServer}
         */
        get_sound_server(): GObject.Object[];

        /**
         * Get soundcards.
         * @returns a {@link GLib.List}-struct containing {@link Ags.Soundcard}
         */
        get_soundcard(): GObject.Object[];

        /**
         * Set audio objects.
         * @param audio a {@link GLib.List}-struct containing {@link AgsAudio.Audio}
         */
        set_audio(audio: Audio[]): void;

        /**
         * Set default soundcard.
         * @param soundcard the {@link GObject.Object} implementing {@link Ags.Soundcard}
         */
        set_default_soundcard(soundcard: GObject.Object): void;

        /**
         * Set default soundcard thread.
         * @param soundcard_thread the default {@link Ags.Thread}
         */
        set_default_soundcard_thread(soundcard_thread: GObject.Object): void;

        /**
         * Set OSC server.
         * @param osc_server a {@link GLib.List}-struct containing {@link AgsAudio.OscServer}
         */
        set_osc_server(osc_server: GObject.Object[]): void;

        /**
         * Set sequencers.
         * @param sequencer a {@link GLib.List}-struct containing {@link Ags.Sequencer}
         */
        set_sequencer(sequencer: GObject.Object[]): void;

        /**
         * Get sound server.
         * @param sound_server a {@link GLib.List}-struct containing {@link Ags.SoundServer}
         */
        set_sound_server(sound_server: GObject.Object[]): void;

        /**
         * Set soundcards.
         * @param soundcard a {@link GLib.List}-struct containing {@link Ags.Soundcard}
         */
        set_soundcard(soundcard: GObject.Object[]): void;
    }


    export const SoundProvider: SoundProviderNamespace & {
        new (): SoundProvider; // This allows `obj instanceof SoundProvider`
    };

    namespace SoundResource {
        /**
         * Interface for implementing SoundResource.
         * Contains only the virtual methods that need to be implemented.
         */
        interface Interface {

            // Virtual methods
            /**
             * Close `sound_resource`.
             * @virtual
             */
            vfunc_close(): void;

            /**
             * Flush `sound_resource`.
             * @virtual
             */
            vfunc_flush(): void;

            /**
             * Get presets of `sound_resource`.
             * @param channels return location of channels
             * @param samplerate return location of samplerate
             * @param buffer_size return location of buffer-size
             * @param format return location of format
             * @virtual
             */
            vfunc_get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

            /**
             * Get information about `sound_resource`.
             * @param frame_count return location of frame count
             * @param loop_start return location of loop-start
             * @param loop_end return location of loop-end
             * @virtual
             */
            vfunc_info(frame_count: number, loop_start: number, loop_end: number): void;

            /**
             * Load audio data of `sound_resource`.
             * @virtual
             */
            vfunc_load(): void;

            /**
             * Open `sound_resource` for reading and assign filename.
             * @param filename the filename as string
             * @virtual
             */
            vfunc_open(filename: string): boolean;

            /**
             * Read `frame_count` number of frames from `sound_resource` and copy the data
             * to `dbuffer` using `format` by skipping `daudio_channels`.
             * @param dbuffer the destination buffer
             * @param daudio_channels destination buffer audio channel count
             * @param audio_channel the audio channel to read
             * @param frame_count the frame count to read
             * @param format the format to read
             * @virtual
             */
            vfunc_read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

            /**
             * Open `sound_resource` for reading/writing and assign filename. Setting
             * `create` to `true` causes to create the file if it doesn't exist.
             * @param filename the filename as string
             * @param audio_channels the audio channels count
             * @param samplerate the samplerate
             * @param create if `true` create file, otherwise don't
             * @virtual
             */
            vfunc_rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

            /**
             * Seek the `sound_resource` `frame_count` from `whence`.
             * @param frame_count the frame count
             * @param whence SEEK_SET, SEEK_CUR or SEEK_END
             * @virtual
             */
            vfunc_seek(frame_count: number, whence: number): void;

            /**
             * Set presets of `sound_resource`.
             * @param channels channels to set
             * @param samplerate samplerate to set
             * @param buffer_size buffer-size to set
             * @param format format to set
             * @virtual
             */
            vfunc_set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

            /**
             * Write `sbuffer` to `sound_resource` `frame_count` number of
             * frames having `format` by skipping `saudio_channels`.
             * @param sbuffer the source buffer
             * @param saudio_channels source buffer audio channel count
             * @param audio_channel the audio channel
             * @param frame_count the frame count to write
             * @param format the format to write
             * @virtual
             */
            vfunc_write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;
        }


        // Constructor properties interface
        interface ConstructorProps extends GObject.Object.ConstructorProps {

        }
    }

    export interface SoundResourceNamespace {
        $gtype: GObject.GType<SoundResource>;
        prototype: SoundResource;
    }
    /**
     * @gir-type Interface
     */
    interface SoundResource extends GObject.Object, SoundResource.Interface {

        // Methods
        /**
         * Close `sound_resource`.
         */
        close(): void;

        /**
         * Flush `sound_resource`.
         */
        flush(): void;

        /**
         * Get presets of `sound_resource`.
         * @param channels return location of channels
         * @param samplerate return location of samplerate
         * @param buffer_size return location of buffer-size
         * @param format return location of format
         */
        get_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Get information about `sound_resource`.
         * @param frame_count return location of frame count
         * @param loop_start return location of loop-start
         * @param loop_end return location of loop-end
         */
        info(frame_count: number, loop_start: number, loop_end: number): void;

        /**
         * Load audio data of `sound_resource`.
         */
        load(): void;

        /**
         * Open `sound_resource` for reading and assign filename.
         * @param filename the filename as string
         * @returns `true` if operation was successful, otherwise `false`.
         */
        open(filename: string): boolean;

        /**
         * Read `frame_count` number of frames from `sound_resource` and copy the data
         * to `dbuffer` using `format` by skipping `daudio_channels`.
         * @param dbuffer the destination buffer
         * @param daudio_channels destination buffer audio channel count
         * @param audio_channel the audio channel to read
         * @param frame_count the frame count to read
         * @param format the format to read
         * @returns the count of frames actually read
         */
        read(dbuffer: (any | null), daudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): number;

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read audio signal from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.AudioSignal}
         */
        read_audio_signal_at_once(soundcard: GObject.Object, audio_channel: number): AudioSignal[];

        /**
         * Read wave from `sound_resource`.
         * @param soundcard the {@link Ags.Soundcard}
         * @param audio_channel the audio channel or -1 for all
         * @param x_offset the x offset
         * @param delay the delay
         * @param attack the attack
         * @returns a {@link GLib.List}-struct containing {@link AgsAudio.Wave}
         */
        read_wave(soundcard: GObject.Object, audio_channel: number, x_offset: (bigint | number), delay: number, attack: number): Wave[];

        /**
         * Open `sound_resource` for reading/writing and assign filename. Setting
         * `create` to `true` causes to create the file if it doesn't exist.
         * @param filename the filename as string
         * @param audio_channels the audio channels count
         * @param samplerate the samplerate
         * @param create if `true` create file, otherwise don't
         * @returns `true` if operation was successful, otherwise `false`.
         */
        rw_open(filename: string, audio_channels: number, samplerate: number, create: boolean): boolean;

        /**
         * Seek the `sound_resource` `frame_count` from `whence`.
         * @param frame_count the frame count
         * @param whence SEEK_SET, SEEK_CUR or SEEK_END
         */
        seek(frame_count: (bigint | number), whence: number): void;

        /**
         * Set presets of `sound_resource`.
         * @param channels channels to set
         * @param samplerate samplerate to set
         * @param buffer_size buffer-size to set
         * @param format format to set
         */
        set_presets(channels: number, samplerate: number, buffer_size: number, format: Ags.SoundcardFormat): void;

        /**
         * Write `sbuffer` to `sound_resource` `frame_count` number of
         * frames having `format` by skipping `saudio_channels`.
         * @param sbuffer the source buffer
         * @param saudio_channels source buffer audio channel count
         * @param audio_channel the audio channel
         * @param frame_count the frame count to write
         * @param format the format to write
         */
        write(sbuffer: (any | null), saudio_channels: number, audio_channel: number, frame_count: number, format: Ags.SoundcardFormat): void;
    }


    export const SoundResource: SoundResourceNamespace & {
        new (): SoundResource; // This allows `obj instanceof SoundResource`
    };

    /**
     * @gir-type Alias
     */
    type MUID = number;

    /**
     * @gir-type Alias
     */
    type UmpWord = number;

    /**
     * Name of the imported GIR library
     * `see` https://gitlab.gnome.org/GNOME/gjs/-/blob/master/gi/ns.cpp#L188
     */
    const __name__: string;

    /**
     * Version of the imported GIR library
     * `see` https://gitlab.gnome.org/GNOME/gjs/-/blob/master/gi/ns.cpp#L189
     */
    const __version__: string;
}

export default AgsAudio;

// END
