{
    "name": "girs",
    "private": true,
    "module": "module",
    "scripts": {
        "publish": "yarn install && yarn workspaces foreach -v npm publish --tolerate-republish --access public",
        "publish:latest": "yarn install && yarn workspaces foreach -v npm publish --tolerate-republish --tag latest --access public",
        "publish:next": "yarn install && yarn workspaces foreach -v npm publish --tolerate-republish --tag next --access public"
    },
    "workspaces": [
        "./*"
    ],
    "packageManager": "yarn@3.5.0"
}
